---
phase: 51.1-grant-audit-atomicity-cr-01
plan: 02
subsystem: audit-integrity-verification
tags: [byte-comparison, invariants, regression, evidence]
requires:
  - phase: 51.1-01
    provides: Atomic github grant/audit repair and RED-to-GREEN oracle
provides:
  - Mechanical proof that protected audit and capability-reader bodies are unchanged
  - Scoped host-safe regression evidence and explicit deferred-test disclosure
affects: [51.1-03, CR-01]
tech-stack:
  added: []
  patterns: [pre-fix byte comparison, honest environment-scoped verification]
key-files:
  created:
    - .planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-02-SUMMARY.md
  modified: []
key-decisions:
  - "Use 9322e55fe2736c85575f78e95dd6f2c459a94d69 as the pre-fix reference, matching Plan 01."
  - "Classify replay_cas as Mailpit-backed and defer it to the shared EC2/Mailpit run; executing it bare contradicts both its source contract and this plan's no-Mailpit rule."
requirements-completed: []
coverage:
  - id: D1
    description: "Protected verifier/MAC functions and has_github_grant are byte-identical to the pre-fix reference."
    requirement: CR-01
    verification:
      - kind: other
        ref: "Plan 51.1-02 Gates A/A2 against 9322e55fe2736c85575f78e95dd6f2c459a94d69"
        status: pass
    human_judgment: false
  - id: D2
    description: "Scoped host-safe regression families pass without source changes."
    requirement: CR-01
    verification:
      - kind: integration
        ref: "brokerd audit/grant/chain/DAG/anchor/provenance/session-integrity and caprun grant commands recorded below"
        status: pass
    human_judgment: false
completed: 2026-08-10
status: complete
---

# Phase 51.1 Plan 02: Mechanical Non-Regression Evidence Summary

**The CR-01 repair preserves the verifier, capability reader, provenance graph, append choke point, dependency surface, and host-safe regression families.**

## Gate A/A2/B/C output (verbatim)

```text
PREFIX REFERENCE: 9322e55fe2736c85575f78e95dd6f2c459a94d69
Gate 1: checking for raw effect-to-sink type in crates/ ...
  PASS — no raw effect-to-sink type found in crates/
Gate 2: checking runtime-core purity ...
  PASS — runtime-core is pure (no I/O, no async, no network)
Gate 3: checking mint-call-site restriction (mint_from_read / mint_from_derivation / mint_from_exec / mint_from_http / .mint()) ...
  PASS — mint_from_read / mint_from_derivation / mint_from_exec / mint_from_http / .mint() restricted to sanctioned loci
Gate 4: checking test-fixtures is never a default feature of crates/brokerd ...
  PASS — test-fixtures is not a default feature of crates/brokerd
Gate 4b: checking mock-egress-ca is never a default feature anywhere in the workspace ...
  PASS — mock-egress-ca is not a default feature of any workspace member
Gate 5: checking aws-lc-rs is absent from the workspace build graph ...
  PASS — aws-lc-rs absent from the workspace build graph (ring-only crypto)
  PASS — no openssl-sys via reqwest (only lettre's native-tls path is allowed)
Gate 6: checking containment-predicate anti-drift (single shared helper + delegation) ...
  PASS — containment predicate lives only in the shared helper; all consumers delegate

All invariant gates PASSED.
GATES A A2 B C PASSED
```

The resolved SHA exactly matches Plan 01's recorded pre-fix reference. `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac`, `verify_anchor_mac`, and `has_github_grant` extracted non-empty and compared byte-identical. The restricted diff contains neither provenance identifier, no added mint/effect/direct-events-insert token, and no manifest change.

## Gate D: recorded dispositions realised

- `record_github_grant` opens `TransactionBehavior::Immediate`, stages the grant, reads the durable head and calls `append_event` through the transaction, then commits only after the append succeeds (`audit.rs:536-574`).
- `append_event` remains the only append choke point and rebinds the persisted parent at the durable head; no production site persists a threaded in-memory parent.
- AR-05 now states 45 sites with the post-fix 2/39/4 breakdown (`audit.rs:1042-1048`), while AR-06 accurately states the sink-blocked group is mutex-serialized and fail-closed but not one SQL transaction (`server.rs:1043-1049`).

## Regression commands and observed status

- `cargo build --workspace` — exit 0.
- `cargo test -p brokerd --lib audit:: -- --test-threads=1` — exit 0; 37 passed.
- `cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1` — exit 0; 2 passed.
- `cargo test -p brokerd --test audit_chain_fork_regression --test audit_dag --test durable_anchor --test extract_provenance_threading --test harden01_session_integrity -- --test-threads=1` — exit 0 outside the restricted sandbox; 2 + 2 + 5 + 4 + 3 passed.
- `cargo test -p caprun --test grant -- --test-threads=1` — exit 0; 2 passed.
- `cargo test -p brokerd --test s38_github_pr --no-run` — exit 0.
- `./scripts/check-invariants.sh` — exit 0; Gates 1–6 passed.
- `git diff --check` and clean-source check — exit 0.

The first sandboxed `durable_anchor` attempt was denied with `EPERM`; rerunning outside the restricted sandbox passed 5/5 without a code change.

## Planning-defect resolution and deferred tests

The original combined command also named `replay_cas`, but that binary declares itself “Linux-only, Mailpit-backed” and performs a live SMTP send. A bare run failed with `Connection refused`, exactly as its source contract predicts. Including it contradicted this plan's simultaneous prohibition on Mailpit/container-backed verification and the research claim that the set was sink-free. It was therefore removed from the host-safe set and assigned to the shared AWS EC2/Mailpit run rather than misreported as a product failure.

Two pre-existing public-network tests were compiled but not executed: `s38_github_pr.rs::github_pr_replay_creates_at_most_one` and `confirmation.rs::github_pr_confirm_with_grant_proceeds_no_dangling`. They issue real GitHub API requests with fake credentials, predate CR-01, and are owned by the shared AWS EC2 run with Phase 52.

## Evidence boundary

The full-workspace composed regression is **UNRUN on this host** and is owned by the shared AWS EC2 run batched with Phase 52. No host-local result recorded here constitutes real-Linux or full-workspace proof.

## Self-Check: PASSED

- No source or CLI file was modified by this plan.
- The pre-fix reference agrees with Plan 01.
- All genuinely host-safe required gates pass.
- Deferred network/Mailpit coverage is named, explained, and assigned rather than silently omitted.

---
*Phase: 51.1-grant-audit-atomicity-cr-01*
*Completed: 2026-08-10*
