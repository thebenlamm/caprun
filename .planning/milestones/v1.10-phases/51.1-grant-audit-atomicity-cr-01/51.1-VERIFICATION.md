---
phase: 51.1-grant-audit-atomicity-cr-01
verified: 2026-08-10T03:15:00Z
status: passed
score: 18/18 must-haves verified
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "The scoped Docker-free host regression includes replay_cas and is green."
    addressed_in: "Shared AWS EC2/Mailpit run batched with Phase 52"
    evidence: "ROADMAP Phase 51.1 Notes explicitly assign the full-workspace regression to that run; replay_cas declares Mailpit-backed live SMTP behavior and cannot honestly be counted as host-safe proof."
---

# Phase 51.1: Grant/Audit Atomicity (CR-01) Verification Report

**Phase Goal:** Close CR-01 so a `github.pr` capability grant and its `github_grant_authorized` audit event are one atomic durable fact, preserving HARDEN-02 and provenance separation, with a genuine fault-injection oracle and fresh independent adversarial trace.
**Verified:** 2026-08-10T03:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The roadmap criteria and all PLAN truths were merged and deduplicated. The one internally contradictory Plan 02 claim about running `replay_cas` without Mailpit is reported under Deferred Items and is not represented as host evidence.

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Grant insert and conditional audit append execute in one Immediate transaction and commit only after append succeeds. | ✓ VERIFIED | `record_github_grant` opens `Transaction::new_unchecked(..., Immediate)`, inserts through `tx`, calls `append_event(&tx, ...)`, then calls `tx.commit()` at `crates/brokerd/src/audit.rs:543-574`. Every pre-commit error propagates with `?`, causing rollback. |
| 2 | `append_event` joins the enclosing transaction without a nested BEGIN and appends at the locked durable head. | ✓ VERIFIED | `append_event` selects its behavior through `conn.is_autocommit()`; the non-autocommit branch invokes `append_at_head` directly. The exact existing test `audit::tests::github_grant_recorded_makes_has_true_and_emits_opaque_event` passed (1/1), excluding a nested-BEGIN failure on the happy path. |
| 3 | Fault injection rolls back the grant, and an unedited retry yields exactly one grant, one authorization event, and a valid chain. | ✓ VERIFIED | Independent run of `cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1`: 2 passed. The oracle drops `events`, asserts the propagated error, zero surviving grant rows and `has_github_grant == false`, repairs the schema, retries, and asserts 1 row, 1 event, active grant and `verify_chain == true`. |
| 4 | The committed regression is a genuine host-runnable RED-to-GREEN oracle. | ✓ VERIFIED | No target/feature cfg, `should_panic`, mock seam, thread, sleep, Docker, or sink exists in the test. Its failure message includes the observed row count. `cmp` proved the current test blob is identical to commit `fc2ee68`; the pre-fix production diff shows the grant insert was autocommit before the failing append, making the asserted observed count 1. Commit history and trace record preserve the RED output. |
| 5 | `has_github_grant` is unchanged and cannot observe an unaudited committed grant after this writer returns/fails. | ✓ VERIFIED | Function-body extraction from pre-fix `9322e55fe2736c85575f78e95dd6f2c459a94d69` and current source compared byte-identical. The writer now commits both facts together; the failure test observes both zero rows and a false capability. |
| 6 | HARDEN-02 verifier/MAC logic remains byte-identical. | ✓ VERIFIED | Independent non-empty body extraction and byte comparison passed for `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac`, and `verify_anchor_mac` against `9322e55...`. |
| 7 | Causal-chain and provenance graphs remain separate. | ✓ VERIFIED | Restricted diff against `9322e55...` has no added/removed content mentioning `read_event_id` or `provenance_chain`; the repair passes only the causal parent/hash into the existing append choke point. |
| 8 | Dependency, mint, raw-effect, and append-choke-point surfaces remain unchanged; invariant Gates 1–6 pass. | ✓ VERIFIED | Restricted diff adds no `mint_from_`, `EffectRequest`, or direct `INSERT INTO events`, and changes neither root `Cargo.toml` nor `Cargo.lock`. Independent `./scripts/check-invariants.sh` run passed Gates 1–6. Repository search found the sole production direct events insert inside `append_event`; the second hit is a test corruption fixture. |
| 9 | Documentation accurately describes the transaction and AR-05/AR-06 facts. | ✓ VERIFIED | `record_github_grant` docstring describes rollback/co-commit; `audit.rs` says 45 sites with 2/39/4; `server.rs` explicitly says the sink-blocked group is ordered but not one SQL transaction. |
| 10 | The concurrency design records the fourth enclosing site and correct inventory. | ✓ VERIFIED | Section 4 names `record_github_grant` as the fourth enclosing site and sole shared-reference `Transaction::new_unchecked` user. Independent Appendix A count: 45 rows = 2 session-root, 39 chain-continuation, 4 in-transaction. |
| 11 | The brief pins the exact review range, changed paths, obligations, and wrong turns before review. | ✓ VERIFIED | `51.1-ADVERSARIAL-TRACE-BRIEF.md` is substantive (53 lines), names pre-fix `9322e55...`, target `747cf6b`, four scoped paths, and eleven falsifiable checks. Both commits exist and the base is an ancestor of the target. |
| 12 | A fresh, non-self reviewer performed the adversarial trace. | ✓ VERIFIED | `51.1-ADVERSARIAL-TRACE.md` identifies `/root/review_51_1_cr01`, explicitly states `author != reviewer`, read-only scope, and receipt of the committed brief rather than authoring transcript. |
| 13 | The trace checks transaction wiring, HARDEN-02, provenance separation, and oracle integrity. | ✓ VERIFIED | Mandatory outcomes 1–11 contain code-path evidence for each required attack, including direct-insert search, protected-body comparison, oracle blob identity, and design inventory recount. |
| 14 | The independent verdict has zero BLOCKER and zero unresolved MAJOR findings. | ✓ VERIFIED | Trace verdict explicitly records `BLOCKER count: 0` and `Unresolved MAJOR count: 0`; the separate code review is also clean (0 critical/warning/info). |
| 15 | Host-local regression evidence is scoped honestly. | ✓ VERIFIED | Summaries and trace explicitly say composed/full-workspace proof is UNRUN here, is not real-Linux proof, and belongs to shared AWS EC2/Mailpit coverage. The `replay_cas` bare failure is disclosed as the expected missing-Mailpit contradiction, not hidden or called a product failure. |
| 16 | The changed writer remains exercised through the real CLI grant path. | ✓ VERIFIED | Production caller at `cli/caprun/src/main.rs:1255` calls `record_github_grant`; Plan 02 records the cross-process `caprun --test grant` integration as passing. No source change since that evidence was recorded. |
| 17 | Replay suppression and session-root chain behavior survive the transaction change. | ✓ VERIFIED | The independently run integration binary includes the fresh-grant/replay guard and passed: exactly one row/event after replay and `verify_chain` true for the root authorization event. |
| 18 | CR-01 in REQUIREMENTS.md is fulfilled without weakening its must-NOT clauses. | ✓ VERIFIED | The requirement demands co-commit, rollback/retry exactly-one proof, HARDEN-02 preservation, and graph separation. Truths 1–8 provide direct implementation, behavioral, historical, and byte-comparison evidence for every clause. |

**Score:** 18/18 truths verified (0 present-but-behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | Plan 02's original host-regression sentence included `replay_cas`. | Shared AWS EC2/Mailpit run batched with Phase 52 | The binary is Linux/Mailpit-backed and performs live SMTP; the bare host run failed `Connection refused`. ROADMAP Phase 51.1 Notes explicitly defer composed coverage. It is excluded from host-safe proof, not treated as green. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `crates/brokerd/tests/grant_audit_atomicity_regression.rs` | Deterministic failure/retry oracle and replay guard | ✓ VERIFIED | 98 substantive lines, current tests 2/2 pass, blob unchanged since RED commit. |
| `crates/brokerd/src/audit.rs` | Atomic writer, unchanged reader/verifier, accurate comment | ✓ VERIFIED | Substantive and wired through the CLI; transaction and append path inspected directly. |
| `crates/brokerd/src/server.rs` | Correct AR-06 commentary | ✓ VERIFIED | Comment accurately disclaims cross-statement SQL atomicity; executable behavior unchanged in the scoped diff. |
| `planning-docs/DESIGN-audit-append-concurrency.md` | Fourth enclosing site and 2/39/4 inventory | ✓ VERIFIED | Section 4 and Appendix A agree with source; inventory independently counted. |
| `51.1-02-SUMMARY.md` | Mechanical gate and scoped-run record | ✓ VERIFIED | Substantive and explicitly corrects the replay-CAS/Mailpit planning contradiction. Claims were independently checked where host-safe. |
| `51.1-ADVERSARIAL-TRACE-BRIEF.md` | Precommitted reviewer contract | ✓ VERIFIED | Exact valid commit range and mandatory checks present. |
| `51.1-ADVERSARIAL-TRACE.md` | Independent trace and clearance | ✓ VERIFIED | Reviewer identity, opened files, per-obligation results, resolutions and explicit verdict present. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `record_github_grant` | `append_event` | Immediate `tx`, durable-head lookup, non-autocommit join | ✓ WIRED | Insert, head lookup, append and commit all use the same transaction handle. |
| Atomic writer | Fault-injection regression | Same public production API, no seam | ✓ WIRED | Test calls `record_github_grant` directly and inspects durable tables plus public reader/verifier. |
| RED commit | Current regression | Identical committed test blob | ✓ WIRED | `cmp` succeeded for `fc2ee68` versus working tree; production fix landed separately. |
| Writer | Human CLI grant path | `cli/caprun/src/main.rs` call | ✓ WIRED | Production call opens the audit DB and invokes the changed writer. |
| Trace brief | Landed diff | Exact SHA range | ✓ WIRED | Range `9322e55...747cf6b` exists and is ancestry-valid. |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `record_github_grant` | Capability row + authorization event | SQLite transaction through real `session_grants`, `events`, and `chain_anchor` writes | Yes | ✓ FLOWING |
| Regression oracle | Persisted row/event counts and chain result | Queries the same in-memory SQLite connection after rollback/commit boundaries | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Failure rollback, retry exactly once, replay suppression, root-chain validity | `cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1` | 2 passed, 0 failed | ✓ PASS |
| Existing grant happy path does not attempt nested BEGIN | `cargo test -p brokerd --lib audit::tests::github_grant_recorded_makes_has_true_and_emits_opaque_event -- --exact` | 1 passed, 0 failed | ✓ PASS |
| Architectural invariants | `./scripts/check-invariants.sh` | Gates 1–6 passed | ✓ PASS |

### Probe Execution

No phase-specific `probe-*.sh` is declared. The committed Rust integration regression is the phase oracle and was executed directly above.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| CR-01 | 51.1-01, 51.1-02, 51.1-03 | Grant/event co-atomicity with fault injection; preserve HARDEN-02 and provenance separation | ✓ SATISFIED | Truths 1–18; current regression 2/2; byte comparisons; restricted diff gates; independent trace. |

No additional requirement mapped to Phase 51.1 is orphaned.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `51.1-02-PLAN.md` | host regression truth/task | `replay_cas` simultaneously described as sink-free despite its Mailpit-backed live SMTP contract | ℹ️ Deferred planning contradiction | Corrected in the summary and excluded from host-safe evidence; covered only by the assigned EC2/Mailpit run. |

No unreferenced TBD/FIXME/XXX markers, cfg-hidden oracle, expected-panic inversion, placeholder implementation, alternate append entry point, protected-function drift, provenance drift, manifest change, or new mint/raw-effect path was found in the phase-modified implementation.

### Human Verification Required

None for the Phase 51.1 goal. The external EC2/Mailpit composed run is explicitly deferred non-regression coverage, not missing proof of the SQLite-only CR-01 atomicity behavior.

### Gaps Summary

No actionable gap blocks CR-01. The grant and authorization event are a single durable transaction fact, the failure/retry invariant is exercised by a genuine unchanged regression, protected security surfaces remain unchanged, and the required independent trace cleared with zero BLOCKER/MAJOR findings. The deferred Mailpit-backed coverage must remain labelled as unrun and must not be cited as host-safe or real-Linux proof.

---

_Verified: 2026-08-10T03:15:00Z_
_Verifier: the agent (gsd-verifier)_
