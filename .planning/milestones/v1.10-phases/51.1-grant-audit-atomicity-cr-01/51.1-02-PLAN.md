---
phase: 51.1-grant-audit-atomicity-cr-01
plan: 02
type: execute
wave: 2
depends_on: ["51.1-01"]
files_modified: []
autonomous: true
requirements: [CR-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The six HARDEN-02 verifier and MAC function bodies are byte-identical to their pre-fix versions, proven by extraction and comparison against the pre-fix reference commit rather than by reading the diff."
    - "has_github_grant is byte-identical to its pre-fix version — the capability-gate reader was not changed to paper over the writer."
    - "No added or removed line under crates/brokerd/src/ mentions either of the two provenance-graph identifiers, so the value-injection enforcement graph stays un-unified with the causal parent chain."
    - "The landed diff changes no dependency manifest, adds no mint call site, adds no forbidden raw effect-to-sink token, and adds no audit append entry point outside the single choke point; check-invariants.sh Gates 1-6 are green."
    - "The scoped, Docker-free, sink-free host regression across the grant-atomicity, audit-chain, anchor, provenance-threading, session-integrity, replay-CAS and CLI-grant families is green, and the two pre-existing network-touching tests compile without being executed."
    - "The plan summary states plainly that the full-workspace composed regression is UNRUN on this host and is owned by the shared AWS EC2 run batched with Phase 52 — no host-local result is described as real-Linux or full-workspace proof."
  artifacts:
    - path: ".planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-02-SUMMARY.md"
      provides: "The verbatim recorded output of Gates A, A2, B and C, the scoped regression command list with observed exit statuses, and the explicit unrun-regression disclosure"
  key_links:
    - from: "the pre-fix reference commit recorded in 51.1-01-SUMMARY.md"
      to: "the working tree after Plan 51.1-01"
      via: "function-body extraction and byte comparison, plus a restricted diff scan, both anchored on that same reference"
      pattern: "verify_chain|compute_anchor_mac|has_github_grant"
    - from: "crates/brokerd/tests/grant_audit_atomicity_regression.rs"
      to: "cli/caprun/tests/grant.rs"
      via: "the same writer is exercised in-process by the oracle and cross-process by the real caprun grant binary, so a fix that only satisfies the in-process oracle is caught"
      pattern: "record_github_grant"
---

<objective>
Prove mechanically that Plan `51.1-01`'s Trusted Computing Base change repaired grant/audit atomicity WITHOUT loosening the verifier, disturbing the provenance graph or widening the dependency surface — and run the scoped, Docker-free host regression that this device can honestly execute.

Purpose: This repository's standing failure mode is a change that makes tests pass by weakening what the tests protect. The 51-07 precedent answered it with byte-level gates rather than reviewer intention, and this plan repeats that shape against the same class of change. The second standing failure mode is claiming real-Linux or full-workspace proof from a host-only run; this plan runs only what genuinely runs here and says so.
Output: A summary carrying the verbatim gate output, the scoped regression command list with observed exit statuses, and an explicit disclosure of what was NOT run and who owns it.

This plan modifies NO source file. Its entire product is evidence.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@planning-docs/DESIGN-audit-append-concurrency.md
@.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-VALIDATION.md
@.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-01-SUMMARY.md
</context>

## Phase success criteria (from ROADMAP Phase 51.1 — CONTEXT-equivalent and binding)

There is no `CONTEXT.md` for this phase; the ROADMAP Phase 51.1 goal, its six numbered
success criteria and its Notes are the locked decisions. This plan owns criteria 4 and 6
and re-verifies criteria 1 and 3 against the pre-fix reference. Criterion 5 is Plan
`51.1-03`.

## Artifacts this phase produces

New file:

- `.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-02-SUMMARY.md`

New symbols: **none.** This plan adds no function, no constant, no test, no file under
`crates/` or `cli/`, no crate, no cargo feature, no environment variable and no CLI flag.

Explicitly NOT produced: no source modification of any kind — if a gate fails, the task
reports the failure and stops rather than adjusting either the gate or the code. No
`Cargo.toml` or `Cargo.lock` change. No edit to `.planning/ROADMAP.md`,
`.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-VALIDATION.md`,
or any artifact under `.planning/phases/51-non-hybrid-live-proof-v1-10-done/`. No
execution of any container runtime, of either container-backed verification script under
`scripts/`, or of the whole-workspace cargo test recipe.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | The capability and its audit event are one atomic fact | 51.1-01, 51.1-02 | COVERED | Plan `51.1-01` delivers it; this plan proves the delivery did not cost anything else. |
| REQ | CR-01 | Must not weaken HARDEN-02 tail-truncation detection or unify the causal chain with the provenance graph | 51.1-02 | COVERED | Task 1 Gates A, A2 and B. |
| CONTEXT (ROADMAP) | crit. 1 | One immediate transaction; no nested transaction | 51.1-02 | COVERED | Task 2 re-runs the grant unit-test family and the cross-process CLI grant test; a nested begin would surface as an error there. |
| CONTEXT (ROADMAP) | crit. 3 | `has_github_grant` cannot observe a grant without its event | 51.1-02 | COVERED | Task 1 Gate A2 proves the reader is byte-unchanged, so the property is structural rather than patched into the reader. |
| CONTEXT (ROADMAP) | crit. 4 | HARDEN-02 intact; provenance graph un-unified | 51.1-02 | COVERED | Task 1 Gates A and B, plus the provenance-threading and durable-anchor regressions in Task 2. |
| CONTEXT (ROADMAP) | crit. 6 | Gates 1–6 green; no manifest change, no new mint site, no raw effect path | 51.1-02 | COVERED | Task 1 Gate C. |
| CONTEXT (ROADMAP Notes) | — | Fix and regression are host-runnable; full-workspace regression batches onto the EC2 run with Phase 52 | 51.1-02 | COVERED | Task 2 runs only host-local, sink-free commands and requires the disclosure in the summary. |
| RESEARCH | Q5 | Blast radius: two pre-existing tests make real outbound requests and are out of CR-01's scope | 51.1-02 | COVERED | Task 2 compiles them without executing them and requires the reason to be named in the summary. |
| RESEARCH | Q6 | The exact host-local command set confirmed to run on this device | 51.1-02 | COVERED | Task 2's command list is drawn from it. |
| CLAUDE.md | — | From Phase 16 onward all container-backed Linux verification runs on EC2; a benign run can reach a live sink | 51.1-02 | COVERED | Task 2 runs no sink-dispatching test and no container-backed script. |
| CLAUDE.md | — | Never bind a named Docker volume as a compile cache | 51.1-02 | N/A | No container is started by this plan at all. |
| RESEARCH | — | Package Legitimacy Audit gate | 51.1-02 | N/A | No package-manager install task; Gate C asserts both manifests are unchanged. |

<tasks>

<task type="auto">
  <name>Task 1: Run Gates A, A2, B and C against the pre-fix reference</name>
  <precondition>`51.1-01-SUMMARY.md` exists and records the pre-fix reference SHA, and at least one commit whose message scope begins with `51.1-` is reachable in this repository. If the automated reference resolution below yields nothing, fall back to the SHA recorded in that summary rather than guessing.</precondition>
  <files>crates/brokerd/src/audit.rs, crates/brokerd/src/server.rs</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-PLAN.md Task 2 — the house pattern these gates reproduce, including the exact function-body extraction technique, the restricted-diff scan and the instruction never to adjust a gate so it passes
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — both independent reviewers named loosening the chain verifier as the single most attractive wrong turn for a change in this area
    - planning-docs/DESIGN-audit-append-concurrency.md sections 1, 2, 8 and 9 — the verifier-unchanged rule, the two-graphs rule, the disposition already recorded for this function, and the non-goals these gates enforce
    - .planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-01-SUMMARY.md — the pre-fix reference SHA and the three commit SHAs this plan gates
    - crates/brokerd/src/audit.rs — the landed `record_github_grant` change and the seven function bodies being compared
    - crates/brokerd/src/server.rs lines 938-982 — the sites where the causal chain head and the provenance read event sit closest together, and where a conflation would most plausibly be introduced
  </read_first>
  <action>
    Run four mechanical gates against the landed diff and record their output verbatim in the plan summary. Modify no source file in this task. If any gate fails, report the failure and STOP — do not adjust the gate, do not adjust the code, and do not rationalise the difference.

    Resolve the pre-fix reference first. Take the OLDEST commit whose message scope begins with this phase's identifier and use its parent as the reference; cross-check that the resolved SHA equals the pre-fix reference SHA recorded in `51.1-01-SUMMARY.md`, and if they disagree, use the recorded SHA and note the discrepancy in the summary. Export the pre-fix copy of the audit module from that reference.

    Gate A — verifier surface unchanged. For each of `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac` and `verify_anchor_mac`, extract the function body from both the pre-fix copy and the working tree using a column-zero function-keyword-to-closing-brace range, assert the extraction is non-empty in both, and assert the two are byte-identical. A difference in any one of the six means tail-truncation detection was weakened to make something pass.

    Gate A2 — capability-gate reader unchanged. Apply the identical extraction and comparison to `has_github_grant`. This is the mechanical form of success criterion 3: the property that the reader can never observe a grant whose authorization event is missing must hold because the writer co-commits both rows, never because the reader was taught to compensate.

    Gate B — the two graphs stay separate. Assert that the diff between the pre-fix reference and the working tree, restricted to `crates/brokerd/src/`, contains no added and no removed content line mentioning either of the two provenance-graph identifiers named in this task's acceptance criteria. Value-injection enforcement and the M7 attribution ride on that second graph; a change that reads, writes or unifies it would pass every functional test while silently destroying the property.

    Gate C — dependency, mint and effect-path surface. Assert the same diff, widened to all of `crates/`, adds no line containing a mint call token, no line containing the raw effect-to-sink type name that invariant Gate 1 forbids, and no line containing a direct insert statement against the events table — the last of these enforces the design note's single-append-choke-point invariant. Assert neither dependency manifest appears in the diff's file list. Then run the repository invariant script and confirm all six of its gates pass. The exact tokens for each of these assertions are given in this task's acceptance criteria.

    Gate D — recorded dispositions realised. By reading the landed code against the design note's disposition section and against the accepted-risk entries carried out of Phase 51, confirm in prose in the summary that the grant writer's read-then-append now runs inside one transaction that commits after the append, that no production append site selects its persisted parent from a threaded in-memory value, and that the two comment-drift defects now state facts matching the code. Record for each the concrete landed-diff evidence.
  </action>
  <verify>
    <automated>FIRST=$(git log --format=%H --grep='(51\.1-' | tail -1) &amp;&amp; test -n "$FIRST" &amp;&amp; BASE=$(git rev-parse "${FIRST}^") &amp;&amp; echo "PREFIX REFERENCE: $BASE" &amp;&amp; git show "$BASE:crates/brokerd/src/audit.rs" &gt; /tmp/caprun_audit_prefix_511_gate.rs &amp;&amp; for f in verify_chain current_chain_head compute_event_hash verify_event_hash compute_anchor_mac verify_anchor_mac has_github_grant; do a=$(sed -n "/^\(pub \)\?fn $f(/,/^}/p" /tmp/caprun_audit_prefix_511_gate.rs); b=$(sed -n "/^\(pub \)\?fn $f(/,/^}/p" crates/brokerd/src/audit.rs); test -n "$a" || { echo "EXTRACTION EMPTY: $f"; exit 1; }; test -n "$b" || { echo "EXTRACTION EMPTY WORKING TREE: $f"; exit 1; }; test "$a" = "$b" || { echo "PROTECTED FUNCTION CHANGED: $f"; exit 1; }; done &amp;&amp; ! git diff "$BASE" -- crates/brokerd/src/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -qE 'read_event_id|provenance_chain' &amp;&amp; ! git diff "$BASE" -- crates/ | grep -E '^\+' | grep -qE 'mint_from_|EffectRequest|INSERT INTO events' &amp;&amp; ! git diff --name-only "$BASE" | grep -qE '^Cargo\.(toml|lock)$' &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; test -z "$(git status --porcelain crates/ cli/)" &amp;&amp; echo "GATES A A2 B C PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - The command prints a `PREFIX REFERENCE:` line whose SHA equals the pre-fix reference SHA recorded in `51.1-01-SUMMARY.md`; any discrepancy is recorded in the summary with the reason.
    - Gate A: `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac` and `verify_anchor_mac` each extract non-empty from both the pre-fix reference and the working tree, and every pair is byte-identical; the command prints no `EXTRACTION EMPTY`, no `EXTRACTION EMPTY WORKING TREE` and no `PROTECTED FUNCTION CHANGED` line.
    - Gate A2: `has_github_grant` extracts non-empty from both sides and the two are byte-identical.
    - Gate B: the diff against the pre-fix reference restricted to `crates/brokerd/src/` contains zero added and zero removed content lines mentioning `read_event_id` or `provenance_chain`.
    - Gate C: the diff against the pre-fix reference restricted to `crates/` adds zero lines containing `mint_from_`, zero lines containing `EffectRequest`, and zero lines containing `INSERT INTO events`; `Cargo.toml` and `Cargo.lock` are absent from the diff's file list.
    - `./scripts/check-invariants.sh` reports Gates 1–6 PASSED.
    - `git status --porcelain crates/ cli/` is empty — no source file was modified while proving non-regression.
    - The command prints `GATES A A2 B C PASSED` and exits 0.
    - Gate D is recorded in the summary in prose with concrete landed-diff evidence for each of its three claims; none is omitted.
    - The summary reproduces the gate command's output verbatim rather than paraphrasing it.
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only verification; it modifies nothing and produces only evidence.</reversibility>
  <done>The verifier surface, the capability-gate reader, the provenance graph and the dependency surface are all mechanically shown not to have moved, and the recorded dispositions are realised in the landed code.</done>
</task>

<task type="auto">
  <name>Task 2: Run the scoped Docker-free host regression and disclose what was not run</name>
  <precondition>`cargo build --workspace` can link on this host — `brokerd` pulls `lettre` through `native-tls` to `openssl-sys`, so `pkg-config` and the OpenSSL development headers must be present. Docker is ABSENT on this device and cannot be installed; this task must complete without it.</precondition>
  <files>crates/brokerd/tests/grant_audit_atomicity_regression.rs, cli/caprun/tests/grant.rs</files>
  <read_first>
    - CLAUDE.md section "Linux-only security tests (critical)" — the standing rule that container-backed Linux verification never runs on this device and that a benign-looking whole-workspace run can trigger a live outbound send with no listener present
    - .planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-VALIDATION.md — the sampling contract, the per-task verification map this run feeds, and the Docker note recording that the composed regression is deferred
    - .planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-RESEARCH.md Q5 and Q6 — the exhaustive list of tests that exercise the changed writer, which two of them make a real outbound request, and the exact host-local command set confirmed to run on this device
    - crates/brokerd/tests/extract_provenance_threading.rs — the regression most sensitive to a conflation of the causal parent with the provenance edge
    - crates/brokerd/tests/durable_anchor.rs — the HARDEN-02 chain-anchor regression most sensitive to an anchor-semantics change
    - cli/caprun/tests/grant.rs — the cross-process exercise of the changed writer through the real compiled binary, which the in-process oracle alone cannot cover
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-PLAN.md Task 3 — the precedent for a scoped, honestly-labelled host regression and for the disclosure paragraph this task must reproduce
  </read_first>
  <action>
    Run the scoped, sink-free, container-free regression set and record every command with its observed exit status in the plan summary. Modify no file.

    Build first: build the whole workspace so a compilation break anywhere is caught before any test result is trusted.

    Audit unit family: run the `brokerd` library tests filtered to the audit module. This covers the four existing grant unit tests, including the happy-path test whose continued success is the mechanical proof that the append joined the enclosing transaction rather than nesting inside it.

    Grant-atomicity family: run the new fault-injection regression binary and expect both of its tests to pass.

    Chain and integrity family: run the audit chain fork regression, the audit DAG tests, the durable anchor tests, the provenance threading tests, the session integrity tests and the replay compare-and-set tests in one invocation, single-threaded.

    Cross-process family: run the `caprun` grant integration test, which spawns the real compiled binary as a separate operating-system process against a persistent database and then reopens it through the broker's public interface. This is the only host-runnable exercise of the changed writer through the actual human grant path.

    Compile-only gate: build, without running, the `brokerd` GitHub pull-request integration test binary. Two pre-existing tests that call the changed writer as a precondition issue a real outbound HTTPS request to the public GitHub API with a fake bearer token. That behaviour predates CR-01, is unaffected by this fix, and is not this phase's to change — but it means those tests are not hermetic on this device. Compiling without executing proves the fix did not break them while keeping this run free of network dependence. Name both tests in the summary, state the reason they were compiled but not executed, and state that they batch onto the shared AWS EC2 run with Phase 52.

    Then run the repository invariant script and a whitespace-error check on the diff.

    Do NOT run any of the following, and do not let any of them appear in any command this task executes.
    <!-- planner-discipline-allow: docker -->
    <!-- planner-discipline-allow: compose-verify.sh -->
    <!-- planner-discipline-allow: mailpit-verify.sh -->
    <!-- planner-discipline-allow: --workspace -->
    `docker` or any other container runtime; `scripts/compose-verify.sh`; `scripts/mailpit-verify.sh`; or `cargo test --workspace`. Docker is absent from this device and cannot be installed, and the whole-workspace recipe is forbidden by CLAUDE.md from Phase 16 onward because a real outbound-send dispatch can reach a live SMTP path with no listener present. The `cargo build --workspace` invocation above is a BUILD, not a test run, and is permitted.

    Close the summary with an explicit disclosure paragraph: the full-workspace composed regression is UNRUN on this host, it is owned by the shared AWS EC2 run batched with Phase 52, and nothing in this phase's evidence constitutes real-Linux or full-workspace proof. Do not soften this, and do not describe any host-local result in terms that imply it.

    If any command fails, retain its full output in the summary, stop, and do not adjust the command set to make it pass.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test -p brokerd --lib audit:: -- --test-threads=1 &amp;&amp; cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1 &amp;&amp; cargo test -p brokerd --test audit_chain_fork_regression --test audit_dag --test durable_anchor --test extract_provenance_threading --test harden01_session_integrity --test replay_cas -- --test-threads=1 &amp;&amp; cargo test -p caprun --test grant -- --test-threads=1 &amp;&amp; cargo test -p brokerd --test s38_github_pr --no-run &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; git diff --check &amp;&amp; test -z "$(git status --porcelain crates/ cli/)"</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the automated verify exits 0, and the summary lists each command with its observed exit status.
    - `cargo test -p brokerd --lib audit:: -- --test-threads=1` reports 37 passed, including `github_grant_recorded_makes_has_true_and_emits_opaque_event`, `github_grant_is_session_scoped`, `github_grant_record_is_idempotent` and `github_grant_migration_is_idempotent`.
    - `cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1` reports 2 passed.
    - `cargo test -p brokerd --test extract_provenance_threading` passes — the provenance threading survived the causal-path change.
    - `cargo test -p brokerd --test durable_anchor` passes — HARDEN-02 anchor semantics and tail-truncation detection survived.
    - `cargo test -p caprun --test grant -- --test-threads=1` passes — the real `caprun grant` binary still records a grant end to end, and its replay case is still a no-op.
    - `cargo test -p brokerd --test s38_github_pr --no-run` exits 0 and the summary records that its two network-touching tests were compiled but NOT executed, names them, gives the reason, and assigns them to the EC2 run with Phase 52.
    - The task's `<verify>` command list contains no invocation of `docker`, no invocation of `scripts/compose-verify.sh`, no invocation of `scripts/mailpit-verify.sh`, and no `cargo test` invocation carrying `--workspace`.
    - `git status --porcelain crates/ cli/` is empty and `git diff --check` exits 0 — no source file was modified and no whitespace error was introduced.
    - `./scripts/check-invariants.sh` reports Gates 1–6 PASSED.
    - The summary contains an explicit paragraph stating that the full-workspace composed regression is UNRUN on this host, that it is owned by the shared AWS EC2 run batched with Phase 52, and that no host-local result in this phase constitutes real-Linux or full-workspace proof.
    - No file under `.planning/` other than this plan's own summary is modified, and no Phase 51 artifact is touched.
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only test execution; it modifies nothing.</reversibility>
  <done>The scoped host regression is green across the grant-atomicity, audit-chain, anchor, provenance, session-integrity, replay and cross-process grant families, the non-hermetic pre-existing tests are shown to still compile, and what was not run is disclosed by name and owner.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: **high**. This plan verifies a Trusted Computing
Base change, so every high threat below carries a `mitigate` disposition realised by a
mechanical gate in a task's automated verify, never by reviewer intention. This plan
modifies no source file, so its own attack surface is the honesty of its evidence.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Pre-fix reference commit → working tree | The comparison basis for every gate; a wrong or unstated reference makes every byte-compare meaningless. |
| Causal `parent_id` chain ↔ the provenance/taint edge graph | Two graphs that must never be equated; value-injection enforcement and the M7 attribution ride on the second. |
| Host-local test results → the phase completion claim | This device has no container runtime, so a host-local green is strictly weaker than the composed Linux gate and must be labelled as such. |
| Landed diff → the dependency and mint-site surface | A quiet manifest or mint-site change would widen the Trusted Computing Base without review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51.1-02-01 | Tampering | HARDEN-02 tail-truncation detection | high | mitigate | Gate A extracts and byte-compares six verifier and MAC function bodies against the pre-fix reference and aborts the task on any difference. |
| T-51.1-02-02 | Tampering | value-injection enforcement via the provenance graph | high | mitigate | Gate B fails if any added or removed line under `crates/brokerd/src/` mentions either protected identifier; Task 2 additionally runs the provenance-threading regression. |
| T-51.1-02-03 | Elevation of privilege | the capability-gate reader changed to compensate for a partial writer | high | mitigate | Gate A2 byte-compares `has_github_grant`; any edit to the reader fails the task. |
| T-51.1-02-04 | Elevation of privilege | a second audit append entry point bypassing the choke point | high | mitigate | Gate C fails if the diff adds any direct insert statement against the events table anywhere under `crates/`. |
| T-51.1-02-05 | Spoofing | claiming real-Linux or full-workspace proof from a host-only run | high | mitigate | Task 2 forbids every container-backed verification path and the whole-workspace test recipe, and its acceptance criteria require an explicit unrun-and-owned-by-EC2 disclosure paragraph in the summary. |
| T-51.1-02-06 | Repudiation | non-hermetic pre-existing tests silently omitted from the evidence | medium | mitigate | Task 2 compiles the network-touching test binary without running it and requires both tests to be named in the summary with the reason and the owning run. |
| T-51.1-02-07 | Tampering | a failing gate quietly adjusted until it passes | high | mitigate | Both tasks' actions require reporting the failure and stopping; both acceptance criteria assert an empty working tree under `crates/` and `cli/`, so no source edit can accompany a gate run. |
| T-51.1-02-08 | Tampering | orchestrator-owned status files edited by an executor | high | mitigate | Acceptance criteria on both tasks assert no `.planning/` file other than this plan's own summary is modified and no Phase 51 artifact is touched. |
| T-51.1-02-SC | Tampering | dependency / package surface | high | mitigate | Gate C asserts both dependency manifests are absent from the diff's file list and that the diff adds no mint call token and no forbidden effect-request token; `check-invariants.sh` Gates 1–6 run in both tasks. |
</threat_model>

<verification>
- Gate A — six verifier and MAC function bodies byte-identical to the pre-fix reference.
- Gate A2 — `has_github_grant` byte-identical to the pre-fix reference.
- Gate B — zero added or removed provenance-identifier lines under `crates/brokerd/src/`.
- Gate C — no manifest change, no mint call token added, no forbidden effect-request token added, no direct events insert added, `check-invariants.sh` Gates 1–6 green.
- Gate D — recorded dispositions realised, evidenced in prose in the summary.
- Scoped host regression: workspace build, audit unit family, the new grant-atomicity binary, the chain/DAG/anchor/provenance/integrity/replay family, the cross-process CLI grant test, and a compile-only gate on the network-touching GitHub test binary.
- NOT run here, and not runnable on this device: `cargo test --workspace`, `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh`, and any container runtime. The full-workspace composed regression is UNRUN on this host and is owned by the shared AWS EC2 run batched with Phase 52. No result produced by this plan is real-Linux or full-workspace proof.
</verification>

<success_criteria>
- Every gate passes against a pre-fix reference whose SHA matches the one recorded by Plan `51.1-01`.
- The seven protected function bodies are byte-identical to their pre-fix versions.
- The landed diff touches neither provenance identifier, neither dependency manifest, no mint call site, no forbidden effect-request token and no direct events insert.
- The scoped Docker-free host regression is green across all seven named families, and the non-hermetic pre-existing tests still compile.
- The summary carries the verbatim gate output, the command list with exit statuses, and the explicit unrun-regression disclosure.
- No source file and no orchestrator-owned planning file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-02-SUMMARY.md` when done. It MUST carry: the resolved pre-fix reference SHA and its agreement or disagreement with the one recorded in `51.1-01-SUMMARY.md`; the verbatim output of the Gate A/A2/B/C command; the Gate D prose with landed-diff evidence; every regression command with its observed exit status; the two network-touching tests named as compiled-but-not-executed with the reason and owning run; and the explicit paragraph stating the full-workspace composed regression is UNRUN on this host and owned by the shared AWS EC2 run batched with Phase 52.

Do NOT edit `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `51.1-VALIDATION.md`, or any artifact under `.planning/phases/51-non-hybrid-live-proof-v1-10-done/`. CR-01 stays `Pending`; the orchestrator owns phase-completion state.
</output>
