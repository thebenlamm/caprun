# Phase 51.1 CR-01 Adversarial Trace

## Reviewer identity and independence

Reviewer: fresh Codex sub-agent `/root/review_51_1_cr01`, external non-self adversarial reviewer. The implementation author is not the reviewer (`author != reviewer`). The review was read-only; the reviewer made no edit or commit and received the committed brief rather than the authoring transcript.

## Files the reviewer opened

- `.planning/phases/51.1-grant-audit-atomicity-cr-01/51.1-ADVERSARIAL-TRACE-BRIEF.md`
- `crates/brokerd/src/audit.rs`
- `crates/brokerd/src/server.rs`
- `crates/brokerd/tests/grant_audit_atomicity_regression.rs`
- `planning-docs/DESIGN-audit-append-concurrency.md`
- `cli/caprun/src/main.rs`
- `51.1-01-SUMMARY.md` and `51.1-02-SUMMARY.md`
- Pre-fix `audit.rs` at `9322e55fe2736c85575f78e95dd6f2c459a94d69`
- Diff `9322e55fe2736c85575f78e95dd6f2c459a94d69..747cf6b`
- Repository-wide Rust searches for append, direct events inserts, grant callers, provenance identifiers, mint/effect surfaces, and manifests.

## Mandatory checks and outcomes

1. **PASS — both writes share one transaction.** `record_github_grant` opens Immediate at `audit.rs:543`, inserts through it at `:547`, appends through it at `:572`, and commits only afterward at `:574`; every intervening error propagates via `?`.

2. **PASS — append joins and selects the durable parent under lock.** `append_event` branches on `is_autocommit` at `audit.rs:1076-1085`; the enclosing path calls `append_at_head` without nesting. Durable-head selection, event insert, and anchor upsert share that boundary at `:1005-1071`.

3. **PASS — rollback and retry are real.** The oracle drops `events` at `grant_audit_atomicity_regression.rs:37-39`, observes zero grant rows and false capability at `:45-53`, repairs and retries at `:55-57`, then asserts exactly one row, one event, active grant, and valid chain at `:59-73`. Reviewer rerun: 2 passed.

4. **PASS — six HARDEN-02 functions unchanged.** Independent diff/body inspection found no executable change intersecting `compute_event_hash` (`audit.rs:835`), `verify_event_hash` (`:874`), `compute_anchor_mac` (`:917`), `verify_anchor_mac` (`:942`), `current_chain_head` (`:1262`), or `verify_chain` (`:1331`).

5. **PASS — causal and provenance graphs remain distinct.** The diff contains no `read_event_id` or `provenance_chain` change. Live provenance derivation remains at `server.rs:825-843`; causal-head comments explicitly reject `read_event_id` at `:941` and `:957`.

6. **PASS — genuine oracle.** No expected-panic inversion, target/feature cfg, or production test seam exists. Both tests are ordinary `#[test]` items. The test blob is identical at RED commit `fc2ee68` and reviewed target; current execution passes unchanged while the pre-fix autocommit shape necessarily leaves the asserted row count at one.

7. **PASS — one append choke point.** The only production direct `INSERT INTO events` is inside `append_event` at `audit.rs:1025-1028`; the other textual hit is a test corruption fixture. The grant writer calls `append_event` at `:572`.

8. **PASS — current callers are safe; future nesting fails closed.** The only production caller opens a fresh connection at `cli/caprun/src/main.rs:1251-1255`. A future same-connection nested caller receives SQLite's nested-BEGIN error before either write.

9. **PASS — dependency/mint/effect surfaces unchanged and comments accurate.** No manifest, lockfile, mint, or effect-surface addition exists. AR-05 states 45 and 2/39/4 at `audit.rs:1042-1048`; AR-06 accurately disclaims a single SQL transaction at `server.rs:1044-1048`.

10. **PASS — evidence framing is honest.** Plan 01 and Plan 02 explicitly state the composed regression is unrun here and EC2-owned; Plan 02 names the deferred Mailpit and public-GitHub cases and claims no real-Linux/full-workspace proof.

11. **PASS — design record matches code.** Manual Appendix A recount: 45 rows, comprising 2 session-root, 39 chain-continuation, and 4 in-transaction. `record_github_grant` is in-transaction at design line 85; the breakdown is line 129; Section 4 lines 36-40 records the fourth enclosing site and shared-reference constructor; the source comment matches.

## Findings and resolutions

| Severity | Finding | Orchestrator re-verification | Resolution |
|---|---|---|---|
| — | No BLOCKER, MAJOR, MINOR, or NIT defect found in the reviewed range. | Confirmed against the live diff, passing oracle, protected-body comparison, append/provenance searches, and Appendix A row count. | Cleared; no fold-in source fix required. |

The orchestrator independently re-ran the oracle and protected-surface gates before review, confirmed a clean source tree, and confirmed the reviewer referenced the live committed range. No reviewer assertion required a source change.

## Verdict

**Independent verdict: PASS for Phase 51.1 CR-01.**

- **BLOCKER count: 0**
- **Unresolved MAJOR count: 0**

The full-workspace composed regression remains **UNRUN on this host** and is owned by the shared AWS EC2 run batched with Phase 52. This trace is not real-Linux or full-workspace proof.
