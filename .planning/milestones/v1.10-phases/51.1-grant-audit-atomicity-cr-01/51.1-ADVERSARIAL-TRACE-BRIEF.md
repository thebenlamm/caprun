# Phase 51.1 CR-01 Adversarial Trace Brief

## Reviewer contract

You are a fresh, external, non-self reviewer. The implementation author is not the reviewer, and an author self-read is explicitly not clearance. Read the repository directly, answer every obligation with file-and-line evidence, classify findings as BLOCKER, MAJOR, MINOR, or NIT, and state an independent verdict.

Do not assume the change is correct. This document supplies scope and questions, not conclusions.

## Scope

- Pre-fix reference: `9322e55fe2736c85575f78e95dd6f2c459a94d69`
- Review range: `9322e55fe2736c85575f78e95dd6f2c459a94d69..747cf6b`
- Reproduce: `git diff 9322e55fe2736c85575f78e95dd6f2c459a94d69..747cf6b -- crates/brokerd/src/audit.rs crates/brokerd/src/server.rs crates/brokerd/tests/grant_audit_atomicity_regression.rs planning-docs/DESIGN-audit-append-concurrency.md`
- Changed paths in scope:
  - `crates/brokerd/src/audit.rs`
  - `crates/brokerd/src/server.rs`
  - `crates/brokerd/tests/grant_audit_atomicity_regression.rs`
  - `planning-docs/DESIGN-audit-append-concurrency.md`

The defect: `record_github_grant` inserted the capability row in autocommit, then appended `github_grant_authorized` in a separate transaction. An append failure could leave a durable active grant without its authorization event; retry then hit `INSERT OR IGNORE`, skipped the event, and returned success.

## Mandatory obligations

1. Does one transaction genuinely enclose both writes and commit only after the append succeeds, with no partial-state commit path? Cite code lines.

2. Does `append_event` join the enclosing transaction instead of nesting, and is the parent selected from the durable head inside the write lock? Cite code lines.

3. Is rollback real on append failure, and does retry yield exactly one row and one event? Trace transaction drop/error behavior and test assertions.

4. Are `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac`, and `verify_anchor_mac` byte-unchanged from the pre-fix reference? Re-derive this rather than trusting Plan 02.

5. Does the causal parent chain remain distinct from the provenance edge graph? Hunt specifically for changes involving `read_event_id` or `provenance_chain`.

6. Is the regression a genuine oracle: no expected-panic inversion, no target/feature cfg, no production test seam, and demonstrably failing on the pre-fix reference? Attack the proof itself.

7. Is `append_event` still the sole audit append entry point, with no new direct `events` insert?

8. Does any current caller invoke `record_github_grant` inside an already-open transaction on the same connection? If a future caller does, is the failure fail-closed or silently wrong?

9. Are dependencies, mint sites, and effect-path surfaces unchanged, and do the AR-05/AR-06 replacement comments now accurately describe the code?

10. Do any summaries overstate host-local evidence as real-Linux or full-workspace proof? The full-workspace composed regression is UNRUN here and owned by the shared AWS EC2 run batched with Phase 52.

11. Does the design record match the shipped code? Recount Appendix A by hand: confirm `record_github_grant` is `in-transaction`, the classes total 2 session-root / 39 chain-continuation / 4 in-transaction, the total remains 45, Section 4 records the fourth enclosing site and shared-reference constructor, and the source comment matches.

## Disclosures to challenge

- The judgment that no new design gate is needed comes from ROADMAP Notes; it was not independently re-derived here.
- `s38_github_pr.rs::github_pr_replay_creates_at_most_one` and `confirmation.rs::github_pr_confirm_with_grant_proceeds_no_dangling` were compiled but not executed because they make real public GitHub API requests.
- `replay_cas` is explicitly Mailpit-backed and was deferred to the shared AWS EC2/Mailpit run after a bare invocation confirmed the plan's host-safe command list was contradictory.
- The expected wrong turns are verifier loosening and causal/provenance graph conflation. Naming them is not a claim they were avoided.

Return outcomes for all eleven obligations, a findings table, and explicit BLOCKER and unresolved-MAJOR counts. The trace record must name you and explicitly state author-not-equal-reviewer.
