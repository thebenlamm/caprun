---
phase: 49-deterministic-multi-step-coding-planner
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - cli/caprun/src/planner.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/planner.rs
  - crates/brokerd/tests/proto_claims.rs
  - .planning/phases/49-deterministic-multi-step-coding-planner/COVERAGE.md
  - .planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "LIVE-08 expressibility: a test-only / default-off proof planner (or plan_next branch) can place an out_* bag handle into a sensitive sink arg (e.g. github.pr body) without changing the success-path recipe (CODE-02)."
    - "Success-path coding recipe still never places out_* handles after expansion work (anti-launder regression holds)."
    - "Email/file plan_next and plan() regression remain green after coding expansion."
    - "Coding RequestFd/claim path is fail-closed for multi-file untrusted demotion: success path does not require multi-file RequestFd before irreversible sinks (CODE-02)."
    - "Concurrency edge explicit: sequential one-Session worker loop; no parallel plan_next; Deny/Block aborts remaining (Phase 48 substrate; unchanged by expansion)."
    - "COVERAGE.md records no external API/SDK and CaprunIntent add-alongside assumption-delta."
    - "49-VALIDATION Wave 0 complete; check-invariants green; stream_substrate regression green on host."
  artifacts:
    - path: cli/caprun/tests/planner.rs
      provides: "coding_i2_proof_places_out_handle expressibility + strengthened anti-launder asserts"
    - path: cli/caprun/src/planner.rs
      provides: "Optional default-off / cfg(test) proof branch OR pure test-only planner for LIVE-08 expressibility"
    - path: cli/caprun/src/worker.rs
      provides: "Final coding RequestFd/claim-skip hygiene if residual demotion risk after Plan 01"
    - path: .planning/phases/49-deterministic-multi-step-coding-planner/COVERAGE.md
      provides: "No external API + CaprunIntent add-alongside assumption-delta"
    - path: .planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md
      provides: "Wave 0 checked; nyquist_compliant true when tests exist"
  key_links:
    - "Proof path: bag out_1 (simulating mint_from_exec output) → github.pr body PlanArg — expressibility only, not LIVE-08 SUCCESS"
    - "Success path plan_next remains intent-key-only; proof path is separate type or default-off flag"
  assumptions:
    - "Full LIVE-08 provenance + verify_chain on real Linux is Phase 51; Phase 49 only proves bag routing expressibility at unit level."
    - "Always-confirm git.push still Blocks under live broker — expansion does not require five Allowed live decisions."
  prohibitions:
    - "No success-path default that places out_* into irreversible args."
    - "No mid-stream ProvideIntent; no Step 0.5 weaken; no CLI multi-node product path; no LIVE SUCCESS claim; no new crates; no new mint helpers outside Gate 3."
---

<objective>
Expand the Plan 01 coding tracer into full CODE-02 anti-laundering discipline: LIVE-08 expressibility (test-only), residual RequestFd/claim hygiene, email/file regression confirmation, COVERAGE/assumption-delta docs, and validation Wave 0 completion — without CLI multi-node productization (Phase 50) and without claiming LIVE-07/08 DONE (Phase 51).

Purpose: Make mid-loop I2 proof routing expressible for Phase 51 while locking success-path trusted-intent discipline; close documentation and Nyquist gaps.

Output: coding_i2_proof unit test; anti-launder regression strength; COVERAGE.md; 49-VALIDATION complete; stream_substrate still green.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-PATTERNS.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-01-PLAN.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@CLAUDE.md
@cli/caprun/src/planner.rs
@cli/caprun/src/worker.rs
@cli/caprun/tests/planner.rs
@cli/caprun/tests/stream_substrate.rs
</context>

<precondition>
Phase 49 Plan 01 complete: SafeCodingWorkflow + named_handles multi-mint + bag seed + plan_next five-node recipe + core planner/proto_claims tests green; 49-01-SUMMARY.md present.
</precondition>

<artifacts_this_phase_produces>
This plan (49-02) produces/completes:

1. **LIVE-08 expressibility unit test** — deliberate out_* → sensitive arg placement on a proof-only path.
2. **Anti-launder + email/file regression confirmation** after expansion.
3. **Worker coding RequestFd/claim residual hygiene** if Plan 01 left a demotion risk.
4. **COVERAGE.md** — no external API; CaprunIntent add-alongside assumption-delta.
5. **49-VALIDATION.md** — Wave 0 + nyquist complete.

Phase 49 overall (with 49-01): coding recipe + trusted mint/bag + expressibility + HYG green — ready for Phase 50 CLI multi-node driver.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LIVE-08 expressibility + anti-launder + coding RequestFd hygiene</name>
  <files>cli/caprun/tests/planner.rs, cli/caprun/src/planner.rs, cli/caprun/src/worker.rs</files>
  <read_first>
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-01-PLAN.md — tracer contracts for bag keys, success-path recipe, prohibitions
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-01-SUMMARY.md — actual symbols/paths landed (if present; else re-read planner.rs/worker.rs)
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md — Pattern 4 LIVE-08 expressibility; Pitfall 2 success-path laundering; Code Example proof placement
    - cli/caprun/tests/planner.rs — MultiNodeTestPlanner out_* placement pattern; coding_success_path_does_not_place_out_handles from Plan 01
    - cli/caprun/tests/stream_substrate.rs — taint-via-bag framing (substrate ≠ LIVE DONE)
    - cli/caprun/src/planner.rs — landed DeterministicPlanner::plan_next coding arm
    - cli/caprun/src/worker.rs — coding RequestFd / claim path as landed in Plan 01
    - planning-docs/DESIGN-multi-step-plan-stream.md §4.5 — mid-loop I2 proof routing vs success path
  </read_first>
  <behavior>
    - coding_i2_proof_places_out_handle: a test-only planner (preferred) OR default-off proof flag on intent/plan_next places out_1 (or equivalent bagged intermediate) into github.pr body (or git.push refspec) while other PR/push args remain intent-minted handles
    - Success-path DeterministicPlanner coding arm still never selects keys starting with out_
    - Strengthened anti-launder: for steps 0..4 on success path, every arg value_id is in the intent-minted key set
    - Worker coding path: if Plan 01 still always RequestFd + claim extract for coding, fix residual so multi-file untrusted demotion is not required on the happy path (skip claim extract; skip or single non-demoting seed only)
    - Module/test docs: expressibility for LIVE-08 — not LIVE-07/08 CLI multi-step DONE
  </behavior>
  <action>
    **LIVE-08 expressibility (CODE-02):** Prefer a **test-only** planner struct in `cli/caprun/tests/planner.rs` (pattern: MultiNodeTestPlanner) that at the github.pr step places `ctx.handles.get("out_1")` into PlanArg name body, keeping other required PR args from intent keys. Do **not** change the production success-path DeterministicPlanner coding arm to place out_*. If a production-side proof flag is used instead, it must default off and be unreachable from product CLI (Phase 50).

    Assert the success-path recipe independently still fails the out_* intersection check (regression of Plan 01).

    **RequestFd/claim hygiene:** Re-read worker coding arm. If coding still runs multi-file untrusted RequestFd + claim extract that can demote Session before irreversible sinks, restructure the coding branch to skip claim extract (and skip RequestFd when safe). Email/file arms must remain unchanged. Do not weaken Step 0.5.

    **Docs in planner module:** Document bag key contract: out_{step} retains untrusted provenance; success path ignores those keys; proof path is deliberate and separate.

    Frame all new tests as Phase 49 expressibility / anti-launder — not Phase 51 LIVE SUCCESS.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test planner -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - coding_i2_proof_places_out_handle (or equivalent name) exists and passes
    - coding_success_path_does_not_place_out_handles still passes
    - worker coding path does not require multi-file untrusted claim demotion before irreversible sinks
    - check-invariants.sh exits 0; planner tests pass
    - no LIVE SUCCESS claim language in product code comments
  </acceptance_criteria>
  <done>Mid-loop I2 proof routing is unit-expressible; success path remains trusted-intent-only; coding demotion risk closed.</done>
</task>

<task type="auto">
  <name>Task 2: COVERAGE + validation Wave 0 + stream regression + hygiene sweep</name>
  <files>.planning/phases/49-deterministic-multi-step-coding-planner/COVERAGE.md, .planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md, cli/caprun/tests/planner.rs, crates/brokerd/tests/proto_claims.rs</files>
  <read_first>
    - .planning/phases/48-plan-stream-substrate/COVERAGE.md — phase-scoped no-external-API format
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md — Wave 0 checklist + per-task map
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md — Assumptions Log A1–A5; Package Legitimacy N/A
    - cli/caprun/tests/stream_substrate.rs — regression suite from Phase 48
    - scripts/check-invariants.sh — Gates 1–3 expectations
  </read_first>
  <behavior>
    - COVERAGE.md states: no external API/SDK integration; Phase 49 extends CaprunIntent + planner recipe over existing sinks
    - Assumption-delta recorded: CaprunIntent coding is add-alongside closed enum variant (not promote/replace email/file); rationale = closed enum is primary identity; coding is new variant beside email/file
    - 49-VALIDATION.md Wave 0 items checked; per-task map points at real test names/commands; nyquist_compliant true and wave_0_complete true only when automated tests exist and host-safe legs pass
    - stream_substrate host tests still pass
    - check-invariants.sh green; zero new crates
  </behavior>
  <action>
    Write `.planning/phases/49-deterministic-multi-step-coding-planner/COVERAGE.md` with:
    - Header: Phase 49 API / integration coverage
    - Claim: No external API integration — Phase 49 extends CaprunIntent + planner recipe over existing shipped sinks; no new external service surface.
    - Assumption-delta section: CaprunIntent gains SafeCodingWorkflow **add-alongside** email/file (closed enum primary identity). Alternatives considered (promote email into multi-step, free-form map) rejected per DESIGN §8.3 / HYG-02.

    Update `49-VALIDATION.md`: mark Wave 0 checkboxes for coding emission, no-out_*, LIVE-08 expressibility, multi-mint, compile sweep; fill per-task map Plan/Wave columns for 49-01/49-02; set frontmatter `nyquist_compliant: true` and `wave_0_complete: true` only if referenced tests exist and pass on host.

    Run host regressions: planner, stream_substrate, check-invariants. Fix only residual compile breaks from IntentAccepted.named_handles if any test fixtures still omit the field (exhaustive construction sweep). Do not expand into Phase 50 CLI verb or Phase 51 LIVE harness.

    Optional: if proto_claims multi-mint test needs a small assert polish after Task 1, keep it in this hygiene sweep.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test planner -- --nocapture &amp;&amp; cargo test -p caprun --test stream_substrate -- --nocapture &amp;&amp; cargo test -p brokerd --test proto_claims -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md exists with no-external-API claim and CaprunIntent add-alongside assumption-delta
    - 49-VALIDATION.md Wave 0 complete with nyquist_compliant true when tests green
    - stream_substrate + planner + proto_claims host tests pass
    - check-invariants.sh exits 0
    - no new crates; no Phase 50/51 product code
  </acceptance_criteria>
  <done>Phase 49 CODE-01/02 planner surface is fully validated, documented, and ready for Phase 50 CLI multi-node driver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proof planner vs success planner | Proof path may place tainted out_* handles; success path must not |
| Worker RequestFd / claims | Untrusted reads demote Session; coding happy path must not require that demotion |
| Documentation claims | Expressibility ≠ LIVE multi-step DONE |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Tampering / Elevation | success-path out_* laundering | high | mitigate | Keep success recipe intent-key-only; separate proof planner; regression test |
| T-49-02 | Elevation | mid-stream ProvideIntent | high | mitigate | No remint path added; substrate guard remains |
| T-49-03 | Elevation | Draft demotion via coding RequestFd | high | mitigate | Coding claim/RequestFd skip hygiene in worker |
| T-49-07 | Integrity of claims | expressibility sold as LIVE DONE | medium | mitigate | Explicit framing in tests + COVERAGE; LIVE is Phase 51 |
| T-49-08 | Tampering | new mint outside Gate 3 | high | mitigate | Expansion adds no mint sites; Gate 3 green |
| T-49-SC | Tampering | package installs | high | accept | Zero new packages — N/A |
</threat_model>

<verification>
- `./scripts/check-invariants.sh`
- `cargo test -p caprun --test planner -- --nocapture`
- `cargo test -p caprun --test stream_substrate -- --nocapture`
- `cargo test -p brokerd --test proto_claims -- --nocapture`
</verification>

<success_criteria>
- LIVE-08 routing expressible at unit level without success-path laundering
- Email/file + stream_substrate regression green
- COVERAGE.md + 49-VALIDATION Wave 0 complete
- Phase 49 CODE-01/02 complete without Phase 50/51 scope creep
</success_criteria>

<output>
Create `.planning/phases/49-deterministic-multi-step-coding-planner/49-02-SUMMARY.md` when done
</output>
