---
phase: 49-deterministic-multi-step-coding-planner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/intent.rs
  - crates/brokerd/src/proto.rs
  - crates/brokerd/src/server.rs
  - cli/caprun/src/planner.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/planner.rs
  - crates/brokerd/tests/proto_claims.rs
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "DeterministicPlanner::plan_next for CaprunIntent::SafeCodingWorkflow emits five nodes in order: file.write → process.exec → git.commit → git.push → github.pr with exact sink_schema PlanArg names (CODE-01)."
    - "All success-path PlanArg ValueIds come only from intent-minted bag keys (write_path, write_contents, test_command, test_args, commit_message, push_remote, push_refspec, pr_owner, pr_repo, pr_base, pr_head, pr_title, pr_body) — never from out_* bag entries (CODE-02)."
    - "ProvideIntent for SafeCodingWorkflow mints one distinct UserTrusted handle per operator literal via sequential mint_from_intent in server.rs only (Gate 3; Phase 15 finding #6); returns them as IntentAccepted.named_handles (CODE-02)."
    - "Worker coding path seeds the opaque bag from named_handles and does not run claim-extract / multi-file untrusted ReportClaims demotion before irreversible sinks (CODE-02; DESIGN §4 trusted-intent success path)."
    - "Email/file single-node DeterministicPlanner plan_next one-shot adapter remains green — coding does not force email/file through the five-node recipe (CODE-01 criterion 3)."
    - "Concurrency model (edge lift): sequential one-Session worker loop from Phase 48; no parallel plan_next; mid-stream Deny/Block aborts remaining nodes (STREAM substrate unchanged)."
    - "Zero new crates; check-invariants Gates 1 and 3 green; ProvideIntent remains once-before-RequestFd (no mid-stream remint)."
  artifacts:
    - path: crates/runtime-core/src/intent.rs
      provides: "CaprunIntent::SafeCodingWorkflow closed enum variant with all success-path operator string fields"
      contains: "SafeCodingWorkflow"
    - path: crates/brokerd/src/proto.rs
      provides: "IntentAccepted.named_handles additive field (Vec of name→ValueId pairs)"
      contains: "named_handles"
    - path: crates/brokerd/src/server.rs
      provides: "ProvideIntent SafeCodingWorkflow multi-mint arm via mint_from_intent only"
      contains: "SafeCodingWorkflow"
    - path: cli/caprun/src/planner.rs
      provides: "DeterministicPlanner::plan_next coding static step-index recipe"
      contains: "plan_next"
    - path: cli/caprun/src/worker.rs
      provides: "Coding IntentAccepted named_handles bag seed + skip claim-extract demotion path"
    - path: cli/caprun/tests/planner.rs
      provides: "CODE-01 five-node emission + CODE-02 success-path no-out_* unit tests"
    - path: crates/brokerd/tests/proto_claims.rs
      provides: "IntentAccepted named_handles round-trip + ProvideIntent multi-mint distinct handles"
  key_links:
    - "ProvideIntent once → N× mint_from_intent → IntentAccepted.named_handles → worker bag seed → plan_next(step) places only bag ValueIds → SubmitPlanNode (Phase 48 loop)"
    - "file.write path/contents origin_role Some(path) for Step 1c role gate"
    - "Missing required bag key → plan_next returns None (fail-closed empty/partial stream)"
  assumptions:
    - "Variant name SafeCodingWorkflow and bag key names from RESEARCH Pattern 1 are locked for this phase (DESIGN §8.3 naming is discretion)."
    - "Staging for git.commit is folded into operator-typed test_command/test_args (e.g. sh -c with git add) — no sixth node required for CODE-01 minimum."
    - "Concurrency: Phase 48 sequential one-Session worker loop is the only multi-node concurrency model; interrupt/Deny/Block aborts remaining (no parallel plan_next)."
  prohibitions:
    - "No free-form tool map or batch DAG authorize path under crates/ or cli/caprun."
    - "No mid-stream ProvideIntent remint; no planner/worker .mint calls."
    - "No success-path placement of out_* handles into sink args."
    - "No LLM tool-use multi-step in DeterministicPlanner or LlmPlanner coding arm."
    - "No CLI multi-node product verb / Block-and-Hold product UX (Phase 50)."
    - "No LIVE-07/08 SUCCESS claim (Phase 51); no Step 0.5 Draft×CommitIrreversible weaken; no new crates."
---

<objective>
Deliver the **deterministic multi-step coding planner tracer**: one production-quality end-to-end path from closed `CaprunIntent::SafeCodingWorkflow` through ProvideIntent multi-field UserTrusted mint, IntentAccepted named handles, worker bag seed, and `DeterministicPlanner::plan_next` five-node emission over shipped sinks — with unit proofs for CODE-01 sequence and CODE-02 trusted-intent success path — without CLI multi-node productization (Phase 50) and without LIVE proof (Phase 51).

Purpose: Close the CODE-01/02 gap left after Phase 48 stream substrate. The load-bearing work is the trusted-arg mint surface + static recipe; the sequential loop already exists.

Output: SafeCodingWorkflow intent; additive named_handles wire; broker multi-mint; worker bag seed + claim-skip; plan_next coding arm; planner + proto_claims tests.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-PATTERNS.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md
@.planning/phases/48-plan-stream-substrate/48-01-SUMMARY.md
@.planning/phases/48-plan-stream-substrate/48-02-SUMMARY.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@planning-docs/DESIGN-GATE-RECORD-v1.10.md
@CLAUDE.md
@crates/runtime-core/src/intent.rs
@crates/brokerd/src/proto.rs
@crates/brokerd/src/server.rs
@cli/caprun/src/planner.rs
@cli/caprun/src/worker.rs
@crates/executor/src/sink_schema.rs
</context>

<precondition>
Phase 48 complete: plan_next + PlanStreamContext + worker sequential loop + opaque bag + stream_substrate / stream_multi_submit green; DESIGN-multi-step-plan-stream.md CLEARED.
</precondition>

<artifacts_this_phase_produces>
Phase 49 overall produces:

1. **`CaprunIntent::SafeCodingWorkflow`** — closed coding variant with operator-typed fields for every success-path sink arg (add-alongside email/file; not a free-form map).
2. **`IntentAccepted.named_handles`** — additive `Vec<(String, ValueId)>` (or equivalent stable ordered pairs) for N named UserTrusted handles.
3. **Broker ProvideIntent multi-mint** — sequential `mint_from_intent` per coding literal inside the existing once-before-RequestFd arm (Gate 3 only).
4. **Worker coding bag seed** — insert named keys; skip claim-extract demotion path for coding success path.
5. **`DeterministicPlanner::plan_next` coding arm** — static steps 0..4 over file.write / process.exec / git.commit / git.push / github.pr.
6. **Unit tests** — five-node emission, success-path no-`out_*`, multi-mint distinct handles, email/file regression.
7. **LIVE-08 expressibility** (Plan 02) — test-only proof path placing `out_*` into a sensitive arg (not success path).
8. **COVERAGE.md + validation** (Plan 02) — no external API; Wave 0 complete.

This plan (49-01) owns items 1–6 (tracer spine + core proofs).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end coding recipe — intent, multi-mint, bag seed, plan_next</name>
  <reversibility rating="costly">Additive CaprunIntent variant + IntentAccepted.named_handles become the Phase 50 CLI coding surface; wrong mint/bag key contracts force coordinated broker+worker+planner rework (not one-way crypto/data migration).</reversibility>
  <files>crates/runtime-core/src/intent.rs, crates/brokerd/src/proto.rs, crates/brokerd/src/server.rs, cli/caprun/src/planner.rs, cli/caprun/src/worker.rs</files>
  <read_first>
    - planning-docs/DESIGN-multi-step-plan-stream.md §1–§2, §4–§5, §8.3 — sequential stream, trusted-intent success path, closed CaprunIntent, PLAN-03 handles-only
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — re-run triggers (stream shape / confirm-hold / trusted-arg mint path pivot); multi-field ProvideIntent-once at same arm is NOT a new mint site
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md — User Constraints LD locked list, Patterns 1–3, Code Examples (SafeCodingWorkflow fields, plan_coding_next, bag keys), Pitfalls 1–7
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-PATTERNS.md — intent.rs / proto.rs / server.rs / planner.rs / worker.rs analogs + exhaustive match blast radius
    - crates/runtime-core/src/intent.rs — CaprunIntent closed enum + Phase 15 multi-mint docs on SendEmailSummary
    - crates/brokerd/src/proto.rs — IntentAccepted three-slot shape (~219–223) and primary_file_derived additive discipline docs
    - crates/brokerd/src/server.rs — ProvideIntent once guard (~2372–2393) + extract match + sequential mint_from_intent chain (~2417–2556)
    - cli/caprun/src/planner.rs — PlanStreamContext key convention, default plan_next one-shot adapter, plan_from_intent, DeterministicPlanner, LlmPlanner match arms
    - cli/caprun/src/worker.rs — IntentAccepted destructure, claim-extract match, bag seed, sequential plan_next loop (Phase 48 — do not re-design loop)
    - crates/executor/src/sink_schema.rs — exact required args for file.write, process.exec, git.commit, git.push, github.pr
  </read_first>
  <behavior>
    - CaprunIntent gains SafeCodingWorkflow with operator String fields: path, contents, test_command, test_args_json, commit_message, remote, refspec, owner, repo, base, head, pr_title, pr_body (serde tag kind unchanged)
    - IntentAccepted gains named_handles: Vec of (String, ValueId) pairs; email/file construction sites pass empty vec; no silent serde default that hides incomplete mint preferred — explicit empty at every construction site
    - ProvideIntent SafeCodingWorkflow + primary_file_derived false: sequential mint_from_intent per field; path and contents use origin_role Some(path); all ValueIds distinct; value_id primary may be write_path; remaining in named_handles under documented bag keys
    - DeterministicPlanner::plan_next on SafeCodingWorkflow: step 0 file.write {path, contents}; step 1 process.exec {command, optional args}; step 2 git.commit {message}; step 3 git.push {remote, refspec}; step 4 github.pr {owner, repo, base, head, title, body}; step ≥5 None; missing bag key → None
    - Success-path coding arm never reads handles whose keys start with out_
    - Non-coding intents fall through to existing default one-shot plan_next / plan() behavior
    - plan_from_intent coding arm fail-closed or unreachable for multi-node (coding uses plan_next only); LlmPlanner / build_planner_request coding arm fail-closed (no LLM multi-step)
    - Worker: destructure named_handles; for SafeCodingWorkflow seed bag from named keys + intent primary; skip claim-extract ReportClaims path (no multi-file demotion); RequestFd of a single seed file may remain if HARDEN-01-safe, but prefer skip RequestFd for coding when it does not break email/file order — document choice in module comment
    - main.rs product CLI kind for coding remains fail-closed (Phase 50 owns caprun run coding verb)
  </behavior>
  <action>
    Implement the CODE-01/02 tracer end-to-end on production types + broker + planner + worker (DESIGN §4, §8.3; RESEARCH Patterns 1–3).

    **1. runtime-core intent.rs — add-alongside closed variant (per CODE-01; assumption-delta: add-alongside, not promote email/file):**
    - Add `CaprunIntent::SafeCodingWorkflow { path, contents, test_command, test_args_json, commit_message, remote, refspec, owner, repo, base, head, pr_title, pr_body }` all `String`.
    - Doc: each field is operator-typed; broker mints DISTINCT UserTrusted handles at ProvideIntent (finding #6); planner matches with `..` and places opaque handles only (PLAN-03). Note test_args_json is JSON-encoded `Vec&lt;String&gt;` for process.exec args (or empty/"[]").
    - Keep SendEmailSummary / CreateFileFromReport byte-stable.

    **2. brokerd proto.rs — additive IntentAccepted field:**
    - Add `named_handles: Vec&lt;(String, runtime_core::plan_node::ValueId)&gt;` (ordered pairs; stable serde). Email/file set empty.
    - Document: coding fills named bag keys; three-slot subject/body remain email-shaped; additive only.
    - Update every `BrokerResponse::IntentAccepted { … }` construction/destructure in the workspace that names fields (server.rs, worker.rs, proto_claims.rs, and any other non-`..` sites). Prefer explicit `named_handles: vec![]` for non-coding.

    **3. brokerd server.rs — ProvideIntent multi-mint (Gate 3 only):**
    - Extend the intent extract match with SafeCodingWorkflow. Prefer a loop over ordered (bag_key, literal, Option&lt;role&gt;) pairs rather than exploding the email 5-tuple further — still call only `mint_from_intent` inside this arm.
    - Roles: write path and contents → `Some("path".into())`; other coding fields descriptive or None per sink_sensitivity.
    - Thread chain-head across all mints (same linear parent pattern as subject/body).
    - `primary_file_derived` for coding success path must be false at the worker send site; do not route coding args through mint_from_read.
    - Set `*intent_provided = true` only after all mints succeed. Return IntentAccepted with primary value_id (write_path) + empty subject/body Options + full named_handles.
    - Do not change the once-before-RequestFd guard. Do not add a new IPC mint verb. Do not call .mint from planner/worker.

    **4. planner.rs — DeterministicPlanner::plan_next override:**
    - Override `plan_next` on DeterministicPlanner only (not the trait default for all implementors): if intent is SafeCodingWorkflow, run static step-index recipe using bag keys write_path, write_contents, test_command, test_args, commit_message, push_remote, push_refspec, pr_owner, pr_repo, pr_base, pr_head, pr_title, pr_body; else delegate to the existing one-shot default body (copy the default adapter logic or call a shared helper so email/file stay green).
    - Exact sink ids and PlanArg names from sink_schema (see RESEARCH table). Never invent string literals for PlanArg values. Never place out_* keys on success path.
    - plan_from_intent: add SafeCodingWorkflow arm that is fail-closed (panic-free: emit a deterministic single-node stub only if required for exhaustiveness, but multi-node coding MUST go through plan_next — preferred: match with `..` and return a non-production placeholder only if compile forces it; better: document that worker always uses plan_next and plan() for coding is not product-called — still exhaust match without reading field literals into PlanArg).
    - LlmPlanner / build_planner_request: coding arm fail-closed (bail / unsupported), no tool-use loop.
    - Extend PlanStreamContext module docs with coding bag key table (intent-minted keys + out_{step}).

    **5. worker.rs — bag seed + coding claim skip:**
    - Destructure named_handles from IntentAccepted.
    - SafeCodingWorkflow match arm: seed bag from named_handles (+ intent primary under write_path / intent as documented); do NOT run email/file claim extract / ReportClaims / ReportDerivedClaim demotion path.
    - Email/file paths unchanged (RequestFd + claims).
    - For coding: skip multi-file untrusted RequestFd when feasible without breaking connect→confine→ProvideIntent order; if a single RequestFd remains for harness compatibility, it must not demote the Session or mint untrusted success-path args. Document the chosen path in a short comment.
    - Do not re-ProvideIntent mid-stream. Do not re-design the Phase 48 sequential loop/branch table.

    **HYG / DESIGN:** Zero new crates. If implementation would pivot stream shape, confirm-hold, or introduce a mint site outside server/quarantine, stop and note adversarial re-run (GATE-RECORD) — do not silent-pivot.

    Write failing planner emission tests first where practical (tdd), then green this production path. Full test suite completion is Task 2.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p runtime-core -- --nocapture 2&gt;/dev/null; cargo check -p brokerd -p caprun 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - intent.rs defines CaprunIntent::SafeCodingWorkflow with the operator fields listed in behavior
    - IntentAccepted includes named_handles; all named construction sites compile
    - server.rs ProvideIntent matches SafeCodingWorkflow and mints only via mint_from_intent
    - DeterministicPlanner::plan_next implements five-step coding recipe with sink_schema arg names
    - worker seeds bag from named_handles for coding and skips claim-extract demotion
    - check-invariants.sh exits 0; cargo check -p brokerd -p caprun succeeds
    - no new crate; main.rs does not productize a coding CLI verb
  </acceptance_criteria>
  <done>One Session coding path can mint trusted handles once, seed the bag, and emit the five-node plan via plan_next without LLM tool-use or success-path out_* placement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CODE-01 emission + CODE-02 multi-mint + email/file regression tests</name>
  <files>cli/caprun/tests/planner.rs, crates/brokerd/tests/proto_claims.rs</files>
  <read_first>
    - cli/caprun/tests/planner.rs — MultiNodeTestPlanner (≈918–960), plan_next_step0_matches_plan_for_email/file, plan_next_step_ge1_returns_none_one_shot, #[path] include pattern
    - crates/brokerd/tests/proto_claims.rs — intent_accepted_response_round_trips; provide_intent_dispatch_returns_intent_accepted_with_resolvable_handle (distinct subject/body)
    - cli/caprun/src/planner.rs — landed plan_next coding arm + bag key convention
    - crates/brokerd/src/server.rs — landed multi-mint arm + named_handles response
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-RESEARCH.md — Validation Architecture test map; anti-launder assert pattern
    - .planning/phases/49-deterministic-multi-step-coding-planner/49-VALIDATION.md — Wave 0 coding_* test names
  </read_first>
  <behavior>
    - coding_plan_next_emits_five_sinks_in_order: bag seeded with all intent keys (synthetic ValueIds); steps 0..4 return Some with sinks file.write, process.exec, git.commit, git.push, github.pr; each node arg names match sink_schema exactly; step ≥5 returns None
    - coding_success_path_does_not_place_out_handles: bag also contains out_0/out_1 fake ids; every success-path PlanArg value_id is NOT equal to any out_* bag value
    - coding_missing_bag_key_fail_closed: omit one required key → that step returns None
    - plan_next_step0_matches_plan_for_email and plan_next_step0_matches_plan_for_file remain green
    - plan_next_step_ge1_returns_none_one_shot remains green for email/file
    - IntentAccepted named_handles serde round-trip
    - ProvideIntent SafeCodingWorkflow (primary_file_derived false) returns N distinct named handles; each resolves in ValueStore as UserTrusted; path/contents roles usable for file.write; no two bag keys share the same ValueId
  </behavior>
  <action>
    Extend `cli/caprun/tests/planner.rs` with host-safe unit tests constructing CaprunIntent::SafeCodingWorkflow in-process (no CLI verb). Seed PlanStreamContext.handles with distinct ValueId::new() per bag key. Assert CODE-01 sequence and CODE-02 no-out_* placement (RESEARCH anti-launder pattern). Keep all existing planner tests green.

    Extend `crates/brokerd/tests/proto_claims.rs` (or sibling under crates/brokerd/tests/ if proto_claims is a poor fit — prefer same file for IntentAccepted continuity):
    - Update IntentAccepted constructors for named_handles.
    - Add coding multi-mint dispatch test patterned on provide_intent_dispatch_returns_intent_accepted_with_resolvable_handle: ProvideIntent with SafeCodingWorkflow, assert named_handles length and distinctness, resolve each id, assert UserTrusted / no untrusted labels on success-path mints.

    Do not claim LIVE-07/08. Do not require five Allowed decisions through live always-confirm git.push — unit emission only.

    Module/test names should match 49-VALIDATION Wave 0 where practical: coding_plan_next / coding_success_path_does_not_place_out_handles / multi-mint named handles.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test planner -- --nocapture &amp;&amp; cargo test -p brokerd --test proto_claims -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - planner tests prove five-node order + exact arg names + step≥5 None + no out_* on success path
    - email/file plan_next regression tests still pass
    - proto_claims (or sibling) proves multi-mint distinct named UserTrusted handles for SafeCodingWorkflow
    - check-invariants.sh exits 0
  </acceptance_criteria>
  <done>CODE-01 emission and CODE-02 trusted multi-mint success path are automated and green on host; email/file one-shot path unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator intent literals → ProvideIntent | Only session-start mint of UserTrusted; worker never constructs ValueRecord |
| IntentAccepted named_handles → worker bag | Opaque ValueIds only; literals/taint stay broker-owned |
| plan_next bag → PlanArg | PLAN-03: handles only; no literal invention; no out_* on success path |
| Instruction channel | task_instruction remains Option&lt;String&gt;, never bindable ValueId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Tampering / Elevation | success-path out_* placement | high | mitigate | Static recipe uses only intent bag keys; unit test coding_success_path_does_not_place_out_handles |
| T-49-02 | Elevation | mid-stream ProvideIntent remint | high | mitigate | Existing once-before-RequestFd guard unchanged; worker loop never re-sends ProvideIntent |
| T-49-03 | Elevation / I0-I1 | Draft demotion fixed by class weaken | high | mitigate | Coding success path skips claim-extract demotion; no Step 0.5 changes |
| T-49-04 | Tampering | degenerate single handle for all args | high | mitigate | One mint_from_intent per literal; assert distinct ValueIds in proto_claims |
| T-49-05 | Spoofing | free-form tool map / batch path | high | mitigate | Closed CaprunIntent + sequential PlanNode only; Gate 1 / check-invariants |
| T-49-06 | Tampering | instruction as bindable ValueId | medium | mitigate | task_instruction stays String; coding recipe ignores it |
| T-49-07 | Integrity of claims | hybrid sold as CLI multi-step DONE | medium | mitigate | Phase 49 frames unit emission only; no LIVE SUCCESS claim; CLI is Phase 50 |
| T-49-08 | Tampering | new mint outside Gate 3 | high | mitigate | mint_from_intent only in server ProvideIntent arm; Gate 3 green |
| T-49-SC | Tampering | package installs | high | accept | Zero new packages — Package Legitimacy N/A |
</threat_model>

<verification>
- `./scripts/check-invariants.sh` (Gates 1+3 especially)
- `cargo test -p caprun --test planner -- --nocapture`
- `cargo test -p brokerd --test proto_claims -- --nocapture`
- `cargo check -p brokerd -p caprun`
</verification>

<success_criteria>
- CODE-01: five-node plan_next sequence over shipped sinks with exact arg names
- CODE-02: trusted-intent multi-mint + bag seed; no success-path out_*; no claim-driven demotion on coding happy path
- Email/file one-shot regression green
- HYG-02: zero new crates; invariants green
- No Phase 50/51 product scope (CLI multi-node / LIVE SUCCESS)
</success_criteria>

<output>
Create `.planning/phases/49-deterministic-multi-step-coding-planner/49-01-SUMMARY.md` when done
</output>
