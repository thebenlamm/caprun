---
phase: 48-plan-stream-substrate
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - cli/caprun/tests/stream_substrate.rs
  - crates/brokerd/src/proto.rs
  - crates/brokerd/src/server.rs
  - cli/caprun/src/worker.rs
  - .planning/phases/48-plan-stream-substrate/COVERAGE.md
  - .planning/phases/48-plan-stream-substrate/48-VALIDATION.md
autonomous: true
requirements:
  - STREAM-01
  - STREAM-02

must_haves:
  truths:
    - "On Deny or policy_deny mid-stream, remaining nodes abort fail-closed — no further SubmitPlanNode after non-Allowed deny (STREAM-01; DESIGN §6.1–§6.2 ordering/idempotency probes)."
    - "On BlockedPendingConfirmation, substrate stops without re-submitting the blocked node (STREAM-01/02; DESIGN §2.2 F-02; Phase 50 owns product hold)."
    - "Genuine taint via bagged process.exec output into a sensitive process.exec command arg yields I2 Block with provenance root on process_exited and verify_chain true (STREAM-02 adjacency/taint probe)."
    - "Bag insert path does not filter by sink id — unit proof that any Some(output_value_id) is stored (F-01)."
    - "Stale process.exec-only comments on PlanNodeDecision / worker / server multi-submit arm are corrected to multi-sink authority (F-01 docs drift)."
    - "No CaprunIntent coding recipe; no full CLI multi-node product path; no mid-stream ProvideIntent remint; no new mint sites (phase prohibitions)."
    - "check-invariants.sh green; host planner + stream unit tests green; Linux taint leg green under mailpit-verify when Docker available."
  artifacts:
    - path: cli/caprun/tests/stream_substrate.rs
      provides: "Deny-abort, Block no re-submit, F-01 bag-any-Some unit, Linux taint-via-bag leg"
    - path: crates/brokerd/src/proto.rs
      provides: "F-01 docs: PlanNodeDecision.output_value_id multi-sink Some semantics"
    - path: crates/brokerd/src/server.rs
      provides: "F-01 docs: multi-submit response comment matches multi-sink mint"
    - path: .planning/phases/48-plan-stream-substrate/COVERAGE.md
      provides: "API coverage exemption — no external API/SDK this phase"
  key_links:
    - "stream_substrate deny test: after Denied, submit counter stays at the denied step (no subsequent SubmitPlanNode)"
    - "taint-via-bag: mint_from_exec / Allowed exec output handle → second process.exec command → BlockedPendingConfirmation + provenance_chain[0] == process_exited event id + verify_chain"
    - "docs drift comments point at process.exec + git.commit + http.request mint arms as authority"
---

<objective>
Expand the Plan 01 tracer into full STREAM fail-closed branches, genuine bag-taint proof, and F-01 documentation hygiene — without coding recipe (Phase 49), without CLI multi-node productization (Phase 50), and without pivoting DESIGN stream/confirm/mint paths.

Purpose: Prove deny-abort remaining, Block stop-without-re-submit, opaque bag F-01 unit, and Linux genuine taint-through-bag (STREAM-02) so Phase 49 can pile coding nodes on a trustworthy substrate.

Output: stream_substrate tests; F-01 comment fixes; COVERAGE.md; validation status update.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-plan-stream-substrate/48-RESEARCH.md
@.planning/phases/48-plan-stream-substrate/48-PATTERNS.md
@.planning/phases/48-plan-stream-substrate/48-VALIDATION.md
@.planning/phases/48-plan-stream-substrate/48-01-PLAN.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@planning-docs/DESIGN-GATE-RECORD-v1.10.md
@CLAUDE.md
@cli/caprun/src/worker.rs
@cli/caprun/tests/s9_process_exec_block.rs
@crates/brokerd/src/proto.rs
@crates/brokerd/src/server.rs
</context>

<precondition>
Phase 48 Plan 01 complete: plan_next + worker sequential loop + bag exist; planner unit tests and stream_multi_submit (or host-equivalent) green; 48-01-SUMMARY.md present.
</precondition>

<artifacts_this_phase_produces>
This plan (48-02) produces/completes:

1. **`cli/caprun/tests/stream_substrate.rs`** — STREAM expansion proofs (deny abort, Block no re-submit, F-01 bag unit, Linux taint-via-bag).
2. **F-01 docs-only** comment corrections in `proto.rs`, `server.rs`, and any remaining worker drift.
3. **`COVERAGE.md`** — no external API integration for Phase 48.
4. **`48-VALIDATION.md`** — mark Wave 0 items addressed; nyquist status update when tests exist.

Phase 48 overall (with 48-01): worker loop + bag + plan_next + multi-submit verify_chain + fail-closed branches + taint-via-bag + HYG green.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deny-abort remaining + Block no re-submit + F-01 bag unit (stream_substrate)</name>
  <files>cli/caprun/tests/stream_substrate.rs</files>
  <read_first>
    - .planning/phases/48-plan-stream-substrate/48-01-PLAN.md — tracer contracts for bag keys, plan_next, branch table
    - .planning/phases/48-plan-stream-substrate/48-01-SUMMARY.md — actual symbols/paths landed in Plan 01 (if present; else re-read planner.rs/worker.rs)
    - cli/caprun/src/worker.rs — sequential loop + bag + decision branches as landed
    - cli/caprun/src/planner.rs — PlanStreamContext, plan_next, handle key convention
    - .planning/phases/48-plan-stream-substrate/48-RESEARCH.md — Minimal Tracer T2/T5; Pattern 4 branch table; Pitfall 5
    - .planning/phases/48-plan-stream-substrate/48-PATTERNS.md — stream_substrate recommended cases
    - planning-docs/DESIGN-multi-step-plan-stream.md §6 — Deny abort remaining; Block hold/no silent continue
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md F-02 — no re-submit blocked node
  </read_first>
  <behavior>
    - F-01 unit: given Allowed decisions with Some(output_value_id) for multiple sink ids (or simulated multi-sink Some values), bag contains every id; insert logic has no process.exec-only filter
    - Deny abort: a test multi-node planner that would emit 3 nodes; after node 2 returns Denied, no third SubmitPlanNode is issued (submit count == 2)
    - Block no re-submit: after BlockedPendingConfirmation on node k, the blocked plan_node is not submitted again; process exits fail-closed / loop stops
    - Empty / single-element: empty stream still fail-closed (regression of Plan 01); single Allowed node then None is success path
    - Tests that do not need process.exec run on host (macOS-safe) via pure planner/bag helpers or mock decision driver
  </behavior>
  <action>
    Create `cli/caprun/tests/stream_substrate.rs` focused on STREAM expansion (host-safe legs first).

    Prefer testing the **decision-branch + bag pure logic** by either:
    (a) extracting a small testable helper from worker if Plan 01 left the loop inline and extraction is a one-file local refactor without architecture change, or
    (b) driving a test harness that mirrors the Plan 01 branch table + bag insert rules with the same key convention and asserts the behaviors above.

    Do **not** invent reconnect-remint, dual-Session stitch, or stay-connected product hold. On Block: stop + no re-submit only (Phase 50 product hold).

    Include a multi-node test planner (or reuse Plan 01 test planner) for deny-abort sequencing. Count SubmitPlanNode emissions (mock stream or counter) to prove abort remaining.

    Frame module docs: substrate proofs for STREAM-01/02 — **not** LIVE-07/08 CLI multi-step DONE.

    If `cli/caprun/Cargo.toml` requires an explicit `[[test]]` entry for this file in this repo's convention, add one only if auto-discovery fails; most `tests/*.rs` are auto-discovered.

    Write tests first where practical (tdd), then green the harness.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test stream_substrate -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - stream_substrate.rs covers F-01 bag-any-Some, deny-abort remaining (no further submit), Block no re-submit
    - host cargo test -p caprun --test stream_substrate passes for non-Linux-gated cases
    - no CaprunIntent coding variant; no CLI multi-node product path changes in main.rs
  </acceptance_criteria>
  <done>Fail-closed mid-stream branches and F-01 bag semantics are automated and green on host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linux taint-via-bag leg + F-01 docs drift + COVERAGE + validation</name>
  <files>cli/caprun/tests/stream_substrate.rs, crates/brokerd/src/proto.rs, crates/brokerd/src/server.rs, cli/caprun/src/worker.rs, .planning/phases/48-plan-stream-substrate/COVERAGE.md, .planning/phases/48-plan-stream-substrate/48-VALIDATION.md</files>
  <read_first>
    - cli/caprun/tests/s9_process_exec_block.rs — genuine exec output → process.exec command Block + provenance backstop + verify_chain (primary analog for T3)
    - cli/caprun/tests/live_acceptance_v1_7_composed.rs — hybrid two-node taint framing honesty (substrate ≠ CLI DONE)
    - crates/brokerd/src/proto.rs — PlanNodeDecision docs (~242–254) process.exec-only drift
    - crates/brokerd/src/server.rs — multi-submit response comment (~2257–2259) and mint arms process.exec / git.commit / http.request
    - cli/caprun/src/worker.rs — any remaining process.exec-only bag comments
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md F-01 — bag any Some; stale comments are drift
    - .planning/phases/48-plan-stream-substrate/48-VALIDATION.md — Wave 0 checklist to update
    - CLAUDE.md — mailpit-verify Linux authority from Phase 16+
  </read_first>
  <behavior>
    - Linux-gated: Node1 trusted process.exec Allowed → Some(output_value_id) with genuine mint_from_exec provenance; Node2 process.exec command = that bag handle → BlockedPendingConfirmation
    - provenance_chain[0] equals the process_exited event id (anti-stapling backstop)
    - verify_chain true for the session after the block path
    - No effect of the blocked node
    - Docs: proto PlanNodeDecision, server multi-submit arm, worker comments state Some on Allowed intermediate mints for process.exec / git.commit / http.request — not process.exec-only
    - COVERAGE.md states no external API/SDK for Phase 48
  </behavior>
  <action>
    **Linux taint-via-bag (STREAM-02 T3):** Extend `stream_substrate.rs` with a `#[cfg(target_os = "linux")]` leg modeled on `s9_process_exec_block.rs` genuine-taint spine. Prefer exercising the **bag + sequential plan surface** so the second node's command arg is the opaque handle stored from the first Allowed output (not a stapled sink-local mint). Hybrid in-crate multi-node is OK for substrate; module comments must say this is **not** LIVE-07 CLI multi-step DONE.

    Document run command in the test module header:
    `MAILPIT_VERIFY_CMD='cargo build --workspace &amp;&amp; cargo test -p caprun --test stream_substrate -- --nocapture' bash scripts/mailpit-verify.sh`

    **F-01 docs-only:** Update drift comments in `proto.rs` (PlanNodeDecision.output_value_id), `server.rs` (SubmitPlanNode response comment near multi-submit arm), and any remaining worker comment so authority matches multi-sink mint (process.exec, git.commit, http.request). **Do not change wire shape or mint sites.** Gate 3 list stays unchanged.

    **COVERAGE.md:** Write `.planning/phases/48-plan-stream-substrate/COVERAGE.md` with exactly the phase-scoped claim that Phase 48 implements internal plan-stream substrate (worker loop + handle bag); no external API/SDK integration.

    **48-VALIDATION.md:** Update Wave 0 checkboxes and per-task map to point at real test files/commands; set `nyquist_compliant: true` and `wave_0_complete: true` only if all automated STREAM tests referenced exist and host-safe legs pass.

    **HYG:** `./scripts/check-invariants.sh` must pass. Zero new crates, zero new mint sites. If implementation would pivot stream shape, confirm-hold, or trusted-arg mint path, stop and note adversarial re-run required (GATE-RECORD) — do not silent-pivot DESIGN.

    Do **not** implement CaprunIntent coding recipe, `caprun run` multi-node driver, or stay-connected Block-and-Hold product UX.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test stream_substrate -- --nocapture &amp;&amp; cargo test -p caprun --test planner -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Linux-gated taint-via-bag test exists with provenance backstop + verify_chain
    - F-01 comments fixed in proto.rs, server.rs, worker.rs (no process.exec-only authority left on output_value_id docs)
    - COVERAGE.md present with no-external-API claim
    - 48-VALIDATION.md Wave 0 items checked when tests exist
    - check-invariants.sh exits 0; host stream_substrate + planner tests pass
    - no DESIGN pivot without adversarial re-run note
  </acceptance_criteria>
  <done>STREAM-02 genuine bag taint proven on Linux path; F-01 docs clean; phase coverage/validation artifacts landed; substrate ready for Phase 49 coding planner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bagged intermediate output → next PlanArg | Opaque ValueId only; I2 re-evaluates full taint/provenance at each node |
| Confirm / Block path | Substrate must not re-submit blocked node or remint UserTrusted |
| Docs vs runtime authority | Comments must not re-teach process.exec-only bag under-bagging |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering / Elevation | bagged exec output → sensitive command | high | mitigate | Linux taint-via-bag leg: I2 Block + genuine provenance root (DESIGN §9 threat 1) |
| T-48-02 | Elevation | ProvideIntent remint after observation | high | mitigate | No worker remint path; Plan 01 T4 + carry-forward (DESIGN §7.1) |
| T-48-04 | Tampering | re-submit blocked node after Block | high | mitigate | stream_substrate Block test: zero re-submits (F-02) |
| T-48-09 | Tampering | silent continue past Deny | high | mitigate | deny-abort test: no further SubmitPlanNode (DESIGN §6.2) |
| T-48-10 | Tampering | under-bag non-exec outputs via doc drift | medium | mitigate | F-01 unit + multi-sink comment fix (GATE-RECORD F-01) |
| T-48-07 | Integrity of claims | hybrid taint sold as LIVE multi-step | medium | mitigate | Explicit substrate framing in stream_substrate module docs |
| T-48-06 | Elevation | new mint for bag convenience | high | mitigate | Gate 3 unchanged; consume mint_from_exec only |
| T-48-SC | Tampering | package installs | high | accept | Zero new packages — N/A |
</threat_model>

<verification>
- `./scripts/check-invariants.sh`
- `cargo test -p caprun --test stream_substrate -- --nocapture`
- `cargo test -p caprun --test planner -- --nocapture`
- Linux: `MAILPIT_VERIFY_CMD='cargo test -p caprun --test stream_substrate -- --nocapture' bash scripts/mailpit-verify.sh`
- Linux: `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test stream_multi_submit -- --nocapture' bash scripts/mailpit-verify.sh` (Plan 01 regression)
</verification>

<success_criteria>
- Deny aborts remaining; Block does not re-submit
- F-01 bag stores any Some(output_value_id); docs match multi-sink mint
- Linux genuine taint-via-bag Block + verify_chain
- COVERAGE.md + validation Wave 0 complete
- Phase 48 STREAM-01/02 substrate complete without Phase 49/50 scope creep
</success_criteria>

<output>
Create `.planning/phases/48-plan-stream-substrate/48-02-SUMMARY.md` when done
</output>
