---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 08
type: execute
wave: 8
depends_on: [51-07]
files_modified:
  - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md
  - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md
  - planning-docs/DESIGN-GATE-RECORD-v1.10.md
autonomous: false
gap_closure: true
requirements: [LIVE-07, LIVE-08]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A written brief pins the exact diff range and the mandatory trace obligations before any reviewer is spawned, so the review cannot drift into a prose skim."
    - "The adversarial code-trace is performed by a FRESH, NON-SELF reviewer spawned by the orchestrator — a gsd-executor self-read is explicitly not clearance."
    - "The trace explicitly checks that the causal chain parent_id is never conflated with the provenance read_event_id, at the three named server.rs sites."
    - "Every production append call site in the design note's Appendix A is confirmed to have received its stated treatment, so an omission in the inventory becomes visible."
    - "The v1.10 gate record moves from PENDING to a recorded verdict with reviewer identity, findings by severity and a BLOCKER count — and only a zero-BLOCKER outcome clears it."
    - "The phase ends ready for Plan 51-04 to be re-run UNCHANGED, with LIVE-07 and LIVE-08 still Pending and broken windows 1, 2 and 3 still open."
  artifacts:
    - path: ".planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md"
      provides: "The diff range, the file:line obligation list, the Appendix A completeness checklist and the mandatory non-conflation check the reviewer must perform"
    - path: ".planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md"
      provides: "Reviewer identity and independence, files opened, findings by severity with code evidence, orchestrator re-verification, and the verdict"
    - path: "planning-docs/DESIGN-GATE-RECORD-v1.10.md"
      provides: "The audit-append gate row moved from PENDING to its recorded verdict, mirroring the existing v1.10 record format"
  key_links:
    - from: ".planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md"
      to: "the Plan 51-07 fix commit"
      via: "an explicit commit SHA and diff range taken from 51-07-SUMMARY.md, never a description of the change"
      pattern: "crates/brokerd/src"
    - from: ".planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md"
      to: "planning-docs/DESIGN-GATE-RECORD-v1.10.md"
      via: "the verdict and BLOCKER count are transcribed into the gate row; a non-zero BLOCKER count leaves the gate uncleared"
      pattern: "BLOCKER"
---

<objective>
Subject the landed D1+D2 TCB fix to a fresh, non-self, orchestrator-owned adversarial code-trace, fold its findings, and record the verdict in the v1.10 design gate — then hand off to Plan 51-04, re-run UNCHANGED, without flipping any status.

Purpose: The fresh-context adversarial review guardrail has caught real BLOCKER and MAJOR defects through v1.9, and this project's standing precedent (`DESIGN-GATE-RECORD-v1.10.md`) is that no change under `crates/brokerd` may be declared cleared until such a trace runs. The specific hazard here is a fix that "unifies" the causal `parent_id` chain with the provenance `read_event_id` graph, which would make every test pass while silently destroying I2.
Output: A trace brief, a trace record, and a gate-record verdict — with no requirement, roadmap, state or ledger status changed anywhere.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@planning-docs/DESIGN-GATE-RECORD-v1.10.md
@planning-docs/DESIGN-audit-append-concurrency.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-SUMMARY.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md
</context>

## Artifacts this phase produces

New files:

- `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md`
- `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md`

Required headings in the trace record (exact text, so the gate is mechanically checkable):

- `## Reviewer identity and independence`
- `## Files the reviewer opened`
- `## Mandatory checks and outcomes`
- `## Findings and resolutions`
- `## Verdict`

Modified:

- `planning-docs/DESIGN-GATE-RECORD-v1.10.md` — the `## Gate: audit append-at-head concurrency (Phase 51 gap closure)` section's status line, reviewer table and revision-history table are filled in from the trace record.

Explicitly NOT produced: no source file changes under `crates/` or `cli/` unless a BLOCKER or MAJOR finding requires one, in which case the fix is scoped, recorded in the trace record, and re-verified through the same gates Plan 51-07 used. No `51-LIVE-EVIDENCE.md`. No edit to `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md` or `51-VALIDATION.md`. No execution of `51-04-PLAN.md` and no modification of it.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real-Linux CLI one-Session run proven end-to-end | 51-08, then 51-04 unchanged | COVERED | This plan clears the gate; 51-04 owns the proof and is NOT re-planned or duplicated here. |
| REQ | LIVE-07 | One Session, `verify_chain` true | 51-08 | COVERED | The trace confirms the causal-chain repair holds under adversarial reading before a proof host is relaunched. |
| REQ | LIVE-08 | Attributable mid-loop I2 Block, chain still valid | 51-08 | COVERED | The mandatory non-conflation check protects exactly the attribution LIVE-08 asserts. |
| RESEARCH | — | Zero new crates / mint sites / `EffectRequest`; HYG-02 | 51-08 | COVERED | Re-asserted by the post-fold gate run in Task 3. |
| RESEARCH | — | Package Legitimacy Audit gate | 51-08 | N/A | No package-manager install task in this plan. |
| CLAUDE.md | — | Fresh non-self orchestrator-owned adversarial trace before TCB clearance | 51-08 | COVERED | Task 2, a blocking checkpoint the orchestrator owns. |
| DEFECT | D1 | Chain fork after external append | 51-08 | COVERED | Trace obligation 1. |
| DEFECT | D2 | Cross-process write collision handling | 51-08 | COVERED | Trace obligation 2. |
| DEFECT | D3 | LIVE-08 stdout assertion removal | 51-08 | COVERED | Trace obligation 8 confirms `caprun run` output was not changed to satisfy a test. |
| DEFECT | latent-1 | `record_github_grant` read-then-append | 51-08 | COVERED | Trace obligation 5 confirms the design-note disposition is realised. |
| DEFECT | latent-2 | Dual-connection same-seed head | 51-08 | COVERED | Trace obligation 5. |

<tasks>

<task type="auto">
  <name>Task 1: Write the adversarial trace brief</name>
  <precondition>`.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-SUMMARY.md` exists and names the single fix commit SHA, and that commit is reachable in the current repository.</precondition>
  <files>.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-SUMMARY.md — the fix commit SHA and the recorded Gate A-D outputs
    - planning-docs/DESIGN-audit-append-concurrency.md — Sections 1 through 9 and Appendix A, which the reviewer must check the diff against
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — the reviewer verdicts, the two named wrong turns, and the explicit instruction that the trace must check the parent/provenance non-conflation
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — the gate-discipline wording, the reviewer-independence table format and the findings-table format this trace must produce
    - crates/brokerd/src/server.rs lines 938-982 — the three sites where the causal head and the provenance read event sit closest together
    - crates/brokerd/src/audit.rs — the changed append path the reviewer will trace
  </read_first>
  <action>
    Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md`. It is the input contract for the reviewer, not a summary of the change — it must let a reviewer with zero prior context reach an independent verdict.

    Open with the exact diff range: the Plan 51-07 fix commit SHA taken from `51-07-SUMMARY.md`, the three changed files, and the command a reviewer runs to see the change. State that the reviewer must trace the LIVE CODE at that revision, not the diff alone and not the design note alone.

    Then enumerate the mandatory obligations as a numbered checklist, each phrased as a question with a falsifiable answer:

    1. Chain-fork closure. Can any production path still append an event whose persisted parent is not the durable chain head at insert time? Trace the connection handler's in-memory head threading, `record_github_grant`, the confirm-granted append and every sink terminal event.
    2. Timeout-and-transaction coupling. Is the busy timeout set on every connection `open_audit_db` returns, including the short-lived ones opened by the grant, confirm, deny and external-hold-poll paths in `cli/caprun/src/main.rs`? Is the head read inside the same immediate transaction as the insert, or could it be hoisted out under any code path including the re-entrant one?
    3. The non-conflation check, mandatory and named. Is the causal chain parent anywhere conflated with, derived from, assigned to, or reused as the provenance read event id? Trace `crates/brokerd/src/server.rs:941`, `:957` and `:973` specifically. I2 and M7 ride on the provenance graph; a fix that unifies the two graphs makes every test pass and silently destroys I2. The answer must be a direct statement about those three sites, never a general reassurance.
    4. Verifier integrity. Are `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac` and `verify_anchor_mac` unchanged, and does HARDEN-02 tail-truncation detection — the MAC over the chain anchor's head plus event count — still hold under the new append ordering?
    5. Inventory completeness and latent instances. Does every row of design-note Appendix A appear in the diff with its stated treatment, and is there any production append call site NOT in that inventory? Are the two latent instances — `record_github_grant`'s read-then-append and the dual-connection same-seed head at `server.rs:307-382` — actually in the state their design-note disposition claims?
    6. Atomicity. Did the sink-blocked group (event append, blocked-literal writes, pending-confirmation insert) become less atomic, and can a blocked event now exist without its checkpoint or without its literal?
    7. Failure modes. What happens on a transaction rollback mid-append, on a poisoned mutex, and on a genuinely contended writer that exhausts the timeout — does anything fail OPEN?
    8. Scope. Does the diff touch `crates/executor/`, any policy surface, any mint site, `Cargo.toml` or `Cargo.lock`, or the `caprun run` output surface? Any of these is out of contract.

    Add a section listing the files the reviewer is expected to open at minimum: the three changed sources, `crates/brokerd/src/quarantine.rs`, `crates/brokerd/src/policy.rs`, the sink modules that append terminal events, the short-lived connection sites in `cli/caprun/src/main.rs`, `crates/brokerd/tests/audit_chain_fork_regression.rs`, and the design note.

    Close with the independence requirements stated as constraints on the spawn rather than as advice: the reviewer must be a fresh-context, read-only, non-authoring agent; the orchestrator owns the spawn because gsd-executors have no Agent tool; a gsd-executor self-read is NOT clearance; and the reviewer must be told not to assume agreement with the design note. State the required output shape — findings by severity with file and line code evidence, plus a BLOCKER count — so the trace record can be assembled from it directly.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md &amp;&amp; grep -qF 'server.rs:941' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md &amp;&amp; grep -qF 'read_event_id' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md &amp;&amp; grep -qF 'Appendix A' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md &amp;&amp; grep -qiF 'not clearance' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md &amp;&amp; test "$(grep -cE '^[0-9]+\.' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md)" -ge 8 &amp;&amp; ./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `51-ADVERSARIAL-TRACE-BRIEF.md` exists and opens with a concrete commit SHA plus the three changed file paths, not a prose description of the change.
    - The brief contains at least eight numbered obligations, and obligation 3 names `server.rs:941`, `server.rs:957` and `server.rs:973` and the token `read_event_id` explicitly.
    - The brief names all six verifier and MAC functions in obligation 4.
    - The brief references design-note `Appendix A` as the completeness checklist and names both latent instances.
    - The brief states that a gsd-executor self-read is not clearance and that the orchestrator owns the reviewer spawn.
    - The brief specifies the required reviewer output shape including a BLOCKER count.
    - `git diff --name-only` lists no path under `crates/` or `cli/`.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>A reviewer with zero prior context can perform a complete, falsifiable trace from this brief alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Orchestrator-owned fresh non-self adversarial code-trace</name>
  <precondition>`51-ADVERSARIAL-TRACE-BRIEF.md` exists and names the fix commit SHA; the orchestrator (not a gsd-executor) is the actor for this task, because spawning a genuinely fresh reviewer requires the Agent tool that gsd-executors do not have.</precondition>
  <files>.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE-BRIEF.md — the obligations the reviewer must answer
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — the reviewer-independence and findings table formats to mirror exactly
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — the prior round's reviewer discipline, including "told not to assume agreement"
  </read_first>
  <what-built>Plan 51-07 landed a single commit changing the audit tamper-evidence append path across `crates/brokerd/src/audit.rs`, `server.rs` and `confirmation.rs`: the durable chain head is now read inside the same immediate transaction that inserts the row, and the audit database's busy timeout is now set explicitly by `open_audit_db`. Both committed red regressions from Plan 51-05 pass unedited, and Task 2 of Plan 51-07 mechanically showed the six verifier and MAC functions byte-identical and no `read_event_id` / `provenance_chain` line added or removed.</what-built>
  <how-to-verify>
    1. Spawn a FRESH, read-only, non-authoring reviewer agent against `51-ADVERSARIAL-TRACE-BRIEF.md`. It must not be the agent that authored the design note or the fix, and it must be told explicitly not to assume agreement with the design note or with the prior reviewers.
    2. Require a code-trace of the live tree at the fix commit, not a prose skim: the reviewer must open the files the brief lists and answer all eight numbered obligations with file and line evidence.
    3. Independently re-verify each reported finding against the live code before folding it — do not accept a finding, or a clean bill, on the reviewer's word.
    4. Write `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md` with the five required headings: `## Reviewer identity and independence` (agent kind, subagent id, model/runtime, effort, and an explicit author-not-equal-reviewer statement), `## Files the reviewer opened`, `## Mandatory checks and outcomes` (one row per numbered obligation with a PASS or FAIL and the evidence), `## Findings and resolutions` (severity, claim, code evidence re-verified by the orchestrator, resolution), and `## Verdict` (counts by severity and an explicit BLOCKER count).
    5. If the BLOCKER count is greater than zero, or any MAJOR finding is unresolved, STOP: do not clear the gate, do not proceed to Task 3's clearance path, and route the findings back into a scoped fix that re-runs Plan 51-07's Task 2 and Task 3 gates before this checkpoint is re-attempted.
    6. Confirm the trace record makes a direct statement about `server.rs:941`, `:957` and `:973` rather than a general reassurance about the provenance graph.
  </how-to-verify>
  <action>
    The orchestrator spawns the fresh reviewer, re-verifies every finding against the live code, and writes the trace record. This checkpoint is NOT auto-approvable: a `workflow.auto_advance` setting must not skip it, and a gsd-executor reading the diff and declaring it fine is explicitly not clearance under this project's standing gate discipline. The reviewer's independence is the control; a self-review re-creates exactly the blind spot that let the two already-fixed defects reach a provisioned host.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md &amp;&amp; for h in "## Reviewer identity and independence" "## Files the reviewer opened" "## Mandatory checks and outcomes" "## Findings and resolutions" "## Verdict"; do grep -qxF "$h" .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md || { echo "MISSING SECTION: $h"; exit 1; }; done &amp;&amp; grep -qF 'server.rs:941' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md &amp;&amp; grep -qF 'BLOCKER' .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md</automated>
    <human-check>Confirm the reviewer was a genuinely fresh, non-authoring agent (identity recorded, author not equal to reviewer), that all eight brief obligations have a recorded PASS or FAIL with file and line evidence, that obligation 3's answer is a direct statement about the three named `server.rs` sites, and that the BLOCKER count is stated explicitly. If BLOCKER count is greater than zero, the gate stays uncleared.</human-check>
  </verify>
  <acceptance_criteria>
    - `51-ADVERSARIAL-TRACE.md` exists with all five required headings verbatim.
    - The reviewer-identity section records the reviewer's kind, an identifier, the runtime, the effort, and an explicit statement that the author of the design note and the fix is not the reviewer.
    - All eight numbered brief obligations appear in `## Mandatory checks and outcomes`, each with PASS or FAIL and file and line evidence.
    - The record makes a direct statement about `server.rs:941`, `server.rs:957` and `server.rs:973` regarding parent versus provenance conflation.
    - `## Verdict` states counts by severity and an explicit BLOCKER count.
    - Every finding in `## Findings and resolutions` carries a note that the orchestrator independently re-verified it against the live code.
    - If the BLOCKER count is non-zero the plan halts here; Task 3 is not attempted and the gate record stays PENDING.
  </acceptance_criteria>
  <done>An independent, fresh-context code-trace of the TCB diff exists, with every mandatory obligation answered and a stated BLOCKER count.</done>
  <resume-signal>Type "approved" once the trace record is written and its BLOCKER count is zero, or describe the BLOCKER/MAJOR findings to route back into a scoped fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fold the verdict into the gate record and hand off to the unchanged 51-04 proof</name>
  <precondition>`51-ADVERSARIAL-TRACE.md` exists and its `## Verdict` section states a BLOCKER count of zero; if it does not, this task must not run.</precondition>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.10.md</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-ADVERSARIAL-TRACE.md — the reviewer identity, findings and verdict to transcribe
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — the PENDING gate section opened by Plan 51-06 and the existing Phase 47 section whose format it must mirror
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md — the authoritative proof plan that is re-run UNCHANGED next; read it to confirm nothing in this gap closure invalidated its preconditions, evidence rules or blocking checkpoint
    - .planning/WINDOWS.md — the three open Phase 51 entries that must remain open
    - .planning/REQUIREMENTS.md — the LIVE-07 and LIVE-08 rows that must remain Pending
  </read_first>
  <action>
    Fill in the `## Gate: audit append-at-head concurrency (Phase 51 gap closure)` section of `planning-docs/DESIGN-GATE-RECORD-v1.10.md` from the trace record, mirroring the existing Phase 47 section's structure: flip the status line from PENDING to the recorded outcome; fill the reviewer-identity table with the reviewer kind, identifier, authoring context, an explicit author-not-equal-reviewer independence statement, the model or runtime, the effort, and the orchestrator's role; list the files the reviewer opened; add a revision-history row with the round, date, reviewer, findings by severity and the result; and add a findings-and-resolutions table with the orchestrator's independent re-verification noted per row. State the BLOCKER count explicitly. Transcribe — do not restate more favourably than the trace record; if a MINOR or NIT finding was folded by amending the design note, say so and name the section amended.

    Then run the closing gate set and record each command with its exit status in the plan summary: `./scripts/check-invariants.sh`; `cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1`; and `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run`. Do NOT run `cargo test --workspace`, `scripts/compose-verify.sh` or `scripts/mailpit-verify.sh` on this host: Docker is absent and CLAUDE.md forbids the bare Linux workspace recipe from Phase 16 onward.

    Finally, write the handoff paragraph into the plan summary. It must state four things plainly. First, the next action is to provision a Docker-capable real-Linux host and execute `51-04-PLAN.md` UNCHANGED — this gap closure does not weaken its preconditions, its evidence rules or its blocking human-verify checkpoint, and nothing in `51-04-PLAN.md` was modified. Second, that host needs the OpenSSL build prerequisites installed before cargo will build, which is broken window 3. Third, LIVE-07 and LIVE-08 remain `Pending` and broken windows 1, 2 and 3 remain `open` — this plan changes no status. Fourth, no `51-LIVE-EVIDENCE.md` exists or may be created here; only an executed compose run may produce it.

    Do not touch `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md`, `51-VALIDATION.md` or `51-04-PLAN.md`. Those are orchestrator-owned or proof-owned, and executors have historically flipped the roadmap checkbox before verification — that is the specific failure this constraint exists to prevent.
  </action>
  <verify>
    <automated>grep -qxF '## Gate: audit append-at-head concurrency (Phase 51 gap closure)' planning-docs/DESIGN-GATE-RECORD-v1.10.md &amp;&amp; grep -A40 'Gate: audit append-at-head concurrency' planning-docs/DESIGN-GATE-RECORD-v1.10.md | grep -qF 'BLOCKER' &amp;&amp; ! grep -A40 'Gate: audit append-at-head concurrency' planning-docs/DESIGN-GATE-RECORD-v1.10.md | grep -qF 'TBD' &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1 &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run &amp;&amp; test ! -f .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-LIVE-EVIDENCE.md &amp;&amp; ! git log --name-only --format= 51-05-baseline..HEAD 2&gt;/dev/null | grep -qE '^(\.planning/(ROADMAP|STATE|REQUIREMENTS|WINDOWS)\.md|\.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN\.md)$' ; ! git show --name-only --format= HEAD | grep -qE '^\.planning/(ROADMAP|STATE|REQUIREMENTS|WINDOWS)\.md$'</automated>
  </verify>
  <acceptance_criteria>
    - The `## Gate: audit append-at-head concurrency (Phase 51 gap closure)` section no longer contains any `TBD` value and no longer reads PENDING; it carries the reviewer identity, the files opened, a revision-history row and a findings table.
    - The gate section states an explicit BLOCKER count, and that count is zero — a non-zero count means this task should never have run.
    - Every findings row records that the orchestrator independently re-verified the claim against the live code.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
    - `cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1` exits 0.
    - `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run` exits 0.
    - `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-LIVE-EVIDENCE.md` does NOT exist.
    - `git show --name-only --format= HEAD` lists none of `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md`; `51-04-PLAN.md` is unmodified across the whole gap-closure range.
    - `.planning/REQUIREMENTS.md` still shows LIVE-07 and LIVE-08 as Pending, and `.planning/WINDOWS.md` still shows `open_count: 3`.
    - The plan summary contains the four-part handoff paragraph naming `51-04-PLAN.md` as the unchanged next action.
    - Neither `docker`, `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh` nor `cargo test --workspace` was invoked.
  </acceptance_criteria>
  <done>The v1.10 gate carries an independently traced verdict, and the phase is handed off to Plan 51-04 re-run unchanged, with every status untouched.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: **high**. This plan changes no executable code by default; its threats are process-integrity threats against the clearance of a TCB change and against premature status flipping.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Authoring context → reviewing context | The value of the trace is entirely in the reviewer being fresh and non-authoring; a self-review provides no assurance while looking identical in the record. |
| Reviewer claim → orchestrator record | A finding accepted on the reviewer's word, or a clean bill accepted unverified, launders an unchecked assertion into a cleared gate. |
| Gap-closure completion → v1.10 DONE claim | Clearing this gate does not prove anything on real Linux; only an executed `51-04-PLAN.md` run does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-08-01 | Spoofing | reviewer independence | high | mitigate | Task 2 is an orchestrator-owned blocking checkpoint; the record must state author-not-equal-reviewer with an identifier, and a gsd-executor self-read is declared not clearance. |
| T-51-08-02 | Tampering | I2 value-injection enforcement | high | mitigate | Brief obligation 3 requires a direct, named statement about `server.rs:941`, `:957` and `:973`; a general reassurance fails the human-check. |
| T-51-08-03 | Elevation of privilege | design-gate clearance with open BLOCKERs | high | mitigate | Task 3's precondition is a zero BLOCKER count; a non-zero count halts the plan and leaves the gate PENDING. |
| T-51-08-04 | Tampering | requirement / roadmap / ledger status | high | mitigate | Acceptance criteria assert `51-LIVE-EVIDENCE.md` absent, LIVE-07/LIVE-08 still Pending, `open_count: 3`, and that the commit touches none of the four orchestrator-owned files. |
| T-51-08-05 | Spoofing | "v1.10 is DONE" claim from a Docker-less host | high | mitigate | The handoff paragraph must state that `51-04-PLAN.md` is re-run UNCHANGED on a provisioned Docker-capable host and that nothing here constitutes real-Linux proof. |
| T-51-08-06 | Repudiation | incomplete call-site inventory | medium | mitigate | Brief obligation 5 makes Appendix A completeness an explicit reviewer question, so an omission surfaces rather than passing silently. |
| T-51-08-07 | Information disclosure | trace artifacts | low | accept | Repository-internal review records; no credentials, no tokens, no customer data. |
| T-51-08-SC | Tampering | package installs | low | accept | No package-manager invocation; the build graph is asserted unchanged by the Plan 51-07 gates this plan re-runs. |
</threat_model>

<verification>
- `51-ADVERSARIAL-TRACE-BRIEF.md` carries eight numbered obligations, names the three `server.rs` conflation sites and the six verifier functions, references design-note Appendix A, and states the independence constraints.
- `51-ADVERSARIAL-TRACE.md` carries all five required headings, a per-obligation PASS/FAIL table with file and line evidence, per-finding orchestrator re-verification, and an explicit BLOCKER count.
- `planning-docs/DESIGN-GATE-RECORD-v1.10.md` gate section contains no `TBD` and states the BLOCKER count.
- `./scripts/check-invariants.sh` Gates 1-6 green; both Plan 51-05 regressions pass; the LIVE module compiles under its Linux and fixture gates.
- Status untouched: no `51-LIVE-EVIDENCE.md`; LIVE-07/LIVE-08 Pending; `open_count: 3`; `51-04-PLAN.md` byte-unchanged.
- NOT run here: `cargo test --workspace`, `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh`, `docker`. The authoritative real-Linux proof is Plan 51-04, executed unchanged on a provisioned host after this plan clears.
</verification>

<success_criteria>
- A fresh, non-self, orchestrator-spawned reviewer performed a code-trace of the Plan 51-07 fix commit against a written brief, and every finding was independently re-verified before being folded.
- The trace makes a direct, named statement that the causal chain parent is not conflated with the provenance read event id at the three cited `server.rs` sites.
- Every row of the design note's Appendix A call-site inventory is confirmed to have received its stated treatment, and no unlisted production append site was found.
- The v1.10 gate record carries the verdict with reviewer identity, findings by severity and a zero BLOCKER count.
- LIVE-07 and LIVE-08 remain Pending, broken windows 1, 2 and 3 remain open, no `51-LIVE-EVIDENCE.md` exists, and `51-04-PLAN.md` is unmodified.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-08-SUMMARY.md` when done, ending with the four-part handoff paragraph. The next action after this plan is NOT a new plan: provision a Docker-capable real-Linux host (installing the OpenSSL build prerequisites first — broken window 3) and execute `51-04-PLAN.md` UNCHANGED. Do NOT create `51-LIVE-EVIDENCE.md`, do NOT edit `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md` or `51-VALIDATION.md`, and do NOT modify `51-04-PLAN.md`.
</output>
