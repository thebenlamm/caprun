---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/tests/live_acceptance_v1_10_cli.rs
autonomous: true
requirements:
  - LIVE-07

must_haves:
  truths:
    - "On real Linux under compose-verify + mock-egress-ca, spawning real CARGO_BIN_EXE_caprun with argv safe-coding-workflow drives edit→test→commit→push(confirm)→PR as one Session; process exit 0 (LIVE-07)."
    - "Genuine caprun audit <session_id> <audit.db> subprocess asserts Chain verification: PASSED / verify_chain true for that coding Session (LIVE-07)."
    - "Exactly one coding session_id owns the multi-node SUCCESS chain — no dual-Session stitch push→PR (LIVE-07; CONFIRM-01 continuity)."
    - "Durable terminal events include git_push_succeeded and github_pr_succeeded (mock hosts admitted under mock-egress-ca) (LIVE-07)."
    - "Framing is machine-checked: SUCCESS test asserts CLI multi-node driver (CARGO_BIN_EXE_caprun + safe-coding-workflow); module docs invert v1.9 hybrid DONE language; LIVE_07_DRIVER / LIVE_07_NOT constants referenced by the success path (LIVE-07 SC2)."
    - "Host-safe guard test always runs (binary path non-empty); LIVE body is cfg-gated all(target_os = linux, feature = mock-egress-ca) so macOS 0-passed is expected, not a gap (LIVE-07)."
    - statement: "Mid-loop always-confirm git.push is released via concurrent external confirm sidecar (CAPRUN_CONFIRM=external) while worker stays connected — product Phase 50 path, not reconnect-remint (LIVE-07)."
      verification: backstop
    - statement: "github.pr grant is recorded by concurrent caprun grant sidecar after session_id surface — not auto-grant inside run (LIVE-07; GITHUB-02)."
      verification: backstop
  artifacts:
    - path: cli/caprun/tests/live_acceptance_v1_10_cli.rs
      provides: "LIVE-07 CLI multi-node SUCCESS e2e + framing pins + host guard + F1/git fixtures + external confirm/grant sidecar"
      contains: "safe-coding-workflow"
      contains: "LIVE_07_DRIVER"
  key_links:
    - "CARGO_BIN_EXE_caprun run --policy … safe-coding-workflow intent workspace audit.db → one Session multi-node stream"
    - "stdout session_id= → sidecar caprun grant; BLOCKED git.push effect_id= → sidecar caprun confirm → PROCEED → github.pr Allowed"
    - "caprun audit session_id audit.db → Chain verification: PASSED"
    - "compose-verify + brokerd/mock-egress-ca → mock push/PR TLS hosts"
  assumptions:
    - "A1/A2/A3 apply only to LIVE-08 product delta (Plan 02); Plan 01 uses product DeterministicPlanner success path only."
    - "Docker/Colima required for compose-verify green LIVE; host implements code + guard when Docker absent — do not mark LIVE-07 Complete from host-only runs."
    - "Mock /accept/repo.git + pulls 201 remain sufficient (RESEARCH A5)."
  prohibitions:
    - "No LIVE-07 SUCCESS claim driven by multi-leg hybrid evaluate_plan_node_and_record_for_test composition"
    - "No dual-Session stitch push then PR"
    - "No auto-grant or auto-confirm or session-wide confirm waiver inside the product run path"
    - "No out_* placement on DeterministicPlanner success path"
    - "No new crates, EffectRequest path, or new mint sites"
    - "No PKG-01 packaging work"
    - "No DESIGN-20 adversarial re-run unless stream/confirm/mint pivots (A6 — Plan 01 has none)"
---

<objective>
Prove **LIVE-07 non-hybrid CLI multi-node SUCCESS** end-to-end: one Session driven by real `caprun run safe-coding-workflow` under compose-verify (edit → test → commit → push confirm-release → open PR), inspected by real `caprun audit` with `verify_chain` true, with machine-checked framing that closes the v1.9 hybrid honesty gap.

Purpose: v1.10 DONE requires the multi-node SUCCESS chain to be CLI-driven — hybrid in-crate multi-leg composition is regression-only, not this claim. Phase 50 productized the driver; this plan composes it on real Linux.

Output: `cli/caprun/tests/live_acceptance_v1_10_cli.rs` with host guard, framing pins, F1+git fixtures, external confirm/grant sidecar, and LIVE-07 SUCCESS body green under compose-verify (when Docker available).
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-RESEARCH.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-PATTERNS.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-VALIDATION.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-02-SUMMARY.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VERIFICATION.md
@.planning/milestones/v1.9-phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@CLAUDE.md
@cli/caprun/tests/coding_cli.rs
@cli/caprun/tests/live_acceptance_v1_9_composed.rs
@cli/caprun/src/main.rs
@scripts/compose-verify.sh
</context>

<precondition>
Phase 50 complete: safe-coding-workflow argv, orchestrate_coding_stream mid-loop confirm (interactive + CAPRUN_CONFIRM=external), grant pointer, CLI-02 exit map 0/2/3/1, worker Block-and-Hold PROCEED/ABORT green. Phase 49 DeterministicPlanner five-node coding recipe green. compose-verify.sh + mock-github assets present. Docker/Colima available on the machine that will green LIVE (research host may lack Docker — implement + host-guard there; LIVE Complete only after compose-verify).
</precondition>

<artifacts_this_phase_produces>
Phase 51 overall produces:

1. **LIVE-07 CLI multi-node SUCCESS** — real `caprun run safe-coding-workflow` one Session under compose-verify; audit PASSED; framing honest (this plan).
2. **External confirm + grant sidecar harness** — concurrent human-action simulation without dual-Session stitch (this plan).
3. **LIVE-08 mid-loop I2 Block** — env-gated `CodingI2ProofPlanner` + genuine taint CLI proof + full regression (Plan 02).
4. **COVERAGE.md** — composition/proof only; no new external API (Plan 02 finalizes honesty).
5. **Framing constants + module docs** — invert v1.9 hybrid DONE language (this plan).

This plan (51-01) owns items 1–2 and framing for LIVE-07.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end LIVE-07 — CLI multi-node one Session SUCCESS</name>
  <reversibility rating="reversible">Test-only harness + fixtures; no product control-plane change. Can delete the test module without reversing Phase 50 driver semantics.</reversibility>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md — LIVE-07 full text
    - .planning/ROADMAP.md — Phase 51 success criteria 1–2, 4
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-RESEARCH.md — Patterns 1–3 (CLI SUCCESS, sidecar, framing), Code Examples LIVE-07 driver, Pitfalls 1/3/4/7/8/9/10, Wave / Plan Split Wave 1
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-PATTERNS.md — live_acceptance_v1_10_cli.rs section (framing invert, F1 layout, argv, sidecar algorithm, success asserts)
    - .planning/milestones/v1.9-phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md §1 — hybrid honesty language to INVERT (SUCCESS is CLI multi-node now)
    - cli/caprun/tests/live_acceptance_v1_9_composed.rs — run_caprun, assert_audit_passed, extract_surfaced_effect_id, setup_git_push_repo, seed_test_key, F1 layout, cfg linux + mock-egress-ca, COMPOSE_VERIFY_CMD module doc (copy helpers; do not copy hybrid SUCCESS driver)
    - cli/caprun/tests/coding_cli.rs — CODING_INTENT_JSON, MINIMAL_POLICY_JSON, CodingLayout F1 sibling pattern
    - cli/caprun/src/main.rs — safe-coding-workflow argv (~333–356), session_id= + grant pointer print (~448–449), confirm_mode_is_external, orchestrate_coding_stream, resolve_external_hold / CAPRUN_CONFIRM_TIMEOUT_SECS
    - scripts/compose-verify.sh — CAPRUN_GITHUB_API_BASE, mock hosts, feature expectation
    - CLAUDE.md — compose-verify / mailpit-verify authority; no named CARGO_TARGET_DIR volume hack
  </read_first>
  <behavior>
    - Host guard test live_acceptance_v1_10_cli_guard_present always compiles/runs: env!(CARGO_BIN_EXE_caprun) non-empty
    - LIVE body only under #[cfg(all(target_os = "linux", feature = "mock-egress-ca"))]
    - Module docs state SUCCESS is real CLI multi-node one Session; name v1.9 hybrid multi-leg composition as not this claim; document COMPOSE_VERIFY_CMD scoped recipe
    - Constants LIVE_07_DRIVER and LIVE_07_NOT exist; success test body references LIVE_07_DRIVER (framing pin)
    - F1-safe tmp layout: audit.db + audit.db.key + policy.json + coding-intent.json as siblings of workspace/; workspace is a git repo with pre-created write target path and identity
    - Intent JSON: kind SafeCodingWorkflow; remote https://github-mock.caprun.test/accept/repo.git; refspec HEAD:refs/heads/caprun-live-07; test_command stages (git add -A); other fields per RESEARCH Pattern 1
    - Policy permits email.send, file.create, file.write, process.exec, git.commit, git.push, github.pr, http.request, http.request.write (coding MINIMAL_POLICY_JSON shape)
    - Spawn: Command::new(CARGO_BIN_EXE_caprun) args [run, --policy, …, safe-coding-workflow, intent, workspace-file, audit.db]; env CAPRUN_CONFIRM=external, CAPRUN_CONFIRM_TIMEOUT_SECS short (e.g. 60), CAPRUN_GIT_PUSH_TOKEN, CAPRUN_GITHUB_TOKEN; stdout+stderr piped; CAPRUN_GITHUB_API_BASE from compose-verify when present
    - Sidecar drive_external_confirm_and_grant: on session_id= once run caprun grant; on git.push pending effect_id run caprun confirm only for push; never dual-Session
    - Child exit code Some(0); exactly one coding session_id; caprun audit asserts Chain verification: PASSED; durable events include git_push_succeeded and github_pr_succeeded
    - Success path does not set CAPRUN_CODING_I2_PROOF (that is Plan 02)
  </behavior>
  <action>
    Implement the LIVE-07 tracer as a single new integration test module that drives the Phase 50 product path end-to-end on real Linux (RESEARCH Pattern 1–2; PATTERNS live_acceptance section).

    **1. New file cli/caprun/tests/live_acceptance_v1_10_cli.rs**

    Module documentation (framing honesty — invert 46-MILESTONE-RECORD §1 / v1.9 composed module doc):
    - Multi-node SUCCESS is driven by real `caprun run safe-coding-workflow` as **one Session**.
    - v1.9 hybrid multi-leg `evaluate_plan_node_and_record_for_test` composition is **not** this DONE claim (keep that test as regression only).
    - Authoritative recipe:
      COMPOSE_VERIFY_CMD='cargo build --workspace &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features brokerd/mock-egress-ca' bash scripts/compose-verify.sh
    - Note cargo build --workspace first so sibling caprun-worker / caprun-exec-launcher resolve via current_exe parent.

    Framing constants (machine-checkable):
    - LIVE_07_DRIVER = "caprun run safe-coding-workflow (CLI multi-node, one Session)"
    - LIVE_07_NOT = "hybrid in-crate evaluate_plan_node_and_record_for_test composition"
    - Success test must assert LIVE_07_DRIVER is non-empty / referenced so the pin cannot be deleted silently.

    Host guard (always-on):
    - `live_acceptance_v1_10_cli_guard_present` — assert env!("CARGO_BIN_EXE_caprun") non-empty.

    Linux + mock-egress-ca module:
    - LiveCodingLayout (or equivalent): seed_test_key pattern from v1.9; policy + intent files; workspace dir with workspace.txt argv file; pre-create src/hello.txt (or chosen write path); git init + author identity + initial commit on main as needed; write path under workspace for O_TRUNC file.write.
    - Intent fields per RESEARCH Pattern 1 table (mock remote, live-07 refspec/head, staging test_command).
    - Helper run_caprun / assert_audit_passed / extract session_id and effect_id from stdout (adapt v1.9 helpers; session_id= line from main ~448).

    **2. Sidecar: drive_external_confirm_and_grant**
    - Background thread or concurrent reader on child stdout lines.
    - On first `session_id=&lt;uuid&gt;`: spawn same caprun bin `grant &lt;uuid&gt; &lt;audit.db&gt;` once (GITHUB-02 human capability — not inside run).
    - On push Block surface (`effect_id=` with sink git.push, or parse BLOCKED / pending list): spawn `confirm &lt;effect_id&gt; &lt;audit.db&gt;` for **push only**.
    - Join sidecar after child wait; fail loud on grant/confirm non-zero.
    - Do not open a second Session to finish PR; grant is session-scoped concurrent with early nodes.

    **3. LIVE-07 test body** (name e.g. `live_07_cli_multi_node_one_session_verify_chain`):
    - Spawn product argv as locked Phase 50: `run --policy &lt;P&gt; safe-coding-workflow &lt;intent.json&gt; &lt;workspace-file&gt; &lt;audit.db&gt;`.
    - Env: CAPRUN_CONFIRM=external; CAPRUN_CONFIRM_TIMEOUT_SECS=60 (or similar short bound); push + GitHub tokens as test placeholders (never assert tokens appear in audit — opaque).
    - Assert exit 0; parse single session_id; assert_audit_passed; assert terminal events git_push_succeeded + github_pr_succeeded (query audit DB or audit stdout — prefer durable events like v1.9 count_events / find_event_by_type).
    - Assert argv path used safe-coding-workflow (driver pin).
    - Optional strengthening: mock receipt ledgers — not required if durable events hold (RESEARCH open Q3).

    **4. Fixture hygiene (Pitfalls 7, 10)**
    - Pre-create write target; git add in test_command; small repo under pack-cap.
    - Push remote uses github-mock.caprun.test public-range host — never 127.0.0.1 as remote.
    - If compose rust:1 lacks `git`, document/install in the COMPOSE_VERIFY_CMD recipe only if required (Wave 0 note in RESEARCH).

    **HYG:** Zero new crates. No product src changes in this plan (Plan 02 owns proof-planner selection). No packaging.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli -- --test-threads=1</automated>
    <automated>COMPOSE_VERIFY_CMD='cargo build --workspace &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features brokerd/mock-egress-ca' bash scripts/compose-verify.sh</automated>
    <note>Host smoke is always required. Second line is LIVE authority when Docker is available; without Docker, host guard + compile green is OK but LIVE-07 Complete is forbidden until compose-verify is green.</note>
  </verify>
  <acceptance_criteria>
    - File cli/caprun/tests/live_acceptance_v1_10_cli.rs exists
    - live_acceptance_v1_10_cli_guard_present passes on host
    - Module docs + LIVE_07_DRIVER / LIVE_07_NOT constants present; success test references LIVE_07_DRIVER
    - Linux+mock-egress-ca body spawns CARGO_BIN_EXE_caprun with safe-coding-workflow argv and CAPRUN_CONFIRM=external
    - Sidecar grants once and confirms git.push only
    - On compose-verify with --features brokerd/mock-egress-ca: LIVE-07 exits 0, one session_id, caprun audit shows Chain verification: PASSED, events include git_push_succeeded and github_pr_succeeded
    - check-invariants.sh exits 0; zero new crate directories
    - If Docker unavailable on executor host: host guard + compile of test module green; LIVE-07 Complete status deferred until compose-verify green (document in SUMMARY)
  </acceptance_criteria>
  <done>One CLI-driven multi-node SUCCESS path works end-to-end under compose-verify (or is fully implemented with host guard when Docker blocked), with audit chain PASSED and single Session ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Framing machine-checks + host regression lock</name>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs — Task 1 module docs, constants, LIVE-07 body
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-RESEARCH.md — Pattern 3 framing checklist (items 1–5)
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-PATTERNS.md — Framing honesty (LIVE-07 SC2)
    - cli/caprun/tests/coding_cli.rs — host argv regression surface (must stay green)
    - cli/caprun/tests/live_acceptance_v1_9_composed.rs — remains regression-only; do not rebrand as LIVE-07
  </read_first>
  <behavior>
    - Framing unit/const test: LIVE_07_DRIVER mentions safe-coding-workflow and CLI multi-node; LIVE_07_NOT mentions hybrid composition token; success path binds to LIVE_07_DRIVER
    - Success test source/module does not use evaluate_plan_node_and_record_for_test as the multi-node SUCCESS driver (grep/source discipline in this file's LIVE-07 body)
    - Host regressions: cargo test -p caprun --test coding_cli --test stream_hold --test stream_substrate --test planner still green
    - v1.9 composed test remains present as separate binary/test name — not deleted, not claimed as LIVE-07 in this module's docs
    - Invariants Gates 1–6 green
  </behavior>
  <action>
    Strengthen LIVE-07 SC2 machine-checks and lock host regressions so hybrid rebrand cannot pass silently (RESEARCH Pattern 3).

    **1. Framing asserts in live_acceptance_v1_10_cli.rs**
    - Add a host-safe test (or const asserts in guard) that:
      - LIVE_07_DRIVER contains substrings identifying CLI multi-node + safe-coding-workflow
      - LIVE_07_NOT contains the hybrid composition token (evaluate_plan_node_and_record_for_test) as the **named non-claim**
      - Success test function (or a shared helper it calls) references LIVE_07_DRIVER so deleting the pin fails compile/link or the assert
    - In the LIVE-07 Linux body, assert the spawned argv slice includes the string safe-coding-workflow (already required by Task 1 — keep explicit).
    - Module doc must retain the invert-of-hybrid paragraph; do not soften to "also hybrid OK".

    **2. Source discipline for this file's SUCCESS path**
    - LIVE-07 success function must drive multi-node via Command::new(env!("CARGO_BIN_EXE_caprun")) only.
    - Do not call brokerd::server::evaluate_plan_node_and_record_for_test for the five coding sinks as the SUCCESS driver inside this test.
    - (v1.9 file may still use hybrid — leave it alone.)

    **3. Host regression lock (run, do not expand scope)**
    - ./scripts/check-invariants.sh
    - cargo test -p caprun --test coding_cli --test stream_hold --test stream_substrate --test planner -- --test-threads=1
    - Confirm coding_cli still does not claim LIVE SUCCESS in its module docs (Phase 50 honesty).

    **4. SUMMARY honesty**
    - When writing 51-01-SUMMARY.md: claim LIVE-07 only if compose-verify scoped command was green; otherwise state "implemented + host guard; LIVE green pending Docker compose-verify".
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --test coding_cli --test stream_hold --test stream_substrate --test planner -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Framing constants asserted on host; LIVE_07_DRIVER referenced by success path
    - LIVE-07 success body uses real binary argv safe-coding-workflow (no multi-leg hybrid driver in this file)
    - Host Phase 50/49 regression suite green; check-invariants.sh green
    - 51-01-SUMMARY.md framing language matches compose-verify status honestly
  </acceptance_criteria>
  <done>Framing is machine-checked against hybrid overclaim; host regressions and invariants stay green; LIVE-07 tracer complete for Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → real caprun binary | Untrusted test env vars + argv; product path must not auto-grant/auto-confirm |
| Parent caprun → confined worker | env_clear allowlist only; tokens stay parent/broker-side |
| Worker → broker UDS | Plan nodes only; no free-form effect path |
| Broker → mock GitHub/push TLS | mock-egress-ca admits mock hosts; SSRF public-range only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Spoofing | live_acceptance_v1_10_cli framing / DONE claim | high | mitigate | Machine-checked CLI spawn + LIVE_07_DRIVER pin; forbid hybrid multi-leg as SUCCESS driver in this module |
| T-51-02 | Elevation | dual-Session stitch after push | high | mitigate | One Session continuity via Phase 50 hold; assert single session_id owns chain |
| T-51-03 | Spoofing | auto-grant / auto-confirm in run | medium | mitigate | Sidecar-only grant/confirm; CAPRUN_CONFIRM=external polls durable pending |
| T-51-04 | Information disclosure | CAPRUN_* tokens in audit DAG | medium | mitigate | Tokens parent/broker env only; scrub_secrets production path; optional absence assert |
| T-51-05 | Tampering | EffectRequest free-form path | high | mitigate | Gate 1 check-invariants; plan nodes only |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase — N/A legitimacy gate |
</threat_model>

<verification>
- Host: `./scripts/check-invariants.sh` + `cargo test -p caprun --test live_acceptance_v1_10_cli --test coding_cli --test stream_hold --test stream_substrate --test planner -- --test-threads=1`
- LIVE authority (Docker): `COMPOSE_VERIFY_CMD='cargo build --workspace &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features brokerd/mock-egress-ca' bash scripts/compose-verify.sh`
- LIVE-07 Complete only when compose-verify scoped run is green
</verification>

<success_criteria>
- Real CLI multi-node coding SUCCESS one Session with audit PASSED under compose-verify (LIVE-07)
- Framing honesty machine-checked (not hybrid DONE)
- Host regressions + invariants green
- No product proof-planner env path yet (Plan 02)
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-01-SUMMARY.md` when done
</output>
