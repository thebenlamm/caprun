---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 09
type: execute
wave: 9
depends_on: [51-08]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_10_cli.rs
autonomous: true
gap_closure: true
requirements: [LIVE-08]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "LIVE-08 identifies the unique durable github.pr/body anchor before choosing any process_exited event, so event insertion order cannot act as the proof oracle."
    - "The process event selected by anchor.read_event_id is uniquely a process.exec event, and its event id is both the provenance-chain root and the anchor read_event_id."
    - "Duplicate dispatch protection remains explicit: the multi-node run has exactly two process_exited events, exactly one for process.exec and exactly one for git.commit."
    - "A focused host regression proves the oracle returns the same attribution when the two exit events are presented in forward and reversed order."
    - "The retained failed real-Linux log remains RED diagnostic evidence only; no LIVE-08 completion or green proof is claimed until Plan 51-04 is re-run unchanged."
  artifacts:
    - path: "cli/caprun/tests/live_acceptance_v1_10_cli.rs"
      provides: "Order-independent LIVE-08 durable-anchor attribution and a focused regression for reversed exit-event order"
  key_links:
    - from: "the unique sink_blocked github.pr/body anchor"
      to: "the genuine process.exec process_exited event"
      via: "anchor.read_event_id equality, followed by actor and provenance-root assertions"
      pattern: "read_event_id|process.exec|provenance_chain"
    - from: "the complete process_exited event set"
      to: "duplicate-dispatch protection"
      via: "exact total and per-actor cardinalities independent of vector position"
      pattern: "process.exec|git.commit"
---

<objective>
Correct the LIVE-08 test proof oracle so it attributes the blocked `github.pr` body through the anchor's durable `read_event_id`, while retaining explicit detection of duplicate or missing process/commit dispatches.

Purpose: The real multi-node LIVE-08 run intentionally emits two `process_exited` events — one from `sink:process.exec:*` and one from `sink:git.commit:*`. The current test incorrectly demands one globally and binds `process_events[0]`, so the retained real-Linux failure is a test-oracle defect rather than evidence that the product path violated LIVE-08.
Output: One test-only Rust change plus the executor-produced `51-09-SUMMARY.md`; the unchanged Plan 51-04 remains the sole owner of the authoritative real-Linux scoped and full rerun.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.codex/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-08-SUMMARY.md
@cli/caprun/tests/live_acceptance_v1_10_cli.rs
</context>

## Artifacts this plan produces

Modified:

- `cli/caprun/tests/live_acceptance_v1_10_cli.rs` — helper/regression and LIVE-08 assertions only.

Generated by execution:

- `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-09-SUMMARY.md` — exact diff, bounded host commands and results, and an explicit handoff to re-run `51-04-PLAN.md` unchanged.

Explicitly not modified: planner, worker, broker, executor, runtime, CLI production source, `51-04-PLAN.md`, `51-LIVE-SCOPED.log`, `51-LIVE-EVIDENCE.md`, `51-VALIDATION.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real-Linux CLI one-Session success plus attributable sibling I2 Block | 51-09 then 51-04 unchanged | COVERED | This plan repairs only the false proof oracle; 51-04 owns execution and evidence. |
| REQ | LIVE-08 | Policy-permitted I2 Block independently attributable to genuine process output | 51-09 | COVERED | Anchor-first selection proves exact durable attribution without temporal ordering. |
| RESEARCH | — | Preserve real CLI, one Session, genuine taint, and honest framing | 51-09 | COVERED | No product path or proof framing changes. |
| RESEARCH | — | Zero new packages/crates/sinks/mints | 51-09 | COVERED | Test-only edit; no install task. |
| CONTEXT | — | No Phase 51 CONTEXT.md decisions | — | N/A | No context artifact exists. |
| DEBUG | LIVE-08 oracle | Two intentional process_exited events invalidate global uniqueness and `[0]` selection | 51-09 | COVERED | Tasks 1-2 encode the diagnosed contract directly. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the anchor-first oracle with an order-independent regression</name>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs — Event imports/helpers and the existing LIVE-08 durable-anchor assertion block
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-LIVE-SCOPED.log — RED diagnostic evidence showing the intentional two-event shape; never treat this failed log as green proof
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md — exact unchanged authoritative real-Linux rerun contract this plan must not duplicate
  </read_first>
  <behavior>
    - "Given one github.pr/body anchor whose read_event_id names the process.exec exit, attribution selects that event even when git.commit appears first."
    - "Reversing the two exit events does not change the selected process event or its provenance-root identity."
    - "The oracle rejects missing/duplicate actor cardinality rather than silently choosing a vector position."
  </behavior>
  <action>
    Extract the smallest test-local helper needed to express LIVE-08 attribution without database or runtime changes. Its inputs must be the durable body anchor and the collected `process_exited` events (or equivalent borrowed test values). It must first use the anchor's `read_event_id` to find exactly one matching event, then validate that event's actor identifies `process.exec`, its id equals `anchor.provenance_chain.first()`, and its id equals `anchor.read_event_id`.

    Preserve duplicate-dispatch protection separately from attribution: require exactly two exit events total, exactly one actor matching `sink:process.exec:*`, and exactly one actor matching `sink:git.commit:*`. Do not restore global single-event uniqueness, do not select by index, and do not infer provenance from temporal ordering.

    Add a focused ordinary host test beside the helper using two representative exit events and the matching anchor. Exercise both original and reversed event order and assert identical process attribution. If constructing full `Event` values is disproportionately noisy, factor the helper over the minimal borrowed fields already available from `Event`; keep the contract and both order cases explicit. This is test code only: do not change production behavior or output to satisfy the oracle.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - A focused regression executes on the host without Docker and covers both forward and reversed exit-event order.
    - The helper chooses the process event by `anchor.read_event_id`, never by collection index or first/last position.
    - The helper/assertions require two exits total, one `process.exec` actor and one `git.commit` actor.
    - The selected event id equals both `anchor.read_event_id` and the first provenance-chain id, and its actor is `process.exec`.
    - Only `cli/caprun/tests/live_acceptance_v1_10_cli.rs` changes.
  </acceptance_criteria>
  <done>The diagnosed two-exit shape is covered by an order-independent host regression that still detects duplicate dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace LIVE-08 global uniqueness and positional binding with the durable oracle</name>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs — Task 1 helper/regression and `live_08_cli_mid_loop_i2_block_genuine_taint`
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md — delegated real-Linux rerun and evidence/status obligations
  </read_first>
  <behavior>
    - "The live assertion finds exactly one github.pr/body anchor before selecting its process event."
    - "The live assertion accepts the intentional process.exec plus git.commit exit pair while rejecting extra or missing actor occurrences."
    - "No green LIVE-08 claim is produced by host compilation/tests alone."
  </behavior>
  <action>
    Rewrite only the LIVE-08 assertion tail to use Task 1's contract. Find and assert the unique durable `sink_blocked` anchor for sink `github.pr` and argument `body` first. Preserve its non-empty/untrusted taint checks. Collect all `process_exited` events, assert the exact total and actor cardinalities, then select the event whose id equals `anchor.read_event_id`; assert that selected event is the `process.exec` actor and that its id is both the provenance-chain root and read-event id.

    Remove the obsolete global count of one and the positional binding to `process_events[0]`. Keep the existing `github_pr_succeeded == 0`, policy-deny exclusion, one-Session check and audit-chain verification intact. Do not edit production source, Plan 51-04, the retained failed log, evidence, requirements, validation, or windows.

    Run only bounded host verification: the focused test/target, the LIVE target compile gate, invariant checks and formatting/diff checks. Record exact commands and exit statuses in `51-09-SUMMARY.md`. State explicitly that the retained failed log is RED evidence only and that LIVE-08 remains Pending. Do not run Docker, `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh`, or a bare full-workspace test here. After this plan completes, delegate the exact scoped and full real-Linux commands, raw-log retention, evidence/status updates and human evidence checkpoint to the existing `51-04-PLAN.md`, re-run unchanged.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca -- --test-threads=1 &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; cargo fmt --all -- --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - LIVE-08 finds the unique github.pr/body anchor before selecting the process event by `anchor.read_event_id`.
    - The selected event actor is `process.exec`; its id equals both `anchor.read_event_id` and the provenance-chain root.
    - The acceptance test explicitly asserts two exit events total, with exactly one process.exec and one git.commit actor.
    - Existing one-Session, permitting-policy, no-PR-effect, untrusted-taint and valid-chain assertions remain present.
    - Bounded host tests, compile gate, Gates 1-6, rustfmt check and diff check exit 0.
    - The summary states no authoritative real-Linux proof ran, LIVE-08 remains Pending, and 51-04 must be re-run unchanged next.
    - The implementation commit contains only `cli/caprun/tests/live_acceptance_v1_10_cli.rs`; the plan completion commit may add only `51-09-SUMMARY.md`.
  </acceptance_criteria>
  <done>LIVE-08's durable attribution oracle matches the intentional multi-node event shape and is ready for the unchanged Plan 51-04 real-Linux rerun.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: high. This plan changes proof code, not product code; false-positive proof remains the primary threat.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Audit database events → test oracle | Unordered durable events must be related through explicit ids, not collection position. |
| Retained failed log → completion claim | A failed external run may diagnose the oracle but cannot establish LIVE completion. |
| Host regression → real-Linux authority | A bounded host test validates logic only; Plan 51-04 supplies compose-backed proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-09-01 | Spoofing | LIVE-08 provenance attribution | high | mitigate | Select the process event exclusively through the unique github.pr/body anchor's read_event_id and verify actor plus provenance root. |
| T-51-09-02 | Tampering | duplicate-dispatch detection | high | mitigate | Assert exactly two exits and exact one-per-actor cardinality independently of attribution selection. |
| T-51-09-03 | Repudiation | order-independence claim | medium | mitigate | Focused regression executes the same oracle with reversed event order. |
| T-51-09-04 | Spoofing | LIVE completion from failed/host-only evidence | high | mitigate | Summary labels retained log RED-only, leaves status untouched and delegates exact authority to unchanged Plan 51-04. |
| T-51-09-05 | Elevation of Privilege | product/TCB scope expansion | high | mitigate | File scope is one integration-test source; diff checks reject planner, worker, broker, executor and runtime changes. |
| T-51-09-SC | Tampering | package installation | low | accept | No package-manager install or dependency change exists in this plan. |
</threat_model>

<verification>
- Host logic: `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca -- --test-threads=1`.
- Compile gate: the same target with `--no-run`.
- Repository safety: `./scripts/check-invariants.sh`, `cargo fmt --all -- --check`, and `git diff --check`.
- Scope: implementation commit changes only `cli/caprun/tests/live_acceptance_v1_10_cli.rs`; completion adds only `51-09-SUMMARY.md`.
- Not run by this plan: Docker, `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh`, or full-workspace tests. The exact authoritative scoped/full real-Linux obligations remain in `51-04-PLAN.md`, which must be re-run unchanged after 51-09.
</verification>

<success_criteria>
- The proof oracle is anchor-first, id-bound, actor-checked and independent of event order.
- Duplicate/missing process.exec or git.commit exit events still fail deterministically.
- Host verification is green without converting the retained failure into LIVE evidence.
- No production source or evidence/status artifact changes.
- The next execution step is the unchanged Plan 51-04 real-Linux rerun.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-09-SUMMARY.md` when done.
</output>
