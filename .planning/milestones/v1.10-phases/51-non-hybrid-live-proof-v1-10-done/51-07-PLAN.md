---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 07
type: execute
wave: 7
depends_on: [51-06]
files_modified:
  - crates/brokerd/src/audit.rs
  - crates/brokerd/src/server.rs
  - crates/brokerd/src/confirmation.rs
autonomous: true
gap_closure: true
requirements: [LIVE-07, LIVE-08]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every production append to a session's audit chain chooses its parent from the durable head INSIDE the same immediate transaction that inserts the row, so an out-of-band append can no longer cause a fork."
    - "The audit database's busy timeout is owned explicitly by open_audit_db rather than inherited from a driver default, and it lands in the SAME commit as the atomic append — never alone."
    - "Both committed red regressions from Plan 51-05 pass unedited."
    - "verify_chain, current_chain_head and the four MAC functions are byte-identical to their pre-fix versions — the verifier was not loosened to make the tests pass."
    - "No line mentioning read_event_id or provenance_chain is added or removed anywhere under crates/brokerd/src/, so the provenance graph that I2 and M7 ride on is untouched."
    - "The scoped, Docker-less host regression covering the audit-chain, provenance-threading, session-integrity and grant families is green, and the Linux + fixture-gated LIVE module still compiles."
  artifacts:
    - path: "crates/brokerd/src/audit.rs"
      provides: "open_audit_db explicit busy timeout plus append_event as the transactional append-at-head choke point, per the design note"
    - path: "crates/brokerd/src/server.rs"
      provides: "Enclosing transactions that wrap an append converted to IMMEDIATE behaviour"
    - path: "crates/brokerd/src/confirmation.rs"
      provides: "The attempt-ledger enclosing transaction converted to IMMEDIATE behaviour"
  key_links:
    - from: "crates/brokerd/src/audit.rs append_event"
      to: "crates/brokerd/tests/audit_chain_fork_regression.rs"
      via: "both red regressions flip to passing without being edited"
      pattern: "broker_append_after_external_grant_must_not_fork_chain|contended_appends_from_independent_connections_stay_linear"
    - from: "planning-docs/DESIGN-audit-append-concurrency.md Appendix A"
      to: "the landed diff"
      via: "every call site classified in the inventory receives its stated treatment and no unlisted production site is touched"
      pattern: "append_event|transaction_with_behavior"
---

<objective>
Land the coordinated D1+D2 fix to the audit append path as ONE atomic commit, under the authority of the design note from Plan 51-06, and prove it green against the two red regressions from Plan 51-05 without touching the verifier or the provenance graph.

Purpose: D1 makes `verify_chain` deterministically false on the exact path v1.10 exists to ship — the mid-loop external confirm IS the feature, so there is no harness workaround. D2 must land in the same change because a busy timeout alone converts the crash into a successfully committed fork, which is strictly worse.
Output: A single revertible commit under `crates/brokerd/src/`, a red-to-green transition on both committed regressions, and mechanical evidence that neither the verifier nor the provenance graph moved.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-audit-append-concurrency.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md
@crates/brokerd/src/audit.rs
@crates/brokerd/tests/audit_chain_fork_regression.rs
</context>

## Artifacts this phase produces

New symbols (exactly one; HYG-02 zero-new-crates and zero-new-mint-sites hold):

- `crates/brokerd/src/audit.rs` — one module-level constant naming the explicit busy timeout in milliseconds, with the name pinned by the design note (`AUDIT_BUSY_TIMEOUT_MS` unless Section 5 pinned a different identifier). If the design note's Section 3 or 4 pinned an additional private helper for the transactional append, that helper is the only other permitted new symbol and must be private to the module.

Changed behaviour (no signature changes, no renames — terminology is locked):

- `brokerd::audit::open_audit_db` — sets the busy timeout explicitly in addition to the existing WAL pragma and the five migrations.
- `brokerd::audit::append_event` — same name, same parameter list, same return type; becomes the transactional append-at-head choke point per design-note Section 3.
- `crates/brokerd/src/server.rs` and `crates/brokerd/src/confirmation.rs` — the three `in-transaction` enclosing sites listed in design-note Appendix A begin their transaction with immediate behaviour instead of the default deferred behaviour.

Explicitly NOT produced: no new crate, no new cargo feature, no new environment variable, no new CLI flag, no new mint site, no `EffectRequest` token, no change to `verify_chain` / `current_chain_head` / `compute_event_hash` / `verify_event_hash` / `compute_anchor_mac` / `verify_anchor_mac`, no change to any `read_event_id` or `provenance_chain` handling, no change to `crates/executor/`, no change to any policy surface, no change to `cli/caprun/src/main.rs` output, no `51-LIVE-EVIDENCE.md`, and no edit to `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md`.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real-Linux CLI one-Session run (success + mid-loop I2 Block) | 51-07, then 51-04 unchanged | COVERED | This plan removes the deterministic blocker; 51-04 remains the authoritative proof and is NOT re-planned. |
| REQ | LIVE-07 | One Session, `verify_chain` true after the mid-loop external confirm | 51-07 | COVERED | Task 1 + Task 2. |
| REQ | LIVE-08 | Attributable mid-loop I2 Block with the chain still valid | 51-07 | COVERED | Task 2's non-conflation gate keeps the provenance attribution intact while the causal chain is repaired. |
| RESEARCH | — | Zero new crates / packages / mint sites / `EffectRequest`; HYG-02 | 51-07 | COVERED | Task 2 asserts the diff adds no `mint_from_` or `EffectRequest` token; `check-invariants.sh` Gates 1 and 3 stay green. |
| RESEARCH | — | Package Legitimacy Audit gate | 51-07 | N/A | No package-manager install task; `Cargo.toml` and `Cargo.lock` are asserted unchanged. |
| CLAUDE.md | — | Design-gate doc must precede TCB code; TCB is Rust; PLAN.md wins | 51-07 | COVERED | `depends_on: [51-06]` plus a Task 1 precondition on the design note existing. |
| DEFECT | D1 | Chain forks after any external append | 51-07 | COVERED | Task 1, verified by Task 2. |
| DEFECT | D2 | Cross-process write collision handling | 51-07 | COVERED | Task 1, in the SAME commit as D1 — never alone. |
| DEFECT | latent-1 | `record_github_grant` non-transactional read-then-append | 51-07 | COVERED | Inherits the choke point per design-note Section 8; asserted in Task 2. |
| DEFECT | latent-2 | Dual-connection same-seed head (`server.rs:307-382`) | 51-07 | COVERED | Disposition carried from design-note Section 8; asserted in Task 2. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Land the coordinated D1+D2 fix as one atomic commit</name>
  <precondition>`planning-docs/DESIGN-audit-append-concurrency.md` exists with its ten numbered sections and Appendix A, and `planning-docs/DESIGN-GATE-RECORD-v1.10.md` carries the PENDING gate row — this project's precedent is that a design-gate doc blocks TCB code, so absent either artifact this task must halt rather than proceed.</precondition>
  <files>crates/brokerd/src/audit.rs, crates/brokerd/src/server.rs, crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - planning-docs/DESIGN-audit-append-concurrency.md — the authority for this task; Sections 3, 4, 5, 6, 7, 8 and Appendix A pin exactly what changes and what must not
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — the two named wrong turns (loosening `verify_chain`; landing a bare busy timeout)
    - crates/brokerd/tests/audit_chain_fork_regression.rs — the committed acceptance oracle this task must satisfy without editing it
    - crates/brokerd/src/audit.rs lines 712-731 — `open_audit_db`'s current pragma and migration sequence
    - crates/brokerd/src/audit.rs lines 955-1045 — `append_event`: the guard, `compute_event_hash` over the serialized payload, the `events` INSERT, the count read-back and the `chain_anchor` upsert
    - crates/brokerd/src/audit.rs lines 1199-1241 — `current_chain_head`, the query the choke point calls inside its transaction
    - crates/brokerd/src/audit.rs lines 534-564 — `record_github_grant`, whose read-then-append becomes atomic through the choke point
    - crates/brokerd/src/server.rs lines 1165-1200 — the first enclosing `transaction()` site and its surrounding comments
    - crates/brokerd/src/server.rs lines 1540-1600 — the second enclosing `transaction()` site
    - crates/brokerd/src/server.rs lines 1017-1059 — the sink_blocked append plus blocked-literal plus pending-confirmation group whose atomicity must not regress
    - crates/brokerd/src/confirmation.rs lines 1150-1200 — the attempt-ledger enclosing `transaction()` site
    - crates/brokerd/src/quarantine.rs — the mint helpers that thread `parent_hash` and inherit the choke point unchanged
  </read_first>
  <behavior>
    - After this task, `broker_append_after_external_grant_must_not_fork_chain` passes unedited: a broker connection holding a stale in-memory head, appending after an out-of-band `record_github_grant`, produces one chain leaf and `verify_chain` true.
    - After this task, `contended_appends_from_independent_connections_stay_linear` passes unedited: six independent connections each appending eight events with an identical stale supplied parent all return `Ok`, produce exactly `1 + 48` rows, one leaf, and `verify_chain` true.
    - Edge case — session root: an append into a session with no events still persists `parent_id` NULL and `parent_hash` NULL, so `verify_chain`'s recursive CTE still finds its root.
    - Edge case — re-entrancy: an append issued on a connection handle that is already inside a transaction must not attempt to open a nested transaction, and must still choose its parent from the durable head read within that enclosing transaction.
    - Edge case — replay suppression: `record_github_grant`'s `INSERT OR IGNORE` plus `rows_affected == 1` gating still suppresses the authorize event on a repeated grant, so a replayed grant remains a genuine no-op.
  </behavior>
  <action>
    Implement exactly what `planning-docs/DESIGN-audit-append-concurrency.md` pins. Do not re-decide the design here; if the note is silent or contradictory on a point you need, stop and report rather than improvising a TCB change.

    In `open_audit_db`, add the explicit busy-timeout call alongside the existing `PRAGMA journal_mode=WAL` execution, using the module constant the design note named. Place it so that EVERY connection the function returns carries it, including the short-lived ones the `caprun grant`, `caprun confirm`, `caprun deny` and external-hold-poll paths open in `cli/caprun/src/main.rs`. Add a comment recording the measured fact from design-note Section 5: rusqlite 0.32.1 already applies an implicit 5000 ms timeout at `Connection::open`, so this call makes the guarantee TCB-owned and driver-version-independent, and it is NOT by itself the D1 fix.

    In `append_event`, make the append atomic and head-derived per design-note Sections 3, 4 and 6. The head read (`current_chain_head` for this event's `session_id`), the `events` INSERT and the `chain_anchor` upsert must all occur inside ONE immediate transaction, opened with `rusqlite::TransactionBehavior::Immediate` via `transaction_with_behavior`; the parent must be selected INSIDE that transaction because `parent_id` is serialized into the payload that `compute_event_hash` covers, so a parent chosen before the write lock is a parent that can go stale between choice and insert. Rebind the persisted `parent_id` and the `parent_hash` argument used for the MAC to the head read inside the transaction, keeping the event's `id` untouched. Preserve the existing `sink_blocked`-requires-anchors guard, the `event_count` read-back (never `+ 1` arithmetic) and the anchor MAC computation exactly as they are. Handle the re-entrant case per the design note: when the supplied connection handle is already inside a transaction, do not open a nested one — join the enclosing boundary and still read the head there.

    Convert the three enclosing transaction sites named in design-note Appendix A — the two in `crates/brokerd/src/server.rs` and the one in `crates/brokerd/src/confirmation.rs` — from the default deferred behaviour to immediate behaviour, so a transaction that reads before it writes cannot hit the upgrade collision SQLite deliberately refuses to retry. Leave every other statement inside those transactions unchanged.

    Do not touch `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac` or `verify_anchor_mac` — not one line. Do not add, remove or reorder any line that mentions `read_event_id` or `provenance_chain`: the causal chain and the provenance graph are two graphs and I2 and M7 ride on the second one. Do not edit `crates/brokerd/tests/audit_chain_fork_regression.rs`. Do not add a crate, a cargo feature, an environment variable or a mint site, and do not introduce the token that invariant Gate 1 forbids under `crates/`.

    Land the whole change — busy timeout, atomic append and the three immediate-transaction conversions — as ONE commit. Splitting them lets a bare busy timeout exist for one commit, which per 51-BLOCKING-DEFECTS.md turns a crash into a successfully committed fork and is strictly worse than the current state.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo build --workspace &amp;&amp; cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1` exits 0 and reports both tests passing.
    - `git diff --name-only` for this task lists exactly `crates/brokerd/src/audit.rs`, `crates/brokerd/src/server.rs`, `crates/brokerd/src/confirmation.rs` — no test file, no `Cargo.toml`, no `Cargo.lock`, no file under `cli/`.
    - `git log --oneline -n 1` shows exactly one new commit for this task, and `git show --stat HEAD` lists all three source files — the busy timeout and the atomic append are in the same commit.
    - `grep -c 'TransactionBehavior::Immediate' crates/brokerd/src/audit.rs` returns at least `1`.
    - `grep -c 'transaction_with_behavior' crates/brokerd/src/server.rs` returns `2` and the same grep on `crates/brokerd/src/confirmation.rs` returns `1`.
    - `grep -c 'busy_timeout' crates/brokerd/src/audit.rs` returns at least `1`, and the call sits inside `open_audit_db`.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED, including Gate 1 (no forbidden raw effect-to-sink token under `crates/`) and Gate 3 (mint call sites unchanged).
    - `cargo build --workspace` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">A TCB change to the tamper-evidence append path. It is revertible as one commit and changes neither the database schema, the MAC scheme nor the `chain_anchor` semantics, so a revert leaves existing audit databases readable and verifiable — costly to re-decide, not one-way.</reversibility>
  <done>Both committed red regressions pass unedited, from a single commit that lands the atomic append and the explicit busy timeout together.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the verifier and the provenance graph did not move</name>
  <precondition>Task 1 produced exactly one commit touching `crates/brokerd/src/`, and the most recent commit whose message contains `(51-06)` is still reachable as the pre-fix reference point.</precondition>
  <files>crates/brokerd/src/audit.rs, crates/brokerd/src/server.rs, crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - planning-docs/DESIGN-audit-append-concurrency.md Sections 1, 2, 8 and 9 — the verifier-unchanged rule, the two-graphs rule and the non-goals this task mechanically enforces
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — both reviewers independently named loosening `verify_chain` as the most attractive wrong turn
    - crates/brokerd/src/audit.rs — the six functions whose bodies must be byte-identical to the pre-fix versions
    - crates/brokerd/src/server.rs lines 938-982 — the two existing in-code statements that the causal head is never `read_event_id`
  </read_first>
  <action>
    Run the mechanical non-regression gates against the landed diff and record their output verbatim in the plan summary. Resolve the pre-fix reference as the most recent commit whose message contains the Plan 51-06 marker, and export the pre-fix copy of `crates/brokerd/src/audit.rs` from it.

    Gate A — verifier surface unchanged. For each of `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac` and `verify_anchor_mac`, extract the function body from both the pre-fix copy and the working tree with a column-zero `fn`-to-closing-brace range, assert the extraction is non-empty in both, and assert the two are byte-identical. A difference in any one of the six means the verifier was loosened to make the tests pass, which is the forbidden wrong turn — fail the task rather than rationalising it.

    Gate B — two graphs stay separate. Assert that the diff between the pre-fix reference and the working tree, restricted to `crates/brokerd/src/`, contains no added and no removed content line mentioning `read_event_id` or `provenance_chain`. I2 and M7 ride on the provenance graph; a fix that reads, writes or unifies it silently destroys I2, and this gate is the specific check the Plan 51-08 adversarial trace is required to repeat by hand.

    Gate C — HYG-02 surface. Assert the same diff adds no line containing a `mint_from_` token and no line containing the raw effect-to-sink type name that invariant Gate 1 forbids, and assert `Cargo.toml` and `Cargo.lock` are absent from the diff's file list.

    Gate D — latent-instance dispositions realised. Confirm, by reading the landed code against design-note Section 8, that `record_github_grant`'s read-then-append now runs under the choke point's transaction, and that no production append site chooses its persisted parent from a threaded in-memory value. Record in the summary, for each of the two latent instances, the disposition stated in the design note and the concrete evidence in the landed diff that realises it. If a disposition was `DEFERRED`, restate the reason and confirm nothing in this diff silently changed it.

    Do not modify any source file in this task. If any gate fails, report the failure and stop; do not adjust the gate to pass.
  </action>
  <verify>
    <automated>BASE=$(git log --format=%H -n 1 --grep='(51-06)') &amp;&amp; test -n "$BASE" &amp;&amp; git show "$BASE:crates/brokerd/src/audit.rs" &gt; /tmp/caprun_audit_prefix.rs &amp;&amp; for f in verify_chain current_chain_head compute_event_hash verify_event_hash compute_anchor_mac verify_anchor_mac; do a=$(sed -n "/^\(pub \)\?fn $f(/,/^}/p" /tmp/caprun_audit_prefix.rs); b=$(sed -n "/^\(pub \)\?fn $f(/,/^}/p" crates/brokerd/src/audit.rs); test -n "$a" || { echo "EXTRACTION EMPTY: $f"; exit 1; }; test "$a" = "$b" || { echo "VERIFIER FUNCTION CHANGED: $f"; exit 1; }; done &amp;&amp; ! git diff "$BASE" -- crates/brokerd/src/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -qE 'read_event_id|provenance_chain' &amp;&amp; ! git diff "$BASE" -- crates/brokerd/src/ | grep -E '^\+' | grep -qE 'mint_from_|EffectRequest' &amp;&amp; ! git diff --name-only "$BASE" | grep -qE '^Cargo\.(toml|lock)$' &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; echo "GATES A-C PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - Gate A: all six named functions extract non-empty from both the pre-fix reference and the working tree, and every pair is byte-identical; the command prints no `VERIFIER FUNCTION CHANGED` line.
    - Gate B: the restricted diff contains zero added or removed content lines mentioning `read_event_id` or `provenance_chain`.
    - Gate C: the restricted diff adds zero lines containing a `mint_from_` token or the Gate-1 forbidden type name, and `Cargo.toml` / `Cargo.lock` do not appear in the diff's file list.
    - Gate D: the plan summary records, for `record_github_grant` and for the dual-connection same-seed head, the design-note disposition and the specific landed-diff evidence that realises it — neither is omitted.
    - The command prints `GATES A-C PASSED` and exits 0.
    - `git diff --name-only` is empty for this task — no source file was modified while proving non-regression.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>The fix is mechanically shown to have repaired the causal chain without loosening the verifier, disturbing the provenance graph or widening the dependency surface.</done>
</task>

<task type="auto">
  <name>Task 3: Run the scoped Docker-less host regression and re-compile the LIVE module</name>
  <precondition>`pkg-config --version` succeeds and `cargo build --workspace` exits 0 on this host; Docker is NOT required and MUST NOT be invoked by this task.</precondition>
  <files>crates/brokerd/src/audit.rs, crates/brokerd/src/server.rs, crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - CLAUDE.md section "Linux-only security tests (critical)" — the standing rule that from Phase 16 onward all Linux verification goes through `scripts/mailpit-verify.sh`, never the bare workspace recipe, because a benign run can trigger a LIVE SMTP send
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-VALIDATION.md — the per-task verification map whose rows this regression feeds
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-04-PLAN.md — the authoritative compose-verify proof this plan must NOT duplicate, weaken or pre-empt
    - crates/brokerd/tests/extract_provenance_threading.rs — the provenance-threading regression most sensitive to a parent/provenance conflation
    - crates/brokerd/tests/durable_anchor.rs — the HARDEN-02 chain-anchor regression most sensitive to an anchor-semantics change
    - cli/caprun/tests/grant.rs — the CLI-level exercise of `record_github_grant`, the external appender at the centre of D1
  </read_first>
  <action>
    Run the scoped, SMTP-free, Docker-free regression set and record each command with its exit status in the plan summary.

    Brokerd family: `cargo test -p brokerd --test audit_chain_fork_regression --test audit_dag --test durable_anchor --test extract_provenance_threading --test harden01_session_integrity --test s9_acceptance -- --test-threads=1`. These cover the hash chain, the HARDEN-02 anchor, the provenance threading that must be untouched, session integrity, and the section 9 acceptance family.

    CLI family: `cargo test -p caprun --test grant --test audit_viewer --test planner --test proof_selector_rejection --test s9_file_write_block --test s9_process_exec_block -- --test-threads=1`. These cover the external grant appender, the audit viewer's read-only path, planner expressibility and anti-launder, the ordinary-build selector rejection, and the two section 9 block families.

    LIVE compile gate: `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run`. This compiles the Linux and fixture-gated module on this host without executing it, which is the gate that would have caught defect F1.

    Then `./scripts/check-invariants.sh` and `git diff --check`.

    Do NOT run `cargo test --workspace` on this host: CLAUDE.md forbids the bare Linux recipe from Phase 16 onward because a real `email.send` dispatch can reach a live SMTP send with no Mailpit listener present, and Mailpit requires Docker, which is absent. Do NOT run `scripts/compose-verify.sh` or `scripts/mailpit-verify.sh` — Docker is absent and the authoritative full-workspace and scoped compose runs belong to Plan 51-04, which is re-run UNCHANGED after this gap closure. If any scoped command fails, retain its full output in the plan summary, stop, and do not adjust the command set to make it pass.

    Record in the summary that the full-workspace regression remains unrun on this host and is Plan 51-04's obligation, and that broken windows 1, 2 and 3 remain open. Do not touch the windows ledger, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, or `51-VALIDATION.md`.
  </action>
  <verify>
    <automated>cargo test -p brokerd --test audit_chain_fork_regression --test audit_dag --test durable_anchor --test extract_provenance_threading --test harden01_session_integrity --test s9_acceptance -- --test-threads=1 &amp;&amp; cargo test -p caprun --test grant --test audit_viewer --test planner --test proof_selector_rejection --test s9_file_write_block --test s9_process_exec_block -- --test-threads=1 &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run &amp;&amp; ./scripts/check-invariants.sh &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the automated verify exits 0, and the summary lists each command with its observed exit status.
    - `cargo test -p brokerd --test extract_provenance_threading` passes — the provenance threading is intact after the causal-chain change.
    - `cargo test -p brokerd --test durable_anchor` passes — HARDEN-02 anchor semantics and tail-truncation detection are intact.
    - `cargo test -p caprun --test grant` passes — the external `caprun grant` appender still works under the choke point, including its replay-suppression no-op.
    - `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run` exits 0.
    - Neither `scripts/compose-verify.sh`, `scripts/mailpit-verify.sh`, `docker`, nor `cargo test --workspace` appears in any command this task runs.
    - `git diff --name-only` is empty for this task, and `.planning/WINDOWS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` and `51-VALIDATION.md` are unmodified.
    - The summary states explicitly that the full-workspace compose regression is unrun on this host and remains Plan 51-04's obligation.
  </acceptance_criteria>
  <done>The scoped Docker-less regression is green across the audit-chain, provenance, anchor, session-integrity and grant families, and the LIVE module still compiles under its Linux and fixture gates.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: **high**. This plan modifies the Trusted Computing Base — the audit tamper-evidence append path — so every high threat below carries a `mitigate` disposition realised by a mechanical gate in Task 2 or Task 3, not by reviewer intention.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Out-of-band CLI process → shared audit SQLite file | `caprun grant` / `caprun confirm` / `caprun deny` append to the same database the broker is writing; this is the boundary D1 lives on and the one the immediate transaction now serialises. |
| Causal `parent_id` chain ↔ provenance `read_event_id` graph | Two graphs that must never be equated; I2 (value injection) and M7 ride on the second. |
| Landed diff → phase completion claim | A fix that loosens the verifier would make the tests pass while deleting HARDEN-02 tail-truncation detection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-07-01 | Tampering | HARDEN-02 tail-truncation detection | high | mitigate | Task 2 Gate A byte-compares the six verifier and MAC functions against the pre-fix reference; any difference fails the task. |
| T-51-07-02 | Tampering | I2 value-injection enforcement via the provenance graph | high | mitigate | Task 2 Gate B fails if any added or removed line under `crates/brokerd/src/` mentions `read_event_id` or `provenance_chain`; Task 3 additionally runs `extract_provenance_threading`. |
| T-51-07-03 | Tampering | audit chain integrity across processes | high | mitigate | The head is read and the row inserted inside one `BEGIN IMMEDIATE` transaction so the parent cannot go stale between choice and insert; proven by the two committed regressions. |
| T-51-07-04 | Denial of service | broker killed by an unhandled `SQLITE_BUSY` | high | mitigate | The explicit busy timeout is set in `open_audit_db` so every connection inherits it, and the three enclosing transactions become immediate so SQLite's non-retryable upgrade collision cannot arise. |
| T-51-07-05 | Elevation of privilege | committing a bare busy timeout without the atomic append | high | mitigate | Acceptance criteria require exactly ONE commit containing all three files; a timeout-only commit would convert a crash into a committed fork. |
| T-51-07-06 | Repudiation | atomicity of the sink_blocked group | medium | mitigate | Design-note Section 7 pins that atomicity must not regress; Task 3 runs the section 9 block families that exercise the group end to end. |
| T-51-07-07 | Spoofing | claiming real-Linux proof from a scoped host run | high | mitigate | Task 3 forbids `compose-verify.sh`, `mailpit-verify.sh`, `docker` and `cargo test --workspace`, and requires the summary to state the full regression is unrun and owned by Plan 51-04. |
| T-51-07-SC | Tampering | dependency / package surface | high | mitigate | Task 2 Gate C asserts no `Cargo.toml` / `Cargo.lock` change and no new `mint_from_` or forbidden effect-request token; `check-invariants.sh` Gates 1, 3, 4 and 5 stay green. |
</threat_model>

<verification>
- `./scripts/check-invariants.sh` — Gates 1-6 green after every task.
- `cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1` — both Plan 51-05 regressions pass unedited.
- Task 2 Gates A-D — verifier byte-identical, provenance graph untouched, dependency surface unchanged, latent-instance dispositions realised.
- Scoped Docker-less regression across the brokerd audit/anchor/provenance/integrity/section-9 families and the CLI grant/viewer/planner/selector/block families.
- `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run` — the Linux and fixture-gated LIVE module compiles.
- NOT run here: `cargo test --workspace` (CLAUDE.md Mailpit rule; Docker absent), `scripts/compose-verify.sh` and `scripts/mailpit-verify.sh` (Docker absent). The authoritative real-Linux proof is Plan 51-04, re-run UNCHANGED after Plan 51-08 clears.
</verification>

<success_criteria>
- One atomic commit lands the immediate-transaction append-at-head and the explicit busy timeout together across `audit.rs`, `server.rs` and `confirmation.rs`.
- Both committed red regressions pass without being edited.
- The six verifier and MAC functions are byte-identical to their pre-fix versions.
- No line mentioning `read_event_id` or `provenance_chain` is added or removed under `crates/brokerd/src/`.
- Both latent instances have their design-note dispositions realised and recorded with landed-diff evidence.
- The scoped Docker-less regression and the LIVE-module compile gate are green; `check-invariants.sh` Gates 1-6 stay green.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-07-SUMMARY.md` when done, including the commit SHA of the single fix commit — Plan 51-08's adversarial trace needs it as the diff range. Do NOT create `51-LIVE-EVIDENCE.md`, do NOT edit `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md` or `51-VALIDATION.md`, and do NOT modify `51-04-PLAN.md`. LIVE-07 and LIVE-08 stay `Pending` and broken windows 1, 2 and 3 stay `open`.
</output>
