---
phase: 47-multi-step-plan-stream-design-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - planning-docs/DESIGN-multi-step-plan-stream.md
  - planning-docs/DESIGN-GATE-RECORD-v1.10.md
autonomous: false
requirements:
  - DESIGN-20
  - HYG-02

must_haves:
  truths:
    - "A FRESH, NON-SELF adversarial reviewer (distinct agent/context from the DESIGN author — Fable-5 or project-standard model) code-traced planning-docs/DESIGN-multi-step-plan-stream.md against live crates/{executor,brokerd,sandbox,runtime-core} + cli/caprun — not a prose skim (DESIGN-20 concurrency: author≠reviewer)."
    - "Every finding was independently re-verified against live code before folding; accepted findings resolve by TIGHTENING the design (no invariant weakened)."
    - "planning-docs/DESIGN-GATE-RECORD-v1.10.md exists with status CLEARED (or BLOCKED if unresolved BLOCKER), reviewer identity and independence, files opened, findings table, re-run triggers, no-TCB-code reconfirmation."
    - "DESIGN-20 re-run triggers are documented: trace re-runs if stream shape, confirm-hold, or trusted-arg mint path pivots mid-implementation (DESIGN-20 idempotency: not silent re-use of an old CLEARED after a pivot)."
    - "HYG-02 reconfirmed at gate clear: check-invariants.sh exit 0; Gate 3 adjacency unchanged or explicit DESIGN amend; zero multi-step TCB code under crates/{executor,brokerd,sandbox,runtime-core} or worker submit/confirm-hold in cli/caprun."
    - "git status --porcelain -- crates cli is empty throughout; mechanism names for multi-step appear only in planning-docs prose, not as landed TCB code."
  artifacts:
    - path: planning-docs/DESIGN-GATE-RECORD-v1.10.md
      provides: "DESIGN-20 clearance authorizing Phases 48–52 multi-step TCB work"
      contains: "CLEARED/BLOCKED status, independence proof, findings ledger, no-TCB reconfirmation, re-run triggers"
    - path: planning-docs/DESIGN-multi-step-plan-stream.md
      provides: "Round-1 amendments (if any) folded from adversarial findings"
      contains: "Amendments (post-review) entries tightening pins"
  key_links:
    - "Gate CLEARED is the sole hard authorization for Phases 48–52 multi-step TCB / worker submit / confirm-hold code."
    - "Reviewer is distinct from author; gate record lists files opened and ties each finding to a DESIGN § resolution."
    - "Top pressure-test surfaces (output_value_id laundering, ProvideIntent remint, Draft×CommitIrreversible weaken, Session-split after confirm, batch/effect-path bypass, hybrid DONE framing) appear in Verified-as-sound or findings ledger."
  prohibitions:
    - statement: "No gsd-executor self-read counts as DESIGN-20 clearance"
      status: must-hold
      verification: "grep -qiE 'reviewer|independence|non-self|Fable' planning-docs/DESIGN-GATE-RECORD-v1.10.md && grep -qiE 'author|distinct|not the author' planning-docs/DESIGN-GATE-RECORD-v1.10.md"
    - statement: "No multi-step TCB code before CLEARED"
      status: must-hold
      verification: "test -z \"$(git status --porcelain -- crates cli)\""
    - statement: "No silent re-use of prior CLEARED after stream/confirm/mint pivot without re-run"
      status: must-hold
      verification: "grep -qiE 're-run|re-runs' planning-docs/DESIGN-GATE-RECORD-v1.10.md"
    - statement: "No EffectRequest under crates/"
      status: must-hold
      verification: "./scripts/check-invariants.sh"
    - statement: "No batch DAG authorize or Draft CommitIrreversible weaken introduced by fold amendments"
      status: must-hold
      verification: "grep -qiE 'not batch|batch.*reject|no batch' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'CommitIrreversible|Draft' planning-docs/DESIGN-multi-step-plan-stream.md"
---

<objective>
Clear the v1.10 multi-step plan-stream design gate (DESIGN-20): run a FRESH, NON-SELF adversarial code-trace of `planning-docs/DESIGN-multi-step-plan-stream.md` (authored in Plan 47-01), fold every accepted finding by tightening the design, and record clearance in `planning-docs/DESIGN-GATE-RECORD-v1.10.md`.

Purpose: This is THE hard gate. Per CLAUDE.md, STATE.md, and unbroken precedent (v1.0 P2 … v1.9 P41), NO multi-step TCB change in `crates/{executor,brokerd,sandbox,runtime-core}` or the worker submit/confirm-hold path in `cli/caprun` may land until this record reads CLEARED. The `fresh-context-adversarial-review` discipline is load-bearing — v1.9 caught a BLOCKER-level I0 escape that plan-checker + green docs-only invariants both missed.

Output: `planning-docs/DESIGN-GATE-RECORD-v1.10.md` (CLEARED), plus any Round-1 amendments folded into `planning-docs/DESIGN-multi-step-plan-stream.md`.

**DOC-ONLY. Write NO code under `crates/` or multi-step worker paths in `cli/`. Git diff must touch only `planning-docs/` (and optional `.planning/` summary).**
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-multi-step-plan-stream-design-gate/47-RESEARCH.md
@.planning/phases/47-multi-step-plan-stream-design-gate/47-PATTERNS.md
@CLAUDE.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@planning-docs/DESIGN-GATE-RECORD-v1.9.md
@planning-docs/DESIGN-GATE-RECORD-v1.8.md
</context>

<critical_reviewer_independence_note>
**ORCHESTRATOR-OWNED SPAWN (MANDATORY).** The review spawn is ORCHESTRATOR-OWNED per DESIGN-20, STATE.md standing mitigations, and unbroken design-gate precedent. 

- The executor of THIS plan MUST spawn a SEPARATE agent as the reviewer via the Agent/Task tool — a distinct **Claude Fable-5** model (`fable`) or the project-standard independent reviewer model, a different model family/context from the doc's authoring context.
- The executor MUST NOT perform the review itself by reading the DESIGN doc as if that were DESIGN-20 clearance (a self-read fails the `fresh-context-adversarial-review` discipline).
- If `advisor` is used and unavailable, fall back to `Agent(model:"fable")` per `advisor-tool-unavailable-fable-fallback` precedent.
- If the Agent tool is unavailable in this runtime, HALT and surface to the human orchestrator — do NOT substitute a self-read. Human adversarial review is an acceptable fallback only when recorded as non-author identity in the gate record.
- `autonomous: false` on this plan: independence cannot be auto-advanced past a failed spawn.
- Gate record MUST name the reviewer agent and prove independence (author≠reviewer, code-traced not prose-read, files-opened listed).
</critical_reviewer_independence_note>

<artifacts_this_phase_produces>
This plan (47-02) produces:

**`planning-docs/DESIGN-GATE-RECORD-v1.10.md`** — modeled on `DESIGN-GATE-RECORD-v1.9.md` / `-v1.8.md`, with: Header (milestone v1.10, document under review `DESIGN-multi-step-plan-stream.md`, gate purpose = authorize/block Phases 48–52 multi-step TCB code, requirements gated DESIGN-19 + DESIGN-20 + HYG-02 reconfirm); Gate status; Reviewer identity and independence (files-opened list); Revision History; Findings and resolutions; Verified-as-sound subsection; Re-run triggers; No-TCB-code reconfirmation; Verdict authorizing Phases 48–52.

Plus Round-1 amendments (if any) in **`planning-docs/DESIGN-multi-step-plan-stream.md`**.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Spawn a fresh non-self Fable-5 adversarial reviewer to code-trace the DESIGN doc</name>
  <precondition>planning-docs/DESIGN-multi-step-plan-stream.md exists from Plan 47-01 with §0–§14 greppable pins</precondition>
  <files>(no file writes this task — spawns a reviewer subagent and captures its findings)</files>
  <read_first>
    - planning-docs/DESIGN-multi-step-plan-stream.md — assemble the reviewer brief from this text; do NOT treat your own read as the adversarial review.
    - planning-docs/DESIGN-GATE-RECORD-v1.9.md — MODEL independence bar, findings table, Verified-as-sound, no-TCB reconfirmation.
    - .planning/phases/47-multi-step-plan-stream-design-gate/47-RESEARCH.md — section "What the adversarial reviewer must pressure-test (Phase 47 specific)" — the 10 attack surfaces.
    - .planning/phases/47-multi-step-plan-stream-design-gate/47-PATTERNS.md — ordered code files the reviewer brief must list.
  </read_first>
  <action>
    Spawn a SEPARATE Fable-5 (or project-standard independent) agent via the Agent/Task tool as the adversarial reviewer. Do NOT review the doc yourself for clearance purposes. Give a self-contained brief that provides ONLY: (1) the full text of planning-docs/DESIGN-multi-step-plan-stream.md, and (2) the instruction to CODE-TRACE every load-bearing file:line claim against the actual repo — opening and reading at minimum: cli/caprun/src/planner.rs, cli/caprun/src/worker.rs, crates/brokerd/src/proto.rs, crates/brokerd/src/server.rs, crates/brokerd/src/confirmation.rs, crates/runtime-core/src/plan_node.rs, crates/runtime-core/src/executor_decision.rs, crates/runtime-core/src/intent.rs, crates/executor/src/sink_sensitivity.rs, crates/executor/src/lib.rs (I2 / Draft class gate), scripts/check-invariants.sh, planning-docs/DESIGN-plan-executor.md, planning-docs/DESIGN-confirmation-release.md — not a prose skim.

    Direct the reviewer to specifically attack the highest-consequence Phase 47 surfaces (from RESEARCH):
    (1) Cross-node taint laundering via output_value_id — does the handle bag preserve genuine taint/provenance without trust upgrade; is planner mint/retype impossible by construction.
    (2) ProvideIntent reopened mid-stream / second declare-trusted verb — cite intent_provided / fd_requested guards.
    (3) Draft demotion fixed by weakening CommitIrreversible Step 0.5 — success path vs LIVE-08 deliberate taint path.
    (4) Batch authorize / free-form effect path / new effect path — Gate 1 + sequential SubmitPlanNode only.
    (5) Mid-loop confirm Session split / reconnect-remint / session-wide waiver — Block-and-Hold + confirm-release no re-submit.
    (6) Instruction channel collapsed into bindable ValueId — PLAN-03 / task_instruction.
    (7) Policy mid-stream rebind or I2 override — POLICY-02/03.
    (8) Hybrid composition framed as CLI multi-step DONE — LIVE acceptance honesty.
    (9) New mint site outside Gate 3 — default zero; adjacency no silent merge.
    (10) P33/P34 confirm-release order under multi-confirm Session.

    Also require I0/I1/I2 non-bypass, always-confirm git.push as mid-loop hold, and empty multi-node / deny-abort fail-closed pins. Instruct return of findings by severity (BLOCKER / MAJOR / MINOR / NIT) with file:line evidence for each claim.

    Capture raw findings verbatim for Task 2. Record reviewer model/identity, effort (tokens/tool-uses if available), and exact list of files it reported opening (needed for gate record independence section).

    If Agent/Task tool is unavailable: HALT with a clear message for the human orchestrator — do not substitute self-review.
  </action>
  <verify>
    <automated>echo "manual-process: reviewer findings + identity + files-opened captured for gate record; self-read is not sufficient"</automated>
  </verify>
  <acceptance_criteria>
    - A distinct Fable-5 (or project-standard) reviewer agent was spawned (NOT a self-read); identity/model is recorded.
    - Reviewer brief provided the DESIGN doc and directed a code-trace against the named crates/ + cli files, with all 10 Phase-47 attack surfaces called out.
    - Reviewer returned severity-tagged findings with file:line evidence; raw findings and files-opened list captured for Tasks 2–3.
    - No file under crates/ or cli/ is modified.
  </acceptance_criteria>
  <done>Fresh non-self reviewer code-traced the DESIGN and returned severity-tagged findings with code evidence; independence metadata captured.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify each finding against live code and fold accepted findings into the DESIGN doc</name>
  <files>planning-docs/DESIGN-multi-step-plan-stream.md</files>
  <read_first>
    - Captured reviewer findings from Task 1.
    - planning-docs/DESIGN-multi-step-plan-stream.md — amend Round-1.
    - planning-docs/DESIGN-GATE-RECORD-v1.9.md — findings re-verified against live code before fold discipline.
    - Each code file:line a finding cites — open and re-verify (AI reviewers generate false positives).
  </read_first>
  <action>
    For each finding, INDEPENDENTLY re-verify its claim against the live code it cites (open exact file:line) before acting. Mark each CONFIRMED REAL or dismissed false-positive with a one-line reason. For each CONFIRMED finding, fold into the DESIGN as a Round-1 amendment: tighten the relevant § so the pinned mechanism absorbs the fix; add an Amendments (post-review) entry recording what changed and why. Do NOT weaken any invariant to make a finding go away — resolve by TIGHTENING (v1.9 precedent: BLOCKER fixed by pinning distinct CommitIrreversible sink id, not by relaxing I0).

    Keep a per-finding resolution ledger (Claim / re-verify outcome / DESIGN § amended) for Task 3. After folding, re-run scripts/check-invariants.sh (exit 0) and confirm git status --porcelain -- crates cli is empty.

    If a CONFIRMED BLOCKER cannot be resolved by a design amendment, leave it open for Task 3 to mark the gate BLOCKED — do not clear past an open blocker.
  </action>
  <verify>
    <automated>bash scripts/check-invariants.sh && test -z "$(git status --porcelain -- crates cli)" && echo "invariants green, no TCB code touched"</automated>
  </verify>
  <acceptance_criteria>
    - Every reviewer finding independently re-verified and marked CONFIRMED or false-positive with reason.
    - Every CONFIRMED finding resolved as Round-1 amendment by TIGHTENING the design (or left open as BLOCKER if unresolvable); Amendments section records changes.
    - Per-finding resolution ledger captured for the gate record.
    - check-invariants.sh exit 0; crates/cli porcelain empty; no batch-authorize or Draft-weaken introduced by amendments.
  </acceptance_criteria>
  <done>Findings re-verified; accepted ones folded as design-tightening amendments; no invariant weakened; no TCB code.</done>
</task>

<task type="auto">
  <name>Task 3: Write DESIGN-GATE-RECORD-v1.10.md and reconfirm no TCB code</name>
  <reversibility rating="costly">CLEARED authorizes Phases 48–52 multi-step TCB work; a wrong clear is costly to reverse once substrate code lands, though the record itself can be amended and the trace re-run.</reversibility>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.10.md</files>
  <read_first>
    - planning-docs/DESIGN-GATE-RECORD-v1.9.md and planning-docs/DESIGN-GATE-RECORD-v1.8.md — MODEL full structure.
    - planning-docs/DESIGN-multi-step-plan-stream.md — post-amendment text; confirm DESIGN-20 re-run triggers still present in §13.
    - Per-finding resolution ledger and reviewer independence metadata from Tasks 1–2.
    - .planning/REQUIREMENTS.md — DESIGN-20 + HYG-02 text the record must satisfy.
  </read_first>
  <action>
    Create planning-docs/DESIGN-GATE-RECORD-v1.10.md modeled on v1.9/v1.8 records:

    Header — Phase 47; DESIGN under review DESIGN-multi-step-plan-stream.md; requirements gated DESIGN-19, DESIGN-20, HYG-02 reconfirm; gate purpose authorize/block Phases 48–52 multi-step TCB / worker submit / confirm-hold.

    Gate discipline restated — orchestrator-owned, non-self, unbroken v1.0 P2 → v1.9 P41 → v1.10 P47; gsd-executor does not self-clear.

    Gate status — CLEARED or BLOCKED, round number, date. Only CLEARED if every finding resolved (folded or dismissed-as-false-positive) and no BLOCKER remains open.

    Reviewer identity and independence — distinct Fable-5 (or project-standard) agent, explicitly NOT the authoring context, code-traced not prose-read, EXACT files-opened list, effort if available.

    Revision History table — round / date / reviewer / findings-by-severity / result.

    Findings and resolutions — per finding: Claim, Code evidence with file:line re-verified, Resolution naming DESIGN § that absorbed it (or dismissed false-positive reason).

    Verified as sound — load-bearing claims the reviewer confirmed by tracing real code; at minimum the 10 Phase-47 attack surfaces (or explicit finding if unsound then fixed). Evidence the review was a genuine code-trace.

    Re-run triggers (DESIGN-20 idempotency) — document that the adversarial trace RE-RUNS if stream shape, confirm-hold mechanism, or trusted-arg mint path changes mid-implementation (Phases 48–50). An old CLEARED must not be silently re-used after such a pivot.

    No-TCB-code reconfirmation — git status --porcelain -- crates cli empty; multi-step stream/handle/hold symbols appear ONLY in planning-docs prose this phase; check-invariants.sh exit 0; HYG-02 Gate 3 adjacency reconfirmed (unchanged or explicit DESIGN amend only).

    Verdict — if CLEARED, authorize Phases 48–52 multi-step TCB work; if BLOCKED, stop and do not authorize.

    Finally: confirm whole-phase diff touches only planning-docs/ and .planning/; run check-invariants.sh once more.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.10.md && grep -qiE 'CLEARED|BLOCKED|APPROVE' planning-docs/DESIGN-GATE-RECORD-v1.10.md && grep -qiE 'reviewer|independence|non-self|Fable' planning-docs/DESIGN-GATE-RECORD-v1.10.md && grep -qiE 're-run|re-runs' planning-docs/DESIGN-GATE-RECORD-v1.10.md && test -z "$(git status --porcelain -- crates cli)" && bash scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md exists with all required sections (header, discipline, status, independence + files-opened, revision history, findings, Verified-as-sound, re-run triggers, no-TCB reconfirmation, verdict).
    - Gate status is CLEARED only if every finding resolved and no BLOCKER open; distinct non-self reviewer named; author≠reviewer documented.
    - DESIGN-20 re-run triggers (stream shape / confirm-hold / trusted-arg mint path) documented in the record.
    - HYG-02 reconfirmed; crates/cli porcelain empty; check-invariants.sh exit 0.
  </acceptance_criteria>
  <done>Gate record exists, reads CLEARED (or BLOCKED with open blocker), documents non-self code-trace + re-run triggers + zero TCB code; Phases 48–52 authorization state is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DESIGN doc → adversarial reviewer | Claims cross to an independent agent that must verify them against real code, not accept prose at face value. |
| reviewer findings → fold | Findings (possibly false positives) cross into design amendments; each must be re-verified against live code before folding. |
| gate record → Phases 48–52 | A CLEARED record is the sole authorization for multi-step TCB code; a wrongly-cleared gate would let laundering valves, Session-split, or Draft-weaken proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47R-01 | Spoofing | reviewer independence | high | mitigate | Reviewer MUST be distinct non-self agent (not self-read); gate record proves author≠reviewer, files-opened listed, code-traced; autonomous false; halt if Agent tool missing. |
| T-47R-02 | Repudiation | finding resolution | high | mitigate | Per-finding ledger ties each claim to re-verified code evidence and amended DESIGN §; no silent drop. |
| T-47R-03 | Tampering | gate clearance | critical | mitigate | CLEARED only if no BLOCKER open and every finding resolved by tightening (never weakening); else BLOCKED. |
| T-47R-04 | Elevation of Privilege | TCB-code leak this phase | critical | mitigate | No-TCB-code reconfirmation: crates/cli porcelain empty; multi-step symbols only in DESIGN prose. |
| T-47R-05 | Tampering | silent re-use of CLEARED after pivot | high | mitigate | Record documents re-run triggers (stream shape / confirm-hold / trusted-arg mint path). |
| T-47R-06 | Information Disclosure | unreviewed top attack surfaces | critical | mitigate | Verified-as-sound must cover the 10 Phase-47 pressure-test surfaces (or findings that fixed them). |
| T-47R-SC | Tampering | cargo installs | low | accept | No package installs this phase (doc + review only). |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-GATE-RECORD-v1.10.md`
- Record reads CLEARED (or BLOCKED), names distinct non-self reviewer, lists files opened, ties findings to DESIGN resolutions, documents re-run triggers, includes Verified-as-sound for top attack surfaces.
- `test -z "$(git status --porcelain -- crates cli)"`
- `bash scripts/check-invariants.sh` exits 0
</verification>

<success_criteria>
- Fresh non-self adversarial code-trace of DESIGN-multi-step-plan-stream.md cleared the doc with every finding resolved (DESIGN-20).
- DESIGN-GATE-RECORD-v1.10.md records clearance, independence, re-run triggers, HYG-02 reconfirm, no-TCB reconfirmation.
- No multi-step TCB code written; phase diff touches only planning-docs/ and .planning/.
- Phases 48–52 multi-step TCB work authorized only if gate is CLEARED.
</success_criteria>

<output>
Create `.planning/phases/47-multi-step-plan-stream-design-gate/47-02-SUMMARY.md` when done.
</output>
