---
phase: 47-multi-step-plan-stream-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-multi-step-plan-stream.md
autonomous: true
requirements:
  - DESIGN-19
  - HYG-02

must_haves:
  truths:
    - "planning-docs/DESIGN-multi-step-plan-stream.md exists and pins multi-step orchestration as DECISIONS (not options): sequential plan-stream on the existing Planner seam (additive multi-node — not batch DAG authorize), worker sequential submit + opaque ValueId handle bag, mid-loop Block-and-Hold same Session, trusted-intent success path without weakening CommitIrreversible Draft denies, instruction vs value channel disjointness, and mid-stream deny/abort (abort remaining) — per DESIGN-19."
    - "ProvideIntent is pinned exactly once before RequestFd for the Session; re-ProvideIntent mid-stream is denied (DESIGN-19 idempotency edge)."
    - "Block-and-Hold pins same Session, same policy bind, same audit chain; no dual-Session stitch, no reconnect-remint resume, no session-wide confirm waiver (DESIGN-19 concurrency edge)."
    - "Sequential submit order is pinned; on Deny or policy_deny remaining nodes abort fail-closed with durable terminal events (HYG-02 ordering edge)."
    - "Empty multi-node stream is rejected or N/A; default zero new crates; default zero new mint sites (HYG-02 empty edge)."
    - "Gate 3 mint-site list is unchanged or explicitly amended — no silent merge with existing loci (HYG-02 adjacency edge)."
    - "HYG-02 is re-asserted: zero new crates unless design-gate-justified (default zero); Gate 1/3 discipline; check-invariants remains architectural gate; compose-verify remains authoritative Linux gate."
    - "Carry-forward locked: ProvideIntent-once, P33/P34 precheck-before-burn + terminal-event-before-state, POLICY-02 non-bypass of I2 (I2 hardcoded unconditional on policy-permitted calls)."
    - "Doc declares DESIGN-20: fresh non-self orchestrator-owned adversarial code-trace with re-run triggers (stream shape, confirm-hold, or trusted-arg mint path pivot)."
    - "No multi-step TCB code: git status --porcelain -- crates cli is empty; check-invariants.sh exits 0."
  artifacts:
    - path: planning-docs/DESIGN-multi-step-plan-stream.md
      provides: "Pinned multi-step plan-stream design contract (§0–§14) unlocking Phases 48–52 after DESIGN-20 clear"
      contains: "plan-stream shape, handle bag, Block-and-Hold, trusted-intent success path, PLAN-03 channels, deny/abort, HYG-02, threat model, DESIGN-20 gate"
  key_links:
    - "Stream shape = N× sequential SubmitPlanNode on one worker connection (broker already multi-submit legal) + additive Planner surface — never batch authorize."
    - "Handle bag stores only opaque ValueIds from PlanNodeDecision.output_value_id; planner never mints; no mid-stream ProvideIntent."
    - "Block-and-Hold: on BlockedPendingConfirmation worker holds same Session; human acts on durable PendingConfirmation; blocked node is not re-submitted."
    - "Success path args minted once at ProvideIntent from operator-typed intent; Draft×CommitIrreversible Step 0.5 stays; LIVE DONE requires CLI multi-node one Session."
  prohibitions:
    - statement: "No multi-step TCB code under crates/{executor,brokerd,sandbox,runtime-core} or worker submit/confirm-hold in cli/caprun before DESIGN-20 CLEARED"
      status: must-hold
      verification: "test -z \"$(git status --porcelain -- crates cli)\""
    - statement: "No EffectRequest path under crates/ (Gate 1)"
      status: must-hold
      verification: "./scripts/check-invariants.sh"
    - statement: "No batch DAG authorize as the multi-step product path"
      status: must-hold
      verification: "grep -qiE 'not batch|batch.*reject|no batch' planning-docs/DESIGN-multi-step-plan-stream.md"
    - statement: "No weakening CommitIrreversible Draft denies to green multi-file RequestFd demotion"
      status: must-hold
      verification: "grep -qiE 'CommitIrreversible|Draft' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'trusted-intent|ProvideIntent' planning-docs/DESIGN-multi-step-plan-stream.md"
    - statement: "No gsd-executor self-review as DESIGN-20 clearance in this plan"
      status: must-hold
      verification: "grep -qiE 'orchestrator-owned|non-self' planning-docs/DESIGN-multi-step-plan-stream.md"
---

<objective>
Author ONE design-gate document — `planning-docs/DESIGN-multi-step-plan-stream.md` — that pins every DESIGN-19 multi-step orchestration mechanism as a DECISION (not options) and re-asserts HYG-02 Gate discipline before any multi-step TCB code lands.

**DOC-ONLY.** Write NO code under `crates/{executor,brokerd,sandbox,runtime-core}` or the worker submit/confirm-hold path in `cli/caprun`. The git diff for this plan must touch only `planning-docs/` (plus optional `.planning/` summary).

Purpose: Phase 47 hard-blocks Phases 48–52. Through v1.9 the broker already accepts N× `SubmitPlanNode`, `output_value_id` is wired (and discarded by the worker), and confirm is durable single-shot — but the worker is one-shot and `caprun run` is single-node. This doc pins composition rules so Phases 48–52 cannot invent laundering valves mid-code.

Output: `planning-docs/DESIGN-multi-step-plan-stream.md` with RESEARCH outline §0–§14, every load-bearing claim cited to live `file:line`, and DESIGN-20 declared as orchestrator-owned (this plan does NOT run the adversarial trace).
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-multi-step-plan-stream-design-gate/47-RESEARCH.md
@.planning/phases/47-multi-step-plan-stream-design-gate/47-PATTERNS.md
@.planning/phases/47-multi-step-plan-stream-design-gate/47-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-v1.9-egress-policy.md
@planning-docs/DESIGN-plan-executor.md
@planning-docs/DESIGN-confirmation-release.md
@planning-docs/DESIGN-session-trust-coherence.md
@planning-docs/DESIGN-GATE-RECORD-v1.9.md
</context>

<artifacts_this_phase_produces>
This plan (47-01) produces exactly one file:

**`planning-docs/DESIGN-multi-step-plan-stream.md`** — the reviewed multi-step plan-stream design contract. Must pin: (a) plan-stream shape on existing Planner seam; (b) worker sequential submit + handle bag; (c) Block-and-Hold confirm continuity; (d) I1×coding-loop bounds; (e) instruction vs value channels; (f) deny/abort mid-stream; carry-forward ProvideIntent-once / Gate 3 / P33/P34 / POLICY-02; HYG-02; §-per-pitfall threat model; invariant checklist; fail-closed defaults; new-symbol summary (expect none); DESIGN-20 orchestrator-owned gate + re-run triggers; acceptance predicate. Amendments placeholder for Plan 47-02 fold.

Phase 47 overall also produces (Plan 47-02): `planning-docs/DESIGN-GATE-RECORD-v1.10.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Author DESIGN §0–§6 — all core DESIGN-19 mechanism pins end-to-end</name>
  <reversibility rating="costly">Stream shape, handle bag, and Block-and-Hold pins become the contract Phases 48–50 implement; wrong pins force coordinated redesign across worker/CLI/LIVE, not a one-file swap.</reversibility>
  <files>planning-docs/DESIGN-multi-step-plan-stream.md</files>
  <read_first>
    - .planning/phases/47-multi-step-plan-stream-design-gate/47-RESEARCH.md — AUTHORITATIVE outline §0–§14, Patterns 1–5, open-question recommended defaults (A1–A4, Open Questions 1–6), pitfalls, code citation targets.
    - .planning/phases/47-multi-step-plan-stream-design-gate/47-PATTERNS.md — header/discipline template, live-path file:line excerpts for planner.rs / worker.rs / proto.rs / server.rs / plan_node.rs / executor_decision.rs / intent.rs / sink_sensitivity.rs.
    - planning-docs/DESIGN-v1.9-egress-policy.md — MODEL shape: decisions-not-options voice, §-numbered, every load-bearing claim `file:line`, fail-closed table, threat model, adversarial-trace §, acceptance predicate.
    - planning-docs/DESIGN-plan-executor.md — PLAN-03 handle-only planner spine (ValueId only; never mint/retype).
    - planning-docs/DESIGN-confirmation-release.md — single-shot confirm; confirm MUST NOT re-invoke submit_plan_node for the blocked node; confirm ≠ session trust waiver.
    - Live code (re-read and cite exact lines this session): cli/caprun/src/planner.rs (Planner::plan signature + PLAN-03 docs); cli/caprun/src/worker.rs (one-shot plan → SubmitPlanNode → discard output_value_id → exit on non-Allowed); crates/brokerd/src/proto.rs (ProvideIntent, SubmitPlanNode, PlanNodeDecision.output_value_id); crates/brokerd/src/server.rs (connection loop multi-submit, intent_provided/fd_requested guards, evaluate_plan_node_and_record, git.push always-confirm rewrite); crates/runtime-core/src/plan_node.rs (PlanNode/PlanArg/ValueId); crates/runtime-core/src/executor_decision.rs (Allowed / BlockedPendingConfirmation / Denied / PolicyDeny); crates/runtime-core/src/intent.rs (closed CaprunIntent); crates/executor/src/sink_sensitivity.rs (sink_effect_class table — CommitIrreversible vs MutateReversible vs Observe).
    - .planning/REQUIREMENTS.md — DESIGN-19 exact text; HYG-02; STREAM/CODE/CLI/CONFIRM/LIVE mapping the doc unlocks.
    - CLAUDE.md + planning-docs/PLAN.md — I0/I1/I2, locked terminology, plan-node effect path, design-gate blocks TCB code.
  </read_first>
  <action>
    Create planning-docs/DESIGN-multi-step-plan-stream.md as the Phase 47 design gate (DESIGN-19). Mirror DESIGN-v1.9-egress-policy.md discipline: header (Milestone v1.10, Phase 47 Design Gate, Status Draft → pending non-self orchestrator-owned adversarial code-trace recorded in DESIGN-GATE-RECORD-v1.10.md, authored by gsd-executor which does NOT run that trace, grounding sources, Requirements DESIGN-19 → enables STREAM/CODE/CLI/CONFIRM/LIVE; DESIGN-20 clears it), design-gate discipline callout (no multi-step TCB / worker submit/confirm-hold until CLEARED; unbroken precedent v1.0 P2 … v1.9 P41), decisions-not-options voice.

    Write these sections with every load-bearing claim cited to live file:line re-verified this session:

    §0 Purpose and Scope — what is pinned (multi-step orchestration composition on shipped single-node substrate); what is deferred (LLM multi-step LLM-MS-01, new sinks, session-wide confirm waiver, agent frameworks); hard-blocks Phases 48–52; DOC-ONLY this phase; locked terminology (Intent, Session, Planner, Worker, Broker, Adapter, Effect, Artifact, Event); no raw plan-node bypass (plan-node path only).

    §1 Plan-stream shape (DESIGN-19a) — pin sequential multi-node on existing Planner seam: one Session, one worker connection, N sequential SubmitPlanNode calls; each node independently policy pre-I2 then I2; broker multi-submit already legal (cite server.rs connection loop + proto SubmitPlanNode). Pin additive multi-node API (static sequence index now; plan_next-style surface allowed as additive extension) while keeping one-shot plan() for email/file + LlmPlanner. EXPLICITLY REJECT batch DAG authorize-all and any new free-form tool-map effect path. Pin Open Question 5 default: static index sufficient for deterministic coding planner; reactive LLM out of v1.10 trait requirements.

    §2 Worker sequential submit + handle bag (DESIGN-19b) — pin replacing worker one-shot (cite worker.rs discard of output_value_id and exit on non-Allowed) with sequential loop + worker-side handle bag of opaque ValueIds only. On Allowed sinks that return output_value_id (e.g. mint_from_exec), store handle for later PlanArgs. Planner may only place handles offered by call-site convention — never literals, never re-mint, never strip taint (PLAN-03 / DESIGN-plan-executor). ProvideIntent remains exactly once before RequestFd (cite server.rs intent_provided / fd_requested guards; proto ProvideIntent). Mid-stream second ProvideIntent DENIED (idempotency pin).

    §3 Mid-loop Block-and-Hold confirm continuity (DESIGN-19c) — pin: on BlockedPendingConfirmation (I2 Block OR always-confirm git.push rewrite — cite server.rs rewrite path as first-class mid-loop hold, not only I2), worker stays connected / holds same Session; human confirms durable PendingConfirmation snapshot; sink executes from snapshot; worker does NOT re-submit the blocked node (DESIGN-confirmation-release); remaining nodes continue only after Allowed release under same Session id, same policy bind (POLICY-03 immutable), same audit chain. REJECT reconnect-remint, dual-Session stitch, session-wide confirm waiver, auto-confirm mid-loop. Pin Open Question 1 recommended primary path: CLI main holds broker lifetime + interactive or documented dual-terminal caprun confirm/deny without worker exit or ProvideIntent remint; mark alternatives rejected with reason. Pin Open Question 2: interactive-in-run vs dual-terminal both OK if same Session + no re-submit; machine-checkable stop semantics either way.

    §4 I1×coding-loop bounds (DESIGN-19d) — pin trusted-intent success path: coding args (paths, commands, messages, remotes/refspecs, PR title/body) minted once at session start via ProvideIntent from operator-typed CLI/intent. Success path does NOT require multi-file untrusted RequestFd before CommitIrreversible nodes (HARDEN-01 demotes non-seed → Draft → Step 0.5 denies). Include code-verified effect-class table from sink_sensitivity.rs (git.commit MutateReversible; file.write/process.exec/git.push/github.pr/http.request.write CommitIrreversible; http GET Observe). EXPLICITLY REJECT weakening CommitIrreversible Draft denies to "fix" multi-file demotion. Pin Open Question 4 default: seed only / none RequestFd for irreversible success path; multi-file untrusted then still push = separate future design-gate. Mid-loop I2 proof (LIVE-08) uses deliberate tainted-handle routing, not success-path laundering.

    §5 Instruction vs value channel disjointness (DESIGN-19e) — pin PLAN-03 / GATE-01: task_instruction Option String may influence which offered handle the planner places; it is never a ValueId and cannot bind as a sink arg. Values only as pre-minted opaque handles (cite planner.rs signature + module docs; plan_node PlanArg.value_id).

    §6 Deny/abort semantics mid-stream (DESIGN-19f) — pin ExecutorDecision taxonomy (cite executor_decision.rs): Allowed → dispatch + maybe output_value_id to bag; BlockedPendingConfirmation → hold (no silent continue); Denied including PolicyDeny → abort remaining nodes fail-closed with durable terminal events; DraftOnlySessionDeniesCommitIrreversible stays. Pin Open Question 3: abort remaining on deny/policy_deny; distinct exit-code taxonomy deferred detail to Phase 50 but semantics locked here. Ordering pin: sequential submit order; no parallel dual-occupancy of same Session stream.

    End Task 1 with sentinel line: gsd:design-tail-pending (Task 2) as an HTML comment so Task 2 appends §7–§14. Do not invent parallel types; cite existing symbols only. Do not paste fenced reproductions of Gate-1-sensitive tokens under crates/; refer to locked plan-node path by concept.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'plan.stream|plan_next|sequential' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'not batch|batch.*reject|no batch' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'handle bag|output_value_id' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'opaque ValueId|planner never mints|PLAN-03' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'Block-and-Hold|block and hold' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'same Session|no reconnect|remint' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'trusted-intent|ProvideIntent' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'CommitIrreversible|Draft' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'task_instruction|instruction.*value|value channel' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'abort|deny' planning-docs/DESIGN-multi-step-plan-stream.md && test -z "$(git status --porcelain -- crates cli)"</automated>
  </verify>
  <acceptance_criteria>
    - planning-docs/DESIGN-multi-step-plan-stream.md exists with header + §0–§6 covering all DESIGN-19 mechanism pins (stream shape, handle bag, Block-and-Hold, I1×coding-loop, instruction/value, deny/abort).
    - §1 rejects batch DAG authorize and free-form tool-map effect path; pins sequential N× SubmitPlanNode + additive Planner surface with file:line citations.
    - §2 pins opaque ValueId handle bag, planner never mints, ProvideIntent exactly once (re-ProvideIntent mid-stream denied).
    - §3 pins Block-and-Hold same Session (incl. always-confirm git.push), no re-submit blocked node, primary hold path chosen, dual-Session/reconnect-remint/session-wide waiver rejected.
    - §4 pins trusted-intent success path + effect-class table + no Draft CommitIrreversible weaken; §5 pins task_instruction non-handle; §6 pins abort remaining.
    - Task-2 sentinel present; zero edits under crates/ or cli/.
  </acceptance_criteria>
  <done>§0–§6 pin every DESIGN-19 core mechanism with live file:line citations and recommended open-question defaults; no TCB code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Author DESIGN §7–§14 — carry-forward, HYG-02, threat model, invariants, DESIGN-20 gate, acceptance</name>
  <files>planning-docs/DESIGN-multi-step-plan-stream.md</files>
  <read_first>
    - planning-docs/DESIGN-multi-step-plan-stream.md — Task 1 body; replace the Task-2 sentinel.
    - planning-docs/DESIGN-v1.9-egress-policy.md — § threat model / invariant preservation / fail-closed / new-symbol / adversarial-trace / acceptance shape.
    - planning-docs/DESIGN-GATE-RECORD-v1.9.md — how DESIGN-20 clearance will be recorded (orchestrator-owned; re-run triggers); doc must declare this, not perform it.
    - scripts/check-invariants.sh — Gate 1 (no EffectRequest under crates/), Gate 3 mint-site allowlist, Gate 5 ring-only; HYG-02 enforcement locus.
    - .planning/phases/47-multi-step-plan-stream-design-gate/47-RESEARCH.md — Common Pitfalls 1–10, Security Domain threat table, Validation Architecture greps, Assumptions A1–A4, Downstream What DESIGN Must Pin for Phases 48–52.
    - .planning/REQUIREMENTS.md — HYG-02 exact text; DESIGN-20 re-run clause.
  </read_first>
  <action>
    Replace the Task-2 HTML-comment sentinel with the remaining sections (keep §0–§6 intact; produce one coherent doc):

    §7 Carry-forward invariants — lock in writing: (1) ProvideIntent exactly once before RequestFd; (2) P33/P34 precheck-before-burn + terminal-event-before-terminal-state for every confirm-releasable multi-node release (multi-confirm Session must not amplify the audit-gap class); (3) POLICY-02 — policy is pre-I2 narrowing only, never overrides/disables I2; I2 stays hardcoded in Rust TCB executor unconditional on policy-permitted calls; POLICY-03 bind once outside worker, immutable for Session; (4) Gate 3 mint-site discipline — multi-step default introduces ZERO new mint sites; if a later phase needs one, DESIGN must explicitly amend Gate 3 (adjacency: no silent merge of loci).

    §8 HYG-02 / Gate discipline — pin default ZERO new crates for entire multi-step milestone unless a later phase design-justifies an exception (none expected for plan-stream substrate); no free-form effect-request token under crates/ (Gate 1); Gate 3 list unchanged or explicitly amended; check-invariants.sh remains architectural gate; compose-verify remains authoritative Linux gate (mailpit-verify when SMTP may fire). Empty multi-node stream: reject or N/A as fail-closed. CaprunIntent coding variant (Phase 49): closed enum only; success-path literals from operator intent at ProvideIntent; exact field names may defer to Phase 49 if this pin holds (Open Question 6).

    §9 Threat model — one row/§ per RESEARCH pitfall → named mechanism. Cover at minimum: (1) cross-node taint laundering via output_value_id; (2) ProvideIntent remint mid-stream; (3) Draft demotion fixed by weakening CommitIrreversible Step 0.5; (4) batch authorize / new effect path; (5) mid-loop confirm Session split / reconnect-remint / session-wide waiver; (6) instruction channel collapsed into bindable ValueId; (7) policy mid-stream rebind or I2 override; (8) hybrid composition framed as CLI multi-step DONE; (9) new mint outside Gate 3; (10) P33/P34 under multi-confirm Session; (11) design-gate process failure (self-review / early TCB code). Each row: threat → structural mechanism that closes it (cite pinning §).

    §10 Invariant preservation checklist — show design weakens NONE of I0/I1/I2; adds NO raw plan-node-bypass effect path; no batch authorize; per-node I2 remains; policy pre-I2 only; handle model intact; kernel confinement unchanged (worker self-confine after connect).

    §11 Fail-closed defaults table — one row per multi-step mechanism default: empty/missing stream → reject; Deny/policy_deny → abort remaining; Block → hold same Session not silent continue; absent handle → cannot bind arg; mid-stream ProvideIntent → deny; unknown sink → existing schema fail-closed; dual-Session resume → rejected product path.

    §12 New-symbol summary — expect none: no new TaintLabel, no new mint_from_*, no new crates. If any symbol is named for Phase 48–50 (e.g. plan_next method name, handle-bag type name, CaprunIntent coding variant name), list as planning-only names appearing ONLY in DESIGN prose this phase. Default A4: no new mint site required for multi-step substrate.

    §13 Adversarial-trace gate (DESIGN-20) — state that a fresh, NON-SELF, ORCHESTRATOR-OWNED adversarial code-trace (NOT a gsd-executor — gsd-executors have no Agent tool) must clear this doc with every BLOCKER/MAJOR resolved before ANY multi-step TCB change in crates/{executor,brokerd,sandbox,runtime-core} or worker submit/confirm-hold in cli/caprun. Trace RE-RUNS if stream shape, confirm-hold, or trusted-arg mint path changes mid-implementation. Record outcome in planning-docs/DESIGN-GATE-RECORD-v1.10.md. This plan does not run the trace.

    §14 Acceptance predicate — Done when: doc pins all DESIGN-19 mechanisms as decisions; HYG-02 re-asserted; carry-forwards locked; threat model closes listed pitfalls; DESIGN-20 orchestrator-owned clear recorded CLEARED; no multi-step TCB code until then.

    Amendments (post-review) — empty placeholder section for Plan 47-02 Round-1 fold.

    After writing: run scripts/check-invariants.sh (must exit 0); confirm git status --porcelain -- crates cli is empty.
  </action>
  <verify>
    <!-- planner-region-allow: gsd:design-tail-pending -->
    <!-- Sequential intra-plan sentinel: Task 1 writes it, Task 2 replaces it, then asserts its absence. Satisfiable by construction. -->
    <automated>grep -qiE 'ProvideIntent.*once|exactly once' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'P33|P34|precheck|terminal.event' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'POLICY-02|never.*override I2|I2.*unconditional' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'zero new crate|HYG-02|Gate 3|check-invariants' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'orchestrator-owned|non-self' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 're-run|re-runs' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'acceptance predicate|done when' planning-docs/DESIGN-multi-step-plan-stream.md && grep -qiE 'threat' planning-docs/DESIGN-multi-step-plan-stream.md && ! grep -q 'gsd:design-tail-pending' planning-docs/DESIGN-multi-step-plan-stream.md && test -z "$(git status --porcelain -- crates cli)" && bash scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - §7 locks ProvideIntent-once, P33/P34, POLICY-02/03, Gate 3 adjacency (unchanged or explicit amend).
    - §8 pins HYG-02 zero-new-crates default, Gate 1/3, check-invariants + compose-verify authority, closed CaprunIntent coding variant constraint.
    - §9 threat model maps all listed multi-step pitfalls to named mechanisms; §10 invariant checklist covers I0/I1/I2 + no plan-node bypass + no batch authorize.
    - §11 fail-closed defaults table present; §12 new-symbol summary defaults to none / no new mint sites.
    - §13 declares DESIGN-20 orchestrator-owned non-self trace + re-run triggers (stream shape / confirm-hold / trusted-arg mint path); §14 acceptance predicate checkable; Amendments placeholder present.
    - Task-1 sentinel removed; check-invariants.sh exit 0; crates/ and cli/ porcelain empty.
  </acceptance_criteria>
  <done>Full DESIGN-19 + HYG-02 doc complete through §14 with DESIGN-20 declared (not executed); invariants green; no TCB code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design → implementation | This plan writes one markdown file and touches zero TCB code. An under-specified DESIGN that greenlights cross-node laundering, ProvideIntent remint, Session-split after confirm, batch authorize, or Draft CommitIrreversible weaken would let unsound multi-step composition reach Phases 48–52. |
| authoring executor → DESIGN-20 gate | This plan MUST NOT self-clear the adversarial gate; clearance is a separate non-self process (Plan 47-02 / orchestrator). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering / Elevation | Cross-node taint laundering via output_value_id | critical | mitigate | §2 pins opaque ValueIds only, planner never mints, no mid-stream ProvideIntent; LIVE-08 genuine provenance; Task 1 acceptance greps handle bag + PLAN-03. |
| T-47-02 | Elevation of Privilege | ProvideIntent remint mid-stream as trust valve | critical | mitigate | §2/§7 pin ProvideIntent exactly once before RequestFd; re-ProvideIntent denied; cite server intent_provided guards. |
| T-47-03 | Elevation of Privilege | Draft demotion "fixed" by weakening CommitIrreversible Step 0.5 | critical | mitigate | §4 pins trusted-intent success path; effect-class table cited; explicit reject of class weaken; prohibition must_have. |
| T-47-04 | Tampering / Spoofing | Batch authorize or new free-form effect path | critical | mitigate | §1 rejects batch DAG; plan-node path only; Gate 1 via check-invariants; DESIGN greps for batch rejection. |
| T-47-05 | Tampering | Mid-loop confirm splits Session / reconnect-remint / session-wide waiver | critical | mitigate | §3 Block-and-Hold same Session; no re-submit blocked node; dual-Session rejected as product path. |
| T-47-06 | Tampering | Instruction channel collapsed into bindable ValueId | high | mitigate | §5 pins task_instruction non-handle PLAN-03 / GATE-01. |
| T-47-07 | Elevation of Privilege | Policy mid-stream rebind or I2 override narrative | high | mitigate | §7 POLICY-02 unconditional I2; POLICY-03 bind once immutable. |
| T-47-08 | Information Disclosure / Integrity of claims | Hybrid composition framed as CLI multi-step DONE | high | mitigate | §0/§14 + threat model: LIVE DONE requires real multi-node caprun run one Session. |
| T-47-09 | Tampering | New mint site outside Gate 3 / silent allowlist merge | high | mitigate | §7–§8/§12 default zero new mints; Gate 3 unchanged or explicit amend (HYG-02 adjacency). |
| T-47-10 | Spoofing | Self-reviewed design greenlights unsound composition | high | mitigate | §13 declares orchestrator-owned non-self DESIGN-20; this plan does not run the trace; Plan 47-02 owns clearance. |
| T-47-11 | Tampering | Executor drifts into multi-step TCB code during doc authoring | high | mitigate | Both tasks verify empty crates/cli porcelain; files_modified is DESIGN only. |
| T-47-SC | Tampering | cargo/npm/pip installs | low | accept | No package installs this docs-only phase; Package Legitimacy N/A. |
</threat_model>

<verification>
1. `test -f planning-docs/DESIGN-multi-step-plan-stream.md`
2. Full DESIGN-19 section-presence greps from 47-VALIDATION.md / 47-RESEARCH Validation Architecture
3. `test -z "$(git status --porcelain -- crates cli)"`
4. `bash scripts/check-invariants.sh` exits 0

**DESIGN-20 is NOT completed by this plan.** Plan 47-02 (orchestrator-constrained non-self spawn) clears the gate.
</verification>

<success_criteria>
- DESIGN-multi-step-plan-stream.md pins all DESIGN-19 mechanisms as decisions with live file:line citations and RESEARCH recommended defaults for open questions.
- HYG-02 zero-new-crates / Gate discipline re-asserted; carry-forwards locked; threat model + acceptance present.
- DESIGN-20 declared orchestrator-owned with re-run triggers (not self-executed).
- No multi-step TCB code; check-invariants green.
</success_criteria>

<output>
Create `.planning/phases/47-multi-step-plan-stream-design-gate/47-01-SUMMARY.md` when done.
</output>
