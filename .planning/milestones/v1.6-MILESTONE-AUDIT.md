---
milestone: v1.6
name: Security Hardening
audited: 2026-07-17
status: passed
scores:
  requirements: 8/8
  phases: 5/5
  integration: 5/5 seams
  flows: 2/2 (live email.send + file.create, proven real-Linux)
gaps: {}
accepted_residuals:
  - id: D-08
    note: "HARDEN-03 at-most-once is PER PLAN NODE (identical value_ids), not a per-session send budget. A worker minting fresh distinct value_ids can still cause N sends. Effects-budget/rate-limit is named future work, explicitly out of v1.6 scope. Documented unsoftened in audit.rs + server.rs comments."
  - id: HARDEN-05-e2e
    note: "HARDEN-05 wires file.create `contents` into I2 (content-sensitive + expected_role Some([\"path\"])) but fires on nothing on the live path today (contents only ever carries a trusted `path`-role value). The end-to-end tainted-contents Block has classification tests but no e2e drive — a synthetic tainted-contents mint site does not exist pre-D-12 (content-extraction pipeline). Low risk: rides the generic Step 2/3 collect-then-Block loop already proven for email.send. Deferred to the future content-extraction work."
  - id: T2
    note: "Slot-type origin/trust-label binding beyond the expected-role table remains a v1.4-era accepted residual; not in v1.6 scope."
tech_debt: {}
---

# v1.6 "Security Hardening" — Milestone Audit

**Status: PASSED.** All 8 requirements satisfied, all 5 phases verified passed, cross-phase
integration clean, and the whole milestone proven live on real Linux (331 passed / 0 failed
across 49 suites + a separate featureless-build gate).

## Definition of done (from ROADMAP)

Close the 5 TCB-local security residuals left after v1.5, each kernel/TCB-enforced and proven
live, with no regression to v1.1–v1.5 behavior.

## Requirements coverage (3-source cross-reference)

| REQ-ID | Phase | Traceability | VERIFICATION | SUMMARY frontmatter | Final |
|--------|-------|-------------|--------------|---------------------|-------|
| DESIGN-11 | 26 | Complete `[x]` | passed | DESIGN-11 (26-01) | **satisfied** |
| DESIGN-12 | 26 | Complete `[x]` | passed | orchestrator-owned (non-self Fable-5 design review; executor has no Agent tool) | **satisfied** (out-of-band, documented at Phase 26 close) |
| HARDEN-01 | 27 | Complete `[x]` | passed | HARDEN-01 (27-01) | **satisfied** |
| HARDEN-04 | 27 | Complete `[x]` | passed | HARDEN-04 (27-02) | **satisfied** |
| HARDEN-02 | 28 | Complete `[x]` | passed | HARDEN-02 (28-01..05) | **satisfied** |
| HARDEN-03 | 29 | Complete `[x]` | passed | HARDEN-03 (29-01,02) | **satisfied** |
| HARDEN-05 | 29 | Complete `[x]` | passed | HARDEN-05 (29-03) | **satisfied** |
| HARDEN-06 | 30 | Complete `[x]` | passed | HARDEN-06 (30-01) | **satisfied** |

No unsatisfied, partial, or orphaned requirements. (DESIGN-12's SUMMARY-frontmatter absence is
explained: the design-review requirement was completed by the orchestrator, not an executor —
the executor role has no Agent tool to spawn a non-self reviewer.)

## Phase verifications

| Phase | Name | Status | Plans |
|-------|------|--------|-------|
| 26 | Security Hardening Design Gate | passed | 2/2 |
| 27 | Session/Connection Integrity Hardening | passed | 2/2 |
| 28 | Authenticated Audit Chain | passed | 5/5 |
| 29 | Sink-Path Hardening (replay CAS + contents slot) | passed | 3/3 |
| 30 | Regression & Live Proof | passed | 2/2 |

## Cross-phase integration (independent checker, traced against code)

**PASS — all 5 seams wired, no broken wiring, no silent bypass.**

1. **HARDEN-03 replay events ↔ HARDEN-02 MAC chain (highest risk): WIRED.** `email_send_attempted` / `email_send_replay_suppressed` append via the same `append_event()` (audit.rs:678) as every event, called at server.rs:998 — no raw-SQL write path — so `verify_chain`'s keyed-HMAC walk covers them identically. `sent_plan_nodes` is a sibling idempotency table, correctly NOT part of the hash chain.
2. **HARDEN-01 demotion ↔ HARDEN-02 confirm/deny gates: WIRED (by design).** `session_demoted` appends through the same locked chain path; `confirm()` surfaces Draft/untrusted posture in the human-facing block display rather than refusing (documented I0 semantics — Draft blocks *auto*-authorization, not human-confirmed release).
3. **HARDEN-05 slot-type ↔ I2 sink decisions: WIRED, no cross-contamination.** `expected_role`/`is_content_sensitive` match per-sink on `sink.0`; `file.create`'s new `contents => Some(["path"])` arm is strictly scoped and cannot affect `email.send`.
4. **HARDEN-04 compile-out: WIRED, no regression.** Default build uses `#[cfg(not(any(test, feature="test-fixtures")))]` returning a disabled-IPC error; production CLI creates sessions in-process (main.rs:244), never via the IPC arm. Gate B proves physical absence in a featureless build (which Gate A's always-`cfg(test)` compile cannot).
5. **Full composed build: 331/0 across 49 suites (Gate A) + featureless Gate B** — the correct pairing.

## Live proof (Phase 30, orchestrator-run, non-laundered)

- Gate A (bare `scripts/mailpit-verify.sh`): true exit 0, 331 passed / 0 failed / 49 suites — fresh v1.6 baseline.
- Gate B (`scripts/verify-harden04-featureless.sh`): true exit 0, criterion-4 assertion executed (no self-skip) + passed.
- Per-criterion named tests all green on real Linux (see 30-VERIFICATION.md).

## Nyquist

VALIDATION.md present for the execution phases (27–30) with `nyquist_compliant: true`. Phase 26 is a design gate (no code). No missing-validation flags.

## Verdict

**v1.6 is DONE.** Ready for `/gsd-complete-milestone v1.6` (archive + tag). Accepted residuals
(D-08 per-session budget, HARDEN-05 e2e-tainted-contents pre-D-12, T2 slot-binding) are
documented above and carried forward as named future work, not gaps.
