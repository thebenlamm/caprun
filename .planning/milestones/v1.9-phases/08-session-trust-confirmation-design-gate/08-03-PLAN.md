---
phase: 08-session-trust-confirmation-design-gate
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - planning-docs/DESIGN-GATE-RECORD-v1.2.md
autonomous: false

requirements: [PROC-01]

must_haves:
  truths:
    - "A gate-record artifact exists that pins sha256 hashes of both new v1.2 DESIGN docs and provides a per-requirement checklist gating PROC-01 completeness, mirroring DESIGN-GATE-RECORD.md's round-1/round-2/APPROVED structure."
    - "The gate-record explicitly states Phase 9 and Phase 10 MUST NOT author any crates/executor or crates/brokerd file implementing session-trust or confirmation-release logic until this record shows Decision: APPROVED and Gate status: UNBLOCKED."
    - "A human reviewer (not the executing agent) sets the final Decision/Gate-status values after reading both docs end-to-end as an attacker — this is a blocking human-verify checkpoint, not an auto-approved artifact."
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.2.md
  key_links:
    - "The gate-record's sha256 table pins planning-docs/DESIGN-session-trust-state.md and planning-docs/DESIGN-confirmation-release.md — any later edit to either doc invalidates APPROVED and requires a fresh gate round, exactly as DESIGN-GATE-RECORD.md's round-2 approval is pinned."
    - "Gate status line is the literal enforceable blocker Phase 9/10 planning must check before authoring executor/brokerd code — this is what makes PROC-01 a hard gate, not just descriptive documentation."
---

<objective>
Author `planning-docs/DESIGN-GATE-RECORD-v1.2.md` — the v1.2 gate-record artifact that reviews `planning-docs/DESIGN-session-trust-state.md` (08-01) and `planning-docs/DESIGN-confirmation-release.md` (08-02) as a pair, mirroring the exact structure of the APPROVED `planning-docs/DESIGN-GATE-RECORD.md` (round 1 -> round 2 -> APPROVED) precedent from the v1.0 Phase 2 design gate.

Purpose: PROC-01 requires the DESIGN doc(s) to exist AND be reviewed before Phase 9 or Phase 10 write any executor code. Without an explicit gate-record artifact carrying a Decision/Gate-status pair, PROC-01 is descriptive, not enforceable — this file is what makes the gate real.
Output: One gate-record Markdown file under `planning-docs/`, with a sha256-pinning table, a per-requirement checklist (grep-prefilled), a 5-step human-review procedure, and a blocking human-verify checkpoint that records the final Decision/Gate status.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-session-trust-confirmation-design-gate/08-RESEARCH.md
@.planning/phases/08-session-trust-confirmation-design-gate/08-PATTERNS.md
@.planning/phases/08-session-trust-confirmation-design-gate/08-VALIDATION.md
@planning-docs/DESIGN-GATE-RECORD.md
@planning-docs/DESIGN-session-trust-state.md
@planning-docs/DESIGN-confirmation-release.md
</context>

<artifacts_this_phase_produces>
This plan (08-03) creates exactly one file:
- `planning-docs/DESIGN-GATE-RECORD-v1.2.md` — gate-record artifact (review artifact) recording the human review Decision/Gate status for both new v1.2 DESIGN docs, per PROC-01.

(This plan depends on 08-01's `planning-docs/DESIGN-session-trust-state.md` and 08-02's `planning-docs/DESIGN-confirmation-release.md` — both must exist before this plan's tasks run.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the gate-record's front matter, sha256-pinning table, and per-requirement grep-prefilled checklist</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.2.md</files>
  <read_first>
    - planning-docs/DESIGN-GATE-RECORD.md (the exact structural analog — front-matter fields, sha256-pinning table format, checklist item format with "Grep matched:" lines, the round-1/round-2/APPROVED revision-history pattern)
    - planning-docs/DESIGN-session-trust-state.md (08-01's output — the doc this record reviews; read it in full to build accurate grep-matched checklist entries)
    - planning-docs/DESIGN-confirmation-release.md (08-02's output — the doc this record reviews; read it in full to build accurate grep-matched checklist entries)
    - .planning/phases/08-session-trust-confirmation-design-gate/08-PATTERNS.md (the "Gate-record artifact" subsection — front-matter fields to copy exactly, sha256-pinning table pattern, checklist pattern, "Decision/Gate-status block pattern")
    - .planning/REQUIREMENTS.md (TAINT-01..04, ORIGIN-01..02, CONFIRM-01..04 exact wording — each checklist item maps 1:1 to one of these)
  </read_first>
  <action>
    Create `planning-docs/DESIGN-GATE-RECORD-v1.2.md`. Copy the front-matter shape from DESIGN-GATE-RECORD.md exactly: `**Date:**` (today's date), `**Reviewer:** Ben Lamm`, `**Phase:** 08-session-trust-confirmation-design-gate — Plan 03`, `**Review round:** 1`.
    Add a "## Documents Under Review (round 1)" section with a `Document | sha256` table. Compute the actual sha256 of both files using `shasum -a 256 planning-docs/DESIGN-session-trust-state.md planning-docs/DESIGN-confirmation-release.md` and insert the real hash values (not placeholders) into the table rows. Include the same disclaimer line as the analog: the reviewer MUST re-run `shasum -a 256` on both files and confirm the values match before setting Decision: APPROVED.
    Add a "## Checklist" section, one item per requirement, each formatted exactly like the analog (`- [ ] **Item N — <title>** (<REQ-ID>)` followed by a `Grep matched:` line showing the actual grep command run and what it found, quoting real doc text). Pre-fill each checkbox as checked ONLY if the corresponding grep genuinely matches the current doc content (run the greps for real against the two files; do not fabricate matches). Map items 1:1 to:
    - Item 1 (TAINT-01/ORIGIN-01/ORIGIN-02 combined or split): SessionStatus::Draft variant + I1 mint_from_read trigger + I0 seed-provenance rule explicitly stated in DESIGN-session-trust-state.md.
    - Item 2 (TAINT-02): draft-only CommitIrreversible denial decided in one executor TCB function (Step 0.5), new DenyReason variant appended to the single taxonomy.
    - Item 3 (TAINT-03): MutateReversible/Observe still allowed on Draft sessions, stated as a non-regression MUST.
    - Item 4 (TAINT-04): session_demoted Event with parent_id = triggering file_read Event id, atomic with the sessions-row UPDATE.
    - Item 5 (CONFIRM-01): caprun confirm CLI contract with exact output format (verbatim literal + provenance).
    - Item 6 (CONFIRM-02): single-shot (sink, arg, literal-digest) release, explicit no-standing-policy statement.
    - Item 7 (CONFIRM-03): durable deny, persisted terminal-state check, no re-confirmation.
    - Item 8 (CONFIRM-04): confirm/deny audited and anchored to effect_id (same key as SinkBlockedAnchor), TCB-resident release path.
    Add a "### Soundness (round 1)" subsection mirroring the analog's Item 10 framing ("Completeness greps pass fully-written-but-wrong specs; this item gates soundness, not presence.") with two soundness items: (a) trust state cannot be self-declared by the worker or IPC (grep-verify the anti-self-declaration language exists and reads as unambiguous), (b) confirm cannot re-invoke submit_plan_node and cannot reconstruct a multi-arg sink from only the blocked-arg anchor (grep-verify the explicit prohibition and the full-arg-set PendingConfirmation schema both exist).
    Do not write the "How to Verify" procedure or the Decision/Gate-status blocks here — those are Task 2.
  </action>
  <acceptance_criteria>
    - `test -f planning-docs/DESIGN-GATE-RECORD-v1.2.md` succeeds.
    - `grep -c 'sha256' planning-docs/DESIGN-GATE-RECORD-v1.2.md` is >= 1, and the table contains two real 64-hex-character hash values (verify with `grep -oE '[a-f0-9]{64}' planning-docs/DESIGN-GATE-RECORD-v1.2.md | wc -l` >= 2).
    - `grep -c '\- \[ \]\|\- \[x\]' planning-docs/DESIGN-GATE-RECORD-v1.2.md` is >= 10 (8 requirement items + 2 soundness items minimum).
    - `grep -c 'Grep matched:' planning-docs/DESIGN-GATE-RECORD-v1.2.md` is >= 10 (one per checklist item).
    - `grep -ci 'soundness' planning-docs/DESIGN-GATE-RECORD-v1.2.md` is >= 1.
    - Every checklist item references a requirement ID from TAINT-01..04, ORIGIN-01..02, or CONFIRM-01..04 — spot-check via `grep -c 'TAINT-\|ORIGIN-\|CONFIRM-' planning-docs/DESIGN-GATE-RECORD-v1.2.md` >= 8.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.2.md && [ "$(grep -oE '[a-f0-9]{64}' planning-docs/DESIGN-GATE-RECORD-v1.2.md | wc -l)" -ge 2 ] && [ "$(grep -c 'Grep matched:' planning-docs/DESIGN-GATE-RECORD-v1.2.md)" -ge 10 ]</automated>
  </verify>
  <done>The gate-record file exists with real (not placeholder) sha256 hashes of both DESIGN docs, a per-requirement checklist (8 requirement items + 2 soundness items, each with a genuine Grep matched: line reflecting actual grep output against the real doc content), matching the structural precedent of DESIGN-GATE-RECORD.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review and approval of both v1.2 DESIGN docs against the gate checklist</name>
  <action>Halt for human review. Present both DESIGN docs (planning-docs/DESIGN-session-trust-state.md, planning-docs/DESIGN-confirmation-release.md) and the pre-filled gate record (planning-docs/DESIGN-GATE-RECORD-v1.2.md) to the reviewer (Ben Lamm), walk the checklist and adversarial review per how-to-verify below, then have the reviewer set Decision and Gate status in planning-docs/DESIGN-GATE-RECORD-v1.2.md. Do not proceed until a resume signal is given.</action>
  <what-built>
    Task 1 produced `planning-docs/DESIGN-GATE-RECORD-v1.2.md` with a sha256-pinned table for both new v1.2 DESIGN docs and a grep-prefilled checklist covering all 10 TAINT/ORIGIN/CONFIRM requirement+soundness items. This checkpoint adds the "How to Verify" human-review procedure and the final Decision/Gate-status blocks, then requires you (Ben) to actually perform the review before the gate can show APPROVED/UNBLOCKED.
  </what-built>
  <how-to-verify>
    1. Read `planning-docs/DESIGN-GATE-RECORD-v1.2.md`'s checklist — confirm every item is checked. If any item is unchecked, `planning-docs/DESIGN-session-trust-state.md` or `planning-docs/DESIGN-confirmation-release.md` is incomplete and must be revised (send back to plan 08-01 or 08-02) before proceeding.
    2. Read `planning-docs/DESIGN-session-trust-state.md` end-to-end as an attacker: can you find a loophole in the SessionStatus::Draft transition rule, the mint_from_read trust-flip site, the I0 seed-provenance rule, or the Step 0.5 executor-only deny placement? Confirm every rule reads as a hard MUST/MUST NOT, not "should".
    3. Read `planning-docs/DESIGN-confirmation-release.md`: confirm the PendingConfirmation schema, the confirm-does-not-re-invoke-submit_plan_node rule, the single-shot release semantics, and the durable-deny semantics are unambiguous enough to implement `crates/executor`/`crates/brokerd` changes without interpretation.
    4. Run `shasum -a 256 planning-docs/DESIGN-session-trust-state.md planning-docs/DESIGN-confirmation-release.md` and confirm the output matches the pinned hashes in the gate-record's table.
    5. If satisfied: edit `planning-docs/DESIGN-GATE-RECORD-v1.2.md` to set `**Decision:** APPROVED` and `**Gate status:** UNBLOCKED` (dated). If not satisfied: set `**Decision:** NEEDS REVISION`, list the gaps, and this phase loops back to revise the affected DESIGN doc(s).
  </how-to-verify>
  <resume-signal>Type "approved" once Decision: APPROVED and Gate status: UNBLOCKED are recorded in planning-docs/DESIGN-GATE-RECORD-v1.2.md, or describe the specific gaps found if NEEDS REVISION.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored DESIGN docs → Phase 9/10 executor code | this gate-record is the sole enforceable checkpoint; nothing else stops Phase 9/10 from starting on unreviewed or defective docs |
| grep-prefilled checklist → human judgment | grep proves presence, never soundness — the human-verify checkpoint is load-bearing, not decorative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Repudiation | gate-record Decision/Gate-status | high | mitigate | Decision/Gate-status is set ONLY by the human-verify checkpoint (Task 2), never auto-approved by the executing agent, mirroring DESIGN-GATE-RECORD.md's precedent where Decision was set by Ben Lamm after manual review. |
| T-08-10 | Tampering | sha256 pinning | medium | mitigate | Gate-record pins real (not placeholder) sha256 hashes of both reviewed docs; any later edit invalidates APPROVED and requires a fresh round, per the existing DESIGN-GATE-RECORD.md convention. |
| T-08-11 | Elevation (silent completeness-without-soundness) | grep-prefilled checklist | medium | mitigate | Checklist explicitly separates presence (grep-matched items) from soundness (dedicated "Soundness" subsection + step 2/3 of the How-to-Verify procedure), so a well-worded-but-wrong doc cannot pass on grep alone. |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-GATE-RECORD-v1.2.md`
- Task 1's acceptance-criteria greps all satisfy their thresholds.
- The human-verify checkpoint (Task 2) is NOT auto-approvable — `autonomous: false` in this plan's frontmatter reflects that.
- After human approval, `grep -c 'Decision: APPROVED' planning-docs/DESIGN-GATE-RECORD-v1.2.md` >= 1 and `grep -c 'UNBLOCKED' planning-docs/DESIGN-GATE-RECORD-v1.2.md` >= 1.
</verification>

<success_criteria>
`planning-docs/DESIGN-GATE-RECORD-v1.2.md` exists, sha256-pins both new v1.2 DESIGN docs, provides a grep-verified 10-item checklist (8 requirement items + 2 soundness items) covering TAINT-01..04, ORIGIN-01..02, and CONFIRM-01..04, and carries a blocking human-verify checkpoint whose outcome (Decision: APPROVED / Gate status: UNBLOCKED, or NEEDS REVISION) is the literal enforceable gate Phase 9 and Phase 10 must check before authoring any crates/executor or crates/brokerd file for this milestone's new mechanisms.
</success_criteria>

<output>
Create `.planning/phases/08-session-trust-confirmation-design-gate/08-03-SUMMARY.md` when done.
</output>
