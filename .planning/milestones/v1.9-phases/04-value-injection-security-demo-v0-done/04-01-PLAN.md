---
phase: 04-value-injection-security-demo-v0-done
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/plan_node.rs
  - crates/runtime-core/src/value_record.rs
  - crates/runtime-core/src/executor_decision.rs
  - crates/runtime-core/src/lib.rs
  - crates/runtime-core/tests/types_compile.rs
  - crates/runtime-core/tests/task2_types.rs
  - crates/brokerd/src/lib.rs
autonomous: true
requirements: [REQ-executor-stub]
must_haves:
  truths:
    - "The planner can express a sink argument that carries ONLY an opaque handle (no literal, no taint) — PlanArg."
    - "A broker-owned value record can carry literal + taint + an ordered provenance_chain anchored at a read Event."
    - "The Block decision payload carries taint labels and the provenance_chain so a held-out test can assert the unbroken chain without a second query."
  artifacts:
    - "crates/runtime-core/src/plan_node.rs with ValueId, PlanArg, and PlanNode.args: Vec<PlanArg>"
    - "crates/runtime-core/src/value_record.rs with ValueRecord { id, literal, taint, provenance_chain }"
    - "crates/runtime-core/src/executor_decision.rs with BlockedPendingConfirmation { literal_value, sink, arg_name, taint, provenance_chain }"
  key_links:
    - "PlanArg.value_id (opaque) is the ONLY thing the planner holds — it never references literal or taint (closes taint-stripping)."
    - "ValueRecord.provenance_chain is the field the §9 held-out test asserts chain[0] against the file_read Event id."
---

<objective>
Establish the runtime-core handle-model type contract that the executor, broker, reader, and §9 test all build on. This is the foundation wave: it closes RESEARCH Gaps 1, 2, and 4 (ValueId/PlanArg handle model, provenance_chain, and the extended Block payload) and repairs every breaking caller so `cargo build --workspace` and `cargo test --workspace` are green.

Purpose: The DESIGN-plan-executor handle model is the spine of I2 soundness — the planner references values by opaque `ValueId` and never authors literal or taint. These types must exist before any executor logic, mint path, or acceptance test can be written.
Output: Updated runtime-core types (ValueId, PlanArg, ValueRecord, extended Provenance, extended ExecutorDecision::BlockedPendingConfirmation), updated re-exports, and all workspace callers compiling green.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-RESEARCH.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md
@planning-docs/DESIGN-plan-executor.md
</context>

<artifacts_this_phase_produces>
New / modified symbols created by THIS plan:
- `runtime_core::plan_node::ValueId(pub uuid::Uuid)` — new opaque handle newtype (derives Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize; plus `ValueId::new()`)
- `runtime_core::plan_node::PlanArg { name: String, value_id: ValueId }` — new planner-facing arg type
- `runtime_core::plan_node::PlanNode.args` — type CHANGED from `Vec<ValueNode>` to `Vec<PlanArg>` (breaking)
- `runtime_core::plan_node::Provenance.provenance_chain: Vec<uuid::Uuid>` — new field (breaking to constructors)
- `runtime_core::value_record::ValueRecord { id: ValueId, literal: String, taint: Vec<TaintLabel>, provenance_chain: Vec<uuid::Uuid> }` — new broker-owned record type (new module)
- `runtime_core::ExecutorDecision::BlockedPendingConfirmation` — variant EXTENDED with `taint: Vec<TaintLabel>` and `provenance_chain: Vec<uuid::Uuid>` (breaking to matchers/constructors)
- Re-exports added in `runtime_core::lib`: `ValueId`, `PlanArg`, `ValueRecord`
- Note: `ValueNode` is RETAINED (legacy value type, still re-exported) but is no longer the PlanNode arg path.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ValueId + PlanArg + ValueRecord, change PlanNode.args, extend Provenance</name>
  <files>crates/runtime-core/src/plan_node.rs, crates/runtime-core/src/value_record.rs, crates/runtime-core/src/lib.rs, crates/runtime-core/tests/types_compile.rs, crates/runtime-core/tests/task2_types.rs</files>
  <read_first>
    - crates/runtime-core/src/plan_node.rs (the type being extended — current ValueNode/PlanNode/Provenance/SinkId/TaintLabel)
    - crates/runtime-core/src/lib.rs (re-export list to update)
    - crates/runtime-core/tests/types_compile.rs and tests/task2_types.rs (callers that construct PlanNode.args with ValueNode — must be updated)
    - planning-docs/DESIGN-plan-executor.md §"ValueRecord & ValueId Handle Model" and §"PlanNode Schema" (the locked contract: planner holds only ValueId; broker owns ValueRecord)
    - .planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md §"crates/runtime-core/src/plan_node.rs (modify ...)"
  </read_first>
  <behavior>
    - ValueId is an opaque newtype over uuid::Uuid; deriving Hash + Eq lets it key a HashMap; ValueId::new() generates a fresh v4 uuid.
    - PlanArg binds a String name to a ValueId and carries NO literal and NO taint field.
    - PlanNode.args is Vec<PlanArg> (not Vec<ValueNode>); a PlanNode can still be constructed and serde round-trips losslessly.
    - Provenance gains provenance_chain: Vec<uuid::Uuid>; existing Provenance constructors compile after adding the field.
    - ValueRecord carries id (ValueId), literal (String), taint (Vec<TaintLabel>), provenance_chain (Vec<uuid::Uuid>) and serde round-trips.
    - Legacy ValueNode still constructs and serde round-trips (its existing tests stay green) but is no longer routed into PlanNode.args.
  </behavior>
  <action>
    In plan_node.rs add the `ValueId` newtype over `uuid::Uuid` with derives Debug, Clone, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize and an associated `new()` constructor returning a fresh `Uuid::new_v4()`. Add the `PlanArg` struct with public fields `name: String` and `value_id: ValueId` (derive Debug, Clone, PartialEq, Serialize, Deserialize). Change `PlanNode.args` from `Vec<ValueNode>` to `Vec<PlanArg>`. Add the field `provenance_chain: Vec<uuid::Uuid>` to `Provenance`, documented as the ordered derivation edges from the originating read Event (length 1 for the v0 linear DAG). Retain the `ValueNode` struct unchanged in shape but add a doc comment stating it is a legacy value type and is NOT the sink-argument path — the sink-argument path is PlanArg resolving to a broker-owned ValueRecord. Create new module file value_record.rs defining `ValueRecord` with public fields `id: ValueId`, `literal: String`, `taint: Vec<TaintLabel>`, `provenance_chain: Vec<uuid::Uuid>` (derive Debug, Clone, PartialEq, Serialize, Deserialize); document that provenance_chain[0] MUST equal the file_read Event id and that this type is broker-owned (the planner never constructs it). In lib.rs add `pub mod value_record;` and extend the plan_node re-export to include `ValueId, PlanArg` and add `pub use value_record::ValueRecord;`. Update tests/types_compile.rs and tests/task2_types.rs everywhere they build `PlanNode { ... args: vec![ValueNode { .. }] }` to instead build `args: vec![PlanArg { name: "to".into(), value_id: ValueId::new() }]`, and add `provenance_chain: vec![]` to every `Provenance { .. }` literal; keep the standalone ValueNode serde tests intact by adding `provenance_chain: vec![]` to their Provenance literals. Do NOT inline implementations into Provenance/ValueRecord beyond field definitions.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "pub struct PlanArg" crates/runtime-core/src/plan_node.rs` returns 1.
    - Source: `grep -c "pub struct ValueId" crates/runtime-core/src/plan_node.rs` returns 1.
    - Source: `grep "pub args" crates/runtime-core/src/plan_node.rs` shows `Vec<PlanArg>` (not `Vec<ValueNode>`).
    - Source: `grep -c "provenance_chain" crates/runtime-core/src/plan_node.rs` is >= 1 (Provenance field present).
    - Source: file crates/runtime-core/src/value_record.rs exists and `grep -c "pub struct ValueRecord" crates/runtime-core/src/value_record.rs` returns 1.
    - Source: `grep -E "ValueId|PlanArg|ValueRecord" crates/runtime-core/src/lib.rs` shows all three re-exported.
    - Test: `cargo test -p runtime-core` passes (updated types_compile.rs and task2_types.rs compile and pass).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p runtime-core</automated>
  </verify>
  <done>ValueId, PlanArg, ValueRecord exist and are re-exported; PlanNode.args is Vec&lt;PlanArg&gt;; Provenance has provenance_chain; runtime-core tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ExecutorDecision::BlockedPendingConfirmation and repair all workspace matchers</name>
  <files>crates/runtime-core/src/executor_decision.rs, crates/runtime-core/tests/task2_types.rs, crates/brokerd/src/lib.rs</files>
  <read_first>
    - crates/runtime-core/src/executor_decision.rs (the enum being extended)
    - crates/runtime-core/tests/task2_types.rs (constructs BlockedPendingConfirmation — must add new fields)
    - crates/brokerd/src/lib.rs (the submit_plan_node stub + its test that constructs PlanNode; must compile under the new arg type)
    - planning-docs/DESIGN-plan-executor.md §"ExecutorDecision variants" (Block payload must carry literal_value, taint, provenance_chain read from the broker-owned ValueRecord)
  </read_first>
  <behavior>
    - ExecutorDecision::BlockedPendingConfirmation carries literal_value, sink, arg_name, taint (Vec<TaintLabel>), provenance_chain (Vec<uuid::Uuid>).
    - The enum still serde round-trips and still has Allowed, Denied, NotImplemented variants.
    - brokerd::submit_plan_node stub still returns NotImplemented and its in-crate test compiles under PlanNode.args: Vec<PlanArg>.
  </behavior>
  <action>
    In executor_decision.rs extend the `BlockedPendingConfirmation` variant with two added fields: `taint: Vec<crate::plan_node::TaintLabel>` and `provenance_chain: Vec<uuid::Uuid>`, documented so a held-out test can assert the unbroken chain directly from the decision payload (provenance_chain[0] equals the file_read Event id). Update tests/task2_types.rs where it constructs `ExecutorDecision::BlockedPendingConfirmation { .. }` to include `taint: vec![]` and `provenance_chain: vec![]`. In crates/brokerd/src/lib.rs update the in-crate test that builds `PlanNode { sink, args: vec![] }` — `args: vec![]` already type-checks under Vec<PlanArg>, so confirm it compiles; leave the stub body returning `ExecutorDecision::NotImplemented` unchanged (the real delegation lands in plan 04). Search the whole workspace for other matchers or constructors of BlockedPendingConfirmation before committing and update any found. Run the full workspace test build to confirm no caller is left broken.
  </action>
  <acceptance_criteria>
    - Source: `grep -A6 "BlockedPendingConfirmation" crates/runtime-core/src/executor_decision.rs` shows both `taint` and `provenance_chain` fields.
    - Build: `cargo build --workspace` succeeds with no errors.
    - Test: `cargo test --workspace` passes (every prior caller repaired).
    - Negative: `grep -rn "args: Vec<ValueNode>" crates/` returns no matches (PlanNode arg path fully migrated).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>BlockedPendingConfirmation carries taint + provenance_chain; the entire workspace builds and tests green under the new handle-model types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → broker | The planner emits PlanArg handles; it must never carry a literal or taint field across this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | PlanArg / planner-facing types | critical | mitigate | PlanArg carries ONLY an opaque ValueId — no literal, no taint field — so an injected planner has nothing to strip (taint-stripping closed by the handle model, DESIGN-plan-executor §ValueRecord & ValueId Handle Model). |
| T-04-SC | Tampering | cargo dependencies | low | accept | This plan adds no new external crate; runtime-core types use only workspace deps (uuid, serde, serde_json) vetted in Phases 1–3. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- `cargo build --workspace` succeeds.
- `cargo test --workspace` passes.
- ValueId, PlanArg, ValueRecord re-exported from runtime-core; PlanNode.args is Vec<PlanArg>; Provenance and BlockedPendingConfirmation carry provenance_chain.
</verification>

<success_criteria>
The handle-model type contract exists in runtime-core and the whole workspace compiles and tests green. Downstream plans (executor, brokerd, reader, §9) can import ValueId, PlanArg, ValueRecord, and the extended Block payload without further type changes.
</success_criteria>

<output>
Create `.planning/phases/04-value-injection-security-demo-v0-done/04-01-SUMMARY.md` when done.
</output>
