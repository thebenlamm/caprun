---
phase: 42-policy-layer-binding-enforcement-the-i2-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/policy.rs
  - crates/runtime-core/src/lib.rs
  - crates/runtime-core/src/executor_decision.rs
autonomous: true
requirements: [POLICY-01]
must_haves:
  truths:
    - "A per-session policy value exists as a hardcoded-schema Rust type (NOT Cedar) that lists which sinks are callable plus coarse arg constraints (allowlisted hosts/paths/repos)."
    - "A sink absent from the policy is NOT callable — the policy is deny-by-default / fail-closed."
    - "The executor decision taxonomy carries a distinct, machine-checkable PolicyDeny outcome separate from the I2 Block variant."
  artifacts:
    - "crates/runtime-core/src/policy.rs — SessionPolicy struct + its evaluation method"
    - "crates/runtime-core/src/executor_decision.rs — DenyReason::PolicyDeny variant"
  key_links:
    - "SessionPolicy::evaluate returns Result<(), DenyReason::PolicyDeny{..}> — Ok = permit, Err = deny; there is NO Allow path (structural POLICY-02 pre-condition: policy can only narrow)."
    - "DenyReason::PolicyDeny is exhaustively matched in both code() and Display (no wildcard arm) — the v1.5 SlotTypeMismatch precedent."
    - "All three constructors (default_fail_closed / allow_all / broker_default) are defined in this wave-1 plan, so Plans 03 (allow_all at policy-agnostic call sites) and 04 (broker_default for the None bind) reference a frozen contract and never edit policy.rs."
---

<objective>
Introduce the two pure foundation pieces the policy layer is built on, both in `runtime-core` (a pure, I/O-free crate): (1) the `SessionPolicy` declarative schema type per DESIGN §5.1, and (2) the distinct `DenyReason::PolicyDeny` executor-decision outcome per DESIGN §5.1 / §8. Neither wires into enforcement yet — this plan lands the types + their exhaustive matches + unit tests so Plan 03 (the executor gate) and Plan 04 (the broker binder) have a stable, tested contract to build against.

Purpose: A hardcoded-schema policy value (POLICY-01) and a machine-checkable policy-deny tag (distinct from an I2 Block, so LIVE-06 leg 3 can attribute the two independently) are the leaf dependencies of the whole phase. Getting them right — deny-by-default, no Allow path, exhaustive matches — is what keeps the later enforcement wiring sound.

Output: `SessionPolicy` type + evaluation method + all three constructors (`default_fail_closed()`, `allow_all()`, `broker_default()`) in `crates/runtime-core/src/policy.rs`; `DenyReason::PolicyDeny` variant with updated `code()`/`Display`; module export in `runtime-core/src/lib.rs`. Defining every constructor here (wave 1) means Plans 03 and 04 build against a frozen policy contract and never edit policy.rs.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-v1.9-egress-policy.md
@crates/runtime-core/src/executor_decision.rs
@crates/runtime-core/src/lib.rs
@crates/runtime-core/src/plan_node.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SessionPolicy hardcoded-schema type to runtime-core</name>
  <files>crates/runtime-core/src/policy.rs, crates/runtime-core/src/lib.rs</files>
  <behavior>
    - A SessionPolicy that lists NO sinks denies every sink (deny-by-default, fail-closed).
    - A SessionPolicy listing `email.send` permits an `email.send` call and denies a `git.commit` call.
    - A coarse arg constraint (e.g. an allowlisted host set for an `http.request.write` `url`, or an allowlisted path prefix for a `file.write` `path`) permits a literal that satisfies the allowlist and denies one that does not.
    - An arg with no configured constraint is permitted at the arg level once its sink is allowed (sink gate is the deny-by-default axis; arg constraints only narrow further).
    - SessionPolicy round-trips through serde (Deserialize/Serialize) from a JSON document — it is loaded from a trusted file in Plan 04.
    - SessionPolicy::broker_default() permits each currently-callable production sink (email.send, file.create, file.write, process.exec, git.commit, http.request, github.pr) and DENIES an unlisted/future sink — it is an explicit allowlist, never allow-everything.
  </behavior>
  <action>
    Create `crates/runtime-core/src/policy.rs` defining `pub struct SessionPolicy` per DESIGN §5.1 — a minimal, declarative, HARDCODED-schema type (NOT Cedar, no dynamic rule engine). Fields: an `allowed_sinks` set (e.g. `BTreeSet<String>` of `SinkId` string names) that is the deny-by-default sink gate, and an `arg_constraints` map keyed by (sink name, arg name) to a coarse allowlist of permitted literal values/prefixes (hosts / paths / repos) per POLICY-01. Derive `serde::Serialize`/`serde::Deserialize` (so Plan 04 can parse it from a trusted JSON file) plus `Debug, Clone, PartialEq`. Keep runtime-core PURE — no `std::fs`/`std::io`/`std::net`/`tokio`/`async` (check-invariants Gate 2 fails the build otherwise); file reading + JSON parsing happen in the brokerd binder in Plan 04, never here.

    Add the evaluation entry point `pub fn evaluate(&self, sink: &SinkId, arg_name: &str, literal: &str) -> Result<(), PolicyDenyKind>` (or an equivalent per-arg method the executor gate calls once per plan-node arg, plus a sink-level `permits_sink(&self, sink: &SinkId) -> bool`). CRITICAL structural pin (POLICY-02 pre-condition): the evaluation surface returns ONLY permit-or-deny — `Ok(())` means permit (the executor falls through to the unchanged I2 loop), `Err(..)` means deny. There is NO variant, return value, or code path by which policy evaluation can produce an Allow-and-skip result. Deny-by-default: a sink not in `allowed_sinks` denies; a literal not matching a configured coarse allowlist denies; an unconstrained arg on an allowed sink permits.

    Provide THREE named constructors so waves 2/3 build against a stable, complete contract (all defined HERE in Plan 01, the wave-1 foundation type — Plans 03 and 04 add NO new constructors to policy.rs):
    - `SessionPolicy::default_fail_closed()` — the empty (denies-everything) policy.
    - `SessionPolicy::allow_all()` — permits every sink with no arg constraints; the permissive constructor Plan 03's POLICY-02 enforcement-order proof uses to show a permissive policy still cannot weaken an I2 Block, and the policy-agnostic default at existing (non-policy-specific) test call sites.
    - `SessionPolicy::broker_default()` — an EXPLICIT deny-by-default allowlist of the currently-callable production sinks (`email.send`, `file.create`, `file.write`, `process.exec`, `git.commit`, `http.request`, `github.pr`) with no arg constraints. This is NOT allow-everything: a future/unknown sink is NOT callable, preserving fail-closed while keeping existing end-to-end flows green. Plan 04's `bind_policy(None, ..)` returns this value — defining it here means Plan 04 never edits policy.rs.

    Register the module: add `pub mod policy;` and `pub use policy::SessionPolicy;` to `crates/runtime-core/src/lib.rs` alongside the existing `pub use` lines. Do NOT emit the deny reason type here if it lives in `executor_decision.rs` (Task 2) — reference it, or keep a small internal `PolicyDenyKind` in policy.rs that Task 2's `DenyReason::PolicyDeny` wraps; pick one and keep the two files' types consistent.
  </action>
  <verify>
    <automated>cargo test -p runtime-core policy:: 2>&1 | tail -20; ./scripts/check-invariants.sh 2>&1 | grep -A1 "Gate 2"</automated>
  </verify>
  <done>SessionPolicy exists in runtime-core with deny-by-default sink gate + coarse arg constraints, serde round-trips, has all three constructors (default_fail_closed() + allow_all() + broker_default()), broker_default() permits the seven production sinks and denies an unlisted sink, unit tests pass, and check-invariants Gate 2 (runtime-core purity) stays PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add distinct DenyReason::PolicyDeny variant</name>
  <files>crates/runtime-core/src/executor_decision.rs</files>
  <behavior>
    - DenyReason::PolicyDeny carries owned fields naming the offending sink and (optionally) arg plus a machine-readable constraint tag.
    - reason.code() returns the stable string "policy_deny".
    - reason.to_string() (Display) renders a non-empty message naming the sink.
    - PolicyDeny is NOT equal to and NOT conflated with any I2 Block — it is a Denied{reason} outcome, never a BlockedPendingConfirmation.
  </behavior>
  <action>
    Add a `PolicyDeny { sink: String, arg: Option<String>, constraint: String }` variant to the `DenyReason` enum in `crates/runtime-core/src/executor_decision.rs`, per DESIGN §5.1 and the §8 symbol table. Follow the v1.5 `SlotTypeMismatch` precedent EXACTLY: use plain owned types (`String`, `Option<String>`) — never borrowed `&'static str` references — because `DenyReason` derives `Deserialize` and crosses the IPC wire (worker.rs deserializes it); borrowed references are not deserializable. Field semantics: `sink` = the offending sink id string; `arg` = the offending arg name when the deny is arg-scoped, `None` when it is a sink-level deny; `constraint` = a short machine-readable tag of which policy rule refused (e.g. `sink-not-allowed`, `host-not-allowlisted`, `path-not-allowlisted`).

    Extend BOTH exhaustive matches with a new arm (there is no wildcard arm in either — a missing arm is a compile error, which is the intended fail-closed discipline): in `code()` return the stable literal `"policy_deny"`; in the `Display` impl render a message that names the sink (and arg when present) and the constraint. Do NOT introduce a second denial error type and do NOT add a free-form `reason: String` to `Denied` — PolicyDeny is an append to the ONE denial taxonomy, exactly as `DraftOnlySessionDeniesCommitIrreversible` and `SlotTypeMismatch` were.

    Add a unit test mirroring the existing `slot_type_mismatch_code_and_display` test: construct a `PolicyDeny`, assert `code() == "policy_deny"`, and assert the rendered Display string is non-empty and contains the sink name.
  </action>
  <verify>
    <automated>cargo test -p runtime-core executor_decision 2>&1 | tail -20</automated>
  </verify>
  <done>DenyReason::PolicyDeny exists with owned fields, both code() and Display arms are added (no wildcard), code() returns "policy_deny", and the unit test passes. The type compiles across the workspace (executor + brokerd + cli link it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trusted config → policy value | The policy schema is data authored by a trusted operator; this plan only defines the TYPE and its evaluation, not where the value comes from (Plan 04 binds it from a trusted source). |
| policy evaluation → executor decision | Policy evaluation must be structurally incapable of producing an Allow — it can only permit (fall through) or deny. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Elevation of Privilege | SessionPolicy evaluation surface | high | mitigate | The evaluation API returns `Result<(), _>` only — no Allow variant exists, so no policy value can grant authority it was not given (DESIGN §5.1/§5.2). Deny-by-default: an unlisted sink denies. |
| T-42-02 | Tampering | DenyReason taxonomy | medium | mitigate | PolicyDeny is a distinct machine-checkable variant (`code()=="policy_deny"`), never conflated with the I2 Block — so a policy-deny cannot masquerade as (or be masked by) an I2 Block in the audit trail (DESIGN §6 row 12). |
| T-42-03 | Information Disclosure | policy type purity | low | accept | SessionPolicy is a pure runtime-core type; no file I/O here keeps Gate 2 green. Parsing untrusted-path files is deferred to the brokerd binder (Plan 04), where the F1 containment check applies. |

No package-manager installs in this plan (v1.9 adds ZERO new crates; serde is already a workspace dependency) — no supply-chain legitimacy checkpoint required.
</threat_model>

<verification>
- `cargo test -p runtime-core` passes (policy + executor_decision unit tests).
- `cargo build --workspace` compiles cleanly with the new variant linked everywhere.
- `./scripts/check-invariants.sh` exits 0 — Gate 2 (runtime-core purity) in particular stays PASS (no std::fs/io/net/tokio/async in policy.rs).
- Note [[cfg-linux-test-blindness]]: all tests in this plan are pure Rust and run on the macOS host directly; there are no `#[cfg(target_os="linux")]` targets here.
</verification>

<success_criteria>
- SessionPolicy is a hardcoded-schema, deny-by-default, serde-round-tripping type in runtime-core with no Allow-producing evaluation path.
- DenyReason::PolicyDeny is a distinct, owned-field, exhaustively-matched variant with a stable `policy_deny` code.
- check-invariants Gate 2 stays green.
</success_criteria>

<output>
Create `.planning/phases/42-policy-layer-binding-enforcement-the-i2-boundary/42-01-SUMMARY.md` when done.
</output>
