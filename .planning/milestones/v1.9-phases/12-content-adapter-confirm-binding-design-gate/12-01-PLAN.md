---
phase: 12-content-adapter-confirm-binding-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-content-adapter-mediation.md
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - "A DESIGN doc under planning-docs/ defines email-body content-sensitivity (CONTENT-01/02) as a single hardcoded match arm for the email sink's args only — not a general taxonomy."
    - "The doc mandates collect-then-Block as a MUST (D-14): the executor's per-arg loop collects ALL sensitive+tainted args then Blocks as a set; the decision/anchor types become plural."
    - "The doc defines real-adapter mediation: the confined worker never performs the SMTP call, secrets live only in the broker, and tainted literals cannot reach a header (CRLF defense)."
    - "The doc preserves I2-over-I1 precedence unchanged (D-15) and states the plan-node API is untouched (D-18)."
  artifacts:
    - planning-docs/DESIGN-content-adapter-mediation.md
  key_links:
    - "collect-then-Block blocked-arg set shape (D-14) is the shared contract that DESIGN-confirm-binding.md (plan 02) binds its combined hash to (D-19)."
    - "content-sensitivity classification already exists at crates/executor/src/sink_sensitivity.rs:93-98 — the doc must state Phase 14 changes Step 3's consequence, not the classification (D-21)."
---

<objective>
Author `planning-docs/DESIGN-content-adapter-mediation.md`: the security-invariant DESIGN doc covering (1) email-body content-sensitivity semantics with the mandated collect-then-Block executor change (CONTENT-01/02, D-01/D-02/D-14 through D-18/D-21), and (2) real broker-mediated SMTP adapter mediation with CRLF/header-injection defense (SMTP-01/02/03/05, D-03 through D-07/D-22).

Purpose: This is half of the DESIGN-01 design gate. No executor/TCB code for CONTENT-01 or SMTP-05 may be written until this doc (plus DESIGN-confirm-binding.md) is adversarially reviewed and the gate record shows APPROVED/UNBLOCKED. The central deliverable is the explicit collect-then-Block mandate that closes the B1-reincarnation risk (a tainted recipient AND a tainted body on the same plan node both surfacing as Blocked, neither silently pre-empting the other).

Output: `planning-docs/DESIGN-content-adapter-mediation.md`.

## Artifacts this phase produces
- `planning-docs/DESIGN-content-adapter-mediation.md` (this plan)
- `planning-docs/DESIGN-confirm-binding.md` (plan 02)
- `planning-docs/DESIGN-GATE-RECORD-v1.3.md` (plan 03)
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-RESEARCH.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md
@CLAUDE.md

# Analog DESIGN docs — structure and MUST/MUST-NOT density to match
@planning-docs/DESIGN-taint-model.md
@planning-docs/DESIGN-session-trust-state.md
@planning-docs/DESIGN-REVIEW-v1.2-round1.md

# Existing code this doc describes (cite file/line, do NOT modify)
@crates/executor/src/lib.rs
@crates/executor/src/sink_sensitivity.rs
@crates/runtime-core/src/executor_decision.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content-sensitivity + collect-then-Block mandate sections</name>
  <files>planning-docs/DESIGN-content-adapter-mediation.md</files>
  <read_first>
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md (D-01, D-02, D-14 through D-18, D-21)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-RESEARCH.md (Pattern 1 collect-then-Block; content-sensitivity-already-exists finding; Pitfall 1)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md (Precedence-must-be-explicit; MUST-NOT-re-invoke section pattern; Done-When pattern)
    - crates/executor/src/lib.rs (current first-match-wins per-arg loop; Step 2 routing block; Step 3 content no-op)
    - crates/executor/src/sink_sensitivity.rs (is_content_sensitive already returns true for subject/body/attachment at lines 93-98)
    - crates/runtime-core/src/executor_decision.rs (singular SinkBlockedAnchor.arg / BlockedPendingConfirmation.literal, lines 108-153)
    - planning-docs/DESIGN-session-trust-state.md (§8 precedence-explicit analog; Done-When predicate §11)
  </read_first>
  <action>
    Create `planning-docs/DESIGN-content-adapter-mediation.md` with title line `# DESIGN-content-adapter-mediation.md — caprun Content-Sensitivity + Real SMTP Adapter Mediation (v1.3)` and an opening `## The Problem Being Solved` section, matching the analog docs' genre. Then author these named sections (exact section titles are the acceptance surface):

    `## Content-Sensitivity Classification (CONTENT-01/CONTENT-02)` — State as a MUST that content-sensitivity for the email sink's body arg is a single hardcoded match arm in the executor TCB, scoped to `email.send`'s args only, NEVER a general content-classification taxonomy or policy file (D-01, and CLAUDE.md's I2/CONTENT-01-hardcoded-in-executor constraint). State explicitly (D-21) that this classification ALREADY EXISTS — cite `crates/executor/src/sink_sensitivity.rs:93-98` (`is_content_sensitive` already returns true for `subject`/`body`/`attachment`) — and that Phase 14's real work is changing Step 3's CONSEQUENCE in `submit_plan_node` (currently a no-op / Tier-4-deferred fall-through) from "mark and fall through" to "Block, same as routing-sensitive," NOT adding a new match arm. Instruct the future reviewer to independently re-verify this claim against the actual source (D-21).

    `## Precedence — Routing-Block vs Body-Block (CONTENT-01, D-02)` — A named, greppable subsection (mirroring DESIGN-session-trust-state.md §8's precedence discipline). State as a MUST that a tainted recipient AND a tainted body on the same plan node BOTH surface as Blocked; neither silently pre-empts, masks, or is dropped. Reference the historical B1 precedence blocker (planning-docs/DESIGN-REVIEW-v1.2-round1.md) as the failure mode this section exists to prevent, reincarnated for the body arg.

    `## Collect-then-Block (D-14, MUST)` — State as a hard MUST (not an implementation detail deferred to Phase 14): the executor's per-arg loop in `submit_plan_node` changes from first-match-wins early-return to collect-ALL-sensitive-and-tainted-args-in-one-pass-then-Block-as-a-set. Cite the current first-match-wins shape (`crates/executor/src/lib.rs` per-arg loop returns inside the loop on the first tainted routing-sensitive arg) and the current singular anchor shape (`crates/runtime-core/src/executor_decision.rs:108-153`, `SinkBlockedAnchor.arg: String` and `BlockedPendingConfirmation.literal: String`). Specify that `ExecutorDecision::BlockedPendingConfirmation` and `SinkBlockedAnchor` become PLURAL — a `Vec<BlockedArg>`, each element carrying its own literal + taint + provenance_chain. Mark any Rust snippet "illustrative shape, not literal code to paste" per the PATTERNS.md convention. Add three MUST sub-statements: (D-15) I2-over-I1 precedence is PRESERVED unchanged — the collect-all loop must complete with NO Block before Step 0.5's draft-only check runs; do not reorder this; (D-16) the EXTRACT-02 unbroken-edge + anti-staple hard gate (raw-read Event to extractor-derived ValueNodes to blocked sink args) must hold for EVERY blocked arg in the set, not just one — a two-tainted-arg Block with only one edge proven is a partial pass, not a pass; (D-17) single-shot semantics extend to the set — confirm authorizes the WHOLE (sink, all-blocked-args, combined-digest) set or denies ALL; no partial confirm of a subset.

    `## Plan-Node API Is Untouched (D-18)` — State as a MUST that this is internal decision/anchor hardening only: `PlanNode { sink, args: Vec<ValueNode> }` input shape is unchanged; only the internal `ExecutorDecision`/`SinkBlockedAnchor` types become plural. This is not a reopening of the locked plan-node API (CLAUDE.md).

    Author every invariant as a hard `MUST`/`MUST NOT`, never "should"/"generally," matching the analog docs' density.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-content-adapter-mediation.md && grep -qi 'collect-then-Block' planning-docs/DESIGN-content-adapter-mediation.md && grep -q 'sink_sensitivity.rs' planning-docs/DESIGN-content-adapter-mediation.md && grep -qi 'precedence' planning-docs/DESIGN-content-adapter-mediation.md && grep -q 'Step 0.5' planning-docs/DESIGN-content-adapter-mediation.md</automated>
  </verify>
  <acceptance_criteria>
    - File `planning-docs/DESIGN-content-adapter-mediation.md` exists.
    - Contains a section titled `## Content-Sensitivity Classification (CONTENT-01/CONTENT-02)` stating the single-hardcoded-match-arm scope as a MUST and citing `crates/executor/src/sink_sensitivity.rs:93-98` as pre-existing (D-01, D-21).
    - Contains a section titled `## Precedence — Routing-Block vs Body-Block (CONTENT-01, D-02)` stating as a MUST that a tainted recipient AND a tainted body both surface as Blocked (D-02).
    - Contains a section titled `## Collect-then-Block (D-14, MUST)` stating the per-arg loop collects ALL sensitive+tainted args then Blocks as a set, with `ExecutorDecision::BlockedPendingConfirmation`/`SinkBlockedAnchor` becoming plural `Vec<BlockedArg>` (D-14), preserving I2-over-I1 via Step 0.5 running only after the loop completes with no Block (D-15), requiring the unbroken-edge gate for EVERY blocked arg (D-16), and single-shot over the whole set / no partial confirm (D-17).
    - Contains a `## Plan-Node API Is Untouched (D-18)` statement.
    - Every invariant is phrased as MUST/MUST NOT (grep confirms no reliance on "should" for the collect-then-Block rules).
  </acceptance_criteria>
  <done>DESIGN-content-adapter-mediation.md exists with the four content-sensitivity/collect-then-Block sections above, each resolving its cited D-IDs as hard MUSTs; the grep verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Real-adapter mediation + CRLF/header-injection defense sections</name>
  <files>planning-docs/DESIGN-content-adapter-mediation.md</files>
  <read_first>
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md (D-03 through D-07, D-22)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-RESEARCH.md (Pattern 2 typed SMTP construction; Pitfall 3 tainted-literal-reaches-header; Pitfall 4 boring-tls; Pitfall 5 reuse existing seccomp net-deny; Architectural Responsibility Map)
    - planning-docs/DESIGN-content-adapter-mediation.md (the file being extended — Task 1's sections)
    - crates/executor/src/lib.rs (existing literal_sha256 digest pattern, lines 112-116)
  </read_first>
  <action>
    Append to `planning-docs/DESIGN-content-adapter-mediation.md` these named sections:

    `## Adapter Mediation Boundary (SMTP-01/02, D-03/D-04)` — State as MUSTs: the confined worker NEVER performs the SMTP call; the broker/adapter performs it ONLY after executor-authorize + human-confirm (D-03); SMTP secrets/credentials live ONLY in the broker process — MUST NOT appear in the worker's env, args, or in any plan-node payload that could reach the tainted/confined context (D-04). Locate the new adapter in `crates/brokerd/src/sinks/email_smtp.rs` (broker-resident, replacing the existing `invoke_email_send_stub`), per the RESEARCH Architectural Responsibility Map.

    `## Kernel-Enforced Negative Net Assertion (SMTP-01, D-05)` — State as a MUST that a confined worker's direct attempt to open an SMTP connection MUST FAIL under default-deny net — a claim about the sandbox boundary, testable on real Linux. Point this assertion at the EXISTING mechanism (do not design a new confinement primitive): `crates/sandbox/src/seccomp.rs`'s `apply_worker_filter()` denies `socket(AF_INET, ...)`/`socket(AF_INET6, ...)` with EPERM, and the existing `confine-probe`/`confinement_integration.rs` `negative_net` pattern proves it. Specify that Phase 13's negative test reuses this pattern to attempt an actual `connect()` to the Mailpit host:port under confinement and assert failure (Pitfall 5).

    `## Local Capture SMTP Target (SMTP-03, D-06)` — State as a MUST that the acceptance-gate test targets a LOCAL capture SMTP (Mailpit — the maintained successor to abandoned MailHog), Linux-verifiable via Colima+Docker, no live infra dependency; live SES/real inbox is explicitly OUT of gate scope (downgraded, non-gated, post-milestone config-swap — SMTP-04). The doc MUST NOT design for live-SES as a milestone requirement.

    `## Wire-Message Construction — CRLF/Header-Injection Defense (SMTP-05, D-07/D-22)` — State as MUSTs: the adapter constructs the outgoing message EXCLUSIVELY through `lettre`'s typed `Message::builder()` setters (`.to()`, `.cc()`, `.bcc()`, `.subject()`, `.body()`) pinned to `lettre >= 0.11.22`; it MUST NOT use `dangerous_new_pre_encoded` or any `format!`-built / string-concatenated header line. Explain the concrete defense answering D-07: recipient/subject reject or RFC2047-encode raw CR/LF by lettre's allow-list grammar and header encoder (cite the verified mechanics from RESEARCH), and the body is inert to header injection because it is placed after the blank-line header/body separator AS LONG AS the adapter never concatenates the body literal into the header-construction chain — a body containing a CRLF-then-`Bcc:` sequence stays body text. Forbid enabling the `boring-tls` feature (RUSTSEC-2026-0141, Pitfall 4). State as a HARD requirement (D-22) that regardless of lettre's by-construction defense, Phase 13 MUST include a passing adversarial CRLF-injection fixture test: specify what it must assert — that a tainted body carrying a CRLF-then-`Bcc:`/extra-recipient sequence produces exactly the intended envelope recipients at Mailpit (no smuggled recipient), verified via Mailpit's HTTP API. Recommend a grep-based negative assertion (mirroring `check-invariants.sh` style) that no `format!` builds a header line and the token `dangerous_new_pre_encoded` never appears in the adapter source.

    End the doc with a `## Done-When (Acceptance Predicate)` section: a named, falsifiable list of conditions this doc's design is "done when" satisfied (collect-then-Block set present; precedence explicit; worker-never-sends; secrets broker-only; CRLF defense specified with the mandated fixture) — matching the analog docs' terminal Done-When/Acceptance-Predicate discipline. Label any Rust snippet "illustrative shape, not literal code."
  </action>
  <verify>
    <automated>grep -q 'email_smtp' planning-docs/DESIGN-content-adapter-mediation.md && grep -q 'dangerous_new_pre_encoded' planning-docs/DESIGN-content-adapter-mediation.md && grep -qi 'seccomp\|default-deny' planning-docs/DESIGN-content-adapter-mediation.md && grep -qi 'Mailpit' planning-docs/DESIGN-content-adapter-mediation.md && grep -qi 'Done-When\|Acceptance Predicate' planning-docs/DESIGN-content-adapter-mediation.md</automated>
  </verify>
  <acceptance_criteria>
    - Contains `## Adapter Mediation Boundary (SMTP-01/02, D-03/D-04)` stating worker-never-sends and secrets-broker-only as MUSTs, locating the adapter at `crates/brokerd/src/sinks/email_smtp.rs`.
    - Contains `## Kernel-Enforced Negative Net Assertion (SMTP-01, D-05)` pointing at the existing `crates/sandbox/src/seccomp.rs` `apply_worker_filter()` / `confine-probe` mechanism, not a new primitive.
    - Contains `## Local Capture SMTP Target (SMTP-03, D-06)` naming Mailpit and stating live-SES is out of gate scope.
    - Contains `## Wire-Message Construction — CRLF/Header-Injection Defense (SMTP-05, D-07/D-22)` requiring lettre typed builder only, forbidding `dangerous_new_pre_encoded`/`format!`-built headers and `boring-tls`, explaining body-inertness, and mandating a Phase-13 CRLF fixture with a specified assertion (D-22).
    - Contains a terminal `## Done-When (Acceptance Predicate)` section.
    - grep verify passes.
  </acceptance_criteria>
  <done>The adapter-mediation and CRLF-defense sections plus a terminal Done-When predicate are appended; all cited D-IDs (D-03 through D-07, D-22) are resolved as hard MUSTs; grep verify passes.</done>
</task>

<task type="auto">
  <name>Task 3: Completeness self-check against D-01/02/14-18/21 and SMTP-01/02/03/05</name>
  <files>planning-docs/DESIGN-content-adapter-mediation.md</files>
  <read_first>
    - planning-docs/DESIGN-content-adapter-mediation.md (the full authored doc)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-VALIDATION.md (Per-Task Verification Map grep tokens)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md (MUST/MUST-NOT density expectation)
  </read_first>
  <action>
    Run the completeness grep checklist over `planning-docs/DESIGN-content-adapter-mediation.md` (the VALIDATION.md Wave-1 tokens plus the D-ID coverage below) and repair any gap in-place before committing. The doc MUST resolve every one of D-01, D-02, D-14, D-15, D-16, D-17, D-18, D-21 (content/collect-then-Block) and D-03, D-04, D-05, D-06, D-07, D-22 (adapter/CRLF). If any token or D-ID is missing, add the missing MUST statement to the appropriate section — do NOT weaken any existing statement. Confirm every collect-then-Block and CRLF-defense rule is phrased MUST/MUST NOT, and confirm MUST-density is comparable to the analog docs (dozens of MUST occurrences, per PATTERNS.md). Do not introduce any executor/TCB/adapter source file — this phase is documentation only.
  </action>
  <verify>
    <automated>for d in D-01 D-02 D-14 D-15 D-16 D-17 D-18 D-21 D-03 D-04 D-05 D-06 D-07 D-22; do grep -q "$d" planning-docs/DESIGN-content-adapter-mediation.md || { echo "MISSING $d"; exit 1; }; done; test "$(grep -c 'MUST' planning-docs/DESIGN-content-adapter-mediation.md)" -ge 20 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every D-ID in {D-01, D-02, D-14, D-15, D-16, D-17, D-18, D-21, D-03, D-04, D-05, D-06, D-07, D-22} appears verbatim in the doc (the grep loop exits 0).
    - `grep -c 'MUST'` on the doc is >= 20 (density comparable to the analog DESIGN docs).
    - No new file exists under `crates/` (documentation-only phase preserved).
  </acceptance_criteria>
  <done>All 14 cited D-IDs are present, MUST-density >= 20, no source code was added; the doc is ready for the plan-02 companion and the plan-03 adversarial review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author session → design gate | The session authoring this DESIGN doc is untrusted to also approve it (D-11). Soundness is established only by the fresh-context adversarial review in plan 03. |
| (design-time, forward-looking) tainted body literal → SMTP header | The doc specifies how a tainted body literal is prevented from crossing into an envelope/header at the future adapter (SMTP-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | executor decision shape (single-arg Block) | critical | mitigate | Doc mandates collect-then-Block as a MUST (D-14) so a confirmed recipient-block cannot silently ship an unconfirmed tainted body (the B1-reincarnation failure mode). |
| T-12-02 | Elevation of Privilege | future SMTP adapter wire-message construction | high | mitigate | Doc requires lettre typed builder only, forbids `dangerous_new_pre_encoded`/string-built headers, and mandates a Phase-13 CRLF fixture (SMTP-05, D-07/D-22). |
| T-12-03 | Information Disclosure | SMTP secrets reaching the confined/tainted context | high | mitigate | Doc states secrets live only in the broker, absent from worker env/args/plan-node payload (D-04); worker's direct SMTP attempt fails under seccomp default-deny net (D-05). |
| T-12-04 | Repudiation | doc authored and self-approved by one session | high | transfer | Approval is deferred to plan 03's fresh-context adversarial review arranged by caprun-opus-77 (D-11); this plan produces the doc only. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this documentation-only phase. `lettre` is added in Phase 13; its legitimacy audit (verdict OK) is already recorded in 12-RESEARCH.md's Package Legitimacy Audit. |
</threat_model>

<verification>
- `planning-docs/DESIGN-content-adapter-mediation.md` exists and resolves every cited D-ID (D-01/02/14-18/21, D-03-07/22) as a hard MUST/MUST NOT.
- No source file under `crates/` was created or modified (documentation-only phase preserved).
- All three task-level `<automated>` greps pass.
</verification>

<success_criteria>
The content-adapter-mediation DESIGN doc exists with explicit, greppable sections mandating collect-then-Block (D-14), routing-vs-body precedence (D-02), worker-never-sends adapter mediation (D-03/04), a kernel-enforced negative net assertion pointed at the existing seccomp mechanism (D-05), a local Mailpit target (D-06), and CRLF/header-injection defense with a mandated Phase-13 fixture (D-07/D-22) — ready for the plan-03 adversarial review.
</success_criteria>

<output>
Create `.planning/phases/12-content-adapter-confirm-binding-design-gate/12-01-SUMMARY.md` when done.
</output>
