---
phase: 12-content-adapter-confirm-binding-design-gate
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - planning-docs/DESIGN-GATE-RECORD-v1.3.md
autonomous: false
requirements: [DESIGN-01]
user_setup:
  - service: caprun-opus-77 (FAMP coordinator)
    why: "D-11 requires the adversarial review to be arranged by caprun-opus-77 via FAMP — the authoring session MUST NOT self-review. The executor stops at the checkpoint and flags opus to arrange a fresh-context reviewer."
must_haves:
  truths:
    - "A DESIGN-GATE-RECORD-v1.3.md exists following the DESIGN-GATE-RECORD-v1.2.md structure: sha256 table of both DESIGN docs, a checklist mapped to CONTENT-01/02 + SMTP-01/02/03/05 + CONFIRM-03, a Both-Documents-Soundness section, How-to-Verify steps, Decision, and Gate status."
    - "The review is genuinely adversarial and NOT self-review — arranged by caprun-opus-77 (fresh-context reviewer) per D-11 — and actively attacks the three D-12 vectors (a: unconfirmable dead end; b: hash over pre- vs post-transform bytes; c: tainted literal reaching a header)."
    - "Round-1 finding of a BLOCKER is an expected, successful outcome; the phase iterates fix→re-review until Decision: APPROVED / Gate status: UNBLOCKED."
    - "No executor/TCB code for CONTENT-01, SMTP-05, or CONFIRM-03 exists in the repo until this record shows APPROVED/UNBLOCKED."
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.3.md
  key_links:
    - "Each D-12(a/b/c) attack vector traces to an actual section + file/line in the DESIGN docs, mirroring how B1's fix was traced in DESIGN-GATE-RECORD-v1.2.md — soundness by content, not keyword presence."
    - "Gate status: UNBLOCKED is exactly what Phases 13-16 grep for before writing any executor/adapter/confirmation code."
---

<objective>
Produce `planning-docs/DESIGN-GATE-RECORD-v1.3.md` and drive the DESIGN-01 adversarial-review gate to closure. This plan authors the gate-record scaffold (completeness checklist + sha256 table + How-to-Verify steps mapped to the three D-12 attack vectors), then STOPS at a non-autonomous checkpoint: the executor MUST NOT self-review its own DESIGN docs (D-11). It flags caprun-opus-77 via FAMP to arrange a fresh-context adversarial reviewer, waits for findings, and iterates (fix DESIGN docs → re-review) until the record shows Decision: APPROVED and Gate status: UNBLOCKED.

Purpose: This is the gate that unblocks Phases 13-16. A round-1 BLOCKER (as v1.2's review found) is a normal, expected, successful outcome — the design gate "earns its cost" by catching a real flaw before any code. The phase is complete only when the record is APPROVED/UNBLOCKED after a genuine adversarial pass.

Output: `planning-docs/DESIGN-GATE-RECORD-v1.3.md` (Decision: APPROVED, Gate status: UNBLOCKED).

## Artifacts this phase produces
- `planning-docs/DESIGN-content-adapter-mediation.md` (plan 01)
- `planning-docs/DESIGN-confirm-binding.md` (plan 02)
- `planning-docs/DESIGN-GATE-RECORD-v1.3.md` (this plan)
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md
@CLAUDE.md

# The gate-record convention to copy exactly (D-13)
@planning-docs/DESIGN-GATE-RECORD-v1.2.md
@planning-docs/DESIGN-REVIEW-v1.2-round1.md

# The DESIGN docs under review (produced by plans 01 and 02)
@planning-docs/DESIGN-content-adapter-mediation.md
@planning-docs/DESIGN-confirm-binding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DESIGN-GATE-RECORD-v1.3.md scaffold + completeness checklist</name>
  <read_first>
    - planning-docs/DESIGN-GATE-RECORD-v1.2.md (top-level structure, sha256 table format, checklist item format, "Both Documents — Soundness" section, How-to-Verify numbered steps, Decision + Gate-status blocks)
    - planning-docs/DESIGN-REVIEW-v1.2-round1.md (round-1 severity-prefixed finding format: BLOCKER/MAJOR/minor, "What's right", "Suggested resolution order")
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md (D-11, D-12, D-13, D-21)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md (DESIGN-GATE-RECORD-v1.3.md pattern assignments)
    - planning-docs/DESIGN-content-adapter-mediation.md, planning-docs/DESIGN-confirm-binding.md (the docs under review)
  </read_first>
  <action>
    Create `planning-docs/DESIGN-GATE-RECORD-v1.3.md` copying the top-level structure of `DESIGN-GATE-RECORD-v1.2.md`: title `# DESIGN Gate Record — v1.3`, a Date/Reviewer/Phase/Review-round header block, `## Documents Under Review`, `## Checklist`, `## Both Documents — Soundness`, `## How to Verify (Human Review Steps)`, `## Decision`, `## Gate status`.

    `## Documents Under Review` — a sha256 table with both docs, computed with `shasum -a 256 planning-docs/DESIGN-content-adapter-mediation.md planning-docs/DESIGN-confirm-binding.md` at authoring time; include the exact "computed at authoring time; the reviewer MUST re-run `shasum -a 256 <files>` and confirm the values match before setting Decision: APPROVED" phrasing from the v1.2 record.

    `## Checklist` — one grep-verified item per requirement group, box checked only if the grep matched: CONTENT-01/02 (content-sensitivity single match arm + collect-then-Block plural anchor), SMTP-01/02 (worker-never-sends + secrets-broker-only), SMTP-03 (Mailpit local capture target), SMTP-05 (lettre typed builder, no `dangerous_new_pre_encoded`/string-built headers, CRLF fixture mandated), CONFIRM-03 (combined digest over full set, post-transform, no truncation), plus the collect-then-Block precedence item (D-02/D-14) and the D-21 re-verification item (record that Phase 14's work is Step 3's consequence, not new classification). Each item states the grep token(s) and the matched count, matching the v1.2 checklist-item format.

    `## Both Documents — Soundness` — a directed-adversarial-re-read section (NOT grep-presence) with one item per D-12 attack vector, each requiring the reviewer to trace the property to an actual section + file/line in the DESIGN docs (mirroring how B1's fix was traced through DESIGN-session-trust-state.md §8/§9/§11 in the v1.2 record):
      - Item — D-12(a): Can CONTENT-01's body-block and the routing-block compose into an unconfirmable dead end (the B1 failure mode reincarnated for the body arg)? The reviewer must verify collect-then-Block + single-shot-over-the-set actually make both args confirmable in one decision.
      - Item — D-12(b): Can CONFIRM-03's literal-binding hash be computed over pre-transformation bytes instead of the post-EXTRACT-03-transformation bytes actually sent? The reviewer must verify the mint-after-transform + no-drift MUSTs close this.
      - Item — D-12(c): Does SMTP-05's message construction have any path where a tainted literal reaches a header? The reviewer must verify the lettre-typed-builder-only + no-`dangerous_new_pre_encoded` + body-inertness argument closes this.

    `## How to Verify (Human Review Steps)` — numbered MUST-imperative steps (copy the v1.2 record's 5-step structure) ending in "If satisfied: set Decision to APPROVED and Gate status to UNBLOCKED. If not satisfied: set Decision to NEEDS REVISION, list the gaps, and the phase loops again." Include an explicit step directing the reviewer to independently re-verify the D-21 `is_content_sensitive`-already-exists claim against `crates/executor/src/sink_sensitivity.rs`.

    Initialize `## Decision` to `**Decision:** NEEDS REVISION (pending fresh-context adversarial review — see Task 2)` and `## Gate status` to `**crates/executor / crates/brokerd (CONTENT-01 / SMTP-05 / CONFIRM-03 additions) is: BLOCKED**` with an `Available resolutions: [ UNBLOCKED / BLOCKED ]` line. These fields are set to APPROVED/UNBLOCKED ONLY by Task 2 after the review, never in this task.

    Do NOT set APPROVED in this task. Do NOT introduce any source file under `crates/`.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -q 'Documents Under Review' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'shasum -a 256' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -q 'D-12' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'Both Documents.*Soundness' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'Gate status' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'BLOCKED' planning-docs/DESIGN-GATE-RECORD-v1.3.md</automated>
  </verify>
  <acceptance_criteria>
    - File `planning-docs/DESIGN-GATE-RECORD-v1.3.md` exists with the v1.2-record top-level structure.
    - `## Documents Under Review` has a sha256 table naming both DESIGN docs and the "reviewer MUST re-run shasum before APPROVED" phrasing.
    - `## Checklist` has one grep-verified item per CONTENT-01/02, SMTP-01/02/03/05, CONFIRM-03, plus the collect-then-Block precedence and D-21 items.
    - `## Both Documents — Soundness` has a dedicated, file/line-tracing item for EACH of D-12(a), D-12(b), D-12(c).
    - `## How to Verify` has numbered MUST-imperative steps including the D-21 independent re-verification step and the APPROVED/NEEDS-REVISION exit test.
    - `## Decision` reads NEEDS REVISION and `## Gate status` reads BLOCKED (not yet approved in this task).
    - No new file under `crates/`.
  </acceptance_criteria>
  <done>The gate-record scaffold exists with the sha256 table, the full checklist, a soundness item per D-12 vector, numbered How-to-Verify steps, and Decision/Gate-status initialized to NEEDS REVISION/BLOCKED; grep verify passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: STOP — flag caprun-opus-77 for the genuine adversarial review; iterate to APPROVED</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.3.md</files>
  <action>
    This is a checkpoint, NOT an autonomous authoring task — the executor MUST NOT self-review or self-approve the DESIGN docs (D-11). STOP here. Flag caprun-opus-77 via FAMP to arrange a fresh-context adversarial reviewer, hand over the two doc paths + sha256 hashes + the three D-12 attack vectors, WAIT for findings, iterate (record finding in v1.2 severity format → fix DESIGN doc(s) → re-hash → re-review) until no blocker/major remains, and only then set Decision: APPROVED / Gate status: UNBLOCKED in planning-docs/DESIGN-GATE-RECORD-v1.3.md. Full procedure is in the how-to-verify block below.
  </action>
  <read_first>
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md (D-11 non-self-review; D-12 the three vectors; D-13 gate-record convention; Specific Ideas "don't self-review your own design doc")
    - planning-docs/DESIGN-GATE-RECORD-v1.2.md (Decision field "History of this Decision field" — the recorded revert-when-self-reviewed precedent)
    - planning-docs/DESIGN-GATE-RECORD-v1.3.md (the scaffold from Task 1)
    - planning-docs/DESIGN-content-adapter-mediation.md, planning-docs/DESIGN-confirm-binding.md (the docs under review)
  </read_first>
  <what-built>
    Task 1 authored the DESIGN-GATE-RECORD-v1.3.md scaffold (sha256 table, checklist, per-D-12 soundness items, How-to-Verify steps, Decision=NEEDS REVISION / Gate=BLOCKED). Both DESIGN docs (plans 01 and 02) are complete. The completeness greps pass. What remains is the one thing this session MUST NOT do itself: judge the docs' SOUNDNESS.
  </what-built>
  <how-to-verify>
    This is the genuine-adversarial-review HARD GATE. The executor MUST NOT self-review or self-approve (D-11, and the recorded v1.2 revert precedent). Steps:

    1. STOP. Do not set Decision: APPROVED. Post a message to caprun-opus-77 via FAMP stating the DESIGN docs are ready for adversarial review, listing the two doc paths + their sha256 hashes, and the three D-12 attack vectors the reviewer must attack: (a) can CONTENT-01's body-block + routing-block compose into an unconfirmable dead end (B1 reincarnated for the body arg); (b) can CONFIRM-03's binding hash be computed over pre- vs post-EXTRACT-03-transformation bytes; (c) does SMTP-05's message construction have any path where a tainted literal reaches a header. Ask opus to arrange a fresh-context reviewer per DEC-ai-review-satisfies-human-gate.
    2. WAIT for the fresh-context reviewer's findings. The reviewer must trace each of the three vectors to an actual section + file/line in the DESIGN docs (soundness by content, not keyword presence), independently re-verify the D-21 `is_content_sensitive`-already-exists claim against `crates/executor/src/sink_sensitivity.rs`, and re-run `shasum -a 256` on both docs to confirm the hashes match.
    3. If the review finds a BLOCKER/MAJOR (an expected, successful round-1 outcome — as v1.2's did): record the finding in the gate record using the v1.2 round-1 severity-prefixed format (BLOCKER/MAJOR/minor + "What's right" + "Suggested resolution order"), FIX the DESIGN doc(s) accordingly, re-hash them in the Documents Under Review table, and request a re-review. Repeat this fix→re-review loop until no blocker/major remains.
    4. Only when the fresh-context reviewer signs off with no unresolved blocker/major: set `## Decision` to APPROVED (naming the reviewer and the DEC-ai-review-satisfies-human-gate authorization) and `## Gate status` to UNBLOCKED, dated.

    Expected outcome for phase completion: `planning-docs/DESIGN-GATE-RECORD-v1.3.md` shows `Decision: APPROVED` and `Gate status: UNBLOCKED`, with each D-12 vector traced to file/line and the review attributed to a reviewer that is NOT this authoring session.
  </how-to-verify>
  <verify>
    <automated>grep -q 'Decision:.*APPROVED' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'Gate status' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -qi 'UNBLOCKED' planning-docs/DESIGN-GATE-RECORD-v1.3.md && grep -q 'D-12' planning-docs/DESIGN-GATE-RECORD-v1.3.md</automated>
    <human-check>caprun-opus-77 confirms a fresh-context reviewer (not this authoring session) performed the adversarial pass, attacked all three D-12 vectors with file/line tracing, and any blocker/major was resolved before APPROVED.</human-check>
  </verify>
  <acceptance_criteria>
    - The gate record shows `Decision: APPROVED` and `Gate status: UNBLOCKED`.
    - The review was performed by a fresh-context reviewer arranged by caprun-opus-77 — NOT the session that authored the DESIGN docs (D-11); the record attributes it explicitly.
    - Each D-12 vector (a/b/c) has a soundness finding traced to an actual section + file/line in the DESIGN docs, not merely a keyword count.
    - The D-21 `is_content_sensitive`-already-exists claim was independently re-verified against `crates/executor/src/sink_sensitivity.rs` and recorded.
    - Any round-1 blocker/major was recorded (v1.2 severity format) and resolved via a fix→re-review loop before APPROVED.
    - No executor/TCB code for CONTENT-01, SMTP-05, or CONFIRM-03 exists in `crates/` at approval time (documentation-only phase preserved).
  </acceptance_criteria>
  <resume-signal>caprun-opus-77's arranged reviewer signs off with no unresolved blocker/major, and the gate record shows Decision: APPROVED / Gate status: UNBLOCKED. Reply "approved" or list the outstanding findings.</resume-signal>
  <done>planning-docs/DESIGN-GATE-RECORD-v1.3.md shows Decision: APPROVED and Gate status: UNBLOCKED, the review is attributed to a fresh-context reviewer (not this authoring session) arranged by caprun-opus-77, each D-12 vector is traced to file/line, and any round-1 blocker/major was resolved via a fix→re-review loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author session → reviewer | The session that authored the DESIGN docs MUST NOT also review/approve them. Soundness is established only by a fresh-context reviewer arranged by caprun-opus-77 (D-11). |
| gate record → downstream phases | Phases 13-16 grep this record's `Gate status: UNBLOCKED` before writing any executor/adapter/confirmation code. A falsely-APPROVED record would authorize unsound code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Repudiation / Spoofing | self-review masquerading as adversarial review | critical | mitigate | Checkpoint is `autonomous: false`; executor MUST stop and flag caprun-opus-77; the recorded v1.2 revert-when-self-reviewed precedent is the enforcement pattern (D-11). |
| T-12-10 | Tampering | gate record marked APPROVED without attacking the three vectors | high | mitigate | Both-Documents-Soundness section requires a file/line-traced finding per D-12(a/b/c); How-to-Verify exit test forbids APPROVED until all three are resolved. |
| T-12-11 | Elevation of Privilege | executor/TCB code written before the gate is UNBLOCKED | critical | mitigate | Success criterion + acceptance criteria assert no CONTENT-01/SMTP-05/CONFIRM-03 code exists in crates/ until APPROVED/UNBLOCKED (ROADMAP criterion 5). |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this documentation-only phase. |
</threat_model>

<verification>
- `planning-docs/DESIGN-GATE-RECORD-v1.3.md` exists and, at phase completion, shows Decision: APPROVED / Gate status: UNBLOCKED.
- The review is attributed to a fresh-context reviewer arranged by caprun-opus-77, not the authoring session (D-11).
- Each D-12(a/b/c) vector is traced to a section + file/line in the DESIGN docs.
- No source file under `crates/` implementing CONTENT-01, SMTP-05, or CONFIRM-03 exists at approval time.
</verification>

<success_criteria>
The DESIGN-01 gate is closed: DESIGN-GATE-RECORD-v1.3.md shows Decision: APPROVED and Gate status: UNBLOCKED after a genuine, non-self, fresh-context adversarial review (arranged by caprun-opus-77) that attacked all three D-12 vectors with file/line tracing and resolved any blocker via a fix→re-review loop — unblocking Phases 13-16, with no executor/TCB code for CONTENT-01/SMTP-05/CONFIRM-03 written beforehand.
</success_criteria>

<output>
Create `.planning/phases/12-content-adapter-confirm-binding-design-gate/12-03-SUMMARY.md` when done.
</output>
