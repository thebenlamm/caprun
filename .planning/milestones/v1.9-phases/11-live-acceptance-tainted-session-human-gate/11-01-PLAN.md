---
phase: 11-live-acceptance-tainted-session-human-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/tests/live_acceptance_tainted_session.rs
  - cli/caprun/tests/s9_live_block.rs
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03]

must_haves:
  truths:
    - "On real Linux, the deny test runs a hostile-file read → I1 demotion → I2 block → `caprun deny` and observes no effect ever proceeds (deny exits 2, neither reports/pwned.txt nor intended_output.txt exists)."
    - "On real Linux, the confirm test runs the same scenario but `caprun confirm` releases the effect exactly once (confirm exits 0, reports/pwned.txt now exists under the workspace root)."
    - "Both tests prove ACC-03: verify_chain() is true and the parent_id walk fd_granted → file_read → session_demoted → sink_blocked → confirm_granted/confirm_denied holds as an unbroken chain."
    - "s9_live_block.rs's stale sink_blocked parent_id assertion is corrected to point at session_demoted, and the corrected test passes on real Linux."
    - "A written acceptance record (in SUMMARY.md) captures the actual Colima+Docker run: the two run commands, exit codes, and the queried audit-DAG parent_id rows for both the deny and confirm runs."
  artifacts:
    - cli/caprun/tests/live_acceptance_tainted_session.rs
    - cli/caprun/tests/s9_live_block.rs
    - ".planning/phases/11-live-acceptance-tainted-session-human-gate/11-01-SUMMARY.md (acceptance record, D-06)"
  key_links:
    - "effect_id hand-off from process 1 to process 2: read from the persisted sink_blocked event's anchor.effect_id (never stdout scraping)."
    - "Persistent SQLite audit DB path passed as the same positional arg to BOTH subprocess invocations — never :memory: (Pitfall 2)."
    - "Same temp workspace-root directory kept alive across both subprocesses so confirm's live sink can reopen it (Pitfall 3)."
    - "sink_blocked.parent_id == session_demoted.id (NOT file_read.id) — the Pitfall-1 edge, asserted in both the new test and the corrected s9_live_block.rs."
---

<objective>
Prove — live on real Linux `caprun`, for BOTH the deny and confirm outcomes — that the full v1.2 chain composes end to end: a confined worker reads a hostile workspace file → the session is demoted to draft-only (I1, `mint_from_read`) → the same extracted value routed to `file.create`'s `path` arg is Blocked (I2) → a human decision via `caprun deny`/`caprun confirm` either durably blocks the effect or releases it exactly once — and the audit DAG shows one unbroken causal chain (`fd_granted → file_read → session_demoted → sink_blocked → confirm_granted/confirm_denied`) for both runs.

This is the v1.2 DONE gate (ACC-01/02/03). No new production mechanism is built — Phases 9 (session demotion) and 10 (confirm/deny release) already implement and unit/integration-test every piece. This plan writes a live, Linux-verified composition proof, fixes a pre-existing stale assertion that would otherwise derail the Linux run (Pitfall 1), and captures a written acceptance record from the actual Colima+Docker output (D-06).

Purpose: Close the v1.2 milestone with a genuine, audited, end-to-end acceptance proof mirroring Phase 4 (§9) and Phase 7 (full acceptance) discipline.
Output: One new Linux-gated integration test file with two `#[test]` functions plus a guard test; a one-assertion fix in `s9_live_block.rs`; a Colima+Docker acceptance record folded into SUMMARY.md.

CONTEXT.md decisions implemented: D-01 (double-duty read: the same hostile file triggers I1 demotion AND provides the I2-blocked value, no `--seed-from-file` needed), D-02 (reuse the existing `file.create` sink, not a new sink), D-03 (new Linux-gated Rust integration test mirroring `s9_live_block.rs`/`confirm.rs`, run via the Colima+Docker recipe), D-04 (two test functions in one file, isolated temp dir/DB per run, reuse `confirm.rs`'s exit-code contract), D-05 (programmatic `caprun confirm`/`caprun deny` invocation satisfies "human decision" for ACC-01/02 — not a requirement to type the commands by hand), D-06 (written acceptance record folded into SUMMARY.md, not a standalone doc).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-acceptance-tainted-session-human-gate/11-RESEARCH.md
@.planning/phases/11-live-acceptance-tainted-session-human-gate/11-PATTERNS.md
@.planning/phases/11-live-acceptance-tainted-session-human-gate/11-CONTEXT.md

# Analog source files the executor MUST read before writing the new test
@cli/caprun/tests/s9_live_block.rs
@cli/caprun/tests/confirm.rs
@crates/brokerd/tests/durable_anchor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write live_acceptance_tainted_session.rs (deny + confirm + guard, ACC-01/02/03)</name>
  <files>cli/caprun/tests/live_acceptance_tainted_session.rs</files>
  <read_first>
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-RESEARCH.md — "Code Examples" contains the complete `live_acceptance_deny_path` skeleton; "The Exact Causal Chain" gives the exact parent_id edges; "Effect-ID Discovery Across Processes" gives the anchor read; "Common Pitfalls" 1-5.
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-PATTERNS.md — exact copy-source line ranges for each pattern (module doc, constants, run helpers, chain assertions, guard test).
    - cli/caprun/tests/s9_live_block.rs — block-producing run pattern (`run_caprun_file_create` lines 173-201), hostile constants (lines 154-160), guard test (lines 401-412), Linux-gating doc comment (lines 1-43).
    - cli/caprun/tests/confirm.rs — cross-process verb pattern (`run_caprun_verb` lines 131-145), exit-code contract (confirm→0, deny→2, second-call→5, unknown→4), `assert_anchored_event` (lines 151-174).
  </read_first>
  <action>
    Create the new file `cli/caprun/tests/live_acceptance_tainted_session.rs`. No `Cargo.toml` change — Cargo auto-discovers `.rs` files under `tests/` (verified in RESEARCH.md "Registration Mechanics").

    Structure the file as:
    1. A module doc comment mirroring `s9_live_block.rs` lines 1-43, substituting the name `live_acceptance_tainted_session` and describing the ACC-01/02/03 human-gate composition (deny + confirm), including the Colima/Docker run recipe: `docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test -p caprun --test live_acceptance_tainted_session`.
    2. An always-compiled guard test `live_acceptance_guard_binary_present` asserting `env!("CARGO_BIN_EXE_caprun")` is non-empty (Pitfall 4 — keeps `cargo test` meaningful on macOS).
    3. Linux-gated constants reused verbatim from `s9_live_block.rs` lines 154-160: `HOSTILE_FC_CONTENT` (the `reports/pwned.txt` exfil sentence) and `HOSTILE_FC_PATH = "reports/pwned.txt"`, each under `#[cfg(target_os = "linux")]`.
    4. Two `#[cfg(target_os = "linux")] #[test]` functions: `live_acceptance_deny_path` (ACC-01) and `live_acceptance_confirm_path` (ACC-02). Import `brokerd::audit::{find_event_by_type, open_audit_db, verify_chain}`.

    Each of the two test functions does, in order:
    - Build an isolated temp dir (`std::env::temp_dir().join(format!("caprun_live_acc_{deny|confirm}_{uuid}"))`), write `workspace.txt` = `HOSTILE_FC_CONTENT`, and set `audit_db = tmp.join("audit.db")` — an explicit persistent file, NEVER `:memory:` (Pitfall 2). Keep this temp dir alive through BOTH subprocesses (Pitfall 3 — confirm reopens the workspace root).
    - Process 1 (blocking run): spawn `env!("CARGO_BIN_EXE_caprun")` via `std::process::Command` with args `create-file-from-report`, `intended_output.txt`, the workspace-file path, the audit_db path; assert the process exits non-zero (`!out1.status.success()`).
    - Discover `effect_id`: in a scoped block, `open_audit_db(audit_db)`, `SELECT id FROM sessions LIMIT 1`, `find_event_by_type(&conn, &session_id, "sink_blocked")` → `.anchor.as_ref().unwrap().effect_id`; let `conn` drop at the end of the block before Process 2 opens its own connection. Do not scrape stdout.
    - Process 2 (human decision): spawn `caprun` with verb `deny` (deny test) or `confirm` (confirm test), `effect_id.to_string()`, and the same audit_db path. Assert `out2.status.code()` == `Some(2)` for deny (Denied) or `Some(0)` for confirm (Released).
    - Effect assertion: deny test asserts BOTH `!tmp.join(HOSTILE_FC_PATH).exists()` and `!tmp.join("intended_output.txt").exists()` (no effect ever proceeds). Confirm test asserts `tmp.join(HOSTILE_FC_PATH).exists()` (the released `file.create` wrote `reports/pwned.txt` under the workspace root exactly once).
    - Chain assertion (ACC-03): reopen the DB, `assert!(verify_chain(&conn, &session_id))`, then fetch via `find_event_by_type` the events `fd_granted`, `file_read`, `session_demoted`, `sink_blocked`, and `confirm_denied` (deny) / `confirm_granted` (confirm). Assert the edges: `file_read.parent_id == Some(fd_granted.id)`; `demoted.parent_id == Some(file_read.id)` (TAINT-04); `blocked.parent_id == Some(demoted.id)` (NOT `file_read.id` — Pitfall 1); `decision.parent_id == Some(blocked.id)` (CONFIRM-04). RESEARCH.md's "Code Examples" is the literal starting skeleton for `live_acceptance_deny_path`; the confirm variant is structurally identical with the verb, exit code, effect-existence, and `confirm_granted` substitutions noted above.

    Do NOT add a second workspace read or a second hostile claim in one run (Pitfall 5 — `find_event_by_type` returns the first row; keep exactly one of each event type per session).
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_tainted_session</automated>
  </verify>
  <acceptance_criteria>
    - File `cli/caprun/tests/live_acceptance_tainted_session.rs` exists and compiles; `cargo test -p caprun --test live_acceptance_tainted_session` passes on macOS (the guard test runs; Linux-gated bodies are cfg-excluded).
    - The file contains an always-compiled guard test asserting `env!("CARGO_BIN_EXE_caprun")` is non-empty.
    - The file contains exactly two `#[cfg(target_os = "linux")] #[test]` functions named `live_acceptance_deny_path` and `live_acceptance_confirm_path`.
    - Both tests pass an explicit persistent audit DB path (a `tmp.join("audit.db")`-style path) to both subprocess invocations; the string `:memory:` does not appear anywhere in the file.
    - The deny test asserts `out2.status.code() == Some(2)`; the confirm test asserts `out2.status.code() == Some(0)`.
    - The deny test asserts neither `HOSTILE_FC_PATH` nor `intended_output.txt` exists; the confirm test asserts `HOSTILE_FC_PATH` exists.
    - Both tests assert `verify_chain(&conn, &session_id)` is true and assert `blocked.parent_id == Some(demoted.id)` where `demoted` is the `session_demoted` event, plus the `file_read → session_demoted` and `sink_blocked → confirm_{granted|denied}` edges.
    - `effect_id` is read from the persisted `sink_blocked` event's `anchor.effect_id`; the file contains no stdout parsing of `effect_id`.
  </acceptance_criteria>
  <done>New Linux-gated live-acceptance test file exists with deny + confirm + guard tests; compiles and guard passes on macOS; all ACC-01/02/03 assertions present per the corrected parent_id chain. Real Linux pass is proven in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale sink_blocked parent_id assertion in s9_live_block.rs (Pitfall 1 / ACC-03 regression guard)</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs lines ~294-322 — the `s9_live_file_create_hostile_block` chain-assertion region (the stale assertion is at lines 310-313, asserting `blocked.parent_id == Some(file_read.id)`).
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-PATTERNS.md — the s9 fix section shows the exact target replacement code.
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-RESEARCH.md — Pitfall 1 and "The Exact Causal Chain" explain why `sink_blocked.parent_id` is `session_demoted.id`, not `file_read.id` (`mint_from_read` advances the connection chain head past `file_read`).
  </read_first>
  <action>
    In `cli/caprun/tests/s9_live_block.rs`, inside `s9_live_file_create_hostile_block`, replace the current single assertion at lines 310-313 (`assert_eq!(blocked.parent_id, Some(file_read.id), "sink_blocked must be causally parented onto file_read (file_read → sink_blocked)")`) with a two-edge assertion that reflects current production wiring:
    1. Fetch the demotion event: `let demoted = find_event_by_type(&conn, &session_id, "session_demoted").expect("query session_demoted").expect("session_demoted event must exist (I1 demotion on hostile read)");` (add this before the assertions).
    2. Assert the TAINT-04 edge: `demoted.parent_id == Some(file_read.id)` with a message noting `session_demoted → file_read`.
    3. Assert the corrected block edge: `blocked.parent_id == Some(demoted.id)` with a message noting the block is parented onto `session_demoted`, not `file_read`, because `mint_from_read`'s chain head advances past `file_read` (see `quarantine.rs`).
    Leave the existing `file_read.parent_id == Some(fd_granted.id)` assertion (lines 305-307) and the genuine-taint `anchor.read_event_id == file_read.id` assertion (lines 316-317) untouched. This is a surgical change: only the stale block-edge assertion changes; no other test logic is modified.
  </action>
  <verify>
    <automated>cargo test -p caprun --test s9_live_block</automated>
  </verify>
  <acceptance_criteria>
    - `s9_live_block.rs` no longer contains the assertion `blocked.parent_id == Some(file_read.id)`.
    - `s9_live_block.rs` contains `assert_eq!(blocked.parent_id, Some(demoted.id), ...)` and `assert_eq!(demoted.parent_id, Some(file_read.id), ...)` where `demoted` is fetched via `find_event_by_type(&conn, &session_id, "session_demoted")`.
    - `cargo test -p caprun --test s9_live_block` compiles and passes on macOS (guard/cross-platform tests run; Linux body is cfg-excluded — the real proof is Task 3).
    - No assertion or logic outside the block-edge region is changed (`file_read → fd_granted` and `anchor.read_event_id == file_read.id` remain).
  </acceptance_criteria>
  <done>The pre-existing stale block-edge assertion is corrected to `Some(session_demoted.id)`, with the TAINT-04 `session_demoted → file_read` edge asserted alongside; the fix is verified live in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Run the live Colima+Docker acceptance recipe and capture the D-06 acceptance record</name>
  <files>cli/caprun/tests/live_acceptance_tainted_session.rs, cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-VALIDATION.md — "Manual-Only Verifications" and "Sampling Rate" (the Colima+Docker run is the ONLY way to observe the Linux-only assertions; must be green before phase done).
    - .planning/phases/11-live-acceptance-tainted-session-human-gate/11-RESEARCH.md — Pitfall 4 (macOS compile check is not proof), "Environment Availability" (Colima 0.10.3 / Docker 29.6.1 verified present), D-06 acceptance-record contents.
    - CLAUDE.md — "Linux-only security tests" section (the canonical Colima+Docker recipe; seccomp=unconfined, no --privileged, Landlock needs kernel ≥5.13).
  </read_first>
  <action>
    Run the phase's live proof on real Linux via the project's standard Colima+Docker recipe (this is NOT a macOS `cargo check` — Pitfall 4). Steps:
    1. Ensure the VM is up: `colima start` (idempotent if already running).
    2. Run the new test binary on Linux: `docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test -p caprun --test live_acceptance_tainted_session -- --nocapture`. Both `live_acceptance_deny_path` and `live_acceptance_confirm_path` must report `test result: ok`.
    3. Run the corrected precedent binary on Linux to prove the Pitfall-1 fix: `docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test -p caprun --test s9_live_block`. Must report `test result: ok` (this is the FIRST run of this scenario on Linux since Phase 9 — if the assertion fix from Task 2 is wrong, this is where it surfaces; iterate on Task 1/2 until green).
    4. Fold a written acceptance record into this plan's SUMMARY.md (D-06 — not a standalone doc). Capture, for BOTH the deny and confirm runs: the exact `docker run ... cargo test` command, the observed test-result line / exit status, and the audit-DAG `parent_id` rows proving the unbroken chain (`fd_granted → file_read → session_demoted → sink_blocked → confirm_denied/confirm_granted`). Include the `s9_live_block` result as the regression-guard evidence.
    If Colima/Docker cannot be started in the execution environment, do NOT declare the phase done from a macOS compile check — stop and surface the blocker (the live run is the ACC-01/02/03 gate).
  </action>
  <verify>
    <automated>docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test -p caprun --test live_acceptance_tainted_session</automated>
  </verify>
  <acceptance_criteria>
    - The Colima+Docker run of `cargo test -p caprun --test live_acceptance_tainted_session` reports `test result: ok` with both `live_acceptance_deny_path` and `live_acceptance_confirm_path` passing (0 failed) — captured as real Docker stdout, not a macOS `cargo test`/`cargo check`.
    - The Colima+Docker run of `cargo test -p caprun --test s9_live_block` reports `test result: ok` (0 failed), proving the Task 2 fix holds on Linux.
    - SUMMARY.md contains an acceptance record with: the two run commands, their observed result/exit status, and the queried audit-DAG `parent_id` chain rows for both the deny and confirm runs.
    - The acceptance record shows evidence of an actual Docker/Colima invocation (Pitfall 4) — not a macOS-only compile check.
  </acceptance_criteria>
  <done>Both new tests and the corrected s9_live_block test are green under Colima+Docker on real Linux; the D-06 acceptance record (commands, exit codes, DAG rows for deny + confirm) is folded into SUMMARY.md. ACC-01/02/03 are proven live.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

Symbols and files newly created or modified by Phase 11 (drift-verification exclusion list):

**New file:** `cli/caprun/tests/live_acceptance_tainted_session.rs`

**New test functions (in the new file):**
- `live_acceptance_deny_path` — `#[cfg(target_os = "linux")] #[test]` (ACC-01)
- `live_acceptance_confirm_path` — `#[cfg(target_os = "linux")] #[test]` (ACC-02)
- `live_acceptance_guard_binary_present` — always-compiled guard `#[test]`

**New constants (in the new file, reused verbatim from s9_live_block.rs semantics):**
- `HOSTILE_FC_CONTENT: &[u8]` — `#[cfg(target_os = "linux")]`
- `HOSTILE_FC_PATH: &str` = `"reports/pwned.txt"` — `#[cfg(target_os = "linux")]`

**Modified file:** `cli/caprun/tests/s9_live_block.rs` — one assertion region in `s9_live_file_create_hostile_block` corrected (adds a `session_demoted` lookup + two-edge parent_id assertion; removes the stale `blocked.parent_id == Some(file_read.id)` assertion).

**No production/TCB code is created or modified.** No new crates, dependencies, CLI verbs, structs, or fields. All runtime mechanisms exercised (`mint_from_read`, executor I2 block, `file.create` sink, `confirm`/`deny`) already exist from Phases 7/9/10. No `Cargo.toml` change (Cargo auto-discovers the new test file).

**Acceptance record (D-06):** written into `.planning/phases/11-live-acceptance-tainted-session-human-gate/11-01-SUMMARY.md`, not a standalone doc.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → broker | The confined worker reads raw untrusted file bytes and reports extracted claims; the broker's `mint_from_read` is the sole taint-mint + I1 demotion site. Untrusted content crosses here. |
| blocking process → confirm/deny process | The only channel between the process that produces the block and the later human-decision process is the persisted SQLite audit DB (the `effect_id` and `PendingConfirmation` state). |

## STRIDE Threat Register

This is a proof phase — it exercises existing hardened mechanisms (Landlock, seccomp, `openat2 RESOLVE_BENEATH`, the executor I1/I2 logic) rather than adding new attack surface. The threats below are to the *trustworthiness of the acceptance proof itself*, not new runtime attack surface (RESEARCH.md "Security Domain").

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | Stale test assertion in `s9_live_block.rs` (Pitfall 1) masking a real Phase-9 chain-head change | medium | mitigate | Task 2 corrects `blocked.parent_id` to `Some(session_demoted.id)`; Task 3 runs it under Colima/Docker so the assertion is actually exercised on Linux (never was since Phase 9). |
| T-11-02 | Repudiation | `:memory:` audit DB silently defeating the cross-process confirm/deny proof — a "passing" test that never exercised the release path (Pitfall 2) | medium | mitigate | Task 1 acceptance criteria forbid `:memory:` and require an explicit persistent DB path passed to both subprocesses; deny/confirm exit codes (2/0) and file existence are asserted to prove the path was really exercised. |
| T-11-03 | Tampering | Confirm's live sink reopens the workspace-root dir persisted at block time; a deleted/mismatched temp dir yields a false ConfirmedButSinkFailed (Pitfall 3) | low | mitigate | Task 1 keeps the same temp workspace-root dir alive across both subprocesses until after all assertions complete. |
| T-11-04 | Repudiation | An acceptance record (D-06) written from a macOS compile check rather than a real Linux run (Pitfall 4) | medium | mitigate | Task 3 requires the acceptance record to capture actual `docker run` output (test-result lines + DAG rows), and forbids declaring the phase done from a macOS-only check. |

**Package legitimacy:** No npm/pip/cargo install tasks — the new test uses only existing workspace dev-dependencies (`rusqlite`, `sha2`, `hex`, `uuid`). No `T-11-SC` supply-chain checkpoint is required (RESEARCH.md "Package Legitimacy Audit": not applicable).
</threat_model>

<verification>
- macOS quick check (per task): `cargo test -p caprun --test live_acceptance_tainted_session` and `cargo test -p caprun --test s9_live_block` compile and pass (guard/cross-platform tests only; Linux bodies cfg-excluded).
- Linux live proof (phase gate, Task 3): under Colima+Docker, both `cargo test -p caprun --test live_acceptance_tainted_session` and `cargo test -p caprun --test s9_live_block` report `test result: ok`, 0 failed.
- ACC-03 chain proof: within both live tests, `verify_chain()` is true and the parent_id walk `fd_granted → file_read → session_demoted → sink_blocked → confirm_granted/confirm_denied` holds.
- No production/TCB code changed (`./scripts/check-invariants.sh` unaffected; test-only diff).
</verification>

<success_criteria>
- ACC-01: The deny test proves hostile read → I1 demotion → I2 block → `caprun deny` (exit 2) → no effect ever proceeds (no `reports/pwned.txt`, no `intended_output.txt`), live on Linux.
- ACC-02: The confirm test proves the same scenario → `caprun confirm` (exit 0) → the effect proceeds exactly once (`reports/pwned.txt` created), live on Linux.
- ACC-03: Both tests assert one unbroken causal chain via `verify_chain()` + the corrected `parent_id` walk, live on Linux.
- The Pitfall-1 stale assertion in `s9_live_block.rs` is fixed and green on Linux.
- A D-06 acceptance record capturing the real Colima+Docker run (commands, exit codes, DAG rows for both runs) is folded into SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/11-live-acceptance-tainted-session-human-gate/11-01-SUMMARY.md` when done, including the D-06 acceptance record (deny + confirm run commands, exit codes, and audit-DAG parent_id rows).
</output>
