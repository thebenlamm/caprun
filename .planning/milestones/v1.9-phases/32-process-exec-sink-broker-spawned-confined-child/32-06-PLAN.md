---
phase: 32-process-exec-sink-broker-spawned-confined-child
plan: 06
type: execute
wave: 5
depends_on: ["32-05", "32-03"]
files_modified:
  - cli/caprun/tests/s9_process_exec_block.rs
  - crates/sandbox/tests/exec_child_confinement.rs
autonomous: true
requirements: [EXEC-02, EXEC-03, EXEC-04]

must_haves:
  truths:
    - "A tainted exec-output value routed to a sensitive sink arg is deterministically Blocked; the audit DAG shows an unbroken edge process_exited → ValueNode(provenance_chain[0]) → sink arg → sink_blocked, and verify_chain is true (non-stapled)."
    - "A clean exec is Allowed and its benign output routed to a non-sensitive context succeeds; a tainted command Blocks before any spawn."
    - "The real caprun-exec-launcher, driven with the 32-02 primitives, cannot write outside WorkspaceRoot, cannot open an outbound socket (denial persists across its execve), and CAN perform its one legitimate execve + a benign in-workspace op."
    - "The Linux compile-check (`cargo build --tests --keep-going` in the rust:1 container) enumerates ZERO cfg(linux) compile errors, and check-invariants.sh exits 0 including the new Gate-3 line."
  artifacts:
    - cli/caprun/tests/s9_process_exec_block.rs
    - crates/sandbox/tests/exec_child_confinement.rs
  key_links:
    - "The EXEC-03 Block assertion checks provenance_chain[0] == the process_exited event id — proving the taint is genuinely propagated, not stapled at the sink."
    - "Because Mac compiles ZERO cfg(linux) test targets, a Mac-green build is NOT sufficient — the container compile-check is mandatory (cfg-linux-test-blindness)."
---

<objective>
Prove the phase's four success criteria with per-requirement tests and run the
MANDATORY Linux compile-check: (1) the EXEC-03 genuine-taint→I2-Block acceptance
(+ clean-Allow control + tainted-command negative), (2) the exec-child confinement
negative-assertion test against the real launcher, and (3) the container
compile-check + invariant gates that guard against cfg(linux) blindness.

Purpose: These are the tests that make Phase 34's composed live acceptance possible
— they must be WRITTEN and COMPILE on Linux now (CLAUDE.md / VALIDATION doc). The
full composed live proof (LIVE-01) is Phase 34; this phase proves the mechanism
per-requirement and enumerates every Linux compile error before then.
Output: s9_process_exec_block.rs, exec_child_confinement.rs, a green container
compile-check + invariant-gate run.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-RESEARCH.md
@.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-VALIDATION.md
@crates/sandbox/tests/confinement_integration.rs
@cli/caprun/tests/s9_live_block.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXEC-03 acceptance — genuine taint → I2 Block (+ clean Allow + tainted-command negative)</name>
  <files>cli/caprun/tests/s9_process_exec_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs (the harness shape to mirror — how it stands up a broker + session + drives plan nodes, and how it asserts on the audit DAG + verify_chain).
    - crates/brokerd/src/server.rs 651-661,862-891,1503-1521 (evaluate_plan_node_and_record return + the process.exec Allowed arm from 32-05; where output_value_id is returned).
    - DESIGN §10 acceptance predicate; ROADMAP Phase 32 success criteria 2 & 3 (taint-minted untrusted, provenance rooted at the exec Event; tainted exec-output → sensitive sink arg → deterministic Block, verify_chain true).
    - 32-RESEARCH.md "Validation Architecture" EXEC-03 row (s9_process_exec_block, §9-style) and the System Architecture Diagram downstream note (a LATER PlanNode references the exec-output ValueId; the collect-then-Block loop Blocks it at a sensitive slot).
    - CLAUDE.md hard constraint 1 (a GENUINE taint chain — raw exec Event → ValueNode → sink arg → block as an unbroken audit-DAG edge; stapled taint proves nothing).
  </read_first>
  <action>
    Create `cli/caprun/tests/s9_process_exec_block.rs`, `#[cfg(target_os="linux")]`,
    mirroring s9_live_block's broker+session harness. Scenarios: (a) BLOCK — run a
    trusted `process.exec` (e.g. echo a marker), capture the returned
    `output_value_id`, then submit a SECOND plan node routing that exec-output
    handle into a sensitive sink arg (a routing- or content-sensitive slot, e.g.
    `email.send` `to`/`body` or `file.create` `path`/`contents`); assert the
    decision is `BlockedPendingConfirmation`, that a durable `sink_blocked` event
    exists, that the minted value's `provenance_chain[0]` equals the `process_exited`
    event id (the unbroken edge exec Event → ValueNode → sink arg → block — genuine,
    NOT stapled), and that `verify_chain` is true over the session. (b) CLEAN ALLOW
    control — a benign `process.exec` is Allowed and its output routed to a
    non-sensitive context does not Block. (c) TAINTED-COMMAND negative — a
    process.exec whose `command`/`args` value is itself untrusted-tainted Blocks
    BEFORE any spawn (no process_exited event; the Block is at the command/args
    sensitivity check). Add a `#[cfg(not(target_os="linux"))]` note module. These
    COMPILE + RUN on Linux this phase; the full composed live acceptance (block +
    clean allow + fs write in ONE run) is Phase 34.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p caprun --test s9_process_exec_block --no-run 2>&1 | tail -3</automated>
  </verify>
  <done>
    s9_process_exec_block.rs compiles (`--no-run` on Mac; the container step below
    runs it). It asserts the genuine non-stapled Block (provenance_chain[0] ==
    process_exited event id, verify_chain true), the clean-Allow control, and the
    tainted-command-Blocks-before-spawn negative.
  </done>
</task>

<task type="auto">
  <name>Task 2: exec-child confinement negative-assertion test</name>
  <files>crates/sandbox/tests/exec_child_confinement.rs</files>
  <read_first>
    - crates/sandbox/tests/confinement_integration.rs (full — the `assert_probe_blocked` pattern, `env!("CARGO_BIN_EXE_...")`, the exit-code convention 0=blocked/1=not-blocked/2=error/100=non-Linux-sentinel, and the async-signal-safety note about spawning a fresh single-purpose binary).
    - cli/caprun-exec-launcher/src/main.rs (the real launcher — the test drives THIS binary, RESEARCH Open Q4 option b) and crates/sandbox/src/{landlock,seccomp}.rs (exec_child_ruleset / exec_child_filter).
    - DESIGN §1.4 (the confinement bounds: no fs outside WorkspaceRoot incl ~/.ssh; net-deny persists across execve — A5; one legitimate execve) and §7 (exec-child confinement negative-assertion test is required, PLAN.md:152 analog).
    - 32-RESEARCH.md Open Implementation Question 4 (recommend option b: spawn the real caprun-exec-launcher with a benign EXEC_COMMAND that probes forbidden ops) and Assumption A6 (net-deny persists across execve — MUST be confirmed empirically here) and A4 (confirm the Landlock system allow-list via ldd/which against the chosen probe commands, in-container).
  </read_first>
  <action>
    Create `crates/sandbox/tests/exec_child_confinement.rs`, all
    `#[cfg(target_os="linux")]`, mirroring confinement_integration.rs's
    binary-spawn assertion style but driving the REAL `caprun-exec-launcher`
    (`env!("CARGO_BIN_EXE_caprun-exec-launcher")`) with EXEC_* env vars pointed at a
    temp WorkspaceRoot. Assert, per DESIGN §1.4: (a) a target that attempts to WRITE
    a path OUTSIDE WorkspaceRoot (e.g. touch /etc/…) fails (Landlock deny); (b) a
    target that attempts an outbound `socket(AF_INET)` fails with EPERM — proving
    the net-deny PERSISTS across the launcher's own execve (A6, the single
    load-bearing kernel-semantics assumption, empirically confirmed here); (c) a
    benign in-WorkspaceRoot op AND the one legitimate target execve SUCCEED (the
    ruleset is narrow-allow, not deny-all). Resolve the exact Landlock system
    allow-list paths against the container's real layout via `ldd`/`which` on the
    chosen probe commands (DESIGN §8 item 1 / A4) and hardcode them in
    exec_child_ruleset (coordinate with 32-02 if the provisional `/usr,/bin,/lib,
    /lib64` list is insufficient). Add a `#[cfg(not(target_os="linux"))]` note.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p sandbox --test exec_child_confinement --no-run 2>&1 | tail -3</automated>
  </verify>
  <done>
    exec_child_confinement.rs compiles (`--no-run` on Mac; container runs it). It
    proves fs-escape denied, outbound socket denied (persists across execve), and
    the legitimate execve + benign in-workspace op succeed — driving the real
    launcher binary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mandatory Linux compile-check + invariant gates</name>
  <files>.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md "Linux-only security tests" + "From Phase 16 onward … scripts/mailpit-verify.sh" (the verification recipe; MAILPIT_VERIFY_CMD override; the true-exit-before-pipe discipline).
    - scripts/mailpit-verify.sh lines 26-91,145-185 (the default cmd `cargo build --workspace && cargo test --workspace --no-fail-fast`; the scoped-override examples; the in-container libssl-dev/pkg-config install; the sibling-binary rationale).
    - 32-VALIDATION.md (the sampling rate + Validation Sign-Off checklist; set nyquist_compliant + wave_0_complete true when done).
    - 32-RESEARCH.md Pitfall 4 (cfg-linux test-blindness — `cargo build --tests --keep-going` in the container is non-negotiable) and Pitfall 3 (cargo build --workspace before any launcher-spawning test).
    - MEMORY note [[verification-exit-code-through-pipe]] — capture $? before any pipe; assert on counts + named tests, never on exit 0 through `| tail`.
  </read_first>
  <action>
    Run, in order, capturing real exit codes (NEVER `script | tail` exit-code
    laundering — capture `$?` before piping): (1) `bash scripts/check-invariants.sh`
    — MUST exit 0, and grep-confirm the new `mint_from_exec(` Gate-3 line is present.
    (2) The Linux compile-check via the container (Colima+Docker, unprivileged,
    seccomp=unconfined): `MAILPIT_VERIFY_CMD='cargo build --workspace && cargo build
    --tests --keep-going' bash scripts/mailpit-verify.sh` — enumerate
    and FIX every `#[cfg(target_os="linux")]` compile error (a Mac-green build is
    not sufficient evidence; this is the cfg-linux-blindness guard). (3) The scoped
    Linux test run for the new targets: `MAILPIT_VERIFY_CMD='cargo build --workspace
    && cargo test -p sandbox --test exec_child_confinement && cargo test -p brokerd
    process_exec && cargo test -p caprun --test s9_process_exec_block' bash
    scripts/mailpit-verify.sh` — assert on named-test pass counts, real exit 0
    captured before any pipe. Then update 32-VALIDATION.md: check the Validation
    Sign-Off boxes that now hold, and set `nyquist_compliant: true` and
    `wave_0_complete: true` in its frontmatter. (Start Colima first if the daemon is
    not running.) Note: the full composed live acceptance (LIVE-01) remains Phase 34;
    this task proves per-requirement compile+run only.
  </action>
  <verify>
    <automated>bash scripts/check-invariants.sh && grep -q 'mint_from_exec(' scripts/check-invariants.sh && echo GATES_OK</automated>
  </verify>
  <done>
    check-invariants.sh exits 0 with the new Gate-3 line present; the container
    `cargo build --tests --keep-going` enumerates zero cfg(linux) compile errors;
    the scoped Linux test run passes on named counts with a real (pre-pipe) exit 0;
    32-VALIDATION.md sign-off boxes updated and nyquist_compliant/wave_0_complete
    set true. Any container failure is fixed (or, if the failure is a genuine
    blocker requiring re-plan, reported — never laundered to a false PASS).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real launcher/kernel | Tests exercise the actual confinement + taint path, not stubs. |
| Mac build → Linux enforcement | cfg(linux) targets compile only in the container; a Mac-green build can hide broken Linux sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-22 | Repudiation | a Mac-green build hides broken cfg(linux) sites (false assurance) | high | mitigate | mandatory container `cargo build --tests --keep-going` enumerates every Linux compile error (cfg-linux-blindness guard) |
| T-32-23 | Tampering | a false PASS via exit-code laundering through a pipe | high | mitigate | capture $? before piping; assert on named-test counts, never bare `script | tail` exit 0 |
| T-32-24 | Elevation of Privilege | stapled taint passes a weak Block test | critical | mitigate | the EXEC-03 test asserts provenance_chain[0] == process_exited event id + verify_chain true (genuine, not stapled) |
| T-32-25 | Information Disclosure | A6 (net-deny persists across execve) assumed but unproven | high | mitigate | exec_child_confinement empirically confirms socket(AF_INET) EPERM in the post-execve target |
</threat_model>

<verification>
- `bash scripts/check-invariants.sh` exits 0; the `mint_from_exec(` Gate-3 line is present.
- Container `cargo build --tests --keep-going` (rust:1) enumerates zero cfg(linux) errors.
- Scoped Linux run of exec_child_confinement + process_exec + s9_process_exec_block
  passes on named counts, real exit 0 captured before any pipe.
- 32-VALIDATION.md nyquist_compliant/wave_0_complete = true.
</verification>

<success_criteria>
All four Phase-32 success criteria are proven per-requirement on real Linux: the
child is broker-spawned + kernel-confined, its output is genuinely taint-minted, a
tainted exec output routed to a sensitive sink arg deterministically Blocks with an
unbroken non-stapled audit-DAG edge, the sink is fail-closed on schema, and spawn +
exit are durably audited — with the cfg-linux-blindness guard satisfied. The
composed live acceptance is deferred to Phase 34 (LIVE-01).
</success_criteria>

<output>
Create `.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-06-SUMMARY.md` when done
</output>
