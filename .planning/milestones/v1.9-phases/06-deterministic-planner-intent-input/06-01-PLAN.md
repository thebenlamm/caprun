---
phase: 06-deterministic-planner-intent-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/intent.rs
  - crates/runtime-core/src/plan_node.rs
  - crates/runtime-core/src/lib.rs
  - crates/runtime-core/tests/intent_taint.rs
autonomous: true
requirements: [PLAN-02, PLAN-03, HARD-02]

must_haves:
  truths:
    - "CaprunIntent::SendEmailSummary round-trips through serde_json unchanged"
    - "TaintLabel::UserTrusted.is_untrusted() == false and LocalWorkspace.is_untrusted() == false"
    - "TaintLabel::ExternalUntrusted/EmailRaw/PdfRaw/LlmGenerated/WorkerExtracted .is_untrusted() == true"
    - "Adding a new TaintLabel variant without updating is_untrusted() is a compile error"
  artifacts:
    - "crates/runtime-core/src/intent.rs contains pub enum CaprunIntent"
    - "crates/runtime-core/src/plan_node.rs contains impl TaintLabel { pub fn is_untrusted }"
    - "crates/runtime-core/src/lib.rs re-exports CaprunIntent"
    - "crates/runtime-core/tests/intent_taint.rs"
  key_links:
    - "is_untrusted() uses an exhaustive `match self` with NO wildcard arm (Pitfall 5: false-allow is worse than false-block)"
    - "CaprunIntent derives Serialize+Deserialize with a serde tag so struct variants wire consistently between caprun main and worker (Pitfall 4)"
---

<objective>
Add the two pure runtime-core types the rest of Phase 6 builds on: the typed `CaprunIntent`
enum (the deterministic planner's only input surface, PLAN-02/PLAN-03) and the
`TaintLabel::is_untrusted()` predicate that makes the HARD-02 executor fix meaningful.

Purpose: runtime-core is the locked home for pure domain types (no I/O, no async — Gate 2).
Landing these first unblocks the executor predicate (Wave 2), the broker IPC (Wave 2), and the
CLI planner (Wave 3) in parallel.
Output: `CaprunIntent`, `TaintLabel::is_untrusted()`, their re-export, and unit tests.

## Artifacts this phase produces (full list — this plan delivers the first two)
- **`CaprunIntent` enum** (+ `SendEmailSummary { recipient: String }` variant) — THIS PLAN
- **`TaintLabel::is_untrusted()`** — THIS PLAN
- `mint_from_intent()` + `intent_received` audit event type — Plan 03
- `BrokerRequest::ProvideIntent` / `BrokerResponse::IntentAccepted` — Plan 03
- executor predicate over `is_untrusted()` — Plan 02
- `cli/caprun/src/planner.rs` + `plan_from_intent()` — Plan 04
- CLI positional args `<intent-kind> <intent-param>` + `INTENT` env var — Plan 04
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deterministic-planner-intent-input/06-RESEARCH.md
@.planning/phases/06-deterministic-planner-intent-input/06-PATTERNS.md

@crates/runtime-core/src/intent.rs
@crates/runtime-core/src/plan_node.rs
@crates/runtime-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CaprunIntent enum + re-export</name>
  <files>crates/runtime-core/src/intent.rs, crates/runtime-core/src/lib.rs</files>
  <read_first>
    - crates/runtime-core/src/intent.rs (existing Intent/IntentStatus derive set — mirror it)
    - crates/runtime-core/src/plan_node.rs lines 12-21 (TaintLabel derive set as the enum template)
    - 06-PATTERNS.md § crates/runtime-core/src/intent.rs (exact enum + serde tag guidance)
  </read_first>
  <behavior>
    - CaprunIntent::SendEmailSummary { recipient } serializes to JSON and deserializes back to an equal value
    - Deserializing an unknown intent kind fails (serde exhaustive enum — fail closed, V5 input validation)
  </behavior>
  <action>
    In crates/runtime-core/src/intent.rs, ADD (do not modify the existing Intent/IntentStatus)
    a public enum `CaprunIntent` with a single struct variant `SendEmailSummary { recipient: String }`.
    Derive `Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize` (mirror the existing
    Intent derive set) and add a serde container tag (`#[serde(tag = "kind")]`) so the struct
    variant wires with a stable, explicit shape between caprun main and the worker (Pitfall 4:
    tag format must be identical on both sides). Doc-comment it as the v0 typed intent surface;
    note LLM-driven intent is v2 (PLAN-F1), not implemented here.
    Then in crates/runtime-core/src/lib.rs extend the existing `pub use intent::{Intent, IntentStatus};`
    line to also re-export `CaprunIntent`. Keep runtime-core pure — no I/O, no async (Gate 2).
  </action>
  <verify>
    <automated>cargo test -p runtime-core caprun_intent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `CaprunIntent` and `CaprunIntent::SendEmailSummary` are public and re-exported from runtime-core root.
    - The serde round-trip test in Task 3 passes.
    - `./scripts/check-invariants.sh` Gate 2 (runtime-core purity) stays green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TaintLabel::is_untrusted() (exhaustive, no wildcard)</name>
  <files>crates/runtime-core/src/plan_node.rs</files>
  <read_first>
    - crates/runtime-core/src/plan_node.rs lines 12-21 (TaintLabel — all 7 variants)
    - 06-RESEARCH.md § Pitfall 5 (why no wildcard arm)
    - 06-PATTERNS.md § is_untrusted() method (exact match arms)
  </read_first>
  <behavior>
    - UserTrusted.is_untrusted() == false; LocalWorkspace.is_untrusted() == false
    - ExternalUntrusted / EmailRaw / PdfRaw / LlmGenerated / WorkerExtracted .is_untrusted() == true
    - Removing any arm (or adding a new TaintLabel variant) fails compilation, not silently defaults
  </behavior>
  <action>
    In crates/runtime-core/src/plan_node.rs, ADD `impl TaintLabel { pub fn is_untrusted(&self) -> bool }`.
    Implement with an EXPLICIT `match self` listing every variant on one of two sides — the five
    untrusted labels (ExternalUntrusted, EmailRaw, PdfRaw, LlmGenerated, WorkerExtracted) return true;
    UserTrusted and LocalWorkspace return false. Do NOT use a wildcard `_ =>` arm and do NOT use the
    matches! macro (both would silently treat a future untrusted label as trusted — a false-allow,
    the security-wrong default per Pitfall 5). Add a doc-comment stating: exhaustive match is
    intentional so a new variant forces a compile error.
  </action>
  <verify>
    <automated>cargo test -p runtime-core is_untrusted 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `is_untrusted()` returns the correct value for all 7 current variants.
    - The `match self` has no wildcard arm (verified by reading the source and by the tests in Task 3).
    - `cargo build -p runtime-core` succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unit tests — serde round-trip + is_untrusted truth table</name>
  <files>crates/runtime-core/tests/intent_taint.rs</files>
  <read_first>
    - crates/runtime-core/tests/types_compile.rs (existing runtime-core test style)
    - 06-PATTERNS.md § Testing pattern (three sample tests)
  </read_first>
  <action>
    Create crates/runtime-core/tests/intent_taint.rs. Add: (1) `caprun_intent_serde_round_trip`
    — construct SendEmailSummary with a recipient literal, to_string then from_str, assert_eq the
    two values; (2) a fail-closed test that from_str on a JSON object with an unknown `kind` returns
    Err; (3) `is_untrusted` truth-table tests asserting false for UserTrusted and LocalWorkspace and
    true for each of the five untrusted variants. Import via `runtime_core::{CaprunIntent, TaintLabel}`.
  </action>
  <verify>
    <automated>cargo test -p runtime-core --test intent_taint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All tests in intent_taint.rs pass.
    - `cargo test --workspace --no-fail-fast` stays green (no regressions).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type system → executor decision | `is_untrusted()` is the classification the TCB acts on; a mis-classified label changes an allow/block outcome |
| CLI/env-var JSON → CaprunIntent | intent text is deserialized; unknown shapes must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | TaintLabel::is_untrusted() | high | mitigate | Exhaustive `match self`, no wildcard/matches! — a new untrusted variant is a compile error, never a silent false-allow (Pitfall 5) |
| T-06-02 | Tampering / Input Validation | CaprunIntent deserialization | medium | mitigate | serde exhaustive enum + explicit `#[serde(tag)]`; unknown kind → Err (fail closed) |

No package installs in this phase (zero new crates); `T-06-SC` not applicable.
</threat_model>

<verification>
- `cargo test -p runtime-core --test intent_taint` green.
- `cargo build --workspace` compiles (confirms downstream import surface).
- `./scripts/check-invariants.sh` green (runtime-core purity, no banned tokens).
</verification>

<success_criteria>
CaprunIntent and TaintLabel::is_untrusted() exist, are re-exported, are exhaustively matched, and are covered by passing unit tests. Wave 2 (executor + broker) can now compile against them.
</success_criteria>

<output>
Create `.planning/phases/06-deterministic-planner-intent-input/06-01-SUMMARY.md` when done.
</output>
