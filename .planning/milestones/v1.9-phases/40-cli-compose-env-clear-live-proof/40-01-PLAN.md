---
phase: 40-cli-compose-env-clear-live-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/src/main.rs
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "The caprun-planner sidecar process inherits NONE of the unconfined caprun parent's ambient environment (no CAPRUN_SMTP_*, no unrelated secrets) — only an explicit minimal allowlist."
    - "The sidecar still receives exactly the vars it needs to function (OPENAI_API_KEY when present, PLANNER_SOCK, CAPRUN_PLANNER_MODEL when present, a minimal PATH, and HTTPS_PROXY/NO_PROXY when present)."
    - "The worker spawn no longer sets a dead environment entry that caprun-worker never reads."
  artifacts:
    - "cli/caprun/src/main.rs — sidecar Command env_clear()'d with an explicit allowlist builder + unit test over that builder."
  key_links:
    - "env_clear() on the sidecar Command precedes envs(allowlist) so no ambient var survives."
    - "webpki-roots (already compiled into the egress TLS config, brokerd) makes the sidecar's OpenAI TLS call hermetic — no SSL_CERT_* / system cert store needed after env_clear()."
---

<objective>
Close ENV-01's sidecar half: env_clear() the caprun-planner sidecar spawn and give it ONLY the minimal environment it needs, matching the discipline the worker spawn already applies. Drop the dead SESSION_ID entry from the worker-spawn allowlist.

Purpose: The sidecar today inherits the full unconfined caprun environment (including CAPRUN_SMTP_*) by construction — defense-in-depth gap flagged by the Phase-34 fresh adversarial trace. This is SAFE to close now because the broker/egress TLS stack uses compiled-in webpki-roots (no SSL_CERT_* dependency), so env_clear() is hermetic for cert validation. Per 40-CONTEXT decision 1.
Output: A hardened sidecar spawn whose surviving environment is an explicit, unit-tested allowlist, plus removal of the dead worker-spawn entry.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cli-compose-env-clear-live-proof/40-CONTEXT.md
@.planning/todos/pending/2026-07-18-planner-sidecar-env-clear.md
@planning-docs/DESIGN-git-github-http-sinks.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: env_clear() the caprun-planner sidecar spawn with an explicit minimal allowlist</name>
  <files>cli/caprun/src/main.rs</files>
  <read_first>
    - cli/caprun/src/main.rs lines ~338-373 (the CAPRUN_PLANNER=llm sidecar spawn block) — the Command currently sets PLANNER_SOCK / OPENAI_API_KEY / CAPRUN_PLANNER_MODEL but never calls env_clear(), so it inherits the full parent environment.
    - cli/caprun/src/main.rs lines ~381-413 (the worker spawn) — the exact env_clear() + explicit-allowlist pattern to mirror (SECURITY comment, .env_clear(), .env("PATH", ...), etc.).
    - planning-docs/DESIGN-git-github-http-sinks.md §5.2 (why env_clear() is now hermetic: compiled-in webpki-roots, no SSL_CERT_* / system store needed for TLS cert validation).
    - .planning/todos/pending/2026-07-18-planner-sidecar-env-clear.md (the exact allowlist the sidecar needs and the TLS-env risk this closes).
  </read_first>
  <behavior>
    - Test: the allowlist builder returns PLANNER_SOCK set to the passed socket path.
    - Test: when CAPRUN_PLANNER_MODEL is present in the parent env, the builder includes it; when absent, it is omitted (never forwarded as an empty string — mirrors the mailpit-verify.sh empty-model bug note).
    - Test: when HTTPS_PROXY / NO_PROXY are present they are forwarded; when absent they are omitted.
    - Test: the builder NEVER includes CAPRUN_SMTP_* or any var outside the documented allowlist, and NEVER includes OPENAI_API_KEY (the secret is set on the Command separately, not via this non-secret builder).
    - Test: the builder always sets a minimal PATH.
  </behavior>
  <action>
    Extract the sidecar's NON-SECRET surviving environment into a small pure helper, e.g. `fn planner_sidecar_allowlist_env(planner_sock: &str) -> Vec<(String, String)>`, returning: ("PLANNER_SOCK", planner_sock); ("CAPRUN_PLANNER_MODEL", <value>) only when std::env::var succeeds; ("HTTPS_PROXY"/"NO_PROXY", <value>) each only when present; and a minimal ("PATH", "/usr/bin:/bin:/usr/local/bin"). The helper reads the parent env for the optional entries but returns ONLY the allowlisted keys — it must never enumerate the full parent env. At the spawn site: call `cmd.env_clear()` FIRST, then set OPENAI_API_KEY conditionally (the ONLY secret, forwarded exactly as today when std::env::var succeeds), then `cmd.envs(planner_sidecar_allowlist_env(&planner_sock))`. Add a `#[cfg(test)] mod` in main.rs unit-testing the helper per the behavior block (use std::env::set_var/remove_var within a serialized test guard since env is process-global). Reference ENV-01 / 40-CONTEXT decision 1 in a code comment explaining WHY (defense-in-depth; hermetic under webpki-roots). Do NOT weaken the worker spawn.
  </action>
  <verify>
    <automated>cargo test -p caprun planner_sidecar_allowlist -- --nocapture</automated>
  </verify>
  <done>
    Acceptance: cargo build --workspace succeeds; the sidecar Command calls env_clear() before setting any var; the surviving env is exactly {PLANNER_SOCK, optional CAPRUN_PLANNER_MODEL, optional HTTPS_PROXY/NO_PROXY, minimal PATH} plus the separately-set optional OPENAI_API_KEY; unit tests over the helper pass; no CAPRUN_SMTP_* or other ambient var can reach the sidecar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drop the dead worker-spawn environment entry that caprun-worker never reads</name>
  <files>cli/caprun/src/main.rs</files>
  <read_first>
    - cli/caprun/src/main.rs line ~399 (the worker spawn sets an environment entry for the session id).
    - .planning/todos/pending/2026-07-18-planner-sidecar-env-clear.md "Related NIT" — the worker never reads that entry (HARD-03 removed the backing field).
  </read_first>
  <action>
    First grep-confirm no reader exists in the worker binary and its deps: run a search across cli/caprun-worker and any worker-side module for a read of that session-id env var. If, and ONLY if, there is genuinely no reader, remove the single `.env("SESSION_ID", ...)` line from the worker spawn (per 40-CONTEXT decision 1). If a reader DOES exist, leave the line, and record the discrepancy in the SUMMARY (do not remove state something still consumes). Make no other change to the worker spawn allowlist.
  </action>
  <verify>
    <automated>grep -rn "SESSION_ID" cli/caprun-worker/ crates/ | grep -v '^Binary' ; cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
    Acceptance: either the dead entry is removed and cargo build --workspace still succeeds with no worker regression, OR a live reader was found and the entry was kept with the discrepancy noted. The grep output shows no worker-side reader when the entry is removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unconfined caprun parent → caprun-planner sidecar | ambient environment (incl. broker secrets) crosses via process inheritance unless cleared |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Information Disclosure | caprun-planner sidecar env inheritance | medium | mitigate | env_clear() the sidecar Command before setting an explicit non-secret allowlist; sidecar receives only PLANNER_SOCK / optional model / optional proxy / minimal PATH + the explicitly-forwarded OPENAI_API_KEY. Unit-tested allowlist builder. |
| T-40-02 | Information Disclosure | dead worker-spawn env entry | low | mitigate | remove the entry (grep-confirmed no reader) so the worker's explicit allowlist carries no unused state; fail-safe = keep if a reader exists. |
</threat_model>

<verification>
- cargo build --workspace succeeds (macOS dev box).
- cargo test -p caprun passes the new allowlist unit tests (host-portable — no Linux confinement involved).
- The full hermetic-under-env_clear proof (a LIVE OpenAI sidecar run + the broker-side live HTTPS GET) is validated in Plan 40-04's composed Linux run; this plan lands the code the live run confirms.
</verification>

<success_criteria>
The sidecar spawn is env_clear()'d with a unit-tested minimal allowlist; the dead worker entry is gone (or kept with a documented reason); no build or worker regression.
</success_criteria>

<output>
Create `.planning/phases/40-cli-compose-env-clear-live-proof/40-01-SUMMARY.md` when done.
</output>
