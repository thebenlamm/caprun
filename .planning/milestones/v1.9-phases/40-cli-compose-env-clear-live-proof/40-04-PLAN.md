---
phase: 40-cli-compose-env-clear-live-proof
plan: 04
type: execute
wave: 3
depends_on: ["40-01", "40-02", "40-03"]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_8_composed.rs
autonomous: true
requirements: [LIVE-03, LIVE-04, ENV-01]
must_haves:
  truths:
    - "On real Linux, a composed workflow runs end-to-end: process.exec (output tainted) -> file.write edit -> git.commit (real confined commit) -> github.pr (live POST to the mock, 201) + an http.request GET leg — every step gated, tainted, and audit-DAG-chained, with per-session verify_chain true over one shared audit.db."
    - "Three adversarial legs are each deterministically Blocked/Denied with verify_chain true: (a) a tainted PR title/body routed into github.pr, (b) a tainted or SSRF/non-allowlisted GET url into http.request, (c) a tainted commit message into git.commit."
    - "A post-env_clear LIVE https GET to real api.github.com succeeds and mints HttpRaw taint — proving env_clear() + webpki-roots is hermetic (no SSL_CERT_* needed)."
    - "The full workspace regression is green on real Linux with no v1.0–v1.7 regression."
  artifacts:
    - "cli/caprun/tests/live_acceptance_v1_8_composed.rs — the Linux-gated composed acceptance test with a macOS guard test."
  key_links:
    - "One shared persisted audit.db across all legs; each session's verify_chain independently true (never a cross-session parent chain, never :memory:)."
    - "The github.pr success leg's real POST rides validate_url + allowlist + resolve-and-pin to the mock (feature-trusted cert); the ENV-01 GET rides the SAME pin to real api.github.com."
    - "Taint on exec output / http body is genuinely minted (mint_from_exec / mint_from_http, provenance rooted on the real event) and propagated into the sink arg — never stapled at the sink."
---

<objective>
Author the v1.8 DONE composed live-acceptance test — the milestone's final hard gate — mirroring the v1.7 composed shape (one shared audit.db, per-session verify_chain, genuine non-stapled taint), extended to the three new sinks plus the ENV-01 live-HTTPS proof and the three adversarial Block legs.

Purpose: LIVE-03 (composed success workflow), LIVE-04 (adversarial Blocks + live HTTPS + regression), and the observable ENV-01 live proof — per 40-CONTEXT decisions 2, 3, 4. Rescoped: NO real git.push leg (deferred to v1.9 per DECISION-git-push-deferral-v1.8.md); the mock accepts the PR create, standing in for the pushed-branch precondition. Do NOT claim the live-OpenAI sidecar path passed unless a key is present in the run.
Output: cli/caprun/tests/live_acceptance_v1_8_composed.rs.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-cli-compose-env-clear-live-proof/40-CONTEXT.md
@planning-docs/DECISION-git-push-deferral-v1.8.md
@cli/caprun/tests/live_acceptance_v1_7_composed.rs
@crates/brokerd/tests/s38_github_pr.rs
@crates/brokerd/tests/s37_http_request.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composed SUCCESS workflow + ENV-01 live-HTTPS proof (Linux-gated, one shared audit.db)</name>
  <files>cli/caprun/tests/live_acceptance_v1_8_composed.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_7_composed.rs IN FULL — the shared-audit.db scaffold, seed_test_key, persist_known_session, seed_root_event, all_session_ids (ORDER BY rowid, never LIMIT 1), the per-session verify_chain sweep, and the macOS guard test to replicate.
    - crates/brokerd/src/sinks/git_commit.rs lines ~91-140 (invoke_git_commit signature + the message arg + the neutralized argv; commit runs in the workspace repo).
    - crates/brokerd/src/sinks/http_request.rs lines ~333-345 (invoke_http_get) and crates/brokerd/src/quarantine.rs lines ~901-945 (mint_from_http signature + HttpRaw taint).
    - crates/brokerd/tests/s38_github_pr.rs — the has_github_grant / record_github_grant / reserve_created_pr / invoke_github_pr_from_resolved ordering for a github.pr success POST, and the opaque-audit (token never in payload) assertion.
    - crates/brokerd/src/sinks/file_write.rs (invoke_file_write signature, as used in the v1.7 leg c).
  </read_first>
  <behavior>
    - Leg exec: a trusted process.exec Allows; its captured output is minted via mint_from_exec with provenance rooted on the REAL process_exited event; verify the process_exited event is durable in the DAG before minting (anti-staple re-check).
    - Leg fs: a trusted-arg file.write Allows and overwrites an existing file within the workspace; sink_executed durable.
    - Leg git.commit: a trusted-message git.commit Allows, runs the REAL confined caprun-exec-launcher against a real git repo staged in the workspace, and appends a durable process_exited event.
    - Leg github.pr SUCCESS: with a recorded session grant + a reserved CAS key, invoke_github_pr_from_resolved POSTs trusted title/body/owner/repo/base/head to the mock (CAPRUN_GITHUB_API_BASE=https://github-mock.caprun.test) and gets a 201; a durable opaque success event is appended and the bearer token literal appears in NO event payload.
    - Leg http GET (ENV-01 live proof): invoke_http_get to a real allowlisted api.github.com endpoint succeeds WITHOUT any SSL_CERT_* env set, and mint_from_http mints [ExternalUntrusted, HttpRaw] rooted on the real http_response_received event.
    - Final sweep: exactly the composed sessions exist in the shared audit.db; each verify_chain independently true.
  </behavior>
  <action>
    Create cli/caprun/tests/live_acceptance_v1_8_composed.rs. Put the real body in a `#[cfg(target_os = "linux")] mod linux { ... }` and add the always-on macOS guard test (assert CARGO_BIN_EXE_caprun resolves), exactly mirroring the v1.7 file. Reuse the v1.7 scaffold helpers (shared persisted audit.db under a temp dir, seed_test_key minted ONCE, persist_known_session, seed_root_event, all_session_ids with ORDER BY rowid). Implement the five SUCCESS legs per the behavior block, driving the production functions in-process (executor::submit_plan_node for decisions; invoke_process_exec/invoke_file_write/invoke_git_commit/invoke_github_pr_from_resolved/invoke_http_get for the live sinks; mint_from_exec/mint_from_http for genuine taint). For the github.pr leg, serialize env access with a process-global lock (mirror s38's GITHUB_ENV_LOCK) and set the broker-env bearer token + CAPRUN_GITHUB_API_BASE within it. Assert genuine (non-stapled) taint provenance for the exec and http mints. Run the ENV-01 GET with SSL_CERT_FILE/SSL_CERT_DIR explicitly removed for the call to prove hermeticity. End with the per-session verify_chain sweep. Add a module doc block stating: NO git.push leg (deferred v1.9); the github.pr POST is a mock (real push NOT proven); the live-OpenAI SIDECAR leg is NOT asserted here (it is the harness's conditional-on-key run) — do not claim it passed. Reference LIVE-03 + ENV-01 in the doc block.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_8_composed 2>&amp;1 | tail -20   # macOS: compiles, runs only the guard test (0 Linux tests — expected). Linux success legs run under scripts/compose-verify.sh.</automated>
  </verify>
  <done>
    Acceptance: the file compiles on macOS (guard test passes, 0 Linux tests — expected per cfg-linux discipline); under scripts/compose-verify.sh on Linux the five success legs pass, the github.pr POST returns 201 from the mock with the token absent from all payloads, the ENV-01 GET to real api.github.com succeeds with cert env removed and mints HttpRaw, and every session's verify_chain is true.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three adversarial Block/Deny legs (tainted PR title/body, tainted/SSRF GET url, tainted commit message)</name>
  <files>cli/caprun/tests/live_acceptance_v1_8_composed.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs lines ~140-179 (git.commit message is content-sensitive; github.pr title/body are content-sensitive, owner/repo/base/head routing-sensitive; http.request url is routing+content sensitive).
    - crates/brokerd/src/sinks/http_request.rs lines ~130-215, 278-345 (ssrf_check denials + the allowlist gate — a non-allowlisted or SSRF-range host errors before any socket).
    - cli/caprun/tests/live_acceptance_v1_7_composed.rs leg (a) — the genuine-taint Block pattern (mint from a real event, route into a sensitive arg, assert BlockedPendingConfirmation with the anchor's provenance root equal to the real event id, append sink_blocked, assert no sink_executed, verify_chain true).
  </read_first>
  <behavior>
    - Leg adv-a (github.pr): route a genuinely-tainted value (e.g. exec- or http-derived) into github.pr title (and/or body); submit_plan_node returns BlockedPendingConfirmation anchored on title/body; no PR POST occurs; verify_chain true.
    - Leg adv-b (http.request url): (i) route a tainted value into the http.request url arg -> BlockedPendingConfirmation (routing+content sensitive); AND (ii) an untainted but SSRF/non-allowlisted url (e.g. a metadata/RFC1918 host, or a non-allowlisted host) -> invoke_http_get errors at validate_url/allowlist/ssrf_check before any socket. Assert both the executor Block and the pin-layer Deny.
    - Leg adv-c (git.commit): route a genuinely-tainted value into the git.commit message arg -> BlockedPendingConfirmation anchored on message; no commit runs; verify_chain true.
    - Each adversarial leg's taint is genuine (provenance rooted on a real minted event), never stapled at the sink.
  </behavior>
  <action>
    Add the three adversarial legs to the same file, each its own session on the shared audit.db (mirror v1.7 leg-a structure). For adv-a and adv-c: mint a genuine taint via mint_from_exec (or mint_from_http), route the tainted ValueId into the sensitive arg (title/body for github.pr; message for git.commit), assert executor::submit_plan_node returns BlockedPendingConfirmation with the blocked-arg anchor's provenance_chain[0] equal to the real minted event id, append the sink_blocked event, assert NO sink_executed / NO PR POST / NO commit occurred, and assert verify_chain true. For adv-b: assert the tainted-url executor Block AND, separately, that invoke_http_get on an SSRF-range/non-allowlisted url returns Err before any network (assert on the error, host-portable for the classifier path). Extend the final all_session_ids sweep count to include these sessions. Keep the module doc's rescoping note (no tainted-push leg — that moves to v1.9 with git.push).
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_8_composed 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Acceptance: each adversarial leg is deterministically Blocked (executor I2) or Denied (pin layer) with genuine non-stapled taint and verify_chain true; no sink effect (PR/commit/GET) occurs on any blocked leg; the file still compiles on macOS with only the guard test running.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tainted value (exec/http-derived) → sensitive sink arg | attacker-influenced content reaching a github.pr title/body, git.commit message, or http.request url must be Blocked before the effect |
| broker egress → mock / real api.github.com | live POST (mock) and live GET (real) cross the network under the resolve-and-pin path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-08 | Tampering | value-injection into new sinks | critical | mitigate | genuine taint (mint_from_exec/mint_from_http, provenance rooted on a real event) routed into title/body/message/url is deterministically Blocked by the executor (I2), asserted with the non-stapled backstop; no sink effect on any blocked leg. |
| T-40-09 | Information Disclosure | SSRF / non-allowlisted GET | high | mitigate | adv-b asserts an SSRF-range/non-allowlisted url errors at validate_url/allowlist/ssrf_check before any socket, and a tainted url Blocks at the executor. |
| T-40-10 | Information Disclosure | bearer-token leak in audit | high | mitigate | the github.pr success leg asserts the token literal appears in NO event payload (opaque audit, GITHUB-01). |
| T-40-11 | Repudiation | broken audit chain | high | mitigate | one shared persisted audit.db; per-session verify_chain asserted true across an enumerated (ORDER BY rowid) session set; never :memory:, never LIMIT 1. |
</threat_model>

<verification>
- macOS: cargo test -p caprun --test live_acceptance_v1_8_composed compiles and runs only the guard test (0 Linux tests — expected, per cfg-linux-test-blindness).
- Linux (via scripts/compose-verify.sh, mock-egress-ca feature, leading cargo build --workspace): all success + adversarial legs pass; full-workspace regression green with no v1.0–v1.7 regression (criterion 4).
- The live-OpenAI sidecar leg (ENV-01 second half) is the harness's conditional-on-key run — if OPENAI_API_KEY is absent it is structurally-verified only and flagged; this test never claims it passed.
- The orchestrator re-runs the composed proof as the CLOSING gate, capturing the true exit code before any pipe and asserting on named tests + counts (40-CONTEXT decision 6).
</verification>

<success_criteria>
On real Linux: the composed success workflow (exec -> fs -> git.commit -> github.pr mock 201 -> http GET) is fully gated/tainted/DAG-chained with per-session verify_chain true; all three adversarial legs are deterministically Blocked/Denied; the post-env_clear live api.github.com GET succeeds and mints HttpRaw; full regression is green.
</success_criteria>

<output>
Create `.planning/phases/40-cli-compose-env-clear-live-proof/40-04-SUMMARY.md` when done.
</output>
