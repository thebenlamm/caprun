---
phase: 23-slot-type-binding-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-slot-type-binding.md
autonomous: true
requirements: [DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10]

must_haves:
  truths:
    - "planning-docs/DESIGN-slot-type-binding.md exists and specifies the origin-role tagging mechanism as an additive `origin_role: Option<String>` field on ValueRecord (value_record.rs:21-31), with I0/I1 trust classification explicitly stated as UNAFFECTED (DESIGN-07a)"
    - "The doc unifies with the existing claim_type taxonomy (reuses `\"email_address\"`/`\"relative_path\"`/`\"doc_fragment\"` verbatim as untrusted-side role tags) AND defines analogous role tags from scratch for ProvideIntent-minted UserTrusted values — `\"recipient\"`/`\"subject\"`/`\"body\"`/`\"path\"` keyed by the server.rs mint call sites (DESIGN-08)"
    - "The doc specifies the hardcoded per-sink-arg expected-role table shape mirroring sink_sensitivity.rs — an `expected_role(sink, arg) -> Option<&'static [&'static str]>` function with the exact per-slot lists and the `None`=unconstrained vs `Some(&[])`=never-construct return-type contract pinned"
    - "The doc names DenyReason::SlotTypeMismatch as the new variant and states its full exhaustive-match blast radius is exactly the 2 sites in executor_decision.rs (code() and Display::fmt), explicitly flagged as something Phase 24 must RE-CONFIRM by re-running the grep, not blindly trust (DESIGN-07b)"
  artifacts:
    - planning-docs/DESIGN-slot-type-binding.md
  key_links:
    - "The origin-role tag and expected-role table are specified precisely enough against value_record.rs:21-31, sink_sensitivity.rs's match shape, and the three mint-site signatures (quarantine.rs mint_from_read/mint_from_intent/mint_from_derivation) that Phase 24 can implement the additive change without touching I0/I1 classification"
    - "The claim_type unification names the dual-vocabulary tradeoff explicitly (untrusted `\"email_address\"` vs trusted `\"recipient\"` both target the to/cc/bcc slots) so the expected-role list per slot must enumerate BOTH spellings — an auditable flat array, not a hidden translation layer"
  prohibitions:
    - "NO code is written in crates/executor or crates/brokerd (or any crates/ mint-site) this plan — the deliverable is a single markdown file under planning-docs/; check-invariants.sh must stay green and `git status --porcelain crates/ cli/` must show no changes"
---

<objective>
Author `planning-docs/DESIGN-slot-type-binding.md` — the v1.5 Phase 23 design gate that must exist and clear a fresh (non-self) adversarial review before any `crates/executor` or `crates/brokerd` mint-site code is written. This plan writes the structural-mechanism half of the doc: the gap statement, the origin-role tag representation (DESIGN-07a), the claim_type unification + from-scratch UserTrusted role tags (DESIGN-08), and the hardcoded per-sink-arg expected-role table shape. Plan 23-02 runs the fresh adversarial gate.

The doc must PIN concrete rulings grounded in the research's file:line map (`.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md`), not survey options. It is bound by the LOCKED v1.5 scoping decisions: the origin-role tag is additive/mechanical only (NO I0/I1 trust-classification change); the expected-role table stays hardcoded per-sink-arg, scoped to the two live sinks `email.send`/`file.create` (NO general/pluggable role framework); role-tagging for other sinks is out of scope.

Purpose: This is a HARD GATE — mirrors the v1.0 Phase 2 / v1.2 Phase 8 / v1.3 Phase 12 / v1.4 Phase 18 design-gate discipline (a standalone reviewed DESIGN doc before any TCB code). No `crates/executor` or `crates/brokerd` mint-site code may be written until this doc clears the fresh adversarial panel in Plan 23-02.
Output: `planning-docs/DESIGN-slot-type-binding.md` (§0-§4; Plan-01 Task 2 appends §5-§10).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-session-trust-coherence.md
@planning-docs/DESIGN-taint-model.md
@crates/runtime-core/src/value_record.rs
@crates/runtime-core/src/executor_decision.rs
@crates/executor/src/sink_sensitivity.rs
@crates/executor/src/lib.rs
@crates/brokerd/src/quarantine.rs
@crates/brokerd/src/proto.rs
</context>

## Artifacts this phase produces

- `planning-docs/DESIGN-slot-type-binding.md` (this plan authors §0-§10 across its two tasks)
- `planning-docs/DESIGN-GATE-RECORD-v1.5.md` (authored by Plan 23-02, the adversarial gate record)

This phase produces NO code symbols. This plan touches ONLY `planning-docs/DESIGN-slot-type-binding.md`. It MUST NOT modify any file under `crates/` or `cli/` — that is Phase 24's job, gated on this doc clearing review.

<tasks>

<task type="auto">
  <name>Task 1: Author §0-§3 — gap statement, origin-role tag representation (DESIGN-07a), and claim_type unification + from-scratch UserTrusted role tags (DESIGN-08)</name>
  <files>planning-docs/DESIGN-slot-type-binding.md</files>
  <read_first>
    - `.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md` — the grounding for the whole doc. Especially: Existing-Code Map §1 (claim_type taxonomy, the three values, that claim_type is DISCARDED at mint and never enters ValueRecord — this is the gap), §2 (the three mint-site signatures + the ValueRecord field to add), and Design Decision Space (a) (why `origin_role: Option<String>` on ValueRecord, not a side-table, not a TaintLabel variant) and (b) (the claim_type-vs-new-UserTrusted-role unification).
    - `crates/runtime-core/src/value_record.rs:21-31` — the `ValueRecord { id, literal, taint, provenance_chain }` struct the additive `pub origin_role: Option<String>` field is added to. Confirm it has no claim_type field today.
    - `crates/brokerd/src/quarantine.rs` — the claim_type taxonomy: `Claim { claim_type: String, value: String }` (~47-53); the three extractors setting `"email_address"` (~79), `"relative_path"` (~110), `"doc_fragment"` (~176); the mint_from_read consumption + taint mapping (~316-335) and the fail-closed "unknown claim_type" error (~336-340); the three mint-site signatures `mint_from_read` (~284-297), `mint_from_intent` (~435-442), `mint_from_derivation` (~574-583). Cite EXACT line numbers.
    - `crates/brokerd/src/server.rs` (~1180-1364) — the three mint_from_intent call sites that already know the field they mint: recipient (~1317), subject (~1330), body (~1347), and the CreateFileFromReport path arm (~1299). These are where the from-scratch UserTrusted role tags are keyed.
    - `planning-docs/DESIGN-session-trust-coherence.md` and `planning-docs/DESIGN-taint-model.md` — HOUSE STYLE ONLY (numbered `## §N. Title` sections, a later `## Acceptance Predicate (Done When)`, an `## Accepted Residual Risks` section, round-tagged amendments after review). Match the structure; do NOT copy their substantive content.
    - `CLAUDE.md` — locked terminology (Intent/Session/Planner/Worker/Broker/Adapter/Effect/Artifact/Event); TCB is Rust; policy is hardcoded-in-Rust-TCB, never a config file (CON-i2-non-bypassable); the two build tripwires; `planning-docs/PLAN.md` is source of truth on any conflict.
  </read_first>
  <action>
    Create `planning-docs/DESIGN-slot-type-binding.md` with a title line and a short preamble stating: this is the v1.5 Phase 23 design gate; it resolves DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10; it is bound by the LOCKED v1.5 scoping decisions (origin-role tag is additive/mechanical only with NO I0/I1 change; expected-role table hardcoded per-sink-arg scoped to `email.send`/`file.create`; no general role framework; no role-tagging for other sinks); and no `crates/executor`/`crates/brokerd` mint-site code may precede this doc clearing the fresh adversarial panel (Plan 23-02).

    Then author these numbered sections (this task writes §0-§3; Task 2 appends §4-§10):

    §0 "The gap, precisely (T2)" — state the residual this milestone closes: a `UserTrusted` value minted for one semantic field (e.g. an email `body`) can today occupy a DIFFERENT plan-node slot (e.g. `to`) with no structural check. It is NOT untrusted, so I2's per-arg Block does not fire; it is not a class-level deny, so I0/I1 do not apply. Ground the gap in code: `ValueRecord` (value_record.rs:21-31) carries no semantic-origin field, and `claim_type` — the only semantic-type signal that exists — is consumed at `mint_from_read` (quarantine.rs ~316-335) to derive taint and then DISCARDED, never surviving onto the record. Name this as exactly the gap DESIGN-08 closes.

    §1 "Origin-role tag representation (DESIGN-07a)" — PIN the ruling: add one additive field `pub origin_role: Option<String>` to `ValueRecord` (value_record.rs:21-31), mirroring `claim_type`'s bare-`String` style. State WHY `Option<String>` (not bare `String`): "no role assigned" must be representable distinctly from any valid role tag, because DESIGN-10's fail-closed default keys off `None` never matching a slot's expected-role list. Name and REJECT the two alternatives with written rationale: (ii) a broker side-table keyed by ValueId (rejected — adds a second lookup with its own staleness risk; `resolve()` already returns everything the executor needs in one call, value_store.rs ~89-91), and (iii) a new `TaintLabel` variant (rejected — `TaintLabel` is the I0/I1 trust-classification vocabulary; folding role into it conflates "is this value trustworthy" with "is this value the right shape for this slot", and Phase 24's mandate is explicit that I0/I1 classification is UNAFFECTED). State plainly: this field is orthogonal to taint by construction; adding it does not change which values become `UserTrusted` vs untrusted.

    §2 "Unification with the existing claim_type taxonomy (DESIGN-08, untrusted side)" — PIN: for `mint_from_read` (untrusted-origin) values, `origin_role` is set to the SAME string already in `claim.claim_type` — `"email_address"`, `"relative_path"`, `"doc_fragment"` — reused verbatim, NOT a parallel enum with matching variant names. This is what "unifies with the existing taxonomy" concretely means: the existing extractor output survives onto the record instead of dying at mint time. Cite the three extractors (quarantine.rs ~79/~110/~176) and the fail-closed unknown-claim_type error (~336-340) as the property that keeps this vocabulary closed. Reference the mint-ordering gotcha from research (mint_from_read appends its Event BEFORE minting, quarantine.rs ~342-366) as a fact Phase 24 must respect — do NOT design new sequencing here, just flag it.

    §3 "From-scratch role tags for UserTrusted values (DESIGN-08, trusted side)" — PIN the from-scratch vocabulary for `mint_from_intent`-minted `UserTrusted` values (no existing claim_type equivalent), keyed by the server.rs call site that mints each: `"recipient"` (server.rs ~1317), `"subject"` (~1330), `"body"` (~1347), `"path"` (the CreateFileFromReport arm, ~1299). State the DUAL-VOCABULARY TRADEOFF explicitly: the untrusted-side spelling (`"email_address"`) and the trusted-side spelling (`"recipient"`) both mean "goes in the to/cc/bcc slot" but are spelled differently, so the expected-role table (§4) must enumerate BOTH spellings per slot — an explicit, auditable flat array, NOT a hidden translation function. State WHY this is preferred over inventing one canonical vocabulary and translating claim_type into it (a translation layer with its own correctness burden for zero behavioral gain).

    Write directive, code-tracing prose. Cite exact file:line for every claim about current behavior. Do NOT include fenced code blocks that reproduce implementation; name identifiers, arms, field names, and line numbers instead. Do NOT modify any source file.
  </action>
  <acceptance_criteria>
    - `planning-docs/DESIGN-slot-type-binding.md` exists with a preamble naming DESIGN-07/08/09/10 and the locked scoping decisions (additive tag, no I0/I1 change, hardcoded per-sink-arg, two live sinks).
    - §1 names `origin_role: Option<String>` on ValueRecord as the chosen mechanism, cites value_record.rs, states I0/I1 is UNAFFECTED, and names+rejects the side-table and TaintLabel-variant alternatives with written rationale.
    - §2 states the untrusted-side role tag reuses claim_type's existing strings verbatim (`"email_address"`/`"relative_path"`/`"doc_fragment"`) and cites the three extractors and the fail-closed unknown-claim_type error.
    - §3 defines the from-scratch UserTrusted role tags (`"recipient"`/`"subject"`/`"body"`/`"path"`) keyed to the server.rs mint call sites, and names the dual-vocabulary tradeoff (both spellings must appear per slot in the §4 table).
    - No section modifies, or instructs modifying, any file under `crates/` or `cli/` — the doc is design only.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-slot-type-binding.md && grep -q "origin_role" planning-docs/DESIGN-slot-type-binding.md && grep -q "ValueRecord" planning-docs/DESIGN-slot-type-binding.md && grep -q "claim_type" planning-docs/DESIGN-slot-type-binding.md && grep -q "email_address" planning-docs/DESIGN-slot-type-binding.md && grep -q "doc_fragment" planning-docs/DESIGN-slot-type-binding.md && grep -qi "recipient" planning-docs/DESIGN-slot-type-binding.md && grep -qi "subject" planning-docs/DESIGN-slot-type-binding.md && grep -q "mint_from_read" planning-docs/DESIGN-slot-type-binding.md && grep -q "mint_from_intent" planning-docs/DESIGN-slot-type-binding.md && grep -qi "i0/i1\|I0 / I1\|unaffected" planning-docs/DESIGN-slot-type-binding.md && test -z "$(git status --porcelain crates/ cli/ 2>/dev/null)" && echo PASS</automated>
  </verify>
  <done>The DESIGN doc exists with §0-§3 covering the T2 gap, the additive `origin_role: Option<String>` tag on ValueRecord (DESIGN-07a, alternatives rejected in writing), the claim_type unification reusing existing strings verbatim (DESIGN-08 untrusted side), and the from-scratch UserTrusted role tags with the dual-vocabulary tradeoff named (DESIGN-08 trusted side) — each cited against real file:line, with no source file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Author §4-§10 — expected-role table shape, derivation propagation (DESIGN-09), ordering ruling + DenyReason blast radius (DESIGN-07b/c), fail-closed default (DESIGN-10), adversarial-preemption, Acceptance Predicate, Accepted Residual Risks</name>
  <files>planning-docs/DESIGN-slot-type-binding.md</files>
  <read_first>
    - `.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md` — Existing-Code Map §4 (the COMPLETE DenyReason blast-radius inventory: exactly 2 exhaustive matches, both in executor_decision.rs; every other reference is a construction site; the CLI uses `matches!`/Debug, NOT an exhaustive match), §5 (the sink_sensitivity.rs shape the expected-role table must mirror + the recommended `expected_role` signature), §6 (the full submit_plan_node step order and the load-bearing precedence), §3 (the Concat transform, its byte-verified `local@domain` output shape); Design Decision Space (c) (derivation role = function of transform_kind only, `"concat"` => `"recipient"`, NEVER inherited/unioned) and (d) (hard-Deny via Step 1c vs collect-then-Block, and the fail-closed default's two failure shapes + the unconstrained-slot carve-out); the Adversarial-Review Angles (all 6) and the Pitfalls (empty-vs-None, role-laundering, confirmable-mismatch).
    - `crates/executor/src/sink_sensitivity.rs` — the `is_routing_sensitive`/`is_content_sensitive` `match sink.0.as_str()` shape (~86-107), the const arrays (`EMAIL_SEND_ROUTING_SENSITIVE = ["to","cc","bcc"]` ~61, `FILE_CREATE_ROUTING_SENSITIVE = ["path"]` ~66, `EMAIL_SEND_CONTENT_SENSITIVE = ["subject","body"]` ~78), and the "security property, not a configuration knob / CON-i2-non-bypassable" doc-comment (~5). The expected-role table must mirror this exactly.
    - `crates/executor/src/lib.rs:54-216` — the full `submit_plan_node` step order: Step 0 schema gate (~66-68), the per-arg loop (~78-158) with Step 1 resolve/DanglingHandle (~81-88), Step 1a empty-taint (~96-100), Step 1b empty-provenance (~105-109), Step 2/3 collect-then-Block (~117-157); the after-loop BlockedPendingConfirmation return (~162-164); Step 0.5 session-status class-deny (~174-213, exhaustive over SessionStatus, no wildcard ~176); the load-bearing comment that I2 Block pre-empts I0 class-deny (~169-172); final Allowed (~215).
    - `crates/runtime-core/src/executor_decision.rs` — `DenyReason` definition (~15-60) and its current variants; the two exhaustive matches: `code()` (~64-80) and `Display::fmt` (~83-112). Confirm the exact variant list and match line ranges by reading.
    - `crates/brokerd/src/proto.rs` — `TransformKind` (~57-61, only `Concat` today) and `as_mint_tag()` (~68-72 → `"concat"`); `crates/brokerd/src/quarantine.rs` mint_from_derivation's `match transform_kind` block (~670-692, fail-closed on unknown), its byte-verify guard proving `local@domain` output (~672-684), and its mint-BEFORE-event ordering (~702-729, the reverse of mint_from_read).
    - `planning-docs/DESIGN-session-trust-coherence.md` — HOUSE STYLE for the `## Acceptance Predicate (Done When)` checklist and `## Accepted Residual Risks` section shape (structure only, not content).
  </read_first>
  <action>
    Append §4-§10 to `planning-docs/DESIGN-slot-type-binding.md`, continuing the numbered-section scheme from Task 1.

    §4 "The hardcoded per-sink-arg expected-role table (mirrors sink_sensitivity.rs)" — PIN the shape: a new function `expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>` living in `crates/executor` (Phase 24), same `match sink.0.as_str()` discipline as sink_sensitivity.rs, hardcoded, scoped to the two live sinks. Give the EXACT per-slot lists (not "TBD"), enumerating BOTH the trusted- and untrusted-side spellings per slot: `email.send` → `to`/`cc`/`bcc` => `Some(&["recipient", "email_address"])`, `subject` => `Some(&["subject"])`, `body` => `Some(&["body"])`; `file.create` → `path` => `Some(&["path", "relative_path"])`, `contents` => `None` (deliberately unconstrained for v1.5). PIN the two-state return contract (this is the crux of DESIGN-10): `None` means "no role check for this slot" (unconstrained), `Some(list)` means "the value's role MUST be in `list`". State that `Some(&[])` (a slot that permits zero roles) must NEVER be constructed — a slot with no valid role is a design bug, not a runtime state — to kill the empty-slice-vs-None ambiguity (research Pitfall 2 / Adversarial Angle 2). State that a general/pluggable role framework is OUT OF SCOPE (locked) — anything more general than this match is over-scoped for v1.5.

    §5 "Role propagation through mint_from_derivation (DESIGN-09)" — PIN the explicit verdict (NOT left implicit): the derived value's role is a deterministic function of `transform_kind` ONLY, computed in the SAME `match transform_kind` block that already exists (quarantine.rs ~670-692). For `"concat"`, hardcode `origin_role = Some("recipient".to_string())`. Ground it: `Concat` is the ONLY transform in scope (proto.rs ~57-61), its byte-verify guard proves the output is always `local@domain` shape (quarantine.rs ~672-684), and the live scenario it serves (Reply-To/Domain fragment join into a recipient candidate) IS recipient-address construction — the exact v1.4 T2 scenario this milestone closes. State that tagging it `None` would be WRONG (not merely cautious): it would fail-closed-Deny every legitimately-derived recipient at the `to` slot, breaking the very flow this milestone's acceptance test exercises (cross-check this `"recipient"` choice against the prior confirm-binding framing during authoring — research Assumption A1). CLOSE the laundering angle explicitly (Adversarial Angle 1 / Pitfall 3): the role is assigned from the transform's OWN verified output shape, NEVER inherited or unioned from the input fragments' roles (both inputs carry `"doc_fragment"`) — an attacker cannot smuggle a `"recipient"`-tagged input in to make the output inherit an unearned role. Note the forward-looking obligation: a future SECOND `TransformKind` variant forces a compile-time decision at this same no-wildcard match.

    §6 "Ordering ruling: hard Denied, and the DenyReason blast radius (DESIGN-07b/c)" — PIN the ruling: a slot-type mismatch returns a HARD `Denied { reason: DenyReason::SlotTypeMismatch { .. } }`, and does NOT join the collect-then-Block `BlockedPendingConfirmation` set. Justify: a role mismatch is a STRUCTURAL/type error with no sound human confirmation response (unlike a tainted-but-plausible recipient, which a human CAN legitimately confirm) — reusing `BlockedPendingConfirmation` would be a category error and would force a new `BlockedArg`/anchor shape; hard-Deny needs only the new `DenyReason` variant (research Decision (d), Pitfall 1). Specify WHERE: a new per-arg guard "Step 1c" between Step 1b and Step 2/3 in submit_plan_node — same tier as the existing DanglingHandle/EmptyTaint/EmptyProvenance structural guards, evaluated per-arg in the same pass. REPRODUCE submit_plan_node's FULL existing step list verbatim (schema 0 > per-arg 1/1a/1b > sensitivity 2/3 collect-then-Block > after-loop BlockedPendingConfirmation > Step 0.5 I0 class-deny > Allowed, lib.rs:54-216) so a reviewer can diff old-vs-new ordering line by line, and state explicitly that Step 0.5 (I0) is UNTOUCHED and still gated on an empty `blocked` set — so I2-Block-before-I0-class-deny precedence is preserved exactly (research §6, Adversarial Angle 3/6). Then give the DenyReason blast radius: name the new variant `DenyReason::SlotTypeMismatch` (match ROADMAP/REQUIREMENTS vocabulary), and state that the full exhaustive-match blast radius is EXACTLY the 2 sites in executor_decision.rs — `code()` (~64-80) and `Display::fmt` (~83-112) — with the workspace grep inventory reproduced (every other `DenyReason` reference is a construction site, and cli/caprun/src/worker.rs uses `matches!`/Debug-format, NOT an exhaustive match, so needs NO update). CRITICAL: state explicitly that this "exactly 2 sites" claim is something Phase 24 MUST RE-CONFIRM by re-running `grep -rn "DenyReason" crates/ cli/` at implementation time — the doc records it as a verified-at-research-time inventory Phase 24 re-checks, NOT a fact Phase 24 blindly trusts. Note the separate `SessionStatus` exhaustive match (lib.rs ~176) is UNCHANGED by this phase (do not conflate blast radii).

    §7 "Fail-closed default (DESIGN-10)" — PIN: the expected-role lookup returns `Option<&[&str]>` per §4. Enumerate the outcomes so no third path exists: (1) value has NO role (`origin_role == None`) AND slot IS role-checked (lookup returns `Some(list)`) → `Denied`; (2) value HAS a role but it's not in the slot's `Some(list)` → `Denied`; (3) (explicit non-failure) slot is UNCONSTRAINED (lookup returns `None`, e.g. `file.create`'s `contents`) → role check is a no-op, arg falls through to Step 2/3 as today. State that (3) is NOT a fail-open bug — it is a scoped-out slot, and DESIGN-10's "never silent pass-through to Allowed" is satisfied at the DOCUMENT level (the carve-out is written, not inferred). Cite research Adversarial Angle 2's exact probe (a `.unwrap_or(&[])` implementation that collapses None-vs-empty is a bug the doc's contract forbids).

    §8 "Adversarial-review preemption" — enumerate and pre-answer the 6 angles a fresh reviewer will probe (from research): (1) can a derived value launder its role — answered in §5; (2) does fail-closed truly fail closed for an unassigned role — answered in §7 with both shapes; (3) does the check reorder/weaken I0/I2 — answered in §6 with the full step list; (4) is the match blast radius actually complete — answered in §6 with the reproducible grep and the worker.rs `matches!`/Debug note; (5) does the claim_type-vs-role dual vocabulary create a bypass — answered by §4's exact per-slot lists; (6) session-status interaction — Step 1c runs unconditionally in the per-arg loop BEFORE Step 0.5's session-status match, so a Draft session's role-mismatch still Denies. Keep each angle to a short paragraph pointing at the resolving section.

    §9 "Acceptance Predicate (Done When)" — a numbered checklist, one line per DESIGN-07(a/b/c), DESIGN-08, DESIGN-09, DESIGN-10, phrased so Plan 23-02's adversarial reviewer can check each against BOTH the doc AND the cited code.

    §10 "Accepted Residual Risks" — collect: (a) the DenyReason "exactly 2 sites" blast radius is a research-time inventory Phase 24 must re-confirm, not a guarantee; (b) the forward-looking obligation for a future second `TransformKind` variant (a named v2 obligation, mirroring the house "named v2 obligation" convention); (c) `file.create`'s `contents` is deliberately role-unconstrained for v1.5 scope (research Assumption A2 — if a reviewer disagrees, it may need a `"file_body"` tag mirroring `body`); (d) explicit note that NO I0/I1 trust-classification change is made and NO general role framework is designed (both locked out of scope).

    Cite exact file:line for every current-behavior claim. Do NOT design a general role framework, a config-file policy, or any raw args-map-to-sink path (keep the table a lookup over `(SinkId, arg_name) -> Option<&[&str]>`). No fenced implementation code — name identifiers, arms, and line numbers. Do NOT modify any source file.
  </action>
  <acceptance_criteria>
    - §4 gives the exact per-slot expected-role lists (including both trusted- and untrusted-side spellings, e.g. `["recipient", "email_address"]` for to/cc/bcc) and pins the `None`=unconstrained vs `Some(&[])`-never-constructed return-type contract; names the general framework as out of scope.
    - §5 pins `mint_from_derivation`'s `"concat"` role as `"recipient"` (explicit verdict, not implicit), grounds it in the byte-verified `local@domain` output shape, and states the role is NEVER inherited/unioned from input fragment roles.
    - §6 rules the mismatch is a hard `Denied` (not BlockedPendingConfirmation) via a per-arg Step 1c, reproduces submit_plan_node's full step order, states Step 0.5 (I0) is untouched, names `DenyReason::SlotTypeMismatch`, states the blast radius is exactly the 2 executor_decision.rs matches with the grep reproduced, and explicitly flags that Phase 24 must RE-CONFIRM the blast radius (not blindly trust it).
    - §7 enumerates the two Deny shapes AND the explicit unconstrained-slot non-failure carve-out, satisfying DESIGN-10's "never silent pass-through" at the document level.
    - §9 (Acceptance Predicate) has one checkable line per DESIGN-07/08/09/10; §10 (Accepted Residual Risks) records the blast-radius-reconfirm obligation, the future-transform obligation, the contents-unconstrained carve-out, and the no-I0/I1-change / no-framework locks.
    - No source file under `crates/` or `cli/` is modified.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "expected_role" planning-docs/DESIGN-slot-type-binding.md && grep -q "sink_sensitivity" planning-docs/DESIGN-slot-type-binding.md && grep -q "SlotTypeMismatch" planning-docs/DESIGN-slot-type-binding.md && grep -q "mint_from_derivation" planning-docs/DESIGN-slot-type-binding.md && grep -qi "concat" planning-docs/DESIGN-slot-type-binding.md && grep -q "submit_plan_node" planning-docs/DESIGN-slot-type-binding.md && grep -qi "denied" planning-docs/DESIGN-slot-type-binding.md && grep -qi "BlockedPendingConfirmation" planning-docs/DESIGN-slot-type-binding.md && grep -qi "fail-closed\|fail closed" planning-docs/DESIGN-slot-type-binding.md && grep -qi "re-confirm\|reconfirm\|re-run" planning-docs/DESIGN-slot-type-binding.md && grep -qi "acceptance predicate" planning-docs/DESIGN-slot-type-binding.md && grep -qi "residual" planning-docs/DESIGN-slot-type-binding.md && test -z "$(git status --porcelain crates/ cli/ 2>/dev/null)" && echo PASS</automated>
  </verify>
  <done>The DESIGN doc's §4-§10 pin the expected-role table shape with exact per-slot lists and the None-vs-Some(&[]) contract, the derivation `"concat"`=>`"recipient"` verdict with the anti-laundering rationale (DESIGN-09), the hard-Denied ordering ruling via Step 1c with the full submit_plan_node step order and the exactly-2-site DenyReason blast radius flagged for Phase-24 re-confirmation (DESIGN-07b/c), the fail-closed default with both Deny shapes + the unconstrained carve-out (DESIGN-10), plus the adversarial-preemption, Acceptance Predicate, and Accepted Residual Risks — all cited against real file:line, with no source file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces ONLY a markdown design document — it introduces no runtime code and crosses no live trust boundary itself. The relevant boundary is downstream: the DESIGN doc is the specification Phase 24 implements into the TCB (`crates/executor` expected-role table + `submit_plan_node` Step 1c, `crates/brokerd` mint sites, `runtime-core` DenyReason variant). An under-specified or wrong ruling here becomes a TCB defect there.

| Boundary | Description |
|----------|-------------|
| DESIGN doc → Phase 24 TCB implementation | An ambiguous/incorrect ruling (empty-vs-None table contract, a laundering-prone derivation role, a wrong blast-radius count, a mismatched ordering) propagates into a TCB change; the doc's precision IS the mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | Fail-open via ambiguous "no constraint" representation — the doc leaves the empty-slice-vs-None table contract unpinned, letting Phase 24 implement a `.unwrap_or(&[])` that collapses unconstrained-slot into allow-anything | high | mitigate | §4 pins `None`=unconstrained vs `Some(&[])`-never-constructed; §7 enumerates both Deny shapes; the fresh gate (Plan 23-02) traces the contract against the fail-closed intent |
| T-23-02 | Elevation of privilege | Role-laundering through derivation — the doc leaves derivation role propagation implicit, letting Phase 24 inherit/union input roles so an attacker fragment smuggles an unearned `"recipient"` role onto a derived value | high | mitigate | §5 pins role = function of `transform_kind` only (byte-verified `local@domain` shape), never inherited from inputs; reviewer re-traces mint_from_derivation |
| T-23-03 | Tampering | Incomplete blast-radius claim — the doc asserts "2 match sites" without a reproducible inventory, and Phase 24 misses a match site (or the count drifts before Phase 24 runs), leaving a non-exhaustive `DenyReason` match | medium | mitigate | §6 reproduces the `grep -rn "DenyReason" crates/ cli/` inventory AND explicitly flags Phase 24 must re-run it, not trust the count; reviewer re-runs the grep |
| T-23-04 | Elevation of privilege | Ordering regression — the doc's Step 1c placement reorders or weakens the existing I0 (Step 0.5) / I2 (collect-then-Block) precedence | high | mitigate | §6 reproduces submit_plan_node's full step list and states Step 0.5 is untouched + still gated on an empty `blocked` set; reviewer diffs old-vs-new ordering |

No package-manager installs occur in this plan (docs only) — the T-{phase}-SC supply-chain checkpoint is not applicable. No new external crate dependency is introduced or designed (the recommended shape uses only `Option<String>`, `&'static str` arrays, and existing `serde` derives).
</threat_model>

<verification>
- `planning-docs/DESIGN-slot-type-binding.md` exists and resolves DESIGN-07/08/09/10 across §0-§10.
- Both automated grep gates (Task 1, Task 2) return PASS.
- The doc cites real file:line (value_record.rs, quarantine.rs, executor_decision.rs, sink_sensitivity.rs, lib.rs, proto.rs, server.rs) for every current-behavior claim.
- No source/TCB file is modified: `git status --porcelain crates/ cli/` shows no changes attributable to this plan.
- `./scripts/check-invariants.sh` stays green (no executor/broker mint-site code added).
</verification>

<success_criteria>
The DESIGN doc is authored and internally complete: it pins the additive `origin_role: Option<String>` tag (DESIGN-07a), the claim_type unification + from-scratch UserTrusted role tags (DESIGN-08), the hardcoded per-sink-arg expected-role table with the None-vs-Some(&[]) contract, the derivation `"concat"`=>`"recipient"` verdict (DESIGN-09), the hard-Denied Step-1c ordering ruling with the exactly-2-site DenyReason blast radius (DESIGN-07b/c, flagged for Phase-24 re-confirmation), and the fail-closed default (DESIGN-10) — all grounded in file:line and consistent with the locked scoping decisions, ready for the fresh adversarial gate in Plan 23-02.
</success_criteria>

<output>
Create `.planning/phases/23-slot-type-binding-design-gate/23-01-SUMMARY.md` when done.
</output>
