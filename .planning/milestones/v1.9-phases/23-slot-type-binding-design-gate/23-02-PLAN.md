---
phase: 23-slot-type-binding-design-gate
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - planning-docs/DESIGN-GATE-RECORD-v1.5.md
  - planning-docs/DESIGN-slot-type-binding.md
autonomous: true
requirements: [DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10]

must_haves:
  truths:
    - "A FRESH, independent adversarial reviewer (NOT the Plan 23-01 authoring session) reviewed planning-docs/DESIGN-slot-type-binding.md by TRACING the cited code (value_record.rs, quarantine.rs, executor_decision.rs, sink_sensitivity.rs, lib.rs, proto.rs), not merely reading the doc (per DEC-ai-review-satisfies-human-gate + the fresh-context-adversarial-review discipline)"
    - "planning-docs/DESIGN-GATE-RECORD-v1.5.md records the reviewer's findings, each with severity + file:line evidence + disposition, a per-requirement checklist over DESIGN-07..10, and a final gate status of CLEARED"
    - "Every issue the reviewer raised is resolved in DESIGN-slot-type-binding.md before the gate is marked CLEARED — no open blocker remains at gate close, and no finding was resolved by weakening a ruling or by designing a locked-out mechanism (a general role framework, or an I0/I1 classification change)"
    - "The review re-runs `grep -rn \"DenyReason\" crates/ cli/` and confirms the doc's exactly-2-exhaustive-match blast-radius claim against live code, and confirms the derivation `\"concat\"`=>`\"recipient\"` role is assigned from the transform's verified output shape, never inherited from input roles"
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.5.md
  key_links:
    - "The reviewer is fresh/independent (spawned as a separate agent, or an explicitly-fresh reviewer pass), not the Plan 23-01 authoring context — a self-review does NOT satisfy the gate (this project's hard-won fresh-context-adversarial-review lesson: a self-read caught 0 of 5 blockers a fresh code-tracing reviewer caught)"
    - "The gate record's CLEARED status is the sole authorization for Phase 24 to begin any crates/executor/crates/brokerd mint-site code — until it reads CLEARED with all findings resolved, no TCB code may be written"
  prohibitions:
    - "NO code is written in crates/executor or crates/brokerd (or any crates/ mint-site) this plan — check-invariants.sh must stay green and `git status --porcelain crates/ cli/` must show no changes; the gate record explicitly re-confirms this before reading CLEARED"
---

<objective>
Run the fresh adversarial gate on `planning-docs/DESIGN-slot-type-binding.md` (authored by Plan 23-01) and record it in `planning-docs/DESIGN-GATE-RECORD-v1.5.md`, mirroring the v1.2/v1.3/v1.4 DESIGN-GATE-RECORD precedent. Per `DEC-ai-review-satisfies-human-gate`, an AI-performed adversarial re-read by a FRESH, independent reviewer (not the authoring session) satisfies the human-gate requirement — but ONLY if the reviewer traces the actual cited code and every raised issue is resolved in the doc before the gate closes.

Purpose: This is the gate that unblocks Phase 24. Until `DESIGN-GATE-RECORD-v1.5.md` records status CLEARED with all findings resolved, no `crates/executor` or `crates/brokerd` mint-site code for slot-type binding may begin.
Output: `planning-docs/DESIGN-GATE-RECORD-v1.5.md` (gate record) + any remediation edits to `planning-docs/DESIGN-slot-type-binding.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md
@planning-docs/DESIGN-slot-type-binding.md
@planning-docs/DESIGN-GATE-RECORD-v1.4.md
@crates/runtime-core/src/value_record.rs
@crates/runtime-core/src/executor_decision.rs
@crates/executor/src/sink_sensitivity.rs
@crates/executor/src/lib.rs
@crates/brokerd/src/quarantine.rs
@crates/brokerd/src/proto.rs
@CLAUDE.md
</context>

## Artifacts this phase produces

- `planning-docs/DESIGN-GATE-RECORD-v1.5.md` (this plan authors it)
- Remediation edits to `planning-docs/DESIGN-slot-type-binding.md` (this plan, if the reviewer raises issues)

This phase produces NO code symbols. This plan touches ONLY `planning-docs/*.md`. It MUST NOT modify any file under `crates/` or `cli/` — the implementation is Phase 24, gated on this record reading CLEARED.

<tasks>

<task type="auto">
  <name>Task 1: Spawn a fresh, independent adversarial reviewer that TRACES the code and record findings in DESIGN-GATE-RECORD-v1.5.md</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.5.md</files>
  <read_first>
    - `planning-docs/DESIGN-slot-type-binding.md` — the document under review (authored by Plan 23-01).
    - `planning-docs/DESIGN-GATE-RECORD-v1.4.md` — the precedent format/rigor to match (Revision History, Documents Under Review, per-requirement Checklist, per-finding disposition table with file:line evidence, "How to Verify (Human Review Steps)", "Decision", "Gate status").
    - `.planning/phases/23-slot-type-binding-design-gate/23-RESEARCH.md` — the Adversarial-Review Angles (all 6), the Pitfalls (empty-vs-None, role-laundering, confirmable-mismatch), and the Assumptions Log (A1 `"recipient"` role choice, A2 `contents` unconstrained, A3 the un-re-verified `ExecutorDecision` grep). The reviewer must confirm each doc ruling is present AND correct against code.
    - The `adversarial-review` skill and the `fresh-context-adversarial-review` discipline (memory: a self-read of a security design is NOT sufficient — a fresh code-tracing reviewer caught 5 blockers a self-read approved, incl. a live exfiltration hole). The reviewer must TRACE CODE.
    - `crates/runtime-core/src/value_record.rs`, `crates/runtime-core/src/executor_decision.rs`, `crates/executor/src/sink_sensitivity.rs`, `crates/executor/src/lib.rs`, `crates/brokerd/src/quarantine.rs`, `crates/brokerd/src/proto.rs` — the cited source the reviewer traces.
  </read_first>
  <action>
    Spawn a FRESH, independent adversarial reviewer against `planning-docs/DESIGN-slot-type-binding.md`. Do NOT self-review from this plan's authoring lineage — the reviewer must be a separate agent/context (per `DEC-ai-review-satisfies-human-gate` and the `fresh-context-adversarial-review` memory: my own read is not enough; spawn a fresh reviewer that traces code).

    Preferred mechanism: use the Task tool to spawn a general-purpose adversarial-review agent. If the Task tool is unavailable in this execution environment, invoke the `adversarial-review` skill, or document in the gate record the exact fresh-reviewer prompt and run it as a distinct, clearly-fresh pass. Whichever mechanism, the reviewer MUST be given: the DESIGN doc, AND the cited source files (`crates/runtime-core/src/value_record.rs`, `crates/runtime-core/src/executor_decision.rs`, `crates/executor/src/sink_sensitivity.rs`, `crates/executor/src/lib.rs`, `crates/brokerd/src/quarantine.rs`, `crates/brokerd/src/proto.rs`).

    Instruct the reviewer to TRACE CODE (not just read the doc) and adversarially test each of these:
      - DESIGN-07a (tag mechanism): Is `origin_role: Option<String>` on ValueRecord (value_record.rs:21-31) genuinely additive and orthogonal to taint? Does adding it leave I0/I1 classification untouched? Are the side-table and TaintLabel-variant alternatives soundly rejected?
      - DESIGN-07b (blast radius): RE-RUN `grep -rn "DenyReason" crates/ cli/` (excluding /target/) and independently confirm the doc's "exactly 2 exhaustive-match sites" claim — the two matches in executor_decision.rs (code() ~64-80, Display::fmt ~83-112) — and confirm cli/caprun/src/worker.rs uses `matches!`/Debug (NOT an exhaustive match). Also re-check whether any exhaustive match over the OUTER `ExecutorDecision` enum would need a new arm (research Assumption A3 was NOT re-verified) — since the recommendation reuses the existing `Denied { reason }`, this should be moot, but the reviewer must confirm.
      - DESIGN-07c (ordering): Is the hard-Denied-via-Step-1c ruling actually correct against submit_plan_node's real step order (lib.rs:54-216)? Does inserting Step 1c between 1b and 2/3 preserve I2-Block-before-I0-class-deny (Step 0.5 still gated on empty `blocked`)? Is Step 0.5 genuinely untouched?
      - DESIGN-08 (unification): Does reusing claim_type's `"email_address"`/`"relative_path"`/`"doc_fragment"` strings verbatim as untrusted-side role tags actually unify (not duplicate) the taxonomy against quarantine.rs? Are the from-scratch `"recipient"`/`"subject"`/`"body"`/`"path"` tags correctly keyed to the real server.rs mint call sites? Do the §4 per-slot expected-role lists enumerate BOTH spellings so no legitimate value is wrongly denied AND no misrouted value is wrongly allowed (over-broad membership)?
      - DESIGN-09 (derivation): Is the `"concat"`=>`"recipient"` role assigned from the transform's byte-verified `local@domain` output shape (quarantine.rs ~672-684), NOT inherited/unioned from the input fragments' roles? Trace mint_from_derivation (~670-729) — is there any path where `origin_role` reads from `inputs`? Is `"recipient"` the right tag (research A1) — does it correctly permit the legitimate concat-derived recipient at `to` while still catching a misroute?
      - DESIGN-10 (fail-closed): Do BOTH failure shapes (None-role at a role-checked slot; role-not-in-list) reach `Denied` with no third path? Is the `None`=unconstrained vs `Some(&[])`-never-constructed contract pinned tightly enough to forbid a `.unwrap_or(&[])` fail-open? Is the `contents`-unconstrained carve-out explicit (not silent)?
      - Cross-cutting: any way a misrouted `UserTrusted` value still reaches a sensitive sink as `Allowed` under the specified design? Any locked-decision violation (the doc designs a general role framework, changes I0/I1 classification, or role-tags a sink beyond `email.send`/`file.create`)? Does the doc correctly flag Phase 24 must RE-CONFIRM the blast radius rather than trust it?

    Capture the reviewer's raw findings. Then create `planning-docs/DESIGN-GATE-RECORD-v1.5.md` in the precedent format: a header (milestone v1.5, document under review, reviewer identity + that it is fresh/independent + the mechanism used), a Revision History, a per-requirement Checklist (DESIGN-07..10, each pass/fail/needs-remediation), a per-finding table (finding, severity, evidence file:line, disposition), a "How to Verify" section, and a "Gate status" line. Set the initial Gate status to BLOCKED/OPEN if any finding needs remediation, or CLEARED only if the reviewer raised nothing. Do NOT fabricate a clean review — record findings honestly.

    Do NOT modify any source/TCB file. Do NOT edit ROADMAP.md/STATE.md or mark the phase complete (per the standing project mitigation: no executor writes phase-completion state — the orchestrator does that after independent verification).
  </action>
  <acceptance_criteria>
    - `planning-docs/DESIGN-GATE-RECORD-v1.5.md` exists and identifies the reviewer as fresh/independent of the authoring session (names the mechanism: spawned agent / adversarial-review skill / explicit fresh pass).
    - The record contains a Checklist covering all four requirements DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10, each with a pass/fail/needs-remediation disposition.
    - Every finding is recorded with a severity and file:line evidence; the record's Gate status reflects reality (OPEN/BLOCKED if any finding is unresolved, not a fabricated CLEARED).
    - The record shows the reviewer traced code — actual line references (executor_decision.rs, lib.rs, quarantine.rs, sink_sensitivity.rs, value_record.rs) appear in the findings/verification — AND records the re-run `grep -rn "DenyReason" crates/ cli/` blast-radius confirmation, not merely a prose re-read.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -qi "fresh\|independent" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "DESIGN-07" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "DESIGN-08" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "DESIGN-09" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "DESIGN-10" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -qi "gate status" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -qi "executor_decision.rs\|quarantine.rs\|lib.rs\|sink_sensitivity.rs" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "DenyReason" planning-docs/DESIGN-GATE-RECORD-v1.5.md && echo PASS</automated>
  </verify>
  <done>A fresh, independent, code-tracing adversarial review of the DESIGN doc is complete — the reviewer re-ran the DenyReason blast-radius grep and traced the mint/executor code — and its findings + per-requirement checklist (DESIGN-07..10) + honest gate status are recorded in DESIGN-GATE-RECORD-v1.5.md.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve every raised finding in the DESIGN doc and close the gate to CLEARED</name>
  <files>planning-docs/DESIGN-slot-type-binding.md, planning-docs/DESIGN-GATE-RECORD-v1.5.md</files>
  <read_first>
    - `planning-docs/DESIGN-GATE-RECORD-v1.5.md` — the findings from Task 1 that must each be resolved.
    - `planning-docs/DESIGN-slot-type-binding.md` — the doc to remediate.
    - `crates/runtime-core/src/executor_decision.rs`, `crates/executor/src/lib.rs`, `crates/executor/src/sink_sensitivity.rs`, `crates/brokerd/src/quarantine.rs`, `crates/brokerd/src/proto.rs`, `crates/runtime-core/src/value_record.rs` — re-consult for any finding that turned on a code fact.
    - `.planning/REQUIREMENTS.md` "Out of Scope" table + `CLAUDE.md` — remediation must NOT resolve a finding by designing a general role framework, changing I0/I1 trust classification, or role-tagging a sink beyond `email.send`/`file.create` (all locked out).
  </read_first>
  <action>
    For each finding recorded in `DESIGN-GATE-RECORD-v1.5.md`, resolve it IN `DESIGN-slot-type-binding.md` by amending the relevant section (round-tagged amendment, house style — fold the fix into the relevant § with an inline tag like "revised after DESIGN-GATE-RECORD-v1.5 Round 1, finding F1", never a separate changelog). Never resolve a finding by weakening a ruling to make it go away, and never by violating a locked scoping decision (no general role framework, no I0/I1 classification change, no new sink). If a finding is a genuine disagreement where the doc is correct and the reviewer erred, record the rebuttal in the gate record with evidence (the doc/code file:line that settles it) rather than editing the doc — but a passing self-argument is not evidence; cite the file:line that decides it.

    If any finding was a BLOCKER (a misrouted value still reaches a sensitive sink as Allowed under the specified design; a fail-open in the table contract; a role-laundering path through derivation; a locked-decision violation), re-run the fresh reviewer against the amended doc after remediation and record the second-round result. Iterate until the reviewer raises no unresolved blocker.

    Update `DESIGN-GATE-RECORD-v1.5.md`: append a Revision History entry per remediation round, mark each finding resolved (with the section/edit that resolved it, or the rebuttal evidence), and set the final Gate status to CLEARED only when every finding is resolved and no blocker remains. The record must make explicit that CLEARED means Phase 24 may now begin the `crates/executor`/`crates/brokerd` mint-site change, AND must re-confirm (with `git status --porcelain crates/ cli/` empty + `./scripts/check-invariants.sh` green) that NO TCB code was written during this design-gate phase.

    Do NOT touch any source/TCB file. Do NOT mark the phase complete or edit ROADMAP.md/STATE.md (per the standing project mitigation: no executor writes phase-completion state — the orchestrator does that after independent verification).
  </action>
  <acceptance_criteria>
    - Every finding in `DESIGN-GATE-RECORD-v1.5.md` has a resolution disposition (resolved-by-edit with the section named, or rebutted-with-evidence citing file:line) — none left open.
    - `DESIGN-GATE-RECORD-v1.5.md`'s final Gate status reads CLEARED and states that Phase 24 may begin, and re-confirms no TCB code was written this phase (git-clean crates/ + cli/, check-invariants.sh green).
    - No finding was resolved by designing a general role framework, an I0/I1 trust-classification change, or role-tagging a sink beyond `email.send`/`file.create` (locked out of scope).
    - After remediation, `DESIGN-slot-type-binding.md` still pins: the additive `origin_role: Option<String>` tag, the hard-Denied ordering ruling, the `"concat"`=>`"recipient"` derivation role, and the fail-closed None-vs-Some(&[]) contract (the load-bearing rulings survive remediation).
    - No source/TCB file (`crates/`, `cli/`) was modified; ROADMAP.md/STATE.md were not touched.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "cleared" planning-docs/DESIGN-GATE-RECORD-v1.5.md && grep -q "origin_role" planning-docs/DESIGN-slot-type-binding.md && grep -q "SlotTypeMismatch" planning-docs/DESIGN-slot-type-binding.md && grep -qi "concat" planning-docs/DESIGN-slot-type-binding.md && grep -qi "fail-closed\|fail closed" planning-docs/DESIGN-slot-type-binding.md && test -z "$(git status --porcelain crates/ cli/ 2>/dev/null)" && echo PASS || echo "UNEXPECTED_SRC_CHANGE_OR_OPEN_GATE"</automated>
  </verify>
  <done>All reviewer findings are resolved in the DESIGN doc (without violating any locked decision), DESIGN-GATE-RECORD-v1.5.md reads CLEARED and authorizes Phase 24, re-confirms no TCB code was written this phase, and no source file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces the review record and remediation edits — markdown only. The boundary it guards is the design gate itself: a fabricated or shallow review that reads CLEARED without genuine code-tracing would let a defective slot-type-binding spec through to Phase 24's TCB change.

| Boundary | Description |
|----------|-------------|
| Fresh reviewer → gate status | A non-fresh or non-code-tracing review that clears the gate lets an unsound design (fail-open table, role-laundering, wrong blast radius, ordering regression) reach TCB implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05 | Repudiation | Gate record claims CLEARED without a genuinely fresh, code-tracing review (e.g. a self-read from the authoring lineage) | high | mitigate | Task 1 mandates a fresh/independent reviewer that traces cited code AND re-runs the DenyReason grep; the record must name the mechanism and cite line numbers; honest gate status required (this project's fresh-context-adversarial-review lesson: a self-read caught 0 of 5 blockers) |
| T-23-06 | Elevation of privilege | A finding is resolved by weakening a ruling or by designing a locked-out mechanism (a general role framework, an I0/I1 change, a new sink) | high | mitigate | Task 2 acceptance criteria forbid locked-decision violations and require the additive-tag / hard-Denied / concat-recipient / fail-closed rulings to survive remediation |
| T-23-07 | Tampering | TCB code is written during this design-gate phase, violating the hard gate | high | mitigate | Both tasks forbid touching `crates/`/`cli/`; Task 2 re-confirms `git status --porcelain crates/ cli/` empty + `check-invariants.sh` green before the gate reads CLEARED |

No package-manager installs occur in this plan (docs only) — the T-{phase}-SC supply-chain checkpoint is not applicable.
</threat_model>

<verification>
- `planning-docs/DESIGN-GATE-RECORD-v1.5.md` exists, identifies a fresh code-tracing reviewer, covers DESIGN-07..10, records the re-run DenyReason blast-radius grep, and reads Gate status CLEARED with every finding resolved.
- `planning-docs/DESIGN-slot-type-binding.md` still honors all locked decisions after remediation (additive tag, hard-Denied ordering, concat=>recipient, fail-closed contract; no general framework, no I0/I1 change).
- No source/TCB file modified: `git status --porcelain crates/ cli/` is empty; `./scripts/check-invariants.sh` green.
- ROADMAP.md / STATE.md untouched by this plan (orchestrator owns phase-completion state).
</verification>

<success_criteria>
The design gate is cleared: a fresh, independent, code-tracing adversarial reviewer examined `DESIGN-slot-type-binding.md` (re-running the DenyReason blast-radius grep and tracing the mint/executor code), every raised issue is resolved in the doc without violating a locked decision, and `DESIGN-GATE-RECORD-v1.5.md` records status CLEARED authorizing Phase 24 to begin the `crates/executor`/`crates/brokerd` mint-site change — with no TCB code written this phase.
</success_criteria>

<output>
Create `.planning/phases/23-slot-type-binding-design-gate/23-02-SUMMARY.md` when done.
</output>
