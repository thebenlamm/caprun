---
phase: 43-http-request-write-post-put-egress
plan: 04
type: execute
wave: 4
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - crates/brokerd/tests/s43_http_write_differential.rs
autonomous: true
requirements: [HTTP-W-01]

must_haves:
  truths:
    - "The tainted-body-Blocks leg and the clean-body-Allowed leg are IDENTICAL in host/url/method/policy — taint is the SOLE variable — so a block-everything I2 regression cannot pass (the clean leg MUST reach Allowed + the live write dispatch)."
    - "A `http.request.write` from a draft / untrusted-seeded session is I0-Denied (CommitIrreversible) — the MAJOR-1 I0-escape leg."
    - "A tainted `body` deterministically Blocks under I2 on the `body` arg specifically (genuine taint, real audit-DAG edge — not stapled)."
    - "A method literal outside {POST,PUT} Denies fail-closed."
  artifacts:
    - "crates/brokerd/tests/s43_http_write_differential.rs — the HTTP-W-01 differential acceptance test."
  key_links:
    - "the clean and tainted legs share url/method/policy/session-status; only the body value's taint differs."
    - "the clean Allowed leg reaches invoke_http_write dispatch (a terminal http_write_* event), proving delivery-intent — not merely 'not blocked'."
---

<objective>
Prove HTTP-W-01 differentially in an in-crate acceptance test: with host, url, method, and policy held identical, a tainted request body Blocks under I2 while a clean body is Allowed and reaches the live write dispatch — plus the I0 draft-deny leg (CommitIrreversible) and the method-enum Deny leg. This is the phase's requirement proof; the full composed live mock-endpoint delivery on real Linux is Phase 46's job (LIVE-06).

Purpose: DESIGN-v1.9-egress-policy §2.5 (`[rev: M4]`) — acceptance is DIFFERENTIAL, not "not blocked". A passing run requires the clean body to be Allowed AND to reach egress AND the tainted body to Block, attributable to I2 specifically, so a block-everything regression cannot masquerade as a pass.

Output: A committed, green differential acceptance test asserting all four HTTP-W-01 legs.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@planning-docs/DESIGN-v1.9-egress-policy.md

# Mirror an existing composed/differential broker acceptance test for shape (session setup, mint, submit_plan_node, audit assertions, verify_chain):
@crates/brokerd/src/server.rs
@crates/brokerd/src/confirmation.rs

@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decision-level differential + I0 + method-enum legs</name>
  <files>crates/brokerd/tests/s43_http_write_differential.rs</files>
  <behavior>
    - LEG A (I0): a draft / untrusted-seeded session submitting http.request.write → Denied (DraftOnlySessionDeniesCommitIrreversible) — proving the distinct id is CommitIrreversible.
    - LEG B (tainted body Blocks): an Active session, url/method/policy fixed, a body minted with an untrusted taint label → BlockedPendingConfirmation whose anchor names the `body` arg.
    - LEG C (clean body Allowed): the SAME Active session shape, SAME url/method/policy, a body minted UserTrusted (clean) — the ONLY difference from LEG B is the body's taint → Allowed.
    - LEG D (method enum): the same clean-body node but with method literal not in {POST,PUT} → Denied fail-closed.
    - The B-vs-C legs assert explicitly that url, method, and policy are byte-identical across the two (taint is the sole variable).
  </behavior>
  <action>Create `crates/brokerd/tests/s43_http_write_differential.rs`. Build a helper that constructs an `http.request.write` plan node + value_store with a fixed url (a write-allowlisted host), a fixed method ("POST"), and a body ValueNode whose taint is the SOLE parameter. Drive `executor::submit_plan_node` (through the broker's value_store + policy — use `SessionPolicy::broker_default()` so the new sink is policy-permitted; assert the SAME policy value is used for B and C). Assert the four legs from the behavior block. For LEG B, assert the returned `BlockedPendingConfirmation` anchor's `arg == "body"` (the Block is attributable to the body specifically, not a blanket sink block). For B-vs-C, assert equality of the url/method literals and the policy across both legs in the test body (make the "taint is the sole variable" property a literal assertion, so a future change that diverges them fails). Follow the anti-stapling discipline used elsewhere: mint the tainted body through the real broker mint path (a genuine provenance chain), never a hand-set taint field.</action>
  <verify>
    <automated>cargo test -p brokerd --test s43_http_write_differential --no-fail-fast</automated>
  </verify>
  <done>All four legs (I0 draft-deny, tainted-body Block on `body`, clean-body Allow with identical url/method/policy, method-enum Deny) pass; B-vs-C identical-except-taint asserted literally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch-level differential — clean leg reaches egress, tainted does not</name>
  <files>crates/brokerd/tests/s43_http_write_differential.rs</files>
  <behavior>
    - Through the broker dispatch path (dispatch_request over a UnixStream pair, mirroring the existing dispatch-arm tests), the clean-body Allowed leg reaches the live write dispatch and records an OPAQUE terminal `http_write_*` event (succeeded or, absent a live endpoint, failed — either proves the socket path was reached, NOT a Block).
    - The tainted-body leg records a `sink_blocked` (I2 Block) and NO `http_write_*` terminal event — the write is never attempted.
    - `verify_chain` is true for the session across each leg (genuine, unbroken audit DAG).
    - The write leg's audit payloads carry no url/body/credential material (opaque).
  </behavior>
  <action>Extend the test file with a dispatch-level differential (mirror the existing server.rs dispatch-arm tests over `UnixStream::pair` + `dispatch_request`). Drive the clean-body Allowed node through dispatch and assert a terminal `http_write_*` event is appended (the write path was REACHED — on a host without a live mock endpoint the terminal event is `http_write_failed`, which still proves the clean body was NOT Blocked and the egress dispatch ran; the actual clean-body DELIVERY to a mock endpoint composes in Phase 46 LIVE-06). Drive the tainted-body node through the SAME dispatch and assert a `sink_blocked` event and the ABSENCE of any `http_write_*` terminal event. Assert `verify_chain` true for both sessions and grep the persisted `http_write_*` payload for absence of the url/body/credential (opaque). Gate the socket-touching assertions `#[cfg(target_os = "linux")]` per the project's Linux-only pattern (CLAUDE.md); keep the pure decision assertions host-portable. Add a doc comment noting the full composed live mock-endpoint delivery + credential-absence-after-real-write assertions are Phase 46 (LIVE-06) — this test proves the differential at the decision + dispatch boundary.</action>
  <verify>
    <automated>cargo test -p brokerd --test s43_http_write_differential --no-fail-fast</automated>
  </verify>
  <done>The clean leg reaches egress dispatch (terminal http_write_* event); the tainted leg Blocks with no write attempted; verify_chain true; write payloads opaque; socket legs Linux-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → executor/broker | the acceptance test drives the real decision + dispatch path (no mocked executor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-12 | Repudiation | a block-everything I2 regression passes a "not blocked" test | high | mitigate | differential design: the clean leg MUST reach Allowed + egress dispatch AND the tainted leg MUST Block — a blanket block fails the clean leg, a blanket allow fails the tainted leg (§2.5, M4). |
| T-43-13 | Tampering | stapled taint passes the Block leg without a genuine DAG edge | high | mitigate | the tainted body is minted through the real broker mint path (genuine provenance); verify_chain asserted true; the Block anchor names the `body` arg. |
</threat_model>

<verification>
- `cargo test -p brokerd --test s43_http_write_differential --no-fail-fast` green (macOS host: decision-level legs).
- On real Linux via `bash scripts/mailpit-verify.sh` with `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test s43_http_write_differential'`: the Linux-gated dispatch legs compile + pass; capture the pre-pipe exit status (never assert on a piped exit 0 — [[verification-exit-code-through-pipe]]).
- `./scripts/check-invariants.sh` passes.
</verification>

<success_criteria>
HTTP-W-01 is proven differentially in-crate: taint is the sole variable between a Blocked and an Allowed write; the distinct id is CommitIrreversible (I0 draft-deny); an out-of-enum method Denies; the clean leg reaches egress and the tainted leg does not; verify_chain true; write audit opaque. Full composed live delivery deferred to Phase 46 (LIVE-06).
</success_criteria>

<output>
Create `.planning/phases/43-http-request-write-post-put-egress/43-04-SUMMARY.md` when done.
</output>
