---
phase: 43-http-request-write-post-put-egress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
  - crates/executor/src/lib.rs
  - crates/runtime-core/src/executor_decision.rs
  - crates/runtime-core/src/policy.rs
autonomous: true
requirements: [HTTP-W-01]

must_haves:
  truths:
    - "A `http.request.write` plan node is classified `CommitIrreversible`, so a draft / untrusted-seeded session I0-denies it (the MAJOR-1 I0-escape fix)."
    - "A tainted `body` on `http.request.write` deterministically Blocks under I2 (content-sensitive); a `url` Blocks (routing + content); an untainted-body node is not Blocked by these tables."
    - "The `method` arg is validated against the fixed {POST,PUT} enum; any other literal (garbage OR tainted) Denies fail-closed before the effect."
    - "`http.request.write` is on the `broker_default()` production policy allowlist, so a policy-permitted node reaches the unmodified I2 loop (never a PolicyDeny for the new sink under the default policy)."
  artifacts:
    - "crates/executor/src/sink_schema.rs — KNOWN_SINKS row for `http.request.write` with args {url, body, method}."
    - "crates/executor/src/sink_sensitivity.rs — explicit CommitIrreversible class row + routing/content/expected_role arms."
    - "crates/executor/src/lib.rs — method-enum fail-closed validation in submit_plan_node."
    - "crates/runtime-core/src/policy.rs — `http.request.write` in PRODUCTION_SINKS."
  key_links:
    - "sink id `http.request.write` (distinct from GET `http.request`) is the single key on which effect-class, allowlist selection, and I0 discipline all pivot."
    - "method-enum Deny must precede any host/verb routing decision (fail-closed §2.6)."
---

<objective>
Register `http.request.write` as a DISTINCT, CommitIrreversible sink in the executor TCB — the load-bearing I0-gate pin from DESIGN-v1.9-egress-policy §2.0 (`[rev: MAJOR-1]`) — with its arg schema, taint-sensitivity tables, method-enum validation, and production-policy allowlist entry. No broker network path yet; this plan makes the executor classify and gate the new write sink correctly.

Purpose: A WRITE POST/PUT must NOT inherit the GET sink's `Observe` class (which falls through to Allowed even in a draft session, letting a prompt-injected session exfiltrate through a POST body with no confirm). A distinct id classified `CommitIrreversible` gets the full irreversible discipline (I0 draft-deny + I2 collect-then-Block + confirm-releasable).

Output: The executor recognizes, schema-gates, taint-governs, and method-validates `http.request.write`; the default policy permits it.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The locked design context (READ §2.0/§2.2/§2.6 before editing):
@planning-docs/DESIGN-v1.9-egress-policy.md

# The files this plan edits — study the shipped GET `http.request` rows as the pattern to mirror-but-diverge-from:
@crates/executor/src/sink_schema.rs
@crates/executor/src/sink_sensitivity.rs
@crates/executor/src/lib.rs
@crates/runtime-core/src/executor_decision.rs
@crates/runtime-core/src/policy.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register http.request.write in KNOWN_SINKS (schema)</name>
  <files>crates/executor/src/sink_schema.rs</files>
  <behavior>
    - `schema_for("http.request.write")` returns `Some` (never UnknownSink at Step 0).
    - A node with exactly {url, body, method} validates Ok.
    - A missing `body` (or `url`, or `method`) → `MissingArg`.
    - An extra arg (e.g. `headers`) → `UnknownArg`; a duplicate → `DuplicateArg`.
    - The shipped GET row `http.request` (allowed/required {url}) is unchanged.
  </behavior>
  <action>Add a new `SinkSchema` entry to `KNOWN_SINKS` for sink id `http.request.write` with `allowed` AND `required` both exactly {"url","body","method"} — the exact-match, all-required shape (mirror the file.write / git.commit exact-match schema, NOT process.exec's optional-arg asymmetry). Per DESIGN §2.0/§2.6: this is a DISTINCT id from the GET `http.request` row (leave that row exactly as-is — GET stays single-arg {url}). Document in the row comment that `method` is a schema-validated {POST,PUT} enum whose VALUE is checked in submit_plan_node (this Step-0 gate enforces only the arg NAME set), that `body` is content-sensitive and `url` routing+content-sensitive (a tainted value Blocks downstream, it does NOT Deny here). Add the mirrored unit tests from the behavior block.</action>
  <verify>
    <automated>cargo test -p executor sink_schema:: --no-fail-fast</automated>
  </verify>
  <done>`http.request.write` is a registered exact-match {url,body,method} sink; GET `http.request` unchanged; new schema tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify http.request.write CommitIrreversible + sensitivity tables</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <behavior>
    - `sink_effect_class(http.request.write) == CommitIrreversible` via an EXPLICIT match row (not only the `_ =>` fail-closed default).
    - `is_routing_sensitive(http.request.write, "url") == true`.
    - `is_content_sensitive(http.request.write, "body") == true` AND `is_content_sensitive(http.request.write, "url") == true`.
    - `is_routing_sensitive(http.request.write, "body") == false` (body is payload, not routing — no over-widening).
    - `is_routing_sensitive/is_content_sensitive(http.request.write, "method") == false` (method is governed by the enum gate, not I2 sensitivity).
    - `expected_role(http.request.write, "url"|"body"|"method") == None` (unconstrained structural role gate — the Block comes from sensitivity+taint, mirroring http.request/github.pr).
  </behavior>
  <action>Add an EXPLICIT `"http.request.write" => EffectClass::CommitIrreversible` arm to `sink_effect_class` (per DESIGN §2.0 — an explicit arm so a future refactor that reorders the schema gate cannot silently relax it, exactly like the github.pr arm; it is ALSO redundantly covered by the `_ => CommitIrreversible` fail-closed default, but must be explicit). Add `HTTP_REQUEST_WRITE_ROUTING_SENSITIVE = &["url"]` and `HTTP_REQUEST_WRITE_CONTENT_SENSITIVE = &["url","body"]` consts and the matching `"http.request.write"` arms in `is_routing_sensitive` and `is_content_sensitive` (per DESIGN §2.2: body content-sensitive like an email/PR body; url routing + content-sensitive like the GET url). Add an `"http.request.write"` arm to `expected_role` returning `None` for url/body/method (reuse the http.request/git.commit rationale: no origin_role-producing mint site for a legit write, so `Some(..)` would fail-closed-Deny the legit flow; the Block comes from sensitivity+taint, not this gate — `None` here is NOT an I2 bypass). Add the mirrored unit tests from the behavior block, including a `http_request_write_is_commit_irreversible` test asserting the EXPLICIT arm (contrast with the GET `http.request` Observe row).</action>
  <verify>
    <automated>cargo test -p executor sink_sensitivity:: --no-fail-fast</automated>
  </verify>
  <done>http.request.write classes CommitIrreversible (explicit); url routing+content, body content, method unclassified; expected_role None; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: method-enum fail-closed gate + production-policy allowlist</name>
  <files>crates/executor/src/lib.rs, crates/runtime-core/src/executor_decision.rs, crates/runtime-core/src/policy.rs</files>
  <behavior>
    - submit_plan_node on `http.request.write` with a resolved `method` literal not exactly "POST" or "PUT" → `Denied` (fail-closed), including when the method value is tainted or garbage.
    - method exactly "POST" or "PUT" passes the gate; a tainted `body` alongside a valid method still Blocks (the method gate does not mask the body Block for the differential proof — validate ordering so a valid-method + clean-body node reaches Allowed, a valid-method + tainted-body node Blocks).
    - `broker_default()` permits `http.request.write` (permits_sink true); `PRODUCTION_SINKS` lists it.
  </behavior>
  <action>In `submit_plan_node` (crates/executor/src/lib.rs), add a fail-closed `method` enum validation for sink id `http.request.write`: resolve the `method` arg's literal from the value_store and require it EQUAL exactly "POST" or "PUT" (case-sensitive, trimmed-empty rejected) — any other literal Denies. Per DESIGN §2.6 the method must NEVER be a free/garbage/tainted literal; because the sink id is already distinct the method cannot steer host/class, so an exact-enum match is the sufficient gate. Place it so it runs before the node can reach `Allowed` and mirrors the Step-1c role-check discipline (immediate return on failure); do NOT make it mask a body Block for a VALID method (a valid method with a tainted body must still Block — keep the enum Deny scoped to invalid methods only). Add a fail-closed `DenyReason` for this (e.g. `DenyReason::InvalidMethod { sink: String, method: String }` in crates/runtime-core/src/executor_decision.rs) OR reuse an existing schema-class reason; if adding a variant, keep the exhaustive-match discipline (no wildcard) at every call site. In crates/runtime-core/src/policy.rs add "http.request.write" to the `PRODUCTION_SINKS` slice (so `broker_default()` and `allow_all()` both permit it and the seven-sink assertions become eight — update `broker_default_permits_the_seven_production_sinks_and_denies_unlisted` and any count-bound test to include the new sink). Add unit tests from the behavior block: invalid/tainted method Deny, valid method + clean body Allowed, valid method + tainted body Blocked, broker_default permits http.request.write.</action>
  <verify>
    <automated>cargo test -p executor --no-fail-fast && cargo test -p runtime-core policy:: --no-fail-fast</automated>
  </verify>
  <done>Invalid/tainted method Denies fail-closed; valid method + tainted body still Blocks; valid method + clean body Allowed; broker_default permits the new sink; all executor + policy tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → executor | plan nodes (sink id, arg names, resolved literals via broker-owned store) cross into the deterministic I2 executor. |
| policy source → executor | the per-session policy narrows which sinks/args are callable (pre-I2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Elevation | sink_effect_class classification of the WRITE id | critical | mitigate | EXPLICIT `http.request.write => CommitIrreversible` arm + `_ =>` fail-closed default so a draft/untrusted-seeded session I0-denies a POST (the MAJOR-1 I0-escape fix, §2.0). |
| T-43-02 | Information disclosure | tainted body routed into the POST payload | high | mitigate | `body` content-sensitive under I2 → collect-then-Block on any untrusted taint (§2.2), genuinely propagated, never stapled. |
| T-43-03 | Tampering | attacker-supplied `method` literal | high | mitigate | method validated to exact {POST,PUT} enum; garbage/tainted method Denies fail-closed before any routing (§2.6). |
| T-43-04 | Elevation | policy omits the new sink → PolicyDeny masks intended I2 path | low | accept | broker_default explicitly allowlists http.request.write; a future/unknown sink stays deny-by-default (documented). |
</threat_model>

<verification>
- `cargo build --workspace` compiles (macOS host).
- `cargo test -p executor --no-fail-fast` and `cargo test -p runtime-core --no-fail-fast` green.
- `./scripts/check-invariants.sh` passes (no new mint site, no raw effect-request-to-sink bypass introduced — this plan touches no sink dispatch).
</verification>

<success_criteria>
http.request.write is a registered, CommitIrreversible, taint-governed, method-enum-gated, policy-permitted sink in the executor TCB, with the GET `http.request` Observe path untouched. All new + existing executor/runtime-core tests green.
</success_criteria>

<output>
Create `.planning/phases/43-http-request-write-post-put-egress/43-01-SUMMARY.md` when done.
</output>
