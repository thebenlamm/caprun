---
phase: 38-github-pr-sink
plan: 05
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - crates/brokerd/src/confirmation.rs
autonomous: true
requirements: [GITHUB-02, GITHUB-03]
must_haves:
  truths:
    - "a tainted title/body github.pr Blocks (BlockedPendingConfirmation) and its verbatim, provenance-annotated title/body is shown to the human at confirm"
    - "confirm WITHOUT a live grant Denies (a bare confirm cannot create a PR) — the row does not burn its one-shot on the missing-grant path"
    - "confirm WITH a live grant + a valid precheck proceeds to dispatch (github.pr admitted at the Step-4.75 entry guard)"
    - "no dangling confirm_granted — every burned confirmation is followed by a terminal github_pr_* event (terminal EVENT before terminal STATE)"
  artifacts:
    - "crates/brokerd/src/confirmation.rs — Step-4.75 allow-list extension, prepare_github_pr precheck call, Step-7 github.pr dispatch arm, regression + block tests"
  key_links:
    - "confirm Step-4.75 guard admits github.pr -> grant gate + prepare_github_pr precheck (before Step 5 confirm_granted / Step 6 burn) -> Step-7 dispatch -> terminal event"
---

<objective>
Wire the `github.pr` confirm-release path in `confirmation.rs`: extend the
Step-4.75 entry-guard allow-list to admit `github.pr`, run the grant gate + the
`prepare_github_pr` precheck BEFORE `confirm_granted` is appended and the one-shot
is burned, add the Step-7 dispatch arm, and prove the recurring P33/P34 audit-gap
class is closed.

Purpose: a tainted `title`/`body` (the marquee exfil vector) Blocks and is
releasable ONLY by a single-shot human confirm (GITHUB-03), and even then a PR is
created only with a LIVE grant (GITHUB-02: a bare confirm cannot create a PR).
Every fallible pre-effect leg folds through the single terminal-event branch —
NEVER a burned `confirm_granted` dangling without a terminal `github_pr_*` event
(DESIGN §9; the exact class a passing verifier + green gates missed twice in v1.7).
Output: the confirmation.rs github.pr wiring + regression/block tests.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-github-pr-sink/38-CONTEXT.md
@planning-docs/DESIGN-git-github-http-sinks.md
@crates/brokerd/src/confirmation.rs
@crates/brokerd/src/sinks/github_pr.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Step-4.75 guard + prepare precheck + Step-7 github.pr dispatch</name>
  <files>crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs — Step 4.75 entry guard (the
      `match pc.sink.0.as_str() { "file.create" | "email.send" | "file.write" |
      "process.exec" => {} other => Err(...) }` allow-list, ~836-846); Step 4.8
      `prepare_process_exec` precheck call (~859-866); Step 5 (confirm_granted append),
      Step 6 (transition_state -> Confirmed burn), Step 7 dispatch arms (the
      file.write and process.exec arms are the closest templates); `ResolvedArg`;
      `render_block_display` (renders ALL args verbatim — this is what surfaces the
      verbatim title/body at confirm).
    - crates/brokerd/src/audit.rs — has_github_grant, github_pr_content_key,
      reserve_created_pr.
    - crates/brokerd/src/sinks/github_pr.rs — prepare_github_pr,
      invoke_github_pr_from_resolved.
  </read_first>
  <behavior>
    - github.pr passes the Step-4.75 guard (a confirm-releasable sink NOT on the
      list is denied there — fail-closed).
    - Missing grant at confirm: the row stays fail-closed and NO confirm_granted is
      burned without a terminal event (either refuse before Step 5, or fold into the
      single terminal-event branch — never a dangling burn).
    - A malformed/empty frozen arg fails at prepare_github_pr BEFORE Step 5 (row
      stays Pending, recoverable — mirror the process.exec malformed-args test).
    - On a valid grant + precheck: Step 7 CASes (reserve_created_pr) before invoking
      invoke_github_pr_from_resolved; a terminal github_pr_* event always follows
      confirm_granted.
  </behavior>
  <action>
    Per DESIGN §4.6/§9 (the entry-guard extension is REQUIRED, not optional — a
    confirm-releasable sink absent from the list is denied at the guard):
    (1) Add `"github.pr"` to the Step-4.75 entry-guard allow-list match arm.
    (2) GRANT GATE + PRECHECK before Step 5: in the same pre-`confirm_granted`
    region as the process.exec `prepare_process_exec` call, for `github.pr`:
    if `!has_github_grant(conn, pc.session_id)` return a fail-closed-RECOVERABLE Err
    (row remains Pending, NEVER a burned confirmation — a bare confirm cannot create
    a PR, GITHUB-02); then call `prepare_github_pr(&pc.resolved_args)` mapping its Err
    the way the process.exec precheck does (row stays Pending). Both run BEFORE Step 5
    appends confirm_granted and Step 6 burns the one-shot.
    (3) Add a Step-7 `"github.pr"` dispatch arm (mirror the process.exec arm's
    two-phase shape): reserve the created_prs CAS (github_pr_content_key over the six
    frozen resolved literals) BEFORE invoking; on a replay (not-fresh) treat as
    at-most-once-suppressed with a terminal event; on fresh, call
    `invoke_github_pr_from_resolved(...)` and map Ok -> Released, Err ->
    ConfirmedButSinkFailed (the sink already appended its own OPAQUE
    github_pr_failed terminal event FIRST — never a dangling confirm_granted). Do NOT
    add a clear-key-on-failure path. github.pr does NOT mint (keep Gate 3's mint-site
    allow-list byte-identical — no mint token in confirmation.rs).
    Cite DESIGN §4.3/§4.6/§9 and GITHUB-02/03 in the arm's doc comments.
  </action>
  <verify>
    <automated>cargo build -p brokerd && ./scripts/check-invariants.sh</automated>
  </verify>
  <done>github.pr is admitted at the guard; grant+precheck run before the burn; Step 7 CASes then dispatches; no mint token added; check-invariants green.</done>
  <acceptance_criteria>
    - The entry-guard allow-list includes github.pr (else it is denied at the guard).
    - The grant check + prepare precheck both run BEFORE confirm_granted / the burn.
    - Terminal EVENT precedes terminal STATE on every leg (P33/P34).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tainted-block, no-grant-Deny, grant-proceeds, no-dangling-confirm tests</name>
  <files>crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs tests — `confirm_on_process_exec_malformed_args_does_not_burn_confirmation`
      (~1965, the no-dangling-confirm_granted regression template: Err, row still
      Pending, NO confirm_granted event) and the render_block_display tests.
    - crates/brokerd/tests/s9_process_exec_block.rs — how a tainted arg is driven to
      BlockedPendingConfirmation and then confirm()'d end-to-end.
  </read_first>
  <behavior>
    - A github.pr whose title (or body) resolves from an untrusted-tainted value
      Blocks (BlockedPendingConfirmation), and render_block_display shows the title/body
      literal verbatim (GITHUB-03).
    - confirm() on that block with NO grant: returns a fail-closed outcome, the row
      is NOT burned (no confirm_granted with no terminal event), NO PR dispatch.
    - confirm() with a live grant: proceeds to the Step-7 dispatch; a terminal
      github_pr_* event follows confirm_granted (no dangling); on macOS the POST
      stubs Err so the terminal event is github_pr_failed — that still proves the
      no-dangling property. verify_chain true throughout.
  </behavior>
  <action>
    Add tests (co-located in confirmation.rs tests or a brokerd integration test,
    matching how the process.exec block/confirm tests are placed):
    - `github_pr_tainted_title_blocks_and_shows_verbatim`: build a github.pr plan
      node whose `title` (and separately `body`) resolves from an ExternalUntrusted/
      HttpRaw/doc_fragment-tainted value; assert submit_plan_node yields
      BlockedPendingConfirmation and render_block_display contains the verbatim title
      literal (GITHUB-03 — human sees exactly what would leave the boundary).
    - `github_pr_confirm_without_grant_does_not_burn`: block a tainted-title
      github.pr, call confirm() with NO grant recorded; assert a fail-closed outcome,
      the pending row is still Pending (or terminal WITH a terminal event — never a
      dangling confirm_granted), and NO github_pr_succeeded/attempted event exists
      (GITHUB-02 — bare confirm cannot create a PR).
    - `github_pr_confirm_with_grant_proceeds_no_dangling`: record a grant, set
      CAPRUN_GITHUB_TOKEN, block then confirm(); assert confirm_granted is present
      AND followed by a terminal github_pr_* event (github_pr_failed on the macOS
      stub is acceptable — it proves no dangling), the created_prs CAS row exists,
      and verify_chain is true (DESIGN §9 regression, mirroring the process.exec
      malformed-args no-burn discipline).
    Serialize on the shared env lock if setting CAPRUN_GITHUB_TOKEN process-wide.
  </action>
  <verify>
    <automated>cargo test -p brokerd github_pr_confirm</automated>
  </verify>
  <done>tainted title/body Blocks with verbatim display; confirm-without-grant does not create a PR and does not dangle; confirm-with-grant proceeds with a terminal event after confirm_granted; chain verifies.</done>
  <acceptance_criteria>
    - The no-dangling-confirm_granted regression test exists and passes (P33/P34 class closed).
    - The verbatim title/body is asserted present in the confirm display.
    - All tests pass host-portably on macOS (POST stubbed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tainted PlanNode arg -> I2 Block -> human confirm | a tainted title/body is releasable only by single-shot human confirm, and only with a live grant |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Information Disclosure | tainted PR title/body | critical | mitigate | I2 content-sensitive Block; verbatim provenance shown at confirm (GITHUB-03/§4.4) |
| T-38-04 | Elevation of Privilege | confirm-release credential use | critical | mitigate | grant gate before Step 5; missing grant -> fail-closed, no burn (§4.3) |
| T-38-12 | Repudiation | confirm-release audit gap | critical | mitigate | prepare_github_pr precheck + terminal-event-before-state; regression test for no dangling confirm_granted (§9, P33/P34) |
</threat_model>

<verification>
- `cargo test -p brokerd` green (guard/dispatch + all four github_pr confirm tests).
- `./scripts/check-invariants.sh` exits 0 — Gate 3 mint-site allow-list BYTE-IDENTICAL (no mint token in confirmation.rs), Gate 1 no EffectRequest.
- The regression test proves no dangling confirm_granted-without-terminal-event.
</verification>

<success_criteria>
A tainted title/body github.pr Blocks and shows verbatim at confirm; a PR is
created on confirm only with a live grant; every burned confirmation is followed by
a terminal github_pr_* event (P33/P34 closed).
</success_criteria>

<output>
Create `.planning/phases/38-github-pr-sink/38-05-SUMMARY.md` when done.
</output>
