---
phase: 18-trust-boundary-coherence-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-session-trust-coherence.md
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]

must_haves:
  truths:
    - "planning-docs/DESIGN-session-trust-coherence.md exists and names reject-a-2nd-connection-to-an-active-session as the CHOSEN fix, with shared coherent multi-connection state named and explicitly rejected (DESIGN-01)"
    - "The doc re-earns the replay risk as 'accepted' IN WRITING under the adaptive-planner threat model, bounds amplification to trusted/human-typed recipients, and adds NO new CAS this milestone (DESIGN-02)"
    - "The doc audits all three mint sites (mint_from_read, mint_from_intent, mint_from_derivation) and states the narrower claim: only ProvideIntent yields a TRUSTED handle from a supplied string (DESIGN-03)"
    - "The doc rules on the decision oracle — Allowed vs BlockedPendingConfirmation{anchors} plus literal_sha256 leak per-handle taint state — and decides whether Phase 20's planner connection sees full decisions or a reduced signal (DESIGN-04)"
    - "The doc specifies the per-verb capability split: a connection may hold NO mint verb (ProvideIntent/ReportClaims/ReportDerivedClaim), forward-looking for Phase 20 (DESIGN-05)"
    - "The doc re-confirms guard-(c) (CAPRUN_ENABLE_IPC_CREATE_SESSION) is not widened by the fix and re-states whether it should finally be compile-excluded (DESIGN-06)"
  artifacts:
    - planning-docs/DESIGN-session-trust-coherence.md
  key_links:
    - "The reject-2nd-connection fix is specified precisely enough against server.rs's actual accept loop (98-121) and per-connection locals (session_status:148, intent_provided/fd_requested:153-154) that Phase 19 can implement it without reintroducing the per-connection-state bug"
    - "The three-mint-site audit maps each claim to the real arm: mint_from_read (ReportClaims ~428), mint_from_intent (ProvideIntent ~949), mint_from_derivation (ReportDerivedClaim ~849), and names the tripwire that reopens T1"
---

<objective>
Author `planning-docs/DESIGN-session-trust-coherence.md` — the v1.4 Phase 0 design gate that must exist and clear a fresh adversarial review before any `server.rs` change. The doc resolves all six design questions (DESIGN-01..06) consistently with the three LOCKED milestone-scoping decisions:

1. Fix shape = reject a 2nd connection to an already-active session (the smaller hammer). Shared coherent multi-connection state is the REJECTED alternative — name the tradeoff, do not design it as the chosen approach.
2. Replay/MAJOR-2 = re-earn "accepted" in writing against the adaptive-planner threat model. Do NOT design a new CAS/idempotency mechanism.
3. T2 (slot-type binding) = deferred to v1.5, OUT OF SCOPE for this doc. Do NOT design slot-type binding enforcement here.

Purpose: This is a HARD GATE — mirrors the v1.0 Phase 2 / v1.2 Phase 8 / v1.3 Phase 12 design-gate discipline (a standalone reviewed DESIGN doc before any TCB code). No `server.rs` (or any TCB) code may be written for the trust-coherence fix until this doc clears the fresh adversarial panel in Plan 18-02.
Output: `planning-docs/DESIGN-session-trust-coherence.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/tmp/v1.4-SCOPE.md
@.planning/tmp/v1.4-REVIEW-RECONCILIATION.md
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-session-trust-state.md
@planning-docs/DESIGN-content-adapter-mediation.md
@crates/brokerd/src/server.rs
@crates/brokerd/tests/two_connection_intent_bypass.rs
@crates/runtime-core/src/executor_decision.rs
@cli/caprun/src/planner.rs
</context>

## Artifacts this phase produces

- `planning-docs/DESIGN-session-trust-coherence.md` (this plan authors it)
- `planning-docs/DESIGN-GATE-RECORD-v1.4.md` (authored by Plan 18-02, the adversarial gate record)

This plan (and this phase) touches ONLY `planning-docs/*.md`. It MUST NOT modify `crates/brokerd/src/server.rs`, `crates/brokerd/tests/two_connection_intent_bypass.rs`, or any other TCB/source file — that is Phase 19's job, gated on this doc clearing review.

<tasks>

<task type="auto">
  <name>Task 1: Author the structural-mechanism sections — fix shape (DESIGN-01), per-verb capability split (DESIGN-05), guard-(c) status (DESIGN-06)</name>
  <files>planning-docs/DESIGN-session-trust-coherence.md</files>
  <read_first>
    - `crates/brokerd/src/server.rs` — the bug lives in per-connection state: `run_broker_server` accept loop spawns a fresh `handle_connection` per connection (lines 98-121); `handle_connection` seeds `session_status` (line 148), `intent_provided`/`fd_requested` (lines 153-154) as per-connection locals, "never re-derived from IPC or the DB". guard(a) is at line 904 (`if *intent_provided || *fd_requested`). The `CreateSession` guard-(c) env-gate `CAPRUN_ENABLE_IPC_CREATE_SESSION` is at lines 267-283. Cite these EXACT line numbers in the doc.
    - `crates/brokerd/tests/two_connection_intent_bypass.rs` — the red-by-design regression the fix must turn green (DO NOT modify it; it belongs to Phase 19). Its Part B (lines 205-287) is the exact attack the fix must defeat: conn#1 RequestFd, then a FRESH conn#2 gets pristine guard(a) + stale-Active status and mints an attacker literal via ProvideIntent.
    - `.planning/tmp/v1.4-SCOPE.md` §"Phase 0" item 1 (authoritative trust state) and item 2 (per-verb capability split) and item 3 (guard-(c) interaction).
    - `.planning/REQUIREMENTS.md` "Out of Scope" table — shared coherent multi-connection state is REJECTED; guard-(c) compile-exclusion is re-confirmed-not-re-scoped (DESIGN-06 only re-states whether it should happen).
    - `planning-docs/DESIGN-session-trust-state.md` — the prior DESIGN doc this project format/rigor mirrors (numbered sections, an "Acceptance Predicate (Done When)", an "Accepted Residual Risks" section). Match its structure.
    - `planning-docs/PLAN.md` — source of truth; confirm terminology (Intent/Session/Broker/Worker/Adapter/Effect) and that a confined worker legitimately holds exactly ONE connection.
  </read_first>
  <action>
    Create `planning-docs/DESIGN-session-trust-coherence.md` with a title line and a short preamble stating: this is the v1.4 Phase 0 design gate; it resolves DESIGN-01..06; it is bound by three LOCKED scoping decisions (reject-2nd-connection, replay-re-earned-not-CAS, T2-deferred-not-designed-here); no `server.rs` change may precede this doc clearing a fresh adversarial panel.

    Then author these numbered sections (this task writes the structural-mechanism half; Task 2 appends the adversarial-ruling half):

    §1 "The bug, precisely" — describe the cross-connection ProvideIntent bypass as it exists in shipped code. Cite server.rs's per-connection `session_status` (line 148), `intent_provided`/`fd_requested` (lines 153-154), seeded fresh per connection by the accept loop (lines 98-121), guard(a) at line 904 being per-connection-local. State the falsified assumption: `UserTrusted` does NOT mean "the human typed it" across connections today. Reference the reproduced live exfil (the regression test) as the evidence anchor, and note reachability (netns not unshared; seccomp permits AF_UNIX; abstract UDS unaffected by Landlock).

    §2 "Fix shape: reject a 2nd connection to an already-active session (DESIGN-01)" — specify the CHOSEN fix: the broker refuses/closes any connection to a session that already has an established/active connection (single-connection-per-session enforcement). Specify WHERE this decision lives relative to server.rs's accept loop (lines 98-121) — an authoritative per-session "connection already established" gate resolved BEFORE `handle_connection` seeds its per-connection locals, so a 2nd connection never gets a pristine guard(a) or a stale-Active status. Name the tradeoff explicitly: the confined worker legitimately holds exactly one connection, so N>1 has no legitimate use in v0. Then name the REJECTED alternative — "shared coherent multi-connection state" (one authoritative per-session store shared across connections) — and state in writing WHY it was rejected (larger blast radius, more TCB surface, no legitimate N>1 need). This section MUST make the reject-2nd-connection choice unambiguous and MUST NOT design shared multi-connection state as the chosen approach. Specify the fix precisely enough (which state is authoritative, where the gate sits, what response a rejected 2nd connection receives) that Phase 19 can implement it in server.rs without reintroducing per-connection trust locals.

    §3 "Per-verb capability split (DESIGN-05)" — specify, forward-looking for Phase 20's planner connection, that a connection must be able to hold NO mint verb: `ProvideIntent`, `ReportClaims`, `ReportDerivedClaim` all unavailable on a capability-restricted connection. Describe the shape of the capability model (a per-connection capability set decided at connection-establishment, checked at each mint-verb dispatch arm, fail-closed default-deny), without writing the Rust. State that Phase 20 (PLANNER-02) will rely on exactly this: the planner connection holds no mint verb. Design it now even though the Phase-0 fix (reject-2nd-connection) does not itself require it.

    §4 "guard-(c) interaction (DESIGN-06)" — re-confirm that the reject-2nd-connection fix does NOT widen `CAPRUN_ENABLE_IPC_CREATE_SESSION` (the guard-(c) env-gate at server.rs 267-283 stays exactly-"1"-opt-in, fail-closed default-deny). Re-state whether guard-(c) should FINALLY be compile-excluded — present the case for/against and give a recommendation — but do NOT commit v1.4 to doing the compile-exclusion (it is re-confirmed, not re-scoped, per REQUIREMENTS.md Out of Scope).

    Write directive, code-tracing prose. Cite exact file:line for every claim about current behavior. Do NOT include fenced code blocks that reproduce implementation; name identifiers, arms, and line numbers instead.
  </action>
  <acceptance_criteria>
    - `planning-docs/DESIGN-session-trust-coherence.md` exists.
    - §2 names reject-a-2nd-connection as the CHOSEN fix AND names "shared coherent multi-connection state" (or equivalent) as the REJECTED alternative with a written rejection rationale — the doc does NOT present shared multi-connection state as the chosen approach.
    - §2 cites server.rs's actual accept loop and per-connection locals by line number (98-121, 148, 153-154) so Phase 19 has an unambiguous implementation target.
    - §3 states a connection may hold NO mint verb and names all three mint verbs: ProvideIntent, ReportClaims, ReportDerivedClaim.
    - §4 re-confirms guard-(c) is not widened and re-states the compile-exclusion question without committing v1.4 to performing it.
    - No section modifies, or instructs modifying, `server.rs` or any source file — the doc is design only.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-session-trust-coherence.md && grep -qi "reject" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "second connection\|2nd connection" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "shared coherent multi-connection\|shared.*multi-connection state" planning-docs/DESIGN-session-trust-coherence.md && grep -q "ProvideIntent" planning-docs/DESIGN-session-trust-coherence.md && grep -q "ReportClaims" planning-docs/DESIGN-session-trust-coherence.md && grep -q "ReportDerivedClaim" planning-docs/DESIGN-session-trust-coherence.md && grep -q "CAPRUN_ENABLE_IPC_CREATE_SESSION" planning-docs/DESIGN-session-trust-coherence.md && echo PASS</automated>
  </verify>
  <done>The DESIGN doc exists with §1-§4 covering the bug, the reject-2nd-connection fix shape (DESIGN-01, chosen over the named+rejected shared-state alternative), the per-verb capability split (DESIGN-05), and guard-(c) status (DESIGN-06), each cited against real server.rs line numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Author the adversarial-ruling sections — three-mint-site audit (DESIGN-03), replay re-earned (DESIGN-02), decision oracle (DESIGN-04)</name>
  <files>planning-docs/DESIGN-session-trust-coherence.md</files>
  <read_first>
    - `crates/brokerd/src/server.rs` — the three mint sites in their arms: `mint_from_read` in the ReportClaims arm (~lines 393-479, mint call ~428), `mint_from_intent` in the ProvideIntent arm (~lines 894-1011, mint calls ~949/962/979), `mint_from_derivation` in the ReportDerivedClaim arm (~lines 795-892, mint call ~849). Also the Allowed `email.send` dispatch with its named REPLAY RESIDUAL RISK (~lines 705-788, esp. the comment at ~771-776).
    - `crates/runtime-core/src/executor_decision.rs` — `ExecutorDecision::Allowed` vs `BlockedPendingConfirmation { anchors: Vec<BlockedArg> }` (lines 185-202); `SinkBlockedAnchor.literal_sha256` (line 144) and its documented meaning (lines 138-143); `BlockedArg.literal` in-memory (lines 170-181).
    - `.planning/tmp/v1.4-REVIEW-RECONCILIATION.md` — MAJOR-2 (replay honesty), MAJOR-3 (three-mint-site audit + the correct narrower claim + the reopening tripwire), MEDIUM-1 (the decision oracle + literal_sha256 offline-guess confirmer). These are the exact findings this task rules on, with file:line evidence.
    - `.planning/tmp/v1.4-SCOPE.md` §"Design-gate inputs carried from the reviews" — MAJOR-2, MAJOR-3, MEDIUM-1 rulings required.
    - `.planning/REQUIREMENTS.md` "Out of Scope" table — no new CAS this milestone (replay is re-earned in writing, not mechanized); T2 slot-type binding deferred to v1.5 (do NOT design it here).
  </read_first>
  <action>
    Append the adversarial-ruling sections to `planning-docs/DESIGN-session-trust-coherence.md` (continue the numbered-section scheme from Task 1):

    §5 "Three-mint-site audit (DESIGN-03)" — audit all THREE mint sites and, for each, state whether it can yield a TRUSTED handle from a supplied string:
      - `mint_from_read` (ReportClaims arm ~428): safe ONLY via the sensitivity map — stamps `ExternalUntrusted` which Blocks at routing-sensitive sinks. Not a trusted-from-supplied-string mint.
      - `mint_from_intent` (ProvideIntent arm ~949): the T1 hole — mints `UserTrusted` from a worker-supplied IPC literal. THIS is the one site that yields a TRUSTED handle from a supplied string.
      - `mint_from_derivation` (ReportDerivedClaim arm ~849): safe via byte-verify + every-input-file_read-rooted guards. Not a trusted-from-supplied-string mint.
      State the corrected NARROWER claim verbatim in the doc: "the only mint yielding a TRUSTED handle from a supplied string is ProvideIntent." Then state the REOPENING TRIPWIRE explicitly: this reduction holds only while every sensitive sink's sensitivity map treats `WorkerExtracted`/`ExternalUntrusted` as blocking on the relevant arg — it reopens the instant any sink's map treats them as non-blocking on some arg. Name that as a standing invariant future changes must preserve.

    §6 "Replay risk re-earned under the adaptive-planner threat model (DESIGN-02)" — rule on MAJOR-2. Re-earn "accepted" IN WRITING (do NOT silently inherit the v1.3 acceptance; do NOT add a CAS this milestone). State the new actor: under an adaptive planner (Phase 21) the replay actor collapses from "external" to "the milestone's own named adversary." Analyze the blast radius: the Allowed `email.send` path has no CAS, so a replayed SubmitPlanNode mints a fresh effect_id and sends again (N submissions = N emails). Show the amplification is BOUNDED — an untrusted/tainted recipient still Blocks (I2 unchanged), so replay amplification is confined to trusted/human-typed recipients = DoS/duplication, NOT new exfiltration. Point to the existing durable per-attempt `email_send_attempted` ledger (server.rs ~750-768) that makes each send auditable. Conclude with an explicit written re-acceptance for v1.4 and a named v2 obligation (minimal CAS) — using the "Accepted Residual Risk" framing this project uses. Use the words "re-earn"/"re-earned" so the ruling is unambiguous.

    §7 "The decision is an oracle (DESIGN-04 / MEDIUM-1)" — document the oracle explicitly: `Allowed` vs `BlockedPendingConfirmation{anchors}` leaks per-handle taint state to whoever sees the decision, and each blocked anchor's `literal_sha256` (executor_decision.rs:144) is an offline literal-guess CONFIRMER (a guesser who knows the digest can brute/confirm a suspected literal offline). RULE on the forward-looking question: does Phase 20's planner connection see the FULL `Allowed`/`BlockedPendingConfirmation{anchors, literal_sha256}` decision, or a REDUCED signal? Give the decision and its rationale (e.g., the planner connection should receive a reduced allow/block signal without anchors/literal_sha256, since it needs only enough to proceed or stop, and full anchors hand it an oracle). Tie the ruling back to §3's capability model.

    §8 "Acceptance Predicate (Done When)" — a checklist of what must be TRUE for this DESIGN doc to be complete, one line per DESIGN-01..06, phrased so Plan 18-02's adversarial reviewer can check each against the doc AND the cited code.

    §9 "Accepted Residual Risks" — collect the residuals named above: replay-without-CAS (re-earned, v2 obligation), the three-mint-site tripwire, the oracle for any connection that DOES see full decisions, and an explicit note that T2 (slot-type binding) is OUT OF SCOPE here — documented as deferred to v1.5, NOT designed in this doc.

    Cite exact file:line for every current-behavior claim. Do NOT design a CAS mechanism. Do NOT design slot-type binding. No fenced implementation code — name identifiers and line numbers.
  </action>
  <acceptance_criteria>
    - §5 names all three mint sites (mint_from_read, mint_from_intent, mint_from_derivation), states the narrower claim ("only ProvideIntent yields a TRUSTED handle from a supplied string" or equivalent verbatim), and names the reopening tripwire tied to the sink sensitivity map.
    - §6 contains the word "re-earn" or "re-earned", bounds replay amplification to trusted/human-typed recipients (untrusted still Blocks), and adds NO new CAS design (names CAS only as a deferred v2 obligation).
    - §7 documents the Allowed-vs-Blocked oracle AND the literal_sha256 offline-guess confirmer (cites executor_decision.rs:144), and gives an explicit ruling on whether Phase 20's planner connection sees full decisions or a reduced signal.
    - §9 explicitly records T2 (slot-type binding) as OUT OF SCOPE / deferred to v1.5 and NOT designed in this doc.
    - No section designs a CAS/idempotency mechanism and no section designs slot-type binding enforcement.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "mint_from_read" planning-docs/DESIGN-session-trust-coherence.md && grep -q "mint_from_intent" planning-docs/DESIGN-session-trust-coherence.md && grep -q "mint_from_derivation" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "re-earn" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "literal_sha256" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "oracle" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "residual" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "v1.5\|deferred" planning-docs/DESIGN-session-trust-coherence.md && echo PASS</automated>
  </verify>
  <done>The DESIGN doc's §5-§9 audit all three mint sites with the narrower claim + tripwire (DESIGN-03), re-earn the replay risk in writing with no new CAS (DESIGN-02), rule on the decision oracle including the literal_sha256 leak (DESIGN-04), and carry an Acceptance Predicate + Accepted Residual Risks (with T2 explicitly deferred, not designed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces ONLY markdown design documentation — it introduces no runtime code and crosses no live trust boundary itself. The relevant boundary is downstream: the DESIGN doc is the specification Phase 19 implements into the TCB (`server.rs`). An under-specified or wrong ruling here becomes a TCB defect there.

| Boundary | Description |
|----------|-------------|
| DESIGN doc → Phase 19 server.rs implementation | An ambiguous/incorrect fix spec propagates into a TCB change; the doc's precision IS the mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | DESIGN doc under-specifies reject-2nd-connection such that Phase 19 reintroduces the per-connection-state bug | high | mitigate | §2 cites exact server.rs line numbers (98-121, 148, 153-154) and names where the authoritative gate sits; the fresh adversarial gate (Plan 18-02) traces code to confirm implementability |
| T-18-02 | Information disclosure | DESIGN doc silently inherits the v1.3 replay acceptance instead of re-earning it, hiding an amplification path under the new adversary | medium | mitigate | §6 re-earns in writing, bounds amplification to trusted recipients, names the v2 CAS obligation; reviewer checks for the "re-earned" ruling |
| T-18-03 | Elevation of privilege | DESIGN doc's mint-site reduction is overclaimed, missing a site that can mint TRUSTED from a supplied string | high | mitigate | §5 audits all three sites against their actual arms and states the narrower claim + reopening tripwire; reviewer re-traces each site |

No package-manager installs occur in this plan (docs only) — the T-{phase}-SC supply-chain checkpoint is not applicable.
</threat_model>

<verification>
- `planning-docs/DESIGN-session-trust-coherence.md` exists and resolves DESIGN-01..06 across §1-§9.
- Both automated grep gates (Task 1, Task 2) return PASS.
- The doc cites real server.rs / executor_decision.rs line numbers for every current-behavior claim.
- No source/TCB file is modified: `git status --porcelain crates/ cli/` shows no changes attributable to this plan.
</verification>

<success_criteria>
The DESIGN doc is authored and internally complete: it names reject-2nd-connection as the chosen fix (with shared-state rejected), re-earns replay in writing (no new CAS), audits all three mint sites with the narrower claim + tripwire, rules on the decision oracle, specifies the per-verb capability split, and re-confirms guard-(c) — all consistent with the three locked scoping decisions, ready for the fresh adversarial gate in Plan 18-02.
</success_criteria>

<output>
Create `.planning/phases/18-trust-boundary-coherence-design-gate/18-01-SUMMARY.md` when done.
</output>
