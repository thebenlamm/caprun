---
phase: 35-design-gate-fresh-adversarial-code-trace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-git-github-http-sinks.md
autonomous: true
requirements: [DESIGN-15]

must_haves:
  truths:
    - "planning-docs/DESIGN-git-github-http-sinks.md exists and pins, per sink, the dispatch pattern (A in-broker egress / B broker-spawned confined child), the effect-class (git.commit=MutateReversible, git.push=CommitIrreversible, github.pr=CommitIrreversible, http.request GET=Observe), the sink args + which are I2-sensitive, the taint flow, and the confinement."
    - "The doc pins the one genuinely NEW mechanism: mint_from_http minting the inbound HTTP response body untrusted-on-arrival, rooted on a real http_response_received Event, and demoting the session to draft-only (I1); plus a new TaintLabel::HttpRaw variant compile-forced into the exhaustive is_untrusted() match."
    - "The doc pins the git config/hook neutralization surface (GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, no aliases, GIT_TERMINAL_PROMPT=0, env_clear()'d child), git.push destination-pinning (remote+branch from trusted intent, never repo .git/config) + the credential-injection mechanism, the SSRF resolve-and-pin model, the github.pr session-scoped human auth-grant model, the duplicate-PR content-derived CAS, and the env_clear() webpki-roots TLS-cert allowlist policy."
    - "The doc closes all 11 design-gate-blocking pitfalls, each with a NAMED mechanism, and states the three grey-area FORK decisions (git.push=net-allowed confined child; crypto=lean ring; github.pr auth-grant=session-scoped capability grant) with rationale."
    - "The doc has an explicit invariant-preservation section: does not weaken I0/I1/I2, introduces NO new raw effect-request-to-sink path (the plan-node path is preserved; check-invariants Gate 1 stays green), and sink sensitivity stays hardcoded in the executor (table rows only, never a swappable policy file)."
    - "The doc mandates the confirm-release terminal-event-before-terminal-state discipline (with a prepare_* precheck) for git.push and github.pr — the recurring P33/P34 audit-gap class — and mandates extending check-invariants.sh Gate 3 for the new mint_from_http( call-site; check-invariants.sh exits 0 and git status --porcelain crates/ cli/ is empty."
  artifacts:
    - planning-docs/DESIGN-git-github-http-sinks.md
  key_links:
    - "Inbound HTTP taint is minted ONLY inside the new mint_from_http helper (quarantine.rs locus, mirroring mint_from_read/mint_from_exec), rooted at a new http_response_received Event — provenance_chain[0] equals that Event id, non-stapled; a fetched value routed into a sensitive sink arg Blocks on a genuine DAG edge (§9 anti-staple standard)."
    - "All four sinks are PlanNode{sink, args} from spawn and route through the UNMODIFIED submit_plan_node collect-then-Block loop — the only executor changes are table rows (KNOWN_SINKS, sink_effect_class, is_routing_sensitive/is_content_sensitive, expected_role); no new I2 logic, no raw request-args-to-sink path."
    - "Every broker-held secret (github.pr bearer token; git.push credential) lives in broker-local env only and is never a ValueNode, plan-node arg, audit-DAG literal, the confined worker, or the planner sidecar — the push credential is injected short-lived to the net-allowed confined child only, env_clear()'d otherwise."
---

<objective>
Author `planning-docs/DESIGN-git-github-http-sinks.md` — the v1.8 design-gate doc (DESIGN-15) that pins the mechanism + fail-closed default for all four new external-effect sinks (`git.commit`, `git.push`, `github.pr`, read-only `http.request` GET) BEFORE any TCB code is written. The doc transcribes and elaborates the AUTHORITATIVE decisions in `35-CONTEXT.md` — it does NOT re-open them — pins each sink onto one of caprun's two shipped dispatch patterns, closes all 11 design-gate-blocking pitfalls with a named mechanism each, and proves the design weakens no invariant.

Purpose: This doc HARD-BLOCKS Phases 36-40. Per CLAUDE.md and the unbroken v1.0–v1.7 precedent (v1.0 P2, v1.2 P8, v1.3 P12, v1.4 P18, v1.5 P23, v1.6 P26, v1.7 P31), NO `crates/executor` / `crates/brokerd` / `crates/sandbox` / `crates/runtime-core` code may be written until this doc clears a fresh non-self adversarial code-trace (done in Plan 35-02). `git.push`'s network-from-a-confined-child path is the riskiest new surface to date — the design must be pinned precisely enough that the fresh reviewer can trace every claim against real code.

Output: `planning-docs/DESIGN-git-github-http-sinks.md` — a §-numbered doc: §0 Purpose/Scope; §1 git.commit; §2 git.push (FORK 1); §3 http.request GET + mint_from_http; §4 github.pr (FORK 3) + duplicate-PR CAS; §5 crypto provider (FORK 2) + env_clear TLS policy; §6 threat model (11 pitfalls → named mechanism); §7 invariant preservation; §8 fail-closed defaults table; §9 confirm-release audit-gap discipline; §10 new-mechanism symbol summary + Gate 3 mandate; §11 open items & accepted residual risks; §12 acceptance predicate.

**This is a DOC-ONLY phase. Write NO code under `crates/` or `cli/`. The git diff for this plan must touch only `planning-docs/`.**
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-effect-breadth-exec.md
@planning-docs/DESIGN-security-hardening.md
@planning-docs/DESIGN-slot-type-binding.md
@planning-docs/DESIGN-taint-model.md
</context>

<artifacts_this_phase_produces>
This plan (35-01) produces exactly one file:

**`planning-docs/DESIGN-git-github-http-sinks.md`** — the reviewed design contract for all four v1.8 sinks. Every mechanism, effect-class, and fail-closed default enumerated in the Phase 35 success criteria and the `35-CONTEXT.md` decisions must be pinned; all 11 design-gate-blocking pitfalls must each be closed with a NAMED mechanism; the three forks must be stated with rationale; and an explicit invariant-preservation section must show the design weakens neither I0/I1/I2 nor the no-raw-effect-request-path constraint.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write §0-§2 — Purpose/Scope, git.commit (Pattern B), and git.push (Pattern B net-allowed child, FORK 1)</name>
  <read_first>
    - .planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md — the AUTHORITATIVE decisions (dispatch patterns, effect classes, FORK 1 git.push net path, FORK 2 crypto, FORK 3 auth-grant, the 11 pitfall closures, the P33/P34 confirm-release discipline, the env_clear TLS policy). Transcribe + elaborate; do NOT re-open.
    - .planning/research/ARCHITECTURE.md + .planning/research/PITFALLS.md — Pattern B (broker-spawned confined child), the git config/hook RCE (P2), swapped-remote push (P3), --force/destructive-refspec (P4), credential-injection (P5), net-deny-widening (P12) pitfalls, and the build-order rationale.
    - planning-docs/DESIGN-effect-breadth-exec.md — MODEL the §-numbered section shape, depth, fail-closed-table convention, and "Accepted Residual Risks" convention on this (the immediately-prior v1.7 design gate for the SAME exec-launcher pattern git.commit/push reuse).
    - CLAUDE.md — I0/I1/I2, locked terminology (Intent/Session/Planner/Worker/Broker/Adapter/Effect/Artifact/Event), the "effect path is locked / no raw effect-request-to-sink" constraint, TCB-is-Rust, the "design-gate docs block executor code" rule.
    - Code to trace and cite precisely (file:line): crates/brokerd/src/sinks/process_exec.rs (Pattern B exemplar — broker-spawns the launcher, self-confine-before-execve, captures stdout/stderr), cli/caprun-exec-launcher/src/ (the confined-child launcher git.commit/push reuse verbatim), crates/brokerd/src/quarantine.rs:838 (mint_from_exec — git output reuses the exec-output taint label), crates/runtime-core/src/effect.rs:27 (the existing GitPush { remote, branch } effect variant — verify + reuse) and the three-variant Effect enum (Observe/MutateReversible/CommitIrreversible, effect.rs:36), crates/brokerd/src/confirmation.rs:735 (the confirm() path + terminal-state discipline for git.push confirm-release).
  </read_first>
  <files>planning-docs/DESIGN-git-github-http-sinks.md</files>
  <action>
    Create the doc with a title header and §0-§2.

    §0 Purpose & Scope: state this doc pins DESIGN-15 (mechanism + fail-closed default for all four sinks) and hard-blocks Phases 36-40 until the DESIGN-16 fresh non-self adversarial code-trace clears it (Plan 35-02). State what is IN scope (git.commit, git.push, github.pr, read-only http.request GET) and what is explicitly DEFERRED to v1.9+ per 35-CONTEXT (http write egress/POST, PR merge/comment, real LLM planner loop, declarative policy file, git2/gix, octocrab). Restate the locked terminology and the "no crates/ or cli/ code this phase" gate. State the two shipped dispatch patterns the whole design rests on: Pattern A = in-broker/broker-helper network egress (exemplar email_smtp.rs); Pattern B = broker-spawned confined child (exemplar caprun-exec-launcher + process_exec.rs); and that NO third pattern and NO raw effect-request-to-sink path is introduced.

    §1 git.commit (Pattern B): Pin dispatch = Pattern B (broker spawns the v1.7 caprun-exec-launcher, which self-confines with Landlock+seccomp+rlimits+timeout BEFORE execve'ing the system `git` binary — reuse process_exec.rs verbatim; cite it). Pin effect-class = MutateReversible — a DELIBERATE, explicitly-justified exception to the fail-closed `unknown → CommitIrreversible` default (a local commit is reversible; only push/PR are external), so it SURVIVES an I1-demoted draft-only session; cite effect.rs's three-variant Effect enum. Pin the sink args and which are I2-sensitive: the commit message is content-sensitive (reuse CONTENT-01) and is the taint CARRIER that must genuinely propagate downstream, NEVER re-minted clean — a tainted message Blocks under the collect-then-Block loop. Pin taint flow: git output reuses the existing exec-output taint label via mint_from_exec (git IS an exec under Pattern B — cite quarantine.rs:838); no new mint site for git. Pin confinement / git config/hook neutralization (closes P2): the launcher hardcodes GIT_CONFIG_NOSYSTEM=1, GIT_CONFIG_GLOBAL=/dev/null, -c core.hooksPath=/dev/null, no aliases, GIT_TERMINAL_PROMPT=0, and env_clear()'s the child so a planted malicious .git/config or hook in the workspace repo does NOT execute; Landlock confined to the workspace repo; seccomp network-deny UNCHANGED (a commit needs no net). State git ≥2.30 as the assumed host binary floor.

    §2 git.push (Pattern B, net-allowed confined child — FORK 1): State FORK 1 DECIDED = net-allowed confined child (Pattern B extended), with the rationale from 35-CONTEXT verbatim-in-substance (keep the broker small; a push credential + network egress live inside a short-lived kernel-confined child, not the long-lived reference monitor; the in-broker `git push` alternative is REJECTED because it widens the reference monitor). Pin effect-class = CommitIrreversible. Pin the net relaxation precisely (closes P12 net-deny-widening): the confined WORKER never gains net; only the git.push child gets a MINIMAL seccomp relaxation permitting ONLY the socket syscalls needed to reach the ONE pinned remote host:port — NO arbitrary egress; Landlock confined to the workspace repo. Pin destination pinning (closes P3 swapped-remote): remote URL + branch are captured from the TRUSTED intent at session creation and passed EXPLICITLY to the child — NEVER resolved from the untrusted repo's .git/config; remote + refspec are I2-gated sink args. Pin --force/destructive-refspec hard-denial (closes P4): no --force / --force-with-lease, no `:refspec` deletion, no `+` force-refspec — hard-denied regardless of confirmation. Pin credential injection (closes P5): the push credential lives in broker-local env only, is injected short-lived to the child via a GIT_ASKPASS/env visible ONLY to that child and env_clear()'d otherwise, and is never a ValueNode/plan-arg/audit-literal/worker/planner-env. Pin the confirm-release: a tainted push remote/refspec Blocks at the sink (I2) and is releasable only by single-shot human confirmation, with the terminal audit event written BEFORE the terminal state via a prepare_git_push precheck (§9 cross-reference; the recurring P33/P34 class). Flag the net-relaxation explicitly as the TOP item for the adversarial review to pressure-test.

    Do NOT paste fenced code blocks that reproduce a token a check-invariants gate negative-matches under crates/; cite code by file:line and named identifier in prose. Refer to the locked no-raw-effect-request-to-sink constraint by concept (the plan-node path is preserved), matching the DESIGN-effect-breadth-exec.md convention.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-git-github-http-sinks.md && grep -q 'GIT_CONFIG_NOSYSTEM' planning-docs/DESIGN-git-github-http-sinks.md && grep -q 'MutateReversible' planning-docs/DESIGN-git-github-http-sinks.md && grep -qi 'net-allowed confined child' planning-docs/DESIGN-git-github-http-sinks.md</automated>
  </verify>
  <acceptance_criteria>
    - planning-docs/DESIGN-git-github-http-sinks.md exists with §0 (Purpose/Scope), §1 (git.commit), and §2 (git.push) sections.
    - §1 pins Pattern B dispatch (exec-launcher reuse), MutateReversible with the reversibility justification, the commit message as the content-sensitive taint carrier propagated via mint_from_exec (no new mint site), and the git config/hook neutralization surface (GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, no aliases, GIT_TERMINAL_PROMPT=0, env_clear()'d).
    - §2 states FORK 1 = net-allowed confined child with rationale, pins CommitIrreversible, the single-pinned-host:port minimal seccomp net relaxation (worker never gains net), intent-origin destination pinning (never repo .git/config), --force/ref-deletion hard-denial, short-lived broker-only credential injection to the child, and the prepare_git_push confirm-release cross-reference.
    - No file under crates/ or cli/ is modified (git status --porcelain crates/ cli/ is empty).
  </acceptance_criteria>
  <done>§0-§2 exist and pin the scope + both git sinks (git.commit MutateReversible; git.push FORK-1 net-allowed confined child) with concrete cited code references and every P2/P3/P4/P5/P12 mechanism named; git diff touches only planning-docs/.</done>
</task>

<task type="auto">
  <name>Task 2: Write §3-§5 — http.request GET + mint_from_http (Pattern A), github.pr + auth-grant + CAS (Pattern A, FORK 3), crypto provider (FORK 2) + env_clear TLS policy</name>
  <read_first>
    - .planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md — the mint_from_http + TaintLabel::HttpRaw + session-demotion decision, FORK 3 (session-scoped auth-grant), FORK 2 (crypto = lean ring), the duplicate-PR CAS, the env_clear TLS policy, the SSRF pitfall closure (P7/P10) and credential/token-scoping closures (P5/P6/P8).
    - .planning/research/ARCHITECTURE.md + .planning/research/PITFALLS.md + .planning/research/STACK.md — Pattern A (in-broker egress), the mint_from_http inbound-mint direction, SSRF (resolve-and-pin), tainted-PR-body exfil (the marquee P6), replay/duplicate-PR CAS (P16, mirrors v1.6 HARDEN-03), and the reqwest=0.13.4 (rustls) / webpki-roots 1.0.8 pins.
    - planning-docs/DESIGN-security-hardening.md — the v1.6 HARDEN-03 content-derived idempotency-key CAS convention; MODEL the duplicate-PR CAS on it.
    - Code to trace and cite (file:line): crates/brokerd/src/sinks/email_smtp.rs (Pattern A exemplar — in-broker network egress, broker-only secret sourcing "D-04"), crates/brokerd/src/quarantine.rs:301 (mint_from_read template that mint_from_http mirrors) and :838 (mint_from_exec, the fresh-rooted-Event precedent), crates/runtime-core/src/plan_node.rs:13 (the TaintLabel enum + :45 is_untrusted() exhaustive match that HttpRaw must be compile-forced into, mirroring ExecRaw at :28/:53), crates/executor/src/sink_sensitivity.rs:140 (is_content_sensitive / CONTENT-01) + :188 (expected_role), cli/caprun/src/main.rs:314-331 (the planner-sidecar spawn NOT env_clear'd — the ENV-01 gap realized in Phase 40).
  </read_first>
  <files>planning-docs/DESIGN-git-github-http-sinks.md</files>
  <action>
    Append §3-§5 to the doc.

    §3 http.request GET (Pattern A) + mint_from_http: Pin dispatch = Pattern A (HTTP client runs in the broker/broker-helper; reqwest=0.13.4 rustls, NOT octocrab; cite email_smtp.rs as the in-broker-egress exemplar). Pin effect-class = Observe (a GET is allowed even in a demoted session), BUT its response demotes the session. Pin the sink arg: `url` is I2-gated (routing-sensitive). Pin the ONE genuinely NEW mechanism — mint_from_http: a new mint site in the sanctioned quarantine.rs locus (mirroring mint_from_read/mint_from_exec — cite :301/:838) that appends a real http_response_received Event FIRST, then mints the inbound response body as untrusted-on-arrival with provenance_chain = [that Event's id] (non-stapled), and MUST demote the session to draft-only (I1) — decided YES. Pin a new TaintLabel::HttpRaw variant compile-forced into the exhaustive is_untrusted() match (mirror ExecRaw — cite plan_node.rs:28/:53; adding the variant is a compile error until the match arm is added). Pin the anti-staple discipline (closes P11 not-minted + P19 stapled-taint): taint is genuinely propagated through the DAG (a fetched value routed into a sensitive sink arg Blocks on a real edge), never stapled at the sink — an anti-staple test is REQUIRED (§9 genuineness standard). Pin the SSRF resolve-and-pin model (closes P7/P10): resolve the host, PIN the destination IP; deny loopback/RFC1918/link-local/CGNAT/cloud-metadata (169.254.169.254)/ULA/IPv6-mapped equivalents; NO redirect following by default; reject userinfo@, non-https schemes, and IP-encoding tricks; host allowlist; connect to the pinned IP with SNI/Host = original host.

    §4 github.pr (Pattern A, FORK 3) + duplicate-PR CAS: Pin dispatch = Pattern A (broker-held bearer token via reqwest; one REST POST). Pin effect-class = CommitIrreversible. Pin credential hygiene (closes P5/P8): the bearer token is read from broker-local env ONLY — never a ValueNode, plan-node arg, audit-DAG literal, the confined worker, or the planner sidecar. Pin FORK 3 DECIDED = session-scoped capability grant, separate from per-effect confirm, with the 35-CONTEXT rationale (a bearer token's authority far exceeds one PR; confirming a PR body ≠ authorizing the credential; it opens a default-deny-net hole; mirrors the v1.4 ConnectionRole capability precedent). Pin the exact mechanism: a distinct human action (`caprun grant <session> ...`) authorizes the broker to USE the token for that session, recorded as its OWN audit event, INDEPENDENT of the per-PR I2 confirm of the (sink, args) — two independent gates (capability grant + per-effect I2); a PR cannot be created on a bare confirm alone. FINALIZE the exact verb/lifetime (session-scoped). Pin token scoping (closes P6 over-scoping): MINIMAL scopes (fine-grained PAT: Pull requests:write + Contents:read), stated as an operator responsibility surfaced at grant time. Pin the tainted-PR-body block (the marquee P6-exfil, closes it): title/body/message are I2-sensitive sink args (reuse CONTENT-01 content-sensitivity — cite sink_sensitivity.rs:140); taint genuinely propagated; the verbatim, provenance-annotated title/body is shown to the human at confirm. Pin the duplicate-PR CAS (closes P16 replay): a content-derived idempotency key (owner/repo/base/head/title/body digest) committed to a CAS table BEFORE the GitHub API call — mirror v1.6 HARDEN-03's Allowed-path replay defense (cite DESIGN-security-hardening.md) — so a replayed submission creates AT MOST one PR. Pin the confirm-release terminal-event-before-state with a prepare_github_pr precheck (§9 cross-reference; P33/P34 class).

    §5 crypto provider (FORK 2) + env_clear TLS policy: State FORK 2 DECIDED = lean `ring` (pure-Rust) for the new egress, with the 35-CONTEXT rationale (minimize untrusted C in the TCB; the planner sidecar already ships aws-lc-rs but is a SEPARATE process so no in-process provider conflict forces a match); note aws-lc-rs is acceptable IF provider-consistency is materially cleaner — low-stakes either way, the doc author picks + justifies, the reviewer sanity-checks; do NOT over-constrain. Pin the env_clear TLS-cert allowlist policy (ENV-01 forward, realized Phase 40): CA roots = compiled-in webpki-roots 1.0.8 (NOT rustls-platform-verifier) so env_clear() is HERMETIC — no SSL_CERT_* / readable system cert store required; the surviving allowlist for any TLS-egress process is ONLY HTTPS_PROXY/NO_PROXY (behind a proxy) + minimal PATH/locale; state this MUST be validated by a LIVE HTTPS run (the only place the TLS-env regression manifests — offline/mocked tests do not catch it), and that it closes the deferred v1.7 planner-sidecar env_clear todo (cite cli/caprun/src/main.rs:314-331 as the not-yet-env_clear'd sidecar spawn).
  </action>
  <verify>
    <automated>grep -q 'mint_from_http' planning-docs/DESIGN-git-github-http-sinks.md && grep -q 'HttpRaw' planning-docs/DESIGN-git-github-http-sinks.md && grep -qi 'resolve-and-pin' planning-docs/DESIGN-git-github-http-sinks.md && grep -qi 'auth-grant' planning-docs/DESIGN-git-github-http-sinks.md && grep -q 'webpki-roots' planning-docs/DESIGN-git-github-http-sinks.md</automated>
  </verify>
  <acceptance_criteria>
    - The doc has §3 (http.request GET + mint_from_http), §4 (github.pr + auth-grant + CAS), and §5 (crypto FORK 2 + env_clear TLS) sections.
    - §3 pins Pattern A dispatch (reqwest, not octocrab), Observe class with response-demotes-session, url I2-gated, mint_from_http rooted on a real http_response_received Event with session demotion (I1), TaintLabel::HttpRaw compile-forced into is_untrusted(), the anti-staple test requirement, and the full SSRF resolve-and-pin denylist.
    - §4 pins CommitIrreversible, broker-local-only bearer token, FORK 3 session-scoped `caprun grant` auth-grant as an independent gate from per-PR I2 confirm, minimal PAT scopes, the tainted title/body I2 block (CONTENT-01) with verbatim provenance at confirm, the content-derived duplicate-PR CAS committed before the API call, and the prepare_github_pr confirm-release cross-reference.
    - §5 states FORK 2 = ring (aws-lc-rs acceptable if cleaner) and pins compiled-in webpki-roots making env_clear() hermetic, the HTTPS_PROXY/NO_PROXY-only surviving allowlist, and the live-HTTPS-run validation requirement.
    - No file under crates/ or cli/ is modified.
  </acceptance_criteria>
  <done>§3-§5 pin the two network sinks (http.request GET with the NEW mint_from_http + HttpRaw + SSRF resolve-and-pin; github.pr with FORK-3 session-scoped auth-grant + duplicate-PR CAS + tainted-body I2 block) and the FORK-2 crypto + env_clear webpki-roots TLS policy; git diff touches only planning-docs/.</done>
</task>

<task type="auto">
  <name>Task 3: Write §6-§12 — 11-pitfall threat model, invariant preservation, fail-closed defaults table, confirm-release discipline, new-symbol summary + Gate 3 mandate, open items/residuals, acceptance predicate; run the invariant gate</name>
  <read_first>
    - .planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md — the full enumerated list of 11 pitfall closures (each must map to a named mechanism in the §6 table), the P33/P34 confirm-release discipline, and the "does not weaken I0/I1/I2, no raw effect-request path, sink sensitivity hardcoded" invariant list.
    - .planning/research/PITFALLS.md — the 11 DESIGN-GATE-BLOCKING pitfalls with their pitfall IDs and pitfall-to-phase mapping (the source the §6 table cross-references).
    - planning-docs/DESIGN-slot-type-binding.md — §Accepted Residual Risks + Acceptance Predicate: MODEL these two closing sections on it. planning-docs/DESIGN-effect-breadth-exec.md — MODEL the fail-closed-defaults table and security-invariant-checklist shape.
    - Code to trace and cite (file:line): scripts/check-invariants.sh (Gate 1 = no EffectRequest under crates/, lines ~24-31; Gate 3 = mint-call-site restriction, ~50-59 — today greps mint_from_read/mint_from_derivation/mint_from_exec and would NOT catch a new mint_from_http), crates/executor/src/sink_sensitivity.rs (the hardcoded is_routing_sensitive/is_content_sensitive/expected_role tables — sink sensitivity is a security property, NOT a config file), crates/brokerd/src/confirmation.rs (the prepare_* precheck + terminal-event-before-state discipline for git.push/github.pr).
  </read_first>
  <files>planning-docs/DESIGN-git-github-http-sinks.md</files>
  <action>
    Append §6-§12, then run the invariant gate.

    §6 Threat model — 11 pitfalls → named mechanism: a table with one row per design-gate-blocking pitfall, each closed with a NAMED mechanism (cross-reference the § that pins it). Cover all 11 from 35-CONTEXT: (1) tainted PR-body/commit-message exfil → title/body/message are I2-sensitive sink args (CONTENT-01), taint genuinely propagated, verbatim provenance at confirm [§1/§4]; (2) git config/hook RCE → hardcoded launcher neutralization (GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, no aliases, GIT_TERMINAL_PROMPT=0, env_clear()'d) [§1]; (3) swapped-remote push → remote+branch from trusted intent, passed explicitly, never repo .git/config [§2]; (4) --force/destructive refspec → hard-denied, remote+refspec I2-gated [§2]; (5) credential leak/flow → broker-local env only, short-lived child injection [§2/§4]; (6) token over-scoping → minimal fine-grained PAT scopes, operator responsibility at grant [§4]; (7) http SSRF → resolve-and-pin, deny loopback/RFC1918/link-local/CGNAT/metadata/ULA/IPv6-mapped, no redirects, reject userinfo@/non-https/IP-encoding, host allowlist [§3]; (8) http response not minted/stapled taint → mint_from_http at arrival rooted on a real event + session demotion + anti-staple test [§3]; (9) net-deny widening → worker never gains net, egress is broker/confined-net-child only, single-pinned-host:port relaxation [§2/§3]; (10) push/PR effect-class → pinned per §1/§2/§4, git.commit's MutateReversible exception justified [§1]; (11) replay/duplicate-PR CAS → content-derived idempotency key committed to CAS before the API call [§4].

    §7 Invariant preservation (explicit): a checklist asserting with a one-line justification each — I0 unaffected (no new session-creation path; a session seeded from external content still starts draft-only); I1 preserved AND extended (mint_from_http demotes a session on an inbound response, exactly the I1 direction); I2 NOT weakened or bypassed (all four sinks route through the UNMODIFIED submit_plan_node collect-then-Block loop; the only executor changes are table rows); NO new raw effect-request-to-sink path is introduced (the plan-node path is preserved; check-invariants Gate 1 stays green with zero new hits — refer to the constraint by concept, cite check-invariants.sh Gate 1); sink sensitivity stays HARDCODED in the executor (is_routing_sensitive/is_content_sensitive/expected_role table rows, never a swappable policy file — cite sink_sensitivity.rs); genuine non-stapled taint (HTTP taint minted only in mint_from_http at the http_response_received Event; git output via mint_from_exec).

    §8 Fail-closed defaults table: one row per new sink arg giving its default deny posture. Rows: git.commit message (content-sensitive, tainted → Block; MutateReversible survives I1); git.push remote (routing-sensitive, from trusted intent only, tainted → Block); git.push refspec (routing-sensitive, --force/deletion hard-denied); http.request url (routing-sensitive I2-gated, non-allowlisted host → Deny, SSRF ranges → Deny); http.request response ValueNode (HttpRaw+untrusted, session demotes, unknown classification → fail-closed); github.pr title/body (content-sensitive CONTENT-01, tainted → Block); github.pr credential use (requires session auth-grant AND per-effect confirm — both gates; absent grant → Deny); duplicate submission (CAS hit → at-most-once, no second PR); unregistered sink or unknown/missing arg (Deny at the Step 0 schema gate before any resolve).

    §9 Confirm-release audit-gap discipline (P33/P34 — MANDATORY for the new confirm-releasable sinks): state that git.push and github.pr are CommitIrreversible + confirm-releasable, and MANDATE for EACH: the confirm-release path writes the TERMINAL AUDIT EVENT before the terminal state (never a terminal STATE before the EVENT that justifies it), with a prepare_* precheck (prepare_git_push, prepare_github_pr) — this is the RECURRING MAJOR class a passing verifier + green gates missed twice (v1.7 P33 file.write, P34 process.exec) that only the fresh adversarial trace caught. Cite confirmation.rs's existing terminal-state discipline as the pattern to extend.

    §10 New-mechanism symbol summary + Gate 3 mandate: list the NEW symbols the implementation phases will introduce (TaintLabel::HttpRaw, mint_from_http + the http_response_received Event, the duplicate-PR CAS table, the session auth-grant event + `caprun grant` verb, prepare_git_push / prepare_github_pr) and note each appears ONLY in DESIGN-doc prose this phase, never under crates/. MANDATE extending scripts/check-invariants.sh Gate 3 with a mint_from_http( call-site check (same sanctioned loci) as part of the http.request phase — cite that Gate 3 today greps only mint_from_read/mint_from_derivation/mint_from_exec and would NOT catch a new mint_from_http.

    §11 Open items & accepted residual risks: flag as OPEN (model pinned, deployment constants deferred) — the exact single-pinned-host:port seccomp net-relaxation syscall set for the git.push child (pin the METHOD: narrowest relaxation to reach the one remote, resolved at the git.push phase); the exact `caprun grant` lifetime/verb surface (session-scoped pinned, exact CLI surface finalized at the github.pr phase); the exact webpki-roots surviving-env allowlist confirmation via a live HTTPS run (Phase 40). Accepted residuals: an Allowed http.request GET in a network-scoped context is inert unless the allowlist permits it; the git.push net-relaxation is the riskiest surface (explicitly top of the review list). Mirror the prior DESIGN docs' Accepted Residual Risks convention.

    §12 Acceptance Predicate — Done When: a bullet list making the phase DONE state checkable — the doc pins all four sinks' mechanisms + fail-closed defaults and closes all 11 pitfalls with a named mechanism each (this plan), and a fresh non-self adversarial code-trace clears it with all findings resolved, recorded in planning-docs/DESIGN-GATE-RECORD-v1.8.md (Plan 35-02), with no TCB code written (git diff touches only planning-docs/ + .planning/).

    After writing, run scripts/check-invariants.sh and confirm it exits 0 (the doc lives in planning-docs/, so its prose cannot trip any Gate under crates/ — confirm).
  </action>
  <verify>
    <automated>grep -qi 'residual' planning-docs/DESIGN-git-github-http-sinks.md && grep -qi 'acceptance predicate' planning-docs/DESIGN-git-github-http-sinks.md && grep -q 'Gate 3' planning-docs/DESIGN-git-github-http-sinks.md && grep -qi 'prepare_git_push' planning-docs/DESIGN-git-github-http-sinks.md && bash scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - The doc has §6 (11-pitfall threat model), §7 (invariant preservation), §8 (fail-closed defaults table), §9 (confirm-release discipline), §10 (new-symbol summary + Gate 3 mandate), §11 (open items/residuals), and §12 (acceptance predicate) sections.
    - §6 closes ALL 11 design-gate-blocking pitfalls, each with a NAMED mechanism cross-referenced to the pinning §.
    - §7 explicitly asserts I0/I1/I2 non-weakening, no new raw effect-request-to-sink path, and executor-hardcoded sink sensitivity.
    - §9 mandates terminal-event-before-terminal-state with prepare_git_push/prepare_github_pr for both confirm-releasable sinks; §10 mandates the Gate 3 mint_from_http extension.
    - scripts/check-invariants.sh exits 0; git status --porcelain crates/ cli/ is empty.
  </acceptance_criteria>
  <done>§6-§12 complete the doc; all 11 pitfalls are closed with named mechanisms, the invariant-preservation and confirm-release-discipline sections are present, the Gate 3 mint_from_http extension is mandated, and the acceptance predicate is checkable; check-invariants.sh is green; git diff touches only planning-docs/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined worker → broker (plan-node submit) | Worker submits `PlanNode{sink, args}` for the four new sinks; untrusted values (incl. HTTP-response and exec/git-output handles) cross here toward sensitive slots (git remote/refspec, PR title/body, http url, commit message). |
| broker → git.push confined child (net-allowed) | NEW boundary: the broker spawns a kernel-confined child with a MINIMAL seccomp net relaxation to reach ONE pinned remote host:port, injecting a short-lived push credential — the riskiest new surface. |
| broker → external network (Pattern A egress) | The broker/broker-helper makes outbound TLS calls (github.pr POST, http.request GET); a broker-held bearer token and inbound untrusted response bytes cross here. |

## STRIDE Threat Register

Design-gate scope: these are the threats the DESIGN doc must pin mitigations for so Phases 36-40 are buildable. Each maps to a §-section the doc must contain.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Information Disclosure | tainted PR-body/commit-message exfil (marquee) | critical | mitigate | Doc §1/§4: title/body/message are I2-sensitive sink args (CONTENT-01), taint genuinely propagated (not stapled), verbatim provenance shown at confirm. |
| T-35-02 | Elevation of Privilege | git config/hook RCE | critical | mitigate | Doc §1: hardcoded launcher neutralization (GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, no aliases, GIT_TERMINAL_PROMPT=0, env_clear()'d child). |
| T-35-03 | Tampering | swapped-remote / destructive push | high | mitigate | Doc §2: remote+branch pinned from trusted intent (never repo .git/config); --force/ref-deletion hard-denied; remote+refspec I2-gated. |
| T-35-04 | Information Disclosure | broker-held credential leak/over-scope | critical | mitigate | Doc §2/§4: token/push-cred in broker-local env only (never ValueNode/plan-arg/audit-literal/worker/planner); short-lived child injection; minimal PAT scopes; session auth-grant gate. |
| T-35-05 | Information Disclosure | http.request SSRF | high | mitigate | Doc §3: resolve-and-pin the IP; deny loopback/RFC1918/link-local/CGNAT/metadata/ULA/IPv6-mapped; no redirects; reject userinfo@/non-https/IP-encoding; host allowlist. |
| T-35-06 | Spoofing / Tampering | inbound HTTP taint not minted / stapled | critical | mitigate | Doc §3: mint_from_http at arrival rooted on a real http_response_received Event + session demotion (I1); anti-staple test proving the downstream Block. |
| T-35-07 | Elevation of Privilege | net-deny widening to the worker | high | mitigate | Doc §2/§3: the confined worker never gains net; egress is broker/confined-net-child only; the git.push child gets a single-pinned-host:port minimal relaxation. |
| T-35-08 | Tampering | replay / duplicate PR | medium | mitigate | Doc §4: content-derived idempotency-key CAS committed BEFORE the GitHub API call (mirrors HARDEN-03) → at-most-one PR. |
| T-35-09 | Repudiation | confirm-release audit gap (P33/P34 class) | high | mitigate | Doc §9: git.push/github.pr write the terminal audit event BEFORE the terminal state, with prepare_git_push/prepare_github_pr prechecks. |
| T-35-SC | Tampering | cargo installs | low | accept | No package installs this phase (doc-only); the RESEARCH Package Legitimacy Audit for reqwest/webpki-roots/ring is verified at the implementation phases (37+) before any install. |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-git-github-http-sinks.md` — the doc exists.
- Section presence: the doc contains §0-§12 covering all four sinks (dispatch pattern, effect-class, I2-sensitive args, taint flow, confinement each), the three fork decisions, mint_from_http + TaintLabel::HttpRaw + duplicate-PR CAS + auth-grant, the 11-pitfall threat table, invariant preservation, the fail-closed defaults table, the confirm-release discipline, and the acceptance predicate.
- `bash scripts/check-invariants.sh` exits 0 (no architectural-invariant regression from the doc's prose).
- `git status --porcelain crates/ cli/` is empty — no TCB code written this plan.
</verification>

<success_criteria>
- `planning-docs/DESIGN-git-github-http-sinks.md` exists and pins, per sink, the dispatch pattern, effect-class (git.commit=MutateReversible, git.push/github.pr=CommitIrreversible, http.request GET=Observe), the I2-sensitive sink args, the taint flow, and the confinement (DESIGN-15).
- The doc pins the mint_from_http inbound-taint mechanism + session demotion, the git config/hook neutralization surface, git.push destination-pinning + credential injection, the SSRF resolve-and-pin model, the github.pr session-scoped human auth-grant, the env_clear() webpki-roots TLS-cert allowlist policy, duplicate-PR CAS semantics, and the new TaintLabel::HttpRaw variant (DESIGN-15).
- The doc closes all 11 design-gate-blocking pitfalls with a named mechanism each; nothing disables/bypasses I2 and no new raw effect-request-to-sink path is introduced; sink sensitivity stays hardcoded in the executor (DESIGN-15).
- `scripts/check-invariants.sh` is green; the git diff touches only `planning-docs/`.
</success_criteria>

<output>
Create `.planning/phases/35-design-gate-fresh-adversarial-code-trace/35-01-SUMMARY.md` when done.
</output>
