---
phase: 45-thin-cli-sdk-read-only-audit-dag-viewer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/src/display.rs
  - crates/brokerd/src/lib.rs
  - crates/brokerd/src/confirmation.rs
autonomous: true
requirements: [U1]

must_haves:
  truths:
    - "`neutralize_control_chars` is a single shared `pub fn` reachable by BOTH `brokerd::confirmation` and the `cli/caprun` viewer — it escapes every `char::is_control()` byte (C0 incl. ESC/CR/LF/TAB, C1 range, DEL) to a visible `\\xNN`/`\\u{NNNN}` and preserves ordinary printable + non-ASCII UTF-8."
    - "`confirmation.rs`'s existing git.push-scoped neutralization (`render_block_display` :718, `render_git_push_payload_summary` :630/:647) calls the shared fn and its behavior is UNCHANGED (still git.push-scoped there under the shipped T-10-04 verbatim-display policy — the viewer is what neutralizes universally, in 45-03)."
    - "An anti-drift test asserts both the confirmation call site and the shared module resolve to the ONE implementation (mirrors the Phase-42 F1 shared-helper anti-drift test), so a second divergent copy cannot be reintroduced silently."
  artifacts:
    - "crates/brokerd/src/display.rs — the shared `pub fn neutralize_control_chars(&str) -> String` + its unit tests."
    - "crates/brokerd/src/lib.rs — `pub mod display;` export."
    - "crates/brokerd/src/confirmation.rs — the private `fn neutralize_control_chars` removed; call sites rewired to `crate::display::neutralize_control_chars`."
  key_links:
    - "one implementation, two callers (confirmation prompt + viewer) — the anti-drift test is what keeps them from diverging (the U1 M3 audit-line-spoofing surface stays closed by construction)."
---

<objective>
Extract the existing private `neutralize_control_chars` (confirmation.rs:575) into a single shared `pub fn` in a small `brokerd::display` module, rewire the shipped confirmation call sites to it (behavior unchanged), and add an anti-drift test — the foundation the read-only viewer (45-03) needs to neutralize every displayed literal (WG-2 / U1 M3).

Purpose: The neutralizer is correct and tested but private + only reachable inside confirmation.rs. The viewer lives in `cli/caprun` (a different crate) and MUST call the SAME proven implementation, not a second copy that could drift weaker (RESEARCH "Don't Hand-Roll": two copies drift). This mirrors the exact Phase-42 MAJOR-2 F1 containment-helper extraction pattern (a shared `pub fn` + an anti-drift test).

Output: `brokerd::display::neutralize_control_chars` shared, confirmation.rs rewired with unchanged behavior, and an anti-drift regression test.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/45-thin-cli-sdk-read-only-audit-dag-viewer/45-RESEARCH.md

# The private fn to extract + its two call sites (render_block_display :718, render_git_push_payload_summary :630/:647):
@crates/brokerd/src/confirmation.rs
@crates/brokerd/src/lib.rs

# The Phase-42 F1 shared-helper extraction to mirror (shared pub fn + anti-drift test):
@crates/adapter-fs/src/containment.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract neutralize_control_chars into a shared brokerd::display pub fn</name>
  <files>crates/brokerd/src/display.rs, crates/brokerd/src/lib.rs</files>
  <behavior>
    - `brokerd::display::neutralize_control_chars("a\x1b[2Kb")` escapes the ESC (`\x1b`) to a visible `\x1b` literal-escape and leaves `a`, `[`, `2`, `K`, `b` intact.
    - Every `char::is_control()` byte is escaped: C0 (incl. CR `\x0d`, LF `\x0a`, TAB `\x09`, ESC `\x1b`), DEL (`\x7f`), and a C1 codepoint (e.g. `\u{0085}`) → the `\u{NNNN}` form.
    - Ordinary printable ASCII and non-ASCII UTF-8 (e.g. `café`, `→`) are preserved byte-for-byte.
    - An empty string returns empty; a control-free string returns itself unchanged.
  </behavior>
  <action>Create `crates/brokerd/src/display.rs` with `pub fn neutralize_control_chars(s: &str) -> String` whose body is the EXACT logic currently in confirmation.rs:575-590 (each `char::is_control()` char → `\xNN` when codepoint ≤ 0xff else `\u{NNNN}`; other chars pushed verbatim). Copy the doc comment (the "mirrors the U1 / VIEW-01 viewer discipline" rationale). Add `pub mod display;` to crates/brokerd/src/lib.rs. Add the unit tests from the behavior block into `display.rs`. This is a pure, deterministic, no-I/O function — no new dependency, no new crate.</action>
  <verify>
    <automated>cargo test -p brokerd display::neutralize --no-fail-fast</automated>
  </verify>
  <done>`brokerd::display::neutralize_control_chars` exists as a `pub fn`, escapes every control byte to a visible form, preserves printable/UTF-8; module exported from lib.rs; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire confirmation.rs to the shared fn + anti-drift test</name>
  <files>crates/brokerd/src/confirmation.rs</files>
  <behavior>
    - `render_block_display` for a git.push block still neutralizes the displayed literal (unchanged behavior); for every non-git.push sink the literal is still shown byte-verbatim (the shipped T-10-04 policy — do NOT widen it here; the viewer widens in 45-03).
    - `render_git_push_payload_summary` still neutralizes the frozen new-oid + per-arg literals via the shared fn.
    - An anti-drift test asserts the shared `brokerd::display::neutralize_control_chars` produces the SAME escaping the confirmation display relies on (a tainted `\x1b[2K` body renders escaped, not as a cleared line), so the two-caller contract cannot silently diverge.
  </behavior>
  <action>Delete the private `fn neutralize_control_chars` from confirmation.rs (:575-590) and rewire its two call sites — `render_block_display` (the `if pc.sink.0 == "git.push"` branch, :718) and `render_git_push_payload_summary` (:630 per-arg literal + :647 frozen_new_oid) — to call `crate::display::neutralize_control_chars`. Behavior MUST be byte-identical to today (still git.push-scoped in confirmation.rs; the viewer's universal neutralization is 45-03's job — do NOT change the `if pc.sink.0 == "git.push"` guard here). Add an anti-drift test (mirror the Phase-42 F1 shared-helper anti-drift test, `crates/adapter-fs/src/containment.rs`): assert the confirmation git.push display path and `crate::display::neutralize_control_chars` agree on a `\x1b[2K`-bearing literal (both escape it), proving both resolve to the ONE implementation. Keep existing confirmation.rs tests green.</action>
  <verify>
    <automated>cargo test -p brokerd confirmation:: --no-fail-fast && cargo test -p brokerd display:: --no-fail-fast</automated>
  </verify>
  <done>The private copy is gone; both confirmation call sites use the shared fn with unchanged git.push-scoped behavior; the anti-drift test passes; all existing confirmation.rs tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tainted literal → human terminal | an attacker-influenceable literal (commit message, POST body, refspec) could embed ANSI/C0/C1 bytes to spoof or hide audit lines; neutralization is the output-encoding boundary (ASVS V5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-05 | Tampering | two divergent neutralizer copies (confirmation vs viewer) — one drifts weaker | high | mitigate | ONE shared `pub fn` + an anti-drift test asserting both callers resolve to it (mirrors the Phase-42 F1 shared-helper discipline). |
| T-45-06 | Repudiation | a behavior change to confirmation display slips in during the extraction | medium | mitigate | the git.push-scoped guard + byte-verbatim non-git.push display are held constant; existing confirmation.rs tests must stay green. |
</threat_model>

<verification>
- `cargo build --workspace` compiles; `cargo test -p brokerd` green (display + confirmation modules).
- `bash scripts/check-invariants.sh` passes (no new crate — Gate 5; no mint/effect-path change).
- The anti-drift test is committed and green.
</verification>

<success_criteria>
`neutralize_control_chars` is one shared `brokerd::display` `pub fn` reachable cross-crate; confirmation.rs calls it with byte-identical (git.push-scoped) behavior; an anti-drift test keeps the two callers from diverging — the foundation for the viewer's universal neutralization (45-03).
</success_criteria>

<output>
Create `.planning/phases/45-thin-cli-sdk-read-only-audit-dag-viewer/45-02-SUMMARY.md` when done.
</output>
