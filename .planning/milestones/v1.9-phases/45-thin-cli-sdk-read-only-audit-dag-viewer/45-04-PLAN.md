---
phase: 45-thin-cli-sdk-read-only-audit-dag-viewer
plan: 04
type: execute
wave: 3
depends_on: ["45-01", "45-03"]
files_modified:
  - cli/caprun/tests/s45_cli_viewer_acceptance.rs
autonomous: true
requirements: [SDK-01, U1]

must_haves:
  truths:
    - "A genuine end-to-end run proves the design-partner loop: `caprun run` (define intent → point at workspace + trusted `--policy`) drives a session against the broker; a sink Blocks under I2; the parent surfaces the blocked `effect_id` + `caprun review` pointer; `caprun review <effect_id>` shows the verbatim literals; and `caprun audit <session_id> <db>` renders the events/decisions + `verify_chain` — no stubs (SDK-01 §1/§2 + U1)."
    - "The viewer's `verify_chain` verdict against the genuine run's chain is PASSED; with the `.key` sibling absent the viewer fails CLOSED (no verdict, non-zero exit); `caprun audit … :memory:` is refused."
    - "A tainted literal in the run's audit DAG (e.g. a tainted body/path from a file/env-sourced value) renders control-char-neutralized in the viewer — no raw ESC/CSI byte reaches the terminal (U1 M3)."
    - "This is the LIVE-05 driver-inspector setup: the composed multi-sink proof driven + inspected via `caprun run`/`caprun audit` is Phase 46 (LIVE-05/06); 45-04 proves the CLI+viewer loop end-to-end on a single Blocking run."
  artifacts:
    - "cli/caprun/tests/s45_cli_viewer_acceptance.rs — the SDK-01 + U1 end-to-end run→Block→surface→review→audit acceptance test + the fail-closed/:memory:/neutralization negative legs."
  key_links:
    - "the SAME session's audit DB is driven by `caprun run` and inspected by `caprun audit` — the driver and inspector share the persisted chain + its `.key` sibling (cross-process MAC custody), exactly as LIVE-05 requires."
    - "the surfaced `effect_id` from the run's Block is the SAME id `caprun review`/`caprun audit` resolve — the loop is closed by a real durable pending_confirmations row."
---

<objective>
Prove the design-partner-runnable loop end-to-end on real Linux: `caprun run` (with a trusted `--policy`) drives a session that I2-Blocks, the parent surfaces the blocked `effect_id` + review pointer, `caprun review` shows the verbatim literals, and `caprun audit` renders the session's events/decisions + a `verify_chain` verdict with tainted literals neutralized — plus the fail-closed-on-absent-key, `:memory:`-refused, and tainted-literal-neutralization negative legs. Genuine, not stubbed.

Purpose: Phase 45 is on the acceptance critical path — Phase 46 LIVE-05 requires the composed proof be DRIVEN (via `caprun run`) and INSPECTED (via `caprun audit`) end-to-end. This test proves the CLI+viewer loop is real on a single Blocking run, so LIVE-05 can compose the full multi-sink workflow on top of a driver-inspector already proven to work.

Output: A committed, green end-to-end CLI+viewer acceptance test (SDK-01 + U1).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/45-thin-cli-sdk-read-only-audit-dag-viewer/45-RESEARCH.md

# The verbs under test (built binaries) + an existing binary-driving CLI test for shape
# (spawn the caprun binary, drive a real confined run, assert on stdout/exit + the persisted DB):
@cli/caprun/src/main.rs
@cli/caprun/src/worker.rs
@crates/brokerd/src/confirmation.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: end-to-end run → Block → surface → review → audit (genuine)</name>
  <files>cli/caprun/tests/s45_cli_viewer_acceptance.rs</files>
  <action>Create `cli/caprun/tests/s45_cli_viewer_acceptance.rs` (mirror the shipped binary-driving CLI test convention — resolve + spawn the built `caprun` binary; run `cargo build --workspace` first per the sibling-binary rule). Drive a GENUINE run whose sink Blocks under I2: use `caprun run` to define an intent, point it at a workspace file whose content forces a taint→I2 Block (reuse an existing hostile-read/doc→action fixture that produces a `sink_blocked` + a durable `pending_confirmations` row), against a trusted `--policy` (or the `broker_default` fallback) with a persistent `<audit-db-path>` (NOT `:memory:` — the viewer needs a persisted chain). Assert, in order: (1) the run exits non-zero AND the parent stdout surfaces the blocked `effect_id` + the `caprun review <effect_id> <db>` / confirm / deny pointers (WG-5); (2) `caprun review <effect_id> <db>` exits 0 and prints the verbatim blocked literal(s) + provenance; (3) `caprun audit <session_id> <db>` exits 0, renders the events/decisions (including the `sink_blocked`/decision line for the surfaced effect_id), and prints `Chain verification: PASSED` (the genuine keyed chain the run wrote). Assert the effect_id surfaced by the run is the SAME id review + audit resolve (the loop is closed by one real durable row). Gate the confined-run legs `#[cfg(target_os = "linux")]` per CLAUDE.md (the run self-confines with Landlock+seccomp); this test is authoritative under the FULL compose-verify on real Linux.</action>
  <verify>
    <automated>cargo test -p caprun --test s45_cli_viewer_acceptance --no-fail-fast</automated>
  </verify>
  <done>A genuine `caprun run` I2-Blocks, surfaces the effect_id + review pointer, `caprun review` shows the verbatim literals, and `caprun audit` renders the DAG + `verify_chain` PASSED for the SAME session — no stubs; Linux-gated confined legs.</done>
</task>

<task type="auto">
  <name>Task 2: negative legs — fail-closed key, :memory: refused, tainted-literal neutralized</name>
  <files>cli/caprun/tests/s45_cli_viewer_acceptance.rs</files>
  <action>Extend the test with the U1 security negative legs against the SAME genuine run's audit DB: (1) FAIL-CLOSED-ON-ABSENT-KEY — move/remove the `<audit-db-path>.key` sibling, then `caprun audit <session_id> <db>` exits non-zero and prints NO `Chain verification:` verdict (it must refuse to render a verdict rather than load/create a fresh key — U1 M2); restore the key after. (2) MEMORY-REFUSED — `caprun audit <session_id> :memory:` exits non-zero with a refusal and renders no verdict. (3) TAINTED-LITERAL-NEUTRALIZED — ensure the run's DAG contains a tainted literal bearing a control/ANSI sequence (a file/env-sourced value carrying a `\x1b[2K` ESC-CSI, minted TAINTED per 45-01's M7 path); assert `caprun audit`'s stdout renders it as a visible escaped form (`\x1b`) with NO raw ESC (0x1b) byte present — the audit-line-spoofing surface is closed for ALL sinks (U1 M3). Add a doc comment: the composed multi-sink workflow driven + inspected via this CLI+viewer is Phase 46 (LIVE-05/06); 45-04 proves the loop + the viewer's fail-closed/neutralization guarantees on a single run. Gate the confined-run-dependent legs `#[cfg(target_os = "linux")]`; keep any leg whose fixture DB can be built without confinement host-portable.</action>
  <verify>
    <automated>cargo test -p caprun --test s45_cli_viewer_acceptance --no-fail-fast</automated>
  </verify>
  <done>With the key absent the viewer refuses a verdict (non-zero, no `Chain verification:` line); `:memory:` is refused; a tainted `\x1b[2K` literal renders escaped with no raw ESC byte — all three negative legs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → caprun binaries | the acceptance test drives the REAL `caprun run`/`review`/`audit` binaries end-to-end (no mocked verbs, a genuine confined run). |
| genuine run's audit DB + key → viewer | the viewer inspects the exact chain the driven run wrote, verifying against its real cross-process MAC key or failing closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-11 | Repudiation | a stubbed/hand-built chain passes the loop without a genuine run | high | mitigate | the test drives a REAL `caprun run` that self-confines and writes a genuine keyed chain; the surfaced effect_id is a real durable `pending_confirmations` row review + audit both resolve. |
| T-45-12 | Spoofing | the viewer blesses a chain with a fresh/`:memory:` key in the acceptance path | critical | mitigate | the absent-key + `:memory:` negative legs assert the viewer refuses a verdict (fail-closed), not a green-but-meaningless PASSED. |
| T-45-13 | Tampering | a tainted ANSI literal spoofs an audit line in the real run's DAG | high | mitigate | the neutralization leg asserts no raw ESC byte reaches stdout for a `\x1b[2K`-bearing tainted literal (U1 M3, all sinks). |
</threat_model>

<verification>
- `cargo build --workspace` FIRST (sibling-binary rule), then `cargo test -p caprun --test s45_cli_viewer_acceptance` — host-portable legs green on macOS; the confined-run legs compile.
- On real Linux via `bash scripts/mailpit-verify.sh` with `MAILPIT_VERIFY_CMD='cargo test -p caprun --test s45_cli_viewer_acceptance'` (or the FULL compose-verify): the Linux-gated run→Block→surface→review→audit legs pass; capture the pre-pipe exit status (never assert on a piped exit 0 — [[verification-exit-code-through-pipe]]).
- `bash scripts/check-invariants.sh` passes; the authoritative gate is the FULL (non-scoped) compose-verify on real Linux (CLAUDE.md).
</verification>

<success_criteria>
The design-partner loop is proven end-to-end: a genuine `caprun run` I2-Blocks, surfaces the effect_id + review pointer, `caprun review` shows the verbatim literals, and `caprun audit` renders the DAG + `verify_chain` PASSED for the same session — with the viewer failing closed on an absent key, refusing `:memory:`, and neutralizing tainted literals. The composed multi-sink LIVE-05/06 proof is set up for Phase 46.
</success_criteria>

<output>
Create `.planning/phases/45-thin-cli-sdk-read-only-audit-dag-viewer/45-04-SUMMARY.md` when done.
</output>
