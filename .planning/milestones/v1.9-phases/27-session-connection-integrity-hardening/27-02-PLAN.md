---
phase: 27-session-connection-integrity-hardening
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - crates/brokerd/src/server.rs
  - crates/brokerd/Cargo.toml
  - crates/brokerd/tests/uds_ipc.rs
  - cli/caprun/tests/harden04_featureless_create_session.rs
autonomous: true
requirements: [HARDEN-04]

must_haves:
  truths:
    - "The CreateSession-IPC forced-Active mint arm is excluded from a default (featureless) production build at compile time via a `test-fixtures` Cargo feature — grep/build evidence shows it absent from a default release build, not merely gated behind the CAPRUN_ENABLE_IPC_CREATE_SESSION runtime flag (SC3)"
    - "A default-build CreateSession over the socket always returns the fail-closed Error with NO opt-in — even with CAPRUN_ENABLE_IPC_CREATE_SESSION=1 set, because the mint code is physically gone (D-10 behavioral negative gate)"
    - "The existing 3 uds_ipc.rs tests (server_accept, create_session_round_trip, create_session_over_ipc_denied_by_default_when_flag_unset) still exercise the forced-Active arm under the new test-fixtures feature — coverage is not silently lost (SC4), verified EMPIRICALLY by running them, not by inspection"
    - "The featureless sibling returns the SAME Error response as today's disabled-path response (behavior on the negative path is unchanged; only physical presence changes)"
    - "cargo build --workspace and cargo test --workspace --no-fail-fast are GREEN"
  prohibitions:
    - statement: "No runtime env flag (CAPRUN_ENABLE_IPC_CREATE_SESSION or any other) can re-enable the forced-Active mint in a default/featureless build"
      status: enforced
      verification: "the #[cfg(not(any(test, feature = \"test-fixtures\")))] sibling returns Error unconditionally with no std::env::var read; the D-10 test asserts Error with the env var set to 1"
    - statement: "The forced-Active mint body is not merely runtime-gated — it is absent from the default-build object/source path"
      status: enforced
      verification: "a default `cargo build --workspace --release` + grep for the mint arm's marker shows it absent; only the test-fixtures/test build contains it"
    - statement: "No existing uds_ipc.rs test is weakened, #[ignore]d, or has its assertion changed to pass under the new gate"
      status: enforced
      verification: "grep of uds_ipc.rs shows no new #[ignore]; the 3 named tests keep their original assertions and pass under `cargo test -p brokerd`"
  artifacts:
    - "crates/brokerd/Cargo.toml — new [features] block with `test-fixtures = []` and a self dev-dependency `brokerd = { path = \".\", features = [\"test-fixtures\"] }` (verbatim executor precedent)"
    - "crates/brokerd/src/server.rs — the CreateSession forced-Active mint extracted into a helper with a #[cfg(any(test, feature = \"test-fixtures\"))] definition (mint, env-gated) and a #[cfg(not(any(test, feature = \"test-fixtures\")))] sibling returning the same Error"
    - "cli/caprun/tests/harden04_featureless_create_session.rs — NEW D-10 behavioral negative gate exercising a featureless build"
  key_links:
    - "crates/executor/Cargo.toml is the exact precedent: :22 [features], :28 `test-fixtures = []`, :36 self dev-dep `executor = { path = \".\", features = [\"test-fixtures\"] }` — copy verbatim, substituting brokerd"
    - "crates/executor/src/sink_schema.rs is the dual-#[cfg] sibling-function precedent: :75/:93 #[cfg(any(test, feature = \"test-fixtures\"))], :98 #[cfg(not(...))] — same-behavior-on-negative-path template"
    - "uds_ipc.rs integration tests link brokerd built WITHOUT cfg(test), so they reach the arm ONLY via the test-fixtures FEATURE (enabled by the self dev-dep) — this is why executor needed its self dev-dep, and why brokerd needs one too (A4)"
    - "Cargo FEATURE UNIFICATION: under `cargo test --workspace`, brokerd's self dev-dep enables test-fixtures graph-wide, so a featureless behavioral test cannot be a plain --workspace test — the D-10 gate must build featureless out-of-band (shell-out) or run scoped; this matches kickoff constraint #6 (the featureless proof is a `cargo build --workspace --release` binary, distinct from `cargo test`)"
---

<objective>
Land HARDEN-04: replace the `CreateSession`-IPC forced-`Active` mint's RUNTIME `CAPRUN_ENABLE_IPC_CREATE_SESSION` env gate with a compile-time `test-fixtures` Cargo feature, so the mint arm is PHYSICALLY ABSENT from a default (featureless) release build — closing the residual where the dangerous mint shipped in the binary and was only kept dark by a runtime default-deny flag. Copy the shipped `executor` crate precedent verbatim (a `test-fixtures = []` feature + a self dev-dependency), split the arm into a `#[cfg(any(test, feature = "test-fixtures"))]` mint sibling and a `#[cfg(not(...))]` sibling that returns the identical Error, keep the 3 existing `uds_ipc.rs` tests exercising the arm under the feature (verified by running them, not by inspection), and add a D-10 behavioral negative gate proving a featureless build denies `CreateSession` even with the old env flag set.

Output: a brokerd whose forced-Active mint is compile-excluded by default, the existing coverage preserved under the feature, a featureless behavioral proof, and grep/build evidence of absence (SC3).
</objective>

<artifacts_this_phase_produces>
NEW symbols/changes introduced by THIS plan (exclude from drift / "symbol not found" verification):
- `crates/brokerd/Cargo.toml` — a `[features]` block with `test-fixtures = []`, and a self dev-dependency `brokerd = { path = ".", features = ["test-fixtures"] }`
- `crates/brokerd/src/server.rs` — the forced-Active CreateSession mint extracted into a `#[cfg(any(test, feature = "test-fixtures"))]` helper (env-gated) + a `#[cfg(not(any(test, feature = "test-fixtures")))]` sibling returning the same Error
- `cli/caprun/tests/harden04_featureless_create_session.rs` — NEW D-10 behavioral negative gate
</artifacts_this_phase_produces>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-security-hardening.md
@planning-docs/DESIGN-GATE-RECORD-v1.6.md
@.planning/phases/27-session-connection-integrity-hardening/27-RESEARCH.md
@.planning/phases/27-session-connection-integrity-hardening/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the test-fixtures Cargo feature + self dev-dep; split the CreateSession arm into a cfg-gated mint sibling and a featureless Error sibling; keep the 3 uds_ipc tests exercising the arm</name>
  <files>crates/brokerd/Cargo.toml, crates/brokerd/src/server.rs, crates/brokerd/tests/uds_ipc.rs</files>
  <read_first>
    - crates/executor/Cargo.toml (:22 [features], :28 `test-fixtures = []`, :31-36 the self dev-dep with the explanatory comment — the EXACT precedent to copy)
    - crates/executor/src/sink_schema.rs (:75/:93 `#[cfg(any(test, feature = "test-fixtures"))]`, :98 `#[cfg(not(any(test, feature = "test-fixtures")))]` — the sibling-function split template)
    - crates/brokerd/Cargo.toml (:33 [dev-dependencies] — currently no [features]; add both here)
    - crates/brokerd/src/server.rs (the CreateSession arm :904-994: env-gate `std::env::var("CAPRUN_ENABLE_IPC_CREATE_SESSION")` match :923-939 with the exact-string "1" test; the mint body `create_session`/`persist_session`/`append_event` :941-993; the disabled-path Error response the featureless sibling must reproduce)
    - crates/brokerd/tests/uds_ipc.rs (server_accept :63, create_session_round_trip :124, create_session_over_ipc_denied_by_default_when_flag_unset :214 — the 3 tests + how each sets/unsets CAPRUN_ENABLE_IPC_CREATE_SESSION under CREATE_SESSION_ENV_LOCK)
    - planning-docs/DESIGN-security-hardening.md §d (the compile-out mechanism + the "same Error on the negative path" requirement)
    - .planning/phases/27-session-connection-integrity-hardening/27-RESEARCH.md (the executor precedent quote + the A4 empirical-verification note about which of the 3 tests keep the env check vs move to feature-gating)
  </read_first>
  <action>
    In crates/brokerd/Cargo.toml: add a `[features]` block with `test-fixtures = []`, and add to `[dev-dependencies]` a self dev-dependency `brokerd = { path = ".", features = ["test-fixtures"] }` — copying crates/executor/Cargo.toml:22/:28/:36 verbatim, substituting `brokerd` for `executor`. This is what makes the integration tests (which link brokerd built WITHOUT cfg(test)) reach the feature-gated arm.

    In crates/brokerd/src/server.rs: extract the forced-Active mint into two sibling helper functions (the sink_schema.rs pattern), because a single match arm cannot cleanly carry two cfg-gated bodies that return different things:
    - `#[cfg(any(test, feature = "test-fixtures"))]` version: contains the CURRENT arm body INCLUDING the runtime `CAPRUN_ENABLE_IPC_CREATE_SESSION` env-gate (:923-939) and the mint (:941-993). Retaining the env-gate INSIDE the test-only arm is deliberate defense-in-depth and preserves the exact semantics of all 3 existing uds_ipc tests (esp. `create_session_over_ipc_denied_by_default_when_flag_unset`, which asserts denial when the flag is unset — that assertion must still hold with test-fixtures ON but the flag unset).
    - `#[cfg(not(any(test, feature = "test-fixtures")))]` sibling: returns the SAME `BrokerResponse::Error { message: ... }` as today's disabled-path response (:927-935 region) UNCONDITIONALLY — no `std::env::var` read at all. This is the shipped default: the mint code is physically absent.
    The `CreateSession { intent_id }` match arm calls the helper. Do NOT leave any `std::env::var("CAPRUN_ENABLE_IPC_CREATE_SESSION")` read in the featureless code path.

    Then EMPIRICALLY verify (do not assume): run `cargo test -p brokerd --no-fail-fast -- create_session` and confirm all 3 named tests still compile and pass, reaching the arm via the test-fixtures feature. If any of the 3 needs the feature added to how its binary is invoked (it should be automatic via the self dev-dep, exactly as it is for executor), resolve it here — do not silently drop coverage. Record in the SUMMARY which of the 3 keep the env-var check (all 3, per the design above).
  </action>
  <verify>
    <automated>cargo test -p brokerd --no-fail-fast -- create_session</automated>
    <automated>cargo test -p brokerd --no-fail-fast -- uds_ipc</automated>
    <automated>cargo build --workspace</automated>
    <automated>./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - crates/brokerd/Cargo.toml contains a `[features]` block with `test-fixtures = []` and a `[dev-dependencies]` line `brokerd = { path = ".", features = ["test-fixtures"] }`
    - crates/brokerd/src/server.rs contains BOTH `#[cfg(any(test, feature = "test-fixtures"))]` and `#[cfg(not(any(test, feature = "test-fixtures")))]` on the two CreateSession helper siblings
    - The featureless sibling contains NO `std::env::var("CAPRUN_ENABLE_IPC_CREATE_SESSION")` read and returns a BrokerResponse::Error
    - `cargo test -p brokerd --no-fail-fast -- create_session` exits 0 with the 3 named tests present and passing (server_accept, create_session_round_trip, create_session_over_ipc_denied_by_default_when_flag_unset)
    - `cargo build --workspace` exits 0; `./scripts/check-invariants.sh` exits 0
    - No new `#[ignore]` in uds_ipc.rs (grep)
  </acceptance_criteria>
  <done>brokerd has a test-fixtures feature + self dev-dep (executor precedent); the CreateSession mint is a cfg-gated sibling present only under test/test-fixtures, with a featureless sibling returning the same Error; the 3 uds_ipc tests still exercise the arm, verified by running them.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 featureless behavioral negative gate — a default build denies CreateSession even with the env flag set</name>
  <files>cli/caprun/tests/harden04_featureless_create_session.rs</files>
  <read_first>
    - crates/brokerd/tests/uds_ipc.rs (how to open a UDS connection and send a BrokerRequest::CreateSession, and read the BrokerResponse — reuse the client idiom)
    - cli/caprun/Cargo.toml (:9 `brokerd = { path = "../../crates/brokerd" }` — no features; caprun's own test graph links brokerd featureless when built scoped)
    - planning-docs/DESIGN-security-hardening.md §d and §j (D-10 is the behavioral negative gate; the formal featureless-release-binary grep proof is Phase 30 — Phase 27 delivers the behavioral test)
    - .planning/phases/27-session-connection-integrity-hardening/27-VALIDATION.md (the D-10 row + the note to VERIFY the featureless invocation empirically, since test-fixtures is not a default feature and `cargo test --workspace` unifies dev-dep features)
    - .planning/phases/27-session-connection-integrity-hardening/27-RESEARCH.md (Anti-Patterns: symbol-inspection is only optional defense-in-depth; the primary gate is behavioral)
  </read_first>
  <action>
    Create cli/caprun/tests/harden04_featureless_create_session.rs asserting that a FEATURELESS build of the broker denies CreateSession over the socket with the fail-closed Error, even when CAPRUN_ENABLE_IPC_CREATE_SESSION=1 is set — proving the mint arm is physically absent, not merely runtime-denied.

    IMPORTANT (Cargo feature unification): under `cargo test --workspace`, brokerd's self dev-dep (Task 1) enables `test-fixtures` graph-wide, so this test must obtain a genuinely featureless broker OUT OF BAND, not by linking brokerd in the ambient test graph. Implement it as a SHELL-OUT gate: within the test, run `cargo build --release` (default features) for the broker/caprun binary in a subprocess, launch it, drive a CreateSession over its UDS with the env flag set, and assert the response is the fail-closed Error. This mirrors kickoff constraint #6 (the featureless proof is a real default `cargo build ... --release` binary, distinct from `cargo test`) and is immune to the ambient feature set.

    EMPIRICALLY DETERMINE and DOCUMENT the exact working invocation before finalizing — do not assume `--no-default-features` alone suffices (test-fixtures is not a default feature to begin with, so the meaningful axis is "not test, not test-fixtures", which a default release build already satisfies). If the shell-out release build is too slow/flaky for CI, the acceptable alternative is a scoped `cargo test -p caprun --test harden04_featureless_create_session` where caprun's graph does not request test-fixtures — but you MUST verify empirically that brokerd is featureless in that graph (e.g. the arm denies with the env flag set) and record the exact command that makes this test meaningful in the SUMMARY. Cfg-gate to Linux only if the harness genuinely needs the confinement stack; CreateSession denial itself does not require landlock, so prefer leaving it ungated so it also proves on the Mac.

    Do NOT make the primary gate a `nm`/`strings`/`objdump` symbol scan — the DESIGN doc demotes that to optional defense-in-depth (it bit-rots across Rust/LLVM versions).
  </action>
  <verify>
    <automated>cargo test -p caprun --no-fail-fast -- harden04_featureless_create_session</automated>
    <automated>cargo test --workspace --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    - cli/caprun/tests/harden04_featureless_create_session.rs exists and drives a CreateSession over a socket against a FEATURELESS (default) broker build
    - The test sets CAPRUN_ENABLE_IPC_CREATE_SESSION=1 and asserts the response is the fail-closed Error (no session minted) — proving physical absence, not runtime denial
    - The SUMMARY records the exact empirically-verified invocation that makes the build featureless
    - `cargo test --workspace --no-fail-fast` exits 0 (the test does not become a false failure under ambient feature unification)
    - The test does not rely on a symbol/strings scan as its primary assertion
  </acceptance_criteria>
  <done>A behavioral negative gate proves a default (featureless) broker build denies CreateSession over IPC even with the legacy env flag set, with the working featureless invocation empirically verified and documented.</done>
</task>

<task type="auto">
  <name>Task 3: SC3 grep/build absence evidence from a default release build</name>
  <files>crates/brokerd/src/server.rs</files>
  <read_first>
    - crates/brokerd/src/server.rs (confirm a stable, greppable marker inside the mint-only sibling — e.g. a distinctive comment/string or the `persist_session` call sequence — that exists ONLY in the test-fixtures sibling, so its absence in a default build is meaningful)
    - planning-docs/DESIGN-security-hardening.md §d (SC3: grep/build evidence of absence from a default release build) and §j (Phase 30 owns the formal milestone-wide proof; Phase 27 produces the phase-scoped evidence)
    - .planning/phases/27-session-connection-integrity-hardening/27-VALIDATION.md (the Manual-Only Verifications row for the release-binary absence proof)
  </read_first>
  <action>
    Produce phase-scoped SC3 evidence that the forced-Active mint is absent from a default release build. Run a default (featureless) `cargo build --workspace --release` and demonstrate the mint arm's marker is not reachable in that build configuration — the definitive check per constraint #6 is a default release build distinct from `cargo test` (which unifies dev-dep features and would re-include the arm). Record the exact command(s) and their output in the SUMMARY as the SC3 evidence.

    Choose a marker that lives ONLY in the `#[cfg(any(test, feature = "test-fixtures"))]` mint sibling (from 27-02 Task 1), so that a `cargo expand`/source-cfg check or a build-time assertion cleanly shows presence under `--features test-fixtures` and absence by default. If using `cargo expand`, note it as tooling evidence; the behavioral proof (Task 2) remains the primary gate. Do NOT claim the formal milestone-wide proof here — that is Phase 30's (§j); this task delivers the phase-27-scoped evidence only.
  </action>
  <verify>
    <automated>cargo build --workspace --release</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --release` exits 0 (default features — the shipped configuration)
    - The SUMMARY records SC3 evidence: the mint arm's marker is present under `--features test-fixtures` and absent in the default build (via `cargo expand`/source-cfg diff or an equivalent build-config demonstration)
    - The evidence explicitly distinguishes the default release build from a `cargo test` build (which unifies dev-dep features), per constraint #6
    - The task does not assert the formal milestone-wide proof (that is deferred to Phase 30)
  </acceptance_criteria>
  <done>Phase-27-scoped SC3 evidence recorded: the forced-Active mint arm is present only under the test-fixtures feature and absent from a default `cargo build --workspace --release`, with the default-build-vs-cargo-test distinction made explicit; the formal proof is handed to Phase 30.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external IPC client → broker (CreateSession) | Any process reaching the broker's UDS could ask it to mint a session; a shipped mint arm gated only at runtime is a latent authority the binary should not carry |
| compile-time feature set → shipped binary | The `test-fixtures` feature is the boundary deciding whether the mint code physically exists in the artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-27-06 | Elevation of Privilege | shipped forced-Active mint arm | high | mitigate | The mint arm is compile-excluded by a `test-fixtures` feature; a default release binary contains only the Error sibling — no runtime flag can re-enable it (SC3, D-10). |
| T-27-07 | Tampering | env-flag flip on a shipped binary | high | mitigate | The featureless sibling never reads CAPRUN_ENABLE_IPC_CREATE_SESSION; setting it to 1 on a default build still yields Error (D-10 asserts exactly this). |
| T-27-08 | Repudiation / regression | silent coverage loss when moving from env-gate to feature | medium | mitigate | The 3 existing uds_ipc tests are kept exercising the arm under test-fixtures and verified by RUNNING them (SC4), not by inspection; the SUMMARY records which keep the env check. |
| T-27-09 | (false-assurance) | cargo test masking absence via feature unification | medium | mitigate | The D-10 gate obtains a featureless build out-of-band (shell-out release build), so `cargo test`'s dev-dep feature unification cannot mask the absence proof (constraint #6). |
</threat_model>

<verification>
- `cargo build --workspace` and `cargo build --workspace --release` green (default/featureless).
- `cargo test --workspace --no-fail-fast` green on the Mac (Linux-only tests report 0 passed by design).
- `./scripts/check-invariants.sh` passes.
- The 3 named uds_ipc tests pass under `cargo test -p brokerd -- create_session` (arm reached via test-fixtures).
- D-10: a featureless build denies CreateSession with CAPRUN_ENABLE_IPC_CREATE_SESSION=1 set — behavioral proof, empirically-verified invocation recorded.
- SC3 evidence: mint marker present under --features test-fixtures, absent in the default release build; distinct from cargo test.
</verification>

<success_criteria>
The CreateSession forced-Active mint arm is compile-excluded from a default production build via the test-fixtures Cargo feature (SC3); a featureless build denies CreateSession even with the legacy env flag set (D-10); the 3 existing uds_ipc tests still exercise the arm under the feature with no coverage loss (SC4); the negative path returns the identical Error and the workspace stays green.
</success_criteria>

<output>
Create `.planning/phases/27-session-connection-integrity-hardening/27-02-SUMMARY.md` when done.
</output>
