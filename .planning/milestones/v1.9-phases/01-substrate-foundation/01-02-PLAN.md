---
phase: 01-substrate-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/brokerd/Cargo.toml
  - crates/brokerd/src/lib.rs
  - scripts/check-invariants.sh
autonomous: true
requirements:
  - REQ-api-stub-plan-node
must_haves:
  truths:
    - "`submit_plan_node(session_id: Uuid, plan: PlanNode) -> ExecutorDecision` exists in brokerd and returns ExecutorDecision::NotImplemented"
    - "The PlanNode/ValueNode shape consumed by submit_plan_node is the one locked in runtime-core (no shadow type)"
    - "`cargo test --workspace` is green and `cargo build --workspace` exits 0"
  artifacts:
    - "crates/brokerd/Cargo.toml — depends on runtime-core via path"
    - "crates/brokerd/src/lib.rs — submit_plan_node stub + a unit test asserting it returns NotImplemented"
    - "scripts/check-invariants.sh — re-runnable gate for the two architectural negative-greps"
  key_links:
    - "brokerd imports PlanNode and ExecutorDecision from runtime-core (path dependency; no circular dep, no duplicated types)"
  prohibitions:
    - "No type, struct, field, or function carrying the raw effect-request-to-sink shape may exist anywhere under crates/ — the only effect entry point is submit_plan_node taking PlanNode (Success Criterion 2, verified by grep, not tests)"
    - "submit_plan_node MUST return the typed NotImplemented value, never `todo!()`, `unimplemented!()`, or `panic!`"
---

<objective>
Add the `brokerd` crate exposing the locked broker effect API surface:
`submit_plan_node(session_id, PlanNode) -> ExecutorDecision`, returning the typed
`NotImplemented` variant. This forces every future effect path through
PlanNode/ValueNode and structurally forbids a raw effect-request-to-sink bypass
(DEC-architectural-lock-plan-nodes). A re-runnable invariant script anchors the
two grep gates the phase is verified by.

Purpose: Satisfy REQ-api-stub-plan-node and Phase 1 Success Criterion 2.
Output: brokerd crate with the stub + unit test, and a durable invariant-check script.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-substrate-foundation/01-RESEARCH.md
@.planning/phases/01-substrate-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: brokerd crate + submit_plan_node stub</name>
  <files>crates/brokerd/Cargo.toml, crates/brokerd/src/lib.rs</files>
  <read_first>
    - 01-RESEARCH.md "Pattern 5: submit_plan_node Stub" (function signature + the ExecutorDecision return) and "Anti-Patterns to Avoid" (no todo!(), no bypass helper)
    - 01-RESEARCH.md "Pitfall 2: EffectRequest Bypass Path" (brokerd in Phase 1 contains ONLY submit_plan_node)
    - crates/runtime-core/src/lib.rs re-exports (import PlanNode and ExecutorDecision from runtime-core)
  </read_first>
  <behavior>
    - submit_plan_node(any Uuid, any PlanNode) returns ExecutorDecision::NotImplemented
    - The function takes PlanNode (from runtime-core) and a session Uuid; it never panics
  </behavior>
  <action>
    Create `crates/brokerd/Cargo.toml` (inherit workspace package keys) with a path
    dependency on runtime-core (`runtime-core = { path = "../runtime-core" }`) and
    workspace deps for uuid and anyhow. The repo-root workspace `members = ["crates/*", ...]`
    glob picks up brokerd automatically — do NOT edit the root Cargo.toml.
    Create `crates/brokerd/src/lib.rs` defining a public function with the locked
    signature `pub fn submit_plan_node(_session_id: uuid::Uuid, _plan: runtime_core::PlanNode)
    -> runtime_core::ExecutorDecision` whose body returns `ExecutorDecision::NotImplemented`
    (a typed value — do NOT use todo!(), unimplemented!(), or panic!). brokerd in
    Phase 1 contains ONLY this function and its test — no other effect-related items,
    no convenience executor helper, and no type representing a raw effect+args map
    routed to a sink (Pitfall 2 / DEC-architectural-lock-plan-nodes). Document the
    no-bypass invariant in a doc comment WITHOUT writing the banned raw-request type
    name as a literal token (phrase it as "no raw effect-to-sink path"), so the
    Task 2 grep gate over crates/ stays clean.
    Add a `#[cfg(test)]` unit test `submit_plan_node_returns_not_implemented` that
    calls the function with `Uuid::new_v4()` and a minimal PlanNode (a SinkId and an
    empty args Vec) and asserts the result equals `ExecutorDecision::NotImplemented`.
  </action>
  <verify>
    <automated>cargo test -p brokerd -- submit_plan_node_returns_not_implemented 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p brokerd -- submit_plan_node_returns_not_implemented` passes
    - `cargo build --workspace` exits 0
    - submit_plan_node's return type is runtime_core::ExecutorDecision and its body contains no todo!()/unimplemented!()/panic!
    - brokerd depends on runtime-core via path; PlanNode/ExecutorDecision are imported, not redefined
  </acceptance_criteria>
  <done>brokerd exposes the locked submit_plan_node stub returning NotImplemented, with a passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Architectural invariant gate (no bypass path, workspace green)</name>
  <files>scripts/check-invariants.sh</files>
  <read_first>
    - 01-RESEARCH.md "Security-Relevant Decisions in Phase 1" (the no-bypass invariant is verified at review time by absence of the wrong type) and "Validation Architecture > Sampling Rate / Phase gate"
    - 01-RESEARCH.md "Pitfall 1" and "Pitfall 4" (the recommended CI grep checks)
  </read_first>
  <action>
    Create `scripts/check-invariants.sh` (executable, `set -euo pipefail`) that
    encodes the two Phase 1 architectural gates and exits non-zero on violation:
    (1) the raw effect-request-to-sink type/path must not exist anywhere under
    crates/ — assert `grep -r "EffectRequest" crates/` finds no matches; and
    (2) runtime-core purity — assert `grep -rE "std::io|std::fs|std::net|tokio|async fn"
    crates/runtime-core/src/` finds no matches. The script prints a clear PASS/FAIL
    line per gate. Then run the script plus `cargo build --workspace` and
    `cargo test --workspace` as the phase exit gate.
    <!-- planner-discipline-allow: EffectRequest -->
  </action>
  <verify>
    <automated>bash scripts/check-invariants.sh && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-invariants.sh` exits 0 (both gates PASS)
    - `grep -r "EffectRequest" crates/` returns no matches (Success Criterion 2 — no raw bypass type anywhere in the crate tree)
    - `cargo test --workspace` is green across runtime-core and brokerd
    - `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The architectural invariants are codified in a re-runnable script and pass; the full workspace is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → broker effect API | Future effect requests enter only via submit_plan_node(PlanNode); no other entry point may exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation | brokerd effect API surface | high | mitigate | The only effect entry point is submit_plan_node taking PlanNode/ValueNode; no raw effect-request-to-sink type or helper exists. Enforced structurally and verified by `grep -r "EffectRequest" crates/` returning nothing (scripts/check-invariants.sh), not by a runtime test (DEC-architectural-lock-plan-nodes, Success Criterion 2). |
| T-01-04 | Denial of Service | submit_plan_node stub | low | accept | Stub returns NotImplemented immediately; no input parsing, no allocation beyond the moved PlanNode. Real DoS surface arrives with the Phase 4 executor; nothing to mitigate in the stub. |
</threat_model>
<!-- planner-discipline-allow: EffectRequest -->

<verification>
- `cargo test --workspace` green
- `bash scripts/check-invariants.sh` exits 0
- `grep -r "EffectRequest" crates/` returns nothing
- submit_plan_node returns ExecutorDecision::NotImplemented (unit test)
</verification>

<success_criteria>
REQ-api-stub-plan-node is satisfied: submit_plan_node exists with the locked
shape and returns NotImplemented; the PlanNode/ValueNode shape is locked in
runtime-core; and there is no raw effect-request-to-sink path anywhere in the
crate tree (Success Criterion 2).
</success_criteria>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols and paths (source-grounding exclusion list):

Functions: submit_plan_node (in crates/brokerd).
New paths: crates/brokerd/ (Cargo.toml, src/lib.rs), scripts/check-invariants.sh.
Consumes (does not redefine): PlanNode, ValueNode, SinkId, ExecutorDecision from
runtime-core (created in plan 01-01).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-substrate-foundation/01-02-SUMMARY.md` when done.
</output>
