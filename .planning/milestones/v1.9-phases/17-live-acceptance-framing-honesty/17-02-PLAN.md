---
phase: 17-live-acceptance-framing-honesty
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_3.rs
autonomous: true
requirements: [ACCEPT-01]
user_setup:
  - service: mailpit
    why: "Live SMTP capture + HTTP API assertions for the composed acceptance run — supplied by scripts/mailpit-verify.sh; no human dashboard action, listed so the executor never runs the bare `docker run rust:1` recipe"
    env_vars:
      - name: CAPRUN_SMTP_HOST
        source: "Set automatically by scripts/mailpit-verify.sh to the resolved Mailpit sidecar container IP (NOT the literal 'mailpit')"

must_haves:
  truths:
    - "ONE shared audit.db file backs 3 sequential caprun sessions (confirm-leg hostile-block, deny-leg hostile-block, clean-control), opened ONCE at the end for the sweep (COORD-T5, COORD-INTERP)."
    - "Tooth #1: verify_chain is true for EACH of the 3 session_id rows, enumerated via `SELECT id FROM sessions ORDER BY rowid` — never a single `LIMIT 1` call against the multi-session file (COORD-T1)."
    - "Tooth #3: the clean-control leg actually DELIVERS — an email_send_succeeded event + a Mailpit capture for its unique recipient, ZERO sink_blocked events, and pending_confirmations == 0 for its session (COORD-T3)."
    - "Tooth #4 (confirm half): the confirm leg sends exactly once to ITS OWN nonced recipient — Mailpit count == 1 for that recipient + an email_send_succeeded event (COORD-T4)."
    - "Tooth #4 (deny half): the deny leg sends NOTHING — Mailpit count == 0 for ITS OWN (distinct) nonced recipient AND no email_send_attempted/email_send_succeeded event for that leg's effect_id (COORD-T4, non-negotiable A: both the on-the-wire count AND the ledger absence)."
    - "The confirm-leg and deny-leg hostile docs have IDENTICAL injection text and IDENTICAL derivation structure, differing ONLY in a per-run-unique nonce in the Domain fragment; neither uses the literal ev1l.com (COORD-A, revised fixture rule)."
  artifacts:
    - "cli/caprun/tests/live_acceptance_v1_3.rs — new live test file: shared-db harness, nonce-doc builder, Mailpit count helper, session/effect discovery, and the composed #[test] fn through the verify_chain sweep"
  key_links:
    - "harness nonce (Uuid::new_v4, one per hostile leg) → doc Domain fragment → worker concat → derived recipient → Mailpit recipient-scoped assertion (the isolation that makes count==0/count==1 airtight)"
    - "each caprun invocation → the SAME audit_db path arg → one shared SQLite file (never :memory:)"
    - "sink_blocked anchor.effect_id → caprun confirm/deny <effect_id> <shared-db> → confirm/deny outcome"
---

<objective>
Build the composed ACCEPT-01 live acceptance scenario in a NEW Linux-gated test
file, driving the real `caprun` binary through THREE sequential sessions that
all share ONE persistent audit.db: (1) a hostile-doc block that is CONFIRMED
and sends exactly once, (2) a SEPARATE hostile-doc block that is DENIED and
sends nothing, (3) a clean-control send that is allowed and delivered ungated.
This plan delivers the harness scaffolding and the composed scenario through
assertion teeth #1, #3, #4, #5. The HARD-GATE genuine-taint re-proof (tooth #2)
is appended by Plan 03 at a sentinel this plan leaves.

Purpose: ACCEPT-01 is the milestone's hero demo composed as ONE run. Per
17-RESEARCH.md every primitive exists individually but has never been composed
into one shared-audit.db, 3-session run with the email sink + a numeric Mailpit
count. The single biggest landmine is the `SELECT id FROM sessions LIMIT 1`
pattern, which silently targets the WRONG session once >1 session shares a file
— this plan replaces it everywhere with rowid-ordered, per-invocation discovery.

Output: `cli/caprun/tests/live_acceptance_v1_3.rs` with a green composed
scenario (teeth #1/#3/#4/#5) under scripts/mailpit-verify.sh on Linux.
</objective>

<artifacts_this_phase_produces>
Phase-wide symbol inventory (across plans 17-01 → 17-04), for the executor to check against:

New files:
- `crates/brokerd/src/provenance_proof.rs` (Plan 01) — pub module
- `cli/caprun/tests/live_acceptance_v1_3.rs` (Plans 02/03) — the composed live test

New public symbols (brokerd::provenance_proof, Plan 01):
- `pub fn assert_unbroken_edge(...)`, `pub fn genuine_derivation_binds(...)`, `pub fn union_provenance_chains(...)` (promoted from the Phase-15 test)

New test-harness symbols in `live_acceptance_v1_3.rs` (Plan 02):
- `hostile_doc_with_nonce_domain(nonce) -> Vec<u8>`, `expected_recipient(nonce) -> String`
- `run_caprun_email_on(recipient, content, audit_db, tag) -> bool` (email-sink variant of run_caprun_block)
- `run_caprun_verb(verb, effect_id, audit_db) -> Option<i32>` (confirm/deny/review runner)
- `mod mailpit_client` incl. new `pub fn count_messages_for_recipient(host, recipient) -> usize`
- `all_session_ids(conn) -> Vec<String>`, `latest_session_id(conn) -> String`, `discover_latest_blocked_effect_id(audit_db) -> Uuid` (LIMIT-1-free discovery)
- `#[test] fn live_acceptance_v1_3_composed()` (the composed scenario; tooth #2 appended by Plan 03)
- `#[test] fn live_acceptance_v1_3_guard_binary_present()` (macOS guard)

PROJECT.md edits (Plan 04): a "What v1.3's live proof does and does not claim (DOC-01)" paragraph in the Current-Milestone-v1.3 section (points 1/2/6/7/8 + revised framing + nonce honesty line + scope sentence) and a "v1.3 residual risks" clause extending the existing Residual-risks bullet (points 3/5/4). No new top-level section.

SUMMARY line (Plan 03): the COORD-SUMMARY "one unbroken DAG = per-session verify_chain across shared log, NOT cross-session chain" sentence in 17-03-SUMMARY.md.
</artifacts_this_phase_produces>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-live-acceptance-framing-honesty/17-RESEARCH.md
@cli/caprun/tests/s9_live_block.rs
@cli/caprun/tests/live_acceptance_tainted_session.rs
@./CLAUDE.md
</context>

<coordinator_gate>
Executed under caprun-opus-77's locked Option-A ruling (FAMP thread
019f3d72-dbce-7d23-91f4-bd129978fcf4). The fixture rule is the REVISED one
(nonce in the Domain fragment; NOT byte-identical). Any FORCED design deviation
discovered during execution → STOP and flag caprun-opus-77, do NOT self-resolve.
Watch (do not "fix"): the recurring GSD bug where the final-wave executor's own
doc-completion commit flips ROADMAP.md's PHASE-level checkbox before independent
verification (2-for-2 across Phases 15/16) — this is the final phase of the
milestone, highest-stakes recurrence; diff-check ROADMAP.md after the final
merge, do not mark the phase-level box.
</coordinator_gate>

<tasks>

<task type="auto">
  <name>Task 1: Shared-DB email harness, per-run nonce-doc builder, Mailpit count helper, and LIMIT-1-free session/effect discovery</name>
  <read_first>
    - cli/caprun/tests/live_acceptance_tainted_session.rs (the existing shared-audit-db 2-invocation pattern: `run_caprun_block(tmp, audit_db)` takes the shared path as a param; `run_caprun_verb(verb, effect_id, audit_db)` for confirm/deny — the exact shapes to extend to the email sink)
    - cli/caprun/tests/s9_live_block.rs (the `mod mailpit_client` module lines 624-775: `host()`, `message_ids`, `detail_addressed_to`, `wait_for_recipient_captured`, `assert_recipient_not_captured`; the `HOSTILE_EMAIL_CONTENT` const lines 76-80 and `CLEAN_PATH_CONTENT` lines 60-61 — the structural fixtures to adapt; note the `accounts@ev1l.com` fixed literal is RESERVED by `s9_control_ab_taint_driven`:893 and must NOT be produced by this file)
    - crates/brokerd/src/quarantine.rs lines 142-199 (`looks_like_doc_fragment` = `!empty && !contains('@')`, `extract_doc_fragments` matches whitespace tokens after `Reply-To:`/`Domain:`, `concat_doc_fragments(local, domain)` = `format!("{local}@{domain}")` — confirms a `Domain: <uuid>.ev1l.test` token derives to `accounts@<uuid>.ev1l.test`)
    - crates/brokerd/src/audit.rs (pub `open_audit_db`, `find_event_by_type`, `verify_chain`)
  </read_first>
  <files>cli/caprun/tests/live_acceptance_v1_3.rs</files>
  <action>
    Create the new Linux-gated test file `cli/caprun/tests/live_acceptance_v1_3.rs`.
    Add a module header doc comment stating this is the composed ACCEPT-01
    run and that all live bodies are `#[cfg(target_os = "linux")]` (macOS
    excludes them). Include an always-compiled guard test
    (`live_acceptance_v1_3_guard_binary_present`, mirroring
    `s9_live_block.rs`:900-907) so the file is meaningful on the macOS box.

    Build the following harness pieces (all `#[cfg(target_os = "linux")]`):

    1. Per-run NONCE + hostile-doc builder (COORD-A revised fixture rule,
       constraints COORD-N1..N4). A function
       `hostile_doc_with_nonce_domain(nonce: &uuid::Uuid) -> Vec<u8>` returning
       bytes IDENTICAL to `HOSTILE_EMAIL_CONTENT` in every line EXCEPT the
       `Domain:` line, which becomes `Domain: {nonce}.ev1l.test`. Keep
       `Reply-To: accounts` and the `Body:` line byte-identical to the existing
       fixture. The derived recipient for a given nonce is therefore
       `accounts@{nonce}.ev1l.test` — compute the expected recipient with a sibling
       `expected_recipient(nonce: &uuid::Uuid) -> String` returning exactly that,
       so assertions never re-parse the doc. Source of per-run uniqueness
       (COORD-N3): mint each leg's nonce with `uuid::Uuid::new_v4()` ONCE at the
       top of the composed test (a distinct value per hostile leg, confirm-nonce
       != deny-nonce) — NOT wall-clock, NOT per-call; the nonce is reused for that
       leg's doc build and its Mailpit assertion within the same run.

    2. `run_caprun_email_on(recipient: &str, content: &[u8], audit_db: &std::path::Path, tag: &str) -> bool`
       — spawns `caprun send-email-summary <recipient> <workspace-file> <audit_db>`
       writing `content` to a workspace file under `audit_db.parent()`, against
       the CALLER-SUPPLIED shared `audit_db` path (never minted per-call, never
       :memory:). Returns `output.status.success()`. Model on
       `live_acceptance_tainted_session.rs`'s `run_caprun_block` shape but for the
       email sink. eprintln stdout/stderr for debugging.

    3. Reuse the confirm/deny CLI runner: add
       `run_caprun_verb(verb: &str, effect_id: uuid::Uuid, audit_db: &std::path::Path) -> Option<i32>`
       identical to `live_acceptance_tainted_session.rs`:95-112 (returns the
       process exit code).

    4. A `mod mailpit_client` for this file: reuse `s9_live_block.rs`'s module
       verbatim (host/message_ids/detail_addressed_to/wait_for_recipient_captured
       — same chunked-decode gotcha handling; do NOT re-derive the chunked decode)
       AND add the new helper (COORD-T4 numeric primitive):
       `pub fn count_messages_for_recipient(host: &str, recipient: &str) -> usize`
       = `message_ids(host).iter().filter(|id| detail_addressed_to(host, id, recipient)).count()`.
       Note: `detail_addressed_to` and `message_ids` are currently file-private in
       s9_live_block — reproduce them in THIS file's module (deliberate small
       duplication, as s9_live_block itself duplicates from
       email_smtp_acceptance.rs; the two test binaries cannot share a module).

    5. Session/effect discovery that is SAFE for a multi-session shared DB
       (17-RESEARCH.md Pitfall 1 — replaces the LIMIT-1 anti-pattern):
       - `fn all_session_ids(conn: &rusqlite::Connection) -> Vec<String>` using
         `SELECT id FROM sessions ORDER BY rowid`.
       - `fn latest_session_id(conn: &rusqlite::Connection) -> String` using
         `SELECT id FROM sessions ORDER BY rowid DESC LIMIT 1`, called IMMEDIATELY
         after an invocation to capture "the session just created."
       - `fn discover_latest_blocked_effect_id(audit_db: &std::path::Path) -> uuid::Uuid`
         that opens the DB, gets `latest_session_id`, finds that session's
         `sink_blocked` event via `find_event_by_type`, and returns
         `blocked.anchors.first().effect_id`.
       Do NOT emit any `SELECT id FROM sessions LIMIT 1` (no ORDER BY) anywhere in
       this file.
  </action>
  <verify>
    <automated>cargo build --workspace --tests</automated>
  </verify>
  <done>`cli/caprun/tests/live_acceptance_v1_3.rs` compiles under `cargo build --workspace --tests` on macOS (live bodies cfg-excluded, guard test present); `grep -n "SELECT id FROM sessions LIMIT 1" cli/caprun/tests/live_acceptance_v1_3.rs` returns nothing; `hostile_doc_with_nonce_domain`, `expected_recipient`, `run_caprun_email_on`, `run_caprun_verb`, `count_messages_for_recipient`, `all_session_ids`, `discover_latest_blocked_effect_id` are all defined.</done>
</task>

<task type="auto">
  <name>Task 2: The composed 3-session shared-DB #[test] with teeth #1, #3, #4, #5</name>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs `s9_control_ab_taint_driven` (lines 795-894 — the clean-half assertions to reproduce INSIDE this composed run: plan_node_evaluated present, no sink_blocked, pending_confirmations == 0, email_send_succeeded, Mailpit capture for a unique recipient)
    - cli/caprun/tests/live_acceptance_tainted_session.rs `live_acceptance_confirm_path`/`live_acceptance_deny_path` (the block→discover-effect_id→confirm/deny sequencing against a shared persistent DB, exit-code contract: confirm=0/Released, deny=2/Denied)
    - Task 1's harness (this file) — the helpers this task composes
  </read_first>
  <files>cli/caprun/tests/live_acceptance_v1_3.rs</files>
  <action>
    Add one `#[cfg(target_os = "linux")] #[test]`
    `fn live_acceptance_v1_3_composed()`. Mint ONE tmp dir and ONE
    `audit_db = tmp.join("audit.db")` path (NEVER :memory:) shared by ALL
    invocations below (COORD-T5). Mint two distinct per-run nonces
    `confirm_nonce`/`deny_nonce` via `Uuid::new_v4()` and a unique clean recipient
    `format!("v13clean-{}@example.test", Uuid::new_v4())`.

    Sequence (all against the SAME `audit_db`):
    (A) CONFIRM LEG — hostile block then confirm (COORD-T4 confirm half):
      - `run_caprun_email_on("ops@company.example", &hostile_doc_with_nonce_domain(&confirm_nonce), &audit_db, "v13_confirm_block")`
        must return false (blocked, non-zero exit).
      - `discover_latest_blocked_effect_id(&audit_db)` → confirm effect_id.
      - `run_caprun_verb("confirm", eid, &audit_db)` must be `Some(0)` (Released).
      - Assert `count_messages_for_recipient(&host, &expected_recipient(&confirm_nonce)) == 1`
        (poll via `wait_for_recipient_captured` FIRST to avoid a delivery race,
        then assert the count is exactly 1) AND that session has an
        `email_send_succeeded` event.
    (B) DENY LEG — a SEPARATE hostile block then deny (COORD-T4 deny half; the
        deny/confirm mutual-exclusivity means a fresh block is required —
        17-RESEARCH.md Pitfall 2):
      - `run_caprun_email_on("ops@company.example", &hostile_doc_with_nonce_domain(&deny_nonce), &audit_db, "v13_deny_block")`
        must return false (blocked).
      - `discover_latest_blocked_effect_id(&audit_db)` → deny effect_id
        (`deny_eid`). Capture this leg's `session_id` immediately (latest_session_id).
      - `run_caprun_verb("deny", deny_eid, &audit_db)` must be `Some(2)` (Denied).
      - Assert BOTH halves of "sends nothing" (non-negotiable A): (i)
        `count_messages_for_recipient(&host, &expected_recipient(&deny_nonce)) == 0`
        (recipient-scoped to the deny leg's OWN nonce, never a bare whole-inbox
        len==0), AND (ii) no `email_send_attempted` and no `email_send_succeeded`
        event exists whose actor is `sink:email.send:{deny_eid}` for the deny
        session (query the deny session's events and assert neither event type
        carries that effect_id in its actor).
    (C) CLEAN CONTROL LEG (COORD-T3):
      - `run_caprun_email_on(&clean_recipient, CLEAN_PATH_CONTENT, &audit_db, "v13_clean")`
        must return true (allowed). (Reuse the `CLEAN_PATH_CONTENT` bytes from
        s9_live_block by re-declaring the const in this file — it carries no
        Reply-To:/Domain: markers, so no recipient is derived and the trusted CLI
        recipient routes to `to`.)
      - For the clean session (latest_session_id right after): assert a
        `plan_node_evaluated` event exists, NO `sink_blocked` event, `SELECT
        COUNT(*) FROM pending_confirmations WHERE session_id = ?1` == 0, an
        `email_send_succeeded` event exists, AND
        `wait_for_recipient_captured(&host, &clean_recipient)`.

    Then the END-OF-RUN SWEEP (teeth #1 and #5): open the shared `audit_db` ONCE,
    and for EVERY sid in `all_session_ids(&conn)` assert `verify_chain(&conn, &sid)`
    is true (COORD-T1 — per-session, enumerated, never LIMIT 1). Assert
    `all_session_ids(&conn).len() == 3` (exactly the three composed sessions).

    Leave a sentinel comment on its own line immediately before the function's
    closing brace, exactly:
    `    // TEETH-2-INSERTION-POINT: genuine-taint re-proof appended by Plan 03 (17-03)`
    so Plan 03 can append tooth #2 without re-deriving the function.

    Do NOT place any fenced code block or the literal `accounts@ev1l.com` in this
    file (the clean + hostile-nonce recipients are the only recipients this file
    ever produces, keeping s9_control_ab_taint_driven's reserved-literal assertion
    intact — COORD-N2).
  </action>
  <verify>
    <automated>MAILPIT_VERIFY_CMD='cargo test -p caprun --test live_acceptance_v1_3 live_acceptance_v1_3_composed' bash scripts/mailpit-verify.sh</automated>
  </verify>
  <done>Under scripts/mailpit-verify.sh on Linux (capture `$?` BEFORE any pipe), `live_acceptance_v1_3_composed` passes: confirm leg count==1 + email_send_succeeded; deny leg count==0 + no attempt/succeeded event for its effect_id; clean leg delivered + zero sink_blocked + pending_confirmations==0; all 3 sessions verify_chain-true; exactly 3 session rows. The sentinel comment is present before the function's closing brace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile doc bytes → confined worker extractor → derived tainted recipient | untrusted content assembled into a sink arg; the whole point under test |
| test harness → shared Mailpit sidecar (many test binaries) | a shared listener; assertions must be recipient-scoped, never whole-inbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Spoofing/Repudiation | LIMIT-1 session lookup silently targeting the wrong session in the shared multi-session DB | high | mitigate | Replace every session lookup with `ORDER BY rowid`(-DESC) discovery captured immediately per-invocation; grep-gate the file for `LIMIT 1` with no ORDER BY |
| T-17-04 | Tampering | Cross-leg / cross-binary Mailpit pollution masking a false send (deny leg appears to "send nothing" only because the count is diluted) | high | mitigate | Distinct per-run nonce recipients per hostile leg; recipient-scoped count==0/count==1 (never whole-inbox len); deny leg additionally asserts effect_id-scoped ledger absence |
| T-17-05 | Repudiation | The reserved `accounts@ev1l.com` literal being sent from this file, breaking s9_control_ab_taint_driven's negative assertion under the shared inbox | high | mitigate | This file never produces ev1l.com; nonced domains use the distinct `.ev1l.test` TLD (COORD-N2) |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency; test-only file additions; Cargo.toml unchanged |
</threat_model>

<verification>
- Per task commit: `cargo build --workspace --tests` (macOS compile-only sanity).
- This plan's live gate: `MAILPIT_VERIFY_CMD='cargo test -p caprun --test live_acceptance_v1_3 live_acceptance_v1_3_composed' bash scripts/mailpit-verify.sh` on Linux (Colima+Docker), capturing `$?` BEFORE any pipe, asserting on the named test + pass count — never a bare exit 0.
</verification>

<success_criteria>
`live_acceptance_v1_3_composed` is green on real Linux: three sessions share one
audit.db; the confirm leg sends exactly once (count==1, email_send_succeeded);
the deny leg sends nothing (count==0 for its own nonce recipient AND no
attempt/succeeded ledger event for its effect_id); the clean control delivers
ungated (pending_confirmations==0, no sink_blocked); every session's verify_chain
is true; the Plan-03 insertion sentinel is present.
</success_criteria>

<output>
Create `.planning/phases/17-live-acceptance-framing-honesty/17-02-SUMMARY.md` when done.
</output>
