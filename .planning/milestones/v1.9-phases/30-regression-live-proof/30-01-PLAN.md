---
phase: 30-regression-live-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-harden04-featureless.sh
  - .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md
autonomous: true
requirements: [HARDEN-06]
must_haves:
  truths:
    - "Running `bash scripts/verify-harden04-featureless.sh` drives the `featureless_create_session_denied_even_with_flag_set` assertion through its real (non-skip) branch, and exits NON-ZERO if the assertion was self-skipped instead of executed (criterion 4)."
    - "Every Phase 27/28/29 test file is independently swept for `#[ignore]`, weakened assertions, and stale/self-triggering skip conditions; any weakened test is surfaced as a gap to fix, never documented-around (HARDEN-06 false-assurance guard)."
  artifacts:
    - scripts/verify-harden04-featureless.sh
    - .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md
  key_links:
    - "verify-harden04-featureless.sh -> scripts/mailpit-verify.sh via a scoped MAILPIT_VERIFY_CMD='cargo build --workspace --release && cargo test -p caprun --test harden04_featureless_create_session' (bypasses `--workspace` feature unification so brokerd::TEST_FIXTURES_ACTIVE is false and the assertion actually runs)."
    - "self-skip sentinel in the scoped test's stdout -> wrapper non-zero exit (a green run cannot be produced by the assertion being skipped)."
---

<objective>
Close the one genuine proof gap for HARDEN-06 (criterion 4) and run the false-assurance
regression audit. 4 of the 5 phase success criteria already have shipped, passing tests
(criteria 1/2/3/5 — cite + re-run in Plan 30-02); this plan produces the ONE new executable
artifact the phase needs plus the audit that guards against a silently-weakened test.

Purpose: Criterion 4's existing negative test (`harden04_featureless_create_session`)
SELF-SKIPS under the bare `cargo test --workspace` recipe because Cargo unifies `brokerd`'s
`test-fixtures` feature workspace-wide, so `brokerd::TEST_FIXTURES_ACTIVE` is always true there
and the D-10 assertion never executes. DESIGN §d/§j pin the fix: a genuinely featureless build
plus a scoped test invocation that actually exercises the assertion. This plan formalizes that
into a standalone script (per RESEARCH Open Question 1: standalone, NOT an edit to the shared
mailpit-verify.sh — keeps every other phase's use of the bare recipe unchanged).

Output: `scripts/verify-harden04-featureless.sh` and
`.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-regression-live-proof/30-RESEARCH.md
@.planning/phases/30-regression-live-proof/30-VALIDATION.md
@planning-docs/DESIGN-security-hardening.md
@scripts/mailpit-verify.sh
@cli/caprun/tests/harden04_featureless_create_session.rs
@.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md
</context>

<artifacts_this_phase_produces>
NEW files created in this phase (both in this plan):
- `scripts/verify-harden04-featureless.sh` — standalone featureless-build + scoped-test proof for HARDEN-04 (criterion 4). Formalizes the DESIGN §d/§j-pinned invocation the bare recipe cannot provide.
- `.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md` — independent re-confirmation sweep of all Phase 27/28/29 test files (mirrors Phase 25's `25-REGRESSION-AUDIT.md`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-harden04-featureless.sh (criterion 4 formalized proof)</name>
  <files>scripts/verify-harden04-featureless.sh</files>
  <read_first>
    - scripts/mailpit-verify.sh (the MAILPIT_VERIFY_CMD override mechanism, the trap/cleanup pattern, the `set -euo pipefail` header, the PASSED sentinel it emits)
    - cli/caprun/tests/harden04_featureless_create_session.rs (lines 150-240: the `brokerd::TEST_FIXTURES_ACTIVE` self-skip branch and its exact skip message, and the real `D-10 VIOLATION` assertion)
    - planning-docs/DESIGN-security-hardening.md (§d lines 471-512; §j lines 822-841 — the pinned featureless-release build + capture-$?-before-pipe disciplines)
    - .planning/phases/30-regression-live-proof/30-RESEARCH.md (Patterns 2 and 3; Per-Criterion Verdict Table row 4)
    - CLAUDE.md ("Linux-only security tests" + the Phase 16 Mailpit-gate mandate)
  </read_first>
  <action>
    Create a new standalone host-side wrapper `scripts/verify-harden04-featureless.sh` (do NOT
    edit `scripts/mailpit-verify.sh` — per RESEARCH Open Question 1, standalone avoids churning
    the shared script every other phase depends on). Start with `#!/usr/bin/env bash` and
    `set -euo pipefail`. The script MUST:

    (1) Delegate the actual Linux run to the existing recipe by invoking `bash
    scripts/mailpit-verify.sh` with a scoped override:
    `MAILPIT_VERIFY_CMD='cargo build --workspace --release && cargo test -p caprun --test harden04_featureless_create_session'`.
    The `cargo build --workspace --release` leg is the genuinely featureless production artifact
    pinned by DESIGN §j (release profile builds no test targets, so no dev-dep feature
    unification); the `cargo test -p caprun --test harden04_featureless_create_session` leg is
    `-p caprun`-scoped (NOT `--workspace`), which excludes brokerd's own test targets from the
    build plan so `brokerd::TEST_FIXTURES_ACTIVE` resolves to false and the D-10 assertion runs
    for real instead of taking the self-skip branch.

    (2) Redirect the delegated run's combined stdout+stderr to a log file (e.g. under `"${TMPDIR:-/tmp}"`)
    and capture the TRUE exit status into a variable IMMEDIATELY after the command, BEFORE any
    pipe/tail/grep (this project's standing `verification-exit-code-through-pipe` discipline — a
    `cmd | tail` returns tail's status, always 0).

    (3) FALSE-ASSURANCE GUARD (the load-bearing part): after the run, inspect the captured log
    and FAIL (exit non-zero) if the scoped test took its self-skip branch rather than executing
    the assertion. Detect the skip by grepping the log for the sentinel phrase the test prints
    when `brokerd::TEST_FIXTURES_ACTIVE` is true (the message beginning with the test name
    followed by the words about skipping the D-10 negative assertion — read the exact string
    from the test file's eprintln at lines ~203-213 and match a stable substring of it). If that
    sentinel is present, print a clear failure explaining the assertion was skipped (feature
    unification was NOT bypassed) and exit 1.

    (4) POSITIVE ASSERTION: require BOTH the delegated exit status to be 0 AND the log to contain
    the passing line for `featureless_create_session_denied_even_with_flag_set` (match on the
    libtest `... ok` line for that test name). If the named test is not reported as run+passed,
    exit non-zero — do not treat a bare exit 0 as proof.

    (5) OPTIONAL defense-in-depth ONLY (do NOT gate the script's exit on it — DESIGN §d
    explicitly forbids making symbol inspection the primary gate because release
    inlining/stripping bit-rots across Rust/LLVM versions): guarded behind an env flag (e.g.
    `HARDEN04_STRINGS_CHECK=1`, default off), a `strings`/`nm` scan of the featureless release
    artifact for a unique mint-arm marker symbol, reported as informational only, never fatal.

    (6) On full success, echo a clear PASSED sentinel line naming criterion 4, and exit 0.

    Do NOT introduce the banned token `EffectRequest` anywhere (check-invariants Gate 1 — not
    under crates/, but keep the sweep-clean discipline). Keep the script surgical: it is
    verification tooling (shell), not TCB code.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/verify-harden04-featureless.sh` exits 0 (valid syntax).
    - `shellcheck scripts/verify-harden04-featureless.sh` produces no error-level (SC2xxx error) findings (warnings acceptable; if shellcheck is unavailable, note it and rely on `bash -n`).
    - `grep -q "cargo test -p caprun --test harden04_featureless_create_session" scripts/verify-harden04-featureless.sh` succeeds (scoped, non-`--workspace` invocation present).
    - `grep -q -- "--workspace --release" scripts/verify-harden04-featureless.sh` succeeds (featureless release build leg present per DESIGN §j).
    - The script captures the delegated exit status into a variable on the line immediately following the run, before any `| tail`/`| grep` (verify by reading: the `rc=$?`-style capture precedes any pipe of the log).
    - The script contains a skip-sentinel detection branch that exits non-zero when the self-skip message is found in the log (false-assurance guard present).
    - Any `strings`/`nm` symbol check is env-gated and non-fatal (the script's exit status does not depend on it).
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-harden04-featureless.sh && grep -q "cargo test -p caprun --test harden04_featureless_create_session" scripts/verify-harden04-featureless.sh && grep -q -- "--workspace --release" scripts/verify-harden04-featureless.sh</automated>
  </verify>
  <done>
    `scripts/verify-harden04-featureless.sh` exists, passes `bash -n`, delegates a scoped
    non-`--workspace` featureless run to `scripts/mailpit-verify.sh`, captures true exit before
    any pipe, fails non-zero on the self-skip sentinel, and asserts the named test passed. The
    authoritative green run is executed by the orchestrator in Plan 30-02 (real Linux); this task
    only authors and syntax-proves the script.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression-fixture audit sweep of Phase 27/28/29 test files</name>
  <files>.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md</files>
  <read_first>
    - .planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md (structure precedent: independent re-run, search commands with counts, per-site CORRECT/NEEDS-FIX verdict table)
    - .planning/phases/30-regression-live-proof/30-RESEARCH.md (Per-Criterion Verdict Table; Anti-Patterns "false-assurance regression test"; the exact test file:line anchors for criteria 2/3/5)
    - crates/brokerd/src/audit.rs (the 5 `#[cfg(test)]` chain tests: forge/truncation/legacy/orphan/key-dependence)
    - crates/brokerd/tests/harden01_session_integrity.rs (fd-release demotion + clean-path tests)
    - crates/brokerd/tests/replay_cas.rs (Linux-gated replay-once test)
    - cli/caprun/tests/harden04_featureless_create_session.rs (the self-skip branch — must be documented as an EXPECTED, loud, non-silent skip, not a weakened test)
    - cli/caprun/tests/s9_live_block.rs (s9_control_ab_taint_driven, ~line 813)
  </read_first>
  <action>
    Create `.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md`, mirroring the
    structure of Phase 25's `25-REGRESSION-AUDIT.md`. Independently re-run (do NOT cite prior
    counts as final) the following sweeps across all Phase 27/28/29 test surfaces and record the
    exact commands + live results in the doc:

    (a) `grep -rn '#\[ignore\]' crates/ cli/ --include='*.rs' | grep -v '/target/'` — enumerate
    every ignored test; for each, give a CLEARED / NEEDS-FIX verdict with rationale (an
    `#[ignore]` on a hardening negative test is a gap to fix, per CLAUDE.md "never soften a
    safety mechanism").

    (b) Sweep for weakened assertions in the hardening test files: look for negative tests whose
    pass/skip path can be triggered by the very outcome they should catch (the false-assurance
    pattern). Explicitly evaluate `harden04_featureless_create_session`'s
    `brokerd::TEST_FIXTURES_ACTIVE` self-skip and CONFIRM it is keyed on the compiled feature set
    (not on the response variant) so a genuine D-10 regression cannot route into the skip branch —
    document WHY this is a correct guard, not a weakened test, and note that Plan 30-01 Task 1's
    script is what forces the non-skip path to run.

    (c) For each of the 5 success criteria, record the exact `file:test-name` that proves it and
    confirm the test still exists at the cited anchor (re-grep the function name; note any
    line-number drift). Give each criterion a COVERED verdict backed by the live grep.

    (d) Confirm no hardening test was converted to a warning, `assert!(true)`, commented-out
    assertion, or `return`-early stub since its origin phase's verification pass.

    Conclude with a bottom-line: either "no weakened/ignored/self-defeating hardening test found —
    regression coverage intact" OR an itemized gap list. If ANY gap is found, surface it clearly
    (it becomes fix-work, NOT something to document-around).
  </action>
  <acceptance_criteria>
    - `.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md` exists.
    - The doc records the literal `grep` command(s) run and their live result counts (independently re-run this session, not copied from RESEARCH).
    - Each of the 5 HARDEN-06 success criteria appears in the doc mapped to an exact `file:test-name` with a COVERED verdict.
    - The doc explicitly evaluates the `harden04_featureless_create_session` self-skip and states whether it is a correct feature-keyed guard or a weakened test (with reasoning).
    - The doc ends with an explicit bottom-line verdict: coverage intact, or an itemized gap list.
    - `grep -c 'NEEDS-FIX\|COVERED\|CLEARED' .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md` returns a nonzero count (verdicts present).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md && grep -q 'harden04_featureless_create_session' .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md && grep -q 'verify_chain\|replay_cas\|harden01_session_integrity' .planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md</automated>
  </verify>
  <done>
    The audit doc exists, independently re-runs the `#[ignore]`/weakened-assertion sweeps with
    live counts, maps all 5 criteria to their proving tests, adjudicates the harden04 self-skip
    as a correct guard, and states a clear bottom-line verdict. Any gap found is surfaced as
    fix-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification tooling → CI/human verdict | A green script run is trusted as proof that criterion 4 holds; if the script can go green without exercising the assertion, the boundary leaks a false PASS. |
| shipped test files → regression verdict | The audit trusts that each hardening test still has teeth; a silently-weakened test crosses this boundary undetected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Repudiation | scripts/verify-harden04-featureless.sh | high | mitigate | Script detects the self-skip sentinel in the scoped test's log and exits non-zero; a green run cannot be produced by the D-10 assertion being skipped. Positive assertion also requires the named test's `... ok` line, not a bare exit 0. |
| T-30-02 | Tampering | Phase 27/28/29 test files | medium | mitigate | Independent re-run audit sweeps for `#[ignore]`, `assert!(true)`, commented-out/early-return assertions; any weakened hardening test is surfaced as fix-work, never documented-around. |
| T-30-03 | Repudiation | featureless-binary symbol check | medium | accept | Symbol/`strings` inspection is env-gated, non-fatal, defense-in-depth only (DESIGN §d: bit-rots across Rust/LLVM; false negatives). The behavioral featureless negative test is the authoritative gate. |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new package-manager installs this phase (RESEARCH Package Legitimacy Audit: N/A — no new crate/npm/pip dependency; Cargo.lock/Cargo.toml unchanged). |
</threat_model>

<verification>
- `bash -n scripts/verify-harden04-featureless.sh` passes; the script is scoped (`-p caprun`, not `--workspace`) and includes the featureless `--release` build leg.
- The script captures true exit before any pipe and fails on the self-skip sentinel.
- `30-REGRESSION-AUDIT.md` exists with live-re-run sweep counts, per-criterion COVERED verdicts, and a bottom-line verdict.
- No banned `EffectRequest` token introduced; no Phase 27/28/29 code edited (surgical — this plan only adds a script and a doc).
- NOTE: the authoritative real-Linux green run of the new script is executed by the orchestrator in Plan 30-02, not here.
</verification>

<success_criteria>
- `scripts/verify-harden04-featureless.sh` authored, syntactically valid, scoped-and-featureless, with a false-assurance skip guard.
- `.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md` authored with independent sweeps and a clear coverage verdict.
</success_criteria>

<output>
Create `.planning/phases/30-regression-live-proof/30-01-SUMMARY.md` when done.
</output>
