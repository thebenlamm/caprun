---
phase: 30-regression-live-proof
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - .planning/phases/30-regression-live-proof/30-VERIFICATION.md
autonomous: false
requirements: [HARDEN-06]
must_haves:
  truths:
    - "The bare `bash scripts/mailpit-verify.sh` recipe runs green on real Linux with a freshly-recorded pass/fail tally that supersedes the stale v1.5 baseline (309/0) — no regression to v1.1-v1.5 behavior (criterion 1)."
    - "Each closed residual is proven by a cited, re-run named test: forged/truncated chain rejected by verify_chain (crit 2); replayed Allowed email.send delivers exactly once (crit 3); forced-Active CreateSession arm absent from the featureless build via scripts/verify-harden04-featureless.sh (crit 4); RequestFd demotes the session while the CONTROL-01 clean path still sends (crit 5)."
    - "Human DONE sign-off is recorded in the phase VERIFICATION artifact BEFORE the final plan flips complete (this project's 'record sign-off before last plan' discipline, to avoid REQUIREMENTS.md reconciliation lag)."
  artifacts:
    - .planning/phases/30-regression-live-proof/30-VERIFICATION.md
  key_links:
    - "30-VERIFICATION.md evidence table -> each of the 5 success criteria mapped to its exact file:test-name AND its exact automated command (true-exit-before-pipe discipline)."
    - "criterion 4 evidence -> scripts/verify-harden04-featureless.sh (the artifact authored in Plan 30-01)."
---

<objective>
Execute the full HARDEN-06 live proof on real Linux and compile the per-criterion evidence
into the phase VERIFICATION artifact, then record human sign-off. This is the milestone's
final proof phase: criteria 1/2/3/5 are cite-and-re-run of already-shipped passing tests;
criterion 4 is proven by the new `scripts/verify-harden04-featureless.sh` from Plan 30-01.

Purpose: Establish the fresh v1.6 regression baseline (the last numeric tally, 309/0 across
46 suites, is stale by three hardening phases) and produce a single evidence table binding
each of the 5 success criteria to an exact file:test-name and automated command — with the
project's TRUE-exit-before-pipe and named-test-count disciplines.

Output: `.planning/phases/30-regression-live-proof/30-VERIFICATION.md` with the fresh Linux
tally, per-criterion evidence, and recorded human DONE sign-off.

IMPORTANT execution note: the live Linux gates are RUN BY THE ORCHESTRATOR (non-laundered,
true exit captured directly), NOT fabricated by the executor. The executor authors the
evidence scaffold and runs the cross-platform (Mac-runnable) unit tier; the orchestrator's own
gate run supplies the real-Linux results and the human sign-off.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-regression-live-proof/30-RESEARCH.md
@.planning/phases/30-regression-live-proof/30-VALIDATION.md
@.planning/phases/30-regression-live-proof/30-REGRESSION-AUDIT.md
@scripts/mailpit-verify.sh
@scripts/verify-harden04-featureless.sh
@planning-docs/DESIGN-security-hardening.md
</context>

<artifacts_this_phase_produces>
NEW file created in this plan:
- `.planning/phases/30-regression-live-proof/30-VERIFICATION.md` — the phase's per-criterion evidence table, fresh v1.6 regression baseline tally, and recorded human DONE sign-off.

(Consumes the two Plan 30-01 artifacts: `scripts/verify-harden04-featureless.sh` and `30-REGRESSION-AUDIT.md`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author 30-VERIFICATION.md evidence scaffold + run the cross-platform unit tier</name>
  <files>.planning/phases/30-regression-live-proof/30-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/30-regression-live-proof/30-RESEARCH.md (Per-Criterion Verdict Table; Validation Architecture "Phase Requirements -> Test Map" — the exact commands and file:test anchors)
    - .planning/phases/30-regression-live-proof/30-VALIDATION.md (Per-Task Verification Map — the exact automated command per criterion)
    - .planning/phases/25-regression-live-proof/25-VERIFICATION.md (structure precedent for a proof-phase VERIFICATION doc)
    - crates/brokerd/src/audit.rs (criterion 2 unit tests — these ARE cross-platform / Mac-runnable)
    - planning-docs/DESIGN-security-hardening.md (§j capture-$?-before-pipe + PASSED-sentinel disciplines)
  </read_first>
  <action>
    Create `.planning/phases/30-regression-live-proof/30-VERIFICATION.md` with a per-criterion
    evidence table (one row per success criterion 1-5). Each row MUST record: the criterion text,
    the exact proving `file:test-name`, the exact automated command, and a RESULT cell left as a
    placeholder pending the orchestrator's real-Linux run (Task 2) EXCEPT where the executor can
    legitimately fill it now. Use these exact commands (from RESEARCH/VALIDATION — do not
    paraphrase):

    - Criterion 1 (full regression, real Linux): `bash scripts/mailpit-verify.sh` — record the
      fresh pass/fail suite tally as the new v1.6 baseline (supersedes 309/0). Orchestrator-run.
    - Criterion 2 (forged/truncated chain rejected): `cargo test -p brokerd --lib self_consistent_forgery_without_key_is_rejected tail_truncation_detected_via_anchor_mismatch legacy_db_without_anchor_fails_closed`.
      These are cross-platform unit tests — RUN THEM NOW on the host and record the live
      pass count in the RESULT cell (this is the executor's automated verify for this task).
    - Criterion 3 (replay delivers once, Linux-gated): `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test replay_cas allowed_email_send_replay_delivers_once' bash scripts/mailpit-verify.sh`. Orchestrator-run.
    - Criterion 4 (featureless compile-exclusion): `bash scripts/verify-harden04-featureless.sh` (the Plan 30-01 artifact). Orchestrator-run.
    - Criterion 5 (fd-release demotion + clean path): `cargo test -p brokerd --test harden01_session_integrity` PLUS `MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_live_block s9_control_ab_taint_driven' bash scripts/mailpit-verify.sh`. Cite BOTH `fd_grant_on_untrusted_path_demotes_without_report_claims`/`fd_grant_on_trusted_path_stays_active` AND `s9_control_ab_taint_driven` (complementary per RESEARCH Pitfall 3 — do not call criterion 5 done on one alone). Orchestrator-run for the Linux legs.

    At the top of the doc, restate the two standing disciplines the orchestrator MUST apply when
    filling the RESULT cells in Task 2: (a) capture `$?` into a variable IMMEDIATELY after each
    gate command, BEFORE any `| tail`/`| grep`; (b) assert on the PASSED sentinel + named-test
    counts, never on exit-0-through-a-pipe. Add a "cfg(linux) test-blindness" note: a green macOS
    run proves nothing about the Linux-gated tests (replay_cas, s9_live_block, harden04); only the
    bare recipe on real Linux is authoritative.

    Leave a "Human sign-off" section with an explicit unchecked line to be filled in Task 2 BEFORE
    the plan is marked complete.
  </action>
  <acceptance_criteria>
    - `.planning/phases/30-regression-live-proof/30-VERIFICATION.md` exists with a 5-row per-criterion evidence table.
    - Each row contains the exact automated command as listed above (verify criterion 4's row contains `bash scripts/verify-harden04-featureless.sh`; criterion 1's row contains the bare `bash scripts/mailpit-verify.sh`).
    - Criterion 2's RESULT cell is filled with a live host run: `cargo test -p brokerd --lib self_consistent_forgery_without_key_is_rejected tail_truncation_detected_via_anchor_mismatch legacy_db_without_anchor_fails_closed` reports 3 passed / 0 failed.
    - Criterion 5's row cites BOTH the harden01 fd-demotion tests AND `s9_control_ab_taint_driven`.
    - The doc restates the capture-$?-before-pipe and cfg(linux)-test-blindness disciplines, and contains an unchecked human sign-off line.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/30-regression-live-proof/30-VERIFICATION.md && grep -q "verify-harden04-featureless.sh" .planning/phases/30-regression-live-proof/30-VERIFICATION.md && grep -q "s9_control_ab_taint_driven" .planning/phases/30-regression-live-proof/30-VERIFICATION.md && cargo test -p brokerd --lib self_consistent_forgery_without_key_is_rejected tail_truncation_detected_via_anchor_mismatch legacy_db_without_anchor_fails_closed</automated>
  </verify>
  <done>
    `30-VERIFICATION.md` exists with the 5-criterion evidence table, exact commands, the
    cross-platform criterion-2 result filled from a live host run, the criterion-5 dual citation,
    the pipe/cfg-linux disciplines restated, and an unchecked human sign-off line awaiting Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator-run real-Linux live proof + human DONE sign-off (v1.6 milestone-close gate)</name>
  <action>STOP. This is a blocking milestone-close gate. The ORCHESTRATOR (not the executor) runs each Linux gate directly, non-laundered, capturing the true exit before any pipe, and fills the RESULT cells in 30-VERIFICATION.md. Do NOT self-approve regardless of auto_advance — this is the v1.6 DONE sign-off. The executor must NOT fabricate a real-Linux pass.</action>
  <what-built>
    Plan 30-01 authored `scripts/verify-harden04-featureless.sh` and `30-REGRESSION-AUDIT.md`;
    Task 1 of this plan authored `30-VERIFICATION.md` with the per-criterion evidence table,
    exact commands, and the cross-platform criterion-2 result. What remains is the authoritative
    real-Linux proof and human sign-off — RUN BY THE ORCHESTRATOR (non-laundered), because the
    executor cannot independently produce a trustworthy real-Linux pass (cfg(linux)
    test-blindness) and MUST NOT fabricate one.
  </what-built>
  <how-to-verify>
    Orchestrator runs each gate directly, capturing `$?` into a variable IMMEDIATELY after the
    command and BEFORE any `| tail`/`| grep`, then records the named-test counts (never exit-0-
    through-a-pipe) into `30-VERIFICATION.md`:

    1. Criterion 1 — full regression baseline: `bash scripts/mailpit-verify.sh`. Record the fresh
       total passed/failed suite tally as the new v1.6 baseline (must be 0 failed; the count
       supersedes the stale 309/0). Confirm the "PASSED" sentinel line.
    2. Criterion 4 — featureless compile-exclusion: `bash scripts/verify-harden04-featureless.sh`.
       Confirm it exits 0 AND did NOT take the self-skip branch (the script fails non-zero on the
       skip sentinel by design) AND `featureless_create_session_denied_even_with_flag_set` is
       reported ok.
    3. Criterion 3 — replay-once: `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test replay_cas allowed_email_send_replay_delivers_once' bash scripts/mailpit-verify.sh`. Confirm 1 passed.
    4. Criterion 5 — fd-release demotion + clean path: `cargo test -p brokerd --test harden01_session_integrity` (host or Linux) AND `MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_live_block s9_control_ab_taint_driven' bash scripts/mailpit-verify.sh` on Linux. Confirm the demotion tests AND `s9_control_ab_taint_driven` pass.
    5. Criterion 2 — already filled from the host run in Task 1; re-confirm it also passes under
       the bare recipe's full run in step 1.

    Fill every RESULT cell with the true exit + named-test counts. Then, ONLY once all 5 criteria
    are green, write the human DONE sign-off line into `30-VERIFICATION.md` BEFORE this plan flips
    complete (record-sign-off-before-last-plan discipline), and let `phase.complete` reconcile
    REQUIREMENTS.md HARDEN-06.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 criteria are recorded green on real Linux and the human sign-off is written into 30-VERIFICATION.md, or describe the failing criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor claim → recorded evidence | An executor cannot produce a trustworthy real-Linux pass (cfg(linux) test-blindness); a fabricated or macOS-only "pass" crossing into the evidence table is a false proof. |
| gate command → PASS verdict | A piped gate command returns the pipe's status (always 0); trusting it crosses the boundary with a false PASS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Repudiation | Linux gate invocation | high | mitigate | Capture `$?` into a variable immediately after each gate command, before any pipe; assert on the PASSED sentinel + named-test counts, never exit-0-through-a-pipe (documented project incident, nearly shipped a false PASS at Phase 15). |
| T-30-05 | Repudiation | cfg(linux)-gated tests | high | mitigate | Authoritative gate is the bare recipe on real Linux; macOS `cargo test` green is explicitly treated as non-authoritative in the doc (only criterion-2 unit tests are legitimately host-runnable). |
| T-30-06 | Spoofing | executor-reported results | high | mitigate | Live Linux gates are orchestrator-run (non-laundered); the executor authors only the scaffold + the cross-platform unit tier and MUST NOT fill Linux RESULT cells itself. Enforced via the blocking human-verify checkpoint. |
| T-30-07 | Repudiation | REQUIREMENTS.md reconciliation | medium | mitigate | Human DONE sign-off is written into 30-VERIFICATION.md BEFORE the final plan flips complete; `phase.complete` then reconciles HARDEN-06 (avoids the fresh-verifier gaps_found lag seen at Phases 25/27/28). |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new package-manager installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `30-VERIFICATION.md` exists with a 5-criterion evidence table, exact commands, and the criterion-2 host result filled from a live run.
- Orchestrator's real-Linux run records: criterion 1 fresh baseline (0 failed, new tally); criteria 3/4/5 named tests green on Linux; criterion 4 did not self-skip.
- Human DONE sign-off written into the artifact before the plan flips complete.
- All results captured with true-exit-before-pipe + named-test-count discipline.
</verification>

<success_criteria>
- All 5 HARDEN-06 success criteria recorded green with exact file:test-name + command evidence, real-Linux authoritative.
- Fresh v1.6 regression baseline tally recorded (supersedes 309/0), 0 failures.
- Human sign-off recorded in 30-VERIFICATION.md before completion.
</success_criteria>

<output>
Create `.planning/phases/30-regression-live-proof/30-02-SUMMARY.md` when done.
</output>
