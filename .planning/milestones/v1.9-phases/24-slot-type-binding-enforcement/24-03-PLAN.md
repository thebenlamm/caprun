---
phase: 24-slot-type-binding-enforcement
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - crates/executor/src/sink_sensitivity.rs
  - crates/executor/src/lib.rs
  - crates/executor/tests/executor_decision.rs
autonomous: true
requirements: [T2-03, T2-05]

must_haves:
  truths:
    - "A hardcoded per-sink-arg expected-role table exists in crates/executor, scoped to email.send + file.create, mirroring sink_sensitivity.rs — not a general framework (SC2)"
    - "submit_plan_node returns a hard Denied{SlotTypeMismatch} when a resolved value's origin_role does not match its slot's expected-role list, evaluated per-arg (SC4)"
    - "A no-role (None) value at a role-checked slot is Denied — fail-closed, never a silent pass-through to Allowed (SC4)"
    - "I0/I2 precedence is unchanged: a tainted value that PASSES the role check still Blocks (I2); the Draft-session class deny (I0, Step 0.5) still fires only after the per-arg loop (SC4)"
    - "An unconstrained slot (file.create's contents, expected_role returns None) falls through unaffected — a documented, intentional scope-out, not fail-open (SC4)"
  artifacts:
    - "crates/executor/src/sink_sensitivity.rs — fn expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>"
    - "crates/executor/src/lib.rs — Step 1c inserted in submit_plan_node between Step 1b (:109) and the sensitivity check (:117)"
  key_links:
    - "expected_role returns Option<&'static [&'static str]>; the Step 1c call site MUST NOT use .unwrap_or(&[]) — that collapses None (unconstrained) and Some(&[]) and breaks the fail-closed contract (DESIGN §7)"
    - "Step 1c is a per-arg hard Denied that RETURNS on first mismatch (idiom of Steps 1/1a/1b), NOT collected into the `blocked` vec (Steps 2/3's collect-then-Block) — preserves I2-before-I0 precedence"
    - "No BlockedArg / SinkBlockedAnchor gains a role field; a role mismatch is a structural error, never confirmable (DESIGN §6)"
---

<objective>
Land the enforcement half of T2 (T2-03 + T2-05): add the hardcoded `expected_role()` table to `sink_sensitivity.rs`, then insert the fail-closed "Step 1c" per-arg role check into `submit_plan_node`, returning a hard `Denied { SlotTypeMismatch }` on a role↔slot mismatch. Depends on Plan 01 (the `origin_role` field it reads) and Plan 02 (the `DenyReason::SlotTypeMismatch` variant it constructs).

Purpose: This is the actual security enforcement — the point where a misrouted `UserTrusted` handle is caught. It closes the v1.4 T2 residual without weakening or reordering the existing I0/I2 precedence.

Output: `expected_role()`, Step 1c in `submit_plan_node`, and three new executor tests (mismatch→Denied; matching-role+tainted→still Blocks; unconstrained-slot→unaffected).
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (exclude from drift verification):
- `expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>` (crates/executor/src/sink_sensitivity.rs)
- Step 1c block inside `submit_plan_node` (crates/executor/src/lib.rs) — the sole construction site of `DenyReason::SlotTypeMismatch`
- Three new test functions in crates/executor/tests/executor_decision.rs (names at executor's discretion, e.g. `role_mismatch_denies`, `matching_role_tainted_still_blocks`, `unconstrained_slot_unaffected`) + expected_role unit tests in sink_sensitivity.rs's #[cfg(test)] module
</artifacts_this_phase_produces>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-slot-type-binding.md
@.planning/phases/24-slot-type-binding-enforcement/24-RESEARCH.md
@.planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md
@.planning/phases/24-slot-type-binding-enforcement/24-01-SUMMARY.md
@.planning/phases/24-slot-type-binding-enforcement/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the hardcoded expected_role() table to sink_sensitivity.rs</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs (is_routing_sensitive :86-93 and is_content_sensitive :102 — the exact shape to mirror; the security-property doc-comment convention on the const arrays; the existing #[cfg(test)] mod tests at the bottom)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (sink_sensitivity.rs section — the drafted expected_role body, confirmed against DESIGN)
    - planning-docs/DESIGN-slot-type-binding.md §3 (pinned table contents), §7 (fail-closed Option-vs-empty-slice contract)
  </read_first>
  <behavior>
    - expected_role(email.send, "to"|"cc"|"bcc") == Some(["recipient", "email_address"]).
    - expected_role(email.send, "subject") == Some(["subject"]); (email.send, "body") == Some(["body"]).
    - expected_role(file.create, "path") == Some(["path", "relative_path"]).
    - expected_role(file.create, "contents") == None (unconstrained, v1.5 — Assumption A2).
    - expected_role(any other sink, any arg) == None; expected_role(email.send, any other arg) == None.
  </behavior>
  <action>
    Add `pub fn expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>` to `sink_sensitivity.rs`, in the SAME file, with the SAME hardcoded `match sink.0.as_str() { "email.send" => ..., "file.create" => ..., _ => None }` discipline as `is_routing_sensitive` (no config file, no pluggable framework — CON-i2-non-bypassable). Doc-comment it as a security property, matching the existing const-array framing.

    Pinned table (DESIGN §3, exact membership — not TBD):
    - email.send: `to`|`cc`|`bcc` → `Some(&["recipient", "email_address"])`; `subject` → `Some(&["subject"])`; `body` → `Some(&["body"])`; any other arg → `None`.
    - file.create: `path` → `Some(&["path", "relative_path"])`; `contents` → `None` (unconstrained for v1.5, Assumption A2, documented not accidental); any other arg → `None`.
    - any other sink → `None`.

    Contract (load-bearing, DESIGN §7): the return type is `Option<&'static [&'static str]>`. `None` = this slot is UNCONSTRAINED (role check is a no-op for that arg — NOT fail-open; a deliberately scoped-out slot). `Some(list)` = this slot IS role-checked. `Some(&[])` (a slot allowing nothing) MUST NEVER be constructed — a zero-valid-role slot is a design bug, not a runtime state.

    Table-construction invariant (DESIGN §3 F4, load-bearing): an untrusted-origin role tag (`"email_address"`, `"relative_path"`, `"doc_fragment"`) may appear in a slot's expected-role list ONLY if that slot is also routing- or content-sensitive (is_routing_sensitive || is_content_sensitive). Every untrusted role in this table satisfies it (`email_address`@to/cc/bcc and `relative_path`@path are all routing-sensitive), so I2's per-arg Block still fires there regardless of role. Do NOT add an untrusted-origin role string to any slot that is neither routing- nor content-sensitive.

    Add `expected_role` cases to this file's existing `#[cfg(test)] mod tests`, same assertion style as the existing is_routing_sensitive/is_content_sensitive tests: cover every table row (a matching case and a non-matching-arg case for each sink), plus the `contents`-is-None unconstrained case and an unknown-sink-is-None case.
  </action>
  <verify>
    <automated>cargo test -p executor sink_sensitivity</automated>
  </verify>
  <acceptance_criteria>
    - crates/executor/src/sink_sensitivity.rs contains `pub fn expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>`
    - grep -c 'Some(&\["recipient", "email_address"\])' crates/executor/src/sink_sensitivity.rs returns 1
    - grep -c 'Some(&\["path", "relative_path"\])' crates/executor/src/sink_sensitivity.rs returns 1
    - grep -c 'unwrap_or(&\[\])' crates/executor/src/sink_sensitivity.rs returns 0
    - `cargo test -p executor sink_sensitivity` exits 0
  </acceptance_criteria>
  <done>expected_role() returns the exact pinned table for both live sinks with the Option-not-empty-slice fail-closed contract; unit tests cover every row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert Step 1c fail-closed role check into submit_plan_node</name>
  <files>crates/executor/src/lib.rs, crates/executor/tests/executor_decision.rs</files>
  <read_first>
    - crates/executor/src/lib.rs (submit_plan_node :54-216; Steps 1/1a/1b structural-guard idiom :81-109 — the return-immediately pattern to copy; the `let sensitive = ...` line at :117 where collect-then-Block begins; the load-bearing precedence comment at :169-172; the Step 0.5 SessionStatus match at :174-213)
    - crates/executor/tests/executor_decision.rs (existing Denied-path test shapes at :371/:580/:637 to mirror; the .mint() fixture calls Plan 01 already updated with origin_role)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (lib.rs Step 1c section — exact insertion idiom, precedence preservation)
    - planning-docs/DESIGN-slot-type-binding.md §6 (Step 1c placement, hard Denied not confirmable), §7 (fail-closed default), §10 (Pitfalls)
  </read_first>
  <behavior>
    - A plan node whose resolved value has origin_role not in the slot's expected-role list → Denied { SlotTypeMismatch { sink, arg, expected, found } }.
    - A plan node whose resolved value has origin_role == None at a role-checked slot → Denied { SlotTypeMismatch } (fail-closed).
    - A value that PASSES the role check but is tainted at a routing/content-sensitive slot → still BlockedPendingConfirmation (I2 precedence preserved).
    - A value at an unconstrained slot (file.create's contents, expected_role == None) → role check is a no-op; outcome unchanged from today.
    - A Draft-session class deny (Step 0.5) still fires only after the per-arg loop completes with an empty blocked set (I0 precedence unchanged).
  </behavior>
  <action>
    Insert "Step 1c" into `submit_plan_node` immediately after the empty-provenance guard block closes (after lib.rs:109), BEFORE the `let sensitive = ...` line at :117. Follow the exact return-immediately idiom of Steps 1/1a/1b (a per-arg structural guard that returns on first violation), NOT the collect-then-Block pattern of Steps 2/3.

    Logic: call `sink_sensitivity::expected_role(&plan_node.sink, &arg.name)`. Match on the `Option` explicitly — `None` (unconstrained) → fall through unchanged; `Some(list)` → the value passes iff `record.origin_role` is `Some(s)` AND `list.contains(&s.as_str())`. On failure (origin_role is `None`, OR its role is not in `list`), construct and RETURN `ExecutorDecision::Denied { reason: DenyReason::SlotTypeMismatch { sink: plan_node.sink.0.clone(), arg: arg.name.clone(), expected: list.iter().map(|s| s.to_string()).collect(), found: record.origin_role.clone() } }`. This is a hard Denied, never a Block/confirm path — the requirement wording "(or blocks)" is CLOSED in favor of Deny (DESIGN §6/§10 Pitfall 1): a role mismatch is a structural type error with no sound human-confirmable response.

    Constraints (all load-bearing):
    - DO NOT use `.unwrap_or(&[])` anywhere in the Step 1c call site or the lookup (DESIGN §7, Pitfall 4) — it collapses the None-vs-Some(&[]) states and breaks fail-closed. Match on the Option explicitly.
    - DO NOT collect the mismatch into the `blocked` vec — Step 1c returns per-arg like 1/1a/1b, it does NOT join Steps 2/3's collect-then-Block set (DESIGN §6).
    - DO NOT add a role field to any BlockedArg / SinkBlockedAnchor shape (DESIGN §6 — Step 1c needs zero new anchor shapes, only the Plan-02 DenyReason variant).
    - DO NOT reorder or weaken the existing I0 (Step 0.5 class-deny) / I2 (per-arg Block) precedence. Step 1c is per-arg and fires BEFORE that arg reaches Steps 2/3, so I2 Block still fires for every arg that PASSES the role check, and Step 0.5 (I0) still runs only after the per-arg loop with an empty blocked set — the :169-172 comment ("the per-arg I2 Block always takes precedence over this I1/I0 class-level deny") stays true. Do NOT touch the SessionStatus match at :176 (a separate exhaustive match, unchanged).

    Add three test cases to crates/executor/tests/executor_decision.rs, mirroring the existing Denied-path test shape (:371/:580): (a) a value with a mismatched role at a role-checked slot → Denied{SlotTypeMismatch}; (b) a value with a MATCHING role that is also tainted at a routing/content-sensitive slot → still BlockedPendingConfirmation (proves I2 precedence preserved); (c) a value at an unconstrained slot (file.create contents) → unaffected (Allowed or whatever the pre-existing outcome is). Add these as NEW test functions — do not modify Plan 01's fixture-threading edits in this file.
  </action>
  <verify>
    <automated>cargo test -p executor executor_decision</automated>
    <automated>cargo test --workspace --no-fail-fast</automated>
    <automated>./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - crates/executor/src/lib.rs constructs DenyReason::SlotTypeMismatch inside submit_plan_node (grep -c 'SlotTypeMismatch' crates/executor/src/lib.rs returns >= 1)
    - grep -c 'unwrap_or(&\[\])' crates/executor/src/lib.rs returns 0
    - grep -c 'expected_role' crates/executor/src/lib.rs returns >= 1 and the call is matched on Option explicitly (no .unwrap_or)
    - The Step 1c block appears textually BEFORE the `let sensitive` line and returns (not blocked.push) on mismatch
    - crates/executor/tests/executor_decision.rs contains the three new test cases (mismatch-denies, matching-role-tainted-still-blocks, unconstrained-slot-unaffected)
    - `cargo test -p executor executor_decision` exits 0
    - `cargo test --workspace --no-fail-fast` exits 0 (Mac: Linux-only tests report 0 passed by design)
    - `./scripts/check-invariants.sh` exits 0
  </acceptance_criteria>
  <done>submit_plan_node hard-Denies a role↔slot mismatch (and a no-role value) at a role-checked slot via Step 1c, fail-closed, with I0/I2 precedence intact; three tests prove mismatch-denies, matching-role-tainted-still-blocks, and unconstrained-slot-unaffected; full workspace green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan node → executor | submit_plan_node is the deterministic I2/T2 enforcement gate; the only path a resolved value reaches a sensitive sink arg |
| expected-role table | hardcoded Rust TCB, non-bypassable, non-configurable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06 | Spoofing / Tampering | misrouted UserTrusted value in wrong slot (the T2 gap) | high | mitigate | Step 1c hard-Denies when origin_role is not in the slot's expected-role list — the phase's core deliverable (DESIGN §6). Proven by the mismatch-denies test. |
| T-24-07 | Elevation of Privilege | fail-open via ambiguous no-constraint representation | high | mitigate | expected_role returns Option<&[&str]>, matched explicitly; NEVER .unwrap_or(&[]) — a None-role value at a role-checked slot Denies, not passes (DESIGN §7). Enforced by the no-unwrap_or acceptance criterion. |
| T-24-08 | Tampering | Step 1c reorders/weakens I0/I2 precedence | high | mitigate | Step 1c is a per-arg return-immediately guard inserted between 1b and 2/3, not collected into `blocked`; Step 0.5 (I0) and the I2 collect-then-Block are untouched. Proven by the matching-role-tainted-still-blocks test. |
| T-24-09 | Elevation of Privilege | role-mismatch mis-modeled as confirmable (Block) | medium | mitigate | Hard Denied, never BlockedPendingConfirmation; no BlockedArg/anchor gains a role field (DESIGN §6/§10 Pitfall 1). Requirement wording "(or blocks)" closed in favor of Deny. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `cargo test -p executor executor_decision` green (mismatch-denies, matching-role-tainted-still-blocks, unconstrained-slot-unaffected).
- `cargo test -p executor sink_sensitivity` green (expected_role table).
- `cargo test --workspace --no-fail-fast` green on the Mac.
- `./scripts/check-invariants.sh` passes.
- No `.unwrap_or(&[])` in sink_sensitivity.rs or lib.rs; Step 1c returns per-arg (not collected); SessionStatus match untouched.
</verification>

<success_criteria>
A hardcoded per-sink-arg expected-role table exists in crates/executor scoped to the two live sinks (SC2); submit_plan_node hard-Denies a role↔slot mismatch (and a no-role value at a role-checked slot) evaluated per-arg in the same pass as the sensitivity check, fail-closed, without weakening or reordering I0/I2 precedence (SC4).
</success_criteria>

<output>
Create `.planning/phases/24-slot-type-binding-enforcement/24-03-SUMMARY.md` when done.
</output>
