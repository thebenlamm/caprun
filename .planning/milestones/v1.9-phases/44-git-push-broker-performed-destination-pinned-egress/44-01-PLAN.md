---
phase: 44-git-push-broker-performed-destination-pinned-egress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
  - crates/runtime-core/src/effect.rs
  - crates/runtime-core/src/policy.rs
  - crates/llm-planner/src/lib.rs
autonomous: true
requirements: [GIT-02, GIT-03]

must_haves:
  truths:
    - "A `git.push` plan node is classified `CommitIrreversible` via an EXPLICIT arm, so a draft / untrusted-seeded session I0-denies it (never inherits an Observe fall-through)."
    - "A tainted `remote` or `refspec` on `git.push` deterministically Blocks under I2 (routing-sensitive); an untainted node is not Blocked by these tables (GIT-03 registration)."
    - "`git.push` is on the `broker_default()` production policy allowlist, so a policy-permitted node reaches the unmodified I2 loop (never a PolicyDeny for the new sink under the default policy)."
    - "The `IrreversibleEffect::GitPush` ontology field matches the sink's `refspec` arg (WG-4 reconciled) — no `branch`-vs-`refspec` drift between the effect ontology and the sink args."
  artifacts:
    - "crates/executor/src/sink_schema.rs — KNOWN_SINKS row for `git.push` with exact-match args {remote, refspec}."
    - "crates/executor/src/sink_sensitivity.rs — explicit CommitIrreversible class row + routing-sensitive {remote,refspec}, content-sensitive none, expected_role None arms."
    - "crates/runtime-core/src/policy.rs — `git.push` in PRODUCTION_SINKS; the negative `!permits_sink(git.push)` test flipped."
    - "crates/runtime-core/src/effect.rs — `IrreversibleEffect::GitPush { remote, refspec }` (field reconciled)."
  key_links:
    - "sink id `git.push` is the single key on which effect-class (CommitIrreversible), allowlist membership, and I0 discipline all pivot."
    - "the `refspec` name is shared across the sink schema, the sensitivity tables, and the effect ontology — a rename in one without the others reintroduces WG-4 drift."
---

<objective>
Register `git.push` as a DISTINCT, CommitIrreversible sink in the executor TCB — arg schema {remote, refspec}, taint-sensitivity tables (remote/refspec routing-sensitive), production-policy allowlist entry — and reconcile the `IrreversibleEffect::GitPush` ontology field to `refspec` (WG-4). No broker network path here; this plan makes the executor classify and gate `git.push` correctly so I0/I1/I2 fire for free.

Purpose: DESIGN-v1.9-egress-policy §1.1/§1.3 pin git.push at effect-class `CommitIrreversible` with `remote`+`refspec` as I2-gated args from TRUSTED intent. A distinct id classed CommitIrreversible gets the full irreversible discipline (I0 draft-deny + I2 collect-then-Block + confirm-releasable), and the RESEARCH §6 WG-4/WG-5 registration couplings (effect ontology, PRODUCTION_SINKS, llm-planner UnknownSink test) are resolved together so nothing silently PolicyDenies or mis-classifies the sink.

Output: The executor recognizes, schema-gates, and taint-governs `git.push`; the default policy permits it; the effect ontology and the llm-planner test are consistent with the new sink.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The locked design context (READ §1.1/§1.3 before editing) + this phase's research (§1 anchor table A15/A17/A18/A26, §6 WG-4/WG-5):
@planning-docs/DESIGN-v1.9-egress-policy.md
@.planning/phases/44-git-push-broker-performed-destination-pinned-egress/44-RESEARCH.md

# The files this plan edits — study the shipped http.request.write / github.pr rows as the pattern to mirror:
@crates/executor/src/sink_schema.rs
@crates/executor/src/sink_sensitivity.rs
@crates/runtime-core/src/effect.rs
@crates/runtime-core/src/policy.rs
@crates/llm-planner/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register git.push in KNOWN_SINKS + reconcile the GitPush effect ontology (WG-4)</name>
  <files>crates/executor/src/sink_schema.rs, crates/runtime-core/src/effect.rs</files>
  <behavior>
    - `schema_for("git.push")` returns `Some` (never UnknownSink at Step 0).
    - A node with exactly {remote, refspec} validates Ok.
    - A missing `remote` (or `refspec`) → `MissingArg`; an extra arg → `UnknownArg`; a duplicate → `DuplicateArg`.
    - `IrreversibleEffect::GitPush` names its second field `refspec` (not `branch`); the runtime-core `task2_types` construction site and any other constructor compile against the reconciled field.
    - The shipped http.request.write / github.pr rows are unchanged.
  </behavior>
  <action>Add a new `SinkSchema` entry to `KNOWN_SINKS` for sink id `git.push` with `allowed` AND `required` both exactly {"remote","refspec"} — the exact-match, all-required shape (mirror the file.write / git.commit exact-match schema, per RESEARCH A15). Document in the row comment that both args are I2 routing-sensitive (a tainted value Blocks downstream, it does NOT Deny here), come from TRUSTED intent at session creation (DESIGN §1.3), and that the broker enforces the structural `--force`/deletion/`+`-refspec refusal at the transfer layer (Plan 44-02), NOT this Step-0 name-set gate. Then reconcile WG-4: in `crates/runtime-core/src/effect.rs` rename the `IrreversibleEffect::GitPush` field `branch` to `refspec` so the effect ontology matches the sink args + DESIGN §1.3; grep the workspace for every `GitPush {` construction/match (at minimum `crates/runtime-core/tests/task2_types.rs`) and update each to the new field name — keep the exhaustive-match discipline (no wildcard). Add the mirrored unit tests from the behavior block.</action>
  <verify>
    <automated>cargo test -p executor sink_schema:: --no-fail-fast && cargo test -p runtime-core effect --no-fail-fast</automated>
  </verify>
  <done>`git.push` is a registered exact-match {remote,refspec} sink; the GitPush ontology field is `refspec` (WG-4 reconciled, all constructors compile); http.request.write/github.pr rows unchanged; new schema tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify git.push CommitIrreversible + routing-sensitive remote/refspec</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <behavior>
    - `sink_effect_class(git.push) == CommitIrreversible` via an EXPLICIT match row (not only the `_ =>` fail-closed default).
    - `is_routing_sensitive(git.push, "remote") == true` AND `is_routing_sensitive(git.push, "refspec") == true`.
    - `is_content_sensitive(git.push, "remote"|"refspec") == false` (the pushed PACK content is worker-controlled and never an I2 arg — it is surfaced at confirm instead, DESIGN §1.6; no content-sensitive arg over-widening here).
    - `expected_role(git.push, "remote"|"refspec") == None` (no origin_role-producing mint site for a legit trusted-intent push; the Block comes from sensitivity+taint, mirroring github.pr/http.request.write).
  </behavior>
  <action>Add an EXPLICIT `"git.push" => EffectClass::CommitIrreversible` arm to `sink_effect_class` (per DESIGN §1.1 — an explicit arm so a future refactor that reorders the schema gate cannot silently relax it, exactly like the github.pr / http.request.write arms; it is ALSO redundantly covered by the `_ => CommitIrreversible` fail-closed default, but must be explicit). Add a `GIT_PUSH_ROUTING_SENSITIVE = &["remote","refspec"]` const and the matching `"git.push"` arm in `is_routing_sensitive`; git.push has NO content-sensitive args, so its `is_content_sensitive` arm returns false for every arg (the pushed pack content is worker-controlled and mediated by the confirm-time payload provenance surface in Plan 44-04, NOT by I2 content-sensitivity — do not add remote/refspec to a content-sensitive set). Add a `"git.push"` arm to `expected_role` returning `None` for remote/refspec (reuse the http.request.write/git.commit rationale: no origin_role mint site for a legit trusted-intent push, so `Some(..)` would fail-closed-Deny the legit flow; `None` here is NOT an I2 bypass — the Block comes from routing-sensitivity+taint). Add the mirrored unit tests from the behavior block, including a `git_push_is_commit_irreversible` test asserting the EXPLICIT arm.</action>
  <verify>
    <automated>cargo test -p executor sink_sensitivity:: --no-fail-fast</automated>
  </verify>
  <done>git.push classes CommitIrreversible (explicit); remote+refspec routing-sensitive; no content-sensitive arg; expected_role None; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Production-policy allowlist + llm-planner UnknownSink test (WG-5)</name>
  <files>crates/runtime-core/src/policy.rs, crates/llm-planner/src/lib.rs</files>
  <behavior>
    - `broker_default()` permits `git.push` (permits_sink true); `PRODUCTION_SINKS` lists it.
    - The shipped negative test asserting `!permits_sink("git.push")` (policy.rs) is flipped to assert it IS now permitted, and any count-bound "broker_default permits the N production sinks" assertion is updated to N+1.
    - The llm-planner test that asserts `git.push => UnknownSink` is updated so the coupling does not silently break; the planner stays deterministic/stub (v1.9 does not teach it the sink) — reflect the actual, intended planner behavior in the test.
  </behavior>
  <action>In `crates/runtime-core/src/policy.rs` add `"git.push"` to the `PRODUCTION_SINKS` slice (so `broker_default()` and `allow_all()` both permit it; RESEARCH A17/WG-5). Fix the narrative prose count comment (policy.rs:209) that says "seven real production sinks" — the slice already holds 8 and adding git.push makes 9, so update that prose count to the correct number (prose-only; no numeric assert exists on it) so the comment stays accurate. Update the existing `git.push`-negative assertion (the `assert!(!policy.permits_sink(&sink("git.push")))` around policy.rs:304/555) to assert it IS now permitted, and bump any count-bound production-sink assertion (`broker_default should permit {s}` loop / any "the N production sinks" test) to include git.push. In `crates/llm-planner/src/lib.rs` update the `git.push => UnknownSink` test (lib.rs:586-605): the deterministic/stub planner does NOT learn git.push in v1.9 (REQUIREMENTS Out-of-Scope: real LLM planner loop deferred to v2), so keep the planner's behavior unchanged but make the test's INTENT explicit — assert the current, correct behavior for a stub planner that does not register git.push as a callable planner sink, and add a comment tying it to WG-5 (registering git.push at the EXECUTOR does not make the STUB PLANNER emit it; the coupling is enumerated so a future planner-learns-git.push change updates this deliberately, not by surprise). Add/adjust unit tests from the behavior block.</action>
  <verify>
    <automated>cargo test -p runtime-core policy:: --no-fail-fast && cargo test -p llm-planner --no-fail-fast</automated>
  </verify>
  <done>broker_default permits git.push; the negative permits_sink test is flipped; count-bound assertions updated; the llm-planner UnknownSink coupling is deliberately reconciled (WG-5); policy + llm-planner tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → executor | plan nodes (sink id, arg names, resolved literals via broker-owned store) cross into the deterministic I2 executor. |
| trusted intent → executor | `remote` + `refspec` originate from TRUSTED session-creation intent, never the untrusted repo `.git/config` (enforced at the broker; the executor classifies them routing-sensitive). |
| policy source → executor | the per-session policy narrows which sinks/args are callable (pre-I2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Elevation | sink_effect_class classification of git.push | critical | mitigate | EXPLICIT `git.push => CommitIrreversible` arm + `_ =>` fail-closed default so a draft/untrusted-seeded session I0-denies a push (never an Observe fall-through), DESIGN §1.1. |
| T-44-02 | Spoofing | tainted `remote`/`refspec` steering the push destination | high | mitigate | remote+refspec routing-sensitive under I2 → collect-then-Block on any untrusted taint (GIT-03, DESIGN §1.3), genuinely propagated, never stapled. |
| T-44-03 | Elevation | policy omits git.push → PolicyDeny masks the intended I2 path | low | accept | broker_default explicitly allowlists git.push (WG-5); a future/unknown sink stays deny-by-default (documented). |
| T-44-04 | Tampering | effect-ontology `branch`-vs-`refspec` drift hides a refspec value from downstream gates | medium | mitigate | WG-4: rename the GitPush ontology field to `refspec` so the ontology, sink schema, and sensitivity tables key on one identical name (no divergent field a refactor could desync). |
</threat_model>

<verification>
- `cargo build --workspace` compiles (macOS host).
- `cargo test -p executor --no-fail-fast`, `cargo test -p runtime-core --no-fail-fast`, and `cargo test -p llm-planner --no-fail-fast` green.
- `./scripts/check-invariants.sh` passes (no new mint site, no raw effect-request-to-sink bypass — this plan touches no sink dispatch).
</verification>

<success_criteria>
git.push is a registered, CommitIrreversible, routing-sensitive (remote/refspec), policy-permitted sink in the executor TCB, with the GitPush effect ontology reconciled to `refspec` (WG-4) and the PRODUCTION_SINKS + llm-planner couplings resolved (WG-5). All new + existing executor/runtime-core/llm-planner tests green.
</success_criteria>

<output>
Create `.planning/phases/44-git-push-broker-performed-destination-pinned-egress/44-01-SUMMARY.md` when done.
</output>
