---
phase: 21-adversarial-llm-planner
plan: 04
type: execute
wave: 3
depends_on: [21-02, 21-03]
files_modified:
  - scripts/mailpit-verify.sh
  - cli/caprun/tests/llm_planner_live_accept.rs
autonomous: true
requirements: [PLANNER-03]
must_haves:
  truths:
    - "A live end-to-end run — real OpenAI API call in the sidecar, worker submitting via its own connection — takes a clean trusted intent to an Allowed decision and a real SMTP delivery captured by Mailpit (exactly one message)."
    - "The LLM planner emits a syntactically valid PlanNode referencing handle IDs only (never a literal), routing the UserTrusted recipient handle to email.send/to."
    - "The proof is captured from real output (verify_chain PASSED + Mailpit count == 1), not asserted from code alone, and runs on real Linux via scripts/mailpit-verify.sh."
  artifacts:
    - cli/caprun/tests/llm_planner_live_accept.rs
    - scripts/mailpit-verify.sh
  key_links:
    - "scripts/mailpit-verify.sh forwards OPENAI_API_KEY and CAPRUN_PLANNER_MODEL into the rust:1 verification container so the in-container sidecar can reach api.openai.com."
    - "The live test invokes caprun with CAPRUN_PLANNER=llm on a clean doc (no injection markers) so the worker routes the UserTrusted intent handle to email.send -> Allowed -> live send."
---

<objective>
Prove PLANNER-03 live: a real OpenAI-backed planner run drives a clean,
trusted intent end-to-end to an Allowed decision and a real delivered email,
using only PlanNode{sink,args} (handle IDs, no literal). This is the phase's
"real API call, live-tested with captured real output" gate. The hostile /
Block scenario is explicitly NOT built here — that is Phase 22's HARD GATE.

Purpose: Close Phase 21 with a genuine live proof that the LlmPlanner,
sidecar, and worker wiring compose into a working end-to-end clean-path send,
per the project's separate-live-acceptance discipline (v1.2 Phase 11, v1.3
Phase 17).

Output: A Linux-gated live acceptance test + the mailpit-verify.sh change that
lets the sidecar reach OpenAI inside the container; measured per-run cost
recorded in the SUMMARY.

## Artifacts this phase produces (rollup)
- `crates/llm-planner/` — pure wire types, build_planner_prompt, build_tool_schema, parse_planner_response (Plan 01).
- `cli/caprun-planner/` — the out-of-process OpenAI sidecar; the only reqwest in the workspace (Plan 02).
- `cli/caprun/src/planner.rs::LlmPlanner` + worker/main wiring — selectable LLM planner behind the Phase-20 seam (Plan 03).
- `cli/caprun/tests/llm_planner_live_accept.rs` — the live clean-path acceptance (this plan).
- `scripts/mailpit-verify.sh` — OPENAI_API_KEY / CAPRUN_PLANNER_MODEL forwarding (this plan).
- SUMMARY documentation of the measured per-run OpenAI cost.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@cli/caprun/tests/live_acceptance_v1_3.rs
@cli/caprun/src/main.rs
@crates/llm-planner/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forward OpenAI env into the Mailpit verification container</name>
  <files>scripts/mailpit-verify.sh</files>
  <read_first>
    - scripts/mailpit-verify.sh (the existing rust:1 verification container invocation, the Mailpit sidecar network setup, and the libssl-dev/pkg-config install step)
    - CLAUDE.md (the "From Phase 16 onward, ALL Linux verification goes through scripts/mailpit-verify.sh" contract and the CAPRUN_SMTP_HOST/PORT wiring)
  </read_first>
  <action>
    Extend scripts/mailpit-verify.sh so the verification container can make the OpenAI call. Pass `-e OPENAI_API_KEY` (forwarded from the host env) and `-e CAPRUN_PLANNER_MODEL` (forwarded if set) into the `docker run rust:1` invocation, alongside the existing CAPRUN_SMTP_HOST/PORT env. Do not change the SMTP wiring, the Mailpit sidecar setup, the libssl-dev/pkg-config install, or the MAILPIT_VERIFY_CMD override mechanism. Fail fast with a clear message if OPENAI_API_KEY is unset in the host env when a caller requests the LLM path (a plain non-LLM run must still work without the key — keep the key forwarding unconditional-but-empty-tolerant, and let the LLM live test itself skip/xfail if the key is absent). Add a short comment noting the container reaches api.openai.com via Docker's default egress.
  </action>
  <verify>
    <automated>bash -n scripts/mailpit-verify.sh && grep -c 'OPENAI_API_KEY' scripts/mailpit-verify.sh</automated>
  </verify>
  <done>
    - `bash -n scripts/mailpit-verify.sh` parses clean.
    - The script passes OPENAI_API_KEY (and CAPRUN_PLANNER_MODEL when set) into the rust:1 container.
    - The existing SMTP / Mailpit / MAILPIT_VERIFY_CMD behavior is unchanged; a non-LLM run still works with no key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Live clean-path LLM acceptance test (real OpenAI + real send)</name>
  <files>cli/caprun/tests/llm_planner_live_accept.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_3.rs (the CARGO_BIN_EXE_caprun invocation pattern, the `mod mailpit_client` HTTP helper on port 8025, the clean-control CLEAN_PATH_CONTENT fixture with no Reply-To:/Domain: markers, and the #[cfg(target_os = "linux")] gating)
    - cli/caprun/src/main.rs (the caprun CLI arg shape: send-email-summary <recipient> <workspace-file> [audit-db-path], and CAPRUN_PLANNER env selection)
  </read_first>
  <action>
    Add a Linux-gated live acceptance test file. Reuse the clean-doc fixture shape from live_acceptance_v1_3.rs (a workspace doc containing a plain address but NO Reply-To:/Domain: marker fragments, so the confined worker derives no tainted recipient and the planner routes the UserTrusted intent handle). Invoke the caprun binary (env!("CARGO_BIN_EXE_caprun")) for a `send-email-summary <clean_recipient> <clean_doc> <persistent_audit_db>` run with the environment set to CAPRUN_PLANNER=llm (and OPENAI_API_KEY / CAPRUN_PLANNER_MODEL inherited from the container env). Assert: (1) caprun exits 0; (2) the printed audit output shows Chain verification: PASSED; (3) via the mailpit_client HTTP helper, exactly one message was captured for clean_recipient. If OPENAI_API_KEY is absent, skip cleanly (do not hard-fail a keyless CI) — but the delivered-live path is the real proof. Add a module-header comment giving the exact run command: `MAILPIT_VERIFY_CMD='cargo build -p caprun-planner && cargo test -p caprun --test llm_planner_live_accept llm_planner_clean_allow_delivers' bash scripts/mailpit-verify.sh` — building caprun-planner first so the sidecar binary is present next to caprun in target/debug. On macOS the live body is cfg-excluded (only a compile guard runs), mirroring the existing live tests.

    Do not attempt the hostile-doc Block scenario here — that is Phase 22 (GATE-01..04). This test proves only the clean Allowed + real-delivery path.
  </action>
  <verify>
    <automated>cargo test -p caprun --test llm_planner_live_accept 2>&1 | tail -15</automated>
    <human-check>On Linux with a real key: `MAILPIT_VERIFY_CMD='cargo build -p caprun-planner && cargo test -p caprun --test llm_planner_live_accept llm_planner_clean_allow_delivers' bash scripts/mailpit-verify.sh` — confirm the captured output shows caprun exit 0, Chain verification: PASSED, and Mailpit count == 1 for the clean recipient. Record the OpenAI request's token counts / cost from the run.</human-check>
  </verify>
  <done>
    - On macOS: the file compiles and the cross-platform guard test passes (live body cfg-excluded).
    - On Linux via mailpit-verify.sh with a real key: a real OpenAI tool-call drives the run to caprun exit 0, verify_chain PASSED, and exactly one Mailpit-captured message for the clean recipient — captured real output, not a mock.
    - The measured per-run OpenAI cost is recorded in the SUMMARY (expected order 1e-4 dollars at gpt-4o-mini pricing).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification container → OpenAI | The test's sidecar reaches api.openai.com from inside the rust:1 container; only handle IDs + slot hints leave. |
| clean intent → email.send | The clean path must reach Allowed and deliver exactly once; a false Allow of a tainted value would be a defense failure (but the hostile path is Phase 22). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-12 | Repudiation | live-send proof | high | mitigate | Proof is captured real output — Mailpit count == 1 + verify_chain PASSED — not asserted from code; run recorded via mailpit-verify.sh. |
| T-21-13 | Information Disclosure | OPENAI_API_KEY in container | medium | mitigate | Key forwarded only as a container env var to the sidecar; never written to the audit DB or the workspace; not passed to the confined worker. |
| T-21-14 | Denial of Service | OpenAI cost/quota | low | accept | One cheap tool-call per run; keyless runs skip cleanly; cost documented. |
</threat_model>

<verification>
- macOS: `cargo test -p caprun --test llm_planner_live_accept` compiles and the guard test passes.
- Linux: the mailpit-verify.sh run captures caprun exit 0 + Chain verification: PASSED + Mailpit count == 1 with a real OpenAI call.
- `./scripts/check-invariants.sh` passes.
- `git diff --name-only` touches only scripts/mailpit-verify.sh and cli/caprun/tests/llm_planner_live_accept.rs — no crates/brokerd, crates/executor, or ConnectionRole/DeclarePlannerRole dispatch code.
</verification>

<success_criteria>
- A real OpenAI-backed clean-path run reaches Allowed and delivers exactly one message, proven live with captured output.
- The LLM planner emits a valid handle-ID-only PlanNode routing the UserTrusted handle to email.send/to.
- Per-run cost documented; no TCB changes.
</success_criteria>

<output>
Create `.planning/phases/21-adversarial-llm-planner/21-04-SUMMARY.md` when done.
</output>
