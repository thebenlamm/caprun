---
phase: 46-composed-live-proof-v1-9-done
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mock-github/server.py
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "A POST to /ingest on the write-allowlisted mock host returns HTTP 201 and records a receipt."
    - "Every pre-existing mock route (PR pulls 201, git-receive-pack, 404 fallbacks) is unchanged."
    - "The mock host string stays absent from a feature-OFF release build (no new guard needed)."
  artifacts:
    - scripts/mock-github/server.py
  key_links:
    - "http.request.write POST → github-mock.caprun.test/ingest → 201 → http_write_succeeded (proven live in 46-02)"
---

<objective>
Close wiring gap G3: add a dedicated 2xx `http.request.write` POST endpoint to the
stdlib-only mock GitHub server so a CLEAN composed POST leg gets a genuine 201
(`http_write_succeeded`) instead of the current 404 (`http_write_failed`).

Purpose: The write-allowlisted mock host (`github-mock.caprun.test`) currently
serves NO generic write endpoint — a clean `http.request.write` POST 404s. Without
this, LIVE-05's composed POST leg cannot deliver a body to a live endpoint (the
HTTP-W-01 "clean leg actually delivered, mock records receipt" sub-clause carried
to Phase 46). Pure test-double change, no TCB — mirrors the Phase-44 WG-9
git-receive-pack mock addition.
Output: `POST /ingest` → 201 (+ recorded receipt) in `scripts/mock-github/server.py`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-composed-live-proof-v1-9-done/46-RESEARCH.md
@scripts/mock-github/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /ingest → 201 write endpoint with a receipt ledger</name>
  <files>scripts/mock-github/server.py</files>
  <read_first>
    - scripts/mock-github/server.py:126 (_RECEIPTS in-memory ledger)
    - scripts/mock-github/server.py:129-133 (_is_pulls_path helper — pure path predicate pattern to mirror)
    - scripts/mock-github/server.py:175-234 (do_POST routing + _handle_receive_pack receipt-record pattern)
    - scripts/mock-github/server.py:1-60 (module docstring — routing contract to extend)
  </read_first>
  <action>
    Add a pure path predicate `_is_ingest_path(path)` (query-string stripped, true
    only for exactly `/ingest`) mirroring the existing `_is_pulls_path` shape at
    server.py:129. Add a separate `_WRITE_RECEIPTS = []` ledger (distinct from the
    `_RECEIPTS` git-push ledger so the two delivery surfaces stay independently
    observable). In `do_POST`, route an `_is_ingest_path(self.path)` request FIRST
    (before the pulls / receive-pack branches): record a receipt dict
    ({path, body_bytes: len(body), authenticated: Authorization header present})
    to `_WRITE_RECEIPTS`, write a `mock-http-write: RECEIPT <json>` line to stderr
    (mirror _handle_receive_pack:224), and reply 201 via `self._send(201, {...})`
    with a minimal JSON acknowledgement (e.g. an "id"/"received": true body — no
    real GitHub data). Leave EVERY existing branch (pulls 201, `/accept/*`
    receive-pack, `/redirect/*`, the two 404 fallbacks) byte-for-byte intact — the
    new branch only adds a case, it must NOT reorder or weaken the receive-pack
    routing that Phase-44 tests depend on. Extend the module docstring with a
    "Phase-46 (G3): http.request.write POST /ingest" section documenting the
    endpoint contract (mirror the WG-9 receive-pack section). Add NO third-party
    import — stdlib only, runs unmodified on python:3-slim.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/mock-github/server.py && python3 -c "import sys; sys.path.insert(0,'scripts/mock-github'); import server; assert server._is_ingest_path('/ingest'); assert server._is_ingest_path('/ingest?x=1'); assert not server._is_ingest_path('/repos/a/b/pulls'); assert not server._is_ingest_path('/accept/repo.git/git-receive-pack'); assert hasattr(server,'_WRITE_RECEIPTS'); print('ingest-routing-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_is_ingest_path` returns True for `/ingest` (with/without query) and False
      for `/repos/*/pulls` and any receive-pack path.
    - `do_POST` on `/ingest` returns 201, appends a receipt to `_WRITE_RECEIPTS`,
      and logs a `mock-http-write: RECEIPT` line to stderr.
    - `python3 -m py_compile` passes; no non-stdlib import added.
    - All pre-existing routes (pulls 201, `/accept/*` receive-pack, `/redirect/*`
      302, 404 fallbacks) are unchanged — the diff only ADDS the ingest branch +
      helper + ledger + docstring section.
  </acceptance_criteria>
  <done>Completed: Add POST /ingest → 201 write endpoint with a receipt ledger; all acceptance criteria above are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker egress → mock endpoint | The composed POST crosses the shipped validate_url→allowlist→resolve-and-pin path to the mock over real TLS |
| feature flag (mock-egress-ca) | The mock host must exist ONLY under the non-default test feature, never in a release build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01-01 | Spoofing | mock `/ingest` accepting unauthenticated writes | low | accept | Test double on an RFC-6761 non-resolvable TLD behind a self-signed cert trusted only under `mock-egress-ca`; records but never acts. Credential presence is recorded (`authenticated`) but not required — mirrors receive-pack mock. |
| T-46-01-02 | Tampering | new branch reorders receive-pack routing, weakening a Phase-44 negative test | medium | mitigate | `/ingest` routed as an additive first-match case; existing branches left byte-identical; acceptance asserts pulls/receive-pack/redirect/404 unchanged. |
| T-46-01-03 | Information disclosure | mock host string leaking into a release build | low | accept | No release-build reference added (Python sidecar only, not compiled into `caprun`); the existing compose-verify feature-OFF guard (compose-verify.sh:104) already proves `github-mock.caprun.test` absent from `target/debug/caprun` — unchanged, no new guard needed. |
| T-46-01-SC | Tampering | new package-manager installs | low | accept | None — stdlib-only Python change, zero new dependencies; no npm/pip/cargo install task. |
</threat_model>

<verification>
- `python3 -m py_compile scripts/mock-github/server.py` succeeds.
- Routing unit assertion (Task 1 automated) passes.
- Genuine live 201 / `http_write_succeeded` delivery is proven downstream by
  46-02's composed POST leg under `scripts/compose-verify.sh` (mock-egress-ca).
</verification>

<success_criteria>
- A clean `http.request.write` POST to `https://github-mock.caprun.test/ingest`
  will receive 201 and be recorded, unblocking LIVE-05's composed delivery leg.
- No pre-existing mock behaviour changed; feature-OFF guard remains valid.
</success_criteria>

<output>
Create `.planning/phases/46-composed-live-proof-v1-9-done/46-01-SUMMARY.md` when done.
</output>
