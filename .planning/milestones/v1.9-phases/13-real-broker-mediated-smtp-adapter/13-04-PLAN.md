---
phase: 13-real-broker-mediated-smtp-adapter
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - scripts/mailpit-verify.sh
  - crates/brokerd/tests/email_smtp_acceptance.rs
  - CLAUDE.md
autonomous: true
requirements: [SMTP-03, SMTP-05]
must_haves:
  truths:
    - "A confirmed email.send effect results in a REAL email captured by a local Mailpit instance, sent by the broker/confirm process — Linux-verifiable, repeatable, no live infra dependency (SMTP-03)."
    - "A CRLF/header-injection fixture (a tainted body containing a CR/LF-then-Bcc sequence) produces EXACTLY the intended envelope recipients at Mailpit — verified by reading Mailpit's HTTP API, not merely that the send succeeded (SMTP-05)."
  artifacts:
    - "crates/brokerd/tests/email_smtp_acceptance.rs — SMTP-03 capture test + SMTP-05 CRLF fixture asserting via Mailpit HTTP API"
    - "scripts/mailpit-verify.sh — reusable Mailpit sidecar + Linux verification helper"
    - "CLAUDE.md verification recipe extended with the Mailpit sidecar + libssl-dev/pkg-config step"
  key_links:
    - "confirm(email.send) → email_smtp adapter → Mailpit SMTP :1025 capture → assert via Mailpit HTTP API :8025 (SMTP-03)"
    - "tainted body CR/LF+Bcc literal → Mailpit envelope To/Cc/Bcc shows NO smuggled recipient (SMTP-05)"
---

<objective>
Prove the full send end-to-end and the CRLF defense against a live capture SMTP. Add a reusable Mailpit sidecar helper, extend the Linux verification recipe (including the new `libssl-dev`/`pkg-config` build dependency from lettre's default native-tls feature), and add an acceptance test file that: (SMTP-03) confirms an `email.send` effect and asserts the real email is captured by Mailpit, sent by the broker process; and (SMTP-05) sends a tainted body carrying a CR/LF-then-`Bcc:` sequence and asserts via Mailpit's HTTP API that the captured envelope has NO smuggled recipient.

Purpose: Turn the by-construction defenses of Plans 01-02 into verified, Linux-repeatable acceptance evidence.
Output: Mailpit helper script, recipe extension, and the SMTP-03/SMTP-05 acceptance test file.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-CONTEXT.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-01-SUMMARY.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-02-SUMMARY.md
@crates/brokerd/src/sinks/email_smtp.rs
@crates/brokerd/src/confirmation.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mailpit sidecar helper + verification recipe extension + live schema probe</name>
  <files>scripts/mailpit-verify.sh, CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the "Linux-only security tests" section — the existing Colima+Docker recipe to extend, NOT replace)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Environment Availability — the recommended sidecar recipe, ASSUMED/A2; Pitfall 3 libssl-dev; Open Question 2/3 — reusable shell helper, not a compose file)
  </read_first>
  <action>
    Create scripts/mailpit-verify.sh (a shell helper, NOT a docker-compose file — matches the project's no-compose convention). It: creates a user-defined docker network, starts `axllent/mailpit` detached (publishing SMTP :1025 and HTTP API :8025) on that network, then runs the existing `rust:1` verification container ON THE SAME NETWORK with `--security-opt seccomp=unconfined` and NO elevated-container-privilege flag (the confinement stack is fully unprivileged), `-e CAPRUN_SMTP_HOST=mailpit -e CAPRUN_SMTP_PORT=1025`, `-e CARGO_TARGET_DIR=/tmp/lt`, and a command that FIRST runs `apt-get update && apt-get install -y libssl-dev pkg-config` (Pitfall 3 — lettre's default native-tls needs these; the base rust:1 image lacks them) THEN `cargo test --workspace --no-fail-fast`. Stop/clean up Mailpit at the end. Smoke-test the network wiring once (Assumption A2) — if Colima's networking needs a host-network mode or explicit port-forwarding instead of container-name DNS, adjust and document the working form in the script.

    Include an EXPLORATORY step (documented in the script or SUMMARY): before finalizing Task 2's assertion, start Mailpit, send one known-good message via the adapter, and `curl http://localhost:8025/api/v1/message/<id> | jq` to confirm the EXACT live field names/shape of the To/Cc/Bcc arrays (Open Question 2 — the research's schema is MEDIUM-confidence, not live-verified). Record the confirmed field path in the SUMMARY so Task 2 asserts against reality.

    Extend CLAUDE.md's "Linux-only security tests" section with a short note pointing at scripts/mailpit-verify.sh as the recipe for phases needing Mailpit (13, and later 17), including the libssl-dev/pkg-config addition. Use Edit (scoped) — do NOT rewrite the file.
  </action>
  <verify>
    <automated>test -x scripts/mailpit-verify.sh && bash -n scripts/mailpit-verify.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mailpit-verify.sh` exists, is executable, and passes `bash -n` (syntax check).
    - The script starts `axllent/mailpit`, runs the `rust:1` container on the same network with `CAPRUN_SMTP_HOST=mailpit`/`CAPRUN_SMTP_PORT=1025`, installs `libssl-dev pkg-config` before `cargo test`, and cleans up Mailpit afterward.
    - The container runs unprivileged: the script uses `--security-opt seccomp=unconfined` and grants no elevated-container-privilege flag (verified by review + `bash -n`).
    - CLAUDE.md references scripts/mailpit-verify.sh in its verification section.
    - The SUMMARY records the empirically-confirmed Mailpit HTTP API field path for To/Cc/Bcc.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SMTP-03 acceptance test — confirmed effect captured by Mailpit</name>
  <files>crates/brokerd/tests/email_smtp_acceptance.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs (confirm() email.send path from Plan 02 — how to drive a confirm end-to-end)
    - crates/brokerd/src/sinks/email_smtp.rs (invoke_email_smtp_from_resolved, smtp_host/smtp_port)
    - crates/brokerd/tests/ (existing test-file conventions — e.g. s9_acceptance / phase5_dispatch style for setting up a PendingConfirmation + confirm)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Validation Architecture SMTP-03 row; Pitfall 4 — Mailpit fields are header-derived)
    - 13-04-SUMMARY.md exploratory-step result (the confirmed live field path from Task 1)
  </read_first>
  <behavior>
    - A confirmed email.send effect (clean recipients + body) results in exactly one message captured by Mailpit, retrievable via its HTTP API, addressed to the intended recipient — sent by the broker/confirm process, not the worker.
  </behavior>
  <action>
    Create crates/brokerd/tests/email_smtp_acceptance.rs, `#[cfg(target_os = "linux")]` gated (requires the Mailpit sidecar; skipped on macOS — expected). Set up a PendingConfirmation for an `email.send` effect with a clean `to`/`subject`/`body`, drive `confirm()` to Released, then query Mailpit's HTTP API (`GET /api/v1/messages` / `GET /api/v1/message/{ID}` on `CAPRUN_SMTP_HOST:8025` — HTTP API port; note the SMTP host env is 1025) and assert exactly one captured message addressed to the intended recipient. Read host from `CAPRUN_SMTP_HOST` env. Use a minimal HTTP GET (existing dev-dep HTTP client if present, else a std TCP/`curl`-shell fallback) — keep it dependency-light. Clean up / tolerate a fresh Mailpit inbox per run.
  </action>
  <verify>
    <automated>cargo build -p brokerd --tests 2>&1 | grep -qi error && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p brokerd --tests` compiles (the test is cfg-gated for Linux).
    - Under `scripts/mailpit-verify.sh`, the SMTP-03 test passes: a confirmed effect yields exactly one Mailpit-captured message to the intended recipient.
    - The test asserts the message was captured (via Mailpit HTTP API), not merely that `confirm()` returned Released.
    - On macOS `cargo test -p brokerd` the test is skipped (0 passed) — expected.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SMTP-05 CRLF fixture — tainted body cannot smuggle a recipient</name>
  <files>crates/brokerd/tests/email_smtp_acceptance.rs</files>
  <read_first>
    - crates/brokerd/tests/email_smtp_acceptance.rs (the SMTP-03 harness from Task 2 to reuse)
    - crates/brokerd/src/sinks/email_smtp.rs (build_message's Address-parse fail-closed boundary)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-CONTEXT.md (D-22 — CRLF fixture is a HARD requirement regardless of lettre's by-construction defense)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Pitfall 4 — assert on Mailpit's header-derived To/Cc/Bcc; the confirmed field path from Task 1's exploratory step)
  </read_first>
  <behavior>
    - A tainted body literal containing a CR/LF sequence followed by a `Bcc:` header line does NOT add any recipient to the captured envelope — Mailpit's parsed To/Cc/Bcc show ONLY the intended recipients, no attacker address.
  </behavior>
  <action>
    Add a `#[cfg(target_os = "linux")]` CRLF-fixture test to email_smtp_acceptance.rs. Construct an `email.send` effect whose `body` literal carries a CR/LF-then-`Bcc: attacker@evil.com` sequence (the injection payload), with a single legitimate `to` recipient. Drive it through the confirm path. Then read the captured message via Mailpit's HTTP API and assert the envelope's To/Cc/Bcc (the empirically-confirmed field path from Task 1) contain ONLY the intended recipient and NO `attacker@evil.com` — proving the CR/LF-then-Bcc in the body was NOT parsed as a real header by the receiving MTA. The attack must fail either because lettre's typed builder wrote the body structurally inert (after the header/body separator) OR because Address parse fail-closed rejected a tainted routing literal — assert the OUTCOME (no smuggled recipient at Mailpit), per D-22: "defends by construction" must be VERIFIED, not assumed.
  </action>
  <verify>
    <automated>cargo build -p brokerd --tests 2>&1 | grep -qi error && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Under `scripts/mailpit-verify.sh`, the CRLF-fixture test passes: the captured Mailpit envelope's To/Cc/Bcc contain the intended recipient and do NOT contain `attacker@evil.com`.
    - The assertion reads Mailpit's HTTP API envelope fields (the confirmed field path), not merely the send return value.
    - The test is `#[cfg(target_os = "linux")]` gated; skipped on macOS (expected).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tainted body literal → receiving MTA envelope | A CR/LF+Bcc payload in the body must not become a real recipient at the capture server |
| confirm path → external capture SMTP | The broker process (not the worker) is the sole sender reaching Mailpit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Tampering | CRLF/header injection via tainted body | high | mitigate | Fixture asserts Mailpit's parsed To/Cc/Bcc envelope shows no smuggled recipient — verified end-to-end, not assumed from lettre's reputation (SMTP-05/D-22) |
| T-13-10 | Spoofing | sender identity (broker vs worker) | medium | mitigate | Acceptance test drives the send through confirm()/broker process; worker network egress is separately kernel-denied (Plan 03) |
| T-13-11 | Information Disclosure | test infra (Mailpit) reachability | low | accept | Mailpit is local capture-only, no live infra, no real inbox; ephemeral sidecar torn down per run |
</threat_model>

<verification>
- `scripts/mailpit-verify.sh` passes `bash -n` and starts/cleans up Mailpit with no --privileged.
- Under the Mailpit recipe, SMTP-03 (real capture) and SMTP-05 (no smuggled recipient) tests pass on Linux.
- CLAUDE.md references the Mailpit helper.
</verification>

<success_criteria>
- A confirmed effect produces a real email captured by Mailpit, sent by the broker.
- A CRLF-in-body fixture cannot smuggle a recipient — verified via Mailpit's HTTP API.
- The verification recipe is reusable (Phase 17 will reuse it).
</success_criteria>

<output>
Create `.planning/phases/13-real-broker-mediated-smtp-adapter/13-04-SUMMARY.md` when done.
</output>
