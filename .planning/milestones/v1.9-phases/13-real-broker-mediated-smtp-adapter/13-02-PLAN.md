---
phase: 13-real-broker-mediated-smtp-adapter
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - crates/brokerd/src/confirmation.rs
  - cli/caprun/src/main.rs
  - crates/brokerd/src/sinks.rs
  - crates/brokerd/src/sinks/email_send.rs
autonomous: true
requirements: [SEND-01, SEND-02]
must_haves:
  truths:
    - "A re-issued confirm, or any duplicate confirm attempt, cannot double-fire the SMTP send — the audit DAG records exactly ONE email_send_attempted per effect_id, because the pending→confirmed CAS and the email_send_attempted append commit in ONE atomic SQLite transaction before any socket opens."
    - "An adapter failure after confirm surfaces as a distinct non-zero CLI exit code (never swallowed, never confused with denied/unknown), records a durable email_send_failed event, and cannot silently retry into a double-send."
  artifacts:
    - "confirm() with signature conn: &mut rusqlite::Connection (7 call sites updated)"
    - "special-cased email.send dispatch arm owning its own conn.transaction() for CAS + email_send_attempted"
    - "ConfirmOutcome::EmailSendFailed variant + CLI exit code 7"
    - "deletion of crates/brokerd/src/sinks/email_send.rs and its module declaration"
  key_links:
    - "conn.transaction() { transition_state(&tx) CAS + append_event(&tx, email_send_attempted) } .commit() — the at-most-once atomicity gate (SEND-01)"
    - "email.send Err from invoke_email_smtp_from_resolved → ConfirmOutcome::EmailSendFailed → exit 7 (SEND-02, never the ConfirmedButSinkFailed swallow-shape)"
---

<objective>
Wire the Plan 01 adapter into the confirm path with at-most-once semantics. Widen `confirm()`'s signature to `&mut rusqlite::Connection` (7 call sites), special-case the `"email.send"` dispatch arm so its `pending→confirmed` CAS and its `email_send_attempted` append commit in ONE atomic SQLite transaction BEFORE any SMTP connection opens (SEND-01), invoke the Plan 01 adapter from the frozen snapshot, add a distinct `ConfirmOutcome::EmailSendFailed` variant + CLI exit code 7 for the non-swallowed failure path (SEND-02), and delete the now-dead `email_send.rs` stub.

Purpose: Make the confirm-triggered send idempotent and failure-safe without a new idempotency token — reusing the EXISTING CAS as the sole authorization gate.
Output: Rewired confirm dispatch, new outcome variant + exit code, stub deletion, and unit tests proving single-fire idempotency and non-swallowed failure.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-CONTEXT.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md
@planning-docs/DESIGN-content-adapter-mediation.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-01-SUMMARY.md
@crates/brokerd/src/confirmation.rs
@crates/brokerd/src/sinks/file_create.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen confirm() to &mut Connection across all 7 call sites</name>
  <files>crates/brokerd/src/confirmation.rs, cli/caprun/src/main.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs (confirm() signature at line 343; transition_state at ~203; the 6 test call sites at lines 717, 750, 753, 794, 819, 841; deny() — must stay &Connection)
    - cli/caprun/src/main.rs (the confirm call site at line 319, inside run_confirm_or_deny)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Pitfall 1 — exact call-site list; the deny() carve-out)
  </read_first>
  <action>
    Change `confirm()`'s parameter from `conn: &rusqlite::Connection` to `conn: &mut rusqlite::Connection` (this enables `conn.transaction()` in Task 2). Update all 7 call sites so the owning binding is `let mut conn = ...` and the call passes `&mut conn`:
    - cli/caprun/src/main.rs:319 — `confirm(&conn, ...)` → `confirm(&mut conn, ...)`; ensure the `conn` binding opened via `open_audit_db` upstream is `let mut conn`.
    - crates/brokerd/src/confirmation.rs test module: lines 717, 750, 753, 794, 819, 841.
    Do NOT change `deny()`'s signature — it opens no transaction and dispatches no send; leave it `&rusqlite::Connection`. `transition_state` and `append_event` also keep their `&rusqlite::Connection` params (a `&Transaction` deref-coerces to `&Connection`, so Task 2 can pass `&tx` to them unchanged).
  </action>
  <verify>
    <automated>cargo build -p brokerd -p caprun 2>&1 | grep -qi "error" && echo FAIL || echo OK; cargo test -p brokerd 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p brokerd -p caprun` compiles with no errors after the signature change.
    - `grep -n 'pub fn confirm' crates/brokerd/src/confirmation.rs` shows `conn: &mut rusqlite::Connection`.
    - `deny()`'s signature is unchanged (`grep -n 'pub fn deny' confirmation.rs` still shows `&rusqlite::Connection`).
    - Existing brokerd tests still pass (`cargo test -p brokerd`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Special-case email.send dispatch with atomic CAS + email_send_attempted transaction</name>
  <files>crates/brokerd/src/confirmation.rs, cli/caprun/src/main.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs (the dispatch match at lines 400-436; the generic Step 6 transition_state call at ~395; the file.create arm at 403-416 to leave byte-for-byte unchanged; ConfirmOutcome enum at 219-238)
    - cli/caprun/src/main.rs (the exit-code map at lines 328-339 — mirror its arm shape)
    - crates/brokerd/src/sinks/email_smtp.rs (invoke_email_smtp_from_resolved signature from Plan 01)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Pattern 2 — atomic CAS+attempt; the anti-pattern of reusing the generic Step 6 call)
  </read_first>
  <behavior>
    - A first confirm of an email.send effect: CAS matches one row, email_send_attempted appended in the same transaction, send performed, email_send_succeeded appended, outcome Released (exit 0).
    - A second confirm of the same effect_id: CAS matches zero rows → the transaction rolls back with NO email_send_attempted appended → outcome AlreadyTerminal (exit 5), and NO second send occurs.
    - When the adapter send returns Err: outcome EmailSendFailed (exit 7), email_send_failed already appended by the adapter, no auto-retry.
  </behavior>
  <action>
    Restructure the `"email.send"` dispatch arm in `confirm()`. It MUST NOT rely on the generic unconditional Step 6 `transition_state` call that currently fires before the match (that produces two separate autocommit statements — silently failing SEND-01). Add `ConfirmOutcome::EmailSendFailed` to the enum.

    Special-case the arm per Pattern 2: open `let tx = conn.transaction()?;`. Inside the tx: call `transition_state(&tx, effect_id, Confirmed)` to get the affected-row count; if `affected == 0`, drop the tx (auto-rollback) and return `Ok(ConfirmOutcome::AlreadyTerminal)` — no attempt event, no send. Otherwise append the `email_send_attempted` event (opaque payload, effect_id only) via `append_event(&tx, ...)` chained to the granted-event hash, then `tx.commit()?`. The CAS and the attempt-append are now one atomic unit.

    AFTER commit (outside the tx), call `email_smtp::invoke_email_smtp_from_resolved(conn, pc.session_id, pc.effect_id, &pc.resolved_args, <attempted_event_id>, <attempted_event_hash>)`. On Ok → `Ok(ConfirmOutcome::Released)`. On Err → route detail to `logger.error()` (the adapter already appended `email_send_failed` + logged), return `Ok(ConfirmOutcome::EmailSendFailed)`. Do NOT use the `Err(_) => Ok(ConfirmedButSinkFailed)` swallow-shape.

    CRITICAL: the file.create arm and its use of the pre-computed generic Step 6 result must stay exactly as today. Only email.send is special-cased. Ensure the generic Step 6 call does not double-transition for email.send (guard it so email.send skips the generic transition and owns its CAS inside the tx).

    In cli/caprun/src/main.rs, add `ConfirmOutcome::EmailSendFailed => (7, Some("email send failed after confirm; recorded, not retried"))` to the exit-code match, distinct from ConfirmedButSinkFailed (3), Denied (2), UnknownEffect (4), AlreadyTerminal (5).
  </action>
  <verify>
    <automated>cargo test -p brokerd -p caprun 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SEND-01 test: confirming the same email.send effect_id twice yields Released then AlreadyTerminal, and exactly ONE `email_send_attempted` event exists in the audit DAG for that effect_id (query the events table) — `cargo test -p brokerd` passes. Mirrors the existing `confirm_twice_returns_already_terminal_and_creates_no_new_file` pattern.
    - SEND-01 atomicity: a test asserts that when the CAS affects zero rows, no `email_send_attempted` event is appended (the transaction rolled back both together).
    - SEND-02 test: forcing an adapter send failure (closed port) yields `ConfirmOutcome::EmailSendFailed`, a durable `email_send_failed` event, and the CLI maps it to exit code 7 (distinct from 2/3/4/5).
    - The file.create dispatch arm is byte-for-byte unchanged (`git diff` on that arm shows no change).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete the dead email_send.rs stub</name>
  <files>crates/brokerd/src/sinks.rs, crates/brokerd/src/sinks/email_send.rs</files>
  <read_first>
    - crates/brokerd/src/sinks.rs (the `pub mod email_send;` declaration to remove)
    - crates/brokerd/src/sinks/email_send.rs (confirm no external references remain after Task 2 rewire)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Open Question 1 / Pitfall 6 — the explicit delete decision)
  </read_first>
  <action>
    Per the phase's explicit Open Question 1 decision (DELETE), remove `crates/brokerd/src/sinks/email_send.rs` entirely and delete the `pub mod email_send;` line from crates/brokerd/src/sinks.rs. Before deleting, grep-confirm the only remaining references to `invoke_email_send_stub` are inside email_send.rs's own module (Task 2 removed the confirmation.rs reference). If any OTHER caller exists, STOP and surface it instead of deleting.
  </action>
  <verify>
    <automated>grep -rn "invoke_email_send_stub\|mod email_send" crates/ cli/ ; cargo build -p brokerd 2>&1 | grep -qi error && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `crates/brokerd/src/sinks/email_send.rs` no longer exists.
    - `grep -rn 'email_send\b' crates/brokerd/src/sinks.rs` returns nothing (the module declaration is gone).
    - `grep -rn 'invoke_email_send_stub' crates/ cli/` returns nothing.
    - `cargo build -p brokerd` and `cargo test -p brokerd` still succeed (no dangling references).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| duplicate confirm attempt → irreversible send | Any re-issued confirm for a terminal effect_id must not re-fire the wire action |
| adapter failure → CLI caller | A post-confirm send failure must be distinguishable from denied/unknown and must not silently retry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Tampering | confirm() email.send CAS | critical | mitigate | pending→confirmed CAS + email_send_attempted append in ONE atomic transaction before any socket opens; zero-row CAS aborts with rollback (SEND-01) |
| T-13-05 | Repudiation | send-failure path | high | mitigate | Failure surfaces as ConfirmOutcome::EmailSendFailed / exit 7, durable email_send_failed event; never the swallow-shape, no auto-retry (SEND-02) |
| T-13-06 | Elevation of Privilege | confirm path bypassing executor | high | mitigate | Adapter invoked only from the frozen resolved_args snapshot; never re-resolves, never calls submit_plan_node (CON-i2-non-bypassable) |
</threat_model>

<verification>
- `cargo test -p brokerd -p caprun` passes including SEND-01 single-fire and SEND-02 failure tests.
- Double-confirm produces exactly one email_send_attempted event.
- Exit code 7 is distinct and reachable on adapter failure.
</verification>

<success_criteria>
- Confirm-triggered email send is idempotent via the existing CAS wrapped in one atomic transaction.
- Adapter failure is surfaced (exit 7), recorded, and non-retryable.
- The old stub is deleted; file.create's flow is untouched.
</success_criteria>

<output>
Create `.planning/phases/13-real-broker-mediated-smtp-adapter/13-02-SUMMARY.md` when done.
</output>
