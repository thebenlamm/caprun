---
phase: 36-git-commit-sink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [GIT-01]

must_haves:
  truths:
    - "git.commit is registered in KNOWN_SINKS with an exact-match arg schema; an unknown/duplicate/missing arg Denies at the Step-0 schema gate."
    - "git.commit is classified MutateReversible (the first non-CommitIrreversible REAL sink)."
    - "git.commit `message` is content-sensitive, so a tainted message Blocks under the unmodified submit_plan_node collect-then-Block loop."
  artifacts:
    - "crates/executor/src/sink_schema.rs — git.commit KNOWN_SINKS row (allowed=[message], required=[message])"
    - "crates/executor/src/sink_sensitivity.rs — sink_effect_class MutateReversible arm + is_content_sensitive + expected_role rows for git.commit"
  key_links:
    - "sink_effect_class(git.commit) => MutateReversible feeds the session-status gate so an Allowed git.commit survives an I1-demoted (Draft) session."
    - "is_content_sensitive(git.commit, message)=true feeds submit_plan_node's collect-then-Block loop so a tainted message Blocks."
---

<objective>
Add the executor's hardcoded TCB table rows that make `git.commit` a callable,
correctly-classified sink: the `KNOWN_SINKS` schema row (Step-0 fail-closed
gate), the `MutateReversible` effect-class arm, and the `message`
content-sensitivity + role rows. This is the mechanical realization of
DESIGN-git-github-http-sinks.md §1.2 and §1.3 (CONTEXT decisions 2 and 3).

Purpose: `git.commit` cannot be dispatched by the broker (Plan 36-02) until the
executor recognizes it — an unregistered sink is `Denied(UnknownSink)` at Step 0.
This plan delivers the executor half, entirely within `crates/executor`, with no
dependency on `crates/brokerd`, so it can build and test independently.
Output: two edited files + host-portable unit tests proving schema, effect-class,
and sensitivity.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-git-commit-sink/36-CONTEXT.md
@planning-docs/DESIGN-git-github-http-sinks.md
@crates/executor/src/sink_schema.rs
@crates/executor/src/sink_sensitivity.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register git.commit in KNOWN_SINKS (Step-0 exact-match schema gate)</name>
  <files>crates/executor/src/sink_schema.rs</files>
  <read_first>
    - crates/executor/src/sink_schema.rs — the KNOWN_SINKS table (lines 40-76, note the process.exec/file.write rows) and validate_schema (lines 131-159). Add the git.commit row alongside the existing entries in the same style.
  </read_first>
  <behavior>
    - Test: a git.commit node with exactly {message} validates Ok(()).
    - Test: a git.commit node with an unknown arg (e.g. `paths`) Denies UnknownArg.
    - Test: a git.commit node with a duplicated `message` Denies DuplicateArg.
    - Test: a git.commit node missing `message` Denies MissingArg("message").
    - Test: git.commit is a registered sink (schema_for returns Some), so it is never UnknownSink.
  </behavior>
  <action>
    Add a new SinkSchema entry to KNOWN_SINKS for git.commit per DESIGN §1.3 and
    CONTEXT decision 3 (GIT-01). Scope the arg set to `message` ONLY:
    allowed = ["message"], required = ["message"]. Do NOT model a paths/pathspec
    arg — Phase 36 commits already-STAGED workspace changes (ROADMAP Phase 36
    success criterion 1), so `git commit -m <message>` needs no pathspec; keeping
    the schema to a single exact-match arg tightens the Step-0 fail-closed gate.
    Follow the exact-match shape of the file.write row (both allowed and required),
    NOT process.exec's optional-arg asymmetry. Add unit tests mirroring the
    existing file.write schema tests (exact-args-ok, unknown-arg-denied,
    duplicate-arg-denied, missing-required-denied) plus an assertion that
    git.commit is not treated as UnknownSink.
  </action>
  <verify>
    <automated>cargo test -p executor sink_schema 2>&1 | tail -20</automated>
  </verify>
  <done>git.commit is in KNOWN_SINKS with allowed=["message"]/required=["message"]; validate_schema Denies unknown/duplicate/missing args; new schema tests pass; `cargo test -p executor` green on macOS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify git.commit MutateReversible + message content-sensitive</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs — sink_effect_class (lines 40-59, note the fail-closed `_ => CommitIrreversible` default and the test-only test.observe arm), is_content_sensitive (lines 140-148), is_routing_sensitive (lines 122-131), expected_role (lines 188-253, note process.exec command/args => None with the "no role mint site" rationale at lines 231-243).
  </read_first>
  <behavior>
    - Test: sink_effect_class(git.commit) == MutateReversible (the first non-CommitIrreversible real sink).
    - Test: is_content_sensitive(git.commit, "message") == true.
    - Test: is_routing_sensitive(git.commit, "message") == false (git.commit has no routing-sensitive arg — no path/destination).
    - Test: expected_role(git.commit, "message") == None.
  </behavior>
  <action>
    Three surgical edits to the hardcoded tables per DESIGN §1.2/§1.3 and CONTEXT
    decisions 2 and 3 (GIT-01):
    (1) sink_effect_class: add a `"git.commit" => EffectClass::MutateReversible`
    arm BEFORE the fail-closed `_ => CommitIrreversible` default. This is a
    deliberate, justified exception to the unknown-defaults-to-CommitIrreversible
    rule — a local commit is reversible (git reset/amend/branch-delete) and causes
    no external effect (only push/pr leave the boundary), matching the locked
    3-class Effect ontology's ReversibleEffect (runtime-core/src/effect.rs). This
    is what lets an Allowed git.commit survive an I1-demoted (Draft) session.
    (2) is_content_sensitive: add a `"git.commit" => ["message"].contains(&arg_name)`
    arm (introduce a `GIT_COMMIT_CONTENT_SENSITIVE: &[&str] = &["message"]` const
    mirroring EMAIL_SEND_CONTENT_SENSITIVE). The `message` is the taint CARRIER
    that must genuinely propagate downstream and Blocks under the unmodified
    collect-then-Block loop exactly like an email.send body.
    (3) expected_role: add a `"git.commit" => match arg_name { "message" => None, _ => None }`
    arm. `message` is deliberately unconstrained at the structural Step-1c role
    gate — reuse the process.exec command/args rationale: there is no
    origin_role-producing mint site for a legitimately-authored commit message, so
    pinning Some(...) would fail-closed-Deny the legit UserTrusted-message flow.
    The Block for a tainted message comes entirely from is_content_sensitive + the
    untrusted-taint check, NOT this gate.
    Do NOT add a git.commit arm to is_routing_sensitive — git.commit has no
    routing-sensitive arg, so `message` correctly falls through to the `_ => false`
    default; add a test asserting that fall-through rather than a table row.
    Add unit tests mirroring the process.exec sensitivity tests.
  </action>
  <verify>
    <automated>cargo test -p executor sink_sensitivity 2>&1 | tail -20</automated>
  </verify>
  <done>sink_effect_class(git.commit)==MutateReversible; is_content_sensitive(git.commit,"message")==true; is_routing_sensitive(git.commit,"message")==false; expected_role(git.commit,"message")==None; new tests pass; `cargo test -p executor` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → executor(I2) | plan-node args (including a possibly-tainted `message`) cross into the deterministic executor for classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Elevation of Privilege | sink_effect_class git.commit arm | high | mitigate | MutateReversible is a scoped, single-arm exception; the fail-closed `_ => CommitIrreversible` default is preserved so every OTHER unregistered sink stays maximally restrictive (DESIGN §1.2). |
| T-36-02 | Information Disclosure | is_content_sensitive(git.commit,message) | high | mitigate | `message` classified content-sensitive so a tainted (exfil-carrying) message Blocks under the unmodified collect-then-Block loop (CONTENT-01 reuse, DESIGN §1.3). |
| T-36-03 | Tampering | sink_schema KNOWN_SINKS row | medium | mitigate | Exact-match schema (allowed=required=[message]) Denies unknown/duplicate/missing args at Step 0 before any resolve/sensitivity/role check. |
| T-36-SC | Tampering | package installs | low | accept | No new npm/pip/cargo dependencies in this plan — pure Rust table-row edits within crates/executor. |
</threat_model>

<verification>
- `cargo test -p executor` passes on macOS (host-portable — no Linux-gated tests in this plan).
- `./scripts/check-invariants.sh` stays green (no new EffectRequest token, sensitivity stays hardcoded, no policy file).
- No fourth EffectClass variant introduced; sink_effect_class callers stay exhaustive.
</verification>

<success_criteria>
git.commit is a registered, MutateReversible sink whose `message` is content-sensitive and whose schema fails closed on malformed args — all provable by `cargo test -p executor` on the dev Mac, with no brokerd/launcher dependency.
</success_criteria>

<output>
Create `.planning/phases/36-git-commit-sink/36-01-SUMMARY.md` when done.
</output>
