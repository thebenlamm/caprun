---
phase: 09-session-trust-state-i1-i0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/session.rs
  - crates/runtime-core/src/executor_decision.rs
autonomous: true
requirements: [TAINT-01, TAINT-02, ORIGIN-01, ORIGIN-02]

must_haves:
  truths:
    - "SessionStatus has a Draft variant, inserted after Active, same enum, no wrapper type (D: DESIGN-session-trust-state.md §1)"
    - "DenyReason has exactly one new variant DraftOnlySessionDeniesCommitIrreversible carrying the offending SinkId; no second denial taxonomy exists (§7)"
    - "SeedProvenance { TrustedArg, FileDerived } exists as a typed enum, not a bool/string (RESEARCH OQ2)"
  artifacts:
    - crates/runtime-core/src/session.rs
    - crates/runtime-core/src/executor_decision.rs
  key_links:
    - "SessionStatus::Draft is the state the executor Step 0.5 (Plan 02) and the broker demotion (Plan 03) both key on"
    - "DenyReason::DraftOnlySessionDeniesCommitIrreversible is the single return value TAINT-02 asserts"
---

<objective>
Add the three foundational domain types this phase's mechanism keys on, in the pure `runtime-core` crate: the `SessionStatus::Draft` variant, the `DenyReason::DraftOnlySessionDeniesCommitIrreversible { sink }` variant, and a new `SeedProvenance` enum. These are pure type additions with no I/O — every later plan (executor deny, broker demotion, CLI on-ramp) builds on them.

Purpose: Establish the shared vocabulary first so the breaking `submit_plan_node` signature change (Plan 02) and the broker/CLI wiring (Plans 03/04) reference types that already exist and compile.
Output: Two modified files in `crates/runtime-core/src`; `cargo test -p runtime-core` green.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-session-trust-state.md
@.planning/phases/09-session-trust-state-i1-i0/09-RESEARCH.md
@.planning/phases/09-session-trust-state-i1-i0/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from source-drift verification):
- `SessionStatus::Draft` — new variant on the existing `runtime_core::SessionStatus` enum
- `SeedProvenance` — new enum in `runtime-core` with variants `TrustedArg`, `FileDerived`
- `DenyReason::DraftOnlySessionDeniesCommitIrreversible { sink: SinkId }` — new variant on the existing `runtime_core::DenyReason` enum
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add SessionStatus::Draft and the SeedProvenance enum in runtime-core session.rs</name>
  <files>crates/runtime-core/src/session.rs</files>
  <read_first>
    - crates/runtime-core/src/session.rs (the file being modified — current SessionStatus enum, its derives, and any in-crate match sites)
    - planning-docs/DESIGN-session-trust-state.md §1 (the exact Draft placement + monotonic Active→Draft transition rule) and §3 (seed-provenance requirement)
    - .planning/phases/09-session-trust-state-i1-i0/09-PATTERNS.md (session.rs section — insert-after-Active, serde tag-by-name insertion is safe)
  </read_first>
  <action>
    Insert a `Draft` variant into the existing `runtime_core::SessionStatus` enum, positioned immediately after `Active` (order: `Active, Draft, WaitingApproval, Done, Failed, RolledBack`). Do NOT create a parallel status type or wrapper struct — extend the one enum, per DESIGN-session-trust-state.md §1. Keep all existing derives (`Debug, Clone, PartialEq, Serialize, Deserialize`) unchanged; serde tags by variant name so insertion position is safe.
    Add a new public enum `SeedProvenance` with exactly two variants, `TrustedArg` and `FileDerived`, in this same file (alongside `SessionStatus`), with the same derive set as `SessionStatus`. This is the typed provenance value the CLI (Plan 04) decides and the broker (Plan 03) consumes — a typed enum, never a bool or raw string (RESEARCH OQ2). Do not add it as a field to any struct in this plan; Plans 03/04 decide where it is threaded/persisted.
    If any exhaustive `match` over `SessionStatus` exists inside `runtime-core` itself, add an explicit `Draft` arm (no wildcard) to keep it compiling — the project's exhaustive-match discipline (DESIGN §10) forbids a `_` arm on security enums.
  </action>
  <acceptance_criteria>
    - Source: `SessionStatus` enum contains a `Draft` variant located between `Active` and `WaitingApproval`.
    - Source: a public `SeedProvenance` enum exists with variants `TrustedArg` and `FileDerived` and no third variant.
    - Behavior: `cargo test -p runtime-core` compiles and passes (no non-exhaustive-match errors introduced).
    - Negative: no new enum or wrapper type named anything other than `SeedProvenance` was introduced for status; `SessionStatus` remains the single status enum.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p runtime-core --no-fail-fast</automated>
  </verify>
  <done>`SessionStatus::Draft` and `SeedProvenance { TrustedArg, FileDerived }` exist in `crates/runtime-core/src/session.rs`; runtime-core builds and tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Append the DraftOnlySessionDeniesCommitIrreversible variant to DenyReason</name>
  <files>crates/runtime-core/src/executor_decision.rs</files>
  <read_first>
    - crates/runtime-core/src/executor_decision.rs (the file being modified — current `DenyReason` enum, its doc comment forbidding a second taxonomy, its derives, and how `SinkId` is imported/referenced)
    - crates/runtime-core/src/plan_node.rs (confirm the `SinkId` type path so the new variant's field type resolves)
    - planning-docs/DESIGN-session-trust-state.md §7 (exact variant name + that it carries the offending `SinkId`) and §10 (exhaustive-match, no wildcard)
  </read_first>
  <action>
    Append exactly one variant to the existing `runtime_core::DenyReason` enum: `DraftOnlySessionDeniesCommitIrreversible { sink: SinkId }`. Use the struct-variant form carrying a named `sink` field of type `SinkId` (matching the doc's §7 shape and the `UnknownSink(String)` convention of carrying the offending identity for audit/CLI legibility). Import/reference `SinkId` via the same path `plan_node.rs` exposes it. Do NOT introduce a second, parallel `DenyReason`-like enum — the enum's own doc comment states "never introduce a second denial error type"; this is an append to the ONE taxonomy (§7).
    If the crate has any exhaustive `match` over `DenyReason` (e.g. Display/serialization helpers), add an explicit arm for the new variant — no `_` wildcard (§10).
  </action>
  <acceptance_criteria>
    - Source: `DenyReason` contains exactly one new variant named `DraftOnlySessionDeniesCommitIrreversible` with a named field `sink: SinkId`.
    - Source: no new enum was added for draft-only denials; the count of `enum DenyReason` declarations in the crate remains 1.
    - Behavior: `cargo test -p runtime-core` compiles and passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p runtime-core --no-fail-fast</automated>
  </verify>
  <done>`DenyReason::DraftOnlySessionDeniesCommitIrreversible { sink: SinkId }` exists on the single `DenyReason` enum; runtime-core builds and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure domain types with no I/O and no trust boundary crossing; boundaries are exercised by Plans 02–04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | New security enums (`SessionStatus`, `DenyReason`) matched non-exhaustively downstream, silently fail-open on a future variant | high | mitigate | Enums are matched exhaustively (no `_` arm) at every downstream call site — enforced in Plans 02/03; this plan preserves in-crate exhaustive matches per DESIGN §10 |
| T-09-SC | Tampering | Supply-chain (package installs) | low | accept | No `cargo add` / package installs this phase (RESEARCH: Package Legitimacy Audit N/A); no `[ASSUMED]`/`[SUS]` packages |
</threat_model>

<verification>
- `cargo test -p runtime-core --no-fail-fast` green.
- `SessionStatus::Draft`, `SeedProvenance`, and `DenyReason::DraftOnlySessionDeniesCommitIrreversible { sink }` all present and referenced by name.
</verification>

<success_criteria>
All three new types exist in `runtime-core`, the crate compiles and its tests pass, and no parallel/duplicate status or denial taxonomy was introduced.
</success_criteria>

<output>
Create `.planning/phases/09-session-trust-state-i1-i0/09-01-SUMMARY.md` when done.
</output>
