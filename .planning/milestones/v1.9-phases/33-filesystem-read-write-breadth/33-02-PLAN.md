---
phase: 33-filesystem-read-write-breadth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [FS-03]
must_haves:
  truths:
    - "file.write is a registered KNOWN_SINKS entry with allowed/required = [path, contents], exact-match — an unregistered arg Denies at Step 0"
    - "file.write path is routing-sensitive and contents is content-sensitive — a tainted value in either Blocks under the UNMODIFIED collect-then-Block loop"
    - "file.write contents expected_role admits the untrusted exec_output and doc_fragment roles PLUS the trusted path role, so a chained exec-output value reaches I2's per-arg Block instead of a structural Step-1c Deny"
    - "file.write is CommitIrreversible — a Draft-status session cannot invoke it (existing I0 class-deny fires unchanged)"
  artifacts:
    - "crates/executor/src/sink_schema.rs: KNOWN_SINKS entry sink=\"file.write\""
    - "crates/executor/src/sink_sensitivity.rs: FILE_WRITE_ROUTING_SENSITIVE, FILE_WRITE_CONTENT_SENSITIVE consts + 4 match arms (sink_effect_class, is_routing_sensitive, is_content_sensitive, expected_role)"
    - "inline unit tests for file.write schema/sensitivity/role"
  key_links:
    - "file.write's contents expected_role = Some(&[\"path\", \"exec_output\", \"doc_fragment\"]) — WIDER than file.create's contents role list (which is Some(&[\"path\"]))"
  prohibitions:
    - "NO change to submit_plan_node's collect-then-Block loop (crates/executor/src/lib.rs) — table entries only"
    - "NO new ExecutorDecision variant"
    - "file.write contents expected_role MUST NOT be a blind copy of file.create's Some(&[\"path\"]) — omitting exec_output would fail-closed-Deny a legitimate process.exec → file.write chain at Step 1c (RESEARCH A4 / DESIGN §4.3)"
---

<objective>
Register `file.write` as a table-entries-only extension of the executor's schema, sensitivity, and slot-type-binding machinery, so its `path`/`contents` args are governed by the UNMODIFIED I2 collect-then-Block loop exactly like `file.create` and `email.send`.

Purpose: FS-03 requires the fs write/edit sink args be governed by the executor under the same I2 discipline — a tainted path or contents Blocks, with no I2 bypass and no new enforcement logic. Per DESIGN §4.1 this is achieved with ONLY new table rows.
Output: a `KNOWN_SINKS` entry and the four `sink_sensitivity` additions (+ consts) for `file.write`, with unit tests.

## Artifacts this plan produces
- `KNOWN_SINKS` row: `SinkSchema { sink: "file.write", allowed: &["path","contents"], required: &["path","contents"] }`
- `FILE_WRITE_ROUTING_SENSITIVE: &[&str] = &["path"]`, `FILE_WRITE_CONTENT_SENSITIVE: &[&str] = &["contents"]`
- `"file.write"` arms in `sink_effect_class`, `is_routing_sensitive`, `is_content_sensitive`, `expected_role`
- unit tests: file.write schema exact-match, path routing-sensitive, contents content-sensitive, contents expected_role includes exec_output/doc_fragment, file.write CommitIrreversible
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/33-filesystem-read-write-breadth/33-RESEARCH.md
@.planning/phases/33-filesystem-read-write-breadth/33-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the file.write KNOWN_SINKS entry and the four sink_sensitivity arms</name>
  <files>crates/executor/src/sink_schema.rs, crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_schema.rs (KNOWN_SINKS at 40-73 — mirror the "file.create" entry at 54-57)
    - crates/executor/src/sink_sensitivity.rs (FILE_CREATE_* consts at 67/86; sink_effect_class arm at 43; is_routing_sensitive at 113; is_content_sensitive at 130; expected_role file.create arm at 182-196)
    - planning-docs/DESIGN-effect-breadth-exec.md §4.3 (fs write/edit slot roles — the WIDER contents role list)
    - .planning/phases/33-filesystem-read-write-breadth/33-PATTERNS.md ("sink_sensitivity.rs" assignment — the exact new arms)
  </read_first>
  <action>
    In `sink_schema.rs`, add a `SinkSchema { sink: "file.write", allowed: &["path", "contents"], required: &["path", "contents"] }` row to `KNOWN_SINKS`, placed after the `file.create` entry. Both args required, exact-match — mirrors file.create (no optional-arg asymmetry like process.exec's args/cwd).

    In `sink_sensitivity.rs`:
    - Add consts `FILE_WRITE_ROUTING_SENSITIVE: &[&str] = &["path"];` and `FILE_WRITE_CONTENT_SENSITIVE: &[&str] = &["contents"];` beside the `FILE_CREATE_*` consts.
    - Add `"file.write" => EffectClass::CommitIrreversible,` to `sink_effect_class`.
    - Add `"file.write" => FILE_WRITE_ROUTING_SENSITIVE.contains(&arg_name),` to `is_routing_sensitive`.
    - Add `"file.write" => FILE_WRITE_CONTENT_SENSITIVE.contains(&arg_name),` to `is_content_sensitive`.
    - Add an `expected_role` arm for `"file.write"`: `"path" => Some(&["path", "relative_path"])` (mirrors file.create's path role verbatim); `"contents" => Some(&["path", "exec_output", "doc_fragment"])` (WIDER than file.create's `Some(&["path"])`); `_ => None`. The `exec_output` entry admits Phase 32's chained-exec origin_role and `doc_fragment` mirrors email.send's body precedent, so a tainted value routed to `contents` is role-admissible and reaches I2's content-sensitivity Block rather than a structural Step-1c Deny (DESIGN §4.3, RESEARCH A4). Keep a comment explaining WHY the list is wider than file.create's, by concept.
  </action>
  <verify>
    <automated>cargo build -p executor</automated>
  </verify>
  <acceptance_criteria>
    - Source: `KNOWN_SINKS` contains a `file.write` entry with allowed=required=[path,contents]; `sink_sensitivity.rs` has both `FILE_WRITE_*` consts and all four `"file.write"` match arms.
    - Behavior: `expected_role(file.write, "contents")` returns a list containing `"exec_output"` and `"doc_fragment"`; `expected_role(file.write, "path")` returns `["path","relative_path"]`.
    - Test command: `cargo build -p executor` compiles clean.
  </acceptance_criteria>
  <done>file.write is fully registered in the schema + sensitivity/role tables; executor compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for file.write schema, sensitivity, and slot-type-binding roles</name>
  <files>crates/executor/src/sink_schema.rs, crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs test module (file.create tests at 244-412, process.exec tests at 422-472 — mirror their assertion shape)
    - crates/executor/src/sink_schema.rs test module (validate_schema tests around 193-198)
  </read_first>
  <behavior>
    - file.write is a valid KNOWN_SINKS entry; a plan node with args {path, contents} validates; an unknown arg (e.g. "mode") Denies at the schema gate.
    - is_routing_sensitive(file.write, "path") == true; is_routing_sensitive(file.write, "contents") == false.
    - is_content_sensitive(file.write, "contents") == true; is_content_sensitive(file.write, "path") == false.
    - sink_effect_class(file.write) == CommitIrreversible.
    - expected_role(file.write, "path") == Some(&["path","relative_path"]).
    - expected_role(file.write, "contents") == Some(&["path","exec_output","doc_fragment"]).
    - expected_role(file.write, "mode") == None.
  </behavior>
  <action>
    Add inline `#[cfg(test)]` tests mirroring the existing file.create / process.exec test functions. Add a `file_write()` SinkId helper (like `file_create()` at 235). Assert each behavior above with a descriptive message. These are plain Rust unit tests that compile and run on macOS (no cfg-linux gate).
  </action>
  <verify>
    <automated>cargo test -p executor file_write</automated>
  </verify>
  <acceptance_criteria>
    - Source: named tests exist covering schema exact-match, routing/content sensitivity split, CommitIrreversible class, and both expected_role lists (including the exec_output/doc_fragment admission).
    - Behavior: all file.write unit tests pass on macOS.
    - Test command: `cargo test -p executor file_write` reports the new tests passing.
  </acceptance_criteria>
  <done>All file.write executor-table unit tests pass on macOS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → executor | worker-submitted plan-node args (path, contents) are classified and taint-checked before any effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Elevation of Privilege | file.write escaping the I2 collect-then-Block loop | critical | mitigate | table-entries-only extension of the UNMODIFIED submit_plan_node; KNOWN_SINKS + routing/content sensitivity rows route every arg through the existing enforcement |
| T-33-04 | Tampering / Information Disclosure | tainted contents (exec_output / doc_fragment) planting attacker data into a file | high | mitigate | contents is content-sensitive → Blocks on untrusted taint, same as email.send body / file.create HARDEN-05 precedent |
| T-33-05 | Elevation / Availability | contents expected_role too narrow → legitimate exec→write chain fail-closed-Denied at Step 1c | medium | mitigate | expected_role includes exec_output + doc_fragment per DESIGN §4.3; unit test asserts the admission |
| T-33-SC | Tampering (supply chain) | dependencies | low | accept | no new package installs this phase — executor deps unchanged |
</threat_model>

<verification>
- `cargo build -p executor` clean.
- `cargo test -p executor file_write` green on macOS.
- No modification to `crates/executor/src/lib.rs` submit_plan_node (table entries only — verify via `git diff --name-only` shows lib.rs untouched by this plan).
</verification>

<success_criteria>
file.write is governed by the executor's I2 machinery via new table rows only: tainted path/contents Block, exec_output/doc_fragment contents is role-admissible, CommitIrreversible class enforced, zero enforcement-logic changes.
</success_criteria>

<output>
Create `.planning/phases/33-filesystem-read-write-breadth/33-02-SUMMARY.md` when done.
</output>
