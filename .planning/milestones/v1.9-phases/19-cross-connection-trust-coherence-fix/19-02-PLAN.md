---
phase: 19-cross-connection-trust-coherence-fix
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [TRUST-03, DOC-02]
must_haves:
  truths:
    - "`scripts/mailpit-verify.sh` is independently re-run on real Linux (Colima+Docker) and the full `cargo test --workspace --no-fail-fast` suite passes green — no regression from the Plan-01 fix."
    - "The three restructured tests in two_connection_intent_bypass.rs (guard-a control, overlapping, sequential-reconnect) all pass green on real Linux — the actual RED→GREEN proof of TRUST-01/TRUST-02."
    - "Actual pass/fail counts are captured and reported from the live run — not assumed from a prior pass."
    - "PROJECT.md's DOC-02 disclosure states the fix has SHIPPED (not just designed), cites the mechanism (one-way accept-time occupancy latch), and references the concrete test evidence (both regression variants green + live mailpit-verify.sh rerun)."
  artifacts:
    - .planning/PROJECT.md
  key_links:
    - "DOC-02's finalized disclosure MUST cite Plan 01's shipped mechanism and THIS plan's live Linux evidence — the doc correction is only valid downstream of a green Linux run (a red run blocks the doc task and returns to Plan 01)."
---

<objective>
Independently prove the Plan-01 fix on real Linux via the project's live acceptance harness, capture the actual pass/fail counts, then finalize PROJECT.md's DOC-02 disclosure against the SHIPPED fix (not the design).

Purpose: TRUST-03 requires the v1.3 live acceptance (`scripts/mailpit-verify.sh` → `cargo test --workspace --no-fail-fast` on real Linux) to be independently re-run and still green — no regression. This same run is the actual RED→GREEN observation of the three restructured tests, which cannot be observed on the macOS dev box (`#[cfg(target_os = "linux")]` → "0 passed"). DOC-02 then finalizes the scoping-time draft disclosure in PROJECT.md against the shipped mechanism and this concrete evidence. Binding spec: `planning-docs/DESIGN-session-trust-coherence.md` §1 (both variants must go green) and the planning brief's DOC-02 charge.

Output: a captured live-Linux verification result (pass/fail counts in the SUMMARY) and a finalized DOC-02 disclosure in `.planning/PROJECT.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@planning-docs/DESIGN-session-trust-coherence.md
@.planning/phases/19-cross-connection-trust-coherence-fix/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the live Linux acceptance and capture real pass/fail counts (TRUST-03)</name>
  <files>(no source files modified — this task runs the verification harness and reports)</files>
  <read_first>
    - CLAUDE.md "Linux-only security tests" + "From Phase 16 onward, ALL Linux verification goes through scripts/mailpit-verify.sh" — the exact recipe, the `MAILPIT_VERIFY_CMD` override, why a Mailpit sidecar is required even for benign runs, and the `libssl-dev`/`pkg-config` prereqs the script installs.
    - scripts/mailpit-verify.sh (read the whole script) — confirm how it resolves the Mailpit container IP, wires `CAPRUN_SMTP_HOST`/`CAPRUN_SMTP_PORT`, and what its default `MAILPIT_VERIFY_CMD` is.
    - .planning/phases/19-cross-connection-trust-coherence-fix/19-01-SUMMARY.md — the three test fn names to scope the first run to.
    - The user's memory note "Verification exit code through a pipe": `script | tail` returns tail's status (always 0). Capture the real exit status and assert on named tests + counts, NEVER on a piped exit 0.
  </read_first>
  <action>
    Ensure Colima is running (`colima start` if needed). Then run the live verification in TWO steps, capturing full output and the REAL exit code of `mailpit-verify.sh` each time (capture `$?` immediately, before any pipe — do not infer PASS from a piped `tail` exit 0, per the "Verification exit code through a pipe" incident).

    Step 1 — scope to the changed test file first for a fast signal:
    `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test two_connection_intent_bypass -- --nocapture' bash scripts/mailpit-verify.sh`
    Confirm all THREE test functions (guard-a control, overlapping, sequential-reconnect) report `ok` / pass — 3 passed, 0 failed, 0 ignored. If any is still `#[ignore]`d or fails, STOP: the fix or the test restructure is wrong — this blocks Task 2 and returns to Plan 01 (gap-closure), never weaken the assertions.

    Step 2 — full no-regression suite (TRUST-03's actual requirement):
    `bash scripts/mailpit-verify.sh` (default `MAILPIT_VERIFY_CMD` = `cargo test --workspace --no-fail-fast`). Confirm the full workspace suite passes with no regression from the pre-fix baseline.

    Capture, for BOTH steps, the exact `test result:` summary line(s) (passed/failed/ignored counts) and the real exit status. Record these verbatim in the SUMMARY — this is TRUST-03's evidence and the RED→GREEN proof for TRUST-01/02. Tear down is handled by the script. If the full run is red for any pre-existing (unrelated) reason, diagnose whether it is a regression from Plan 01's change specifically before proceeding.
  </action>
  <acceptance_criteria>
    - Step 1: `cargo test -p brokerd --test two_connection_intent_bypass` on Linux reports 3 passed, 0 failed, 0 ignored — the guard-a control, overlapping, and sequential-reconnect variants all green. The real (pre-pipe) exit status of `mailpit-verify.sh` is 0.
    - Step 2: `bash scripts/mailpit-verify.sh` (full `cargo test --workspace --no-fail-fast`) reports 0 failed across the workspace; real exit status 0. No test that passed pre-fix now fails (no regression).
    - The captured `test result:` summary lines and real exit codes for both steps are recorded verbatim in the SUMMARY — counts are reported, not assumed.
    - The `two_connection_intent_bypass` module reports NON-zero tests run on Linux (proving the `#[cfg]` gate compiled it in and the `#[ignore]` was removed) — not "0 passed".
  </acceptance_criteria>
  <verify>
    <automated>colima status 2>&1 | tail -2 && MAILPIT_VERIFY_CMD='cargo test -p brokerd --test two_connection_intent_bypass -- --nocapture' bash scripts/mailpit-verify.sh; echo "SCOPED_EXIT=$?"</automated>
  </verify>
  <done>Both the scoped run (3/3 new tests green) and the full `cargo test --workspace --no-fail-fast` run pass on real Linux with real exit code 0, and the actual pass/fail counts are captured verbatim. This is the shipped-fix proof consumed by Task 2. A red result blocks Task 2 and returns to Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize PROJECT.md's DOC-02 disclosure against the shipped fix (DOC-02)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 20-50 (the v1.4 Active-section Goal + scoping-time framing) and lines 96-125 (the `<details>` "⚠️ Superseded finding (v1.4 Phase 0)" block + the DOC-01 paragraph naming the cross-connection gap as an accepted residual), and lines 290-315 (the Phase 18/Phase 19 progress notes). These carry the scoping-time DRAFT disclosure that DOC-02 now finalizes against the SHIPPED fix.
    - .planning/REQUIREMENTS.md line 59 (DOC-02's exact charge: record that v1.3's guard(a) was cross-connection-bypassable and that v1.4 Phase 0 FIXES it; finalize the scoping-time draft against the shipped fix).
    - .planning/phases/19-cross-connection-trust-coherence-fix/19-01-SUMMARY.md (the shipped mechanism: exact latch, one-way, the three test variants) and THIS plan's Task 1 evidence (the live Linux pass/fail counts).
    - planning-docs/DESIGN-session-trust-coherence.md §2 (the one-way accept-time latch mechanism to cite by name).
  </read_first>
  <action>
    Update the DOC-02 disclosure in PROJECT.md so it reads as FINALIZED against the shipped fix, not aspirational. Concretely, edit the "⚠️ Superseded finding (v1.4 Phase 0)" block (and any Active-section note that says Phase 19 is "next"/pending) to state that the fix has SHIPPED: (1) name the mechanism — a one-way, session-lifetime occupancy latch in the broker's accept loop that rejects any 2nd connection to an already-active session before any per-connection guard(a)/`session_status` state is seeded; (2) state that this restores the `UserTrusted == human-typed` invariant across connections; (3) cite the concrete test evidence — `two_connection_intent_bypass.rs`'s overlapping AND sequential-reconnect variants both green, and the full `scripts/mailpit-verify.sh` (`cargo test --workspace --no-fail-fast`) live Linux rerun green with the counts from Task 1.

    Keep the honest framing already present (not a production incident — nothing deployed, repo unpushed). Do NOT overclaim: the fix closes the cross-connection `ProvideIntent` bypass specifically; the replay residual (DESIGN §6, re-earned) and the other v1.3 residuals remain as separately disclosed. Make only surgical edits to the DOC-02 disclosure and its cross-references — do not rewrite unrelated PROJECT.md sections.

    Do NOT touch ROADMAP.md or STATE.md (the orchestrator owns phase-completion state, per the standing v1.4 mitigation).
  </action>
  <acceptance_criteria>
    - PROJECT.md's superseded-finding / DOC-02 block states the fix has SHIPPED and names the mechanism as a one-way accept-time occupancy latch that rejects a 2nd connection before per-connection trust state is seeded. Evidence: `grep -niE 'shipped|one-way|occupancy|latch|accept-time' .planning/PROJECT.md` returns matches in the DOC-02 region.
    - The disclosure cites the concrete test evidence: both `two_connection_intent_bypass.rs` variants (overlapping + sequential-reconnect) green AND the live `mailpit-verify.sh` rerun. Evidence: `grep -n 'two_connection_intent_bypass\|mailpit-verify' .planning/PROJECT.md` returns matches in the DOC-02 region.
    - No aspirational "Phase 19 (next)"/"will fix"/"pending" language remains describing this fix as unshipped in the Active section.
    - `git diff --stat .planning/PROJECT.md` shows only PROJECT.md changed; ROADMAP.md and STATE.md are untouched by this plan.
  </acceptance_criteria>
  <verify>
    <automated>grep -niE 'shipped|one-way|occupancy latch|accept-time' .planning/PROJECT.md | head && grep -n 'two_connection_intent_bypass\|mailpit-verify' .planning/PROJECT.md | head</automated>
  </verify>
  <done>PROJECT.md's DOC-02 disclosure is finalized: it states the fix shipped, names the one-way accept-time occupancy-latch mechanism, cites both regression variants + the live Linux rerun as evidence, keeps the honest "not a production incident" framing, and touches no other file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev claim → shipped reality (documentation honesty) | DOC-02 asserts a security property to future readers; the boundary is between what the doc claims and what the code+tests actually prove. Mitigated by requiring live-Linux evidence before the doc is finalized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Repudiation | live verification evidence (piped exit code) | high | mitigate | Capture the real `$?` of `mailpit-verify.sh` before any pipe and assert on named tests + counts, never on a piped `tail` exit 0 (per the "Verification exit code through a pipe" incident that nearly shipped a false PASS). |
| T-19-05 | Spoofing | DOC-02 disclosure overclaiming | medium | mitigate | DOC-02 is only finalized downstream of a green Linux run; a red run blocks the doc task. The disclosure is scoped to the cross-connection bypass and does not claim the replay residual is closed. |
| T-19-SC | Tampering | cargo dependency surface | low | accept | No new crate dependency added this phase; `mailpit-verify.sh` installs only the pre-existing `libssl-dev`/`pkg-config` build prereqs the v1.3 harness already required. No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- Colima+Docker live run of `scripts/mailpit-verify.sh` executed, real exit codes captured (not piped), pass/fail counts recorded verbatim.
- Scoped run: 3/3 `two_connection_intent_bypass` tests green on Linux (RED→GREEN proof for TRUST-01/02).
- Full run: `cargo test --workspace --no-fail-fast` green, no regression (TRUST-03).
- PROJECT.md DOC-02 disclosure finalized against shipped fix + live evidence; only PROJECT.md changed; ROADMAP.md/STATE.md untouched.
</verification>

<success_criteria>
- The live v1.3 acceptance (`scripts/mailpit-verify.sh`, full `cargo test --workspace --no-fail-fast`) is independently re-run on real Linux and passes green with captured counts — no regression (TRUST-03).
- Both regression variants (overlapping + sequential-reconnect) pass green on Linux — the shipped-fix proof.
- PROJECT.md's DOC-02 disclosure is finalized: fix shipped, mechanism named (one-way accept-time occupancy latch), concrete test evidence cited, honest framing preserved (DOC-02).
</success_criteria>

<artifacts_produced>
This phase (Plan 02) creates/modifies:
- `.planning/PROJECT.md` — DOC-02 disclosure finalized against the shipped fix (DOC-02)
- (verification artifact) captured live-Linux pass/fail counts recorded in `19-02-SUMMARY.md` (TRUST-03 evidence)
</artifacts_produced>

<output>
Create `.planning/phases/19-cross-connection-trust-coherence-fix/19-02-SUMMARY.md` when done. Record: the verbatim `test result:` summary lines + real exit codes for both the scoped and full Linux runs, confirmation all three regression variants are green, and the DOC-02 edit made. Do NOT touch ROADMAP.md or STATE.md (the orchestrator owns phase-completion state).
</output>
