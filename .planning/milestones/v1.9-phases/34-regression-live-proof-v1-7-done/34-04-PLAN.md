---
phase: 34-regression-live-proof-v1-7-done
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_7_composed.rs
autonomous: true
requirements: [LIVE-01, LIVE-02]
must_haves:
  truths:
    - "On real Linux, ONE composed acceptance run (a single shared audit.db, N legs, each session's verify_chain independently true) proves: (a) a tainted exec-output routed to a sensitive sink arg is Blocked (I2, genuine non-stapled taint chain); (b) a clean exec/fs path is Allowed; (c) a fs write/edit within WorkspaceRoot succeeds and is audited; (d) the EXEC-05 confirm-release path is exercised (D-12)."
    - "The composed run is executed via mailpit-verify.sh (exec-scoped MAILPIT_VERIFY_CMD), true exit code captured BEFORE any pipe, asserted on named tests + counts (D-13)."
    - "Full-workspace regression re-runs green on real Linux with no regression to v1.0-v1.6, asserted on counts + named tests, with a dedicated negative test per new sink (process.exec, fs write/edit) (D-14)."
  artifacts:
    - "cli/caprun/tests/live_acceptance_v1_7_composed.rs — the multi-leg shared-audit.db composed live-proof test (cfg linux)"
    - "A recorded LIVE-01 scoped run result (true-exit-0, named tests + counts)"
    - "A recorded LIVE-02 full-workspace regression result (true-exit-0, named tests + counts)"
  key_links:
    - "composed test legs -> one shared audit.db (never a fresh :memory: per leg)"
    - "each leg session -> its own verify_chain assertion (true)"
    - "LIVE-02 full run -> the existing s9_process_exec_block + s9_file_write_block per-sink negative tests (must still pass)"
  prohibitions:
    - "Never assert success on exit 0 through a pipe (`script | tail`) — capture `$?` before any pipe (D-13/D-14)."
    - "Never reopen a fresh in-memory DB per leg — the composed proof is one shared audit.db across sessions (RESEARCH composed pattern)."
    - "This plan adds no TCB source — only a test file + verification runs. No new ExecutorDecision / submit_plan_node / raw effect-request path."
---

<objective>
Prove v1.7 end-to-end on real Linux and regress the whole workspace. LIVE-01: author `live_acceptance_v1_7_composed.rs` — one shared-audit.db composed run whose legs cover the tainted-exec-output Block (I2, non-stapled, `verify_chain` true), a clean exec/fs Allow, a fs write/edit within `WorkspaceRoot`, and the EXEC-05 confirm-release path — then run it scoped via `mailpit-verify.sh` with true-exit-before-pipe. LIVE-02: run the full workspace regression green on real Linux with no regression to v1.0-v1.6, asserted on counts + named tests, with the dedicated per-new-sink negative tests confirmed passing.

Purpose: the v1.7 DONE gate. Authorized only after 34-03's release gates (compile-check + Fable-5 trace) are green.

Output: the composed live-proof test file; recorded LIVE-01 and LIVE-02 results. Closeout (D-17): a human DONE sign-off precedes marking the milestone; NOT pushed unless the operator explicitly asks — this is an orchestrator/operator closeout condition, not an executor task.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-CONTEXT.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-RESEARCH.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-02-SUMMARY.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author live_acceptance_v1_7_composed.rs (shared-audit.db, multi-leg)</name>
  <files>cli/caprun/tests/live_acceptance_v1_7_composed.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_4_composed.rs — the composed multi-leg shared-audit.db structure to mirror (one audit.db across N legs, each leg its own session_id, EACH session's verify_chain independently asserted true; never a per-leg fresh :memory: DB).
    - cli/caprun/tests/live_acceptance_v1_3.rs — the earlier composed proof (Blocked + clean-control + confirm legs) for the block/allow leg shapes.
    - cli/caprun/tests/s9_process_exec_block.rs — the exec Block setup + the EXEC-05 confirm-release leg from 34-02 (reuse its fixtures for legs (a) and (d)).
    - cli/caprun/tests/s9_file_write_block.rs — the fs write/edit within WorkspaceRoot shape for leg (c).
  </read_first>
  <action>
Create `cli/caprun/tests/live_acceptance_v1_7_composed.rs`, `#[cfg(target_os = "linux")]`-gated (the exec legs spawn the confined launcher — macOS stubs prove nothing). Mirror `live_acceptance_v1_4_composed.rs`'s single-shared-audit.db pattern: open ONE audit.db, run each leg as its own `session_id`, and assert each session's `verify_chain` is true independently. Legs (D-12):
  - (a) BLOCK: a first exec produces tainted output (minted via `mint_from_exec`); a second `process.exec` plan node routes that tainted handle into its own `command` arg → the executor Blocks on taint (I2 — a plain taint Block, NOT SlotTypeMismatch; the command slot's expected_role is None, Pitfall 4). Assert the unbroken audit-DAG edge (exec Event → ValueNode → sink arg → block) and `verify_chain` true.
  - (b) ALLOW: a clean exec (or clean fs read) path with trusted args is Allowed and its output/effect is produced ungated.
  - (c) FS WRITE/EDIT: a fs write/edit modifies an existing file within `WorkspaceRoot`, succeeds, and is durably audited.
  - (d) EXEC-05 RELEASE: reuse the 34-02 confirm-release scaffolding to Block a process.exec then release it via `caprun confirm` — runs exactly once, output taint-minted, sink Event chained on the confirm_granted head, `verify_chain` true.
Do not add any TCB source; this is a test that drives the already-built broker/sink paths.
  </action>
  <verify>
    <automated>cargo build --workspace --tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `cli/caprun/tests/live_acceptance_v1_7_composed.rs` exists, is `#[cfg(target_os = "linux")]`-gated, opens ONE shared audit.db, and has the four legs (a)-(d).
    - BEHAVIOR: each leg's session asserts `verify_chain` true; leg (a) asserts a genuine non-stapled taint edge (exec Event → ValueNode → sink arg → block); leg (d) asserts exactly-once release.
    - TEST: `cargo build --workspace --tests` compiles the new test target clean (host build compiles the fn; the confined legs run under Linux in Task 2).
  </acceptance_criteria>
  <done>The composed multi-leg live-proof test compiles, is Linux-gated, uses one shared audit.db, and asserts per-session verify_chain + a genuine (non-stapled) exec-taint Block edge + the EXEC-05 release.</done>
</task>

<task type="auto">
  <name>Task 2: LIVE-01 — run the composed proof on real Linux (true-exit-before-pipe)</name>
  <files>cli/caprun/tests/live_acceptance_v1_7_composed.rs</files>
  <read_first>
    - scripts/verify-harden04-featureless.sh:107-112 — the `set +e ; ... ; rc=$? ; set -e` true-exit-before-pipe pattern to reuse verbatim.
    - scripts/mailpit-verify.sh — the `MAILPIT_VERIFY_CMD` scoping mechanism (Mailpit sits idle for the exec/fs-scoped run; that is fine).
  </read_first>
  <action>
Run the composed proof on real Linux, scoped to the new test target, capturing the TRUE exit code before any pipe (D-13):
  `set +e ; MAILPIT_VERIFY_CMD='cargo build --workspace && cargo test -p caprun --test live_acceptance_v1_7_composed' bash scripts/mailpit-verify.sh > /tmp/34-04-live01.log 2>&1 ; rc=$? ; set -e ; echo "true_exit=$rc"`
Then assert on NAMED tests + counts extracted from `/tmp/34-04-live01.log` (e.g. the composed test name passed, N passed / 0 failed) — NEVER on `$?` through a pipe. Record the true exit code, the named-test pass lines, and the pass/fail counts in the plan SUMMARY. Expected: `true_exit=0`, the composed test passes with all four legs, each session `verify_chain` true.
  </action>
  <verify>
    <automated>set +e ; MAILPIT_VERIFY_CMD='cargo build --workspace && cargo test -p caprun --test live_acceptance_v1_7_composed' bash scripts/mailpit-verify.sh > /tmp/34-04-live01.log 2>&1 ; rc=$? ; set -e ; echo "true_exit=$rc" ; grep -E 'test result|live_acceptance_v1_7' /tmp/34-04-live01.log | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TEST: `true_exit=0` captured BEFORE any pipe (rc=$? immediately after the run); the log shows the composed test named and passing with 0 failed.
    - BEHAVIOR: assertions are on named tests + counts from the log file, never on `$?` through a pipe (D-13).
    - SOURCE: LIVE-01 result (true exit code + named-test lines + counts) recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The composed proof passes on real Linux with true-exit-0 before pipe, all four legs green, each session verify_chain true; result recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: LIVE-02 — full-workspace regression + per-new-sink negative tests</name>
  <files>cli/caprun/tests/live_acceptance_v1_7_composed.rs</files>
  <read_first>
    - scripts/mailpit-verify.sh — default (unscoped) invocation runs `cargo test --workspace --no-fail-fast` with the Mailpit sidecar up (required for the pre-existing email.send suite that must not regress).
    - cli/caprun/tests/s9_process_exec_block.rs, cli/caprun/tests/s9_file_write_block.rs — the dedicated per-new-sink negative tests (must still pass).
  </read_first>
  <action>
Run the full-workspace regression on real Linux with the default unscoped harness (Mailpit up), capturing the TRUE exit code before any pipe (D-14):
  `set +e ; bash scripts/mailpit-verify.sh > /tmp/34-04-live02.log 2>&1 ; rc=$? ; set -e ; echo "true_exit=$rc"`
Then assert green on COUNTS + NAMED tests extracted from `/tmp/34-04-live02.log` — total passed / 0 failed, and specifically the per-new-sink negative tests present and passing: `s9_process_exec_block` (incl. the EXEC-05 confirm-release + entry-guard legs from 34-02) and `s9_file_write_block`. Confirm NO regression to v1.0-v1.6 (the prior suites — live_acceptance_v1_3/v1_4, s9_* — all still pass). NEVER assert on `$?` through a pipe. Record the true exit code, total counts, and the named per-sink negative-test pass lines in the SUMMARY.
Closeout (D-17, NOT an executor step — flag for the operator/orchestrator): after LIVE-01 + LIVE-02 are green, a human DONE sign-off precedes marking the v1.7 milestone; the milestone is NOT pushed unless the operator explicitly requests it.
  </action>
  <verify>
    <automated>set +e ; bash scripts/mailpit-verify.sh > /tmp/34-04-live02.log 2>&1 ; rc=$? ; set -e ; echo "true_exit=$rc" ; grep -E 'test result|s9_process_exec_block|s9_file_write_block' /tmp/34-04-live02.log | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - TEST: `true_exit=0` captured before any pipe; the log shows total passed / 0 failed for the full workspace on real Linux.
    - BEHAVIOR: `s9_process_exec_block` and `s9_file_write_block` (the per-new-sink negative tests) are present and passing; no v1.0-v1.6 suite regressed.
    - SOURCE: LIVE-02 result (true exit code + counts + named per-sink lines) recorded in the SUMMARY; the D-17 human-sign-off / no-push closeout condition is flagged for the operator.
  </acceptance_criteria>
  <done>Full-workspace regression is green on real Linux (true-exit-0 before pipe, counts + named tests), per-new-sink negative tests pass, no v1.0-v1.6 regression; D-17 closeout flagged for the operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| piped test output → success claim | A `script | tail` returns the pipe's exit code, not the command's — a false PASS boundary this plan explicitly guards. |
| new sink surface → v1.0-v1.6 guarantees | The regression proves the two new sinks did not weaken any prior milestone's security guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-04-I | Information Disclosure/false-assurance | piped exit code | high | mitigate | `set +e ; ... ; rc=$? ; set -e` true-exit-before-pipe; assert on counts + named tests, never exit 0 through a pipe (D-13/D-14). |
| T-34-04-T | Tampering | stapled taint in composed Block leg | high | mitigate | Leg (a) asserts a genuine non-stapled edge (exec Event → ValueNode → sink arg → block) + verify_chain true (D-12). |
| T-34-04-E | Elevation of Privilege | new sinks regressing prior guarantees | high | mitigate | Full-workspace regression green with no v1.0-v1.6 regression + per-new-sink negative tests (D-14). |
| T-34-04-SC | Tampering | crates dependencies | high | accept | Zero new packages this phase. |
</threat_model>

<verification>
- LIVE-01: composed proof passes on real Linux via scoped `mailpit-verify.sh`, true-exit-0 before pipe, four legs green, per-session verify_chain true.
- LIVE-02: full-workspace regression green on real Linux, true-exit-0 before pipe, asserted on counts + named tests; per-new-sink negative tests pass; no v1.0-v1.6 regression.
- Closeout (D-17): human DONE sign-off before marking the milestone; not pushed unless the operator requests.
</verification>

<success_criteria>
- One composed real-Linux run proves the tainted-exec Block (I2, non-stapled, verify_chain true) + clean Allow + fs write/edit audited + EXEC-05 release (LIVE-01/D-12/D-13).
- Full-workspace regression green with no v1.0-v1.6 regression, counts + named tests, per-new-sink negative tests (LIVE-02/D-14).
- D-17 closeout condition (human sign-off, no unrequested push) flagged for the operator.
</success_criteria>

<output>
Create `.planning/phases/34-regression-live-proof-v1-7-done/34-04-SUMMARY.md` when done, recording the LIVE-01 and LIVE-02 results and the D-17 closeout flag.
</output>
