---
phase: 50-cli-multi-node-driver-mid-loop-confirm-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/src/stream_hold.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/stream_substrate.rs
  - cli/caprun/tests/stream_hold.rs
autonomous: true
requirements:
  - CLI-02
  - CONFIRM-01

must_haves:
  truths:
    - "On BlockedPendingConfirmation for SafeCodingWorkflow, the worker stays connected, emits a machine-readable BLOCKED line with effect_id + sink, waits for parent PROCEED or ABORT, and never re-submits the blocked PlanNode (CONFIRM-01; CLI-02)."
    - "After PROCEED, step_index advances by one and plan_next continues with later nodes only — submit count grows only for subsequent sinks (no second SubmitPlanNode for the blocked step) (CONFIRM-01)."
    - "ABORT after Block terminates the stream as denied/aborted without further SubmitPlanNode calls (CONFIRM-01; CLI-02)."
    - "Denied / NotImplemented abort remaining nodes; worker emits DENIED with code= (including policy_deny) and exits 2 (CLI-02)."
    - "Full Allowed stream (or Allowed after hold-release with remaining Allowed) emits STREAM_DONE and exits 0 when submitted ≥ 1 (CLI-02)."
    - "Stream exit taxonomy pure mapper: 0 success, 2 denied/aborted, 3 blocked/hold-incomplete, 1 infra/empty/usage (CLI-02)."
    - "Non-coding intents keep stop-on-Block (no multi-node hold wait); Block maps to exit 3 so blocked is distinct from deny=2 and infra=1 (CLI-02)."
    - "Silent continue-past-Block is impossible: Block never returns success without an explicit PROCEED token (CLI-02)."
    - statement: "No reconnect-remint product path and no dual-Session stitch in the hold design — stay-connected parent-pipe only (CONFIRM-01)."
      verification: backstop
    - statement: "ProvideIntent remains exactly once before the stream loop; hold path does not re-open ProvideIntent (CONFIRM-01)."
      verification: backstop
  artifacts:
    - path: cli/caprun/src/stream_hold.rs
      provides: "Parent↔worker line protocol parse/format + StreamExitCode mapper + HoldResume tokens"
      contains: "caprun-stream:"
    - path: cli/caprun/src/worker.rs
      provides: "Block-and-Hold branch for SafeCodingWorkflow; DENIED/STREAM_DONE lines; exit 2/3 mapping"
      contains: "PROCEED"
    - path: cli/caprun/tests/stream_hold.rs
      provides: "Protocol contract + exit-map unit tests (Wave 0)"
    - path: cli/caprun/tests/stream_substrate.rs
      provides: "HoldContinue / HoldAbort drive_stream proofs — no re-submit after Block"
  key_links:
    - "BlockedPendingConfirmation → emit BLOCKED → stdin PROCEED → step_index++ → plan_next (no re-SubmitPlanNode of blocked node)"
    - "BlockedPendingConfirmation → emit BLOCKED → stdin ABORT → exit 2; no further submits"
    - "ExecutorDecision::Denied { reason } → DENIED code=reason.code() → exit 2"
    - "stream_hold::map_stream_exit pure function used by worker (and later main in 50-02)"
  assumptions:
    - "Parent stdin/stdout line protocol is sufficient without a new broker Wait IPC verb (RESEARCH A2)."
    - "Hold only for CaprunIntent::SafeCodingWorkflow; email/file keep stop-on-Block mapped to exit 3 (RESEARCH A5)."
    - "Exit integers 0/2/3/1 are the locked CLI-02 taxonomy for this phase (RESEARCH A4)."
  prohibitions:
    - "No reconnect-remint caprun continue verb that re-opens ProvideIntent"
    - "No dual-Session stitch after push Block"
    - "No session-wide confirm waiver"
    - "No silent advance past Block without PROCEED"
    - "No auto-confirm or auto-grant inside worker"
    - "No free-form effect path token under crates/ or cli/caprun (Gate 1)"
    - "No LIVE-07/08 SUCCESS claim; no new crates"
---

<objective>
Prove **Block-and-Hold same-Session continuity** end-to-end on the worker stream branch table: stay-connected hold, parent-pipe PROCEED/ABORT protocol, no re-submit of the blocked node, honest DENIED/STREAM_DONE lines, and pure exit-code taxonomy — with Wave 0 automated proofs — before main productizes the coding argv driver (Plan 02).

Purpose: Always-confirm `git.push` rewrites Allowed → BlockedPendingConfirmation on the coding success path; without hold, multi-node coding cannot reach `github.pr` on one Session. This plan is the load-bearing CONFIRM-01 + CLI-02 substrate for Plan 02 orchestration.

Output: `stream_hold` protocol module; worker hold branch; stream_hold + stream_substrate tests green on host.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-PATTERNS.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md
@.planning/phases/49-deterministic-multi-step-coding-planner/49-01-SUMMARY.md
@.planning/phases/48-plan-stream-substrate/48-02-SUMMARY.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@CLAUDE.md
@cli/caprun/src/worker.rs
@cli/caprun/tests/stream_substrate.rs
</context>

<precondition>
Phase 49 complete: SafeCodingWorkflow + ProvideIntent multi-mint + bag seed + plan_next five-node recipe green; Phase 48 sequential worker loop + stream_substrate green; DESIGN-multi-step-plan-stream.md CLEARED (Block-and-Hold Option A).
</precondition>

<artifacts_this_phase_produces>
Phase 50 overall produces:

1. **Parent↔worker hold protocol** (`stream_hold`) — machine lines BLOCKED / DENIED / NODE_ALLOWED / STREAM_DONE + PROCEED / ABORT tokens + exit taxonomy mapper.
2. **Worker Block-and-Hold** — stay-connected for SafeCodingWorkflow; no re-submit; Deny exit 2.
3. **Main coding driver** — `safe-coding-workflow` JSON argv, POLICY-03 bind once, piped orchestration, mid-loop in-process confirm, grant pointer, exit codes (Plan 02).
4. **Wave 0 tests** — protocol, exit map, HoldContinue/HoldAbort, coding argv, email/file + confirm/grant regression.
5. **COVERAGE.md** — no external API; no LIVE-07 claim (Plan 02).

This plan (50-01) owns items 1–2 and Wave 0 hold/protocol/exit-map proofs.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Block-and-Hold — protocol + worker PROCEED path</name>
  <reversibility rating="costly">Product hold protocol and worker branch table become the Phase 50/51 multi-node continuity contract; wrong re-submit or remint semantics force coordinated main+worker+test rework (not one-way crypto, but costly to reverse once partners depend on it).</reversibility>
  <files>cli/caprun/src/stream_hold.rs, cli/caprun/src/worker.rs, cli/caprun/tests/stream_hold.rs, cli/caprun/tests/stream_substrate.rs</files>
  <read_first>
    - planning-docs/DESIGN-multi-step-plan-stream.md §3 Block-and-Hold (stay-connected, no re-submit, dual-Session/reconnect-remint rejected), §6 deny/abort branch table
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md — Patterns 2–3 (hold protocol table, exit taxonomy 0/2/3/1), Code Examples (worker hold arm), Pitfalls 1–3,9
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-PATTERNS.md — worker.rs Block arm, stream_substrate apply_stream_decision / drive_stream, protocol line table
    - cli/caprun/src/worker.rs — sequential plan_next loop, BlockedPendingConfirmation exit-1 arm (~427–434), Denied exit-1 (~436–441), empty-stream fail-closed, ProvideIntent once, SafeCodingWorkflow bag seed
    - cli/caprun/tests/stream_substrate.rs — StreamTerminal / StreamBranch / apply_stream_decision / drive_stream / block_stops_without_resubmit
    - crates/runtime-core/src/executor_decision.rs — DenyReason::code() including policy_deny; BlockedPendingConfirmation anchors shape
    - crates/runtime-core/src/intent.rs — CaprunIntent::SafeCodingWorkflow variant (hold gated on this kind)
  </read_first>
  <behavior>
    - stream_hold formats/parses lines prefixed with caprun-stream: for BLOCKED effect_id=&lt;uuid&gt; sink=&lt;id&gt;, DENIED code=&lt;code&gt; sink=&lt;id&gt;, NODE_ALLOWED step=&lt;n&gt; sink=&lt;id&gt; (optional), STREAM_DONE submitted=&lt;n&gt;
    - Hold resume tokens: exact trim match PROCEED and ABORT only; unknown token is fail-closed error (not success)
    - StreamExitCode: Success=0, DeniedAborted=2, BlockedIncomplete=3, Infra=1; pure map function from terminal kind
    - Worker SafeCodingWorkflow + BlockedPendingConfirmation: print BLOCKED line to stdout; read one stdin line; PROCEED → step_index += 1; continue without re-issuing the blocked SubmitPlanNode; ABORT → process exit 2
    - Worker SafeCodingWorkflow success end: print STREAM_DONE submitted=N; return Ok (exit 0 via main of binary)
    - Worker Denied/NotImplemented: print DENIED code=… (use DenyReason::code() when Denied; NotImplemented uses a stable non-empty code string); process exit 2
    - drive_stream / apply_stream_decision gain HoldContinue semantics: after Block, optional hold action Proceed advances step without counting a second submit of the same blocked node; subsequent plan_next submits are new steps only
    - Existing block_stops_without_resubmit remains green for non-hold StopBlocked path (email/file substrate)
    - Unit tests: parse round-trip BLOCKED/DENIED/STREAM_DONE; map_stream_exit table; HoldContinue coding five-step mock (Allowed×3, Block, Proceed, Allowed) asserts submit count and sinks never re-submit git.push after PROCEED
  </behavior>
  <action>
    Implement the CONFIRM-01 / CLI-02 tracer: pure protocol + worker stay-connected hold + host unit proofs (DESIGN §3, RESEARCH Pattern 2–3).

    **1. New cli/caprun/src/stream_hold.rs (shared by both bins via `mod stream_hold;`):**
    - Types: StreamLine enum (Blocked { effect_id, sink }, Denied { code, sink }, NodeAllowed { step, sink }, StreamDone { submitted }), HoldResume (Proceed, Abort), StreamExitCode (Success=0, DeniedAborted=2, BlockedIncomplete=3, Infra=1) with From/as_i32 as appropriate.
    - format_line / parse_line for the exact `caprun-stream: …` table in RESEARCH (whitespace-tolerant parse; fail-closed on garbage).
    - parse_hold_resume(line) → Proceed | Abort | error on unknown.
    - map_stream_exit(terminal) pure function covering success / denied / blocked-incomplete / infra-or-empty.
    - Module docs: product path is stay-connected parent-pipe only; reconnect-remint and dual-Session stitch rejected (DESIGN §3.3). No broker Wait verb.
    - Keep module free of I/O side effects except pure string transforms (worker/main own println/read_line).

    **2. Worker Block-and-Hold (cli/caprun/src/worker.rs) — REPLACE exit-1 Block arm for coding:**
    - `mod stream_hold;` at binary root (same pattern as `mod planner;`).
    - On ExecutorDecision::BlockedPendingConfirmation { anchors }:
      - Extract effect_id from anchors[0].anchor.effect_id (fail-closed if missing anchors).
      - Extract sink id string from anchors[0].anchor.sink.
      - If intent is CaprunIntent::SafeCodingWorkflow { .. }:
        - println formatted BLOCKED line (stdout — parent protocol).
        - eprintln human hold notice (stderr OK).
        - read_line from stdin; match parse_hold_resume:
          - Proceed: step_index += 1; continue (MUST NOT call SubmitPlanNode again for the blocked node; MUST NOT ProvideIntent).
          - Abort: eprintln; std::process::exit(2).
      - Else (email/file single-node): print BLOCKED line for observability optional; std::process::exit(3) — blocked-incomplete, not deny.
    - On Denied { reason } | NotImplemented: print DENIED line with code= (reason.code() or stable NotImplemented code); std::process::exit(2).
    - On loop natural end with submitted ≥ 1: print STREAM_DONE; Ok(()) (exit 0).
    - Optional NODE_ALLOWED lines after Allowed are fine for progress; not required for tracer if STREAM_DONE + BLOCKED/DENIED exist.
    - Update the large worker module doc (steps e/f and exit 0 claim) to describe hold for coding and exit taxonomy; remove "substrate Phase 50 later" language.
    - Do not change planner recipe, ProvideIntent once, bag F-01, or RequestFd coding skip.

    **3. Wave 0 tests (write RED first where practical):**
    - New cli/caprun/tests/stream_hold.rs with `#[path = "../src/stream_hold.rs"] mod stream_hold;` — protocol parse/format round-trips, unknown line fail-closed, hold resume tokens, map_stream_exit matrix (0/2/3/1).
    - Extend stream_substrate.rs:
      - Add StreamBranch::HoldContinue (or equivalent helper) modeling Proceed after Block without re-submit.
      - Add drive_stream_with_hold (or extend drive_stream) that on Block applies a caller-supplied hold outcome: Proceed advances step_index only; Abort returns AbortDenied without further submits.
      - Test hold_continue_no_resubmit_blocked_sink: mock decisions Allowed, Allowed, Allowed, Block(push), then after Proceed Allowed(pr) — assert submitted count, node sink order file.write→process.exec→git.commit→git.push→github.pr, and git.push appears exactly once in submitted nodes.
      - Keep block_stops_without_resubmit green for StopBlocked non-hold path.

    **HYG:** Zero new crates. No new mint sites. If implementation would pivot to reconnect-remint or dual-Session, stop — DESIGN re-run trigger (GATE-RECORD §13).
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test stream_hold --test stream_substrate -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - stream_hold.rs exists with parse/format for BLOCKED, DENIED, STREAM_DONE and map_stream_exit 0/2/3/1
    - worker SafeCodingWorkflow Block path reads PROCEED/ABORT and does not re-submit the blocked node on PROCEED
    - worker Deny path exits 2 with DENIED code= line; non-coding Block exits 3
    - stream_hold tests cover protocol + exit map; stream_substrate HoldContinue proves no second submit of blocked sink
    - check-invariants.sh exits 0; no new crate directory under crates/
    - main.rs product coding verb still absent (Plan 02 owns argv driver)
  </acceptance_criteria>
  <done>One stay-connected hold path works: Block → PROCEED advances without remint/re-submit; protocol and exit mapper are unit-proven; Deny is exit 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HoldAbort + deny/policy_deny labels + substrate regression lock</name>
  <files>cli/caprun/src/worker.rs, cli/caprun/src/stream_hold.rs, cli/caprun/tests/stream_substrate.rs, cli/caprun/tests/stream_hold.rs</files>
  <read_first>
    - cli/caprun/src/worker.rs — hold arm from Task 1; Denied match; empty stream fail-closed
    - cli/caprun/src/stream_hold.rs — types and mappers from Task 1
    - cli/caprun/tests/stream_substrate.rs — deny_aborts_remaining style tests; HoldContinue from Task 1
    - crates/runtime-core/src/executor_decision.rs — DenyReason::PolicyDeny code() == policy_deny
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md — CLI-02 notes on policy_deny stdout label vs shared exit 2
  </read_first>
  <behavior>
    - HoldAbort: after Block, Abort token → no further plan_next submits; terminal denied/aborted (exit 2)
    - policy_deny Denied decision produces DENIED code=policy_deny (stdout machine line); exit still 2 (shared denied/aborted bucket)
    - Empty stream still fail-closed (infra / non-success — not exit 0); map_stream_exit treats empty as Infra=1
    - Existing Phase 48 stream_substrate deny/block/empty tests remain green
    - stream_hold docs state: parent must not treat BLOCKED as success; PROCEED only after human confirm release (main wires this in Plan 02)
  </behavior>
  <action>
    Expand the tracer hold slice to full CLI-02 branch honesty for abort and policy labels (CONFIRM-01 ABORT path).

    **1. stream_substrate HoldAbort test:**
    - Mock Allowed then Block then Abort — assert submitted stops at the blocked node; no later sinks submitted; terminal is abort/denied class.

    **2. policy_deny label test (stream_hold or stream_substrate):**
    - Format/parse DENIED code=policy_deny; assert code string equals DenyReason::PolicyDeny { .. }.code().
    - Document that exit integer shares 2 with other denies; distinction is the code= field (RESEARCH open question resolution).

    **3. Worker polish:**
    - Ensure NotImplemented and Denied both exit 2 (not 1).
    - Ensure empty submitted==0 path still bails (anyhow error → process non-zero / infra) and does not print STREAM_DONE.
    - Ensure unknown hold resume token fails closed (bail or exit 1 infra) — never Proceed.

    **4. Regression lock:**
    - Run stream_substrate full host suite; fix any breakage from StreamBranch enum expansion.
    - Do not implement main orchestration, coding argv, or grant UX (Plan 02).
    - Do not claim LIVE-07.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test stream_hold --test stream_substrate --test planner -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - HoldAbort test proves zero submits after Abort
    - DENIED lines can carry code=policy_deny
    - worker Denied/NotImplemented exit code is 2; empty stream is not STREAM_DONE success
    - planner + stream_substrate + stream_hold tests pass; invariants green
  </acceptance_criteria>
  <done>Abort and policy_deny stop semantics are machine-checkable; substrate regressions green; hold protocol ready for main orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker stdout → Main | Machine protocol lines are untrusted input to main's parser (forged lines must not auto-authorize effects) |
| Main stdin → Worker | PROCEED/ABORT tokens; only parent should write them; worker must not self-PROCEED |
| Worker → Broker UDS | Existing SubmitPlanNode / ProvideIntent once; hold must not add remint |
| Confirm process → Audit DB | Confirm still acts on durable pending only (Plan 02 wires main call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Elevation of Privilege | worker hold / ProvideIntent | high | mitigate | Stay-connected only; ProvideIntent once; no reconnect-remint path; occupancy latch unchanged |
| T-50-02 | Tampering | dual-Session stitch | high | mitigate | Product hold never creates a second Session; tests assert single-stream continue after PROCEED |
| T-50-03 | Tampering | silent continue-past-Block | high | mitigate | Branch table: Block waits for token; unit HoldContinue requires explicit Proceed; no success on Block alone |
| T-50-04 | Elevation of Privilege | session-wide confirm waiver | high | mitigate | Hold is per effect_id signal only; subsequent nodes full I2; no stream-wide waiver flag |
| T-50-05 | Spoofing | forged PROCEED without confirm | medium | mitigate | Worker trusts parent pipe (same as today trusts parent env); Plan 02 main only writes PROCEED after ConfirmOutcome::Released — not after mere BLOCKED parse |
| T-50-06 | Information Disclosure | BLOCKED line content | low | accept | effect_id + sink are operator-facing; literals stay in durable pending / confirm display |
| T-50-SC | Tampering | package installs | high | mitigate | Zero new packages this phase — N/A legitimacy; keep T-50-SC disposition mitigate with no installs |
| T-50-07 | Elevation of Privilege | free-form effect path | high | mitigate | Gate 1 / check-invariants; plan nodes only |
| T-50-08 | Spoofing | hybrid sold as CLI multi-step | medium | mitigate | This plan is worker/protocol only; Plan 02 requires real binary argv path; LIVE claim deferred Phase 51 |
</threat_model>

<verification>
Host-safe: `./scripts/check-invariants.sh && cargo test -p caprun --test stream_hold --test stream_substrate --test planner -- --test-threads=1`.  
No LIVE-07 compose-verify SUCCESS required. Linux mailpit not required for this plan (no SMTP on coding hold unit path).
</verification>

<success_criteria>
- CONFIRM-01 hold physics proven: PROCEED advances without re-submit; ABORT stops; no remint.
- CLI-02 exit taxonomy and DENIED/BLOCKED/STREAM_DONE machine lines exist and are unit-tested.
- Worker coding path ready for Plan 02 parent orchestration.
</success_criteria>

<output>
Create `.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-01-SUMMARY.md` when done.
</output>
