---
phase: 50-cli-multi-node-driver-mid-loop-confirm-continuity
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - cli/caprun/src/main.rs
  - cli/caprun/tests/coding_cli.rs
  - cli/caprun/tests/e2e.rs
  - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/COVERAGE.md
  - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CONFIRM-01

must_haves:
  truths:
    - "caprun run accepts safe-coding-workflow with a JSON intent file that deserializes to CaprunIntent::SafeCodingWorkflow, plus workspace-file and optional audit DB, and reuses --policy / CAPRUN_POLICY / broker_default via one bind_policy call at session create (CLI-01; POLICY-03)."
    - "Coding driver prints session_id and a caprun grant pointer before/at first hold; does not auto-grant (CLI-01; GITHUB-02)."
    - "Existing review / confirm / deny / grant / audit first-branch verbs remain dispatchable and are pointed at from the mid-loop Block surface (CLI-01)."
    - "For SafeCodingWorkflow, main spawns worker with piped stdin/stdout, keeps the broker task alive for the entire hold window, and only aborts the broker after STREAM_DONE / DENIED / worker death (CONFIRM-01; CLI-01)."
    - "On worker BLOCKED line: main surfaces effect_id + review/confirm/deny pointers; interactive (TTY and CAPRUN_CONFIRM not external) prompts the human then calls brokerd::confirmation confirm/deny in-process against the same audit DB; on ConfirmOutcome::Released writes PROCEED; on Denied writes ABORT (CONFIRM-01)."
    - "CAPRUN_CONFIRM=external (or non-TTY default per research): main prints pointers and polls list_pending_confirmations_for_session until the effect is no longer pending, then PROCEED or ABORT from durable terminal state — dual-terminal alternate, same Session (CONFIRM-01)."
    - "Main never re-submits the blocked PlanNode; never re-opens ProvideIntent; never rebinds policy mid-stream; never creates a second Session to finish the chain (CONFIRM-01)."
    - "caprun run stream exit codes: 0 full success (incl. Block-released + remaining Allowed), 2 denied/aborted (incl. policy_deny label on stdout and human ABORT), 3 blocked/hold-incomplete, 1 infra/usage/empty/crash (CLI-02)."
    - "Email/file single-node paths remain green; hold orchestration is coding-primary; email/file Block maps to honest non-success (prefer exit 3) without requiring multi-node hold (CLI-01 regression)."
    - "Silent continue-past-Block is forbidden on the product path: PROCEED only after Released confirm or durable confirmed state (CLI-02)."
    - statement: "Phase 50 does not claim LIVE-07/08 non-hybrid SUCCESS — product path is real binary orchestration ready for Phase 51 (CLI-01 honesty)."
      verification: backstop
  artifacts:
    - path: cli/caprun/src/main.rs
      provides: "safe-coding-workflow argv arm; piped worker orchestration; mid-loop confirm; exit map; grant pointer; broker lifetime"
      contains: "safe-coding-workflow"
    - path: cli/caprun/tests/coding_cli.rs
      provides: "CLI-01 argv → SafeCodingWorkflow JSON; unknown kind fail-closed; policy flag accepted; --seed-from-file coding rejected"
    - path: .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/COVERAGE.md
      provides: "No external API declaration + assumption-delta no-change for CaprunIntent identity"
    - path: .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md
      provides: "Per-task map filled; Wave 0 complete; nyquist_compliant true"
  key_links:
    - "JSON intent file → CaprunIntent::SafeCodingWorkflow → INTENT env → worker ProvideIntent once → plan_next stream"
    - "bind_policy once at session create → policy_bound event → immutable for hold window"
    - "worker BLOCKED → main confirm/deny on durable pending → PROCEED/ABORT → worker continues same Session"
    - "stream_hold::parse_line + map_stream_exit shared with Plan 01 worker protocol"
  assumptions:
    - "JSON intent file (not 13 positionals) is the locked argv shape (RESEARCH A1)."
    - "Interactive in-process confirm is primary UX; CAPRUN_CONFIRM=external is dual-terminal alternate (RESEARCH A3)."
    - "Full git.push + github.pr LIVE on real Linux is Phase 51; host tests prove argv + orchestration contracts without claiming LIVE SUCCESS."
  prohibitions:
    - "No auto-confirm mid-loop and no auto-grant inside caprun run"
    - "No dual-Session stitch product path"
    - "No reconnect-remint ProvideIntent"
    - "No session-wide confirm waiver"
    - "No policy rebind mid-stream"
    - "No free-form effect path token under crates/ or cli/caprun"
    - "No LIVE-07/08 SUCCESS claim in SUMMARY or COVERAGE"
    - "No new crates"
---

<objective>
Productize the **CLI multi-node coding driver** on top of Plan 01 hold: `safe-coding-workflow` JSON argv, POLICY-03 bind-once, piped worker + broker lifetime across mid-loop confirm, grant/review pointers, honest exit codes, and email/file + confirm/grant regression — without claiming LIVE-07/08.

Purpose: Design partners drive the full multi-node coding chain from the real `caprun` binary with same-Session Block-and-Hold at always-confirm `git.push`, preserving human confirm/deny/grant as first-class gates.

Output: main.rs coding orchestration; coding_cli tests; COVERAGE.md; 50-VALIDATION Wave 0 complete.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-PATTERNS.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md
@.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-01-PLAN.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@CLAUDE.md
@cli/caprun/src/main.rs
@cli/caprun/src/stream_hold.rs
@cli/caprun/src/worker.rs
@cli/caprun/tests/e2e.rs
@cli/caprun/tests/confirm.rs
@cli/caprun/tests/grant.rs
</context>

<precondition>
Phase 50 Plan 01 complete: stream_hold protocol + worker SafeCodingWorkflow hold (PROCEED/ABORT) + DENIED/STREAM_DONE + exit taxonomy unit tests green; 50-01-SUMMARY.md present.
</precondition>

<artifacts_this_phase_produces>
This plan (50-02) produces/completes:

1. **safe-coding-workflow CLI arm** — JSON → CaprunIntent::SafeCodingWorkflow; reject file-derived coding seed at CLI.
2. **Orchestrated multi-node run** — piped stdio; broker kept alive through hold; mid-loop confirm/deny; PROCEED/ABORT.
3. **Exit-code product map** for `caprun run` using stream_hold taxonomy.
4. **Grant + review/confirm/deny pointers** on coding path.
5. **coding_cli tests + email/file e2e / confirm / grant regression**.
6. **COVERAGE.md + 50-VALIDATION** Wave 0 / nyquist complete.

Phase 50 overall (with 50-01): design-partner CLI multi-node path + same-Session hold ready for Phase 51 LIVE.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coding argv + orchestrated hold lifecycle + mid-loop confirm</name>
  <reversibility rating="costly">Product CLI coding verb and mid-loop confirm orchestration become the design-partner interface for v1.10; wrong broker-lifetime or auto-confirm behavior is costly to reverse after partner scripts depend on it.</reversibility>
  <files>cli/caprun/src/main.rs</files>
  <read_first>
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md — Patterns 1,2,4,5; architecture diagram steps 1–8; exit taxonomy; grant pointer; Pitfalls 1,4,5,7,8
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-PATTERNS.md — main.rs intent match, POLICY-03 bind, worker spawn env_clear, wait-then-abort replace, run_confirm_or_deny, grant print shape
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-01-PLAN.md — stream_hold line contracts and exit integers worker already emits
    - cli/caprun/src/main.rs — early verb dispatch (confirm/deny/review/grant/audit), intent kind match (~309–318), bind_policy (~376–396), broker spawn, worker spawn (~550–603), child.wait + broker_task.abort (~595–619), post-Block surface (~634–667), run_confirm_or_deny
    - cli/caprun/src/stream_hold.rs — parse_line, HoldResume, map_stream_exit (from Plan 01)
    - cli/caprun/src/worker.rs — hold arm + STREAM_DONE/DENIED (Plan 01) so main matches real lines
    - crates/brokerd/src/confirmation.rs — confirm / deny / ConfirmOutcome (Released, Denied, sink-fail variants)
    - crates/brokerd/src/audit.rs — list_pending_confirmations_for_session; record_github_grant / has_github_grant (pointer only — do not auto-grant)
    - crates/runtime-core/src/intent.rs — SafeCodingWorkflow serde(tag=kind) field list
  </read_first>
  <behavior>
    - Argv: caprun [run] [--policy P] safe-coding-workflow &lt;coding-intent.json&gt; &lt;workspace-file&gt; [audit.db]
    - JSON deserializes to CaprunIntent; fail-closed unless SafeCodingWorkflow; email/file arms unchanged
    - --seed-from-file with safe-coding-workflow fails closed at CLI (broker also rejects primary_file_derived=true)
    - After session_id known for coding: print session_id=… and grant: caprun grant &lt;session_id&gt; &lt;audit-path&gt;
    - Coding path: Command stdin(Stdio::piped()) stdout(Stdio::piped()); stderr inherit OK; env_clear allowlist unchanged (INTENT, BROKER_SOCK, WORKSPACE_FILE, PRIMARY_SEED_FILE_DERIVED, optional planner)
    - Orchestration loop reads worker stdout lines; on BLOCKED runs mid-loop confirm path then writes PROCEED or ABORT to worker stdin; on DENIED maps exit 2; on STREAM_DONE breaks success path
    - Interactive mode (stdin TTY and CAPRUN_CONFIRM not external): print review/confirm/deny pointers; prompt human for confirm|deny; call confirmation::confirm or deny in-process (same key load + workspace root pattern as run_confirm_or_deny); Released → PROCEED; Denied → ABORT; sink-fail outcomes → stream failure (infra or documented non-zero, not silent success)
    - External mode (CAPRUN_CONFIRM=external or non-TTY): print pointers; poll pending for effect_id until terminal; confirmed → PROCEED; denied → ABORT; abandoned timeout → blocked exit 3
    - Broker task aborted only after worker terminal; never abort broker on first BLOCKED
    - Final process exit uses stream_hold taxonomy from worker status + parsed terminal + pending rows (0/2/3/1)
    - Email/file may keep simpler wait path but must map worker exit 3 (Block) and 2 (Deny) to the same process exit codes (no silent success)
  </behavior>
  <action>
    Implement CLI-01 product driver + CONFIRM-01 main-side hold orchestration + CLI-02 exit mapping (RESEARCH Patterns 1–5).

    **1. Intent arm (CLI-01):**
    - Add `"safe-coding-workflow"` match: read intent_param as filesystem path; serde_json::from_str into CaprunIntent; require SafeCodingWorkflow variant; update usage string / module docs for the new kind.
    - If seed_from_file / primary_file_derived would be true for coding, bail before session create with a clear error.
    - Keep send-email-summary and create-file-from-report byte-stable.
    - Do not add clap; hand-rolled argv only (HYG-02).

    **2. Policy + session (CLI-01 / POLICY-03):**
    - Reuse existing single bind_policy call and policy_bound event — no second binder, no mid-stream rebind.
    - After session_id for SafeCodingWorkflow: println session_id and grant pointer (Pattern 4). Never call record_github_grant from run.

    **3. Coding orchestration (CONFIRM-01):**
    - Branch lifecycle for SafeCodingWorkflow (email/file may retain simpler wait if cleaner, but shared helpers preferred):
      - Spawn worker with Stdio::piped for stdin and stdout.
      - Loop: read lines from child stdout (spawn_blocking or async equivalent that does not stall forever without timeout policy — document external-mode poll interval).
      - parse_line via stream_hold (`mod stream_hold;` in main.rs).
      - BLOCKED: print the same review/confirm/deny pointer text currently used post-exit (~651–660); then interactive or external confirm path; write PROCEED\n or ABORT\n to child stdin and flush.
      - DENIED: record code= (including policy_deny) for operator stdout; break toward exit 2.
      - STREAM_DONE: break toward success path.
      - Unparseable lines: forward to human stdout or ignore non-protocol lines carefully — protocol lines are authoritative for control flow.
    - After loop: wait child; tear down planner sidecar if any; abort broker; print audit DAG + verify_chain (existing).
    - Map final exit: prefer std::process::exit with stream_hold codes rather than anyhow::bail that collapses to 1 when the stream already reported a structured terminal. Usage/spawn failures remain exit 1.

    **4. Mid-loop confirm implementation details:**
    - Factor shared open-audit + load_or_create_key + WorkspaceRoot::open(pc.workspace_root_path) + confirm/deny calls from run_confirm_or_deny so in-process hold does not duplicate key/F1 mistakes.
    - PROCEED only on ConfirmOutcome::Released (or external poll observing confirmed terminal). Never PROCEED on Reviewed-only or UnknownEffect.
    - ABORT on ConfirmOutcome::Denied or human deny choice.
    - Sink-fail outcomes (ConfirmedButSinkFailed, EmailSendFailed, …): do not PROCEED as success; treat as stream failure (exit 1 or document as 2 — pick one and test; prefer non-success distinct from full success).
    - Preserve early-branch caprun confirm/deny/review/grant/audit verbs unchanged.

    **5. Email/file honesty (CLI-02):**
    - When worker exits 3 (Block stop-on-Block), main should surface pending pointers (existing list_pending path) and exit 3, not only bail as generic 1.
    - When worker exits 2 (Deny), main exits 2.
    - Minimize e2e churn: hold wait loop is coding-primary.

    **HYG / honesty:** Zero new crates. No LIVE-07 SUCCESS language in code comments claiming Phase 50 done as live multi-node proof. Real binary path only — no hybrid in-crate multi-submit sold as product CLI.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo check -p caprun 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - main.rs matches safe-coding-workflow and builds CaprunIntent::SafeCodingWorkflow from JSON file
    - coding run path uses piped stdio and does not abort broker on first BLOCKED
    - mid-loop path can call confirmation confirm/deny and write PROCEED/ABORT
    - grant pointer printed for coding sessions; auto-grant absent
    - stream exit mapping uses 0/2/3/1 taxonomy
    - check-invariants.sh exits 0; cargo check -p caprun succeeds
  </acceptance_criteria>
  <done>Design partner can invoke coding multi-node from caprun with same-Session hold orchestration wired end-to-end in main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coding_cli tests, regressions, COVERAGE, validation Wave 0</name>
  <files>cli/caprun/tests/coding_cli.rs, cli/caprun/tests/e2e.rs, .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/COVERAGE.md, .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md</files>
  <read_first>
    - cli/caprun/src/main.rs — coding arm and exit mapping from Task 1
    - cli/caprun/tests/e2e.rs — binary spawn layout, F1-safe temp workspace, CARGO_BIN_EXE_caprun
    - cli/caprun/tests/grant.rs — run_caprun_grant argv pattern
    - cli/caprun/tests/confirm.rs — seed pending + cross-process confirm (dual-terminal analog)
    - cli/caprun/tests/stream_hold.rs — protocol tests from Plan 01 (must stay green)
    - .planning/phases/49-deterministic-multi-step-coding-planner/COVERAGE.md — format analog for no-external-API declaration
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-VALIDATION.md — Wave 0 checklist and per-task map
    - .planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-RESEARCH.md — coding intent JSON fixture shape
  </read_first>
  <behavior>
    - coding_cli host tests: valid SafeCodingWorkflow JSON fixture deserializes / CLI accepts kind (binary smoke may stop at usage or early fail without full Linux confinement — document); unknown intent kind still fail-closed; --policy path accepted without panic; --seed-from-file + safe-coding-workflow fails closed
    - Pure unit coverage for any extracted parse helpers if Task 1 factored them
    - e2e email path still passes (update only if Block exit code changed to 3 — assert honest non-success)
    - confirm + grant integration tests still pass
    - stream_hold + stream_substrate + planner still pass
    - COVERAGE.md: No external API integration; CaprunIntent identity assumption-delta = no-change (add-alongside already done Phase 49); Phase 50 is CLI orchestration only; explicitly not LIVE-07/08
    - 50-VALIDATION.md: fill plan/task IDs; check Wave 0 boxes; set nyquist_compliant true when automated commands exist
  </behavior>
  <action>
    Close CLI-01/02 verification surface and documentation gates for Phase 50.

    **1. cli/caprun/tests/coding_cli.rs (Wave 0):**
    - Fixture JSON matching RESEARCH shape (kind SafeCodingWorkflow + 13 fields).
    - Tests:
      - serde / construction: JSON → CaprunIntent::SafeCodingWorkflow field presence.
      - Binary: `CARGO_BIN_EXE_caprun` with unknown kind → non-zero.
      - Binary: safe-coding-workflow with missing file → non-zero usage/infra.
      - Binary: safe-coding-workflow with valid JSON + temp workspace layout — host may not complete full five-node confined stream; assert at least argv acceptance progresses past unknown-kind (session_id printed and/or structured non-success that is not "unknown intent kind"). Prefer host-safe assertions; gate full multi-node Linux body with cfg(target_os = "linux") only if environment-realistic, and never assert LIVE-07 SUCCESS.
      - Reject coding + --seed-from-file (non-zero + error text about file-derived / primary_file_derived).
      - --policy with a temp policy file path is accepted by argv parse (bind may fail-closed on invalid policy content — use broker_default or a minimal valid policy fixture if one exists in tree; otherwise assert flag is not "unknown argument").

    **2. Regression:**
    - cargo test -p caprun --test e2e --test confirm --test grant --test stream_hold --test stream_substrate --test planner -- --test-threads=1
    - Adjust e2e only if Block exit taxonomy changed expectations; keep email success path exit 0.

    **3. COVERAGE.md** in phase directory:
    - Declare no external API/SDK; zero new packages; internal UDS unchanged.
    - Assumption-delta: CaprunIntent closed-enum identity **no-change** this phase (CLI consumes Phase 49 SafeCodingWorkflow).
    - Framing honesty: Phase 50 = product CLI + hold continuity; LIVE-07/08 = Phase 51.

    **4. 50-VALIDATION.md:**
    - Map Task IDs to 50-01 / 50-02; mark Wave 0 items done when tests exist; set frontmatter nyquist_compliant: true and wave_0_complete: true when criteria met.

    **5. SUMMARY discipline:**
    - When writing 50-02-SUMMARY, list artifacts and explicitly state LIVE-07/08 not claimed.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test coding_cli --test stream_hold --test stream_substrate --test planner --test e2e --test confirm --test grant -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - coding_cli.rs exists with SafeCodingWorkflow JSON + fail-closed unknown kind + seed-from-file reject tests
    - e2e, confirm, grant, stream_hold, stream_substrate, planner tests pass on host (Linux-gated bodies may show 0 passed — expected)
    - COVERAGE.md declares no external API and no LIVE-07 claim
    - 50-VALIDATION.md Wave 0 complete and nyquist_compliant true
    - check-invariants.sh exits 0
  </acceptance_criteria>
  <done>CLI-01/02/CONFIRM-01 product path is test-backed on host; docs honest about Phase 51 LIVE; regressions green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator argv / intent JSON | Untrusted structure until fail-closed serde + closed enum; must not inject free-form sinks |
| Policy file path | Trusted operator path outside workspace (POLICY-03 / F1); never re-read from worker workspace mid-stream |
| Main ↔ Worker pipes | Protocol control plane; PROCEED authorizes stream continue only after durable confirm |
| Main → Audit DB / confirm() | Human gate; confirm executes sink from snapshot — highest privilege product action |
| Grant verb | Distinct capability for github.pr; must not fold into confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Elevation of Privilege | reconnect remint | high | mitigate | Product path stay-connected; no continue-remint verb; ProvideIntent once |
| T-50-02 | Tampering | dual-Session stitch | high | mitigate | One session_id for entire coding run; no second create_session after push Block |
| T-50-03 | Tampering | silent continue-past-Block | high | mitigate | PROCEED only after ConfirmOutcome::Released or durable confirmed; tests + branch table |
| T-50-04 | Elevation of Privilege | session-wide confirm waiver | high | mitigate | Single-shot per effect_id via existing confirmation.rs; no stream waiver flag |
| T-50-05 | Elevation of Privilege | policy rebind mid-stream | high | mitigate | Single bind_policy at session create; no second bind in hold loop |
| T-50-06 | Elevation of Privilege | auto-confirm / auto-grant | high | mitigate | Interactive prompt or external dual-terminal only; print grant pointer; never record_github_grant inside run |
| T-50-07 | Spoofing | hybrid sold as CLI multi-step | medium | mitigate | Real caprun binary argv path; COVERAGE/SUMMARY forbid LIVE-07 SUCCESS claim |
| T-50-08 | Elevation of Privilege | free-form effect path | high | mitigate | Closed CaprunIntent only; Gate 1; plan nodes only |
| T-50-09 | Elevation of Privilege | post-confirm output laundering | high | mitigate | F-02 out-of-bag; success path uses intent-minted bag keys only (Phase 49); hold does not re-seed bag from confirm outputs |
| T-50-10 | Tampering | forged BLOCKED → auto PROCEED | medium | mitigate | Main never PROCEED on parse alone; requires confirm release |
| T-50-SC | Tampering | package installs | high | mitigate | Zero new packages; no legitimacy checkpoint needed |
</threat_model>

<verification>
Host: `./scripts/check-invariants.sh && cargo test -p caprun --test coding_cli --test stream_hold --test stream_substrate --test planner --test e2e --test confirm --test grant -- --test-threads=1`.  
Optional Linux: `bash scripts/mailpit-verify.sh` only if SMTP-touching tests run; coding path does not require SMTP.  
Do not require `compose-verify` LIVE-07 SUCCESS for Phase 50 gate.
</verification>

<success_criteria>
- CLI-01: real binary accepts coding multi-step intent + policy bind + drives multi-node stream orchestration.
- CLI-02: exit codes 0/2/3/1; Block surfaces effect_id + pointers; deny/policy_deny abort labeled; no silent continue-past-Block.
- CONFIRM-01: mid-loop hold same Session; confirm/deny on durable pending; PROCEED/ABORT; no remint/rebind/dual-Session/waiver.
- Email/file + confirm/grant regression green; COVERAGE honest; no LIVE-07 claim.
</success_criteria>

<output>
Create `.planning/phases/50-cli-multi-node-driver-mid-loop-confirm-continuity/50-02-SUMMARY.md` when done.
</output>
