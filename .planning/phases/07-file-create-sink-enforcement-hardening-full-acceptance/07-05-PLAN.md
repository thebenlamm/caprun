---
phase: 07-file-create-sink-enforcement-hardening-full-acceptance
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-04]
files_modified:
  - crates/brokerd/tests/durable_anchor.rs
  - cli/caprun/tests/s9_live_block.rs
  - crates/brokerd/tests/s9_acceptance.rs
autonomous: true
requirements: [ACC-03, ACC-04, ACC-05, ACC-07]

must_haves:
  truths:
    - "A live caprun run with hostile input → RelativePath claim → mint_from_read → file.create is BLOCKED: no file on disk, CLI exits non-success, a durable sink_blocked event with a non-None anchor is in the audit DB."
    - "A live caprun run with a broker-minted trusted intent path (mint_from_intent) creates exactly the expected file under the workspace root and records sink_executed."
    - "The audit DB shows one unbroken causal chain per run: fd_granted → file_read → plan_node_evaluated → (sink_blocked | sink_executed), verified via verify_chain + a parent-depth walk."
    - "AFTER-EXIT DB-ALONE sentinel: file-backed DB, drop+reopen; verify_chain passes FIRST, THEN the anchor is trusted; the DAG has a file_read Event with id == anchor.read_event_id and untrusted taint (via is_untrusted()); anchor.provenance_chain[0] == that id; Event.taint == anchor.taint == record.taint."
    - "Tamper-evidence: UPDATE the real payload column (change the literal), reopen → verify_chain returns false."
  artifacts:
    - "crates/brokerd/tests/durable_anchor.rs — after-exit DB-alone anti-stapling sentinel + tamper-evidence test (new)"
    - "cli/caprun/tests/s9_live_block.rs — live file.create hostile-block + clean-allow tests"
  key_links:
    - "The after-exit sentinel drives the hostile block through dispatch_request against a file-backed DB, drops+reopens the connection, then asserts verify_chain THEN the genuine-taint backstops on the persisted anchor — an event-order-only assertion is insufficient (anti-stapling)."
  prohibitions:
    - "Never assert sink_blocked.parent_id == read_event_id (the two graphs are never equated; causal parent is the chain head, value-lineage lives in the anchor)."
    - "No effect executed on the block path — no sink_executed / no email_send_stub / no file on disk when blocked."
    - "Do not weaken the anti-stapling assertion — verify_chain must pass BEFORE the anchor is trusted, and the file_read backstop must hold in the reopened DB."
---

<objective>
Prove the full live §9 acceptance contract and make ACC-07 actually true. Phase 6's email hostile block became unreachable (the intent CLI always routes a `UserTrusted` recipient into `email.send/to`), so the live hostile-block proof must be re-established — this plan restores a continuously-proven live §9 guarantee via `file.create`. It delivers: the live caprun hostile-block (ACC-03) and clean-allow (ACC-04) runs, the one-unbroken-causal-chain assertion (ACC-05), and the canonical ACC-07 proof — a dispatch-level, **after-exit, DB-alone** anti-stapling sentinel plus a tamper-evidence test — per `DESIGN-durable-anchor-and-label-partition.md` §7 and `PHASE-7-HANDOFF.md` §4. The in-process `s9_acceptance.rs` (reshaped by 07-02) is kept as the faster backstop.

This plan is TESTS ONLY — it consumes the durable anchor (07-02) and the live `file.create` sink (07-04); it adds no production code. The live caprun tests are `#[cfg(target_os="linux")]`; the after-exit/tamper anchor tests are cross-platform (the block path performs no file I/O).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-file-create-sink-enforcement-hardening-full-acceptance/07-CONTEXT.md
@.planning/phases/07-file-create-sink-enforcement-hardening-full-acceptance/07-RESEARCH.md
@.planning/phases/07-file-create-sink-enforcement-hardening-full-acceptance/07-VALIDATION.md
@planning-docs/DESIGN-durable-anchor-and-label-partition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: after-exit DB-alone anti-stapling sentinel (ACC-07)</name>
  <files>crates/brokerd/tests/durable_anchor.rs</files>
  <read_first>
    - crates/brokerd/src/audit.rs — `open_audit_db` (`:55`), `compute_event_hash` (`:69`), `append_event` (`:100`), `find_event_by_type` (`:174`, deserializes `payload` → Event with taint), `verify_chain` (`:200+`, recomputes from raw stored strings).
    - crates/brokerd/src/server.rs — `dispatch_request` (`:193-398`) signature (now carrying `Arc<WorkspaceRoot>` + minting `effect_id`); the `SubmitPlanNode` block branch that persists `sink_blocked` with the anchor.
    - crates/brokerd/tests/phase5_dispatch.rs — an existing dispatch-driving test to mirror for harness setup (file-backed connection, session bootstrap, `ReportClaims` → `SubmitPlanNode`).
    - DESIGN §7 "Durable anchor (Phase-7 ACC-07)" — the exact after-exit assertions + §0 two-graph backstops.
  </read_first>
  <action>
Create `crates/brokerd/tests/durable_anchor.rs`. In one test, drive the hostile block end-to-end through `dispatch_request` against a **file-backed** SQLite DB (a temp path, NOT `:memory:`): create session → `ReportClaims` with a `WorkerClaim::RelativePath("../escape")` (or any tainted path) → `SubmitPlanNode` with a `file.create` plan node routing that value's `ValueId` into `path`. The decision must be `BlockedPendingConfirmation`; a `sink_blocked` event persists with `anchor = Some(..)`.
Then **DROP the connection and REOPEN** the DB from the same path (simulating process exit). Against the reopened connection, assert IN THIS ORDER:
1. `verify_chain(&reopened, session_id)` returns `true` FIRST (trust the chain before the anchor).
2. Read the `sink_blocked` event via `find_event_by_type`; its `anchor` is `Some`.
3. Genuine-taint backstops: the DAG has a `file_read` Event with `id == anchor.read_event_id`; that event's taint `is_untrusted()`; `anchor.provenance_chain[0] == that file_read id`; `anchor.read_event_id == anchor.provenance_chain[0]`.
4. Taint consistency: `sink_blocked Event.taint == anchor.taint`, and the anchor literal equals the hostile path string.
5. NO effect executed: no `sink_executed` (and no `email_send_stub`) event exists for the session.
An event-order-only assertion is explicitly INSUFFICIENT — the value-lineage backstops above are the anti-stapling proof.
  </action>
  <verify>
    <automated>cargo test -p brokerd durable_anchor --no-fail-fast</automated>
  </verify>
  <done>`durable_anchor.rs` drives the block through `dispatch_request` on a file-backed DB, drops+reopens, and asserts (in order) verify_chain=true, anchor=Some, the file_read genuine-taint backstops (`id == read_event_id == provenance_chain[0]`, untrusted taint), `Event.taint == anchor.taint`, the literal, and NO sink_executed. Passes on macOS and Linux.</done>
</task>

<task type="auto">
  <name>Task 2: tamper-evidence test (mutate payload → verify_chain false)</name>
  <files>crates/brokerd/tests/durable_anchor.rs</files>
  <read_first>
    - crates/brokerd/src/audit.rs — `verify_chain` recomputes each row's hash from the RAW stored `payload`/`taint` strings, so mutating `payload` breaks the chain. The `events` table columns (`:25-42`: id, parent_id, session_id, event_type, payload, taint, parent_hash, hash).
    - DESIGN §7 tamper-evidence bullet (codex #6: mutate the DB, not memory).
  </read_first>
  <action>
Add a second test in `durable_anchor.rs`: build the same file-backed hostile-block DB, confirm `verify_chain` is `true`, then run a raw `UPDATE events SET payload = <payload-with-a-changed-literal> WHERE event_type = 'sink_blocked'` (change the anchor's literal inside the serialized payload — e.g. string-replace the path). Reopen and assert `verify_chain` returns `false`. This proves the anchor is inside the hashed payload and is tamper-evident.
  </action>
  <verify>
    <automated>cargo test -p brokerd durable_anchor --no-fail-fast</automated>
  </verify>
  <done>After an `UPDATE` to the real `payload` column changing the anchor literal, `verify_chain` returns `false` on reopen — the persisted anchor is tamper-evident.</done>
</task>

<task type="auto">
  <name>Task 3: live file.create hostile block (ACC-03) + causal chain (ACC-05)</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs — `run_caprun_intent_on` (`:55-79`, spawns the real `caprun` binary via `CARGO_BIN_EXE_caprun` with `<intent-kind> <intent-param> <workspace-file> [audit-db]`) and the existing clean-allow test (`:89+`); all `#[cfg(target_os="linux")]`. Mirror the harness for a `file.create` intent.
    - The 07-04 CLI intent-kind + plan wiring that makes a hostile workspace-read path route to `file.create/path`.
    - crates/brokerd/src/audit.rs — `find_event_by_type`, `verify_chain` for the audit-DB assertions.
  </read_first>
  <action>
Add a `#[cfg(target_os="linux")] #[test] fn s9_live_file_create_hostile_block()`: seed a workspace file whose content yields a hostile `RelativePath` claim, run `caprun` with the file-create intent kind pointed at it (writing to a temp audit DB), then assert: the process exits NON-zero (block); the target file was NOT created on disk; a durable `sink_blocked` event exists with a non-None anchor; NO `sink_executed` event exists. Then assert the causal chain (ACC-05): `verify_chain` is true and the ordered events `fd_granted → file_read → plan_node_evaluated → sink_blocked` are present and parent-linked (walk `parent_id` depth). Run under the CLAUDE.md Colima/Docker recipe (`seccomp=unconfined`, no `--privileged`).
  </action>
  <verify>
    <automated>cargo build -p caprun --tests && echo "(Linux-gated: run via Docker recipe) cargo test -p caprun --test s9_live_block"</automated>
  </verify>
  <done>A Linux-gated live test drives a real `caprun` file.create hostile run: non-zero exit, no file created, durable `sink_blocked` with anchor, no `sink_executed`, and a verified `fd_granted → file_read → plan_node_evaluated → sink_blocked` causal chain. Compiles on macOS (0-passed expected); passes under the Docker recipe.</done>
</task>

<task type="auto">
  <name>Task 4: live file.create clean allow (ACC-04)</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs — the existing `s9_live_clean_allow_path` test (`:89+`) for the assertion shape (exit 0, durable events, no sink_blocked).
    - 07-04 Task 6 — the trusted-intent path that supplies a `mint_from_intent` (`[UserTrusted]`) value to `file.create/path`.
  </read_first>
  <action>
Add a `#[cfg(target_os="linux")] #[test] fn s9_live_file_create_clean_allow()`: run `caprun` with a trusted intent path (broker-minted `[UserTrusted]` via `mint_from_intent`) for `file.create`, writing to a temp workspace root + audit DB. Assert: the process exits 0; the expected file EXISTS under the workspace root with the expected contents; a durable `sink_executed` event exists (carrying the effect_id); NO `sink_blocked` event exists; `verify_chain` is true. This is the reachable clean-allow half of the restored live §9 guarantee.
  </action>
  <verify>
    <automated>cargo build -p caprun --tests && echo "(Linux-gated: run via Docker recipe) cargo test -p caprun --test s9_live_block"</automated>
  </verify>
  <done>A Linux-gated live test drives a real `caprun` file.create clean-allow run: exit 0, the expected file created under the workspace root with correct contents, a durable `sink_executed`, no `sink_blocked`, verify_chain true.</done>
</task>

<task type="auto">
  <name>Task 5: confirm in-process s9_acceptance backstop is green post-reshape</name>
  <files>crates/brokerd/tests/s9_acceptance.rs</files>
  <read_first>
    - crates/brokerd/tests/s9_acceptance.rs — the anti-stapling assertions reshaped by 07-02 (`BlockedPendingConfirmation { anchor }`, `anchor.provenance_chain[0]`). This task only verifies the backstop still holds for `file.create` semantics and does not regress.
    - DESIGN §7 "Authoritative §9": the dispatch-level after-exit test (Task 1) is canonical; `s9_acceptance.rs` is the faster in-process backstop.
  </read_first>
  <action>
Confirm `s9_acceptance.rs` compiles and passes after the 07-02 reshape, and (if it currently only exercises `email.send`) add or adjust a minimal in-process assertion that a tainted `file.create/path` arg yields `BlockedPendingConfirmation` with a genuine `anchor.provenance_chain[0]` — keeping the fast in-process backstop aligned with the new live sink. Do NOT weaken the existing genuine-taint assertion.
  </action>
  <verify>
    <automated>cargo test --workspace --no-fail-fast && ./scripts/check-invariants.sh</automated>
  </verify>
  <done>`s9_acceptance.rs` is green post-reshape and covers a `file.create/path` tainted-block in-process; `cargo test --workspace` green on macOS; `check-invariants.sh` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted audit DAG → after-exit verifier | The genuine-taint proof must be reconstructable from the DB ALONE after process exit — an in-memory-only assertion proves nothing durable. |
| stored payload → tamper attempt | An attacker with DB write access must not be able to alter the anchor literal without breaking the hash chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-51 | Repudiation | after-exit sentinel | high | mitigate | Drop+reopen; `verify_chain` FIRST, then genuine-taint backstops on the persisted anchor — proves the taint edge survives process exit, not just event order. |
| T-07-52 | Tampering | payload column | high | mitigate | Anchor rides in the hashed `payload`; a raw `UPDATE` to the literal makes `verify_chain` false — tamper-evident. |
| T-07-53 | EoP (stapled taint) | durable anchor | high | mitigate | The file_read backstop (`id == read_event_id == provenance_chain[0]`, untrusted taint) fails if taint were stapled at the sink; assertion not weakenable. |
| T-07-54 | Spoofing (block leaks effect) | block path | high | mitigate | Assert NO `sink_executed`/`email_send_stub` and no file on disk on the block path — the block executes no effect. |
</threat_model>

<verification>
- `cargo test -p brokerd durable_anchor --no-fail-fast` — the after-exit DB-alone sentinel + tamper-evidence tests pass (cross-platform; the block path has no file I/O).
- `cargo test --workspace --no-fail-fast` — green on macOS; `s9_acceptance.rs` backstop green post-reshape.
- Linux (Colima/Docker recipe, `seccomp=unconfined`, no `--privileged`): `cargo test -p caprun --test s9_live_block` — live hostile-block (non-zero exit, no file, durable `sink_blocked`+anchor, no `sink_executed`, verified causal chain) and live clean-allow (exit 0, file created, `sink_executed`, no `sink_blocked`) both pass.
- `./scripts/check-invariants.sh` green.
</verification>

<success_criteria>
The full live §9 acceptance is green and ACC-07 is TRUE: a real `caprun` `file.create` run blocks a genuine-tainted path (no file, non-zero exit, durable `sink_blocked` with anchor, no effect) and allows a trusted intent path (file created, `sink_executed`); each run shows one unbroken causal chain (ACC-05); and the canonical proof is a dispatch-level, after-exit, DB-alone sentinel — `verify_chain` first, then the file_read genuine-taint backstops on the persisted anchor — plus tamper-evidence (mutating `payload` breaks `verify_chain`). This re-establishes a continuously-proven live §9 guarantee and closes the durable genuine-taint edge the git-log "v0 DONE" line predated.
</success_criteria>

<output>
Create `.planning/phases/07-file-create-sink-enforcement-hardening-full-acceptance/07-05-SUMMARY.md` when done.
</output>
