---
phase: 41-v1-9-design-gate-fresh-adversarial-code-trace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-v1.9-egress-policy.md
autonomous: true
requirements: [DESIGN-17, DESIGN-18]

must_haves:
  truths:
    - "A DESIGN doc exists at planning-docs/DESIGN-v1.9-egress-policy.md pinning all three v1.9 TCB mechanisms as DECISIONS (not options): (a) git.push egress, (b) http.request WRITE egress, (c) the policy-vs-I2 boundary incl. POLICY-03 binding (DESIGN-17)."
    - "The git.push section pins candidate (b) from the research — a broker-performed git smart-HTTP transfer: broker terminates TLS + does resolve-and-pin, the push child stays FULLY net-denied (no seccomp relaxation), pin lives in the application layer that SEES the destination IP:port, NEVER seccomp."
    - "The doc addresses the research's three adversarial attack points explicitly: credential leak via broker-side HTTP echoes/logs; pin-bypass via receive-pack redirect or DNS-rebind between the info/refs GET and the POST (one frozen pinned IP across both, POST redirects refused); payload-vs-destination confirm TOCTOU (pack frozen/generated from the confirmed commit range at-or-after confirm)."
    - "The http.request WRITE section pins a DISTINCT write host-allowlist (separate from the shipped GET allowlist), taint-governed content-sensitive body under I2, routing-sensitive url, broker-env-only credential custody with response scrub, and differential acceptance."
    - "The policy-vs-I2 boundary section pins: policy is a PRE-I2 narrowing gate (never a post-I2 override), I2 stays HARDCODED in the Rust TCB executor, and POLICY-03 binding — broker binds the policy at session creation from a trusted source outside the confined worker's reach, reusing the F1 containment check from cli/caprun/src/key.rs (refuse if the policy path resolves at-or-beneath the workspace root), immutable for the session, hash recorded as an audit-DAG event."
    - "The doc carries forward v1.8 §2 / §2.5 credential-scrub / §2.7 payload-at-confirm / §9 confirm-release by reference (extends/references, does not restate wholesale)."
    - "The doc pins the crypto-provider decision (ring-only, reuse shipped webpki-roots — ZERO new crates), a fail-closed defaults table, any new TaintLabel/DenyReason/audit-event symbols, and a §-per-pitfall threat model."
    - "The doc formalizes the git.push safety-valve: if no fully-unprivileged destination-pinning mechanism proves sound under review, git.push defers (disclosed, sign-off-gated) and the other three tracks proceed — never a silent drop."
    - "The doc states the fresh non-self adversarial code-trace is ORCHESTRATOR-owned (NOT a gsd-executor) and re-runs if the git.push trust-posture or transport-dependency choice changes mid-implementation (DESIGN-18)."
    - "No TCB code exists: git status for crates/ and cli/ is empty at phase end (the check-invariants no-TCB-code discipline holds)."
  artifacts:
    - planning-docs/DESIGN-v1.9-egress-policy.md
  key_links:
    - "git.push pin locus = reqwest .resolve(host, pinned_ip) in the broker application layer (where the destination IP:port is visible); child net-denied identically to git.commit — the pin is NEVER in seccomp."
    - "POLICY-03 binding reuses the F1 containment check verbatim from cli/caprun/src/key.rs — refuse any policy path canonicalizing at-or-beneath the workspace root."
    - "I2 is HARDCODED in the Rust TCB executor and runs unconditionally on every policy-PERMITTED call; policy can only narrow which sinks/args are callable, never disable/override I2."
---

<objective>
Author ONE reviewed DESIGN doc — `planning-docs/DESIGN-v1.9-egress-policy.md` — that
pins the TCB mechanisms for all three v1.9 surfaces before any TCB code is written:
(a) `git.push` fully-unprivileged broker-mediated destination-pinned egress,
(b) `http.request` WRITE (POST/PUT) egress, and (c) the policy-vs-I2 boundary incl.
POLICY-03 provenance/binding (DESIGN-17). The doc pins DECISIONS, not options —
mirroring the v1.8 doc's shape — and carries forward v1.8 §2 / §2.5 / §2.7 / §9 by
reference.

This is a DESIGN-GATE phase (standing caprun precedent: v1.0 P2, v1.2 P8, v1.3 P12,
v1.4 P18, v1.5 P23, v1.6 P26, v1.7 P31, v1.8 P35). The ONLY deliverable is this markdown
doc under `planning-docs/`. ZERO changes to `crates/{executor,brokerd,sandbox,runtime-core}`,
`cli/`, or any Rust source.

DESIGN-18 (the fresh non-self adversarial code-trace clearing the doc) is an
ORCHESTRATOR-OWNED gate that runs AFTER this plan completes — gsd-executors have no
Agent tool, so the executor's job is ONLY to author the doc, and the doc must itself
state that the trace is orchestrator-owned and re-runs on a mid-build trust-posture /
transport-dependency pivot.

Purpose: Clear the design gate that HARD-BLOCKS Phases 42-46, pinning git.push's
network-from-a-confined-child path (the riskiest surface in the project) precisely enough
that a fresh reviewer can trace every claim against real code.
Output: `planning-docs/DESIGN-v1.9-egress-policy.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Research input — the git.push mechanism decision (candidate (b))
@.planning/research/GIT-PUSH-EGRESS.md

# The v1.8 DESIGN doc — its §2 (git.push model) / §2.5 (credential scrub) /
# §2.7 (payload-at-confirm) / §9 (confirm-release) CARRY FORWARD into v1.9.
# The new doc extends/references these; it does NOT restate them wholesale.
@planning-docs/DESIGN-git-github-http-sinks.md
@planning-docs/DESIGN-GATE-RECORD-v1.8.md

# Canonical plan — I0/I1/I2 invariants, layer roles, the locked plan-node effect path
@planning-docs/PLAN.md
@CLAUDE.md

# Live code the doc must cite with accurate file:line (re-read this session; the doc's
# groundedness will be traced against real code by the orchestrator-owned reviewer).
@crates/brokerd/src/sinks/http_request.rs
@crates/sandbox/src/seccomp.rs
@crates/brokerd/src/sinks/process_exec.rs
@cli/caprun/src/key.rs
@scripts/check-invariants.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DESIGN doc scaffold + both egress mechanisms (git.push, http-write) + crypto/supply-chain + fail-closed defaults</name>
  <files>planning-docs/DESIGN-v1.9-egress-policy.md</files>
  <read_first>
    - .planning/research/GIT-PUSH-EGRESS.md — the AUTHORITATIVE git.push mechanism input; pin candidate (b) (broker-performed smart-HTTP transfer) and reproduce its three adversarial attack points (credential leak; redirect/DNS-rebind pin-bypass; payload-vs-destination TOCTOU) as named, closed pitfalls.
    - planning-docs/DESIGN-git-github-http-sinks.md — the v1.8 doc; §2 (git.push Pattern-B + net-denied child), §2.5 (captured-output credential scrub), §2.7 (payload-at-confirm provenance summary), §8 (fail-closed defaults table), §5 (crypto provider / env_clear TLS-cert allowlist). CARRY THESE FORWARD BY REFERENCE — cite section numbers, do not paste them wholesale.
    - crates/brokerd/src/sinks/http_request.rs — the shipped resolve-and-pin egress: validate_url → ssrf_check → vet_resolved → build_pinned_client with .resolve(host, pinned_ip) + redirect(Policy::none()) + HOST_ALLOWLIST. Cite exact symbols/lines; the WRITE path reuses this pin, and the git.push broker leg reuses .resolve(host, pinned_ip).
    - crates/sandbox/src/seccomp.rs — exec_child_filter denies socket(AF_INET)/socket(AF_INET6) only; AF_UNIX is PERMITTED. This is the ground truth for "child stays net-denied" and for the backstop that send-pack's own connect() would fail closed.
    - crates/brokerd/src/sinks/process_exec.rs — the Pattern-B confined-child launcher with run_launcher env_clear() + one-var credential injection; the git.push child (local pack generation) rides this pattern unchanged from git.commit.
    - scripts/check-invariants.sh — Gate 5 is the workspace-scoped supply-chain absence assertion (cargo tree --workspace -i aws-lc-rs / openssl-sys = absent, ring-only). HYG-01 will re-run this AFTER the transport-dep choice; reference it as the enforcement locus.
  </read_first>
  <action>
    Create planning-docs/DESIGN-v1.9-egress-policy.md, mirroring the v1.8 doc's shape (decisions-not-options, §-numbered, every load-bearing claim cited to a real file:line re-verified this session). Write the header/frontmatter (milestone v1.9, Phase 41 Design Gate, Status: Draft → pending fresh orchestrator-owned adversarial code-trace, Requirements: DESIGN-17 enables POLICY/GIT/HTTP-W/SDK/U1/HYG/LIVE phases), a §0 Purpose & Scope, then these sections:

    (1) §git.push egress — pin candidate (b) per DESIGN-17: a broker-performed git smart-HTTP transfer. The broker plays the HTTP-mover role (reqwest, TLS terminates broker-side, .resolve(host, pinned_ip) = the pin) driving the net-denied child's local-only pack generation (git send-pack --stateless-rpc, no socket). Pin these DECISIONS: child stays FULLY net-denied (identical to git.commit; NO seccomp relaxation — cite seccomp.rs exec_child_filter); the destination pin lives in the broker application layer where the IP:port is visible, NEVER seccomp; remote URL + refspec come from TRUSTED intent, never the untrusted repo's .git/config; --force/--force-with-lease/ref-deletion/+-force-refspec hard-denied by construction (unreachable even via human confirm). Then close the research's THREE attack points each with a named mechanism: (i) credential leak — the push token (Authorization header / userinfo@) never enters a minted ValueNode, audit-DAG literal, or broker log line; captured child/transport output scrubbed or not minted (carry forward v1.8 §2.5); (ii) pin-bypass — freeze ONE resolved IP across both the info/refs GET and the receive-pack POST, refuse POST 3xx redirects (redirect Policy::none() governs the POST, not just GET); (iii) payload-vs-destination confirm TOCTOU — the pack is frozen/generated from the confirmed commit range at-or-after confirm, and the tainted-file provenance summary shown at confirm describes the SAME bytes POSTed (carry forward v1.8 §2.7). State the confirm-release discipline carries forward v1.8 §9 (P33/P34: terminal audit event written before terminal state; prepare_git_push precheck). Include the SAFETY-VALVE per DESIGN-17: if no fully-unprivileged destination-pinning mechanism proves sound under the review, git.push defers (disclosed, sign-off-gated, the git.push leg auto-descopes from LIVE-05/06) and the other three tracks proceed — never a silent drop, never shipping arbitrary net-allowed child egress.

    (2) §http.request WRITE (POST/PUT) egress per DESIGN-17/HTTP-W-01: pin a DISTINCT write host-allowlist (separate from the shipped GET HOST_ALLOWLIST — a GET-readable host is NOT implicitly POST/PUT-writable); the request BODY is taint-governed and content-sensitive under I2 (a tainted body deterministically Blocks, exactly like an email/PR body); the url is routing-sensitive; reuse v1.8's SSRF resolve-and-pin (loopback/RFC1918/link-local/metadata/userinfo@/redirect denied) + webpki-roots egress (cite http_request.rs); any write credential lives in broker-local env only (never a ValueNode/plan-arg/audit-literal/worker/planner) with the response scrubbed or not minted; acceptance is DIFFERENTIAL (tainted-body-Blocks vs clean-body-Allowed identical in host/url/method/policy, clean leg confirmed delivered to the mock).

    (3) §crypto provider + supply-chain (HYG-01): pin ring-only, reuse the already-shipped reqwest+rustls(ring)+webpki-roots stack — ZERO new crates (per the research). State that the workspace-scoped supply-chain absence assertion (check-invariants Gate 5: cargo tree --workspace -i aws-lc-rs/openssl-sys = absent) RE-RUNS after the git.push transport-dependency is chosen, enumerating any new transport deps; any new dep must honor the ring-only recipe.

    (4) §Fail-closed defaults table — one row per new mechanism (git.push destination-pin, WRITE allowlist, credential custody, redirect refusal), each stating the safe default when a precondition is absent/ambiguous.

    End Task 1 with the sentinel line `<!-- gsd:policy-section-pending (Task 2) -->` so Task 2 knows where to append the policy boundary + threat model + acceptance sections. Do NOT paste v1.8 section bodies wholesale — reference them by section number.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-v1.9-egress-policy.md && grep -q "broker-performed" planning-docs/DESIGN-v1.9-egress-policy.md && grep -q "net-denied" planning-docs/DESIGN-v1.9-egress-policy.md && grep -q "resolve(host" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "WRITE allowlist\|write host-allowlist\|distinct" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "safety-valve\|defers" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "ring-only\|zero new crate" planning-docs/DESIGN-v1.9-egress-policy.md && grep -q "§2.5\|§2.7\|§9" planning-docs/DESIGN-v1.9-egress-policy.md && test -z "$(git status --porcelain -- crates cli)"</automated>
  </verify>
  <done>
    planning-docs/DESIGN-v1.9-egress-policy.md exists with the §git.push egress section pinning
    candidate (b) (broker-performed smart-HTTP, net-denied child, application-layer pin) and
    closing all three research attack points; the §http-write section pinning a distinct WRITE
    allowlist + taint-governed body + differential acceptance; the §crypto/supply-chain section
    pinning ring-only / zero-new-crates + the Gate-5 re-run; a fail-closed defaults table; the
    git.push safety-valve; and v1.8 §2.5/§2.7/§9 carried forward by reference. No changes to
    crates/ or cli/ (git status empty for those paths).
  </done>
  <acceptance_criteria>
    - git.push pins candidate (b): broker-side TLS + reqwest .resolve(host, pinned_ip) pin; child FULLY net-denied (no seccomp relaxation, cited to seccomp.rs); pin in the application layer, never seccomp.
    - Remote/refspec from TRUSTED intent (never untrusted .git/config); --force/--force-with-lease/ref-deletion/+-force-refspec hard-denied by construction.
    - All three research attack points closed with named mechanisms: credential-leak scrub; frozen-single-IP + POST-redirect-refusal; pack-frozen-at-or-after-confirm anti-TOCTOU.
    - http-write pins a DISTINCT write allowlist, taint-governed content-sensitive body under I2, routing-sensitive url, broker-env-only credential + response scrub, differential acceptance.
    - Crypto/supply-chain pins ring-only + ZERO new crates + the Gate-5 workspace-scoped absence re-run after the transport-dep choice.
    - v1.8 §2 / §2.5 / §2.7 / §9 carried forward BY REFERENCE (not restated wholesale).
    - Zero edits to crates/{executor,brokerd,sandbox,runtime-core} or cli/.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the policy-vs-I2 boundary (incl. POLICY-03 binding), the §-per-pitfall threat model, invariant preservation, the orchestrator-owned-review gate, and the acceptance predicate</name>
  <files>planning-docs/DESIGN-v1.9-egress-policy.md</files>
  <read_first>
    - cli/caprun/src/key.rs — the F1 containment check (canonicalize the workspace root, refuse if the candidate path is equal-to-or-beneath it; an unresolvable/absent path is itself a refusal; :memory: runs no F1 check). POLICY-03 REUSES THIS VERBATIM for policy binding — cite the exact function + line range.
    - planning-docs/PLAN.md — the I0/I1/I2 invariant definitions + the locked plan-node effect path (no raw EffectRequest{args}→sink). The policy section must show policy weakens NONE of I0/I1/I2.
    - planning-docs/DESIGN-git-github-http-sinks.md — §6 (threat-model, §-per-pitfall → named mechanism), §7 (invariant preservation), §9 (confirm-release), §10 (new-symbol summary), §12 (acceptance predicate). Mirror this shape.
    - planning-docs/DESIGN-GATE-RECORD-v1.8.md — how the orchestrator-owned fresh non-self adversarial code-trace is recorded (the gate the doc must declare it will pass); the executor does NOT run this trace.
    - .planning/REQUIREMENTS.md — POLICY-01/02/03 exact text incl. the [rev: …] annotations; the doc's policy section must satisfy all three.
  </read_first>
  <action>
    Replace the `<!-- gsd:policy-section-pending (Task 2) -->` sentinel with the remaining sections:

    (5) §policy-vs-I2 boundary (POLICY-01/02/03) — the #1 adversarial-trace risk; pin EXACTLY what policy can and cannot do per DESIGN-17: policy is a PRE-I2 narrowing gate that only decides WHICH sinks/args are callable (a hardcoded-schema struct/file, NOT Cedar), refusing a non-permitted sink/arg with a DISTINCT machine-checkable policy-deny outcome separate from an I2 Block (POLICY-01); policy can NEVER disable or override I2 — I2 stays HARDCODED in the Rust TCB executor and executes UNCONDITIONALLY on every policy-PERMITTED call, never short-circuited by any policy outcome (POLICY-02, LOCKED). Then pin POLICY-03 binding: the broker binds the policy at session creation from a trusted source PROVABLY outside the confined worker's reach — canonicalized and REFUSED if it resolves at-or-beneath the workspace root (reuse the F1 containment check from cli/caprun/src/key.rs verbatim — cite the function + line range), immutable for the session's life, identity/hash recorded as a genuine audit-DAG event; a confined worker cannot mutate its own policy (proven later by a negative live leg). Name any new symbols (e.g. a PolicyDeny DenyReason distinct from the I2 Block, a policy-bound audit-event kind).

    (6) §Threat model — one § per pitfall → named mechanism (mirror v1.8 §6). Cover at minimum: git.push credential leak; git.push pin-bypass (redirect/DNS-rebind); git.push payload-vs-destination TOCTOU; send-pack self-egress backstop (seccomp AF_INET→EPERM fail-closed); http-write tainted-body exfil; http-write credential leak in captured response; policy-as-I2-override (the LOCKED anti-requirement); policy-from-worker-writable-location (the F1-precedent attack); tainted policy path traversal. Each row: threat → the structural mechanism that closes it.

    (7) §Invariant preservation — show the design weakens NONE of I0/I1/I2 and adds NO raw EffectRequest{args}→sink path (every effect stays a plan node). Explicitly: policy narrows but never widens authority; I2 stays in the TCB.

    (8) §New-mechanism symbol summary — the new TaintLabel/DenyReason/audit-event symbols the v1.9 sinks introduce + any mandated check-invariants gate extension.

    (9) §Adversarial-trace gate (DESIGN-18) — state that a fresh, NON-SELF, ORCHESTRATOR-OWNED adversarial code-trace (NOT a gsd-executor — gsd-executors have no Agent tool) must clear this doc with every BLOCKER/MAJOR resolved before ANY crates/{executor,brokerd,sandbox,runtime-core} or cli/ TCB code, AND that the trace RE-RUNS if the git.push trust-posture or transport-dependency choice changes mid-implementation. Note the executor does not run this trace; it is the orchestrator's gate, recorded in a DESIGN-GATE-RECORD-v1.9.md the orchestrator writes.

    (10) §Acceptance predicate — "Done when": the doc pins all three mechanisms as decisions, carries forward v1.8 §2/§2.5/§2.7/§9, includes the safety-valve, and clears the orchestrator-owned adversarial trace; and no TCB code exists until it does.
  </action>
  <verify>
    <!-- planner-region-allow: gsd:policy-section-pending -->
    <!-- Sequential intra-plan sentinel: Task 1 writes it, Task 2 replaces it, then asserts its absence. Satisfiable by construction. -->
    <automated>grep -q "POLICY-03" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "F1 containment\|at-or-beneath\|beneath the workspace" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "pre-I2 narrowing\|never.*override I2\|never disable" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "HARDCODED\|hardcoded" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "orchestrator-owned" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "re-run" planning-docs/DESIGN-v1.9-egress-policy.md && grep -qi "acceptance predicate\|done when" planning-docs/DESIGN-v1.9-egress-policy.md && ! grep -q "gsd:policy-section-pending" planning-docs/DESIGN-v1.9-egress-policy.md && test -z "$(git status --porcelain -- crates cli)"</automated>
  </verify>
  <done>
    The doc's §policy-vs-I2 boundary pins POLICY-01/02/03 (distinct policy-deny outcome; I2
    HARDCODED and unconditional on permitted calls; POLICY-03 F1-containment binding reusing
    key.rs verbatim, immutable, audit-hashed); a §-per-pitfall threat model closes every listed
    threat with a named mechanism; §invariant-preservation shows I0/I1/I2 intact + no raw
    EffectRequest path; the §adversarial-trace gate declares the trace ORCHESTRATOR-owned (not a
    gsd-executor) and re-running on a mid-build trust-posture/transport pivot; and an acceptance
    predicate closes the doc. The sentinel is gone. No changes to crates/ or cli/.
  </done>
  <acceptance_criteria>
    - Policy pinned as a PRE-I2 narrowing gate with a DISTINCT machine-checkable policy-deny outcome; I2 stays HARDCODED in the Rust TCB executor and runs unconditionally on every policy-permitted call (POLICY-01/02).
    - POLICY-03 binding reuses the F1 containment check from key.rs VERBATIM (cited to function + line range), refuses at-or-beneath-workspace paths, immutable for the session, hash recorded as an audit-DAG event.
    - Threat model has one § per pitfall → named mechanism, covering all three git.push research attack points + the send-pack backstop + http-write + the two policy anti-requirements.
    - Invariant-preservation section shows I0/I1/I2 unweakened and no raw EffectRequest{args}→sink path.
    - The adversarial-trace gate (DESIGN-18) is declared ORCHESTRATOR-owned (not a gsd-executor) and re-runs on a mid-implementation git.push trust-posture / transport-dep pivot.
    - The Task-1 sentinel is removed; the doc reads as one coherent artifact.
    - Zero edits to crates/{executor,brokerd,sandbox,runtime-core} or cli/.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design → implementation | This phase crosses NO runtime trust boundary — it writes one markdown file and touches zero TCB code. The load-bearing "boundary" is temporal: an under-specified or ungrounded DESIGN doc that greenlights an unsound git.push mechanism would let an arbitrary-egress exfil primitive reach the implementation phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | DESIGN doc greenlights an unsound git.push egress (net-allowed child / seccomp-pinned destination) | critical | mitigate | The doc PINS candidate (b) with the child FULLY net-denied + application-layer pin; the fresh orchestrator-owned adversarial code-trace (DESIGN-18) must clear it before any TCB code; the safety-valve defers git.push (disclosed, sign-off-gated) if no sound mechanism holds. |
| T-41-02 | Elevation of Privilege | DESIGN doc lets policy override/disable I2 (the #1 adversarial-trace risk) | critical | mitigate | The policy section pins policy as a PRE-I2 narrowing gate only; I2 stays HARDCODED in the Rust TCB executor, unconditional on every policy-permitted call — asserted as a LOCKED invariant + traced by the orchestrator-owned reviewer. |
| T-41-03 | Elevation of Privilege | Policy bound from a worker-writable location (worker widens its own allowlist) | high | mitigate | POLICY-03 pins F1 containment reused verbatim from key.rs — refuse any policy path at-or-beneath the workspace root; immutable for the session; hash recorded as an audit-DAG event. |
| T-41-04 | Information Disclosure | git.push / http-write credential leaks via broker HTTP echoes, logs, or captured output minted as a ValueNode | high | mitigate | The doc carries forward v1.8 §2.5 scrub discipline: token never enters a ValueNode/audit literal/broker log; captured output scrubbed or not minted; asserted in the threat-model section. |
| T-41-05 | Spoofing | git.push destination-pin bypass via receive-pack redirect / DNS-rebind between the two requests | high | mitigate | The doc pins one frozen resolved IP across the info/refs GET and the receive-pack POST + POST 3xx redirect refusal (redirect Policy::none() governs the POST). |
| T-41-06 | Tampering | Executor drifts into TCB code during doc authoring (violates the design-gate) | high | mitigate | Both tasks' verify gates assert `git status --porcelain -- crates cli` is empty; files_modified is the DESIGN doc only; the check-invariants no-TCB-code discipline holds at phase end. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this docs-only phase — no legitimacy gate required. HYG-01's supply-chain absence re-run for the git.push transport dep is scoped to Phase 44, where the transport dependency is actually chosen. |
</threat_model>

<verification>
Phase-level checks (the orchestrator owns the DESIGN-18 gate; the two below are the
executor-observable checks):

1. `test -f planning-docs/DESIGN-v1.9-egress-policy.md` — the single deliverable exists.
2. `test -z "$(git status --porcelain -- crates cli)"` — NO TCB code was written (the
   design-gate discipline held).
3. Structural greps (per-task `<automated>`) confirm the doc pins all three mechanisms,
   carries forward v1.8 §2.5/§2.7/§9, includes the safety-valve, POLICY-03 F1 binding, the
   pre-I2-narrowing / HARDCODED-I2 boundary, and the orchestrator-owned re-running trace.

**DESIGN-18 (orchestrator-owned, NOT an executor task):** After this plan completes, the
ORCHESTRATOR spawns a fresh, non-self adversarial code-trace of the DESIGN doc (a different
model, non-self — the standing `fresh-context-adversarial-review` guardrail that has caught
9+ real BLOCKER/MAJOR defects through v1.8). Every BLOCKER/MAJOR must be resolved and folded
back into the doc before the gate clears and before ANY Phase 42-46 TCB code. The trace
RE-RUNS if the git.push trust-posture or transport-dependency choice changes mid-implementation.
The orchestrator records the outcome in `planning-docs/DESIGN-GATE-RECORD-v1.9.md`. gsd-executors
have no Agent tool and MUST NOT be tasked with running or self-performing this review.
</verification>

<success_criteria>
- planning-docs/DESIGN-v1.9-egress-policy.md exists and pins, as DECISIONS: (a) git.push
  broker-performed smart-HTTP destination-pinned egress with the child net-denied, (b)
  http.request WRITE egress on a distinct allowlist, (c) the policy-vs-I2 boundary incl.
  POLICY-03 F1-containment binding (DESIGN-17).
- The doc closes the research's three git.push attack points, carries forward v1.8
  §2/§2.5/§2.7/§9 by reference, pins ring-only / zero-new-crates + the Gate-5 re-run, has a
  fail-closed defaults table + a §-per-pitfall threat model, and formalizes the git.push
  safety-valve (disclosed deferral, never a silent drop).
- The doc declares the fresh adversarial code-trace ORCHESTRATOR-owned (not a gsd-executor)
  and re-running on a mid-build trust-posture/transport pivot (DESIGN-18).
- No TCB code exists: `git status --porcelain -- crates cli` is empty.
</success_criteria>

<output>
Create `.planning/phases/41-v1-9-design-gate-fresh-adversarial-code-trace/41-01-SUMMARY.md` when done.
</output>
