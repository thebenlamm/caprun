---
phase: 06-deterministic-planner-intent-input
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-04"]
files_modified:
  - cli/caprun/tests/s9_live_block.rs
autonomous: true
requirements: [PLAN-01, PLAN-04, HARD-02]

must_haves:
  truths:
    - "A live `caprun send-email-summary <recipient> <clean-file>` run exits 0 (success)"
    - "The run's audit DB contains an intent_received event AND a plan_node_evaluated event"
    - "The run's audit DB contains NO sink_blocked event"
    - "The intent_received event carries no taint (taint: [])"
  artifacts:
    - "cli/caprun/tests/s9_live_block.rs clean allow-path e2e test (Linux-gated)"
  key_links:
    - "The clean-path e2e is the end-to-end proof that HARD-02 (executor allows UserTrusted) + PLAN-04 (mint_from_intent anchor) + PLAN-01 (intent CLI) compose into a reachable allow-path"
    - "Test body is #[cfg(target_os = \"linux\")]; on macOS it is a 0-assertion no-op — real proof is the Colima/Docker full suite (phase gate)"
    - "Content is CLEAN (no email address) so the only routing-sensitive arg value is the UserTrusted intent recipient — isolating the allow-path from the hostile block"
---

<objective>
Prove the clean allow-path end to end: a real `caprun` invocation with a user-provided intent recipient
against clean workspace content exits 0, with an `intent_received` and a `plan_node_evaluated` event
(and no `sink_blocked`) in the audit DB. This is the phase's live success-criteria proof (SC1 + SC4).

Purpose: unit and in-process tests prove the pieces; this Linux-gated e2e proves they compose across
the confined worker, the ProvideIntent round-trip, the executor predicate, and the email.send stub.
Output: a new clean allow-path test in the existing s9_live_block.rs e2e suite.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deterministic-planner-intent-input/06-RESEARCH.md
@.planning/phases/06-deterministic-planner-intent-input/06-PATTERNS.md

@cli/caprun/tests/s9_live_block.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean allow-path e2e test (Linux-gated)</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs (run_caprun_on helper lines 39-60, the hostile s9_live test, and the #[cfg(target_os = "linux")] gating pattern; the ungated cross-platform guard test at the end)
    - 06-PATTERNS.md § s9_live_block.rs (clean-path differences, run_caprun_intent_on helper, assertions)
    - crates/brokerd/src/audit.rs find_event_by_type (audit DB query helper)
  </read_first>
  <action>
    Extend cli/caprun/tests/s9_live_block.rs with a Linux-gated clean allow-path test. Add a helper
    (or extend the existing one) that runs the caprun binary with the new positional args:
    intent-kind "send-email-summary", an intent-param recipient literal, a workspace file containing
    CLEAN content (no email address — e.g. a plain summary sentence), and an audit-db path. Gate all new
    bodies with #[cfg(target_os = "linux")], matching the existing tests; leave the always-compiled
    cross-platform guard test ungated. Assert: (1) caprun exits success (status.success() == true);
    (2) the audit DB contains an intent_received event; (3) the audit DB contains a plan_node_evaluated
    event; (4) the audit DB contains NO sink_blocked event; (5) the intent_received event carries empty
    taint. Reuse find_event_by_type / the existing audit-DB query approach — do not hand-roll SQL.
  </action>
  <verify>
    <automated>cargo test -p caprun --test s9_live_block 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - New test compiles and passes on macOS as a 0-assertion no-op (cfg-gated) — expected.
    - On Linux (Colima/Docker full suite) the test drives a real run: caprun exits 0, intent_received + plan_node_evaluated present, no sink_blocked, intent_received taint empty.
    - `cargo test --workspace --no-fail-fast` green on macOS.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live caprun run | The e2e exercises the full confined-worker → broker → executor → email.send stub path under real kernel confinement (Linux) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Repudiation | unaudited allow-path | high | mitigate | Test asserts intent_received + plan_node_evaluated are durably in the audit DB — the allow decision is recorded, not silent |
| T-06-12 | Spoofing | false-green from macOS no-op | medium | mitigate | cfg-gated body means the real assertions only run on Linux; the phase gate requires the Colima/Docker full suite green before verify-work |

No package installs in this phase; `T-06-SC` not applicable.
</threat_model>

<verification>
- macOS: `cargo test -p caprun --test s9_live_block` compiles and passes (cfg-gated no-op on macOS).
- Linux phase gate: `docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test --workspace --no-fail-fast` — clean allow-path test green.
- `./scripts/check-invariants.sh` green.
</verification>

<success_criteria>
A real Linux caprun run of the clean allow-path exits 0 with a genuinely-audited intent_received → plan_node_evaluated chain and no block — closing Phase 6's four ROADMAP success criteria end to end.
</success_criteria>

<output>
Create `.planning/phases/06-deterministic-planner-intent-input/06-05-SUMMARY.md` when done.
</output>
