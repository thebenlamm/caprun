---
phase: 46-composed-live-proof-v1-9-done
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_9_composed.rs
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "The full authorized-write success chain runs on real Linux over ONE shared persisted audit.db, every leg its own session with verify_chain independently true."
    - "The git.push leg Blocks-pending-confirmation then a genuine confirm releases it to a single git_push_succeeded terminal event."
    - "The clean http.request.write POST delivers a body to the mock /ingest endpoint (http_write_succeeded on 201)."
    - "The whole run is inspected by a GENUINE compiled `caprun audit` subprocess reporting Chain verification: PASSED per composed session."
    - "At least one leg is driven by a genuine `caprun run` subprocess whose I2 Block is surfaced + reviewed + audited."
    - "The test file and its SUMMARY state BLUNTLY what is CLI-caprun-run-driven vs composed-in-crate-through-the-real-arms vs caprun-audit-inspected — no inflation."
  artifacts:
    - cli/caprun/tests/live_acceptance_v1_9_composed.rs
  key_links:
    - "raw read Event → ValueNode provenance_chain[0] == real read-event id → sink arg → gated dispatch (genuine, non-stapled taint)"
    - "evaluate_plan_node_and_record_for_test → BlockedPendingConfirmation → confirmation::confirm → git_push_succeeded"
    - "http.request.write POST → github-mock.caprun.test/ingest → 201 → http_write_succeeded"
    - "caprun audit <session> <db> → Chain verification: PASSED"
---

<objective>
Close wiring gap G1 (the composition approach) with the HYBRID decided by the
orchestrator: build a new Linux-gated composed live proof that (a) drives the
multi-sink SUCCESS chain through the REAL broker arms over one shared persisted
`audit.db`, (b) INSPECTS the whole run via a genuine compiled `caprun audit`
subprocess, and (c) includes at least ONE genuine `caprun run` CLI-driven leg.
This is half of the v1.9 DONE gate (LIVE-05).

Purpose: `caprun run` drives only single-node email/file intents — it cannot
express the v1.9 multi-sink write chain, and building a multi-node composed-intent
planner is out-of-scope new TCB against the project's manual-ops-first discipline.
The hybrid is faithful (real sinks, real mock endpoints, genuine non-stapled taint,
verify_chain true) AND honest about what each layer drives. FRAMING HONESTY IS
MANDATORY (this project's v1.3 DOC-01 / v1.4 P22 precedent): the test + SUMMARY
must never claim `caprun run` drove the whole chain.
Output: `cli/caprun/tests/live_acceptance_v1_9_composed.rs` — the composed success
proof, run under `scripts/compose-verify.sh --features brokerd/mock-egress-ca`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-composed-live-proof-v1-9-done/46-RESEARCH.md
@cli/caprun/tests/live_acceptance_v1_8_composed.rs
@cli/caprun/tests/s45_cli_viewer_acceptance.rs
@crates/brokerd/tests/s44_git_push_differential.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composed-run scaffold, shared persisted audit.db, and the FRAMING-HONESTY module doc</name>
  <files>cli/caprun/tests/live_acceptance_v1_9_composed.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_8_composed.rs:1-100 (module doc + cfg gating + imports — the exact structural template)
    - cli/caprun/tests/live_acceptance_v1_8_composed.rs:174-290 (shared-key + open_audit_db + shared persisted audit.db setup, F1-safe layout, session-id tracking)
    - cli/caprun/tests/live_acceptance_v1_8_composed.rs:928-967 (end-of-run sweep + host-portable guard binary present test)
    - .planning/phases/46-composed-live-proof-v1-9-done/46-RESEARCH.md (§ "How to Drive Composed + Inspect", Pitfalls 1-4)
  </read_first>
  <action>
    Create the new test file with a `#[cfg(target_os = "linux")]` body (macOS
    compiles 0 legs — expected per CLAUDE.md), mirroring
    live_acceptance_v1_8_composed.rs. Set up ONE shared persisted `audit.db` (never
    `:memory:`) with its sibling `.key`, in an F1-safe layout (sibling of the
    workspace roots, never nested beneath a workspace root — the `caprun audit`
    subprocess and verify_chain must MAC against the same key). Seed the MAC key
    BEFORE any append (s45 seed_test_key pattern) so the subprocess reads it back.
    Track every persisted session id in a Vec for the final sweep. Use a single
    `#[tokio::test]` fn with sequential legs (no parallelism — avoids the G6 env
    race on CAPRUN_GITHUB_TOKEN / CAPRUN_GIT_PUSH_TOKEN / CAPRUN_GITHUB_API_BASE).
    Write a module doc comment that states BLUNTLY and in full (framing honesty,
    LIVE-05 locked decision #1): the multi-sink SUCCESS chain is COMPOSED IN-CRATE
    through the REAL broker arms (`evaluate_plan_node_and_record_for_test` +
    `confirmation::confirm`) — NOT driven by one `caprun run`; the run is INSPECTED
    by a genuine compiled `caprun audit` subprocess; and at least one leg is
    genuinely CLI-driven via `caprun run`. Do NOT claim `caprun run` drove the
    whole chain anywhere in the doc or assertions.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_9_composed --no-run 2>&1 | grep -Eq 'Compiling|Finished|warning' && echo compiled-ok</automated>
  </verify>
  <acceptance_criteria>
    - File compiles on macOS with the Linux body cfg-excluded (0 legs run on host).
    - One shared persisted `audit.db` + sibling `.key`, F1-safe layout, key seeded
      before any append.
    - Module doc contains a blunt framing-honesty statement distinguishing
      in-crate-composed-through-real-arms vs `caprun run`-driven vs
      `caprun audit`-inspected, with NO claim that `caprun run` drove the chain.
    - Single sequential `#[tokio::test]` fn; per-leg env set_var/remove_var.
  </acceptance_criteria>
  <done>Completed: Composed-run scaffold, shared persisted audit.db, and the FRAMING-HONESTY module doc; all acceptance criteria above are met.</done>
</task>

<task type="auto">
  <name>Task 2: The six success legs through the REAL broker arms (genuine, non-stapled)</name>
  <files>cli/caprun/tests/live_acceptance_v1_9_composed.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_8_composed.rs:290-700 (process.exec, file edit, git.commit, github.pr legs through the real arms + per-leg verify_chain)
    - crates/brokerd/tests/s44_git_push_differential.rs:521-600 (setup_git_push_repo small mock repo + evaluate_and_confirm confirm-release pattern)
    - crates/brokerd/tests/s43_http_write_differential.rs:118-192 (mint_clean body + write_node PlanNode shape for http.request.write)
    - crates/brokerd/tests/s44_git_push_differential.rs:649-700 (leg_c clean confirmed push reaches mock receive-pack)
  </read_first>
  <action>
    Chain the SUCCESS legs, each its own session, over the shared audit.db, driven
    through the REAL production arms (`evaluate_plan_node_and_record_for_test` — the
    `test-fixtures`-gated verbatim delegate to the live arm, closing the Phase-38
    mirror-drift finding): (1) process.exec (mint provenance via the real
    exec-read path); (2) filesystem edit; (3) git.commit; (4) git.push — mint clean
    remote/refspec via the trusted-intent path, submit → assert
    `BlockedPendingConfirmation` (git.push is ALWAYS confirm-gated per Phase 44 —
    server.rs always-confirm-gate, NO auto-dispatch; locked decision #2), read the
    `effect_id` from `pending_confirmations`, then `confirmation::confirm` →
    `ConfirmOutcome::Released` → exactly one opaque `git_push_succeeded` terminal
    event (reuse s44 evaluate_and_confirm verbatim; push a SMALL one-commit mock
    repo — the 10MB pack-cap is non-blocking here, note it in the SUMMARY);
    (5) github.pr (mock 201); (6) http.request.write POST to
    `https://github-mock.caprun.test/ingest` with a UserTrusted (clean) body,
    method POST, `SessionPolicy::broker_default()` (permits http.request.write) →
    Allowed dispatch → `invoke_http_write_sink` → 201 → one `http_write_succeeded`
    terminal event (requires 46-01's /ingest endpoint). Assert per-leg
    `verify_chain` is independently true. Enforce anti-stapling (Pitfall 1): for the
    tainted-provenance mints assert `anchor.provenance_chain[0]` equals a REAL read
    event id (e.g. a `process_exited` / `http_response_received`) — a genuine
    unbroken DAG edge, never a hand-set taint field. Thread blocked/terminal events
    onto the chain HEAD the mint returns (Pitfall 3), never onto the raw response id.
    Set/remove the push + github token env vars around each leg (G6).
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_9_composed --no-run 2>&1 | grep -Eq 'Finished|Compiling' && echo compiled-ok</automated>
  </verify>
  <acceptance_criteria>
    - All six success legs present, each its own session, driven through
      `evaluate_plan_node_and_record_for_test` (+ `confirmation::confirm` for push).
    - git.push asserts `BlockedPendingConfirmation` THEN a confirm release to a
      single `git_push_succeeded`; the POST leg asserts `http_write_succeeded` on 201.
    - Every leg asserts `verify_chain` true; tainted mints assert
      `provenance_chain[0]` == a real read-event id (non-stapled).
    - Authoritative pass is under compose-verify (Linux); host compile is green.
  </acceptance_criteria>
  <done>Completed: The six success legs through the REAL broker arms (genuine, non-stapled); all acceptance criteria above are met.</done>
</task>

<task type="auto">
  <name>Task 3: Genuine caprun audit inspection + genuine caprun run Block leg + guard + final sweep</name>
  <files>cli/caprun/tests/live_acceptance_v1_9_composed.rs</files>
  <read_first>
    - cli/caprun/tests/s45_cli_viewer_acceptance.rs:61-135 (run_caprun subprocess helper, CARGO_BIN_EXE_caprun, seed_test_key, extract_surfaced_effect_id)
    - cli/caprun/tests/s45_cli_viewer_acceptance.rs:271-401 (run_genuine_hostile_file_create + end_to_end_run_block_surface_review_audit: Chain verification PASSED assertions)
    - cli/caprun/tests/live_acceptance_v1_8_composed.rs:961-967 (host-portable guard binary present test)
  </read_first>
  <action>
    Add the CLI inspection + genuine-driver layer. (a) For each composed session,
    spawn the REAL compiled `caprun audit <session_id> <db>` subprocess (via
    `env!("CARGO_BIN_EXE_caprun")`) and assert stdout contains
    `Chain verification: PASSED` plus the rendered terminal/sink events for that
    session — the same read-only viewer proven in s45 (U1). (b) Add a genuinely
    CLI-driven leg: run `caprun run --policy <trusted> create-file-from-report ...`
    as a subprocess against untrusted (tainted) report content (verbatim s45
    `run_genuine_hostile_file_create`) so a confined worker's I2 Block is surfaced
    by the parent (`effect_id=` + the `caprun review` pointer), then inspect that
    session via `caprun audit` — this literally satisfies "driven via `caprun run`"
    on at least one confined leg (LIVE-05 / decision #1c). (c) Add a host-portable
    `#[test]` guard asserting `CARGO_BIN_EXE_caprun` resolves (mirror v1.8
    composed:961) so macOS `cargo test -p caprun` stays meaningful. (d) Final sweep:
    open the shared audit.db ONCE, enumerate sessions (ORDER BY rowid, never LIMIT
    1), assert EXACTLY the composed success + CLI-block session set exists and every
    `verify_chain` is independently true.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_9_composed 2>&1 | grep -Eq 'test result: ok|0 passed' && echo host-guard-ok</automated>
  </verify>
  <acceptance_criteria>
    - A genuine `caprun audit` subprocess renders `Chain verification: PASSED` for
      every composed session.
    - A genuine `caprun run` subprocess drives a confined leg whose I2 Block
      surfaces an `effect_id` + `caprun review` pointer and is then audited.
    - Host-portable guard test passes on macOS; the Linux body stays cfg-gated.
    - Final sweep asserts the exact composed session set with all verify_chain true.
  </acceptance_criteria>
  <done>Completed: Genuine caprun audit inspection + genuine caprun run Block leg + guard + final sweep; all acceptance criteria above are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted read → ValueNode taint | raw read bytes must carry genuine provenance (non-stapled) into any sink arg |
| confined worker → broker | the only egress is broker-mediated plan nodes; the `caprun run` leg exercises a real confined worker |
| audit key custody | verify_chain + `caprun audit` must MAC against the same persisted key, out of the worker's reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02-01 | Tampering | stapled taint = false §9 proof | high | mitigate | Mint via real broker read paths; assert `provenance_chain[0]` == a real read-event id (Pitfall 1); the DONE gate requires a genuine unbroken DAG edge. |
| T-46-02-02 | Repudiation | verify_chain fork on tainted mint | high | mitigate | Thread blocked/terminal events onto the mint's returned chain head, never the raw response id (Pitfall 3); per-leg + final-sweep verify_chain asserted. |
| T-46-02-03 | Elevation | git.push auto-dispatch bypassing confirm | high | mitigate | Assert `BlockedPendingConfirmation` FIRST (always-confirm-gate), release only via `confirmation::confirm` — no auto-dispatch arm exercised. |
| T-46-02-04 | Info disclosure | `caprun audit` verdict meaningless on wrong/`:memory:` key | medium | mitigate | Shared persisted `.key` seeded before append; F1-safe layout; `:memory:` refusal proven in s45 — reused viewer fails closed. |
| T-46-02-05 | Spoofing | framing inflation (claiming caprun run drove the chain) | medium | mitigate | Blunt framing-honesty module doc + SUMMARY (v1.3 DOC-01 precedent); at least one genuine `caprun run` leg; the rest labelled composed-in-crate. |
| T-46-02-SC | Tampering | new package installs | low | accept | None — new test file only, no new dependency; compose-verify installs pre-existing libssl-dev/pkg-config only. |
</threat_model>

<verification>
- Host: `cargo test -p caprun --test live_acceptance_v1_9_composed` compiles; the
  host-portable guard test passes; Linux legs are cfg-excluded (0 run — expected).
- Authoritative (Linux, orchestrator at phase close):
  `COMPOSE_VERIFY_CMD='cargo build --workspace && cargo test -p caprun --test live_acceptance_v1_9_composed --features brokerd/mock-egress-ca' bash scripts/compose-verify.sh`
  — every success leg + the caprun audit / caprun run legs green, verify_chain PASSED.
</verification>

<success_criteria>
- The full authorized-write success chain runs on real Linux over one shared
  persisted audit.db, DRIVEN through the real broker arms + genuinely inspected via
  `caprun audit`, with at least one genuine `caprun run` leg, verify_chain true
  across the run, and honest framing throughout — satisfying LIVE-05.
</success_criteria>

<output>
Create `.planning/phases/46-composed-live-proof-v1-9-done/46-02-SUMMARY.md` when done.
State BLUNTLY in the SUMMARY what is CLI-`caprun run`-driven vs
composed-in-crate-through-the-real-arms vs `caprun audit`-inspected; note the git.push
leg pushed a SMALL mock repo (10MB pack-cap deferred, non-blocking).
</output>
