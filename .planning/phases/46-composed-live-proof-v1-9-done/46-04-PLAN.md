---
phase: 46-composed-live-proof-v1-9-done
plan: 04
type: execute
wave: 3
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - .planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md
autonomous: false
requirements: [LIVE-05, LIVE-06]
must_haves:
  truths:
    - "The FULL compose-verify.sh (mock-egress-ca) runs green on real Linux with the new composed success + negative-leg tests AND no v1.0–v1.8 regression."
    - "A v1.9 milestone DONE record states BLUNTLY what is CLI-caprun-run-driven vs composed-in-crate vs caprun-audit-inspected — no inflation."
    - "The record documents git.push SHIPPED (safety-valve NOT triggered, no descope) and the 10MB pack-cap deferred item (non-blocking)."
    - "A human sign-off is written into the record BEFORE any phase-completion state flips."
  artifacts:
    - .planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md
  key_links:
    - "compose-verify rc captured BEFORE any pipe → asserted on named tests + counts (never exit 0)"
    - "record sign-off written first → phase.complete reconciles REQUIREMENTS (executor never touches ROADMAP/STATE)"
---

<objective>
Close the v1.9 DONE gate: run the FULL authoritative Linux regression
(`scripts/compose-verify.sh --features brokerd/mock-egress-ca`) proving the new
composed success proof (46-02) + the 5 negative legs (46-03) pass AND no v1.0–v1.8
regression (Success Criterion 3), then write the v1.9 milestone DONE record with
blunt framing honesty and a human sign-off. Covers the acceptance-closure of
LIVE-05 + LIVE-06.

Purpose: A DONE-gate phase must produce the real Linux green evidence — "substrate
working ≠ v1.9 done" (CLAUDE.md). The record must be honest about the hybrid
composition (v1.3 DOC-01 precedent) and must carry human sign-off BEFORE any
checkbox flips (the [[gsd-record-signoff-before-last-plan]] lesson — flipping the
last requirement auto-rolls the milestone; let phase.complete reconcile REQUIREMENTS).
Output: `46-MILESTONE-RECORD.md` with the gate evidence, framing-honesty statement,
git.push safety-valve disposition, and sign-off.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-composed-live-proof-v1-9-done/46-RESEARCH.md
@scripts/compose-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the authoritative full-workspace Linux regression gate</name>
  <files>.planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md</files>
  <read_first>
    - scripts/compose-verify.sh:44-110 (COMPOSE_VERIFY_CMD default, feature-OFF guard, mock-egress-ca)
    - scripts/compose-verify.sh:195-207 (rc captured BEFORE any pipe; assert on named tests + counts)
    - CLAUDE.md ("Linux-only security tests" + "From Phase 16 onward, ALL Linux verification goes through scripts/mailpit-verify.sh / compose-verify.sh")
  </read_first>
  <action>
    Run the FULL authoritative gate with the DEFAULT COMPOSE_VERIFY_CMD
    (`cargo build --workspace && cargo test --workspace --no-fail-fast --features
    brokerd/mock-egress-ca`) so the ENTIRE workspace — the new 46-02 + 46-03 tests
    AND every v1.0–v1.8 test — runs on real Linux: `bash scripts/compose-verify.sh`.
    Capture the exit code from the script (it captures `rc` before any pipe,
    compose-verify.sh:199 — MEMORY [[verification-exit-code-through-pipe]]); do NOT
    trust a piped exit 0. Assert green on: the feature-OFF guard step
    (mock host absent from the release build), the new composed success test
    (`live_acceptance_v1_9_composed`), the new negative-legs test
    (`s46_negative_legs_composed`), and the full-workspace pass count with zero
    failures (no v1.0–v1.8 regression, Success Criterion 3). Record the named-test
    results + pass/fail counts verbatim into 46-MILESTONE-RECORD.md as the gate
    evidence. If ANYTHING fails, STOP — surface the failing test; do NOT soften,
    skip, or downgrade (Critical Rules). This is the executor's evidence run; the
    orchestrator re-runs the same gate authoritatively at phase close.
  </action>
  <verify>
    <automated>bash scripts/compose-verify.sh 2>&1 | tee /tmp/46-compose.log; grep -Eq 'feature-OFF-guard-passed' /tmp/46-compose.log && grep -Eq 'test result: ok' /tmp/46-compose.log && ! grep -Eq 'test result: FAILED|FAILED\. ' /tmp/46-compose.log && echo compose-verify-green</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compose-verify.sh` exits 0 (rc captured before any pipe); the
      feature-OFF guard, `live_acceptance_v1_9_composed`, and
      `s46_negative_legs_composed` all pass on real Linux.
    - Full-workspace pass count recorded with ZERO failures (no v1.0–v1.8 regression).
    - The gate evidence (named tests + counts) is written into 46-MILESTONE-RECORD.md.
  </acceptance_criteria>
  <done>Completed: Run the authoritative full-workspace Linux regression gate; all acceptance criteria above are met.</done>
</task>

<task type="auto">
  <name>Task 2: Write the v1.9 milestone DONE record with framing honesty and safety-valve disposition</name>
  <files>.planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:43-44 (LIVE-05 / LIVE-06 locked text + the M6/n1 safety-valve clause)
    - .planning/ROADMAP.md (Phase 39 deferred → Phase 44 SHIPPED; Phase 46 Success Criteria)
    - .planning/STATE.md ("Deferred Items" — the git.push 10MB pack-cap functional note)
  </read_first>
  <action>
    Write the v1.9 "Authorized Egress + Policy & Audit Surface" DONE record. Include,
    BLUNTLY and in full (framing honesty — NEVER compressed away): (a) the hybrid
    composition disclosure — the multi-sink SUCCESS chain was COMPOSED IN-CRATE
    through the REAL broker arms (`evaluate_plan_node_and_record_for_test` +
    `confirmation::confirm`), INSPECTED via a genuine `caprun audit` subprocess, with
    at least one genuinely `caprun run`-driven confined Block leg; the whole chain was
    NOT driven by a single `caprun run` (planner is single-node by design — no
    multi-node planner was built, per manual-ops-first scope). (b) The 5 negative legs
    with their distinct machine-checkable tags (sink_blocked vs code()=="policy_deny",
    redirect-refused, credential-absence). For leg 5, state HONESTLY which push proved
    each clause: value-store + audit-chain absence on the clean 200 push, and
    broker-log (stderr) absence on the ERROR-PATH (redirect-refused) push where
    `scrub_secrets`→`eprintln!` fires — NOT on the clean push (which emits no log).
    (c) git.push SHIPPED in
    Phase 44 — the LIVE-05/06 safety-valve (M6/n1) did NOT trigger; there is NO
    descope, and the git.push leg IS in the composed proof (locked decision #6).
    (d) The 10MB pack-cap deferred item: the composed proof pushed a SMALL mock repo,
    so the cap is non-blocking here — carry it forward as a disclosed, non-blocking
    deferral (STATE Deferred Items). (e) The gate evidence from Task 1. Do NOT flip
    any ROADMAP.md / STATE.md / REQUIREMENTS.md checkbox — the executor never touches
    phase-completion state; note explicitly that REQUIREMENTS LIVE-05/06 reconciliation
    is performed by the orchestrator's `phase.complete` after human sign-off.
  </action>
  <verify>
    <automated>grep -Eiq 'composed in-crate|in-crate through the real|hybrid' .planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md && grep -Eiq 'safety-valve|did not trigger|SHIPPED' .planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md && grep -Eiq 'pack-cap|10\s?MB' .planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md && echo record-ok</automated>
  </verify>
  <acceptance_criteria>
    - The record states BLUNTLY what is CLI-`caprun run`-driven vs composed-in-crate
      vs `caprun audit`-inspected — no inflation, no claim that `caprun run` drove
      the whole chain.
    - git.push SHIPPED / safety-valve NOT triggered / no descope is recorded; the
      10MB pack-cap non-blocking deferral is carried forward.
    - No ROADMAP/STATE/REQUIREMENTS checkbox flipped by the executor.
  </acceptance_criteria>
  <done>Completed: Write the v1.9 milestone DONE record with framing honesty and safety-valve disposition; all acceptance criteria above are met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the v1.9 DONE record</name>
  <action>Present the completed 46-MILESTONE-RECORD.md and the compose-verify gate evidence to the human for sign-off. Write the human's approval verdict into the record BEFORE any phase-completion state is reconciled. Do NOT flip ROADMAP.md / STATE.md / REQUIREMENTS.md — the orchestrator's phase.complete reconciles LIVE-05/06 after approval.</action>
  <files>.planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md</files>
  <what-built>The v1.9 DONE gate: a composed authorized-write live proof on real Linux (success chain in-crate through the real broker arms + genuine caprun audit inspection + a genuine caprun run Block leg), 5 independently-attributable negative legs, full-workspace regression green, and a framing-honest milestone DONE record.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/46-composed-live-proof-v1-9-done/46-MILESTONE-RECORD.md` — confirm the framing-honesty statement is blunt and accurate (nothing claims `caprun run` drove the whole chain).
    2. Confirm the compose-verify gate evidence shows the feature-OFF guard + both new tests green and ZERO full-workspace failures (no v1.0–v1.8 regression).
    3. Confirm git.push SHIPPED / safety-valve-not-triggered and the 10MB pack-cap non-blocking deferral are disclosed.
    4. Optionally re-run `bash scripts/compose-verify.sh` yourself.
  </how-to-verify>
  <resume-signal>Type "approved" to sign off the v1.9 milestone as DONE (the orchestrator then reconciles REQUIREMENTS LIVE-05/06 via phase.complete), or describe gaps to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification pipe | a piped exit status masks the real gate result — rc must be captured before any pipe |
| record → phase-completion state | the DONE record must carry human sign-off BEFORE any checkbox flips |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04-01 | Repudiation | false PASS via a piped exit code | high | mitigate | compose-verify captures `rc` before any pipe and asserts on named tests + counts (compose-verify.sh:199; MEMORY [[verification-exit-code-through-pipe]]); the executor asserts named tests, never exit 0 alone. |
| T-46-04-02 | Spoofing | inflated DONE record claiming full `caprun run` drive | high | mitigate | Blunt framing-honesty statement mandated + human sign-off checkpoint (v1.3 DOC-01 precedent). |
| T-46-04-03 | Tampering | executor flipping ROADMAP/STATE/REQUIREMENTS prematurely | high | mitigate | Executor writes sign-off into the record only; phase-completion state is orchestrator-owned (the [[gsd-executor-self-marks-phase-complete]] guardrail). |
| T-46-04-04 | Info disclosure | a silently descoped git.push leg | medium | mitigate | git.push SHIPPED (Phase 44) — safety-valve explicitly recorded as NOT triggered; the composed proof INCLUDES the push leg (locked decision #6). |
| T-46-04-SC | Tampering | new package installs | low | accept | None — compose-verify installs pre-existing libssl-dev/pkg-config only; no new dependency. |
</threat_model>

<verification>
- `bash scripts/compose-verify.sh` exits 0 with the feature-OFF guard + both new
  tests green and zero full-workspace failures.
- 46-MILESTONE-RECORD.md contains the framing-honesty statement, git.push
  safety-valve disposition, pack-cap deferral, and gate evidence.
- Human sign-off recorded via the blocking checkpoint before phase-completion state
  is reconciled by the orchestrator.
</verification>

<success_criteria>
- The v1.9 DONE gate is met: composed proof + 5 negative legs green on real Linux
  with no v1.0–v1.8 regression, a framing-honest milestone record, and human
  sign-off — closing LIVE-05 + LIVE-06.
</success_criteria>

<output>
Create `.planning/phases/46-composed-live-proof-v1-9-done/46-04-SUMMARY.md` when done.
The milestone DONE record lives at `46-MILESTONE-RECORD.md`. Do NOT modify
ROADMAP.md / STATE.md / REQUIREMENTS.md — the orchestrator reconciles them at phase close.
</output>
