---
phase: 44-git-push-broker-performed-destination-pinned-egress
plan: 05
type: execute
wave: 4
depends_on: ["44-01", "44-02", "44-03", "44-04"]
files_modified:
  - scripts/mock-github/server.py
  - crates/brokerd/tests/s44_git_push_differential.rs
  - scripts/check-invariants.sh
  - scripts/compose-verify.sh
autonomous: true
requirements: [GIT-02, GIT-03, HYG-01]

must_haves:
  truths:
    - "The tainted-remote/refspec-Blocks leg and the clean-push-Allowed-then-confirmed leg are IDENTICAL in remote/refspec/policy/session-status EXCEPT taint (and, for the confirmed leg, the human confirm) — taint is the sole Block variable, so a block-everything I2 regression cannot pass (the clean leg MUST reach the mock git-receive-pack)."
    - "A clean git.push, after human confirm, dispatches through the frozen-IP client to the mock `git-receive-pack` endpoint on real Linux (mock records receipt), with the pack-gen child provably net-denied, the destination IP pinned, and a receive-pack 3xx refused."
    - "`--force`/`+`-refspec/`:delete` are refused by construction (unreachable even via confirm); a git.push from a draft/untrusted-seeded session is I0-Denied (CommitIrreversible)."
    - "After a real push, NO credential or remote-URL substring appears in the value store, the audit chain, OR broker log output; `verify_chain` is true across the run (LIVE-06 leg 5 slice, DESIGN §1.4)."
    - "The workspace-scoped supply-chain absence assertion re-runs after the transport-dep choice (aws-lc-rs/openssl-sys absent via any reqwest path; ring present); the compose-verify feature-OFF guard + the workspace-wide Gate 4b grep are in place (HYG-01)."
  artifacts:
    - "scripts/mock-github/server.py — a git-receive-pack mock (GET info/refs advertisement + POST git-receive-pack consuming the pack, backed by a bare repo, returning an unpack-ok report-status)."
    - "crates/brokerd/tests/s44_git_push_differential.rs — the GIT-02/GIT-03 differential + mock-endpoint dispatch acceptance test."
    - "scripts/check-invariants.sh — Gate 4b broadened workspace-wide; HYG-01 re-run asserts enumerated."
    - "scripts/compose-verify.sh — a feature-OFF guard step proving the mock host/anchor is absent from release builds."
  key_links:
    - "the tainted and clean legs share remote/refspec/policy/session-status; only the arg taint (and the confirm) differ — the 'taint is the sole variable' property is a literal assertion."
    - "the clean confirmed leg reaches the mock git-receive-pack (a recorded receipt), proving delivery-intent — not merely 'not blocked'; full composed LIVE-05/06 is Phase 46."
---

<objective>
Prove GIT-02/GIT-03 for git.push in isolation: a differential acceptance test where taint is the sole Block variable, plus a real-Linux dispatch of a clean confirmed push to a pinned mock `git-receive-pack` endpoint (net-denied child, IP pinned, redirect refused, `--force`/`:delete` refused by construction, credential/remote-URL absent from value-store + audit + broker logs, `verify_chain` true). Extend the mock harness with a git-receive-pack endpoint (WG-9) and complete HYG-01 (the post-transport-dep supply-chain absence re-run + the compose-verify feature-OFF guard + the workspace-wide Gate 4b grep).

Purpose: DESIGN §1 acceptance + ROADMAP Phase 44 Success Criteria 1-4. Acceptance is DIFFERENTIAL (RESEARCH §8 / the Phase-43 M4 discipline): the clean leg MUST reach egress AND the tainted leg MUST Block, attributable to I2 specifically, so a block-everything regression cannot masquerade as a pass. The full composed workflow (process.exec→edit→commit→push→PR + POST) driven via the CLI/viewer is Phase 46 (LIVE-05/06); 44-05 proves the git.push leg.

Output: A committed, green git.push differential + mock-endpoint acceptance test; a git-receive-pack mock; and the completed HYG-01 gate items.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The locked design context (READ §1.4/§1.5/§1.6 + §10 acceptance) + this phase's research (§7 HYG-01 command set, WG-9 mock, §8 acceptance shape):
@planning-docs/DESIGN-v1.9-egress-policy.md
@.planning/phases/44-git-push-broker-performed-destination-pinned-egress/44-RESEARCH.md

# The shipped mock-GitHub harness to extend, the invariant/compose gates to broaden, and an existing differential/composed broker acceptance test for shape (session setup, mint, submit_plan_node, confirm, audit assertions, verify_chain):
@scripts/mock-github/server.py
@scripts/check-invariants.sh
@scripts/compose-verify.sh
@crates/brokerd/tests/s43_http_write_differential.rs

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-receive-pack mock endpoint (WG-9)</name>
  <files>scripts/mock-github/server.py</files>
  <behavior>
    - `GET /<repo>/info/refs?service=git-receive-pack` returns a valid pkt-line ref advertisement (service announcement pkt + flush + ref lines + flush) for a backing bare repo.
    - `POST /<repo>/git-receive-pack` consumes the command-list + PACK body and returns an `application/x-git-receive-pack-result` report-status (`unpack ok` + per-ref `ok`), ideally by applying the pack to a real bare repo so the push is genuinely accepted (records receipt).
    - The mock records that a push was received (a receipt artifact/log the test can assert on).
    - Existing `POST /repos/*/pulls` behavior is unchanged; unrelated paths still 404.
  </behavior>
  <action>Extend `scripts/mock-github/server.py` (test-harness only, not TCB — Python is allowed here) with a git smart-HTTP receive-pack mock (RESEARCH WG-9): serve `GET /<repo>/info/refs?service=git-receive-pack` returning a valid pkt-line advertisement (the `# service=git-receive-pack` announcement pkt + flush, then ref lines, then a flush-pkt) for a backing bare repo, and `POST /<repo>/git-receive-pack` consuming the command-list + PACK body and returning a valid `application/x-git-receive-pack-result` report-status (a clean unpack + per-ref ok). Prefer backing it with a real bare git repo (init once, apply the received pack via `git receive-pack` or an equivalent) so the pushed pack is genuinely accepted and the mock records a receipt the test can assert on — matching the shipped mock-GitHub live-leg pattern. Keep the existing `POST /repos/*/pulls` PR endpoint and the 404-everything-else default unchanged. Ensure the mock host is served under the `mock-egress-ca` sidecar so it is only reachable when that non-default feature/harness is active.</action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/mock-github/server.py').read())"</automated>
  </verify>
  <done>The mock serves a valid git-receive-pack info/refs advertisement + accepts a receive-pack POST (backed by a bare repo, records receipt); PR endpoint + 404 default unchanged; the file parses.</done>
</task>

<task type="auto">
  <name>Task 2: git.push differential + mock-endpoint dispatch acceptance test</name>
  <files>crates/brokerd/tests/s44_git_push_differential.rs</files>
  <behavior>
    - LEG A (I0): a draft/untrusted-seeded session submitting git.push → Denied (DraftOnlySessionDeniesCommitIrreversible) — the CommitIrreversible I0 leg.
    - LEG B (tainted arg Blocks): an Active session, remote/refspec/policy fixed, a `remote` (and separately a `refspec`) minted with an untrusted taint label → BlockedPendingConfirmation whose anchor names the tainted arg.
    - LEG C (clean Allowed → confirm-gated → confirmed → delivered): the SAME Active session shape, SAME remote/refspec/policy, args minted UserTrusted — git.push is confirm-gated (BlockedPendingConfirmation even when clean), and after a single-shot confirm the push dispatches through the frozen-IP client to the mock git-receive-pack (Linux; mock records receipt), appending an opaque `git_push_succeeded` terminal event.
    - LEG D (structural denial): a `--force`/`+`-refspec and a `:delete` refspec are refused by construction (never dispatched), even through confirm.
    - LEG E (destination-pin / redirect): a receive-pack 3xx is refused (never followed) — the pin/redirect-none holds.
    - Credential-absence: after LEG C's real push, no credential or remote-URL substring appears in the value store, the audit chain, OR captured broker log output.
    - `verify_chain` is true for each session; B-vs-C assert remote/refspec/policy byte-identical (taint the sole Block variable).
  </behavior>
  <action>Create `crates/brokerd/tests/s44_git_push_differential.rs` (mirror `s43_http_write_differential.rs` shape). Build a helper constructing a git.push plan node + value_store with a fixed remote (a git-push-allowlisted mock host under the `mock-egress-ca` feature), a fixed refspec, and the taint on the target arg as the SOLE parameter; drive `executor::submit_plan_node` through the broker's value_store + `SessionPolicy::broker_default()` (assert the SAME policy for B and C). Assert LEG A (I0 draft-deny), LEG B (tainted remote Blocks on `remote`; tainted refspec Blocks on `refspec` — anchor names the arg, minted through the real broker mint path, genuine provenance, never a hand-set taint field), LEG C (clean args → BlockedPendingConfirmation because git.push is always confirm-gated → a single-shot confirm → dispatch reaches the mock git-receive-pack + a `git_push_succeeded` terminal event + mock receipt), LEG D (a force/`+`/`:delete` refspec is refused by construction — validate_git_refspec/command-list Err, no dispatch, no terminal success — even after a confirm), and LEG E (a receive-pack 3xx from the mock is refused, terminal git_push_failed, never followed). Assert B-vs-C equality of remote/refspec/policy literals in the test body (the "taint is the sole variable" property as a literal assertion). After LEG C, grep the persisted value store + audit-chain payloads + captured broker log output for absence of the credential token and any `<scheme>://<userinfo>@` remote-URL material (DESIGN §1.4 MINOR-4 / LIVE-06 leg 5 slice). Assert `verify_chain` true per session. Gate the socket/git/mock-endpoint legs `#[cfg(target_os = "linux")]` (the mock-endpoint delivery + credential-absence-after-real-push are the Linux legs); keep the pure decision legs (A, B, D-value-refusal) host-portable. Add a doc comment: the full composed multi-sink workflow driven via the CLI/viewer is Phase 46 (LIVE-05/06); this proves the git.push leg's differential + pinned mock-endpoint dispatch + credential absence in isolation.</action>
  <verify>
    <automated>cargo test -p brokerd --test s44_git_push_differential --no-fail-fast</automated>
  </verify>
  <done>All legs pass: I0 draft-deny; tainted remote/refspec Block on the named arg; clean confirm-gated push delivered to the mock git-receive-pack (receipt, net-denied child, IP pinned); force/delete refused by construction; redirect refused; credential/remote-URL absent from value-store+audit+logs; verify_chain true; B-vs-C identical-except-taint asserted literally.</done>
</task>

<task type="auto">
  <name>Task 3: HYG-01 — supply-chain absence re-run + compose-verify feature-OFF guard + Gate 4b broadening</name>
  <files>scripts/check-invariants.sh, scripts/compose-verify.sh</files>
  <behavior>
    - After the git.push transport code has landed, `cargo tree --workspace -i aws-lc-rs` resolves to absent and `cargo tree --workspace -i openssl-sys` shows it present ONLY via lettre (never via a reqwest/git-push path); ring is present; reqwest is unchanged (=0.13.4, no new features). No new crate was added.
    - `check-invariants.sh` Gate 4b (mock-egress-ca never a default feature) is a workspace-wide grep (not just brokerd).
    - `compose-verify.sh` has a feature-OFF guard step: a build/test run with NO `mock-egress-ca` feature proving the mock host/anchor (incl. the new git-receive-pack host) is absent from release builds.
    - The existing invariant gates (Gate 1/3/5) still pass.
  </behavior>
  <action>Complete HYG-01 (RESEARCH §7, ROADMAP Success Criterion 4). Run the supply-chain absence re-run NOW that the git.push transport code has landed: `cargo tree --workspace -i aws-lc-rs` (expect absent), `cargo tree --workspace -i openssl-sys` (expect present only via lettre's native-tls, never a reqwest/git-push path — verify with `cargo tree --workspace -i openssl-sys` and confirm the path), `cargo tree --workspace -i ring` (present), `cargo tree --workspace -i reqwest` (unchanged =0.13.4, no new features) — enumerating that NO new transport dep crept in (git.push added zero crates: pure pkt-line glue + reuse). Record the result in the SUMMARY. Broaden `scripts/check-invariants.sh` Gate 4b (the mock-egress-ca-never-default check, ~:180-189) from a brokerd-scoped grep to a workspace-wide grep (so no crate can make mock-egress-ca a default feature). Add a feature-OFF guard step to `scripts/compose-verify.sh`: a build/test run with NO `mock-egress-ca` feature that proves the mock hosts/anchors (the mock-GitHub host AND the new git-receive-pack host + the mock CA) are absent from release builds (mirror the existing feature-gating invariant style; assert the release binary/build does not reference the mock host). If (contrary to expectation) any new dep WERE added, it MUST use rustls-no-provider + an explicit ring CryptoProvider or Gate 5 fails (the resolver-3 feature-unification lesson). Keep the assertion count exact and the pre-pipe exit status captured (never assert on a piped exit 0 — [[verification-exit-code-through-pipe]]).</action>
  <verify>
    <automated>bash scripts/check-invariants.sh && cargo tree --workspace -i aws-lc-rs; test $? -ne 0 && echo "aws-lc-rs absent OK"</automated>
  </verify>
  <done>The post-transport-dep supply-chain absence re-run confirms zero new forbidden C-crypto crate (aws-lc-rs absent, openssl-sys only via lettre, ring present, reqwest unchanged); Gate 4b is workspace-wide; compose-verify has a feature-OFF guard proving the mock hosts/anchors are absent from release builds; all invariant gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → executor/broker | the acceptance test drives the real decision + confirm + dispatch path (no mocked executor). |
| broker → mock git-receive-pack | the clean confirmed push crosses to the pinned mock endpoint on real Linux. |
| build features → release binary | the feature-OFF guard proves mock hosts/anchors never ship in a release build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-20 | Repudiation | a block-everything I2 regression passes a "not blocked" test | high | mitigate | differential design: the clean leg MUST reach the mock git-receive-pack (recorded receipt) AND the tainted leg MUST Block on the named arg — a blanket block fails the clean leg, a blanket allow fails the tainted leg (RESEARCH §8, Phase-43 M4). |
| T-44-21 | Information disclosure | credential/remote-URL leaks past a real push into value-store/audit/logs | critical | mitigate | the post-push credential-absence assertion greps value store + audit chain + captured broker log output for the token + userinfo-URL material (DESIGN §1.4 MINOR-4, LIVE-06 leg 5). |
| T-44-22 | Tampering | a supply-chain C-crypto crate creeps in via the transport-dep choice | high | mitigate | HYG-01 re-run AFTER transport code lands (aws-lc-rs absent, openssl-sys only via lettre, ring present, reqwest unchanged); Gate 4b workspace-wide; a new dep would fail Gate 5 unless ring-only. |
| T-44-23 | Spoofing | the mock host/anchor ships in a release build (test surface in production) | medium | mitigate | the compose-verify feature-OFF guard proves the mock hosts (incl. git-receive-pack) + mock CA are absent when mock-egress-ca is off. |
| T-44-24 | Elevation | a force/delete push slips through the acceptance proof | high | mitigate | LEG D asserts `--force`/`+`-refspec/`:delete` are refused by construction (validate_git_refspec + command-list Err), unreachable even after a confirm. |
</threat_model>

<verification>
- `cargo test -p brokerd --test s44_git_push_differential --no-fail-fast` green (macOS host: decision-level legs A/B/D-value-refusal).
- On real Linux via `bash scripts/mailpit-verify.sh` with `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test s44_git_push_differential'` (mock-egress-ca sidecar active, git-receive-pack mock reachable): the Linux-gated legs (C mock-endpoint delivery, E redirect refusal, credential-absence) compile + pass; run `cargo build --workspace` FIRST (sibling-binary rule); capture the pre-pipe exit status (never assert on a piped exit 0 — [[verification-exit-code-through-pipe]]).
- `bash scripts/check-invariants.sh` passes (Gate 1/3/5 green, Gate 4b workspace-wide); the HYG-01 `cargo tree` absence re-run recorded in the SUMMARY.
- `bash scripts/compose-verify.sh` passes incl. the feature-OFF guard step.
</verification>

<success_criteria>
GIT-02/GIT-03 are proven for git.push in isolation: taint is the sole Block variable between a Blocked and an Allowed-then-confirmed push; the clean confirmed push is delivered to a pinned mock git-receive-pack on real Linux (net-denied child, IP pinned, redirect refused) with the credential/remote-URL absent from value-store + audit + broker logs; `--force`/`:delete` are refused by construction; I0 draft-deny holds; `verify_chain` true. HYG-01 is complete (post-transport-dep absence re-run, workspace-wide Gate 4b, compose-verify feature-OFF guard). Full composed LIVE-05/06 deferred to Phase 46.
</success_criteria>

<output>
Create `.planning/phases/44-git-push-broker-performed-destination-pinned-egress/44-05-SUMMARY.md` when done.
</output>
