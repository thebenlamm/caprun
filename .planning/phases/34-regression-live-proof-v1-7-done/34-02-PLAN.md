---
phase: 34-regression-live-proof-v1-7-done
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - crates/brokerd/src/confirmation.rs
  - cli/caprun/src/main.rs
  - cli/caprun/tests/s9_process_exec_block.rs
autonomous: true
requirements: [EXEC-05]
must_haves:
  truths:
    - "`caprun confirm` on an I2-Blocked process.exec releases it: the Step-7 dispatch routes to invoke_process_exec_from_resolved, the command runs exactly once, its output is taint-minted via the sanctioned mint_from_exec, and the sink Event is durably chained onto the confirm_granted head (verify_chain true) (D-03/D-04/D-05/D-06)."
    - "The Step-4.75 entry-guard and the Step-7 dispatch both list \"process.exec\" — added in the same diff — so no guard/dispatch drift; any STILL-un-dispatchable sink stays fail-closed-recoverable (row remains Pending) (D-07)."
    - "confirm() is async and threaded through run_confirm_or_deny to its call site in main() (already #[tokio::main]) with no runtime nesting."
  artifacts:
    - "crates/brokerd/src/confirmation.rs :: async confirm() + \"process.exec\" Step-4.75 guard arm + \"process.exec\" Step-7 dispatch arm + inline-annotated mint_from_exec call site"
    - "cli/caprun/src/main.rs :: async run_confirm_or_deny awaited at its call site"
    - "cli/caprun/tests/s9_process_exec_block.rs :: cfg(linux) confirm-release acceptance leg + entry-guard fail-closed leg (D-11)"
  key_links:
    - "confirmation.rs Step-7 \"process.exec\" arm -> sinks::process_exec::invoke_process_exec_from_resolved (.await)"
    - "confirmation.rs Step-7 arm -> quarantine::mint_from_exec (the ONLY confirm-time exec-output mint call, inline-annotated)"
    - "main.rs run_confirm_or_deny -> confirm().await (async thread from #[tokio::main])"
  prohibitions:
    - "No new mint site in the invariant gate's file allow-list — the confirmation.rs mint call is authorized by the inline planner-discipline-allow marker only, so the gate's two-file array stays byte-identical (D-03/D-10)."
    - "No new ExecutorDecision, no submit_plan_node call, no raw effect-request path (D-08/D-09) — confirm() still never re-evaluates via the executor."
    - "No tokio runtime nesting (no Runtime::new().block_on inside confirm) — thread async/.await top-to-bottom instead."
---

<objective>
Wire the EXEC-05 confirm-release path: make `confirmation::confirm()` async, add `"process.exec"` to BOTH the Step-4.75 entry-guard and the Step-7 dispatch (same diff — the guard's own doc comment mandates they stay in sync), dispatch to `invoke_process_exec_from_resolved` (from 34-01), and mint the released output via `mint_from_exec` at an inline-annotated call site in `confirmation.rs`. Thread the async change through `run_confirm_or_deny` to its `#[tokio::main]` call site in `main.rs`. Add the `cfg(linux)` D-11 acceptance test (confirm-release leg + entry-guard fail-closed leg).

Purpose: closes EXEC-05 — a Blocked `process.exec` becomes human-releasable at parity with file.create/file.write/email.send, closing the P33 adversarial-trace open item. This is a mechanical mirror of the shipped `"file.write"` arm plus one genuinely new wrinkle (the sink is async).

Output: async confirm-release for process.exec, its invariant-preserving mint call, the async plumbing, and the Linux acceptance test.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-CONTEXT.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-RESEARCH.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async confirm() + process.exec guard arm + Step-7 dispatch + mint</name>
  <files>crates/brokerd/src/confirmation.rs</files>
  <read_first>
    - crates/brokerd/src/confirmation.rs:713-990 — the full confirm() body: the Step-4.75 entry-guard (~820-834, with the doc comment mandating guard/dispatch sync), the Step-7 dispatch match, and the shipped `"file.write"` arm (~900-914) that is the EXACT mirror for the new async arm.
    - crates/brokerd/src/quarantine.rs:838-870 — `mint_from_exec` signature `(store: &mut ValueStore, session_id, combined_output: String, spawn_event_id: Uuid) -> Result<ValueId>`; provenance_chain == [spawn_event_id], non-stapled.
    - crates/brokerd/src/server.rs:1131 — the existing Allowed-path `mint_from_exec` call (the taint labels + shape to match).
    - scripts/check-invariants.sh:84-136 — Gate 3: `check_mint_token "mint_from_exec(" quarantine.rs server.rs` PLUS the inline `planner-discipline-allow` exemption mechanism (lines ~81, ~122). The two-file allow-list array MUST stay byte-identical (D-03).
    - crates/brokerd/src/sinks/process_exec.rs — `invoke_process_exec_from_resolved` (the 34-01 callee: async, plain &Connection, returns (event_id, hash, combined_output)).
  </read_first>
  <action>
Make `confirm()` async and wire the process.exec release:
  - Change `pub fn confirm(...)` to `pub async fn confirm(...)` (same params: `conn: &mut rusqlite::Connection, key, effect_id, workspace_root`). The three existing sink arms (file.create/email.send/file.write) are sync internally and need NO `.await` on their own calls — only the new process.exec arm awaits.
  - Step-4.75 entry-guard: add `"process.exec"` to the fail-closed allow-list match so it reads `"file.create" | "email.send" | "file.write" | "process.exec" => {}`. Leave the `other =>` refusal (row stays Pending) intact — this is the fail-closed-recoverable behavior that MUST NOT regress OPEN (D-07). Edit this in the SAME diff as the Step-7 arm below (drift = a silently-burned one-shot confirmation, the exact P33 MAJOR-1 class).
  - Step-7 dispatch: add a `"process.exec" =>` arm mirroring the `"file.write"` arm's shape, but async + mint. It: `.await`s `crate::sinks::process_exec::invoke_process_exec_from_resolved(conn, key, pc.session_id, pc.effect_id, &pc.resolved_args, workspace_root, granted_event_id, &granted_hash)`. On `Ok((sink_event_id, _hash, combined_output))`: mint the released output by calling `crate::quarantine::mint_from_exec` with a local throwaway `ValueStore`, `pc.session_id`, `combined_output`, and `sink_event_id` (the REAL process_exited id just appended — genuinely-rooted, non-stapled). Map mint `Ok(_)` -> `ConfirmOutcome::Released`, mint `Err(_)` -> `ConfirmOutcome::ConfirmedButSinkFailed`. On the outer `Err(_)` (spawn failed) -> `ConfirmOutcome::ConfirmedButSinkFailed` (D-06/Pitfall 5: the process may have genuinely run and is durably audited either way).
  - The mint call MUST live here in confirmation.rs, NOT in sinks/process_exec.rs (that module never mints) and NOT in server.rs (not on this path). Because Gate 3's file allow-list is `quarantine.rs` + `server.rs` only, authorize this call site with the inline exemption marker on its own line immediately above the call: `// planner-discipline-allow: mint_from_exec` (the marker check-invariants.sh Gate 3 already honors). Do NOT add confirmation.rs to the gate's two-file array — D-03 requires that array stay byte-identical.
  - The throwaway `ValueStore` is deliberate: there is no live worker at confirm time to receive `output_value_id` (the Allow-path worker already discards it). What EXEC-05 requires is that `mint_from_exec` RUNS (producing a genuinely-rooted ValueId), not that the id is persisted beyond the audit Event chain.
  - Do NOT call `submit_plan_node`, add an `ExecutorDecision`, or introduce a raw effect-request path — confirm() still never re-evaluates through the executor (D-08/D-09).
  </action>
  <verify>
    <automated>cargo build -p brokerd 2>&1 | tail -5 ; ./scripts/check-invariants.sh 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'pub async fn confirm' crates/brokerd/src/confirmation.rs` returns one hit; the Step-4.75 guard line contains `"process.exec"`; a `"process.exec" =>` Step-7 arm exists that awaits `invoke_process_exec_from_resolved`.
    - SOURCE: exactly one `mint_from_exec(` call exists in confirmation.rs and it is preceded by a `planner-discipline-allow: mint_from_exec` marker line; `git diff scripts/check-invariants.sh` is EMPTY (allow-list array byte-identical, D-03).
    - BEHAVIOR: `./scripts/check-invariants.sh` exits 0 — Gate 1 (no raw effect-request path) and Gate 3 (mint-site restriction) both green.
    - TEST: `cargo build -p brokerd` compiles clean.
  </acceptance_criteria>
  <done>confirm() is async, process.exec is dispatchable (guard + Step-7 in one diff), the released output is minted via the sanctioned helper at an inline-annotated site, and both invariant gates stay green with an unchanged allow-list array.</done>
</task>

<task type="auto">
  <name>Task 2: Thread async through run_confirm_or_deny to its main() call site</name>
  <files>cli/caprun/src/main.rs</files>
  <read_first>
    - cli/caprun/src/main.rs:417-500 — `run_confirm_or_deny` (currently sync) and its `ConfirmOutcome` match; the `#[tokio::main] async fn main()` and the single inline call to `run_confirm_or_deny`.
    - 34-01-SUMMARY.md and confirmation.rs — confirm() is now `async` (Task 1), so its callers must await.
  </read_first>
  <action>
Make `run_confirm_or_deny` an `async fn` and `.await` the `confirm(...)` call inside it. `deny(...)` stays sync internally — call it without `.await` (it is fine to call a sync fn from an async fn body); do not force deny async. Update the single call site of `run_confirm_or_deny` in `main()` to `.await` it — `main` is already `#[tokio::main] async fn`, so no new runtime, no `block_on`, no `tokio::main` addition is needed (avoids the runtime-nesting footgun; RESEARCH "Alternatives Considered"). The `ConfirmOutcome` -> exit-code mapping is unchanged. This is pure plumbing — touch no security-posture token (`ExecutorDecision` / `submit_plan_node` / raw effect-request path all untouched).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'async fn run_confirm_or_deny' cli/caprun/src/main.rs` returns one hit; the confirm call is `.await`ed; the call site in main is `.await`ed.
    - SOURCE: no `Runtime::new()` / `block_on` was added to main.rs (`grep -c 'block_on\|Runtime::new' cli/caprun/src/main.rs` counts only pre-existing occurrences, if any).
    - TEST: `cargo build --workspace` compiles clean (the whole workspace links with the now-async confirm chain).
  </acceptance_criteria>
  <done>`run_confirm_or_deny` is async and awaited at its call site; the workspace builds; no runtime nesting introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cfg(linux) EXEC-05 acceptance test — confirm-release + entry-guard fail-closed legs (D-11)</name>
  <files>cli/caprun/tests/s9_process_exec_block.rs</files>
  <behavior>
    - Confirm-release leg: an I2-Blocked process.exec (tainted exec-output routed into a second exec's `command` arg — a plain taint Block, NOT SlotTypeMismatch; Pitfall 4) is released by a REAL `caprun confirm` subprocess; the command runs EXACTLY ONCE (asserted via a marker/side-effect); the output is taint-minted; the sink Event is durably chained onto the confirm_granted head; `verify_chain` is true. A second `caprun confirm` on the same effect_id returns the already-terminal exit code (no double-spawn, D-06).
    - Entry-guard fail-closed leg: a still-un-dispatchable sink name is refused by the Step-4.75 guard with the row left Pending (the P33 guard mechanism still fires for FUTURE sinks — process.exec itself is now dispatchable, so use a different unwired sink name; Pitfall 1/D-07).
  </behavior>
  <read_first>
    - cli/caprun/tests/s9_process_exec_block.rs:47+ — the `#[cfg(target_os = "linux")] mod linux` scaffolding, `TEST_KEY`, `mint_trusted`/`fresh_workspace`/`seed_root_event` fixtures, and the EXISTING exec-block setup (reuse it verbatim; the Block is already proven — only add the release leg after it).
    - cli/caprun/tests/confirm.rs:64-293 — `seed_pending_*_block` + `run_caprun_verb("confirm", ...)` cross-process harness + the `..._releases_once_and_second_confirm_is_already_terminal` shape (mirror it; note the exec case needs a REAL process_exited event, so this harness runs inside the `#[cfg(target_os = "linux")]` module).
    - crates/brokerd/src/confirmation.rs — the Step-4.75 guard (for the fail-closed leg's expected refusal + Pending row).
  </read_first>
  <action>
Extend `s9_process_exec_block.rs`'s existing `#[cfg(target_os = "linux")] mod linux` (per RESEARCH open-Q1 recommendation — it already has every fixture helper; avoid a duplicate file):
  - Add a confirm-release test: reuse the existing exec-block setup to produce the `BlockedPendingConfirmation`, then spawn a REAL `caprun confirm <effect_id>` subprocess (via the `confirm.rs` `run_caprun_verb` pattern). Assert exit 0 (`Released`); assert the command ran exactly once (a marker file / captured side-effect); assert a durable exec sink Event exists chained on the `confirm_granted` head and that its minted output ValueId's provenance is genuinely rooted at that sink Event (non-stapled); assert `verify_chain` true for the session. Then spawn a SECOND `caprun confirm <effect_id>` and assert the already-terminal exit code (5) — proving no double-spawn (D-06).
  - Add an entry-guard fail-closed leg: seed a pending confirmation whose sink is a still-UN-dispatchable name (not process.exec — process.exec is now wired), call confirm, assert it is refused BEFORE any state transition and the row remains Pending (D-07). This proves the guard mechanism itself did not regress OPEN.
  Gate the whole addition under `#[cfg(target_os = "linux")]` and use `#[tokio::test]` where the path is async — the confined spawn is a no-op stub on macOS, so a Mac run proves nothing (Pitfall 3 / cfg-linux-test-blindness).
  </action>
  <verify>
    <automated>MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_process_exec_block' bash scripts/mailpit-verify.sh > /tmp/34-02-t3.log 2>&1 ; rc=$? ; echo "true_exit=$rc" ; tail -n 40 /tmp/34-02-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the new tests are inside a `#[cfg(target_os = "linux")]` block; the confirm-release test spawns a real `caprun confirm` subprocess (not an in-process confirm() call) and asserts exactly-once + already-terminal-on-second-confirm.
    - BEHAVIOR: the release leg asserts `verify_chain` true and a non-stapled minted output (provenance == [real sink Event id]); the guard leg asserts a Pending row after a refused unwired-sink confirm.
    - TEST: capture the TRUE exit code before any pipe (`rc=$?` before `tail`); `true_exit=0` and the named test(s) pass on real Linux. Never assert on exit 0 through a pipe.
  </acceptance_criteria>
  <done>The cfg(linux) confirm-release + entry-guard fail-closed legs pass on real Linux with true-exit-before-pipe; the release runs exactly once with a genuine, `verify_chain`-true audit chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `caprun confirm` process → broker confirm() | A separate later OS process re-enters confirm() to release a frozen sink; it must NOT be able to double-spawn or bypass I2. |
| released exec output → downstream sinks | The minted output is untrusted and must carry genuine (non-stapled) taint so a later routing to a sensitive arg still Blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02-R | Repudiation/Tampering | confirm-release double-spawn | high | mitigate | `transition_state`'s SQL `WHERE state='pending'` guard makes release exactly-once; the second confirm returns already-terminal (D-06, asserted in Task 3). |
| T-34-02-D | DoS (self-inflicted)/audit gap | Step-4.75 guard vs Step-7 drift | critical | mitigate | Both lists edited in one diff; entry-guard fail-closed leg asserts the mechanism still refuses future unwired sinks (Pitfall 1, the exact P33 MAJOR-1 class). |
| T-34-02-E | Elevation of Privilege | confirm-release bypassing I2 | critical | mitigate | confirm() never calls `submit_plan_node`/adds an `ExecutorDecision`; release re-invokes only the frozen resolved_args; I2 stays table-entries-only (D-08). |
| T-34-02-T | Tampering | stapled taint on released mint | high | mitigate | `mint_from_exec` provenance == [the real process_exited Event id just appended], never a fabricated root (D-03). |
| T-34-02-SC | Tampering | crates dependencies | high | accept | Zero new packages this phase. |
</threat_model>

<verification>
- `cargo build --workspace` compiles clean on the macOS host.
- `./scripts/check-invariants.sh` exits 0: Gate 1 (no raw effect-request path), Gate 3 (mint-site restriction) — and `git diff scripts/check-invariants.sh` is empty (allow-list array byte-identical, D-03).
- On real Linux via `mailpit-verify.sh` (scoped to `s9_process_exec_block`): confirm-release + entry-guard legs pass, true-exit-0 before any pipe.
</verification>

<success_criteria>
- A Blocked `process.exec` is human-releasable via `caprun confirm`: runs exactly once, output taint-minted (non-stapled), sink Event chained on the confirm_granted head, `verify_chain` true (EXEC-05).
- Step-4.75 guard and Step-7 dispatch both list `"process.exec"`; the guard still fail-closes recoverably for un-dispatchable sinks (D-07).
- confirm() is async and threaded to `main()` with no runtime nesting; both invariant gates green with an unchanged allow-list array.
</success_criteria>

<output>
Create `.planning/phases/34-regression-live-proof-v1-7-done/34-02-SUMMARY.md` when done.
</output>
