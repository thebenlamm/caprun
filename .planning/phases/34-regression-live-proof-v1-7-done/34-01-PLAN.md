---
phase: 34-regression-live-proof-v1-7-done
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/src/sinks/process_exec.rs
autonomous: true
requirements: [EXEC-05]
must_haves:
  truths:
    - "A confirm-time sibling of the Allowed-path exec sink re-runs a released process.exec from the frozen resolved args, spawning the same broker-spawned confined child (Landlock + seccomp + net-deny + rlimits + wall-clock timeout + byte cap) and capturing stdout/stderr."
    - "On success it appends a process_exited audit Event chained onto the passed parent (the confirm_granted head), and returns the captured combined output for the caller to mint (D-04)."
    - "On spawn failure it appends process_spawn_failed chained onto the same parent and propagates (no retry, no double-spawn)."
  artifacts:
    - "crates/brokerd/src/sinks/process_exec.rs :: invoke_process_exec_from_resolved (async, plain &rusqlite::Connection)"
    - "crates/brokerd/src/sinks/process_exec.rs :: #[cfg(target_os = \"linux\")] unit tests for the new function"
  key_links:
    - "invoke_process_exec_from_resolved -> run_launcher (the existing conn-free async spawn helper, reused unmodified)"
    - "process_exited/process_spawn_failed Event -> parent_id/parent_hash (the confirm_granted chain head)"
  prohibitions:
    - "The module still NEVER mints (no exec-output mint call added here) — the sole mint call site stays in confirmation.rs's Step-7 arm (D-10)."
    - "No new ExecutorDecision, no submit_plan_node call, no raw effect-request path (D-08/D-09)."
    - "No Arc<Mutex<Connection>> in the new signature — plain &rusqlite::Connection, matching the file_write twin (Pitfall 2)."
---

<objective>
Add `invoke_process_exec_from_resolved` — the confirm-time release twin of the Allowed-path `invoke_process_exec` — to `crates/brokerd/src/sinks/process_exec.rs`. It re-runs a human-released `process.exec` from the frozen `PendingConfirmation.resolved_args`, mirroring the *structure* of the already-shipped `invoke_file_write_from_resolved` (two-phase durable audit, `_from_resolved` sibling shape) while carrying the *async body and spawn discipline* of `invoke_process_exec`.

Purpose: EXEC-05 (the P33 adversarial-trace open item) needs a function `confirmation.rs` can dispatch to on `caprun confirm`. This plan produces that function and its Linux unit coverage; plan 34-02 wires it in. Splitting the sink function from the confirmation wiring keeps the TCB change within context budget and gives the guard+dispatch coupling (34-02) a stable callee.

Output: `invoke_process_exec_from_resolved` (async, plain `&rusqlite::Connection`, returns `(process_exited event_id, hash, combined_output)`) plus its `#[cfg(target_os = "linux")]` unit tests.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-CONTEXT.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-RESEARCH.md
@CLAUDE.md
@planning-docs/DESIGN-effect-breadth-exec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add invoke_process_exec_from_resolved (async release twin)</name>
  <files>crates/brokerd/src/sinks/process_exec.rs</files>
  <read_first>
    - crates/brokerd/src/sinks/file_write.rs:160-237 — `invoke_file_write_from_resolved`: the EXACT structural mirror (two-phase durable audit, `_from_resolved` sibling shape, plain `&rusqlite::Connection`, `resolved_literal` helper, sink_invocation_failed-first-on-error).
    - crates/brokerd/src/sinks/file_create.rs — `invoke_file_create_from_resolved` (the second twin, for the resolved-arg lookup + optional-arg pattern).
    - crates/brokerd/src/sinks/process_exec.rs:111-205 — `invoke_process_exec` (the Allowed-path body to carry over: arg resolution incl. optional `args` JSON `Vec<String>` and optional `cwd`, the `run_launcher(...).await` call, the two-phase `process_exited`/`process_spawn_failed` append).
    - crates/brokerd/src/sinks/process_exec.rs:207+ — `run_launcher` (the conn-free async spawn helper — call it unmodified).
  </read_first>
  <action>
Add `pub async fn invoke_process_exec_from_resolved(conn: &rusqlite::Connection, key: &[u8], session_id: Uuid, effect_id: Uuid, resolved_args: &[ResolvedArg], workspace_root: &WorkspaceRoot, parent_id: Uuid, parent_hash: &str) -> Result<(Uuid, String, String)>`. Follow `invoke_file_write_from_resolved`'s structure exactly, swapping the sink body for the exec spawn/capture path from `invoke_process_exec`:
  - Resolve `command` (required) via the module's private `resolved_literal` helper (mirror `file_write.rs`'s private copy; do not build a shared abstraction). Resolve `args` (optional) by reading its JSON literal and decoding to `Vec<String>` with `serde_json::from_str` exactly as `invoke_process_exec` does, and `cwd` (optional) the same way — reading from `resolved_args` instead of a live `ValueStore`.
  - Call the SAME conn-free `run_launcher(...).await` helper `invoke_process_exec` uses (it takes no `conn`, so it is directly reusable). Keep the identical confined-child discipline: launcher self-confines (Landlock + seccomp + default-deny net + rlimits), wall-clock timeout, byte cap on captured stdout/stderr.
  - On `Ok((_exit_status, combined_output))`: append a `process_exited` audit Event chained onto `parent_id` / `parent_hash` (these ARE the `confirm_granted` head passed by 34-02, per D-04 — do NOT start a fresh root). Return `(process_exited_event_id, hash, combined_output)`.
  - On `Err(e)`: append `process_spawn_failed` chained onto `parent_id` / `parent_hash` FIRST, then propagate the error (no retry — matches the file_write twin's `sink_invocation_failed`-first discipline). This is the exactly-once contract's failure leg (D-06).
Signature discipline: use a plain `&rusqlite::Connection` (NOT `&Arc<Mutex<rusqlite::Connection>>`). The Allowed-path uses `Arc<Mutex<>>` only because the broker serves concurrent connections; confirm-time is single-shot with no concurrency (Pitfall 2). Do NOT add any exec-output mint call in this module — it deliberately never mints (that stays in 34-02's confirmation.rs arm, keeping the invariant-gate mint-site list satisfied for this file per D-10). Do NOT call `submit_plan_node`, introduce any `ExecutorDecision`, or add a raw effect-request path (D-08/D-09).
  </action>
  <verify>
    <automated>cargo build -p brokerd 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'pub async fn invoke_process_exec_from_resolved' crates/brokerd/src/sinks/process_exec.rs` returns exactly one hit with a plain `&rusqlite::Connection` first param (not `Arc<Mutex<`).
    - SOURCE: the function body contains a `run_launcher` call and appends `process_exited` on success and `process_spawn_failed` on error, both chained on the `parent_id`/`parent_hash` params.
    - BEHAVIOR: `grep -c 'mint_from_exec' crates/brokerd/src/sinks/process_exec.rs` (excluding comment lines: `grep -v '^\s*//' ... | grep -c` ) is 0 — the module still never mints.
    - TEST: `cargo build -p brokerd` compiles clean (macOS host compiles the fn; the confined-spawn behavior is exercised by the Linux unit tests in Task 2).
  </acceptance_criteria>
  <done>`invoke_process_exec_from_resolved` exists, compiles, reuses `run_launcher`, appends the two-phase audit on the passed parent head, and adds no mint / no submit_plan_node / no raw effect-request path.</done>
</task>

<task type="auto">
  <name>Task 2: Linux unit tests for invoke_process_exec_from_resolved</name>
  <files>crates/brokerd/src/sinks/process_exec.rs</files>
  <read_first>
    - crates/brokerd/src/sinks/file_write.rs:377-490 — the `invoke_file_write_from_resolved` unit tests (`..._success_records_sink_executed`, `..._failure_records_sink_invocation_failed`): the exact test shape to mirror (open audit db, seed a root/parent event, build the resolved-arg set, call the fn, assert the durable Event was appended and chained).
    - crates/brokerd/src/sinks/process_exec.rs — existing `#[cfg(test)]`/`#[cfg(target_os = "linux")]` test scaffolding + launcher-path resolution used by `invoke_process_exec`'s own tests (reuse the same fixture helpers).
  </read_first>
  <action>
Add a `#[cfg(target_os = "linux")]` test module mirroring `file_write.rs`'s `_from_resolved` unit tests, but Linux-gated because `invoke_process_exec_from_resolved` genuinely spawns `caprun-exec-launcher` (whose confinement primitives are macOS no-op stubs — a Mac run proves nothing about the spawn path; Pitfall 3):
  - `..._success_appends_process_exited_chained_on_parent`: seed a parent event (stand in for `confirm_granted`), call the fn with a trivial `command` (e.g. an echo-equivalent the launcher can exec), assert `Ok(...)` and that the returned `process_exited` Event's parent id/hash equal the passed `parent_id`/`parent_hash`, and that `combined_output` is the captured stdout.
  - `..._spawn_failure_appends_process_spawn_failed`: force a spawn failure (e.g. a non-existent command), assert a `process_spawn_failed` Event was durably appended chained on the parent, and the fn returns `Err`.
Use `#[tokio::test]` for the async calls. Reuse the module's existing launcher-path + audit-db fixtures; do not build new harness abstractions.
  </action>
  <verify>
    <automated>MAILPIT_VERIFY_CMD='cargo test -p brokerd invoke_process_exec_from_resolved' bash scripts/mailpit-verify.sh > /tmp/34-01-t2.log 2>&1 ; rc=$? ; echo "true_exit=$rc" ; tail -n 30 /tmp/34-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'cfg(target_os = "linux")' crates/brokerd/src/sinks/process_exec.rs` shows the new test module is Linux-gated.
    - BEHAVIOR: the success test asserts the returned `process_exited` Event chains on the passed `parent_id`/`parent_hash`; the failure test asserts a durable `process_spawn_failed` Event and an `Err` return.
    - TEST: capture the TRUE exit code before any pipe (`rc=$?` immediately after the run, before `tail`); the run reports `true_exit=0` and both named tests pass on real Linux. macOS `cargo test -p brokerd` shows these as 0 run — expected (cfg-linux gate), never treated as a pass.
  </acceptance_criteria>
  <done>Two `#[cfg(target_os = "linux")]` `#[tokio::test]` cases exist and pass on real Linux via `mailpit-verify.sh` with true-exit-0 captured before the pipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm-time CLI → broker sink | A later, separate `caprun confirm` OS process re-enters the broker to re-invoke a released sink from frozen state; the child it spawns is untrusted. |
| exec child → captured output | The confined child's stdout/stderr are untrusted bytes that MUST be taint-minted (minting happens in 34-02, not here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01-DS | Denial of Service | invoke_process_exec_from_resolved | high | mitigate | Reuse `run_launcher`'s wall-clock timeout + byte cap unchanged; no unbounded capture. |
| T-34-01-T | Tampering | process_exited/process_spawn_failed audit | high | mitigate | Chain both Events on the passed `parent_id`/`parent_hash` (the real `confirm_granted` head), never a fabricated root — anti-stapling discipline (D-04). |
| T-34-01-E | Elevation of Privilege | new sink function | high | mitigate | Plain `&Connection`, no `submit_plan_node`, no `ExecutorDecision`, no mint here — function only spawns + audits; I2 stays table-entries-only (D-08/D-10). |
| T-34-01-SC | Tampering | crates dependencies | high | accept | Zero new packages this phase (RESEARCH confirmed) — no install, no legitimacy gate needed. |
</threat_model>

<verification>
- `cargo build -p brokerd` compiles clean on the macOS host.
- `./scripts/check-invariants.sh` stays green (Gate 1 no raw effect-request path; Gate 3 mint-site list unchanged — this file adds no mint).
- On real Linux via `mailpit-verify.sh`: the two new unit tests pass, true-exit-0 captured before any pipe.
</verification>

<success_criteria>
- `invoke_process_exec_from_resolved` exists as an `async fn` taking a plain `&rusqlite::Connection`, reusing `run_launcher`, appending the two-phase audit on the passed parent head, returning `(event_id, hash, combined_output)`.
- The module still never mints; no new `ExecutorDecision` / `submit_plan_node` / raw effect-request path.
- Linux unit tests green with true-exit-before-pipe discipline.
</success_criteria>

<output>
Create `.planning/phases/34-regression-live-proof-v1-7-done/34-01-SUMMARY.md` when done.
</output>