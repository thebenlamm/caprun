---
phase: 34-regression-live-proof-v1-7-done
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified: []
autonomous: false
requirements: [EXEC-05]
must_haves:
  truths:
    - "After the EXEC-05 TCB slice lands (34-01 + 34-02) and BEFORE the composed live proof, a Linux compile-check proves every cfg(target_os=\"linux\") target compiles (guards cfg-linux test-blindness) with true-exit-0 captured before any pipe (D-15)."
    - "A fresh, non-self Fable-5 adversarial code-trace of the confirm-release TCB diff returns APPROVED or has all findings resolved (D-16) — this is a standing project guardrail that has caught real MAJORs a passing verifier + green gates missed 7x."
    - "Both gates BLOCK authorization of LIVE-01 — the live proof (34-04) does not start until both pass."
  artifacts:
    - "A recorded Linux compile-check result (true-exit-0, captured before pipe)"
    - "A recorded Fable-5 adversarial-trace verdict (APPROVED or findings-resolved) for the confirm-release diff"
  key_links:
    - "34-02 confirm-release diff -> Fable-5 fresh-context reviewer (orchestrator-owned spawn)"
    - "both gates -> 34-04 depends_on (LIVE-01 authorization)"
  prohibitions:
    - "These gates are ORCHESTRATOR-OWNED — a gsd-executor must NOT self-adjudicate the adversarial trace nor mark the milestone. The orchestrator (or a human) runs them."
    - "No source changes in this plan (files_modified empty) — it is a verification/gate boundary only."
---

<objective>
The MANDATORY release gates between the EXEC-05 TCB slice and the composed live proof. Both are ORCHESTRATOR-OWNED (a human/orchestrator runs them, NOT a gsd-executor) and both BLOCK LIVE-01: (a) a Linux compile-check that every `cfg(target_os = "linux")` target builds (D-15, guarding the project's standing cfg-linux test-blindness gotcha), and (b) a fresh non-self Fable-5 adversarial code-trace of the confirm-release TCB diff (D-16, the standing guardrail that has repeatedly caught MAJORs green gates missed).

Purpose: the composed live proof (34-04) must not run against a diff that only compiled on macOS or that has not survived an independent fresh-context adversarial read of the TCB change.

Output: a recorded compile-check result and a recorded adversarial-trace verdict; on both green, LIVE-01 is authorized.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-CONTEXT.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-01-SUMMARY.md
@.planning/phases/34-regression-live-proof-v1-7-done/34-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate A: Linux compile-check of all cfg(linux) targets (D-15, ORCHESTRATOR-OWNED)</name>
  <files>(none — verification gate; no source changes)</files>
  <what-built>The EXEC-05 confirm-release TCB slice (34-01 + 34-02): `invoke_process_exec_from_resolved`, the async confirm() with the process.exec guard + Step-7 arm + mint, the async main.rs plumbing, and the cfg(linux) acceptance test.</what-built>
  <action>
ORCHESTRATOR-OWNED (a human/orchestrator runs this; a gsd-executor does NOT self-adjudicate it). Run the Linux compile-check that forces compilation of every `#[cfg(target_os = "linux")]` target — a green macOS build compiles ZERO such targets (the project's standing cfg-linux blindness gotcha). Use `MAILPIT_VERIFY_CMD='cargo build --tests --workspace --keep-going'` through `scripts/mailpit-verify.sh`, redirected to a log, capturing the TRUE exit code before any pipe (`set +e ; ... ; rc=$? ; set -e`). Confirm `true_exit=0` and that the tail of the log shows a clean workspace `--tests` build in the rust:1 Colima container. Record the true exit code + tail excerpt in the plan SUMMARY.
  </action>
  <verify>
    <automated>set +e ; MAILPIT_VERIFY_CMD='cargo build --tests --workspace --keep-going' bash scripts/mailpit-verify.sh > /tmp/34-03-compile.log 2>&1 ; rc=$? ; set -e ; echo "true_exit=$rc" ; tail -n 40 /tmp/34-03-compile.log</automated>
  </verify>
  <acceptance_criteria>
    - TEST: `true_exit=0` captured before any pipe; the log shows `cargo build --tests --workspace --keep-going` succeeded in the rust:1 container.
    - SOURCE: the recorded result is in the SUMMARY (not just chat), with the true exit code stated explicitly.
  </acceptance_criteria>
  <done>Linux compile-check is true-exit-0 (captured before any pipe); all cfg(linux) targets compile; result recorded in SUMMARY.</done>
  <resume-signal>Type "approved" once the Linux compile-check is true-exit-0, or describe the build failures to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate B: Fresh non-self Fable-5 adversarial code-trace of the confirm-release diff (D-16, ORCHESTRATOR-OWNED)</name>
  <files>(none — verification gate; no source changes)</files>
  <what-built>The confirm-release TCB diff from 34-01 + 34-02 (the `git diff` of `crates/brokerd/src/sinks/process_exec.rs`, `crates/brokerd/src/confirmation.rs`, `cli/caprun/src/main.rs`, and the new `s9_process_exec_block.rs` legs).</what-built>
  <action>
ORCHESTRATOR-OWNED — the orchestrator (NOT a gsd-executor) spawns a FRESH-context Fable-5 agent (or the advisor/Fable-5 fallback) and hands it ONLY the confirm-release diff (`git diff` of the four files above) — no chat history, no self-justification. Ask it to code-trace for: guard/dispatch drift (a burned one-shot confirmation with no effect — the exact P33 MAJOR-1 class), double-spawn, any I2 bypass (`submit_plan_node` / `ExecutorDecision` / raw effect-request re-entry), stapled taint on the released mint, tokio runtime-nesting, and the fail-closed-recoverable guard for un-dispatchable sinks. Resolve every finding (or obtain an explicit APPROVED). Record the verdict + finding dispositions in the plan SUMMARY. This standing guardrail has caught a real MAJOR the passing verifier + green gates missed 7x on this project; it BLOCKS LIVE-01.
  </action>
  <verify>
    <human-check>Fresh non-self Fable-5 adversarial code-trace of the confirm-release diff returns APPROVED, or every finding is resolved and recorded in the SUMMARY, before LIVE-01 is authorized.</human-check>
  </verify>
  <acceptance_criteria>
    - SOURCE: a recorded Fable-5 verdict (APPROVED, or each finding with its resolution) in the SUMMARY, attributable to a fresh non-self context.
    - BEHAVIOR: no unresolved MAJOR/CHANGES-REQUIRED finding remains open when LIVE-01 is authorized.
  </acceptance_criteria>
  <done>Fresh non-self Fable-5 adversarial code-trace is APPROVED or all findings resolved; verdict recorded in SUMMARY; LIVE-01 authorized only after.</done>
  <resume-signal>Type "approved" once the Fable-5 trace is APPROVED / all findings resolved, or list the findings to address in a follow-up before LIVE-01.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS build signal → real Linux behavior | A green macOS build is NOT evidence any Linux-gated target compiles or passes — the compile-check crosses this boundary. |
| self-review → independent review | The implementing context is not a trustworthy reviewer of its own TCB diff — a fresh non-self trace crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-03-I | Information Disclosure/false-assurance | cfg-linux test-blindness | high | mitigate | Force-compile all `--tests` targets on real Linux with true-exit-0 before any pipe (D-15). |
| T-34-03-T | Tampering/undetected-regression | TCB confirm-release diff | critical | mitigate | Fresh non-self Fable-5 adversarial code-trace blocks LIVE-01 until APPROVED/resolved (D-16). |
</threat_model>

<verification>
- Gate A: `cargo build --tests --workspace --keep-going` via `mailpit-verify.sh` is true-exit-0 (captured before any pipe); result recorded in SUMMARY.
- Gate B: fresh non-self Fable-5 adversarial trace of the confirm-release diff is APPROVED or all findings resolved; verdict recorded in SUMMARY.
- Both must be green before 34-04 (LIVE-01) is authorized.
</verification>

<success_criteria>
- Linux compile-check green (true-exit-0, before-pipe) — all cfg(linux) targets compile.
- Fresh non-self adversarial trace APPROVED / findings resolved.
- LIVE-01 authorized only after both gates pass (D-15/D-16).
</success_criteria>

<output>
Create `.planning/phases/34-regression-live-proof-v1-7-done/34-03-SUMMARY.md` when done, recording both gate results.
</output>
