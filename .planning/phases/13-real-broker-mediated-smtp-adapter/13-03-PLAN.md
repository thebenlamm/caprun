---
phase: 13-real-broker-mediated-smtp-adapter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/sandbox/src/bin/confine-probe.rs
  - crates/sandbox/tests/confinement_integration.rs
  - scripts/check-smtp-secrets-absent.sh
autonomous: true
requirements: [SMTP-01, SMTP-02]
must_haves:
  truths:
    - "A confined worker's direct attempt to open an SMTP connection to the Mailpit host:port FAILS under kernel-enforced default-deny net — proven on real Linux by a negative assertion, not by code inspection."
    - "SMTP endpoint config tokens (CAPRUN_SMTP_*) are absent from every worker-spawn code path — the worker never receives host/port/credentials in env, args, or any plan-node payload."
  artifacts:
    - "confine-probe smtp <host> <port> op that attempts socket()+connect() under confinement (Pitfall 5 option b)"
    - "Linux-only negative-net test asserting the confined connect() to Mailpit is kernel-denied"
    - "scripts/check-smtp-secrets-absent.sh — grep gate proving CAPRUN_SMTP_ tokens absent from worker-spawn paths"
  key_links:
    - "seccomp-bpf apply_worker_filter denies socket(AF_INET/AF_INET6) with EPERM BEFORE connect() — the existing kernel mechanism the new op exercises (SMTP-01)"
    - "worker-spawn code paths never reference CAPRUN_SMTP_ tokens (SMTP-02)"
---

<objective>
Prove SMTP-01 and SMTP-02 without touching the adapter. SMTP-01: add a host:port-aware `confine-probe smtp <host> <port>` op (Pitfall 5 option b — explicit decision) and a Linux-only negative-net test asserting a confined worker's attempt to `connect()` to the Mailpit host:port is kernel-denied by the EXISTING seccomp filter (`apply_worker_filter`) — a negative assertion, not code inspection (D-05). SMTP-02: a grep gate proving the `CAPRUN_SMTP_*` endpoint tokens never appear in any worker-spawn code path.

Purpose: Independently verify the confinement + secrets-custody claims; this plan is parallel to the adapter work (different crates, no file overlap).
Output: New confine-probe op, Linux-only negative test, and a structural secrets-absence gate.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-CONTEXT.md
@.planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md
@crates/sandbox/src/bin/confine-probe.rs
@crates/sandbox/tests/confinement_integration.rs
@crates/sandbox/src/seccomp.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host:port-aware confine-probe smtp op (Pitfall 5 option b)</name>
  <files>crates/sandbox/src/bin/confine-probe.rs</files>
  <read_first>
    - crates/sandbox/src/bin/confine-probe.rs (main + run_linux dispatch lines 29-66; probe_net at 133; note run_linux currently takes only args[1] as op)
    - crates/sandbox/src/seccomp.rs (apply_worker_filter — the socket(AF_INET/AF_INET6) EPERM denial)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Pitfall 5 — option b rationale; the socket()-fails-before-connect() reality)
  </read_first>
  <action>
    Extend confine-probe to accept a new `smtp <host> <port>` op. Widen the arg handling in `main`/`run_linux` so that when op is `smtp`, the host (args[2]) and port (args[3]) are read; keep the existing `fs`/`net`/`exec` ops unchanged (they take no extra args). Add a `#[cfg(target_os = "linux")]` `probe_smtp(host: &str, port: &str)` that, AFTER `apply_confinement()`, attempts to create a TCP socket and `connect()` to `host:port` (e.g. via `std::net::TcpStream::connect`). Expectation: the underlying `socket(AF_INET, ...)` syscall is denied with EPERM by the existing seccomp filter BEFORE `connect()` can proceed, so the attempt fails; the probe must exit non-zero (denial observed) and print a diagnostic. If the connect UNEXPECTEDLY succeeds, exit with a distinct failure code signalling the confinement regressed.

    This is the explicit Pitfall 5 option (b) choice: a real connect() attempt to the Mailpit endpoint, more faithful to "attempts an actual connect() to the Mailpit host:port" than reusing destination-independent probe_net. Do NOT remove or alter probe_net.
  </action>
  <verify>
    <automated>cargo build -p sandbox --bin confine-probe 2>&1 | grep -qi error && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p sandbox --bin confine-probe` compiles on macOS and Linux (the probe_smtp body is `#[cfg(target_os = "linux")]`, with a non-Linux stub matching the existing convention).
    - `confine-probe smtp <host> <port>` is a recognized op in run_linux's dispatch; unknown-op handling for other strings is unchanged.
    - `grep -n 'fn probe_smtp' crates/sandbox/src/bin/confine-probe.rs` returns a match; `grep -n 'fn probe_net' ...` still returns the original (unremoved).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linux-only negative-net test — confined connect() to Mailpit is kernel-denied</name>
  <files>crates/sandbox/tests/confinement_integration.rs</files>
  <read_first>
    - crates/sandbox/tests/confinement_integration.rs (the existing negative_net test pattern — how it spawns confine-probe and asserts the exit/denial)
    - crates/sandbox/src/bin/confine-probe.rs (the new probe_smtp op from Task 1)
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Validation Architecture SMTP-01 row; Linux-only cfg-gating convention)
  </read_first>
  <behavior>
    - Under confinement, spawning `confine-probe smtp <mailpit-host> <port>` exits non-zero (kernel-denied) — the confined process cannot open the SMTP socket.
  </behavior>
  <action>
    Add a `#[cfg(target_os = "linux")]` integration test mirroring the existing `negative_net` test's structure: spawn the `confine-probe` binary with the new `smtp` op pointed at the Mailpit host:port (read from `CAPRUN_SMTP_HOST`/`CAPRUN_SMTP_PORT` env, defaulting to `127.0.0.1`/`1025` so the test is runnable without the sidecar too), and assert the child process exits non-zero — proving the kernel-enforced `socket()` EPERM denial, NOT a code-inspection claim. Gate the entire test `#[cfg(target_os = "linux")]` per the project convention; document in a comment that "0 passed" on macOS is expected.
  </action>
  <verify>
    <automated>cargo build -p sandbox --tests 2>&1 | grep -qi error && echo FAIL || echo OK; cargo test -p sandbox 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p sandbox --tests` compiles.
    - The new test is `#[cfg(target_os = "linux")]` gated (`grep -B2 'fn.*smtp.*denied\|fn.*negative.*smtp' crates/sandbox/tests/confinement_integration.rs` shows the cfg attribute).
    - Under the Linux+Docker verification recipe, the test passes (confined connect() to Mailpit exits non-zero). On macOS `cargo test -p sandbox` it is skipped (0 passed) — expected, not a gap.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SMTP-02 grep gate — SMTP endpoint tokens absent from worker-spawn paths</name>
  <files>scripts/check-smtp-secrets-absent.sh</files>
  <read_first>
    - scripts/check-invariants.sh (grep-gate style to mirror)
    - cli/caprun/src/main.rs (worker-spawn code path — the process/env construction for caprun-worker)
    - crates/sandbox/src/ (worker confinement/spawn helpers — confirm which files constitute "worker-spawn paths")
    - .planning/phases/13-real-broker-mediated-smtp-adapter/13-RESEARCH.md (Validation Architecture SMTP-02 row; endpoint-sourcing D-04)
  </read_first>
  <action>
    Create scripts/check-smtp-secrets-absent.sh mirroring check-invariants.sh's style. It FAILS (non-zero + diagnostic) if the token `CAPRUN_SMTP_` appears in any worker-spawn code path — the files that construct the confined `caprun-worker` process env/args (the worker-spawn region of cli/caprun/src/main.rs and any sandbox worker-spawn helper). Scan non-comment lines. The gate proves the SMTP endpoint config reaches ONLY the broker/confirm process (email_smtp.rs), never the worker. Make the script executable. If identifying the precise worker-spawn file set is ambiguous, enumerate the exact files scanned in a comment header so the assertion is auditable.
  </action>
  <verify>
    <automated>bash scripts/check-smtp-secrets-absent.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-smtp-secrets-absent.sh` exits 0 against the current tree (worker-spawn paths contain no `CAPRUN_SMTP_` token).
    - The script's header comment enumerates exactly which files it treats as worker-spawn paths.
    - Introducing a `CAPRUN_SMTP_HOST` reference into a worker-spawn file makes the gate exit non-zero (verify by temporary insertion, then revert — noted in SUMMARY).
    - `test -x scripts/check-smtp-secrets-absent.sh` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined worker → network | The worker must be unable to open any SMTP socket — kernel-enforced |
| broker config → worker process | SMTP endpoint/credentials must never cross into the worker's env/args/plan-node payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Elevation of Privilege | confined worker network egress | critical | mitigate | Existing seccomp apply_worker_filter denies socket(AF_INET/AF_INET6) with EPERM; new confine-probe smtp op + Linux negative test exercise it against the real Mailpit endpoint (SMTP-01) |
| T-13-08 | Information Disclosure | SMTP endpoint/credentials custody | high | mitigate | Grep gate asserts CAPRUN_SMTP_ tokens absent from worker-spawn paths; endpoint sourced only from broker-process env (SMTP-02, D-04) |
</threat_model>

<verification>
- `cargo build -p sandbox --bin confine-probe --tests` compiles.
- Under the Linux+Docker recipe, the confined connect()-to-Mailpit test exits non-zero (kernel-denied).
- `bash scripts/check-smtp-secrets-absent.sh` exits 0.
</verification>

<success_criteria>
- A confined worker cannot open an SMTP socket to Mailpit (Linux negative assertion).
- SMTP endpoint config never reaches worker-spawn code paths.
</success_criteria>

<output>
Create `.planning/phases/13-real-broker-mediated-smtp-adapter/13-03-SUMMARY.md` when done.
</output>
