---
phase: 43-http-request-write-post-put-egress
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - crates/brokerd/src/server.rs
  - crates/brokerd/src/confirmation.rs
autonomous: true
requirements: [HTTP-W-01]

must_haves:
  truths:
    - "An Allowed `http.request.write` decision invokes the live write egress (invoke_http_write_sink) and records the OPAQUE two-phase audit; a non-Allowed decision never opens the socket."
    - "The write dispatch mints nothing (output_value_id stays None) and never holds the audit-db mutex across the `.await`."
    - "A tainted-body `http.request.write` that Blocks is confirm-releasable: `caprun confirm` runs the shared `prepare_http_write` precheck BEFORE burning the one-shot confirmation, then dispatches via invoke_http_write_from_resolved."
    - "`http.request.write` is added to the confirm-release entry-guard allow-list AND the Step-7 dispatch match — the guard/match stay in sync (no drift)."
  artifacts:
    - "crates/brokerd/src/server.rs — Allowed-decision dispatch arm for http.request.write."
    - "crates/brokerd/src/confirmation.rs — entry-guard allow-list entry + Step-4.8 precheck + Step-7 dispatch arm."
  key_links:
    - "server.rs Allowed arm gates on `plan_node.sink.0 == \"http.request.write\"` and calls invoke_http_write_sink."
    - "confirmation.rs precheck uses the SAME prepare_http_write the dispatch uses (terminal-event-before-terminal-state, P33/P34)."
---

<objective>
Wire the `http_write` egress (built in Plan 43-02) into the broker's two control-flow paths: the Allowed-decision dispatch (server.rs) and the single-shot confirm-release (confirmation.rs). After this plan an untainted-body write to a write-allowlisted host is dispatched live, and a tainted-body write that Blocks under I2 can be released by exactly one human `caprun confirm`.

Purpose: Close the loop so HTTP-W-01's confirm-releasability (DESIGN §2.0) holds end-to-end, mirroring the shipped github.pr Allowed + confirm-release arms — while preserving the terminal-event-before-terminal-state discipline (the recurring P33/P34 confirm-release audit-gap class).

Output: A dispatchable + confirm-releasable `http.request.write` sink.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@planning-docs/DESIGN-v1.9-egress-policy.md

# The github.pr Allowed-dispatch arm (server.rs ~1355-1540) and confirm-release arm (confirmation.rs ~840-1200) are the pattern to mirror (minus grant-gate/CAS):
@crates/brokerd/src/server.rs
@crates/brokerd/src/confirmation.rs
@crates/brokerd/src/sinks/http_write.rs

@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server.rs Allowed-decision dispatch arm for http.request.write</name>
  <files>crates/brokerd/src/server.rs</files>
  <behavior>
    - On `ExecutorDecision::Allowed` AND `plan_node.sink.0 == "http.request.write"`, the broker calls invoke_http_write_sink (resolving args from the broker-owned value_store), producing an opaque `http_write_succeeded`/`http_write_failed` terminal event.
    - The arm mints nothing: output_value_id stays None on this branch.
    - conn is never locked across the write `.await` (the sink locks internally only for its terminal append).
    - A non-Allowed decision (Block/Deny) never reaches this arm and never opens a socket.
  </behavior>
  <action>Add an Allowed-decision dispatch arm in server.rs's dispatch flow for `plan_node.sink.0 == "http.request.write"`, mirroring the github.pr arm's shape (~line 1378) but SIMPLER: no auth-grant gate, no content CAS (DESIGN §2 defines no grant/dedup for http.request.write — a single confirm-releasable write). Call `crate::sinks::http_write::invoke_http_write_sink(...)` passing the shared `conn`, `key`, `value_store`, `session_id`, `effect_id`, `plan_node`, and the current chain head (`*last_event_id` / `last_event_hash`) as parent — advancing the chain head to the returned terminal event id/hash exactly as the github.pr arm does. http.request.write CONSUMES; it mints nothing, so leave `output_value_id` = None (mirror the file.write/github.pr arms). Do NOT hold the audit-db mutex across the sink's `.await` (the sink locks internally for its terminal append). Follow the existing arm's Err handling: a propagated Err from the sink is surfaced per the surrounding dispatch convention (the sink already appended its opaque `http_write_failed` terminal event first). Add an in-crate test over `UnixStream::pair` / `dispatch_request` (mirror the existing dispatch-arm tests) asserting an Allowed http.request.write node reaches the write dispatch and records a terminal `http_write_*` event with no minted output value.</action>
  <verify>
    <automated>cargo test -p brokerd server:: --no-fail-fast</automated>
  </verify>
  <done>An Allowed http.request.write dispatches the live write egress, appends an opaque terminal event, mints nothing, and never locks across `.await`; dispatch test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: confirmation.rs confirm-release wiring (guard + precheck + dispatch)</name>
  <files>crates/brokerd/src/confirmation.rs</files>
  <behavior>
    - "http.request.write" is added to the Step-4.75 entry-guard allow-list AND the Step-7 dispatch match (the two stay in sync — a drift hits the explicit guard/match-drift error arm, never a silent fail).
    - Before Step 5 burns the one-shot confirmation, a Step-4.8-style precheck runs `prepare_http_write` on the frozen resolved args; a precheck failure returns fail-closed-RECOVERABLE (row stays Pending), never a burned confirmation with a dangling confirm_granted and no terminal event.
    - On confirm, invoke_http_write_from_resolved dispatches from the frozen ResolvedArg snapshot and appends the opaque terminal event.
  </behavior>
  <action>In crates/brokerd/src/confirmation.rs: (1) add `"http.request.write"` to the Step-4.75 entry-guard `match pc.sink.0.as_str()` allow-list (the `"file.create" | "email.send" | "file.write" | "process.exec" | "github.pr" => {}` arm) — required, not optional (a confirm-releasable sink absent from the guard is denied). (2) Add a Step-4.8-style precheck for `http.request.write` (mirror the process.exec `prepare_process_exec` and github.pr `prepare_github_pr` prechecks): call `crate::sinks::http_write::prepare_http_write(&pc.resolved_args)` and map a failure to a fail-closed-RECOVERABLE Err (row remains Pending) — this runs BEFORE Step 5 appends `confirm_granted` and Step 6 burns the one-shot, using the SAME prepare the Step-7 dispatch uses so precheck and dispatch cannot validate differently (the P33/P34 audit-gap discipline). (3) Add a Step-7 dispatch arm `"http.request.write" => { ... }` calling `crate::sinks::http_write::invoke_http_write_from_resolved(conn, key, pc.session_id, pc.effect_id, &pc.resolved_args, <current chain head parent id/hash>)` — mirror the process.exec async arm (this is not email.send, so the Step-6 `transition_state` burn applies as for the other non-email sinks). (4) Update the guard/match-drift comment allow-list set to include the new sink so the documented invariant stays accurate. Add tests: a tainted-body http.request.write Blocks then confirm releases exactly once (mirror the github.pr confirm-release test); a precheck failure leaves the row Pending (fail-closed-recoverable, no burned confirmation).</action>
  <verify>
    <automated>cargo test -p brokerd confirmation:: --no-fail-fast</automated>
  </verify>
  <done>http.request.write is guard+dispatch-wired (in sync); the pre-burn precheck reuses prepare_http_write; confirm releases exactly once; a precheck failure leaves the row Pending; confirm-release tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor decision → broker dispatch | only an Allowed decision may reach the live write dispatch. |
| human confirm → broker | a single-shot `caprun confirm` releases exactly one (sink,arg,literal-digest) write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-09 | Elevation | a non-Allowed decision reaching the write socket | critical | mitigate | the dispatch arm gates on `Allowed` + the exact sink id; Block/Deny never reaches it (mirror the shipped github.pr arm). |
| T-43-10 | Repudiation | burned one-shot confirmation with no terminal event (the P33/P34 audit-gap class) | high | mitigate | Step-4.8 `prepare_http_write` precheck runs BEFORE the confirm_granted/burn; failure is fail-closed-RECOVERABLE (row stays Pending); every dispatch leg folds into an opaque terminal `http_write_*` event first. |
| T-43-11 | Tampering | guard/dispatch drift silently admits an unwired sink | medium | mitigate | add the sink to BOTH the entry-guard allow-list and the Step-7 match; the explicit drift arm errors on mismatch. |
</threat_model>

<verification>
- `cargo build --workspace` compiles; `cargo test -p brokerd --no-fail-fast` green.
- `./scripts/check-invariants.sh` passes (no new mint site; no raw effect-request-to-sink bypass).
- On real Linux via `bash scripts/mailpit-verify.sh` with `MAILPIT_VERIFY_CMD='cargo test -p brokerd server:: confirmation::'`: dispatch + confirm-release arms compile and pass (watch [[cfg-linux-test-blindness]] — any signature change threads through Linux-gated callers; the done-gate is a container `cargo build --workspace --tests`).
</verification>

<success_criteria>
An untainted-body http.request.write to a write-allowlisted host dispatches live; a tainted-body write Blocks and is releasable by exactly one `caprun confirm` through the shared precheck; guard/match in sync; opaque terminal events on every leg; no mint, no cross-`.await` lock.
</success_criteria>

<output>
Create `.planning/phases/43-http-request-write-post-put-egress/43-03-SUMMARY.md` when done.
</output>
