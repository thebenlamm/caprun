---
phase: 43-http-request-write-post-put-egress
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - crates/brokerd/src/sinks/http_request.rs
  - crates/brokerd/src/sinks/http_write.rs
  - crates/brokerd/src/sinks/mod.rs
autonomous: true
requirements: [HTTP-W-01]

must_haves:
  truths:
    - "A WRITE (POST/PUT) egress is admitted ONLY to a host on a distinct `WRITE_HOST_ALLOWLIST`, separate from the GET `HOST_ALLOWLIST` (GET-readable ≠ POST/PUT-writable)."
    - "The WRITE path reuses the shipped SSRF resolve-and-pin verbatim (validate_url → write-allowlist gate BEFORE resolve → resolve_and_pin → vet_resolved/ssrf_check → redirect-none pin) — no classifier re-implemented."
    - "Any write credential is read from broker-local env ONLY; it is never a plan arg / ValueNode / audit literal, and never appears in log output on the write leg."
    - "The write response is NOT minted (Gate 3 mint-site allow-list stays byte-identical); the two-phase audit event is OPAQUE (no url, token, or body in the hashed payload)."
    - "A tainted-body write and a clean-body write validate IDENTICALLY through `prepare_http_write` (present + non-empty + constructible + method re-check), so precheck and dispatch cannot drift."
  artifacts:
    - "crates/brokerd/src/sinks/http_request.rs — WRITE_HOST_ALLOWLIST + generic invoke_http_write(url, method, body, optional bearer)."
    - "crates/brokerd/src/sinks/http_write.rs — new orchestration module (resolve/prepare/opaque-audit invoke + invoke_..._from_resolved)."
  key_links:
    - "invoke_http_write gates on WRITE_HOST_ALLOWLIST (NOT HOST_ALLOWLIST) — the write-vs-read allowlist split."
    - "prepare_http_write shared by the Allowed dispatch AND the confirm-release precheck (P33/P34 terminal-event-before-terminal-state discipline)."
---

<objective>
Build the broker-resident WRITE (POST/PUT) egress path for `http.request.write`: a distinct write host-allowlist, a generic SSRF-pinned write-invoke reusing the shipped GET infra verbatim, broker-env-only credential custody with an opaque non-minting two-phase audit, and a socket-free `prepare_http_write` precheck shared by the Allowed and confirm-release paths.

Purpose: DESIGN-v1.9-egress-policy §2.1/§2.3/§2.4 — a host being GET-readable must not imply it is POST/PUT-writable; the write rides the SAME defense-in-depth as the GET; the credential never leaves broker-local env and the response never enters the value store or audit chain.

Output: `invoke_http_write` + the `http_write` orchestration module — ready for the server.rs Allowed-dispatch arm and confirmation.rs confirm-release wiring (Plan 43-03).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The locked design context (READ §2.1/§2.3/§2.4 before editing):
@planning-docs/DESIGN-v1.9-egress-policy.md
@planning-docs/DESIGN-GATE-RECORD-v1.9.md

# The shipped GET/POST egress infra to reuse verbatim, and the github.pr sink to mirror for orchestration/audit/credential-custody:
@crates/brokerd/src/sinks/http_request.rs
@crates/brokerd/src/sinks/github_pr.rs

# Project Linux-verification rule (do NOT use the bare `docker run rust:1` recipe):
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WRITE_HOST_ALLOWLIST + generic invoke_http_write in http_request.rs</name>
  <files>crates/brokerd/src/sinks/http_request.rs</files>
  <behavior>
    - A distinct `WRITE_HOST_ALLOWLIST` const exists, separate from `HOST_ALLOWLIST`.
    - invoke_http_write rejects (Err) a host NOT on WRITE_HOST_ALLOWLIST BEFORE any resolve, even if that host IS on the GET HOST_ALLOWLIST (write-vs-read split proven by a host-portable test).
    - invoke_http_write rejects non-https, explicit-port, userinfo@, and IP-literal urls at validate_url (reuse the shipped gate).
    - The client is built redirect-none + IP-pinned via the SAME resolve_and_pin/build_pinned_client the GET uses.
    - method "POST" and "PUT" both build a valid request; any other method value is an Err (defense-in-depth alongside the executor enum gate).
  </behavior>
  <action>Add a distinct `WRITE_HOST_ALLOWLIST: &[&str]` broker-local trusted-config constant (per DESIGN §2.1 — separate from the GET `HOST_ALLOWLIST`; a security property, never runtime-configurable from a plan node/ValueNode/audit DB). Seed its default membership per DESIGN §2.1 (the operator-surfaced write deployment constant); under the NON-DEFAULT `mock-egress-ca` feature ALSO admit the Phase-46 write mock host (mirror the existing `MOCK_EGRESS_HOST` gating in `is_host_allowlisted`, but for the write allowlist — keep the feature-OFF release write-allowlist provably the base set via a `not(mock-egress-ca)` invariant test). Add `is_write_host_allowlisted` (mirror `is_host_allowlisted`). Add a generic `invoke_http_write(url: &str, method: &str, body: &str, bearer: Option<&str>) -> Result<(u16, String)>` that: `validate_url` → `is_write_host_allowlisted` gate (Err BEFORE any resolve, fail-closed) → the SAME `resolve_and_pin` → redirect-none pinned client → issue POST or PUT (match the validated method; anything else Err) with the body, setting a bearer auth header ONLY when `bearer` is `Some` → stream the response with the SAME `check_body_cap` fail-closed byte cap → `(status, capped_text)`. Do NOT re-implement any SSRF classifier — invoke the shipped functions. Do NOT reuse `invoke_pinned_post` (it gates on HOST_ALLOWLIST and hardcodes GitHub headers). Keep the Linux socket leg `#[cfg(target_os = "linux")]` with a macOS no-op stub, mirroring `do_pinned_get`/`do_pinned_post`. Confirm (MINOR-3, DESIGN-GATE-RECORD) that a SINGLE write is one request through the shipped pin — no second re-resolve/rebind window is introduced. Add host-portable tests: write-allowlist rejects a GET-only-allowlisted host; non-https/userinfo/port rejected; client builds for a pinned write host.</action>
  <verify>
    <automated>cargo test -p brokerd sinks::http_request:: --no-fail-fast</automated>
  </verify>
  <done>WRITE_HOST_ALLOWLIST is distinct; invoke_http_write gates on it before resolve, reuses resolve-and-pin, supports POST/PUT with optional broker-env bearer, redirect-none; write-vs-read split + validate_url tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: http_write orchestration module — resolve, prepare, opaque non-minting audit</name>
  <files>crates/brokerd/src/sinks/http_write.rs, crates/brokerd/src/sinks/mod.rs</files>
  <behavior>
    - `prepare_http_write(resolved_args)` validates url/body/method present + non-empty, method ∈ {POST,PUT}, and url constructible — socket-free, appends no event — and is used by BOTH invoke paths (Allowed + confirm-release) so they validate identically.
    - The credential (if any) is read from broker-local env only; an unset optional token means no auth header (a plain write with no credential is valid).
    - On a 2xx: an OPAQUE `http_write_succeeded` event is appended (parent-chained). On non-2xx OR Err: the raw status goes to the eprintln logger ONLY (never the url/body/token), an OPAQUE `http_write_failed` event is appended FIRST (terminal EVENT before any terminal disposition — P33/P34), then a non-swallowed Err propagates.
    - The module mints NOTHING (no mint_from_* / .mint()); the write response never enters the value store.
    - Grep of the persisted `http_write_*` payload shows no url, no body, no credential material.
  </behavior>
  <action>Create `crates/brokerd/src/sinks/http_write.rs` mirroring `github_pr.rs`'s structure (module-doc security role, credential custody, opaque audit, "NO mint here"). Implement: a broker-env credential reader (e.g. `write_bearer() -> Option<String>` from a broker-local env var such as `CAPRUN_HTTP_WRITE_TOKEN`, returning `None` when unset — NEVER read from a plan arg/ValueNode/audit DB, mirror `github_pr::github_token` but OPTIONAL not fail-closed, since a write may legitimately need no auth); `resolve_all_args` for {url, body, method} (mirror github_pr's, fail-closed on a dangling handle); a `PreparedWrite { url, method, body }` and a socket-free `prepare_http_write(&[ResolvedArg])` doing the present+non-empty + method-enum re-check + url-constructible validation (mirror `prepare_github_pr` — this is the P33/P34 precheck the confirm path reuses); `post_write(&PreparedWrite)` that reads the optional broker-env bearer and calls `http_request::invoke_http_write`; an `append_write_outcome` folding the POST result into the OPAQUE two-phase audit (`http_write_succeeded` on 2xx; else raw status to eprintln ONLY — per gate MINOR-4, NO url/body/token in ANY log line on the write leg — then an OPAQUE `http_write_failed` event FIRST, then propagate Err); and the two entry points `invoke_http_write_sink(conn: &Arc<Mutex<Connection>>, ...)` (Allowed dispatch, locks conn only for the terminal append, never across `.await`) and `invoke_http_write_from_resolved(conn: &Connection, ...)` (confirm-release, pre-locked conn) — mirror `invoke_github_pr` / `invoke_github_pr_from_resolved` exactly, minus the CAS/grant machinery (http.request.write needs no auth-grant and no content CAS per DESIGN §2 — a single confirm-releasable write). This module performs NO mint and appends only opaque events, keeping check-invariants Gate 3's mint-site allow-list byte-identical. Register `pub mod http_write;` in `crates/brokerd/src/sinks/mod.rs`. Add the tests from the behavior block, including an opaque-audit test grepping the persisted `http_write_failed` payload for absence of url/body/token (mirror github_pr's `opaque_audit_token_literal_absent_from_appended_event`).</action>
  <verify>
    <automated>cargo test -p brokerd sinks::http_write:: --no-fail-fast</automated>
  </verify>
  <done>http_write module compiles + registered; prepare_http_write shared/socket-free; opaque two-phase audit (failed-event-before-terminal-state); no mint; broker-env-only optional credential; payload-scrub + no-log-leak tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → external host | the single outbound POST/PUT socket crosses the trust boundary. |
| broker env → sink | the write credential is read from broker-local process env only. |
| sink → audit chain / value store | only OPAQUE events cross into the audit DAG; NO minted response value crosses into the value store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05 | Elevation | write to a GET-only-readable host | high | mitigate | distinct WRITE_HOST_ALLOWLIST gated BEFORE resolve; a host on the GET allowlist is not writable unless separately on the write allowlist (§2.1). |
| T-43-06 | Spoofing/SSRF | DNS-rebind / redirect / internal-range write target | critical | mitigate | reuse verbatim validate_url → resolve_and_pin → vet_resolved/ssrf_check → redirect(none) pin (§2.3); single-request write introduces no rebind window (MINOR-3). |
| T-43-07 | Information disclosure | credential or url/body leaked to audit or logs | high | mitigate | credential broker-env-only, never a plan arg/ValueNode/audit literal; opaque `http_write_*` events (no url/body/token); write leg logs status+effect_id only (MINOR-4, §2.4). |
| T-43-08 | Repudiation | a failed/aborted write leaves no terminal event | medium | mitigate | append `http_write_failed` FIRST (terminal event before terminal state), then propagate non-swallowed Err (P33/P34 discipline). |
| T-43-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO new crates — reuse shipped reqwest+rustls(ring)+webpki-roots (HYG-01); no package-install task in this plan. |
</threat_model>

<verification>
- `cargo build --workspace` compiles (macOS host); `cargo test -p brokerd --no-fail-fast` green.
- `./scripts/check-invariants.sh` passes — Gate 3 mint-site allow-list byte-identical (http_write.rs mints nothing); Gate 5 (aws-lc-rs absent) still green; no raw effect-request-to-sink bypass added.
- On real Linux via `bash scripts/mailpit-verify.sh` with `MAILPIT_VERIFY_CMD='cargo test -p brokerd sinks::http_write:: sinks::http_request::'`: the Linux socket-leg compiles + the host-portable gate tests pass.
</verification>

<success_criteria>
The broker has a distinct-write-allowlisted, SSRF-pinned, broker-env-credentialed, opaque-non-minting WRITE egress path (`invoke_http_write` + the `http_write` module) with a shared socket-free precheck — ready to wire into dispatch and confirm-release. No new crate, no new mint site.
</success_criteria>

<output>
Create `.planning/phases/43-http-request-write-post-put-egress/43-02-SUMMARY.md` when done.
</output>
