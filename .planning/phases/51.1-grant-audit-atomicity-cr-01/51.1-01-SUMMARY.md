---
phase: 51.1-grant-audit-atomicity-cr-01
plan: 01
subsystem: audit-integrity
tags: [sqlite, transactions, audit-chain, fault-injection]

requires:
  - phase: 51
    provides: Atomic append-at-durable-head audit choke point
provides:
  - Co-atomic github.pr capability grant and authorization event
  - Host-runnable RED-to-GREEN fault-injection regression
  - Corrected audit append inventory and sink-blocked atomicity documentation
affects: [51.1-02, 51.1-03, CR-01]

tech-stack:
  added: []
  patterns: [shared-reference immediate transaction, conditional append within enclosing transaction]

key-files:
  created:
    - crates/brokerd/tests/grant_audit_atomicity_regression.rs
  modified:
    - crates/brokerd/src/audit.rs
    - crates/brokerd/src/server.rs
    - planning-docs/DESIGN-audit-append-concurrency.md

key-decisions:
  - "record_github_grant retains its shared Connection signature and uses Transaction::new_unchecked with Immediate behavior."
  - "append_event joins the enclosing transaction through its existing autocommit branch; verifier behavior is unchanged."
  - "The sink-blocked write group remains mutex-serialized and fail-closed but is explicitly documented as not one SQL transaction."

patterns-established:
  - "Fault-injection oracle: commit the failing regression before the production repair, then pass it unedited."
  - "Capability authorization rows and their audit events share one immediate transaction."

requirements-completed: [CR-01]

coverage:
  - id: D1
    description: "A failed github grant audit append rolls back the capability row, and retry yields exactly one row and one authorization event."
    requirement: CR-01
    verification:
      - kind: integration
        ref: "crates/brokerd/tests/grant_audit_atomicity_regression.rs#append_failure_after_grant_insert_rolls_back_and_retry_yields_exactly_one"
        status: pass
    human_judgment: false
  - id: D2
    description: "Replay suppression and existing audit behavior remain intact under the enclosing transaction."
    requirement: CR-01
    verification:
      - kind: unit
        ref: "cargo test -p brokerd --lib audit:: -- --test-threads=1 (37 passed)"
        status: pass
      - kind: integration
        ref: "cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1 (2 passed)"
        status: pass
    human_judgment: false

completed: 2026-08-09
status: complete
---

# Phase 51.1 Plan 01: Grant/Audit Atomicity Summary

**The github.pr grant row and its authorization event now commit or roll back together under one immediate SQLite transaction, proven by an unedited RED-to-GREEN fault-injection oracle.**

## Accomplishments

- Landed a deterministic host-local regression that observed one surviving grant row on the pre-fix tree, then passed unedited after the fix with zero rows surviving the injected append failure.
- Wrapped the grant insert, durable-head read, conditional audit append, and commit in one `TransactionBehavior::Immediate` boundary without changing `has_github_grant` or the HARDEN-02 verifier/MAC functions.
- Corrected AR-05 and AR-06 and updated the Appendix A inventory from 2/40/3 to 2/39/4 while retaining the 45-site total.

## Task Commits

1. **Task 1: Commit the RED fault-injection oracle** — `fc2ee68`
   - **Pre-fix reference (Task 1 parent):** `9322e55`
2. **Task 2: Make grant and authorization event co-atomic** — `765fa50`
3. **Task 3: Correct comments and amend the design record** — `62d8b66`

## Verification

- Pre-fix oracle at `fc2ee68`: exited non-zero because the observed surviving `session_grants` row count was `1`.
- Post-fix regression: 2 passed, 0 failed.
- Audit unit suite: 37 passed, 0 failed.
- Audit-chain fork regression: 2 passed, 0 failed.
- `cargo build --workspace`: passed with pre-existing warnings only.
- `./scripts/check-invariants.sh`: Gates 1–6 PASSED.
- Protected bodies `verify_chain`, `current_chain_head`, `compute_event_hash`, `verify_event_hash`, `compute_anchor_mac`, `verify_anchor_mac`, and `has_github_grant` are byte-identical to pre-fix reference `9322e55`.
- Full-workspace composed regression: **UNRUN on this host**; owned by the shared AWS EC2 run batched with Phase 52.

## Decisions Made

- Preserved the locked `&Connection` API and used the sanctioned shared-reference transaction constructor.
- Kept `append_event` as the single audit append choke point and relied on its mandatory autocommit detection to join rather than nest.
- Left the sink-blocked group behavior unchanged and corrected its documentation to describe the accepted non-transactional limitation.

## Deviations from Plan

Execution moved inline after two typed executor contexts exceeded the configured stall threshold without reporting or making filesystem progress. The implementation scope, commit boundaries, and verification contract remained unchanged.

## Issues Encountered

- The initial executor produced the RED commit and production edit, then stalled. A fresh recovery executor also stalled. Both were interrupted; their preserved changes were validated and completed inline.
- Existing workspace build warnings in `sandbox` and `caprun` remain unchanged and outside this plan.

## Self-Check: PASSED

- All three task commits are reachable.
- The regression file was unchanged after its RED commit.
- Task 2 touched only `crates/brokerd/src/audit.rs`.
- Task 3 touched exactly the two Rust comment locations and the designated design record; executable Rust lines were unchanged.
- `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, Phase 51 artifacts, and `planning-docs/DESIGN-GATE-RECORD-v1.10.md` were not modified.

## Next Phase Readiness

Plan 51.1-02 can mechanically compare the protected TCB bodies and dependency surface against pre-fix reference `9322e55`, then run the scoped host regression. Plan 51.1-03 remains responsible for the fresh non-self adversarial trace.

---
*Phase: 51.1-grant-audit-atomicity-cr-01*
*Completed: 2026-08-09*
