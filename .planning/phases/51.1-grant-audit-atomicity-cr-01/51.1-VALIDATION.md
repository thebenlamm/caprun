---
phase: 51.1
slug: grant-audit-atomicity-cr-01
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-09
---

# Phase 51.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

**Source:** `51.1-RESEARCH.md` § Validation Architecture. The fix mechanism and the
fault-injection technique were empirically pre-validated during research (prototype
compiled, run, and removed — working tree confirmed clean).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `cargo test` (workspace `resolver = "3"`, `edition = "2021"`) |
| **Config file** | none — standard `Cargo.toml` |
| **Quick run command** | `cargo test -p brokerd --lib audit:: -- --test-threads=1` |
| **Full suite command** | `cargo test -p brokerd --lib && cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1 && cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1 && ./scripts/check-invariants.sh` |
| **Estimated runtime** | ~1 s quick (37 tests, measured 0.94 s); < 30 s full local |

**Docker note.** The full-workspace `compose-verify.sh` regression is **not** runnable on
this host (constrained device, no Docker) and is **not** required for CR-01's own
verification. It is deliberately deferred to the shared AWS EC2 run batched with Phase 52,
per the Phase 51.1 ROADMAP Notes. Every command above runs locally and touches no
`email.send` sink, so CLAUDE.md's Phase-16 Mailpit rule is satisfied without a sidecar.

---

## Sampling Rate

- **After every task commit:** `cargo test -p brokerd --lib audit:: -- --test-threads=1` (~1 s)
- **After every plan wave:** add both `-p brokerd --test` regression files plus `./scripts/check-invariants.sh`
- **Before the fresh non-self adversarial trace (success criterion 5):** full local suite green
- **Before `/gsd-verify-work`:** full local suite green
- **Max feedback latency:** ~30 s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 51.1-01-T2 | 01 | 1 | CR-01 (crit. 1) | — | `record_github_grant` wraps insert + conditional append in ONE `Immediate` transaction; `append_event` joins it without nesting | unit | `cargo test -p brokerd --lib audit::tests::github_grant_recorded_makes_has_true_and_emits_opaque_event -- --exact` | ✅ exists | ✅ green |
| 51.1-01-T1/T2 | 01 | 1 | CR-01 (crit. 2) | — | An `append_event` failure rolls back the staged `session_grants` row; a retry yields exactly one grant row and exactly one `github_grant_authorized` event | integration | `cargo test -p brokerd --test grant_audit_atomicity_regression -- --test-threads=1` | ✅ exists | ✅ green |
| 51.1-01-T1/T2 | 01 | 1 | CR-01 (crit. 3) | — | `has_github_grant` cannot observe a grant whose authorization event is absent | integration | same as crit. 2 (asserts false after failure, true after retry) | ✅ exists | ✅ green |
| 51.1-02-T1/T2 | 02 | 2 | CR-01 (crit. 4) | — | HARDEN-02 and append-chain linearity remain intact | regression/gate | `cargo test -p brokerd --test audit_chain_fork_regression -- --test-threads=1` plus landed diff gates | ✅ exists | ✅ green |
| 51.1-02-T1/T2 | 02 | 2 | CR-01 (crit. 6) | — | No new crate, no new mint site, no raw effect path | shell | `./scripts/check-invariants.sh` (Gates 1–6) | ✅ exists | ✅ green |
| 51.1-03-T2 | 03 | 3 | CR-01 (crit. 5) | — | Fresh non-self adversarial code-trace returned 0 BLOCKER / 0 unresolved MAJOR | manual gate | `51.1-ADVERSARIAL-TRACE.md` | ✅ retained | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs are placeholders pending the planner's task breakdown; the plan-checker reconciles them.*

---

## Wave 0 Requirements

- [x] `crates/brokerd/tests/grant_audit_atomicity_regression.rs` — integration test covering CR-01 criteria 2 and 3 via deterministic `events`-table fault injection.
- [x] No framework install needed — `cargo test` is already the workspace runner.

*Everything else this fix touches is already covered by existing `brokerd` unit tests and the `audit_chain_fork_regression.rs` precedent.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Fresh non-self adversarial code-trace of the landed diff returns 0 BLOCKER / 0 unresolved MAJOR | CR-01 (crit. 5) | Reviewer independence is the whole value — a self-read is not clearance (standing precedent, `51-08` T-51-08-01). Cannot be automated. | Orchestrator spawns a fresh-context, read-only, non-authoring reviewer against the landed diff; record reviewer identity and explicit author-≠-reviewer in the trace artifact; orchestrator independently re-verifies findings against live code before folding. Per the user's standing direction, use an external reviewer (Codex/Grok) for genuine independence. |
| Full-workspace composed regression | CR-01 (non-regression, deferred) | Requires Docker sidecars (Mailpit + mock GitHub on a public-range subnet); impossible on this host. | Batch onto the shared AWS EC2 run with Phase 52. Retain raw logs with SHA-256. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30 s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** CR-01 fault-injection, happy-path, replay, chain-linearity, CLI grant, and invariant checks are green; the retained independent adversarial trace has no blocking finding.

## Validation Audit 2026-08-11

| Metric | Count |
|--------|-------|
| Requirements audited | 1 |
| Covered | 1 |
| Partial | 0 |
| Missing | 0 |
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Current focused reruns passed: the CR-01 integration binary (2/2), the existing grant happy
path (1/1), append-chain fork regressions (2/2), and the real CLI grant tests (2/2).
