---
phase: 16-confirm-ux-literal-binding-negative-controls
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - cli/caprun/tests/s9_live_block.rs
autonomous: true
requirements: [CONTROL-01, CONTROL-02]
must_haves:
  truths:
    - "In one cargo test invocation, a send built from TRUSTED INTENT is Allowed (no sink_blocked, no confirm gate, no pending_confirmations row) AND actually delivered — a real email_send_succeeded event + the message captured in Mailpit — while a send whose args are DOC-DERIVED is Blocked (sink_blocked, no send, nothing in Mailpit). The A/B proves the gate is taint-driven, not a blanket email block, AND that a trusted send genuinely reaches the wire."
    - "A doc that taints ONLY the body (recipient TRUSTED, sourced from the CLI intent arg) still produces a sink_blocked event whose anchors are EXACTLY [\"body\"] — never [\"body\",\"to\"] and never empty — proving the body dimension is live, not dead code redundant with the routing block."
    - "Both controls run live through the real confined-worker + broker + executor stack (Linux-gated), against the same digest-producing block path Plan 16-01 built, and — for the CONTROL-01 clean half — the email.send Allowed-decision dispatch Plan 16-01 wired into server.rs, verified against a live Mailpit sidecar via scripts/mailpit-verify.sh."
  artifacts:
    - "cli/caprun/tests/s9_live_block.rs CONTROL-01 composed A/B test (trusted-intent Allowed + real Mailpit delivery vs doc-derived Blocked + no send, same run)"
    - "cli/caprun/tests/s9_live_block.rs CONTROL-02 body-tainted-only live fixture + single-anchor assertion"
  key_links:
    - "CONTROL-01's clean half exercises the email.send Allowed-decision dispatch branch Plan 16-01 added to server.rs, and asserts real delivery by querying the Mailpit HTTP API (the DETAIL endpoint documented in scripts/mailpit-verify.sh) for the sent message; the hostile half reuses s9_live_email_hostile_block's 2-anchor block mechanism — both asserted in one run."
    - "CONTROL-02's fixture models CLEAN_PATH_CONTENT's recipient side (no Reply-To:/Domain: markers → to falls through to the trusted CLI intent value) plus a Body: marker only (taints body)."
  prohibitions:
    - "MUST NOT claim CONTROL-01 is 'the same doc content with taint flipped' — the clean half sources its args from TRUSTED INTENT (CLI arg), not from a doc; the honest claim is 'trusted-intent send Allowed (and delivered) vs doc-derived send Blocked (and not sent)'."
    - "MUST NOT let CONTROL-02's fixture accidentally carry a Reply-To:/Domain: pair (that would taint `to` too, turning the intended 1-anchor body-only block into a 2-anchor block and failing to prove recipient-TRUSTED)."
    - "MUST NOT run the CONTROL-01 delivery assertion under the bare `rust:1` recipe — a real Mailpit capture requires the sidecar; use scripts/mailpit-verify.sh (the CLAUDE.md-mandated Mailpit-backed Linux recipe)."
---

<objective>
Prove the two negative controls that turn the demo from an anecdote into a controlled experiment:
CONTROL-01 (the gate is taint-driven — a trusted-intent send is Allowed with no gate, in the same run
as a doc-derived hostile block) and CONTROL-02 (a tainted body with a TRUSTED recipient still blocks —
the body dimension is not dead code redundant with the routing block).

Purpose: without CONTROL-01 the demo could be dismissed as "blocks all email"; without CONTROL-02,
CONTENT-01 is vacuously satisfied by the recipient block alone.

Output: a composed CONTROL-01 A/B test (trusted-intent Allowed AND delivered to Mailpit vs doc-derived
Blocked and not sent) and a new CONTROL-02 body-tainted-only live fixture in s9_live_block.rs, both
Linux-verified under scripts/mailpit-verify.sh.

SCOPE (coordinator ruling, BLOCKER-1 — supersedes RESEARCH Open Question 1's deferral recommendation).
CONTROL-01 is assigned to Phase 16 in REQUIREMENTS.md (:95); Phase 17/ACCEPT-01 only COMPOSES it
(ROADMAP:194 "composes", REQUIREMENTS:55 "Composes CONTROL-01") and has no plans yet. The earlier
recommendation to defer actual trusted-path SMTP delivery to Phase 17 is RETRACTED: it left the wiring
UNOWNED (16 refusing it citing 17, 17 assuming 16 built it) — the exact hollowing pattern the panel
flagged. The stated Gate-3 cost of the deferral was also FALSE: check-invariants.sh already allowlists
server.rs for the mint tokens, and an email.send Allowed-dispatch branch calling the RESOLVING
sinks::email_smtp trips no gate. Therefore Plan 16-01 BUILDS the email.send Allowed-decision dispatch
(a faithful mirror of the file.create branch), and THIS plan asserts a REAL trusted clean send lands in
Mailpit. Phase 17 KEEPS a distinct job: composing CONTROL-01 + the hostile block into ONE run / ONE
unbroken DAG (one Uuid, one audit.db, one session) — the current tests use a fresh Uuid + audit.db per
call (two DBs, not one run); that single-run composition is Phase 17's, not this plan's.

FRAMING HONESTY (carried from Phase 15 Round-3 MAJOR-2, and from 15-04-SUMMARY's own framing note):
s9_live_clean_allow_path sources its `to`/`subject`/`body` from the TRUSTED INTENT (the CLI arg), while
s9_live_email_hostile_block derives them from a hostile doc. These are NOT a same-content A/B with "taint
flipped." The honest CONTROL-01 claim this plan asserts and documents is: "a send built from trusted
intent is Allowed, ungated, AND delivered to Mailpit; a send whose args are doc-derived is Blocked and
never sent." All test names, comments, and success-criteria language MUST use this framing — never the
stronger/false "same doc, taint flipped."
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-confirm-binding.md
@.planning/phases/16-confirm-ux-literal-binding-negative-controls/16-RESEARCH.md
@.planning/phases/15-deterministic-doc-action-extraction/15-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONTROL-01 — composed taint-driven A/B (trusted-intent Allowed vs doc-derived Blocked)</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs:1-229 — the module doc, CLEAN_PATH_CONTENT (61) and
      HOSTILE_EMAIL_CONTENT (76-80) constants, run_caprun_intent_on (85-110), s9_live_clean_allow_path
      (118-166), and s9_live_email_hostile_block (185-229) — the two proofs being composed and their
      exact assertions (plan_node_evaluated present + sink_blocked absent for clean; 2-anchor
      sink_blocked + no sink_executed for hostile).
    - .planning/phases/15-deterministic-doc-action-extraction/15-04-SUMMARY.md — the "Framing honesty"
      section stating clean sources from trusted intent, not the doc.
    - .planning/phases/16-confirm-ux-literal-binding-negative-controls/16-01-PLAN.md Task 3 — the
      email.send Allowed-decision dispatch this plan's clean half now exercises (a trusted send actually
      reaches the SMTP adapter). This plan DEPENDS on 16-01 for that wiring.
    - scripts/mailpit-verify.sh — the Mailpit sidecar recipe AND (in its trailing comment block) the
      exact Mailpit HTTP DETAIL-endpoint field path (message["To"][i]["Address"]) the delivery
      assertion must query. Use this recipe, not the bare rust:1 one, for the live capture.
  </read_first>
  <action>
    Add one Linux-gated test (e.g. s9_control_ab_taint_driven) that, in a SINGLE cargo test invocation,
    runs BOTH scenarios via run_caprun_intent_on and asserts the A/B contrast:

    (A) the trusted-intent clean run (send-email-summary with a trusted CLI recipient over
    CLEAN_PATH_CONTENT) exits 0, has a plan_node_evaluated event, has NO sink_blocked event, AND — the
    NEW send-level proof enabled by Plan 16-01's email.send Allowed-decision dispatch — has an
    `email_send_succeeded` event and the message is CAPTURED IN MAILPIT (query the Mailpit HTTP API's
    DETAIL endpoint per scripts/mailpit-verify.sh's documented field path, asserting the trusted
    recipient arrived). ALSO assert the clean session has ZERO rows in `pending_confirmations`
    (`SELECT COUNT(*) FROM pending_confirmations == 0` for that session) — CONTROL-01 claims "no confirm
    gate," which is a DIFFERENT and stronger claim than merely "sink_blocked absent"; assert it directly.

    (B) the doc-derived hostile run (over HOSTILE_EMAIL_CONTENT) exits non-zero, has a sink_blocked
    event, has NO email_send_succeeded, and its recipient does NOT appear in Mailpit.

    Assert both outcomes in the one test body so the taint-driven contrast is a single controlled
    experiment, not two unrelated tests. The test's doc comment MUST state the honest framing
    (trusted-intent Allowed AND delivered vs doc-derived Blocked and not sent — NOT "same doc, taint
    flipped").

    Reuse the existing constants and helper; do not duplicate them. This test's live half MUST run under
    scripts/mailpit-verify.sh (the Mailpit sidecar), NOT the bare rust:1 recipe — a real capture needs
    the sidecar + libssl-dev/pkg-config that script provides.
  </action>
  <verify>
    <automated>cargo build -p caprun --tests
# Linux verification (authoritative — macOS bodies are cfg-excluded; a real Mailpit capture is asserted):
#   bash scripts/mailpit-verify.sh
# (mailpit-verify.sh runs `cargo test --workspace --no-fail-fast` on the Mailpit network; to scope to
#  just this test, set MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_live_block s9_control_ab_taint_driven'
#  if that override is wired, else run the full suite the script drives.)</automated>
  </verify>
  <acceptance_criteria>
    - One test asserts, in the same run: trusted-intent clean → exit 0 + plan_node_evaluated + no sink_blocked + ZERO pending_confirmations rows + email_send_succeeded + message captured in Mailpit; doc-derived hostile → non-zero + sink_blocked + no email_send_succeeded + not in Mailpit.
    - The test doc comment uses the honest framing (Allowed AND delivered vs Blocked and not sent); no "same doc, taint flipped".
    - The live half runs under scripts/mailpit-verify.sh (Mailpit sidecar), not the bare rust:1 recipe.
    - Compiles on macOS (cfg-gated bodies) and passes under the Mailpit-backed Linux run.
  </acceptance_criteria>
  <done>One live A/B test asserts trusted-intent Allowed, ungated (zero pending_confirmations), AND delivered to Mailpit vs doc-derived Blocked and not sent — in the same run, with honest framing, exercising the email.send Allowed-decision dispatch Plan 16-01 wired, verified under scripts/mailpit-verify.sh.</done>
</task>

<task type="auto">
  <name>Task 2: CONTROL-02 — body-tainted-only, recipient-trusted, single-anchor live block</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs:51-80 — CLEAN_PATH_CONTENT (recipient side: no Reply-To:/Domain:
      markers → to falls through to the trusted CLI intent value) and HOSTILE_EMAIL_CONTENT (the Body:
      marker mechanism that taints body).
    - cli/caprun/tests/s9_live_block.rs:185-229 — s9_live_email_hostile_block's 2-anchor assertion shape
      (arg_names sorted == vec!["body","to"]) — CONTROL-02 asserts the 1-anchor variant.
    - 16-RESEARCH.md Pitfall 5 — the accidental-recipient-taint failure mode to avoid.
  </read_first>
  <action>
    Add a body-tainted-only fixture constant: workspace content with a `Body:` marker (tainting the
    body) but NO `Reply-To:`/`Domain:` recipient markers — so the confined worker derives NO recipient
    and the planner routes the TRUSTED CLI intent recipient into email.send/to. Add a Linux-gated test
    (e.g. s9_control02_body_tainted_recipient_trusted_blocks) that runs caprun send-email-summary with a
    trusted CLI recipient over this content, asserts caprun exits non-zero, and asserts the sink_blocked
    event's anchors' arg names are EXACTLY ["body"] (not ["body","to"], not empty) and that no
    sink_executed event exists. Assert the single-anchor shape explicitly (a length check plus the
    name), so an accidental recipient taint (Pitfall 5) fails the test rather than passing silently.
  </action>
  <verify>
    <automated>cargo build -p caprun --tests
# Linux verification (authoritative):
# docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test -p caprun --test s9_live_block s9_control02_body_tainted_recipient_trusted_blocks</automated>
  </verify>
  <acceptance_criteria>
    - The fixture taints ONLY body (no Reply-To:/Domain: markers); the recipient is the trusted CLI intent value.
    - The live run exits non-zero with a sink_blocked event whose anchors are exactly ["body"] and no sink_executed.
    - The single-anchor assertion is explicit (rejects a 2-anchor block), guarding Pitfall 5.
    - Compiles on macOS; passes under the Colima/Docker Linux run.
  </acceptance_criteria>
  <done>A body-tainted-only, recipient-trusted live fixture blocks with exactly the ["body"] anchor and no sink_executed, proving the content dimension is not dead code; single-anchor assertion guards Pitfall 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile doc bytes → confined worker extractor | Untrusted document content crosses into the extractor; the controls prove the gate discriminates by taint provenance, not by sink identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Spoofing (false security signal) | CONTROL-01 A/B | high | mitigate | Prove taint-driven discrimination live (trusted-intent Allowed vs doc-derived Blocked in one run) so "blocks all email" (a vacuous gate) is falsified — not merely claimed. |
| T-16-08 | Repudiation (dead-code content dimension) | CONTROL-02 | high | mitigate | Assert the body-only block has EXACTLY the body anchor with a trusted recipient, so the content-sensitivity path cannot be silently satisfied by the routing block alone. |
| T-16-09 | Tampering (mis-scoped claim) | CONTROL-01 framing | medium | mitigate | Test/comment language pinned to the honest "trusted-intent Allowed AND delivered vs doc-derived Blocked and not sent" claim — never the stronger/false "same doc, taint flipped." Delivery is now genuinely proven via a live Mailpit capture (not deferred), and the clean half additionally asserts zero pending_confirmations (the "no gate" claim), so the control is not overclaimed NOR under-proven. |
| T-16-SC | Tampering | cargo installs | n/a | accept | No new Cargo dependency; test-only changes. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build -p caprun --tests` compiles on macOS (cfg-gated live bodies excluded).
- Authoritative (real Mailpit capture required for CONTROL-01's clean half): `bash scripts/mailpit-verify.sh`
  — the two new controls plus the pre-existing s9 live tests pass; the trusted send is captured in Mailpit.
- This plan's test files change only (cli/caprun/tests/s9_live_block.rs); the email.send Allowed-decision
  dispatch it exercises is Plan 16-01's server.rs change, not this plan's.
- CONTROL-01's test doc comment uses the honest framing (Allowed AND delivered vs Blocked and not sent).

MANDATE — "deny sends nothing" send-level proof (do NOT let it vanish into TBD): the hero demo's other
half — a human DENYING a confirm results in NO SMTP send — currently has NO send-level proof in any
written plan (this phase has no deny-path Mailpit assertion; Phase 17's plans do not exist yet). This is
recorded here as an explicit REQUIREMENT for Phase 17/ACCEPT-01 (or a follow-up to this plan if the
coordinator pulls it forward): an explicit post-`caprun deny` assertion that Mailpit's message count for
that recipient is 0 / the inbox is empty AND no email_send_succeeded event exists. It MUST appear in a
concrete plan task, not be assumed. Flag this in the 16-03 SUMMARY so the orchestrator carries it into
Phase 17 planning.
</verification>

<success_criteria>
Both negative controls pass live on real Linux (under scripts/mailpit-verify.sh): the gate is proven
taint-driven (trusted-intent send Allowed, ungated — zero pending_confirmations — AND delivered to
Mailpit vs doc-derived send Blocked and not sent, one run) and the body dimension is proven live (a
tainted body with a trusted recipient blocks with exactly the body anchor). The email.send
Allowed-decision dispatch (Plan 16-01) is exercised end-to-end. The "deny sends nothing" send-level
proof is recorded as an explicit Phase-17 mandate so it is not lost.
</success_criteria>

<output>
Create `.planning/phases/16-confirm-ux-literal-binding-negative-controls/16-03-SUMMARY.md` when done.
</output>
