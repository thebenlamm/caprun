---
phase: 16-confirm-ux-literal-binding-negative-controls
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - cli/caprun/tests/s9_live_block.rs
autonomous: true
requirements: [CONTROL-02]
must_haves:
  truths:
    - "A doc that taints ONLY the body (recipient TRUSTED, sourced from the CLI intent arg) still produces a sink_blocked event whose anchors are EXACTLY [\"body\"] — never [\"body\",\"to\"] and never empty — proving the body dimension is live, not dead code redundant with the routing block."
    - "The control runs live through the real confined-worker + broker + executor stack (Linux-gated), against the same digest-producing block path Plan 16-01 built; because the body-tainted node BLOCKS, no send occurs — so this control needs no live SMTP/Mailpit, only the block assertion."
  artifacts:
    - "cli/caprun/tests/s9_live_block.rs CONTROL-02 body-tainted-only live fixture + single-anchor (\"body\") assertion"
  key_links:
    - "CONTROL-02's fixture models CLEAN_PATH_CONTENT's recipient side (no Reply-To:/Domain: markers → to falls through to the trusted CLI intent value) plus a Body: marker only (taints body), so the executor's collect-then-Block returns a ONE-anchor block on body."
  prohibitions:
    - "MUST NOT let CONTROL-02's fixture accidentally carry a Reply-To:/Domain: pair (that would taint `to` too, turning the intended 1-anchor body-only block into a 2-anchor block and failing to prove recipient-TRUSTED) — RESEARCH Pitfall 5."
    - "MUST NOT assert any Mailpit delivery here — a body-tainted node BLOCKS; nothing is sent. CONTROL-01's real-delivery proof lives in Plan 16-04 (which owns the email.send Allowed-dispatch)."
---

<objective>
Prove CONTROL-02: a tainted body with a TRUSTED recipient STILL blocks — the body (content) dimension
is not dead code redundant with the routing block. Without it, CONTENT-01 is vacuously satisfied by the
recipient block alone.

Purpose: turns "the gate blocks tainted recipients" into "the gate blocks tainted CONTENT too, even when
the recipient is trusted" — the second half of the negative-control experiment.

Output: a body-tainted-only, recipient-trusted live fixture in s9_live_block.rs that blocks with EXACTLY
the ["body"] anchor and no sink_executed, Linux-verified.

SCOPE NOTE (restructure this round). CONTROL-01 (a trusted-intent send Allowed AND delivered to Mailpit,
in the same run as a doc-derived hostile block) has MOVED to Plan 16-04. Reason: CONTROL-01's clean half
asserts a REAL trusted send landing in Mailpit, which requires the email.send Allowed-decision dispatch —
and that dispatch, plus its three mandatory security guards, is now its own security-surface plan (16-04)
that gets a distinct adversarial pass BEFORE execution (coordinator BLOCKER-1 STRUCTURE mandate). Since
CONTROL-01's delivery assertion depends on 16-04's code, it belongs WITH 16-04, not in a wave that
precedes it. CONTROL-02 stays here because a body-tainted node BLOCKS — it needs neither the Allowed
dispatch nor a live Mailpit capture, so it can prove the content dimension in wave 2 without depending
on 16-04. (The Mailpit HTTP-client / inbox-isolation / env-var fixes the Round-2 plan assigned here also
moved to 16-04 along with CONTROL-01, since they exist only to assert a live delivery.)
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-confirm-binding.md
@.planning/phases/16-confirm-ux-literal-binding-negative-controls/16-RESEARCH.md
@.planning/phases/15-deterministic-doc-action-extraction/15-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONTROL-02 — body-tainted-only, recipient-trusted, single-anchor live block</name>
  <files>cli/caprun/tests/s9_live_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_live_block.rs:51-110 — CLEAN_PATH_CONTENT (recipient side: no Reply-To:/Domain:
      markers → to falls through to the trusted CLI intent value), HOSTILE_EMAIL_CONTENT (the Body:
      marker mechanism that taints body), and run_caprun_intent_on (the real-binary runner + audit-db-path return).
    - cli/caprun/tests/s9_live_block.rs:185-229 — s9_live_email_hostile_block's 2-anchor assertion shape
      (arg_names sorted == vec!["body","to"]) — CONTROL-02 asserts the 1-anchor ["body"] variant.
    - 16-RESEARCH.md Pitfall 5 — the accidental-recipient-taint failure mode to avoid.
  </read_first>
  <action>
    Add a body-tainted-only fixture constant: workspace content with a `Body:` marker (tainting the
    body) but NO `Reply-To:`/`Domain:` recipient markers — so the confined worker derives NO recipient
    and the planner routes the TRUSTED CLI intent recipient into email.send/to. Add a Linux-gated test
    (e.g. s9_control02_body_tainted_recipient_trusted_blocks) that runs caprun send-email-summary with a
    trusted CLI recipient over this content, asserts caprun exits non-zero, and asserts the sink_blocked
    event's anchors' arg names are EXACTLY ["body"] (not ["body","to"], not empty) and that no
    sink_executed / email_send_succeeded event exists. Assert the single-anchor shape EXPLICITLY (a length
    check == 1 PLUS the name == "body"), so an accidental recipient taint (Pitfall 5) fails the test
    rather than passing silently.

    Reuse the existing constants and run_caprun_intent_on helper; do not duplicate them. This test asserts
    a BLOCK (no send), so it needs no live SMTP listener and no Mailpit query — it runs under the standard
    Linux recipe (from Phase 16 on, that is scripts/mailpit-verify.sh per the CLAUDE.md update Plan 16-04
    lands; the bare rust:1 recipe would also suffice for this block-only test, but keep the suite uniform).
  </action>
  <verify>
    <automated>cargo build -p caprun --tests
# Linux verification (authoritative — macOS bodies are cfg-excluded; this control asserts a BLOCK, no send).
# From Phase 16 on all Linux verification goes through scripts/mailpit-verify.sh; capture $? directly
# (never `script | tail`, which returns tail's status):
#   bash scripts/mailpit-verify.sh ; echo "exit=$?"
# (mailpit-verify.sh's MAILPIT_VERIFY_CMD override — wired by Plan 16-04 — lets you scope to this test:
#   MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_live_block s9_control02_body_tainted_recipient_trusted_blocks' bash scripts/mailpit-verify.sh)</automated>
  </verify>
  <acceptance_criteria>
    - The fixture taints ONLY body (no Reply-To:/Domain: markers); the recipient is the trusted CLI intent value.
    - The live run exits non-zero with a sink_blocked event whose anchors are EXACTLY ["body"] and no sink_executed / email_send_succeeded.
    - The single-anchor assertion is explicit (length == 1 AND name == "body"), rejecting a 2-anchor block, guarding Pitfall 5.
    - Compiles on macOS; passes under the Linux run.
  </acceptance_criteria>
  <done>A body-tainted-only, recipient-trusted live fixture blocks with exactly the ["body"] anchor and no send, proving the content dimension is not dead code; single-anchor assertion guards Pitfall 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile doc bytes → confined worker extractor | Untrusted document content crosses into the extractor; CONTROL-02 proves the gate discriminates by taint on the CONTENT dimension, not only on the routing dimension. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-08 | Repudiation (dead-code content dimension) | CONTROL-02 | high | mitigate | Assert the body-only block has EXACTLY the body anchor with a trusted recipient, so the content-sensitivity path cannot be silently satisfied by the routing block alone. |
| T-16-SC | Tampering | cargo installs | n/a | accept | No new Cargo dependency; test-only change. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build -p caprun --tests` compiles on macOS (cfg-gated live body excluded).
- Authoritative Linux run: `bash scripts/mailpit-verify.sh` — the new CONTROL-02 test plus the
  pre-existing s9 live tests pass; CONTROL-02 asserts a block (no send), so no Mailpit capture is queried.
- This plan changes only cli/caprun/tests/s9_live_block.rs.
</verification>

<success_criteria>
CONTROL-02 passes live on real Linux: a tainted body with a trusted recipient blocks with exactly the
["body"] anchor and no send — the body dimension is proven live, not dead code. CONTROL-01's live
trusted-delivery A/B is proven in Plan 16-04 (which owns the email.send Allowed-dispatch it exercises).
</success_criteria>

<output>
Create `.planning/phases/16-confirm-ux-literal-binding-negative-controls/16-03-SUMMARY.md` when done.
</output>
