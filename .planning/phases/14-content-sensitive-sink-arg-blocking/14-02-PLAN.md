---
phase: 14-content-sensitive-sink-arg-blocking
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/runtime-core/src/event.rs
  - crates/brokerd/src/server.rs
  - crates/brokerd/src/audit.rs
  - crates/brokerd/src/confirmation.rs
  - crates/brokerd/tests/durable_anchor.rs
  - crates/brokerd/tests/phase5_dispatch.rs
  - crates/brokerd/tests/s9_acceptance.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/confirm.rs
autonomous: true
requirements: [CONTENT-01]

must_haves:
  truths:
    - "A `sink_blocked` audit Event carries ALL blocked anchors (`anchors: Vec<SinkBlockedAnchor>`), not just the first — every blocked arg is durably recorded in the hash-chained audit DAG (CONTENT-01 durability end-to-end)."
    - "Non-`sink_blocked` events serialize byte-identically to before the migration — the golden-byte fixture stays green (empty `anchors` omitted via `skip_serializing_if`), so the audit hash chain is undisturbed for unrelated event types."
    - "`audit.rs` fails closed: a `sink_blocked` event with an EMPTY `anchors` collection is rejected (Defect-B guard preserved, now plural)."
    - "`server.rs` constructs the plural `sink_blocked` event and the `PendingConfirmation` from the plural decision; the block-time `resolved_args` full-arg snapshot is unchanged."
    - "`cargo test --workspace --no-fail-fast` is GREEN and `./scripts/check-invariants.sh` (Gate 1 + Gate 2) passes — the whole workspace compiles against the plural type again."
  artifacts:
    - "crates/runtime-core/src/event.rs — `Event.anchors: Vec<SinkBlockedAnchor>` with `#[serde(default, skip_serializing_if = \"Vec::is_empty\")]`; `Event::sink_blocked(.., anchors: Vec<SinkBlockedAnchor>)`; re-verified golden-byte test."
    - "crates/brokerd/src/server.rs, audit.rs, confirmation.rs — plural-aware decision handling, fail-closed guard, and render safeguard."
    - "Migrated brokerd + cli test/consumer sites."
  key_links:
    - "ExecutorDecision::BlockedPendingConfirmation.anchors (14-01) → server.rs → Event::sink_blocked(anchors) → audit.rs empty-check."
    - "skip_serializing_if = Vec::is_empty preserves golden bytes for non-block events (no DB migration)."
    - "every element of anchors shares one effect_id → PendingConfirmation.effect_id = anchors[0].anchor.effect_id."
---

<objective>
Migrate every consumer of the now-plural `ExecutorDecision::BlockedPendingConfirmation` and make the audit Event carry ALL blocked anchors, restoring a green workspace. This is the consumer/control-plane layer of Phase 14.

Purpose: 14-01 changed the executor decision and anchor to a `Vec`-shaped set but intentionally left the workspace red — brokerd, `event.rs`, and cli still destructure the singular shape. This plan updates the audit Event to record the full set of blocked anchors (so CONTENT-01's block is durably auditable end-to-end), migrates the broker decision handling and the fail-closed guard, and mechanically updates the remaining test/consumer sites.

Output: `Event.anchors` plural + preserved golden bytes; plural-aware `server.rs`/`audit.rs`/`confirmation.rs`; a green `cargo test --workspace`.

Scope guard: This plan does NOT implement CONFIRM-04's multi-arg block narration. `render_block_display` keeps its existing single-arg display path for the single-blocked-arg case; a genuinely-plural block gets a fail-closed safeguard (assert/panic) rather than silent truncation, pending Phase 16's real narration.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-content-sensitive-sink-arg-blocking/14-RESEARCH.md
@.planning/phases/14-content-sensitive-sink-arg-blocking/14-PATTERNS.md
@planning-docs/DESIGN-content-adapter-mediation.md
@.planning/phases/14-content-sensitive-sink-arg-blocking/14-01-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's symbol changes (see 14-01-PLAN for the full-phase list):

- `Event.anchors: Vec<SinkBlockedAnchor>` (was `anchor: Option<SinkBlockedAnchor>`) in `crates/runtime-core/src/event.rs`, with `#[serde(default, skip_serializing_if = "Vec::is_empty")]`.
- `Event::sink_blocked(id, parent_id, session_id, timestamp, anchors: Vec<SinkBlockedAnchor>)` — sets `Event.taint` by merging every anchor's taint; sets `anchors`.
- `Event::new` still sets the empty collection (`anchors: vec![]`).
- Re-verified golden-byte test (renamed to reflect the empty-collection case, e.g. `anchors_empty_event_serializes_byte_identical_and_round_trips`).
- Plural-aware `server.rs` decision arms; `audit.rs` `anchors.is_empty()` Defect-B guard; `confirmation.rs` fail-closed multi-block safeguard.
- No new DenyReason variant; no new adapter/sink code (this is a type-shape migration only).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Make the audit Event carry all blocked anchors, preserving golden bytes</name>
  <read_first>
    - crates/runtime-core/src/event.rs (the file being modified — `anchor: Option<SinkBlockedAnchor>` field lines 30-38, `Event::new` 47-67, `Event::sink_blocked` 75-92, golden-byte test 95-141)
    - crates/runtime-core/src/executor_decision.rs (the plural shape landed by 14-01 — SinkBlockedAnchor + BlockedArg)
    - .planning/phases/14-content-sensitive-sink-arg-blocking/14-PATTERNS.md (event.rs target shape + skip_serializing_if discipline)
    - .planning/phases/14-content-sensitive-sink-arg-blocking/14-RESEARCH.md (Pitfall 2: golden-byte fixture)
  </read_first>
  <files>crates/runtime-core/src/event.rs</files>
  <action>
    Change the `Event.anchor: Option<SinkBlockedAnchor>` field to `anchors: Vec<SinkBlockedAnchor>` and change its serde attribute to `#[serde(default, skip_serializing_if = "Vec::is_empty")]` so an empty collection is OMITTED from JSON (matching today's `None`-omitted behavior, keeping non-block events byte-identical — no DB migration). Update the field doc comment to describe the plural collection and the empty-vec omission.
    Update `Event::new` to set `anchors: vec![]` (was `anchor: None`).
    Update `Event::sink_blocked` to take `anchors: Vec<SinkBlockedAnchor>` instead of a single `anchor`; set `Event.taint` by merging every anchor's taint (flat_map over the collection, collecting each anchor's `taint` clone) instead of the single `anchor.taint.clone()`; set the `anchors` field. Keep `actor = "executor"` and `event_type = "sink_blocked"`.
    Re-verify and update the golden-byte test: rename it to reflect the empty-collection case, assert the serialized non-block event is byte-identical to the existing GOLDEN string (no `anchors` key present), and update the round-trip assertion from `restored.anchor.is_none()` to `restored.anchors.is_empty()`. Do NOT change the GOLDEN literal — the whole point is that it stays byte-identical.
    runtime-core stays pure (Gate 2) — no I/O/async introduced.
  </action>
  <verify>
    <automated>cargo test -p runtime-core</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p runtime-core` exits 0, including the golden-byte fixture test.
    - `grep -c 'anchors: Vec<SinkBlockedAnchor>' crates/runtime-core/src/event.rs` returns 1.
    - `grep -c 'skip_serializing_if = "Vec::is_empty"' crates/runtime-core/src/event.rs` returns 1.
    - The GOLDEN string literal in the test is unchanged (the non-block event still serializes with no anchor/anchors key): the test asserting byte-identity passes.
    - `./scripts/check-invariants.sh` Gate 2 passes.
  </acceptance_criteria>
  <done>The audit Event carries a plural anchors collection; non-block events serialize byte-identically; the golden fixture is green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate brokerd decision handling, fail-closed guard, and render safeguard</name>
  <read_first>
    - crates/brokerd/src/server.rs (decision arms 419-485: BlockedPendingConfirmation destructure at 420, Event::sink_blocked construction at 421, PendingConfirmation assembly at 453, resolved_args snapshot loop)
    - crates/brokerd/src/audit.rs (Defect-B guard at line 204: `event.anchor.is_none()`)
    - crates/brokerd/src/confirmation.rs (render_block_display 282-308 operating on pc.resolved_args; the test fixture constructing a singular decision ~line 653)
    - crates/brokerd/tests/durable_anchor.rs, phase5_dispatch.rs, s9_acceptance.rs (singular destructure sites)
    - .planning/phases/14-content-sensitive-sink-arg-blocking/14-PATTERNS.md (server.rs / audit.rs / confirmation.rs analog sections)
  </read_first>
  <files>crates/brokerd/src/server.rs, crates/brokerd/src/audit.rs, crates/brokerd/src/confirmation.rs, crates/brokerd/tests/durable_anchor.rs, crates/brokerd/tests/phase5_dispatch.rs, crates/brokerd/tests/s9_acceptance.rs</files>
  <action>
    In server.rs, change the `BlockedPendingConfirmation { anchor, literal }` decision arm(s) (lines ~420, ~453) to destructure `{ anchors }`. Construct the `sink_blocked` Event by passing the collection of per-element `SinkBlockedAnchor`s (map each `BlockedArg` to its `anchor` clone) into the updated `Event::sink_blocked`. Set `PendingConfirmation.effect_id` from `anchors[0].anchor.effect_id` (every element shares one effect_id — one Block, one effect_id, N blocked args). The redactable `blocked_literals` side-table write must persist EVERY blocked arg's live literal (iterate all `anchors`, writing each element's `literal`), not just the first — do not drop any blocked literal. The `resolved_args` full-arg snapshot loop is UNCHANGED (it already resolves every arg on the plan node).
    In audit.rs, change the Defect-B fail-closed guard from `event.anchor.is_none()` to `event.anchors.is_empty()` and update its error message to "sink_blocked event requires at least one anchor (Defect B guard)".
    In confirmation.rs, migrate any site that constructs or destructures the singular `BlockedPendingConfirmation { anchor, literal }` shape (including the ~line 653 test fixture) to the plural shape. Leave `render_block_display`'s single-arg display path functionally UNCHANGED for the single-blocked-arg case (it operates on `pc.resolved_args`, unaffected by the anchor-type change). Add a fail-closed safeguard: if a genuinely-plural block (more than one blocked arg) reaches `render_block_display` before Phase 16's narration lands, prefer an explicit assert/panic over silently displaying only one of N — do NOT implement multi-arg narration here (that is CONFIRM-04 / Phase 16) and do NOT leave a silent data-dropping TODO.
    Mechanically migrate the singular destructure/`matches!` sites in the three brokerd test files (durable_anchor.rs lines ~201,384,421,455; phase5_dispatch.rs ~120; s9_acceptance.rs ~128,176,435) to the plural shape, preserving each test's assertion intent.
  </action>
  <verify>
    <automated>cargo test -p brokerd</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p brokerd` exits 0.
    - `grep -c 'event.anchor.is_none()' crates/brokerd/src/audit.rs` returns 0 and `grep -c 'anchors.is_empty()' crates/brokerd/src/audit.rs` returns 1.
    - `grep -c '{ anchor, literal }' crates/brokerd/src/server.rs` returns 0 (no singular destructure remains).
    - server.rs writes every blocked literal to the side table (the block-literal write iterates the anchors collection, not a single element): reviewable in the diff and covered by the durable_anchor tests passing.
    - No new DenyReason variant added: `git diff crates/runtime-core/src/executor_decision.rs` in this plan is empty (this plan does not touch that file).
  </acceptance_criteria>
  <done>Broker decision handling, the fail-closed guard, and brokerd tests are plural-aware; brokerd suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the cli consumers and restore a green workspace</name>
  <read_first>
    - cli/caprun/src/worker.rs (singular destructure at line ~204)
    - cli/caprun/tests/confirm.rs (singular site at line ~78)
    - .planning/phases/14-content-sensitive-sink-arg-blocking/14-RESEARCH.md ("Files requiring mechanical updates" exhaustive list — re-grep to catch drift)
  </read_first>
  <files>cli/caprun/src/worker.rs, cli/caprun/tests/confirm.rs</files>
  <action>
    Migrate the remaining singular `BlockedPendingConfirmation { anchor, literal }` (or `{ anchor, .. }`) destructure/`matches!` sites in cli/caprun/src/worker.rs (~line 204) and cli/caprun/tests/confirm.rs (~line 78) to the plural `{ anchors }` shape, preserving each site's original behavior (e.g., worker exit-code / display logic derives from the collection; for the single-blocked-arg path use `anchors[0]`).
    Then run a re-grep sweep across the workspace for any remaining singular-shape site the research pass may not have caught (`grep -rn "BlockedPendingConfirmation" --include="*.rs"` and inspect each for a singular `{ anchor, literal }` destructure) and migrate any stragglers. Confirm the whole workspace builds and passes, and that the architectural invariants gate is green.
  </action>
  <verify>
    <automated>cargo test --workspace --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace --no-fail-fast` exits 0 (workspace fully green — the red state 14-01 left is resolved).
    - `grep -rn 'BlockedPendingConfirmation { anchor, literal }' --include='*.rs' .` returns no matches (no singular destructure survives anywhere).
    - `grep -rn 'BlockedPendingConfirmation { anchor' --include='*.rs' .` returns no matches.
    - `./scripts/check-invariants.sh` exits 0 (Gate 1 `EffectRequest` token absent under crates/, Gate 2 runtime-core purity).
  </acceptance_criteria>
  <done>All cli consumers migrated; `cargo test --workspace` and check-invariants.sh are green; Phase 14's type-shape refactor is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor decision → broker control plane | The plural block decision is consumed by server.rs and persisted; dropping an anchor here loses durably-recorded blocked authority. |
| broker → audit DAG (hash chain) | A type-shape change to `Event` can silently alter the hashed payload bytes of UNRELATED event types. |
| pending confirmation → human confirm UX | A plural block displayed as a single arg would let a human confirm less than they think (deferred to Phase 16, guarded fail-closed here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | `runtime-core::event.rs` serialization | high | mitigate | Preserve `skip_serializing_if = "Vec::is_empty"` so non-block events serialize byte-identically; re-run the unchanged golden-byte fixture to prove the audit hash chain is undisturbed for unrelated event types. |
| T-14-06 | Information Disclosure (unconfirmed authority) | `server.rs` block-literal side-table write | high | mitigate | Persist EVERY blocked arg's literal (iterate the anchors collection), so no blocked value is dropped before the human confirm/deny decision. Covered by durable_anchor tests. |
| T-14-07 | Elevation of Privilege (fail-open) | `audit.rs` Defect-B guard | high | mitigate | Keep the guard fail-closed with the plural shape: reject a `sink_blocked` event whose `anchors` is empty. |
| T-14-08 | Information Disclosure (partial confirm display) | `confirmation.rs::render_block_display` | medium | accept (interim) | Fail-closed assert/panic on a genuinely-plural block rather than silently showing one of N; full multi-arg narration is Phase 16 (CONFIRM-04). Accepted as interim because the single-blocked-arg case (CONTROL-02) is unaffected and no data is silently dropped. |

No package-manager installs in this plan — no supply-chain (T-*-SC) row applies.
</threat_model>

<verification>
Per task: `cargo test -p runtime-core` (Task 1), `cargo test -p brokerd` (Task 2), `cargo test --workspace --no-fail-fast` (Task 3) exit 0.

Phase gate (after this plan): `cargo test --workspace --no-fail-fast` GREEN and `./scripts/check-invariants.sh` GREEN (Gate 1 + Gate 2). This is the point at which the red state left by 14-01 is fully resolved.

Linux-only note: this plan adds no new `#[cfg(target_os = "linux")]` enforcement tests. Existing Linux-gated tests (e.g. brokerd/cli enforcement, s9 live) show "0 passed" on the macOS host — expected, not a gap. To exercise them, run the workspace suite under the Colima+Docker recipe in CLAUDE.md ("Linux-only security tests": `docker run --rm --security-opt seccomp=unconfined -v "$PWD":/work -w /work -e CARGO_TARGET_DIR=/tmp/lt rust:1 cargo test --workspace --no-fail-fast`). The type-shape migration is host-portable, so a green macOS `cargo test --workspace` is sufficient for this plan's gate; the Linux run is confirmatory.
</verification>

<success_criteria>
- The audit Event records ALL blocked anchors (CONTENT-01 durable end-to-end).
- Non-block events serialize byte-identically; golden fixture green.
- audit.rs fails closed on empty `anchors`.
- server.rs persists every blocked literal and builds the plural sink_blocked event.
- `cargo test --workspace --no-fail-fast` and `./scripts/check-invariants.sh` green.
</success_criteria>

<output>
Create `.planning/phases/14-content-sensitive-sink-arg-blocking/14-02-SUMMARY.md` when done. Record: the final `Event.sink_blocked` signature, the render_block_display safeguard chosen, any stragglers the re-grep sweep caught beyond the research list, and confirmation the workspace + check-invariants.sh are green.
</output>
