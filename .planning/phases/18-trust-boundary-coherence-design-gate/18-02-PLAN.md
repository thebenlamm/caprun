---
phase: 18-trust-boundary-coherence-design-gate
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - planning-docs/DESIGN-GATE-RECORD-v1.4.md
  - planning-docs/DESIGN-session-trust-coherence.md
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]

must_haves:
  truths:
    - "A FRESH, independent adversarial reviewer (not the authoring session) reviewed DESIGN-session-trust-coherence.md by TRACING the cited code, not merely reading the doc (DESIGN-01, per DEC-ai-review-satisfies-human-gate)"
    - "planning-docs/DESIGN-GATE-RECORD-v1.4.md records the reviewer's findings, their disposition, and a final gate status of CLEARED"
    - "Every issue the reviewer raised is resolved in DESIGN-session-trust-coherence.md before the gate is marked cleared — no open blocker remains at gate close"
    - "The review confirms each of DESIGN-01..06 is resolved consistently with the three locked scoping decisions (reject-2nd-connection, replay-re-earned-not-CAS, T2-deferred-not-designed)"
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.4.md
  key_links:
    - "The reviewer is fresh/independent (spawned as a separate agent or an explicitly-fresh reviewer pass), not the Plan 18-01 authoring context — a self-review does NOT satisfy the gate (fresh-context-adversarial-review discipline)"
    - "The reviewer traces actual code (server.rs line numbers, executor_decision.rs:144, the three mint arms) to confirm the doc's claims are true, not just internally consistent"
---

<objective>
Run the fresh adversarial gate on `planning-docs/DESIGN-session-trust-coherence.md` and record it in `planning-docs/DESIGN-GATE-RECORD-v1.4.md`, mirroring the v1.2/v1.3 DESIGN-GATE-RECORD precedent. Per `DEC-ai-review-satisfies-human-gate`, an AI-performed adversarial re-read by a FRESH, independent reviewer (not the authoring session) satisfies the human-gate requirement — but ONLY if the reviewer traces the actual cited code and every raised issue is resolved in the doc before the gate closes.

Purpose: This is the gate that unblocks Phase 19. Until `DESIGN-GATE-RECORD-v1.4.md` records status CLEARED with all findings resolved, no `server.rs` change for the trust-coherence fix may begin.
Output: `planning-docs/DESIGN-GATE-RECORD-v1.4.md` (gate record) + any remediation edits to `planning-docs/DESIGN-session-trust-coherence.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/tmp/v1.4-REVIEW-RECONCILIATION.md
@planning-docs/DESIGN-session-trust-coherence.md
@planning-docs/DESIGN-GATE-RECORD-v1.3.md
@planning-docs/DESIGN-GATE-RECORD-v1.2.md
@crates/brokerd/src/server.rs
@crates/runtime-core/src/executor_decision.rs
</context>

## Artifacts this phase produces

- `planning-docs/DESIGN-GATE-RECORD-v1.4.md` (this plan authors it)
- Remediation edits to `planning-docs/DESIGN-session-trust-coherence.md` (this plan, if the reviewer raises issues)

This plan touches ONLY `planning-docs/*.md`. It MUST NOT modify `crates/brokerd/src/server.rs` or any TCB/source file — the fix is Phase 19, gated on this record reading CLEARED.

<tasks>

<task type="auto">
  <name>Task 1: Spawn a fresh, independent adversarial reviewer that TRACES the code and record findings in DESIGN-GATE-RECORD-v1.4.md</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.4.md</files>
  <read_first>
    - `planning-docs/DESIGN-session-trust-coherence.md` — the document under review (authored by Plan 18-01).
    - `planning-docs/DESIGN-GATE-RECORD-v1.3.md` and `planning-docs/DESIGN-GATE-RECORD-v1.2.md` — the precedent format/rigor to match (Revision History, Documents Under Review, Checklist, per-finding disposition, "How to Verify (Human Review Steps)", "Decision", "Gate status").
    - `.planning/tmp/v1.4-REVIEW-RECONCILIATION.md` — the original findings (MAJOR-2, MAJOR-3, MEDIUM-1) the doc had to rule on; the reviewer must confirm each ruling is present AND correct against code.
    - The `adversarial-review` skill and the `fresh-context-adversarial-review` discipline: a self-read of a security design is NOT sufficient — the reviewer must trace CODE.
  </read_first>
  <action>
    Spawn a FRESH, independent adversarial reviewer against `planning-docs/DESIGN-session-trust-coherence.md`. Do NOT self-review from this plan's authoring lineage — the reviewer must be a separate agent/context (per `DEC-ai-review-satisfies-human-gate` and the `fresh-context-adversarial-review` memory: my own read is not enough; spawn a fresh reviewer that traces code).

    Preferred mechanism: use the Task tool to spawn a general-purpose adversarial-review agent. If the Task tool is unavailable in this execution environment, invoke the `adversarial-review` skill, or document in the gate record the exact fresh-reviewer prompt and run it as a distinct, clearly-fresh pass. Whichever mechanism, the reviewer MUST be given: the DESIGN doc, and the cited source files (`crates/brokerd/src/server.rs`, `crates/runtime-core/src/executor_decision.rs`, `crates/brokerd/tests/two_connection_intent_bypass.rs`).

    Instruct the reviewer to TRACE CODE (not just read the doc) and adversarially test each of these:
      - DESIGN-01: Is reject-2nd-connection actually implementable given server.rs's accept loop (lines 98-121) and where per-connection locals are seeded (148, 153-154)? Does the doc's authoritative-gate placement actually prevent a 2nd connection from getting pristine guard(a) + stale-Active status? Is shared-state genuinely named and rejected (not smuggled back in)?
      - DESIGN-02: Is the replay risk actually RE-EARNED in writing (not silently inherited)? Is the amplification bound (trusted/human-typed recipients only; untrusted still Blocks) TRUE against the executor's I2 logic? Does the doc avoid designing a new CAS?
      - DESIGN-03: Does the three-mint-site audit hold against the ACTUAL arms (mint_from_read ~428, mint_from_intent ~949, mint_from_derivation ~849)? Is "only ProvideIntent yields TRUSTED from a supplied string" actually correct? Is the reopening tripwire named?
      - DESIGN-04: Is the literal_sha256 oracle claim true against executor_decision.rs:144? Is the Allowed-vs-Blocked leak real? Does the doc give a concrete ruling on what Phase 20's planner connection sees?
      - DESIGN-05: Does the per-verb capability split actually let a connection hold NO mint verb, and is it coherent with the reject-2nd-connection fix?
      - DESIGN-06: Is guard-(c) genuinely not widened by the fix? Is the compile-exclusion question re-stated without over-committing?
      - Cross-cutting: any way the fix as specified STILL permits an attacker literal to reach a sensitive sink as Allowed? Any locked-decision violation (designs shared state / a CAS / slot-type binding)?

    Capture the reviewer's raw findings. Then create `planning-docs/DESIGN-GATE-RECORD-v1.4.md` in the precedent format: a header (milestone v1.4, document under review, reviewer identity + that it is fresh/independent), a Revision History, a per-requirement Checklist (DESIGN-01..06), a per-finding table (finding, severity, evidence file:line, disposition), a "How to Verify" section, and a "Gate status" line. Set the initial Gate status to BLOCKED/OPEN if any finding needs remediation, or CLEARED only if the reviewer raised nothing. Do NOT fabricate a clean review — record findings honestly.
  </action>
  <acceptance_criteria>
    - `planning-docs/DESIGN-GATE-RECORD-v1.4.md` exists and identifies the reviewer as fresh/independent of the authoring session (names the mechanism: spawned agent / adversarial-review skill / explicit fresh pass).
    - The record contains a Checklist covering all six requirements DESIGN-01..06, each with a pass/fail/needs-remediation disposition.
    - Every finding is recorded with a severity and file:line evidence; the record's Gate status reflects reality (OPEN/BLOCKED if any finding is unresolved, not a fabricated CLEARED).
    - The record shows the reviewer traced code (references to actual server.rs / executor_decision.rs line numbers appear in the findings/verification), not merely a prose re-read.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -qi "fresh\|independent" planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -q "DESIGN-01" planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -q "DESIGN-06" planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -qi "gate status" planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -qi "server.rs\|executor_decision.rs" planning-docs/DESIGN-GATE-RECORD-v1.4.md && echo PASS</automated>
  </verify>
  <done>A fresh, independent, code-tracing adversarial review of the DESIGN doc is complete and its findings + per-requirement checklist + honest gate status are recorded in DESIGN-GATE-RECORD-v1.4.md.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve every raised finding in the DESIGN doc and close the gate to CLEARED</name>
  <files>planning-docs/DESIGN-session-trust-coherence.md, planning-docs/DESIGN-GATE-RECORD-v1.4.md</files>
  <read_first>
    - `planning-docs/DESIGN-GATE-RECORD-v1.4.md` — the findings from Task 1 that must each be resolved.
    - `planning-docs/DESIGN-session-trust-coherence.md` — the doc to remediate.
    - `crates/brokerd/src/server.rs`, `crates/runtime-core/src/executor_decision.rs` — re-consult for any finding that turned on a code fact.
    - `.planning/REQUIREMENTS.md` "Out of Scope" — remediation must NOT resolve a finding by designing shared state, a CAS, or slot-type binding (those are locked out).
  </read_first>
  <action>
    For each finding recorded in `DESIGN-GATE-RECORD-v1.4.md`, resolve it IN `DESIGN-session-trust-coherence.md` by amending the relevant section — never by weakening a ruling to make a finding go away, and never by resolving it in a way that violates a locked scoping decision (no shared-state design, no CAS, no slot-type binding). If a finding is a genuine disagreement where the doc is correct and the reviewer erred, record the rebuttal in the gate record with evidence (the doc/code fact that settles it) rather than editing the doc — but a passing self-argument is not evidence; cite the file:line that decides it.

    If any finding was a BLOCKER (the fix as specified still permits an attacker literal to reach a sensitive sink as Allowed, or a locked-decision violation), re-run the fresh reviewer against the amended doc after remediation and record the second-round result. Iterate until the reviewer raises no unresolved blocker.

    Update `DESIGN-GATE-RECORD-v1.4.md`: append a Revision History entry per remediation round, mark each finding resolved (with the section/edit that resolved it or the rebuttal evidence), and set the final Gate status to CLEARED only when every finding is resolved and no blocker remains. The record must make explicit that CLEARED means Phase 19 may now begin the server.rs change.

    Do NOT touch any source/TCB file. Do NOT mark the phase complete or edit ROADMAP.md/STATE.md (per the standing project mitigation: no phase-18 executor writes phase-completion state — the orchestrator does that after independent verification).
  </action>
  <acceptance_criteria>
    - Every finding in `DESIGN-GATE-RECORD-v1.4.md` has a resolution disposition (resolved-by-edit with the section named, or rebutted-with-evidence citing file:line) — none left open.
    - `DESIGN-GATE-RECORD-v1.4.md`'s final Gate status reads CLEARED and states that Phase 19 may begin.
    - No finding was resolved by designing shared multi-connection state, a CAS/idempotency mechanism, or slot-type binding enforcement (locked out of scope).
    - `DESIGN-session-trust-coherence.md` still names reject-2nd-connection as the chosen fix and still re-earns replay without a CAS after remediation (locked decisions survive remediation).
    - No source/TCB file (`crates/`, `cli/`) was modified; ROADMAP.md/STATE.md were not touched.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "gate status.*cleared\|status:.*cleared\|CLEARED" planning-docs/DESIGN-GATE-RECORD-v1.4.md && grep -qi "reject" planning-docs/DESIGN-session-trust-coherence.md && grep -qi "re-earn" planning-docs/DESIGN-session-trust-coherence.md && git status --porcelain crates/ cli/ | grep -q . && echo "UNEXPECTED_SRC_CHANGE" || echo PASS</automated>
  </verify>
  <done>All reviewer findings are resolved in the DESIGN doc (without violating any locked decision), DESIGN-GATE-RECORD-v1.4.md reads CLEARED and authorizes Phase 19, and no source file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces the review record and remediation edits — markdown only. The boundary it guards is the design gate itself: a fabricated or shallow review that reads CLEARED without genuine code-tracing would let a defective fix spec through to Phase 19's TCB change.

| Boundary | Description |
|----------|-------------|
| Fresh reviewer → gate status | A non-fresh or non-code-tracing review that clears the gate lets an unsound design reach TCB implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Repudiation | Gate record claims CLEARED without a genuinely fresh, code-tracing review | high | mitigate | Task 1 mandates a fresh/independent reviewer that traces cited code; the record must name the mechanism and cite line numbers; honest gate status required |
| T-18-05 | Elevation of privilege | A finding is resolved by weakening a ruling or by designing a locked-out mechanism (shared state / CAS / slot-type binding) | high | mitigate | Task 2 acceptance criteria forbid locked-decision violations and require the reject-2nd-connection + no-CAS rulings to survive remediation |

No package-manager installs occur in this plan (docs only) — the T-{phase}-SC supply-chain checkpoint is not applicable.
</threat_model>

<verification>
- `planning-docs/DESIGN-GATE-RECORD-v1.4.md` exists, identifies a fresh code-tracing reviewer, covers DESIGN-01..06, and reads Gate status CLEARED with every finding resolved.
- `planning-docs/DESIGN-session-trust-coherence.md` still honors all three locked decisions after remediation.
- No source/TCB file modified: `git status --porcelain crates/ cli/` is empty.
- ROADMAP.md / STATE.md untouched by this plan (orchestrator owns phase-completion state).
</verification>

<success_criteria>
The design gate is cleared: a fresh, independent, code-tracing adversarial reviewer examined the DESIGN doc, every raised issue is resolved in the doc without violating a locked decision, and DESIGN-GATE-RECORD-v1.4.md records status CLEARED authorizing Phase 19 to begin the server.rs change.
</success_criteria>

<output>
Create `.planning/phases/18-trust-boundary-coherence-design-gate/18-02-SUMMARY.md` when done.
</output>
