---
phase: 40-cli-compose-env-clear-live-proof
plan: 03
type: execute
wave: 2
depends_on: ["40-02"]
files_modified:
  - scripts/mock-github/server.py
  - scripts/mock-github/certs/github-mock.caprun.test.pem
  - scripts/mock-github/certs/github-mock.caprun.test.key
  - scripts/mock-github/README.md
  - scripts/compose-verify.sh
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "A tiny local HTTPS server answers 201 with a plausible PR JSON to POST /repos/*/pulls, presenting the github-mock.caprun.test cert Plan 40-02's feature trusts."
    - "The compose-verify.sh harness stands up BOTH the Mailpit SMTP sidecar and the mock GitHub HTTPS sidecar on ONE user-defined docker network whose subnet is a public range (so the mock IP passes ssrf_check), maps github-mock.caprun.test to the mock IP inside the rust:1 container, and runs the unprivileged rust:1 verification container with the mock-egress-ca feature enabled."
    - "The harness runs cargo build --workspace BEFORE cargo test (sibling-binary placement) and captures the TRUE exit code before any pipe."
  artifacts:
    - "scripts/mock-github/server.py — stdlib-only Python HTTPS mock (no new dependency)."
    - "scripts/mock-github/certs/* — the PEM cert + key matching Plan 40-02's DER trust anchor."
    - "scripts/compose-verify.sh — the composed Linux verification harness (Mailpit + mock GitHub)."
  key_links:
    - "The mock's cert == Plan 40-02's trust anchor (same self-signed cert), so the broker egress validates the mock over real TLS under the feature."
    - "The public-range subnet + per-container host mapping let github-mock.caprun.test resolve to the mock and pass ssrf_check unchanged; api.github.com is NOT remapped, so the ENV-01 live GET still reaches real GitHub."
---

<objective>
Build the composed Linux verification harness: a stdlib-only mock GitHub HTTPS endpoint plus a compose-verify.sh that runs the full workspace + composed acceptance test against BOTH a Mailpit sidecar and the mock GitHub sidecar, unprivileged, with the mock-egress-ca feature enabled.

Purpose: LIVE-03/LIVE-04 need a live github.pr POST that completes against a mock (40-CONTEXT decisions 3+5) while Mailpit stays up for the email-send regression tests, and the ENV-01 live GET must still reach REAL api.github.com. Per 40-CONTEXT decision 5 (extend the Mailpit harness / add a sibling) and decision 6 (true-exit-before-pipe). This is a sibling of scripts/mailpit-verify.sh (not an edit to it) to avoid destabilizing the existing single-test recipe.
Output: scripts/mock-github/server.py, its cert fixtures + README, and scripts/compose-verify.sh.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-cli-compose-env-clear-live-proof/40-CONTEXT.md
@scripts/mailpit-verify.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stdlib-only mock GitHub HTTPS server + cert fixtures</name>
  <files>scripts/mock-github/server.py, scripts/mock-github/certs/github-mock.caprun.test.pem, scripts/mock-github/certs/github-mock.caprun.test.key, scripts/mock-github/README.md</files>
  <read_first>
    - crates/brokerd/src/sinks/github_pr.rs lines ~96-165 (the pulls URL path repos/OWNER/REPO/pulls and the POST headers/body shape the mock must accept).
    - crates/brokerd/tests/fixtures/mock-egress-ca.der (Plan 40-02) — the mock's PEM cert MUST be the SAME self-signed cert this DER encodes.
    - scripts/mailpit-verify.sh (the sidecar + network conventions to mirror).
  </read_first>
  <action>
    Write scripts/mock-github/server.py using ONLY the Python standard library (http.server + ssl — NO pip install, NO new dependency): bind an HTTPS server on port 443, load the PEM cert+key from the certs/ dir, respond HTTP 201 with a small JSON body resembling a created-PR response (an html_url + number field) to any POST whose path ends in /pulls under a /repos/ prefix, and 404 to anything else. Keep it small (~60 lines). Generate the matching cert fixtures with the SAME offline openssl command used in Plan 40-02 (self-signed, CN and SAN both github-mock.caprun.test, basicConstraints CA:TRUE) — the PEM cert here and the DER anchor in 40-02 encode the SAME certificate; check in both the .pem and the .key (test-only, non-real domain, no production value). Add scripts/mock-github/README.md documenting the exact openssl command so the cert is reproducible and stating the domain is test-only.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/mock-github/server.py').read())" &amp;&amp; test -f scripts/mock-github/certs/github-mock.caprun.test.pem &amp;&amp; test -f scripts/mock-github/certs/github-mock.caprun.test.key &amp;&amp; openssl x509 -in scripts/mock-github/certs/github-mock.caprun.test.pem -noout -text | grep -q 'github-mock.caprun.test'</automated>
  </verify>
  <done>
    Acceptance: server.py parses under stdlib only; the cert fixtures exist and carry SAN github-mock.caprun.test; the checked-in cert matches the DER anchor from Plan 40-02 (same fingerprint); README documents reproduction.
  </done>
</task>

<task type="auto">
  <name>Task 2: compose-verify.sh — Mailpit + mock GitHub sidecars on a public-range network, unprivileged rust:1 run</name>
  <files>scripts/compose-verify.sh</files>
  <read_first>
    - scripts/mailpit-verify.sh in full (network create, sidecar run, IP resolve, the docker_args array, the libssl-dev/pkg-config install, the trap cleanup, and the cargo build --workspace BEFORE cargo test rationale).
    - CLAUDE.md "Linux-only security tests" + the Phase-16+ mailpit mandate + the verification-exit-code-through-pipe note.
    - .planning/phases/40-cli-compose-env-clear-live-proof/40-CONTEXT.md decisions 5 and 6.
  </read_first>
  <action>
    Write scripts/compose-verify.sh (a sibling of mailpit-verify.sh — do NOT edit mailpit-verify.sh). It must: (1) create a user-defined docker network with an explicit PUBLIC-range subnet 203.0.113.0/24 (TEST-NET-3 — not loopback/RFC1918/link-local/CGNAT/metadata/ULA, so ssrf_check accepts a mock IP in it); (2) start the Mailpit sidecar on that network (reuse mailpit-verify.sh's pattern) and resolve its IP for CAPRUN_SMTP_HOST; (3) start the mock GitHub sidecar as a python:3-slim container on that network at a fixed IP 203.0.113.2, mounting scripts/mock-github and running server.py; (4) run the unprivileged rust:1 verification container (--security-opt seccomp=unconfined, NO privileged flag) on the SAME network, adding a host mapping so github-mock.caprun.test resolves to 203.0.113.2 (docker run --add-host), forwarding CAPRUN_SMTP_HOST/PORT and OPENAI_API_KEY (empty-tolerant) and CAPRUN_PLANNER_MODEL (only when set, per the mailpit-verify bug note), and CAPRUN_GITHUB_API_BASE=https://github-mock.caprun.test; (5) install libssl-dev/pkg-config then run the verification command with the mock-egress-ca feature enabled — the DEFAULT command must be `cargo build --workspace && cargo test --workspace --no-fail-fast --features brokerd/mock-egress-ca` (leading build is REQUIRED for sibling-binary placement), overridable via a COMPOSE_VERIFY_CMD env var; (6) trap-cleanup BOTH sidecars unconditionally on EXIT. Do NOT remap api.github.com — the ENV-01 live GET must reach real GitHub. Capture the container run's TRUE exit code BEFORE any pipe (set +e; rc=$?; set -e) and exit non-zero on failure; never assert success through a pipe.
  </action>
  <verify>
    <automated>bash -n scripts/compose-verify.sh &amp;&amp; grep -q '203.0.113' scripts/compose-verify.sh &amp;&amp; grep -q 'add-host' scripts/compose-verify.sh &amp;&amp; grep -q 'mock-egress-ca' scripts/compose-verify.sh &amp;&amp; grep -q 'cargo build --workspace' scripts/compose-verify.sh &amp;&amp; grep -Eq 'rc=\$\?|set \+e' scripts/compose-verify.sh &amp;&amp; grep -q 'seccomp=unconfined' scripts/compose-verify.sh &amp;&amp; ! grep -q 'privileged' scripts/compose-verify.sh</automated>
  </verify>
  <done>
    Acceptance: shell parses; the harness creates a 203.0.113.0/24 network, starts Mailpit + the mock GitHub sidecar, add-hosts github-mock.caprun.test to the mock IP (api.github.com untouched), runs the unprivileged rust:1 container with the mock-egress-ca feature and a leading cargo build --workspace, captures the true exit code before any pipe, and cleans up both sidecars on exit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification container → mock GitHub sidecar | the mock stands in for GitHub's write API; a real live POST crosses this boundary over TLS |
| verification container → real api.github.com | the ENV-01 live GET crosses here over real TLS (must NOT be redirected to the mock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-06 | Spoofing | mock host resolution | medium | mitigate | only github-mock.caprun.test is add-host-mapped to the mock; api.github.com is never remapped, so the ENV-01 live GET reaches real GitHub over webpki-roots TLS. |
| T-40-07 | Tampering | verification harness egress claims | high | mitigate | run is unprivileged (seccomp=unconfined only, no privileged flag); mock IP is in a public range so ssrf_check runs unmodified; true exit code captured before any pipe (never assert on exit 0 through a pipe). |
| T-40-SC | Tampering | python:3-slim / rust:1 base images | low | accept | pinned public Docker base images; mock is stdlib-only (no pip installs); no new package-manager dependency is introduced by this plan. |
</threat_model>

<verification>
- bash -n parses scripts/compose-verify.sh; scripts/mock-github/server.py parses under stdlib.
- The full live composed run is exercised by Plan 40-04's test under this harness and by the orchestrator's closing gate.
- api.github.com is not remapped anywhere in the harness (grep); only github-mock.caprun.test is.
</verification>

<success_criteria>
A stdlib-only HTTPS mock + a sibling compose-verify.sh that stands up Mailpit + the mock on a public-range network, runs the unprivileged rust:1 suite with the mock-egress-ca feature and a leading workspace build, and captures the true exit code before any pipe.
</success_criteria>

<output>
Create `.planning/phases/40-cli-compose-env-clear-live-proof/40-03-SUMMARY.md` when done.
</output>
