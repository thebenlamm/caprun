---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 06
type: execute
wave: 6
depends_on: [51-05]
files_modified:
  - planning-docs/DESIGN-audit-append-concurrency.md
  - planning-docs/DESIGN-GATE-RECORD-v1.10.md
autonomous: true
gap_closure: true
requirements: [LIVE-07, LIVE-08]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A written design note pins the append-at-head concurrency discipline BEFORE any TCB code changes, honouring this project's standing precedent that design-gate docs block TCB code."
    - "The note states unambiguously that verify_chain is correct and that the append path is the bug, so the most attractive wrong turn is closed in writing before an executor can take it."
    - "The note separates the causal parent_id chain from the provenance read_event_id / provenance_chain graph and forbids unifying them."
    - "The note records the measured rusqlite 0.32.1 implicit busy-timeout fact so the D2 fix is not built on a false premise."
    - "Both latent instances of the same defect class carry an explicit written disposition — fixed or deferred — and neither is silently dropped."
    - "A complete production append call-site inventory exists, classified by role, so the fix has a checkable scope and the adversarial trace has a checklist."
  artifacts:
    - path: "planning-docs/DESIGN-audit-append-concurrency.md"
      provides: "The design gate for the D1+D2 coordinated TCB fix: invariant restatement, choke-point contract, transaction discipline, busy-timeout ownership, parent rebinding consequences, atomicity non-regression, latent-instance dispositions, non-goals, acceptance oracle, rollback, and the call-site inventory appendix"
    - path: "planning-docs/DESIGN-GATE-RECORD-v1.10.md"
      provides: "A pending gate entry naming the new DESIGN doc, blocking the audit-append TCB change until a fresh non-self orchestrator-owned adversarial trace clears it"
  key_links:
    - from: "planning-docs/DESIGN-audit-append-concurrency.md"
      to: "crates/brokerd/src/audit.rs append path"
      via: "a call-site inventory that names every production append site by file and line and classifies it as chain continuation, in-transaction, or session root"
      pattern: "append_event|current_chain_head|open_audit_db"
    - from: "planning-docs/DESIGN-GATE-RECORD-v1.10.md"
      to: "Plan 51-07 TCB change"
      via: "a PENDING gate row that Plan 51-08 flips to CLEARED only after the fresh non-self adversarial trace"
      pattern: "DESIGN-audit-append-concurrency"
---

<objective>
Write the design gate that authorises the coordinated D1+D2 fix to the audit append path, and record it as a pending gate. No `crates/brokerd/src/` code changes in this plan.

Purpose: This project's locked precedent (CLAUDE.md: "Two design-gate docs block executor code"; `DESIGN-GATE-RECORD-v1.10.md`: "No multi-step change under `crates/{executor,brokerd,sandbox,runtime-core}` ... may land until this DESIGN doc clears a fresh, non-self, ORCHESTRATOR-owned adversarial code-trace") applies here. D1 is a defect in the tamper-evidence machinery itself; changing it without a written contract is exactly how the two most attractive wrong turns — loosening `verify_chain`, or landing `busy_timeout` alone — get taken.
Output: `planning-docs/DESIGN-audit-append-concurrency.md` plus a PENDING row in the v1.10 gate record.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-GATE-RECORD-v1.10.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md
@crates/brokerd/src/audit.rs
</context>

## Artifacts this phase produces

New file:

- `planning-docs/DESIGN-audit-append-concurrency.md`

Required section headings inside it (exact text, so the gate is mechanically checkable):

- `## 1. Invariant restated: the event parent_id chain is linear`
- `## 2. Two graphs, never conflated`
- `## 3. The choke point`
- `## 4. Transaction discipline`
- `## 5. busy_timeout ownership`
- `## 6. parent_id rebinding and payload consequences`
- `## 7. Atomicity non-regression for the sink_blocked group`
- `## 8. Latent instance dispositions`
- `## 9. Non-goals`
- `## 10. Acceptance oracle and rollback`
- `## Appendix A: production append call-site inventory`

Modified:

- `planning-docs/DESIGN-GATE-RECORD-v1.10.md` — one appended section `## Gate: audit append-at-head concurrency (Phase 51 gap closure)` with status `PENDING`.

Explicitly NOT produced: no source file changes anywhere under `crates/` or `cli/`, no new crate, no new symbol, no `51-LIVE-EVIDENCE.md`, and no edit to `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md`.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real-Linux CLI one-Session run proven end-to-end | 51-05..51-08, then 51-04 unchanged | COVERED | This plan is the gate that authorises the fix that unblocks the goal. |
| REQ | LIVE-07 | `verify_chain` true across the whole one-Session run | 51-06, 51-07 | COVERED | The note pins that the verifier is correct and the append path changes. |
| REQ | LIVE-08 | Attributable I2 Block with the chain still valid | 51-06, 51-07 | COVERED | Section 2 forbids conflating the causal chain with the provenance graph that I2 and M7 ride on. |
| RESEARCH | — | Zero new crates / mint sites / `EffectRequest`; HYG-02 | 51-06 | COVERED | Section 9 records these as non-goals; no code changes in this plan. |
| RESEARCH | — | Package Legitimacy Audit gate | 51-06 | N/A | Documentation-only plan; no package-manager install task. |
| CLAUDE.md | — | Design-gate docs block TCB code; TCB is Rust; terminology locked | 51-06 | COVERED | The whole plan exists to honour this precedent. |
| DEFECT | D1 | Chain forks after an external append | 51-06 | COVERED | Sections 1, 3, 4, 6. |
| DEFECT | D2 | Cross-process write collision handling | 51-06 | COVERED | Sections 4, 5 — designed jointly with D1, never alone. |
| DEFECT | latent-1 | `record_github_grant` non-transactional read-then-append (`audit.rs:546-561`) | 51-06 | COVERED | Section 8 must give it an explicit disposition. |
| DEFECT | latent-2 | Dual-connection same-seed head (`server.rs:307-382`) | 51-06 | COVERED | Section 8 must give it an explicit disposition. |

<tasks>

<task type="auto">
  <name>Task 1: Write the append-at-head concurrency design note</name>
  <precondition>`crates/brokerd/tests/audit_chain_fork_regression.rs` exists (Plan 51-05 landed), because Section 10 cites it by test name as the acceptance oracle.</precondition>
  <files>planning-docs/DESIGN-audit-append-concurrency.md</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — the authoritative defect characterisation, the two named wrong turns, and the latent-instance list
    - CLAUDE.md — the design-gate precedent, the TCB-is-Rust rule, HYG-02, and the locked terminology
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — the exact gate-discipline wording and record format this note must slot into
    - planning-docs/DESIGN-security-hardening.md — HARDEN-02's `chain_anchor` head+count MAC contract that must survive unchanged
    - crates/brokerd/src/audit.rs lines 712-731 — `open_audit_db` and the WAL-only pragma set
    - crates/brokerd/src/audit.rs lines 955-1045 — `append_event`: the `parent_hash` parameter, `compute_event_hash` over the serialized payload, the `events` INSERT and the `chain_anchor` upsert
    - crates/brokerd/src/audit.rs lines 1199-1241 — `current_chain_head` and its explicit "callers MUST gate on verify_chain FIRST" caveat
    - crates/brokerd/src/audit.rs lines 1243-1300 — `verify_chain`'s recursive-CTE walk and the anchor cross-check that provides tail-truncation detection
    - crates/brokerd/src/audit.rs lines 534-564 — `record_github_grant`'s read-then-append shape
    - crates/brokerd/src/server.rs lines 304-384 — the accept loop's per-connection `initial_last_event_id` / `initial_last_event_hash` seeding
    - crates/brokerd/src/server.rs lines 542-560 and 1017-1059 — the in-memory head threading and the sink_blocked append + blocked-literal + pending-confirmation group
    - crates/brokerd/src/server.rs lines 938-982 — the two comments that already state the causal head is never `read_event_id`
    - crates/brokerd/src/confirmation.rs lines 760-810 and 1077-1096 — `current_chain_head_or_bail` and the `confirm_granted` append
  </read_first>
  <action>
    Create `planning-docs/DESIGN-audit-append-concurrency.md` using the eleven exact section headings listed under "Artifacts this phase produces". Keep it a design contract, not an implementation: name functions, SQL statements, pragma names and file:line anchors, but do not paste implementation bodies.

    Section 1 must state that the per-session event `parent_id` chain is LINEAR BY DESIGN, cite `audit.rs:1203-1219` and `confirmation.rs:777-783` and the structural incompatibility of the MAC'd `chain_anchor` (single `head_event_id` + `event_count`) with branches, and record explicitly that loosening `verify_chain` is FORBIDDEN because it would delete HARDEN-02's tail-truncation detection. State the conclusion in one line: the verifier is correct; the append path is the bug.

    Section 2 must state that the causal `parent_id` chain and the provenance graph (`read_event_id`, `provenance_chain`, `SinkBlockedAnchor`) are two different graphs that are never equated, cite the existing in-code statements at `server.rs:941` and `server.rs:973`, and record as a hard rule that this change must not read, write, derive or unify `read_event_id` or `provenance_chain` — I2 and M7 ride on that graph and a fix that "unifies the two graphs" silently destroys I2.

    Section 3 must pin the choke point. The pinned direction — carried in from Plan 51-05's committed contention oracle and to be justified in this section, not relitigated — is that the SINGLE public append entry point `brokerd::audit::append_event` itself becomes the transactional append-at-head, keeping its current name and parameter list. Record the three reasons: it leaves all 141 existing call sites compiling unchanged; it removes the forking capability rather than adding a parallel safe path beside a still-dangerous one; and it subsumes both latent instances for free. State the new published contract precisely: the durable head read inside the transaction is AUTHORITATIVE, the caller-supplied `parent_hash` argument becomes advisory, and the return value continues to be the new row's hash. If a variant placement is chosen instead, this section must show how the public API remains incapable of forking; a design in which a raw forking append survives as a public function is rejected by this gate.

    Section 4 must pin the transaction discipline: one `BEGIN IMMEDIATE` (rusqlite `TransactionBehavior::Immediate` via `transaction_with_behavior`) wrapping the head read, the `events` INSERT and the `chain_anchor` upsert, so the write lock is taken up front and the parent is chosen INSIDE the transaction — `parent_id` is serialized into the hashed payload, so a parent chosen outside is a parent that can go stale between choice and insert. It must also pin the re-entrancy rule for call sites that already hold a transaction, naming `server.rs:1184`, `server.rs:1557` and `confirmation.rs:1163`: state whether the append detects an existing transaction via `rusqlite::Connection::is_autocommit` and joins it, and state the accompanying requirement that each of those enclosing transactions must itself be started IMMEDIATE (they use the default DEFERRED behaviour today, which is precisely the read-then-upgrade shape SQLite refuses to retry).

    Section 5 must record the MEASURED fact, not the assumption: `rusqlite` 0.32.1 already calls `sqlite3_busy_timeout(db, 5000)` unconditionally in `InnerConnection::open_with_flags` (`inner_connection.rs:119`), so the absence of the token from this repository did NOT mean a zero timeout, and any test asserting a non-zero pragma value today would be a false red. State the consequence for the fix: `open_audit_db` sets the timeout EXPLICITLY so the guarantee is TCB-owned and survives a driver upgrade, and name the constant the fix will introduce; and state plainly that the timeout is NOT what fixes D1 — an immediate transaction is — so the two must land in the same change, because a timeout alone converts the crash into a successfully committed fork.

    Section 6 must state what rebinding the parent inside the transaction means for callers: the event's `id` is unchanged, but the PERSISTED `parent_id` (and therefore the serialized payload the MAC covers) is the durable head rather than whatever the caller constructed. Record the derived rule: no caller may assert on its locally constructed `parent_id` after the append, and the fix must audit for any existing assertion of that shape.

    Section 7 must state whether the sink_blocked group at `server.rs:1017-1059` (event append, then every `insert_blocked_literal`, then `insert_pending_confirmation`) becomes more, less or equally atomic. Today those are three separate autocommit statements executed under one mutex. The design must record that atomicity MUST NOT regress, and pin whether the group is hoisted into a single IMMEDIATE transaction or deliberately left as today with a stated reason.

    Section 8 must give both latent instances an explicit, written disposition — never a silent drop. For `record_github_grant` (`audit.rs:546-561`, a read-then-append pair outside any transaction) and for the dual-connection same-seed head (`server.rs:307-382`, where both connections receive the same `initial_last_event_id` / `initial_last_event_hash` and the Planner role is documented unused): state for each whether it is FIXED BY CONSTRUCTION under the Section 3 choke point, FIXED EXPLICITLY, or DEFERRED with a reason and a tracking note. If a disposition is "fixed by construction", the section must say what would have to be true for that to be false, so the adversarial trace has something to attack.

    Section 9 must list the non-goals: `verify_chain` unchanged; the MAC scheme, `EVENT_MAC_DOMAIN` / `ANCHOR_MAC_DOMAIN` and `chain_anchor` semantics unchanged; `current_chain_head`'s query unchanged; the provenance graph untouched; executor I2 untouched; policy untouched; zero new crates, zero new mint sites, no `EffectRequest` token (HYG-02, invariant Gates 1 and 3); no change to `caprun run` output; and no re-planning or weakening of Plan 51-04.

    Section 10 must name the acceptance oracle by test path and test name — `crates/brokerd/tests/audit_chain_fork_regression.rs`, `broker_append_after_external_grant_must_not_fork_chain` and `contended_appends_from_independent_connections_stay_linear` — state that both must go from failing to passing without being edited, and state the rollback: the change is a single revertible commit range confined to `crates/brokerd/src/`, rated `costly` rather than one-way because the audit database format and the MAC scheme do not change, so a revert leaves existing databases readable.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; for h in "## 1. Invariant restated: the event parent_id chain is linear" "## 2. Two graphs, never conflated" "## 3. The choke point" "## 4. Transaction discipline" "## 5. busy_timeout ownership" "## 6. parent_id rebinding and payload consequences" "## 7. Atomicity non-regression for the sink_blocked group" "## 8. Latent instance dispositions" "## 9. Non-goals" "## 10. Acceptance oracle and rollback"; do grep -qxF "$h" planning-docs/DESIGN-audit-append-concurrency.md || { echo "MISSING SECTION: $h"; exit 1; }; done &amp;&amp; grep -qF 'BEGIN IMMEDIATE' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; grep -qF 'read_event_id' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; grep -qF 'inner_connection.rs:119' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; grep -qF 'record_github_grant' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; grep -qF 'audit_chain_fork_regression' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; ! git diff --name-only | grep -qE '^(crates|cli)/' &amp;&amp; ./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `planning-docs/DESIGN-audit-append-concurrency.md` exists and contains all ten numbered `##` headings verbatim as listed above.
    - Section 1 contains the literal file anchors `audit.rs:1203-1219` and `confirmation.rs:777-783`, and states in one sentence that `verify_chain` is correct and the append path is the bug.
    - Section 2 names `read_event_id` and `provenance_chain` and states the non-unification rule as a hard rule, citing `server.rs:941` and `server.rs:973`.
    - Section 3 names `append_event` as the single public append entry point and states that no public forking append survives the change.
    - Section 4 contains `BEGIN IMMEDIATE` and names all three enclosing-transaction sites `server.rs:1184`, `server.rs:1557`, `confirmation.rs:1163`.
    - Section 5 contains `inner_connection.rs:119` and the value `5000`, and states that the timeout alone does not fix D1.
    - Section 8 gives a one-word disposition (`FIXED BY CONSTRUCTION`, `FIXED EXPLICITLY` or `DEFERRED`) for each of `record_github_grant` and the dual-connection same-seed head; neither is absent.
    - Section 10 names both test function names from `crates/brokerd/tests/audit_chain_fork_regression.rs`.
    - `git diff --name-only` lists no path under `crates/` or `cli/` — this plan changes zero source files.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <reversibility rating="costly">The design note pins the shape of a TCB change to the tamper-evidence machinery; the note itself is a document and freely editable, but re-deciding the choke-point placement after Plan 51-07 lands would invalidate the committed acceptance oracle and the adversarial trace.</reversibility>
  <done>The append-at-head concurrency discipline is written down and gates the TCB fix, with both wrong turns explicitly closed in prose.</done>
</task>

<task type="auto">
  <name>Task 2: Build the append call-site inventory and open a PENDING gate record entry</name>
  <precondition>`planning-docs/DESIGN-audit-append-concurrency.md` exists with its ten numbered sections (Task 1 complete).</precondition>
  <files>planning-docs/DESIGN-audit-append-concurrency.md, planning-docs/DESIGN-GATE-RECORD-v1.10.md</files>
  <read_first>
    - planning-docs/DESIGN-audit-append-concurrency.md — the sections the appendix must be consistent with, especially Sections 3, 4 and 8
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md lines 1-45 — the gate-discipline wording, the reviewer-independence table format and the revision-history table format to mirror
    - crates/brokerd/src/server.rs — every `append_event` call site and the enclosing `transaction()` sites
    - crates/brokerd/src/confirmation.rs — `append_digest_mismatch_event`, the `confirm_granted` append and the in-transaction attempt-ledger append
    - crates/brokerd/src/quarantine.rs — the `parent_hash` threading in `mint_from_read`, `mint_from_derivation`, `mint_from_exec` and `mint_from_http`
    - crates/brokerd/src/policy.rs — the session-root and `policy_bound` appends
    - crates/brokerd/src/sinks/ — the terminal sink event appends (`file_write.rs`, `file_create.rs`, `process_exec.rs`, `git_commit.rs`, `git_push.rs`, `github_pr.rs`, `email_smtp.rs`, `http_write.rs`)
  </read_first>
  <action>
    Append `## Appendix A: production append call-site inventory` to `planning-docs/DESIGN-audit-append-concurrency.md`. Derive the list mechanically — run a repository search for `append_event(` restricted to `crates/*/src/` and `cli/*/src/`, excluding any line at or after a file's own `#[cfg(test)]` marker and any path containing `/tests/` — and record every remaining site in a table with columns: file, line, calling function, role, and required treatment.

    Classify each site's role as exactly one of: `session-root` (parent is genuinely `None` on an empty session, e.g. `policy.rs`'s `session_created`); `chain-continuation` (passes a head hash and must inherit the choke point's in-transaction head read); or `in-transaction` (the connection handle is a `Transaction`, so the enclosing transaction owns the boundary and must be started IMMEDIATE). Give the required treatment per row: `inherits choke point (no call-site edit)`, `enclosing transaction must become IMMEDIATE`, or `explicit note` with the reason.

    Add a short paragraph under the table stating the total count, the count per role, and the explicit statement that no call site outside this table exists in production code — the adversarial trace in Plan 51-08 uses this table as its completeness checklist, so an omission here is a hole in the review.

    Then append a new section to `planning-docs/DESIGN-GATE-RECORD-v1.10.md` titled `## Gate: audit append-at-head concurrency (Phase 51 gap closure)`. Mirror the existing record's structure: the DESIGN doc under review (`planning-docs/DESIGN-audit-append-concurrency.md`), the requirements gated (LIVE-07, LIVE-08 and the standing DESIGN-20 fresh-non-self-adversarial-trace obligation), a `**Status:** ⏳ PENDING` line stating that no change under `crates/brokerd/src/audit.rs`, `server.rs` or `confirmation.rs` for this defect class may be declared cleared until an orchestrator-owned fresh non-self adversarial code-trace runs against the landed diff, an empty reviewer-identity table with the field names present and values `TBD`, and an empty revision-history table. Do NOT pre-fill any reviewer, finding, date or verdict — Plan 51-08 owns those, and pre-filling them would be exactly the self-review this gate exists to prevent. State in the section body that a gsd-executor self-read is not clearance.
  </action>
  <verify>
    <automated>grep -qxF '## Appendix A: production append call-site inventory' planning-docs/DESIGN-audit-append-concurrency.md &amp;&amp; grep -qxF '## Gate: audit append-at-head concurrency (Phase 51 gap closure)' planning-docs/DESIGN-GATE-RECORD-v1.10.md &amp;&amp; grep -qF 'PENDING' planning-docs/DESIGN-GATE-RECORD-v1.10.md &amp;&amp; test "$(grep -c 'audit.rs\|server.rs\|confirmation.rs\|quarantine.rs\|policy.rs\|sinks/' planning-docs/DESIGN-audit-append-concurrency.md)" -ge 15 &amp;&amp; ! git diff --name-only | grep -q '^crates/' &amp;&amp; ! git diff --name-only | grep -q '^cli/' &amp;&amp; ./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - Appendix A exists as a markdown table and every row carries a file, a line number, a calling function, one of the three role values, and a required treatment.
    - The appendix names at least these files: `crates/brokerd/src/audit.rs`, `crates/brokerd/src/server.rs`, `crates/brokerd/src/confirmation.rs`, `crates/brokerd/src/quarantine.rs`, `crates/brokerd/src/policy.rs`, and at least four files under `crates/brokerd/src/sinks/`.
    - The three `in-transaction` sites `server.rs:1184`, `server.rs:1557` and `confirmation.rs:1163` each appear with the treatment requiring an IMMEDIATE enclosing transaction.
    - The paragraph under the table states a total count and a per-role count and asserts completeness for production code.
    - `planning-docs/DESIGN-GATE-RECORD-v1.10.md` contains the new gate section with status PENDING, a reviewer table whose values are `TBD`, and an empty revision-history table — no reviewer identity, no finding, no verdict, no date is pre-filled.
    - `git diff --name-only` lists no path under `crates/` or `cli/`.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>The fix has a mechanically derived scope checklist and a pending, un-prefilled gate record that only an independent trace can clear.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: **high**. This plan changes no executable code; its threats are process-integrity threats against the gate that authorises a TCB change.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Design note → TCB implementation authority | The note is what authorises a change to the tamper-evidence machinery; an incomplete or self-cleared note is an unauthorised TCB change wearing a document. |
| Author context → reviewer context | A gsd-executor that writes the note and also fills in the clearance is a self-review; the standing precedent forbids it. |
| Call-site inventory → adversarial trace checklist | An omitted call site is a hole the independent review will not know to look for. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-06-01 | Elevation of privilege | design-gate clearance | high | mitigate | Task 2 forbids pre-filling reviewer identity, findings, verdict or date; the gate row ships as PENDING and only Plan 51-08's orchestrator-owned trace flips it. |
| T-51-06-02 | Tampering | HARDEN-02 tail-truncation detection | high | mitigate | Section 1 records loosening `verify_chain` as FORBIDDEN in writing, with the anchor head+count incompatibility argument, before any executor can reach for it. |
| T-51-06-03 | Tampering | I2 / M7 provenance graph | high | mitigate | Section 2 states the non-unification rule as a hard rule and names `read_event_id` and `provenance_chain` explicitly, so a "unify the graphs" simplification is out of contract. |
| T-51-06-04 | Spoofing | the D2 premise | high | mitigate | Section 5 records the measured rusqlite 0.32.1 implicit 5000 ms timeout so the fix and its tests are not built on the false premise of a zero timeout. |
| T-51-06-05 | Repudiation | latent-instance handling | medium | mitigate | Section 8 requires a written disposition for both latent instances; a silent drop fails the acceptance criteria. |
| T-51-06-06 | Information disclosure | design note contents | low | accept | Repository-internal architecture documentation only; no credentials, no literals, no customer data. |
| T-51-06-SC | Tampering | package installs | low | accept | Documentation-only plan; zero package-manager invocations, zero build-graph change. |
</threat_model>

<verification>
- Section-heading presence check over `planning-docs/DESIGN-audit-append-concurrency.md` (all ten numbered sections plus Appendix A).
- Content anchors: `BEGIN IMMEDIATE`, `read_event_id`, `inner_connection.rs:119`, `record_github_grant`, `audit_chain_fork_regression`.
- `planning-docs/DESIGN-GATE-RECORD-v1.10.md` carries a PENDING, un-prefilled gate row.
- `git diff --name-only` lists no path under `crates/` or `cli/` for either task.
- `./scripts/check-invariants.sh` Gates 1-6 green.
- NOT run here: any cargo test that would change (Plan 51-07 owns the fix), `cargo test --workspace` (CLAUDE.md Mailpit rule, Docker absent), `scripts/compose-verify.sh` (Docker absent; Plan 51-04 owns it).
</verification>

<success_criteria>
- The design note exists with all eleven required sections and closes both named wrong turns in writing.
- The causal `parent_id` chain and the provenance `read_event_id` graph are explicitly separated and the non-unification rule is stated as a hard rule.
- The rusqlite implicit-busy-timeout measurement is recorded, so D2's fix is correctly scoped.
- Both latent instances carry explicit dispositions.
- A complete, mechanically derived production call-site inventory exists.
- The v1.10 gate record carries a PENDING, un-prefilled entry for this gate.
- Zero source files changed.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-06-SUMMARY.md` when done. Do NOT create `51-LIVE-EVIDENCE.md`, do NOT edit `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md`, and do NOT modify `51-04-PLAN.md` — it is re-run UNCHANGED after this gap closure lands.
</output>
