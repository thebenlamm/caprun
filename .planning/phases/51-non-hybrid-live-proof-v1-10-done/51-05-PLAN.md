---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - crates/brokerd/tests/audit_chain_fork_regression.rs
  - cli/caprun/tests/live_acceptance_v1_10_cli.rs
autonomous: true
gap_closure: true
requirements: [LIVE-07, LIVE-08]

user_setup:
  - service: host-build-toolchain
    why: "brokerd links lettre -> native-tls -> openssl-sys; without pkg-config and the OpenSSL development headers NO cargo command in this workspace can build, so every automated verify in Plans 51-05..51-08 is unrunnable. This is broken window 3."
    dashboard_config:
      - task: "Install the OpenSSL build prerequisites as root: sudo apt-get install -y pkg-config libssl-dev"
        location: "The execution host shell (Ubuntu/Debian). Fedora equivalent: sudo dnf install -y pkgconf-pkg-config openssl-devel"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed, host-runnable (non-cfg-linux, non-Docker) regression test reproduces D1 deterministically: after an external append reads the durable head, a broker append off its stale in-memory head forks the chain and verify_chain returns false."
    - "A committed, host-runnable contention regression proves the D1+D2 pair: independent connections appending to one session with a stale supplied parent must all succeed AND leave exactly one chain leaf with verify_chain true — a state a busy_timeout alone cannot reach."
    - "Both new tests FAIL on the pre-fix tree, and the failure message names the fork (leaf count) rather than only a bare boolean."
    - "The LIVE-08 acceptance body no longer gates on a stdout substring that only the separate review/confirm/deny processes can print, so its durable-anchor attribution assertions execute."
    - "Both LIVE bodies surface broker/worker stderr on the sidecar-failure path instead of discarding it."
    - "The Linux-gated, feature-gated LIVE module compiles on this host without Docker, closing the cfg-linux blindness that let defects F1 and F2 ship."
  artifacts:
    - path: "crates/brokerd/tests/audit_chain_fork_regression.rs"
      provides: "Two red regression tests for D1 (sequential external-append fork) and D1+D2 (multi-connection contention), with no target_os or feature cfg gate"
    - path: "cli/caprun/tests/live_acceptance_v1_10_cli.rs"
      provides: "D3 stdout-assertion removal and D4 stderr-join reordering in both LIVE-07 and LIVE-08 bodies"
  key_links:
    - from: "crates/brokerd/tests/audit_chain_fork_regression.rs"
      to: "crates/brokerd/src/audit.rs append/verify path"
      via: "public brokerd::audit API only (append_event, record_github_grant, current_chain_head, verify_chain) so the tests compile against the pre-fix tree and go green only when the append path is corrected"
      pattern: "verify_chain|current_chain_head|record_github_grant"
    - from: "cli/caprun/tests/live_acceptance_v1_10_cli.rs LIVE-08 body"
      to: "durable github.pr/body SinkBlockedAnchor assertions"
      via: "removal of the earlier-firing stdout substring gate so the anchor/provenance assertions are reached"
      pattern: "read_event_id|provenance_chain"
---

<objective>
Make the four surviving Phase 51 defects reproducible and diagnosable on a Docker-less host, before any TCB code changes. Land a committed RED regression for D1 (audit hash-chain fork after an external append) and for the D1+D2 pair (contended appends), and repair the LIVE harness so a future failing run reports the real cause (D4) and actually executes LIVE-08's attribution assertions (D3).

Purpose: The two already-fixed defects (F1, F2) shipped because the Linux/feature-gated test bodies were never compiled or executed on the development host, and the remaining blocker D1 was discovered only by burning a provisioned EC2 host. This plan converts the next real-Linux run from discovery into confirmation.
Output: One new brokerd integration-test binary carrying two failing-today regressions, and a corrected LIVE acceptance module that compiles under its Linux + fixture features on this host.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-VERIFICATION.md
@crates/brokerd/src/audit.rs
@cli/caprun/tests/live_acceptance_v1_10_cli.rs
@crates/brokerd/tests/durable_anchor.rs
</context>

## Artifacts this phase produces

New file:

- `crates/brokerd/tests/audit_chain_fork_regression.rs`

New symbols in that file:

- `const TEST_KEY: &[u8]` — 32-byte MAC key literal, mirroring the existing brokerd test convention.
- `fn unique_audit_db(tag: &str) -> std::path::PathBuf` — file-backed temp DB path helper (`std::env::temp_dir()` + `Uuid::new_v4()`; brokerd has no `tempfile` dev-dependency).
- `fn leaf_count(conn: &rusqlite::Connection, session_id: &str) -> i64` — counts events that are no other event's `parent_id` within the session.
- `fn seed_session_root(conn, session_id) -> (uuid::Uuid, String)` — appends the `session_created` root and returns `(id, hash)`.
- `#[test] fn broker_append_after_external_grant_must_not_fork_chain()` — D1 sequential reproducer.
- `#[test] fn contended_appends_from_independent_connections_stay_linear()` — D1+D2 contention reproducer.

Modified (no new symbols):

- `cli/caprun/tests/live_acceptance_v1_10_cli.rs` — assertion removal (D3) and join reordering (D4) inside `live_07_cli_multi_node_one_session_verify_chain` and `live_08_cli_mid_loop_i2_block_genuine_taint`.

Explicitly NOT produced by this plan: no new crate, no new cargo feature, no new dev-dependency, no new CLI flag, no new environment variable, no new mint site, no `EffectRequest` token, no change to any file under `crates/brokerd/src/`, no `51-LIVE-EVIDENCE.md`, and no edit to `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, or `.planning/WINDOWS.md`.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real-Linux CLI one-Session run (success + mid-loop I2 Block) proven end-to-end | 51-05..51-08, then 51-04 re-run unchanged | COVERED | This plan makes the blocking failure reproducible without Docker; 51-04 owns the authoritative proof and is NOT re-planned. |
| REQ | LIVE-07 | One-Session CLI success with `verify_chain` true | 51-05, 51-07 | COVERED | D1 makes `verify_chain` deterministically false on the LIVE-07 path; the red test pins it. |
| REQ | LIVE-08 | Attributable mid-loop I2 Block, no effect, chain still valid | 51-05, 51-07 | COVERED | D3 removal lets LIVE-08's real attribution assertions execute for the first time. |
| RESEARCH | — | Zero new crates / packages / mint sites / `EffectRequest` (HYG-02) | 51-05 | COVERED | Tests use existing dev-dependencies only. |
| RESEARCH | — | Package Legitimacy Audit gate | 51-05 | N/A | No npm/pip/cargo install task exists in this plan; the only host install is an OS package pair declared under `user_setup` and executed by the human. |
| RESEARCH | — | Genuine (non-stapled) taint asserts as the LIVE-08 oracle | 51-05 | COVERED | Task 3 preserves and unblocks the durable-anchor assertions rather than the stdout grep. |
| CONTEXT | — | No `51-CONTEXT.md` exists for this phase | — | N/A | Authority is 51-BLOCKING-DEFECTS.md + 51-VERIFICATION.md + the gap-closure brief. |
| DEFECT | D1 | Audit hash chain forks after any external append | 51-05 (red), 51-07 (fix) | COVERED | Task 1 + Task 2. |
| DEFECT | D2 | No repo-owned `busy_timeout`; cross-process collision handling | 51-05 (red), 51-07 (fix) | COVERED | Task 2 — deliberately paired with D1 so a bare timeout cannot pass it. |
| DEFECT | D3 | LIVE-08 asserts a string the driver cannot print | 51-05 | COVERED | Task 3. |
| DEFECT | D4 | Harness discards broker/worker stderr on the failure path | 51-05 | COVERED | Task 3, landed early so any future failing run is diagnostic. |

<tasks>

<task type="tracer">
  <name>Task 1: Commit a host-runnable RED regression that reproduces the D1 chain fork</name>
  <precondition>`pkg-config --version` and `cargo --version` both succeed on this host, and `ls /usr/include/openssl/ssl.h` resolves — without the OpenSSL build prerequisites no cargo command in this workspace can link, and the verify below cannot run. If unmet, halt and surface the `user_setup` item; do not weaken or skip the verify.</precondition>
  <files>crates/brokerd/tests/audit_chain_fork_regression.rs</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md — the authoritative D1 reproducer steps and the standing prohibition on loosening `verify_chain`
    - crates/brokerd/src/audit.rs lines 534-564 — `record_github_grant`, the external appender that reads a fresh durable head
    - crates/brokerd/src/audit.rs lines 955-1045 — `append_event`, its `parent_hash` parameter and the `chain_anchor` upsert
    - crates/brokerd/src/audit.rs lines 1199-1241 — `current_chain_head` and its documented single-leaf/linear-chain contract
    - crates/brokerd/src/audit.rs lines 1243-1300 — `verify_chain`'s recursive-CTE walk and HARDEN-02 anchor cross-check
    - crates/brokerd/tests/durable_anchor.rs lines 55-80 — the established file-backed unique temp-DB helper convention (brokerd has no `tempfile` dev-dependency)
    - crates/brokerd/tests/audit_dag.rs — the established `TEST_KEY` + append/verify integration-test shape
  </read_first>
  <behavior>
    - Test 1 (`broker_append_after_external_grant_must_not_fork_chain`): a session whose events are appended by a broker connection that remembers its head in memory, interleaved with one out-of-band connection that reads the durable head, must end with `verify_chain == true` and exactly one chain leaf.
    - Edge case pinned by the same test: the out-of-band append is performed by the real production function `record_github_grant`, not by test-local SQL, so the reproducer tracks the shipped `caprun grant` path rather than a synthetic one.
    - The failure message must report the observed leaf count and the parent id that acquired two children, so a future failing run is self-describing.
  </behavior>
  <action>
    Create `crates/brokerd/tests/audit_chain_fork_regression.rs` as a plain Cargo integration-test binary for the `brokerd` package. It MUST carry NO `#[cfg(target_os = "linux")]` and NO `#[cfg(feature = ...)]` gate anywhere in the file — the whole point is that this defect needs no kernel feature, no Docker and no cargo feature, and cfg-gated blindness is what let defects F1 and F2 reach a provisioned host. Do not add any dev-dependency; use only `brokerd`, `runtime_core`, `rusqlite`, `uuid` and `chrono`, all already present in the brokerd dev-dependency set.

    Add module-level helpers: a 32-byte `TEST_KEY` byte-string literal; `unique_audit_db(tag)` returning `std::env::temp_dir().join(format!("caprun_chainfork_{tag}_{}.db", uuid::Uuid::new_v4()))` (file-backed, never `:memory:` — two connections must share one database); `seed_session_root(conn, session_id)` which builds a `runtime_core::Event::new` with `event_type` `session_created`, parent `None`, empty taint, appends it via `brokerd::audit::append_event(conn, TEST_KEY, &event, None)` and returns `(event.id, hash)`; and `leaf_count(conn, session_id)` which runs `SELECT COUNT(*) FROM events WHERE session_id = ?1 AND id NOT IN (SELECT parent_id FROM events WHERE session_id = ?1 AND parent_id IS NOT NULL)`.

    Write `#[test] fn broker_append_after_external_grant_must_not_fork_chain()` following 51-BLOCKING-DEFECTS.md D1 verbatim: open connection A via `brokerd::audit::open_audit_db` on the unique path (this is the broker); seed the session root and keep `(root_id, root_hash)` in local variables exactly as `handle_connection` threads `last_event_id` / `last_event_hash`; append event E1 of type `plan_node_evaluated` through A parented on that remembered head and overwrite the local variables with E1's id and hash; then open a SECOND connection B via `open_audit_db` on the SAME path (this is the out-of-band `caprun grant` process) and call `brokerd::audit::record_github_grant(&b, TEST_KEY, &session_id_string)` so the external appender reads the durable head itself; then, back on connection A and WITHOUT re-reading the durable head, append event E3 of type `plan_node_evaluated` using the still-remembered E1 id and E1 hash. Assert `leaf_count == 1` first, with a message naming the observed count and E1's id as the parent that gained two children, and then assert `brokerd::audit::verify_chain(&a, &session_id_string, TEST_KEY)` is `true`. Both assertions must be genuine requirements, not `should_panic` inversions — this test is expected to FAIL on the current tree and to pass unchanged once Plan 51-07 lands.

    Delete the temp database file at the end of the happy path only; a failing run must leave the artifact behind for inspection. Add a file-level doc comment recording that this file is the committed RED oracle for D1, that it must never be gated behind a target_os or feature cfg, and that `verify_chain` is correct by design so the append path — never the verifier — is what may change to make it pass.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p brokerd --test audit_chain_fork_regression --no-run &amp;&amp; ! cargo test -p brokerd --test audit_chain_fork_regression -- --exact broker_append_after_external_grant_must_not_fork_chain</automated>
  </verify>
  <acceptance_criteria>
    - `crates/brokerd/tests/audit_chain_fork_regression.rs` exists and `grep -cE '#\[cfg\((target_os|feature)' crates/brokerd/tests/audit_chain_fork_regression.rs` returns `0`.
    - `cargo test -p brokerd --test audit_chain_fork_regression --no-run` exits 0 (the target compiles on this host with no Docker and no extra cargo feature).
    - `cargo test -p brokerd --test audit_chain_fork_regression -- --exact broker_append_after_external_grant_must_not_fork_chain` exits NON-ZERO on the pre-fix tree, and its output names the observed leaf count.
    - `grep -c 'should_panic' crates/brokerd/tests/audit_chain_fork_regression.rs` returns `0` — the test asserts the requirement directly, it does not encode the bug as expected behavior.
    - The test body calls `brokerd::audit::record_github_grant` (the production external appender) and `brokerd::audit::verify_chain`; it contains no `UPDATE events`, no `DELETE FROM events` and no direct `INSERT INTO events`.
    - No file under `crates/brokerd/src/` or `cli/caprun/src/` is modified by this task.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>The D1 chain fork is a committed, host-runnable, deterministically failing test rather than an EC2-only discovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the contention RED regression that D2 alone cannot satisfy</name>
  <precondition>Task 1's `crates/brokerd/tests/audit_chain_fork_regression.rs` exists and compiles via `cargo test -p brokerd --test audit_chain_fork_regression --no-run`.</precondition>
  <files>crates/brokerd/tests/audit_chain_fork_regression.rs</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md section D2 — why a bare `busy_timeout` would make the situation strictly worse
    - crates/brokerd/src/audit.rs lines 712-731 — `open_audit_db`, which today sets WAL only and owns no explicit busy timeout
    - crates/brokerd/src/audit.rs lines 971-1045 — `append_event`, including the `chain_anchor` upsert that must stay consistent under concurrency
    - crates/brokerd/tests/audit_chain_fork_regression.rs — the helpers added by Task 1 that this test reuses
  </read_first>
  <behavior>
    - Test 2 (`contended_appends_from_independent_connections_stay_linear`): N independent connections to one file-backed audit DB, each appending K events to the SAME session while supplying the same already-stale parent, must every one return `Ok`, must leave exactly `1 + N*K` event rows for the session, must leave exactly ONE chain leaf, and must leave `verify_chain` true.
    - Edge case: this state is unreachable by adding a busy timeout alone. A timeout without an atomic read-head-then-insert converts the crash into a successfully committed fork, so the leaf-count assertion still fails. It is likewise unreachable by an immediate transaction with no timeout, because contended writers then error instead of waiting.
    - Determinism: because every worker supplies the identical stale parent, the pre-fix tree produces `N*K` children of one node with no dependence on scheduling — the test is a deterministic RED, not a race hunt.
  </behavior>
  <action>
    Extend `crates/brokerd/tests/audit_chain_fork_regression.rs` with `#[test] fn contended_appends_from_independent_connections_stay_linear()`. Use `N = 6` worker threads and `K = 8` appends per worker, declared as named `const` items in the test so the numbers are auditable.

    Open the coordinating connection via `brokerd::audit::open_audit_db` on a fresh `unique_audit_db` path, seed the session root with `seed_session_root`, and capture `(root_id, root_hash)`. Open all N worker connections on the main thread, sequentially, BEFORE spawning any thread — `open_audit_db` runs `SCHEMA_DDL` plus five migrations on every open, and serialising those here keeps the test's subject the append path rather than concurrent DDL. Move one connection into each `std::thread::spawn` closure together with clones of `root_id`, `root_hash` and the session id string.

    Each worker loops K times constructing a `runtime_core::Event::new` of type `plan_node_evaluated` with a fresh `uuid::Uuid::new_v4()` id, parent `Some(root_id)` and empty taint, and calls `brokerd::audit::append_event(&conn, TEST_KEY, &event, Some(&root_hash))` — deliberately supplying the SAME stale parent from every worker, which is exactly the shape `handle_connection`'s in-memory `last_event_id` / `last_event_hash` threading produces after any out-of-band append. Collect each `Result` into a per-thread `Vec` and return it from the closure; join every handle and fail the test naming the worker index and the rusqlite error text if any append returned `Err` (this is the D2 leg: no writer may fail with a busy error).

    Then assert, in this order, with messages that name the observed value: total row count for the session equals `1 + N*K` via `SELECT COUNT(*) FROM events WHERE session_id = ?1`; `leaf_count` equals `1`; and `brokerd::audit::verify_chain` returns `true`. Do not assert on any pragma value — record in the test's doc comment the measured fact that rusqlite 0.32.1 already applies an implicit 5000 ms `sqlite3_busy_timeout` at `Connection::open` (`inner_connection.rs:119`), so a `PRAGMA busy_timeout != 0` assertion would be a FALSE red and would let a wrong fix pass. The behavioural assertions above are the oracle.

    Keep this test in the same file as Task 1's so both RED oracles ship and are discovered together, and so `cargo test -p brokerd --test audit_chain_fork_regression` is the single command that flips from failing to passing in Plan 51-07.
  </action>
  <verify>
    <automated>cargo test -p brokerd --test audit_chain_fork_regression --no-run &amp;&amp; ! cargo test -p brokerd --test audit_chain_fork_regression -- --exact contended_appends_from_independent_connections_stay_linear &amp;&amp; ./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p brokerd --test audit_chain_fork_regression -- --exact contended_appends_from_independent_connections_stay_linear` exits NON-ZERO on the pre-fix tree.
    - The failure output names the observed leaf count (a value greater than 1), proving the fork rather than only reporting a false `verify_chain`.
    - `grep -c 'PRAGMA busy_timeout' crates/brokerd/tests/audit_chain_fork_regression.rs` returns `0` — no pragma-value assertion is used as the oracle.
    - `grep -c 'should_panic' crates/brokerd/tests/audit_chain_fork_regression.rs` returns `0`.
    - The file still has zero `#[cfg(target_os` and zero `#[cfg(feature` occurrences.
    - `crates/brokerd/Cargo.toml` is unchanged: `git diff --name-only` does not list it.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>A single brokerd test binary now encodes the joint D1+D2 requirement that no partial fix can satisfy, and it fails on the current tree.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the LIVE harness so failures are diagnostic and LIVE-08's attribution actually runs</name>
  <precondition>`cargo build --workspace` succeeds on this host (requires the `user_setup` OpenSSL prerequisites), so the Linux + fixture-gated LIVE module can be compile-checked without Docker.</precondition>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-BLOCKING-DEFECTS.md sections D3 and D4 — the prohibition on changing `caprun run`'s output to satisfy a test, and the reason stderr must be joined first
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs lines 265-330 — the LIVE-07 body's spawn, stderr reader thread, sidecar join and assertions
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs lines 332-409 — the LIVE-08 body, its terminal-output assertions and its durable anchor / provenance assertions
    - cli/caprun/src/main.rs lines 792-808 — `print_blocked_pending_banner` / `print_effect_pointers`, the actual `caprun run` surface line format
    - crates/brokerd/src/confirmation.rs lines 690-733 — `render_block_display`, which runs only inside the separate review/confirm/deny processes
    - cli/caprun/Cargo.toml `[features]` — the `live-proof-fixtures` and `mock-egress-ca` gate names needed for the compile check
  </read_first>
  <action>
    D4 first. In BOTH `live_07_cli_multi_node_one_session_verify_chain` and `live_08_cli_mid_loop_i2_block_genuine_taint`, the current sequence unwraps the sidecar `JoinHandle` result before `stderr_reader.join()` runs, so a sidecar failure panics while the broker and worker stderr is still unread and is then discarded — precisely the failure path on which that output is the only diagnostic. Restructure both bodies to: bind the sidecar join result to a local without unwrapping it; then join `stderr_reader`, recovering from a panicked reader with a placeholder string rather than a nested panic; then resolve the sidecar result, and on either the thread-panic or the `Err` arm panic with a message that interpolates the captured stderr as well as the sidecar error text. The stdout transcript returned by the sidecar on the success arm must continue to feed the existing assertions unchanged.

    D3 second. In `live_08_cli_mid_loop_i2_block_genuine_taint`, DELETE the assertion that greps the driver's stdout for the block-display sink line. That string is emitted only by `render_block_display` in the separate review/confirm/deny processes; the `caprun run` driver prints the `effect_id=` / `sink=` pointer form instead, so the assertion fires before — and therefore permanently prevents the execution of — LIVE-08's real claim. Do NOT change `caprun run`'s output, do not add a new surface line, and do not substitute a different stdout grep: delete the check outright. Keep the immediately preceding negative assertion that the run did not terminate through the policy-deny code, and keep every assertion from the one-session count onward exactly as written — the durable `github.pr` / `body` anchor lookup, the non-empty and untrusted taint checks, and the three identity assertions binding the anchor's read event and provenance root to the unique `process_exited` event id. Those are the requirement; the deleted stdout grep was its weakest form.

    Add a short comment above the LIVE-08 assertion block recording that the durable anchor assertions are the attribution oracle and that a driver-stdout substring must never be reintroduced as a substitute. Change nothing else in the file: no argv, no environment variable, no feature gate, no fixture layout, no sidecar policy about which effects are confirmed.
  </action>
  <verify>
    <automated>cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run &amp;&amp; ! grep -qF 'Sink: github.pr' cli/caprun/tests/live_acceptance_v1_10_cli.rs &amp;&amp; test "$(grep -c 'stderr_reader.join()' cli/caprun/tests/live_acceptance_v1_10_cli.rs)" = 2 &amp;&amp; ./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run` exits 0 — the Linux + fixture-gated module compiles on this host with no Docker, which is the gate that would have caught defect F1.
    - `grep -F 'Sink: github.pr' cli/caprun/tests/live_acceptance_v1_10_cli.rs` finds nothing.
    - In each of the two LIVE test bodies, the line calling `stderr_reader.join()` precedes the line that resolves the sidecar `Result` into `stdout`; verified by reading the file, and both call sites still exist (`grep -c 'stderr_reader.join()'` returns `2`).
    - Every LIVE-08 assertion from `session_ids.len()` onward is byte-identical to the pre-task version: the `github.pr`/`body` anchor uniqueness check, `!anchor.taint.is_empty()`, the `is_untrusted()` check, `anchor.provenance_chain.first()`, `anchor.read_event_id == process_event_id`, and `anchor.read_event_id == anchor.provenance_chain[0]`.
    - `cli/caprun/src/main.rs` is NOT in `git diff --name-only` — the driver's output format is unchanged.
    - `git diff --name-only` for this task lists exactly `cli/caprun/tests/live_acceptance_v1_10_cli.rs`.
    - `./scripts/check-invariants.sh` reports Gates 1-6 PASSED.
  </acceptance_criteria>
  <done>A future failing LIVE run reports broker/worker stderr, and LIVE-08's durable attribution assertions are reachable for the first time.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: **high** — every `critical` or `high` threat below must carry a `mitigate` disposition with a concrete, in-plan mitigation. Nothing in this plan touches the confinement boundary, the executor, or any policy surface.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Out-of-band CLI process → shared audit SQLite file | `caprun grant` / `caprun confirm` run in separate OS processes and append to the same audit DB the broker is writing; this is the boundary D1 lives on. |
| Test harness assertions → phase completion claim | A weakened or inverted assertion silently converts a blocking defect into a green phase. |
| Child process stderr → failure diagnosis | Discarded stderr on the failure path turns a determinate defect into a guess (the D4 class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-05-01 | Tampering | audit event `parent_id` chain across processes | high | mitigate | Commit the D1 reproducer as a genuine assertion (`verify_chain` true, one leaf), never a `should_panic` inversion; acceptance criteria grep for `should_panic == 0`. |
| T-51-05-02 | Spoofing | "the defect is fixed" claim | high | mitigate | Both regressions must be shown to FAIL on the pre-fix tree by the task's own automated verify (`! cargo test ...`), so a later green is evidence rather than assertion. |
| T-51-05-03 | Repudiation | LIVE run failure diagnostics | high | mitigate | D4 reordering guarantees broker/worker stderr is captured and interpolated into the panic message on the sidecar-failure path in both LIVE bodies. |
| T-51-05-04 | Tampering | LIVE-08 attribution oracle | high | mitigate | D3 deletes the weak stdout grep and the acceptance criteria require the six durable-anchor/provenance assertions to remain byte-identical; `cli/caprun/src/main.rs` must not appear in the diff. |
| T-51-05-05 | Information disclosure | temp audit DB left in `std::env::temp_dir()` | low | accept | Test data only (synthetic session ids, no tokens, no literals); the file is removed on the happy path and deliberately retained on failure for inspection. |
| T-51-05-06 | Denial of service | contention test flakiness | low | accept | Worker connections are opened before threads spawn, counts are small named constants, and the pre-fix failure is deterministic (identical stale parent) rather than schedule-dependent. |
| T-51-05-SC | Tampering | package/dependency installs | high | mitigate | Zero new crates, zero new dev-dependencies, `crates/brokerd/Cargo.toml` asserted unchanged. The only host install is an OS `pkg-config` / `libssl-dev` pair declared under `user_setup` and performed by the human, never by cargo, so no package enters the build graph. |
</threat_model>

<verification>
- `./scripts/check-invariants.sh` — Gates 1-6 green after every task.
- `cargo test -p brokerd --test audit_chain_fork_regression --no-run` — the new RED binary compiles with no cfg gate, no Docker.
- `! cargo test -p brokerd --test audit_chain_fork_regression` — both regressions fail on the pre-fix tree.
- `cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca --no-run` — the Linux + fixture-gated LIVE module compiles on this host.
- `git diff --name-only` — only the two files in `files_modified` are touched.
- NOT run here and NOT this plan's job: `cargo test --workspace` (CLAUDE.md forbids the bare Linux recipe from Phase 16 onward because a benign run can trigger a LIVE SMTP send; Mailpit requires Docker) and `scripts/compose-verify.sh` (Docker absent; owned by Plan 51-04, which is re-run UNCHANGED after this gap closure).
</verification>

<success_criteria>
- Two committed regression tests reproduce D1 and the D1+D2 pair on a Docker-less host, with no `target_os` or `feature` cfg gate, and both fail today.
- The contention regression cannot be satisfied by a busy timeout alone or by an immediate transaction alone.
- The LIVE-08 body no longer short-circuits on a stdout substring, and its durable-anchor attribution assertions are intact and reachable.
- Both LIVE bodies surface broker/worker stderr on the sidecar-failure path.
- `check-invariants.sh` Gates 1-6 stay green; no source file under `crates/*/src/` or `cli/*/src/` is modified.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-05-SUMMARY.md` when done. Do NOT create `51-LIVE-EVIDENCE.md`, do NOT edit `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md` — LIVE-07 and LIVE-08 stay `Pending` and broken windows 1, 2, 3 stay `open` until Plan 51-04 is re-run unchanged and passes.
</output>
