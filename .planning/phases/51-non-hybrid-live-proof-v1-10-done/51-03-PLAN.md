---
phase: 51-non-hybrid-live-proof-v1-10-done
plan: 03
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - cli/caprun/Cargo.toml
  - cli/caprun/src/main.rs
  - cli/caprun/src/planner.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/live_acceptance_v1_10_cli.rs
  - cli/caprun/tests/planner.rs
  - cli/caprun/tests/proof_selector_rejection.rs
autonomous: true
gap_closure: true
requirements: [LIVE-07, LIVE-08]
must_haves:
  truths:
    - "LIVE-07 and ordinary release builds cannot activate the destructive proof planner from ambient CAPRUN_CODING_I2_PROOF state."
    - "Every caprun subprocess in the LIVE harness receives a declared fixture environment, so caller CAPRUN_* and proxy state cannot select another path."
    - "LIVE-08 identifies the exact github.pr body anchor and proves its ValueId provenance root is the Session's real process_exited event."
  artifacts:
    - path: "cli/caprun/Cargo.toml"
      provides: "Non-default live-proof-fixtures feature contract"
      contains: "live-proof-fixtures"
    - path: "cli/caprun/tests/live_acceptance_v1_10_cli.rs"
      provides: "Hermetic LIVE-07/LIVE-08 process harness and exact durable attribution assertions"
    - path: "cli/caprun/tests/proof_selector_rejection.rs"
      provides: "Ordinary-build CLI regression proving the ambient proof selector is rejected before Session, audit, workspace, or git effects"
    - path: "cli/caprun/src/main.rs"
      provides: "Feature-contained proof selector forwarding and normal-build rejection"
  key_links:
    - from: "cli/caprun/tests/live_acceptance_v1_10_cli.rs"
      to: "events.payload sink_blocked anchor"
      via: "Deserialize the exact github.pr/body SinkBlockedAnchor and compare value_id/read_event_id/provenance_chain with process_exited"
      pattern: "github\\.pr|read_event_id|provenance_chain|value_id"
    - from: "cli/caprun/src/main.rs"
      to: "cli/caprun/src/worker.rs"
      via: "CAPRUN_CODING_I2_PROOF forwarded only when live-proof-fixtures is compiled"
      pattern: "live-proof-fixtures"
---

<objective>
Close code-review findings WR-01, WR-02, and WR-03 before claiming either real-Linux proof: contain the proof selector behind a non-default test feature, make every LIVE subprocess hermetic, and replace temporal inference with exact durable github.pr I2/provenance attribution.

Purpose: Ensure the later Docker result proves the intended release-safe CLI paths and that LIVE-08 is independently attributable from the audit database.
Output: Feature-contained proof routing plus a hermetic acceptance harness with exact durable anchor assertions.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.codex/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-VERIFICATION.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-REVIEW.md
@.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-02-SUMMARY.md
@cli/caprun/Cargo.toml
@cli/caprun/src/main.rs
@cli/caprun/src/planner.rs
@cli/caprun/src/worker.rs
@cli/caprun/tests/live_acceptance_v1_10_cli.rs
@crates/runtime-core/src/event.rs
@crates/runtime-core/src/executor_decision.rs
</context>

## Artifacts this phase produces

- New Cargo feature: `live-proof-fixtures`, non-default, enabling the LIVE-08 proof selector and its fixture-only planner path.
- Normal-build selector rejection branch for `CAPRUN_CODING_I2_PROOF`.
- Shared hermetic caprun command builder/fixture allowlist in `live_acceptance_v1_10_cli.rs`.
- Durable LIVE-08 attribution helper/assertions for the exact `github.pr` / `body` `SinkBlockedAnchor`, including `value_id`, `read_event_id`, and `provenance_chain` checks.
- No new crate, package, sink, mint site, external API, database table, or ORM schema.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry the LIVE-08 selector through a test-feature-only CLI path</name>
  <files>cli/caprun/Cargo.toml, cli/caprun/src/main.rs, cli/caprun/src/planner.rs, cli/caprun/src/worker.rs, cli/caprun/tests/planner.rs, cli/caprun/tests/proof_selector_rejection.rs</files>
  <read_first>
    - cli/caprun/Cargo.toml — existing mock-egress feature forwarding and test target declarations
    - cli/caprun/src/main.rs — current ambient selector forwarding and worker env_clear allowlist
    - cli/caprun/src/worker.rs — current proof-planner selection arm
    - cli/caprun/src/planner.rs — CodingI2ProofPlanner implementation and deterministic planner separation
    - cli/caprun/tests/planner.rs — proof expressibility and deterministic anti-launder regressions
    - cli/caprun/tests/coding_cli.rs — ordinary-build CLI fixture layout and pre/post filesystem and git assertions to reuse in the selector rejection regression
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-REVIEW.md — WR-01 acceptance boundary
  </read_first>
  <behavior>
    - Without `live-proof-fixtures`, CAPRUN_CODING_I2_PROOF cannot select CodingI2ProofPlanner and is rejected explicitly before irreversible coding steps.
    - A no-feature caprun binary invoked with CAPRUN_CODING_I2_PROOF emits the stable nonzero diagnostic without creating a Session or audit database and without changing workspace files or git state.
    - With `live-proof-fixtures`, selector value `1` reaches the confined worker and selects CodingI2ProofPlanner only for SafeCodingWorkflow.
    - The default DeterministicPlanner remains selected when the selector is absent, including the LIVE-07 success path.
    - The existing success-path anti-launder assertion remains green: DeterministicPlanner never places an out_* handle into github.pr.
  </behavior>
  <action>
    Introduce a non-default `live-proof-fixtures` Cargo feature and compile the proof-only planner, main forwarding branch, and worker selection arm only under that feature. Add a normal-build fail-closed check near CLI coding dispatch so the proof selector is rejected with a stable diagnostic before session creation or filesystem/git effects. Keep `mock-egress-ca` as the network-fixture feature; make the authoritative LIVE target enable both fixture capabilities explicitly. Do not widen CaprunIntent, add a sink, or change the deterministic success recipe. Add focused cfg-aware tests that exercise the enabled selector path and normal/default planner path, preserving LIVE-07 coverage per LIVE-07 and the deliberate sibling proof path per LIVE-08. Add `proof_selector_rejection.rs` as a no-feature binary regression: build a complete disposable coding fixture, snapshot workspace bytes and git HEAD/status, invoke the ordinary `CARGO_BIN_EXE_caprun` with `CAPRUN_CODING_I2_PROOF=1`, assert the stable nonzero diagnostic, then prove no audit database/key or Session was created and the workspace and git snapshots are unchanged.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test proof_selector_rejection --no-default-features proof_selector_is_rejected_before_effects -- --exact --test-threads=1 &amp;&amp; cargo test -p caprun --test planner -- --test-threads=1 &amp;&amp; cargo check -p caprun &amp;&amp; cargo check -p caprun --features live-proof-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p caprun` contains no compiled selector-to-proof-planner path and fails closed if the ambient selector is supplied at runtime.
    - The no-feature binary regression observes the stable nonzero selector diagnostic and proves no Session, audit database/key, workspace mutation, git commit, or git worktree change occurred.
    - `cargo check -p caprun --features live-proof-fixtures` compiles the fixture selector path.
    - Deterministic planner anti-launder and proof-planner placement tests pass.
    - Invariant Gates 1–6 pass; no crate, sink, or mint site is added.
  </acceptance_criteria>
  <done>WR-01 is closed: the destructive proof planner is unavailable to an ordinary build and remains reachable only through an explicit fixture build used by LIVE-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make LIVE subprocesses hermetic and assert exact durable github.pr provenance</name>
  <files>cli/caprun/tests/live_acceptance_v1_10_cli.rs</files>
  <read_first>
    - cli/caprun/tests/live_acceptance_v1_10_cli.rs — all run, grant, confirm, and audit subprocess construction plus current row-order assertion
    - crates/runtime-core/src/event.rs — persisted Event payload and sink_blocked anchors
    - crates/runtime-core/src/executor_decision.rs — SinkBlockedAnchor fields and provenance invariants
    - crates/brokerd/src/audit.rs — events payload persistence and Event deserialization helpers
    - cli/caprun/tests/s9_process_exec_block.rs — genuine process_exited provenance equality pattern
    - .planning/phases/51-non-hybrid-live-proof-v1-10-done/51-REVIEW.md — WR-02 and WR-03 exact findings
  </read_first>
  <behavior>
    - LIVE-07 stays on the deterministic planner even when the parent test process has conflicting CAPRUN_* variables.
    - LIVE-08 alone supplies the proof selector; run, grant, confirm, and audit subprocesses all start from an empty environment and receive only the exact fixture allowlist.
    - The selected sink_blocked Event is the unique anchor with sink `github.pr` and arg `body`, not the earlier always-confirm git.push block or another event.
    - That anchor's ValueId is the submitted body handle, its `read_event_id` equals its first provenance root, and that UUID equals the real Session `process_exited` Event id.
    - The exact github.pr anchor carries the expected untrusted/I2 taint, while the permitting policy and absence of github_pr_succeeded prove I2 Block with no PR effect.
  </behavior>
  <action>
    Centralize subprocess creation in a helper that calls `env_clear()` and restores only a deterministic PATH plus the explicitly required compose mock endpoint/CA/proxy values, confirmation settings, and opaque fixture tokens. Define per-case overlays so LIVE-07 removes the proof selector and planner overrides, while LIVE-08 adds the selector; use the same helper for run, grant, confirm, and audit. Preserve required compose networking without accepting arbitrary CAPRUN_* or proxy variables by name.

    Replace the rowid-order provenance inference with database-alone attribution: load and deserialize every sink_blocked `events.payload` for the Session, locate exactly one `SinkBlockedAnchor` whose sink is github.pr and arg is body, load the unique process_exited Event id, and assert `anchor.read_event_id == anchor.provenance_chain[0] == process_exited.id`. Also assert a nonempty untrusted taint set, the expected terminal machine line for github.pr I2 blocking, zero github_pr_succeeded, one Session id, and a passing real `caprun audit`. Keep the earlier git.push confirmation block distinguishable rather than counting generic sink_blocked rows. This implements LIVE-08's durable exact attribution and preserves LIVE-07's one-Session assertions.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test live_acceptance_v1_10_cli --features live-proof-fixtures,mock-egress-ca -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Every Command spawning caprun in the LIVE harness is constructed through the hermetic helper and starts from an empty environment.
    - A regression test poisons the parent environment with conflicting planner/proof controls and demonstrates LIVE-07 remains deterministic and LIVE-08 remains explicitly selected.
    - LIVE-08 parses the hashed Event payload and uniquely matches github.pr/body; generic event order or count is not its provenance oracle.
    - The matched anchor's `read_event_id` and first provenance UUID equal the real process_exited Event id, and its `value_id` is recorded/asserted as the blocked body handle.
    - The policy permits github.pr, the terminal is I2-blocked, no github_pr_succeeded exists, and `caprun audit` reports Chain verification PASSED.
  </acceptance_criteria>
  <done>WR-02 and WR-03 are closed with hermetic process execution and durable, exact github.pr body attribution to genuine process output.</done>
</task>

</tasks>

<threat_model>
## Security policy

OWASP ASVS level: L1. Blocking threshold: high. Critical/high threats must be mitigated before Plan 51-04 may claim proof completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent environment → real CLI subprocess | Ambient process state is untrusted test input and must not select planners or routes. |
| Test feature → production CLI/worker | Proof-only destructive routing must be absent from ordinary builds. |
| Audit SQLite payload → LIVE-08 claim | Only tamper-evident exact anchors may establish sink, argument, ValueId, and provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-03-01 | Elevation of Privilege | ambient proof selector | high | mitigate | Compile selector and planner under non-default live-proof-fixtures; reject selector in normal builds before effects. |
| T-51-03-02 | Spoofing | LIVE subprocess configuration | high | mitigate | env_clear every caprun subprocess and restore an explicit per-case fixture allowlist. |
| T-51-03-03 | Tampering | LIVE-08 provenance claim | high | mitigate | Deserialize hashed sink_blocked Event anchors and equate github.pr/body read_event_id and provenance root to process_exited UUID. |
| T-51-03-04 | Repudiation | generic blocked-event counting | medium | mitigate | Require exactly identified sink and argument plus machine terminal, no-effect assertion, and passing chain audit. |
| T-51-03-SC | Tampering | package installation | low | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
- `./scripts/check-invariants.sh`
- `cargo test -p caprun --test planner -- --test-threads=1`
- `cargo check -p caprun` and `cargo check -p caprun --features live-proof-fixtures`
- Host-capable acceptance build/test for `live_acceptance_v1_10_cli`; authoritative Docker execution is reserved for Plan 51-04.
</verification>

<success_criteria>
- WR-01, WR-02, and WR-03 each have a code/test change and an automated regression.
- Ordinary builds cannot activate CodingI2ProofPlanner from ambient state.
- LIVE subprocesses are hermetic and case-specific.
- LIVE-08 durable evidence binds the exact github.pr body anchor to the genuine process_exited root.
- LIVE-07 and LIVE-08 remain pending until Plan 51-04 executes on real Linux.
</success_criteria>

<output>
Create `.planning/phases/51-non-hybrid-live-proof-v1-10-done/51-03-SUMMARY.md` when done.
</output>
