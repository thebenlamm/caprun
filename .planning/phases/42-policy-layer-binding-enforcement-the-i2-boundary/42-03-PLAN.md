---
phase: 42-policy-layer-binding-enforcement-the-i2-boundary
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - crates/executor/src/lib.rs
  - crates/executor/src/policy_gate.rs
  - crates/brokerd/src/lib.rs
  - crates/brokerd/src/server.rs
  - crates/executor/tests/policy_gate.rs
autonomous: true
requirements: [POLICY-01, POLICY-02]
must_haves:
  truths:
    - "The executor evaluates a session policy as a PRE-I2 narrowing gate: a non-permitted sink/arg yields Denied{PolicyDeny}, distinct from an I2 Block."
    - "The policy gate is a deny-only early return — a PERMIT falls through UNCHANGED to the collect-then-Block I2 loop; there is no Allow-and-skip-I2 path."
    - "A maximally-permissive policy does NOT weaken an I2 taint Block: a tainted sensitive arg on a policy-PERMITTED sink+arg still yields BlockedPendingConfirmation, identical to the no-policy baseline (POLICY-02 enforcement-order proof)."
    - "I2 sensitivity + the collect-then-Block loop stay HARDCODED and unmodified; policy is additive and deny-only."
  artifacts:
    - "crates/executor/src/policy_gate.rs — the deny-only policy evaluation over a plan node"
    - "crates/executor/tests/policy_gate.rs — POLICY-01 distinctness + POLICY-02 enforcement-order proofs"
  key_links:
    - "executor::submit_plan_node gains a `policy: &SessionPolicy` param; the gate runs AFTER validate_schema and BEFORE the `let mut blocked` collect loop."
    - "brokerd::submit_plan_node wrapper + server.rs::evaluate_plan_node_and_record thread the policy param; run_broker_server constructs the policy value (real binding source arrives in Plan 04)."
---

<objective>
Wire the policy layer into the executor as a PRE-I2 narrowing gate per DESIGN §5.1/§5.2, and prove POLICY-02 (the LOCKED invariant) with an enforcement-order test. `executor::submit_plan_node` gains a `policy: &SessionPolicy` parameter; immediately after the Step-0 schema gate and BEFORE the collect-then-Block I2 loop, a deny-only gate evaluates the policy — a non-permitted sink or arg returns `Denied { reason: DenyReason::PolicyDeny{..} }`; a PERMIT does nothing (falls through) so the UNMODIFIED I2 loop still runs on every permitted call. The policy param is threaded through `brokerd::submit_plan_node` and `server.rs::evaluate_plan_node_and_record`, with `run_broker_server` constructing the policy value internally for now (Plan 04 replaces that with a trusted-source-bound policy).

Purpose: POLICY-01 needs a distinct policy-deny outcome that is separate from an I2 Block; POLICY-02 needs I2 to execute unconditionally on every policy-permitted call, never short-circuited by any policy outcome. The load-bearing structural pin is placement + direction: the gate is deny-only and runs BEFORE the I2 loop, so a PERMIT can never skip I2 and a permissive policy can never remove an I2 Block.

Output: the policy gate in the executor TCB (`policy_gate.rs` + a call site in `submit_plan_node`); the threaded param through brokerd/server; and the POLICY-01/POLICY-02 test suite. NO trusted-file binding yet (Plan 04) — `run_broker_server` constructs the policy value so the build stays green end-to-end.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-v1.9-egress-policy.md
@crates/executor/src/lib.rs
@crates/executor/src/sink_sensitivity.rs
@crates/brokerd/src/lib.rs
@crates/brokerd/src/server.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the deny-only pre-I2 policy gate to the executor</name>
  <files>crates/executor/src/policy_gate.rs, crates/executor/src/lib.rs</files>
  <behavior>
    - A plan node whose sink is NOT in the policy's allowed set returns Denied{PolicyDeny{sink, arg:None, constraint:"sink-not-allowed"}}.
    - A plan node on an allowed sink whose constrained arg literal violates a coarse allowlist returns Denied{PolicyDeny{sink, arg:Some(..), constraint:..}}.
    - A plan node the policy fully permits returns exactly what the current no-gate submit_plan_node returns for the same input (Allowed, or BlockedPendingConfirmation on taint) — the gate adds NO behavior on a permit.
    - The gate runs AFTER validate_schema (an unknown sink still Denies with UnknownSink at Step 0, not PolicyDeny) and BEFORE the collect-then-Block loop.
  </behavior>
  <action>
    Create `crates/executor/src/policy_gate.rs` with a function `pub fn policy_gate(policy: &SessionPolicy, plan_node: &PlanNode, value_store: &ValueStore) -> Result<(), DenyReason>` that evaluates the plan node against the session policy per DESIGN §5.1: first the deny-by-default sink check (`policy.permits_sink` false → `Err(DenyReason::PolicyDeny{ sink, arg: None, constraint: "sink-not-allowed" })`); then, for each arg carrying a configured coarse constraint, resolve its literal from `value_store` and call `policy.evaluate(sink, arg_name, literal)` — a deny maps to `Err(PolicyDeny{ sink, arg: Some(name), constraint })`. Return `Ok(())` when the policy permits everything (this is the PERMIT = fall-through case). This function returns ONLY `Result<(), DenyReason>` — it has NO Allowed return, structurally guaranteeing policy can only narrow (POLICY-02).

    Wire it into `submit_plan_node` (`crates/executor/src/lib.rs`): add a `policy: &SessionPolicy` parameter to the signature, and insert the gate as a deny-only early return IMMEDIATELY AFTER the Step-0 `sink_schema::validate_schema` early return (lib.rs:66-68) and BEFORE the `let mut blocked: Vec<BlockedArg> = Vec::new();` line (lib.rs:76): `if let Err(reason) = policy_gate::policy_gate(policy, plan_node, value_store) { return ExecutorDecision::Denied { reason }; }`. Do NOT touch the collect-then-Block loop, the Step 1/1a/1b/1c guards, the sensitivity checks, or the Step-0.5 session-status class gate — I2 stays HARDCODED and unmodified (DESIGN §5.2, §7). Add `pub mod policy_gate;` to lib.rs. Add a doc comment stating the load-bearing ordering: policy gate is pre-I2 and deny-only; a PERMIT falls through so I2 always runs.

    Update the executor's own inline unit tests / any in-crate call sites of `submit_plan_node` to pass a policy (use `SessionPolicy::allow_all()` where the existing test intent is "policy is not the thing under test").
  </action>
  <verify>
    <automated>cargo test -p executor 2>&1 | tail -25</automated>
  </verify>
  <done>submit_plan_node takes `policy: &SessionPolicy`, the deny-only gate is placed after validate_schema and before the collect loop, a non-permitted sink/arg returns Denied{PolicyDeny}, a permitted node behaves identically to baseline, and all executor tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the policy param through brokerd and the live server path</name>
  <files>crates/brokerd/src/lib.rs, crates/brokerd/src/server.rs</files>
  <action>
    Propagate the new `policy` parameter so the workspace still compiles and the live dispatch path enforces it:
    - `crates/brokerd/src/lib.rs`: add `policy: &runtime_core::SessionPolicy` to the `brokerd::submit_plan_node` wrapper (lib.rs:46-57) and forward it to `executor::submit_plan_node`. Update the two in-module unit tests to pass `&SessionPolicy::allow_all()`.
    - `crates/brokerd/src/server.rs`: add a `policy: &runtime_core::SessionPolicy` parameter to `evaluate_plan_node_and_record` (server.rs:694) and pass it into the `executor::submit_plan_node` call (server.rs:725). Thread the policy from `run_broker_server` down through `handle_connection` / `classify_second_connection` / `dispatch_request` to that call, mirroring how `session_status` / `trusted_workspace_path` / `key` are already threaded (session-scoped state constructed once at the top of `run_broker_server` and cloned/borrowed per connection). Because the policy is IMMUTABLE for the session (DESIGN §5.3), thread it as an owned `SessionPolicy` cloned per connection (or an `Arc<SessionPolicy>` clone) — it does NOT need the `Arc<Mutex<..>>` treatment `session_status` gets, since nothing ever mutates it.
    - For THIS plan only, construct the policy value INSIDE `run_broker_server` (e.g. `let policy = SessionPolicy::allow_all();` with a comment that Plan 04 replaces this with a trusted-source-bound policy passed in as a parameter). Do NOT add a `run_broker_server` parameter or touch `main.rs` here — that is Plan 04's change, which keeps the trusted-binding wiring in one place.

    Keep the change surgical: the ONLY behavioral change on the live path is that a policy-denied plan node now returns `Denied{PolicyDeny}` before the I2 loop; the `allow_all()` policy means every currently-passing dispatch/e2e test still permits every sink, so existing behavior is preserved.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5; cargo test -p brokerd 2>&1 | tail -20</automated>
  </verify>
  <done>The policy param is threaded through brokerd::submit_plan_node and the full server.rs dispatch path; run_broker_server constructs an allow_all() policy internally; the workspace compiles and existing brokerd tests pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POLICY-01 distinctness + POLICY-02 enforcement-order proofs</name>
  <files>crates/executor/tests/policy_gate.rs</files>
  <behavior>
    - POLICY-01 distinctness: a plan node on a policy-DENIED sink returns Denied{PolicyDeny}, and this is asserted to be a Denied variant with code "policy_deny" — NOT a BlockedPendingConfirmation.
    - POLICY-02 enforcement-order (the marquee proof): with an allow_all() policy that PERMITS `email.send` + `to`, a tainted routing-sensitive `to` value STILL returns BlockedPendingConfirmation — byte-identical to the same input evaluated with the pre-policy baseline. A permissive policy does not, and cannot, remove the I2 Block.
    - Independence: a plan node that is BOTH policy-denied AND carries a tainted sensitive arg returns Denied{PolicyDeny} (the deny-only pre-gate fires first) — proving policy-deny and I2-Block are independently attributable, never conflated.
    - A policy-PERMITTED, clean (untainted, trusted-role) plan node returns Allowed — policy permit + I2 pass.
  </behavior>
  <action>
    Add `crates/executor/tests/policy_gate.rs` (an integration test linking the executor with its `test-fixtures` feature, matching the existing `crates/executor/tests/` convention). Build value-store fixtures the same way the existing executor tests do (mint a tainted routing-sensitive value and a clean trusted value). Write the four proofs described in the behavior block. The POLICY-02 enforcement-order test is the load-bearing one: assert that `submit_plan_node(.., &SessionPolicy::allow_all(), ..)` on a plan node with a tainted `to` returns `BlockedPendingConfirmation` (NOT `Denied`), demonstrating that the most-permissive policy leaves the hardcoded I2 Block fully intact — the same guarantee LIVE-06 leg 3 re-demonstrates live in Phase 46. Give each test a name that states the invariant it proves so a green run is legible in CI output.
  </action>
  <verify>
    <automated>cargo test -p executor --test policy_gate 2>&1 | tail -25</automated>
  </verify>
  <done>All four proofs pass: PolicyDeny is a distinct Denied outcome (not a Block); a permissive policy does NOT weaken the I2 Block; policy-deny fires before I2 on a doubly-offending node; a permitted clean node is Allowed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy gate → I2 loop | The gate runs before I2 and may ONLY deny; a permit hands control to the unmodified collect-then-Block loop. |
| plan node → executor decision | Every effect is still a PlanNode of opaque-handle args (no raw EffectRequest path); policy narrows the plan-node path, never bypasses it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Elevation of Privilege | policy-as-I2-override (the #1 locked anti-requirement) | critical | mitigate | I2 stays HARDCODED and unmodified in the executor; the policy gate is a deny-only early return with no Allowed path; POLICY-02 enforcement-order test proves a permissive policy cannot remove an I2 Block (DESIGN §5.2, §6 row 9). |
| T-42-08 | Spoofing | policy-deny vs I2-Block conflation | high | mitigate | PolicyDeny is a distinct Denied{code=="policy_deny"} outcome; the independence test proves the two are separately attributable (DESIGN §5.1, §6 row 12, LIVE-06 leg 3). |
| T-42-09 | Tampering | I2 sensitivity/loop regressions from the wiring | high | mitigate | The gate insertion is purely additive (after validate_schema, before the collect loop); no I2 code path is modified; existing executor + brokerd suites stay green as the regression fence. |
| T-42-10 | Elevation of Privilege | raw effect-to-sink bypass | high | mitigate | No EffectRequest path introduced; check-invariants Gate 1 stays green; every effect remains a plan node. |

No package-manager installs in this plan — no supply-chain legitimacy checkpoint required.
</threat_model>

<verification>
- `cargo test -p executor` and `cargo test -p executor --test policy_gate` pass (POLICY-01/POLICY-02 proofs).
- `cargo test -p brokerd` passes (existing dispatch/e2e behavior preserved under allow_all()).
- `cargo build --workspace` compiles with the threaded param.
- `./scripts/check-invariants.sh` exits 0 — Gate 1 (no raw effect-to-sink) in particular stays PASS.
- Note [[cfg-linux-test-blindness]]: the policy-gate logic + proofs are pure Rust and run on the macOS host. server.rs threading may link cfg(linux) dispatch/e2e targets — at execute time run `cargo build --tests -p brokerd` (and the workspace) in the Linux container to compile-check every gated caller of the changed `submit_plan_node` / `evaluate_plan_node_and_record` signatures ([[cargo-test-workspace-missing-sibling-binary]]: run `cargo build --workspace` before any test that resolves a sibling binary).
</verification>

<success_criteria>
- The executor enforces the session policy as a deny-only pre-I2 gate; PolicyDeny is distinct from an I2 Block.
- A permissive policy provably does not weaken an I2 taint Block (POLICY-02).
- The param is threaded through brokerd + the live server path with existing suites green; no I2 code modified; no raw effect path.
</success_criteria>

<output>
Create `.planning/phases/42-policy-layer-binding-enforcement-the-i2-boundary/42-03-SUMMARY.md` when done.
</output>
