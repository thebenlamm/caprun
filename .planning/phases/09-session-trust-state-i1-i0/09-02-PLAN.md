---
phase: 09-session-trust-state-i1-i0
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - crates/executor/src/sink_sensitivity.rs
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/lib.rs
  - crates/executor/tests/executor_decision.rs
autonomous: true
requirements: [TAINT-02, TAINT-03]

must_haves:
  truths:
    - "Step 0.5 (draft-only CommitIrreversible deny) runs AFTER the per-arg I2 loop completes with NO Block, immediately before the final Allowed return — NEVER before the loop (DESIGN §8 B1; this was the round-1 blocker and must be unmissable)"
    - "A Draft session submitting a CommitIrreversible sink whose per-arg loop produced no Block returns Denied { DraftOnlySessionDeniesCommitIrreversible { sink } } (TAINT-02)"
    - "A Draft session submitting an Observe/MutateReversible sink is NOT denied by Step 0.5 and passes through unchanged (TAINT-03)"
    - "session_status is an explicit &SessionStatus parameter on submit_plan_node, never read off PlanNode or IPC (DESIGN §4)"
    - "sink_effect_class is a hardcoded match keyed by SinkId, fail-closed (unknown sink -> CommitIrreversible), never a PlanNode field or config file (DESIGN §6)"
    - "Step 0.5's match over SessionStatus is exhaustive with no wildcard arm (DESIGN §10)"
  artifacts:
    - crates/executor/src/sink_sensitivity.rs
    - crates/executor/src/lib.rs
    - crates/executor/tests/executor_decision.rs
  key_links:
    - "sink_effect_class(&plan_node.sink) == EffectClass::CommitIrreversible is the exact predicate Step 0.5 gates on"
    - "The #[cfg(test)] test.observe fixture sink is the ONLY vehicle that makes TAINT-03 testable (both live sinks are CommitIrreversible)"
    - "The new 5th parameter breaks 8 call sites in executor_decision.rs which are updated in the same plan"
---

<objective>
Implement the executor half of the draft-only deny mechanism: a new `EffectClass` enum and hardcoded `sink_effect_class` classifier, a `#[cfg(test)]` fixture sink to make the "still-allowed" case testable, the breaking `session_status: &SessionStatus` parameter on `submit_plan_node`, and the new **Step 0.5** class-deny that runs *after* the existing per-arg I2 loop. This is the single TCB deny function — the security differentiator — so ordering and exhaustive matching are load-bearing, not stylistic.

Purpose: TAINT-02 (draft denies CommitIrreversible) and TAINT-03 (draft still allows Observe/MutateReversible), decided in one executor function, one taxonomy, never a broker pre-check.
Output: Modified `executor` crate; `cargo test -p executor` green with new TAINT-02/TAINT-03 tests.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-session-trust-state.md
@planning-docs/DESIGN-plan-executor.md
@.planning/phases/09-session-trust-state-i1-i0/09-RESEARCH.md
@.planning/phases/09-session-trust-state-i1-i0/09-PATTERNS.md
@crates/executor/src/lib.rs
@crates/executor/src/sink_sensitivity.rs
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from source-drift verification):
- `EffectClass` — new enum in the `executor` crate: `Observe`, `MutateReversible`, `CommitIrreversible`
- `sink_effect_class(sink: &SinkId) -> EffectClass` — new hardcoded classifier fn in `crates/executor/src/sink_sensitivity.rs`
- `"test.observe"` — new `#[cfg(test)]`-gated fixture sink (registered in `sink_schema.rs`, classified `EffectClass::Observe` in `sink_effect_class`)
- new 5th parameter `session_status: &SessionStatus` on `executor::submit_plan_node`
- Step 0.5 block inside `submit_plan_node` (the draft-only class deny)
- New tests in `crates/executor/tests/executor_decision.rs`: `draft_session_denies_commit_irreversible`, `draft_session_allows_observe`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EffectClass, sink_effect_class, and the #[cfg(test)] test.observe fixture sink</name>
  <files>crates/executor/src/sink_sensitivity.rs, crates/executor/src/sink_schema.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs (the file being modified — the exact `is_routing_sensitive` hardcoded-`match` shape to mirror, and how `SinkId` is imported)
    - crates/executor/src/sink_schema.rs (the file being modified — `KNOWN_SINKS` / `schema_for` structure, both entries currently `CommitIrreversible`, so the registration shape for a new test-only sink is clear)
    - planning-docs/DESIGN-session-trust-state.md §6 (EffectClass 3 variants, hardcoded classifier, fail-closed unknown -> CommitIrreversible, both live sinks -> CommitIrreversible) and §10 (the internal `&str` sink-name match may keep its `_` arm; only enum matches must be exhaustive)
    - .planning/phases/09-session-trust-state-i1-i0/09-RESEARCH.md Pitfall 3 (why the fixture must be exercised end-to-end, not just unit-tested in isolation)
    - crates/runtime-core/src/effect.rs (the locked 3-class `Effect` ontology EffectClass mirrors — do not add a 4th variant)
  </read_first>
  <behavior>
    - `sink_effect_class(&SinkId("email.send".into())) == EffectClass::CommitIrreversible`
    - `sink_effect_class(&SinkId("file.create".into())) == EffectClass::CommitIrreversible`
    - `sink_effect_class(&SinkId("<any unregistered name>".into())) == EffectClass::CommitIrreversible` (fail-closed)
    - under `#[cfg(test)]`: `sink_effect_class(&SinkId("test.observe".into())) == EffectClass::Observe`
  </behavior>
  <action>
    Add a new public enum `EffectClass` to the `executor` crate with exactly three variants — `Observe`, `MutateReversible`, `CommitIrreversible` — mirroring the locked 3-class `Effect` ontology in `runtime-core`; do NOT add a fourth variant. Place it in `sink_sensitivity.rs` (RESEARCH A3: executor crate, nothing outside executor needs it). Derive at least `Debug, Clone, PartialEq`.
    Add a public function `sink_effect_class(sink: &SinkId) -> EffectClass` in `sink_sensitivity.rs`, mirroring the existing `is_routing_sensitive` hardcoded-`match` shape exactly. Map `"email.send"` and `"file.create"` to `EffectClass::CommitIrreversible`. The unknown-sink branch MUST be fail-closed: `_ => EffectClass::CommitIrreversible` (the most restrictive class, per DESIGN §6 — never a permissive `Observe`/`MutateReversible` default). This internal `&str` match keeping a `_` arm is explicitly permitted (§10) because it is a string match, not a match over the `EffectClass` enum.
    Register a `#[cfg(test)]`-gated fixture sink named `"test.observe"`: add it to the sink registry (a `#[cfg(test)]` branch in `schema_for`, or a `#[cfg(test)]` `TEST_KNOWN_SINKS` slice consulted only under `cfg(test)`) with a minimal/empty arg schema so a real `PlanNode` can target it and pass Step 0's schema gate, AND add a matching `#[cfg(test)] "test.observe" => EffectClass::Observe` arm to `sink_effect_class`. This fixture never appears in the production `KNOWN_SINKS` surface (Pitfall 3) — it exists solely so TAINT-03 can be driven through the FULL `submit_plan_node` path.
  </action>
  <acceptance_criteria>
    - Source: `enum EffectClass` has exactly the three variants `Observe`, `MutateReversible`, `CommitIrreversible`.
    - Source: `sink_effect_class`'s unknown-sink arm returns `EffectClass::CommitIrreversible` (fail-closed) — the `_ =>` arm does NOT return `Observe` or `MutateReversible`.
    - Source: the `test.observe` sink registration and its `Observe` classification are both behind `#[cfg(test)]`.
    - Behavior: `cargo test -p executor` compiles; unit assertions on the four `sink_effect_class` inputs above hold.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p executor --no-fail-fast</automated>
  </verify>
  <done>`EffectClass`, `sink_effect_class` (fail-closed), and the `#[cfg(test)] test.observe` fixture all exist; executor compiles under both prod and test cfg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the session_status parameter and the post-loop Step 0.5 deny to submit_plan_node</name>
  <files>crates/executor/src/lib.rs</files>
  <read_first>
    - crates/executor/src/lib.rs (the file being modified — current `submit_plan_node` 4-arg signature, Step 0 schema gate, the per-arg `for` loop Steps 1/1a/1b/2/3, and the final `ExecutorDecision::Allowed` return where Step 0.5 attaches)
    - planning-docs/DESIGN-session-trust-state.md §8 (the EXACT Step 0.5 placement — AFTER the per-arg loop completes with no Block, before Allowed; the exhaustive match over all 6 SessionStatus variants) and §9 (non-regression MUSTs) and §11 conditions 3/4
    - .planning/phases/09-session-trust-state-i1-i0/09-RESEARCH.md "State of the Art" table (round-1 ordering was BEFORE the loop and broke ACC-01/02; round-2 ordering is AFTER — implement round-2)
    - .planning/phases/09-session-trust-state-i1-i0/09-PATTERNS.md (executor/src/lib.rs section — the extension point between the loop's closing brace and the `Allowed` return)
  </read_first>
  <behavior>
    - Draft + CommitIrreversible sink + per-arg loop produced no Block -> `Denied { DraftOnlySessionDeniesCommitIrreversible { sink } }`
    - Draft + Observe sink (test.observe) + no Block -> `Allowed`
    - Active + CommitIrreversible sink + no Block -> `Allowed` (Step 0.5 does not fire for Active)
    - Any session + tainted routing-sensitive arg -> the per-arg loop returns `BlockedPendingConfirmation` and Step 0.5 is NEVER reached (I2 precedence, DESIGN §8 B1)
  </behavior>
  <action>
    Change the signature of `executor::submit_plan_node` to add a 5th parameter `session_status: &SessionStatus` (import `SessionStatus` from `runtime_core`), exactly as DESIGN §4 specifies. Do NOT read session status from `plan_node` or any IPC field — it arrives only as this parameter (the broker resolves it in Plan 03).
    Insert the new **Step 0.5** block AFTER the per-arg `for` loop closes with no Block and immediately BEFORE the final `ExecutorDecision::Allowed` return. It MUST NOT run before or inside the per-arg loop: if any arg Blocks during Steps 1/1a/1b/2/3, the function returns `BlockedPendingConfirmation` as it does today and Step 0.5 is never reached (I2 per-arg Block takes precedence over the I1/I0 class deny — DESIGN §8/§9/§11 condition 4; reversing this reopens round-1 blocker B1 and breaks the v1.1 §9 acceptance).
    Step 0.5 is an EXHAUSTIVE `match *session_status` over all six variants, no `_` wildcard arm (DESIGN §10): `Draft` -> if `sink_effect_class(&plan_node.sink) == EffectClass::CommitIrreversible` return `ExecutorDecision::Denied { reason: DenyReason::DraftOnlySessionDeniesCommitIrreversible { sink: plan_node.sink.clone() } }`, otherwise fall through to `Allowed` (TAINT-03); `Active` -> fall through; `WaitingApproval | Done | Failed | RolledBack` -> explicit no-deny arm (matched by name, not wildcard, so a future variant is a compile error). Do NOT alter Steps 0/1/1a/1b/2/3 logic, ordering, or the values they read from `value_store` (DESIGN §9).
  </action>
  <acceptance_criteria>
    - Source: `submit_plan_node`'s signature has `session_status: &SessionStatus` as its 5th parameter.
    - Source: the Step 0.5 `match` names all six `SessionStatus` variants explicitly with no `_` arm.
    - Source: the Step 0.5 block textually appears after the per-arg loop's closing brace and before the final `Allowed` return (not before or inside the loop).
    - Behavior: `cargo test -p executor` green including the Task 3 tests that assert I2-Block-precedence over the draft deny.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p executor --no-fail-fast</automated>
  </verify>
  <done>`submit_plan_node` takes `&SessionStatus`; Step 0.5 runs post-loop, exhaustive, and denies only Draft+CommitIrreversible with no prior Block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the 8 executor test call sites and add TAINT-02/TAINT-03 tests</name>
  <files>crates/executor/tests/executor_decision.rs</files>
  <read_first>
    - crates/executor/tests/executor_decision.rs (the file being modified — all 8 existing `submit_plan_node` call sites, verified at lines ~72, ~133, ~155, ~184, ~219, ~240, ~273, ~297, and the existing helpers for building a `PlanNode`/`ValueStore`)
    - .planning/phases/09-session-trust-state-i1-i0/09-RESEARCH.md Pitfall 1 (the call-site inventory) and Validation Architecture Test Map (TAINT-02/TAINT-03 test names)
    - .planning/phases/09-session-trust-state-i1-i0/09-PATTERNS.md (14-call-sites section — pass `&SessionStatus::Active` to preserve existing semantics)
  </read_first>
  <behavior>
    - `draft_session_denies_commit_irreversible`: build a clean (untainted, no routing-sensitive) `PlanNode` targeting `file.create`, call `submit_plan_node(.., &SessionStatus::Draft)`, assert `Denied { DraftOnlySessionDeniesCommitIrreversible { sink } }` with `sink == file.create`.
    - `draft_session_allows_observe`: build a `PlanNode` targeting the `test.observe` fixture sink, call with `&SessionStatus::Draft`, assert `Allowed`.
    - (regression) an existing Active-session clean-allow test still returns `Allowed`; an existing tainted-routing-arg test still returns `BlockedPendingConfirmation` — now called with `&SessionStatus::Active` (semantics preserved).
  </behavior>
  <action>
    Update all 8 existing `submit_plan_node` calls in this file to pass a 5th argument `&SessionStatus::Active`, preserving their existing behavior exactly (Pitfall 1 — mechanical). Add two new tests: `draft_session_denies_commit_irreversible` (Draft + `file.create`, clean args, no Block -> `Denied` with the new `DenyReason` variant, asserting the carried `sink`) proving TAINT-02; and `draft_session_allows_observe` (Draft + the `#[cfg(test)] test.observe` fixture sink -> `Allowed`) proving TAINT-03 end-to-end through the full `submit_plan_node` path (Step 0 schema gate -> per-arg loop -> Step 0.5), not by unit-testing `sink_effect_class` in isolation (Pitfall 3). Import `SessionStatus` from `runtime_core`.
  </action>
  <acceptance_criteria>
    - Behavior: `cargo test -p executor --test executor_decision` passes, including `draft_session_denies_commit_irreversible` and `draft_session_allows_observe`.
    - Source: no `submit_plan_node` call in this file passes fewer than 5 arguments.
    - Behavior: the draft-allow test constructs a real `PlanNode` for `test.observe` and drives `submit_plan_node` (not a bare `sink_effect_class` assertion).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p executor --no-fail-fast</automated>
  </verify>
  <done>All 8 call sites updated to 5 args; TAINT-02 and TAINT-03 tests exist and pass; `cargo test -p executor` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker -> executor | `session_status` and `plan_node` cross into the pure TCB decision function; only `session_status` supplied by the broker's trusted resolution is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Elevation of Privilege | Worker/planner self-declares session `Active` to bypass Step 0.5 | critical | mitigate | `session_status` is an explicit broker-resolved parameter, never read from `PlanNode`/IPC (DESIGN §4); executor is a pure function that only consumes it |
| T-09-03 | Elevation of Privilege | Step 0.5 evaluated before the per-arg I2 loop, making the confirmable Block unreachable and breaking §9 acceptance | critical | mitigate | Step 0.5 placed strictly AFTER the loop, reached only on no-Block; enforced by the precedence regression test in Task 3 (DESIGN §8 B1) |
| T-09-04 | Elevation of Privilege | Future `SessionStatus`/`EffectClass` variant silently fails open via a wildcard match arm | high | mitigate | Exhaustive `match` with no `_` arm on `SessionStatus` in Step 0.5; `EffectClass` matched exhaustively (DESIGN §10) |
| T-09-05 | Elevation of Privilege | Unregistered/unknown sink classified permissively | medium | mitigate | `sink_effect_class` unknown-sink branch is fail-closed to `CommitIrreversible`; Step 0's schema gate already rejects unknown sinks first (DESIGN §6) |
| T-09-SC | Tampering | Supply-chain (package installs) | low | accept | No `cargo add` / package installs this phase (RESEARCH: audit N/A) |
</threat_model>

<verification>
- `cargo test -p executor --no-fail-fast` green (includes the two new tests and all 8 updated call sites).
- Step 0.5 is textually after the per-arg loop and exhaustively matched.
- Note: `cargo test -p brokerd` and `cargo test --workspace` are EXPECTED to fail to compile after this plan until Plan 03 updates brokerd's call sites — this is the planned breaking-signature ripple (RESEARCH Pitfall 1), not a regression. Do not "fix" it here.
</verification>

<success_criteria>
The executor decides draft-only denial in one TCB function: Draft+CommitIrreversible (no prior Block) -> Denied with the new variant; Draft+Observe -> Allowed; I2 per-arg Block always takes precedence; all matches exhaustive; `cargo test -p executor` green.
</success_criteria>

<output>
Create `.planning/phases/09-session-trust-state-i1-i0/09-02-SUMMARY.md` when done.
</output>
