---
phase: 26-security-hardening-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-security-hardening.md
autonomous: true
requirements: [DESIGN-11]
must_haves:
  truths:
    - "The design doc pins a MECHANISM and a FAIL-CLOSED DEFAULT for each of the five residuals (§a HARDEN-01, §b HARDEN-02, §c HARDEN-03, §d HARDEN-04, §e HARDEN-05), each grounded in a real file:line from 26-RESEARCH.md."
    - "The doc pins the three cross-cutting rulings (X-01 label continuity, X-02 shared-store recovery authority, X-03 TOCTOU commit-before-dispatch) as ONE uniform rule each."
    - "The doc contains an explicit RULING on the RESEARCH §f NEW finding (proposed X-04 Planner-connection stale session_status), deciding fold-into-HARDEN-01 vs named-residual — not silently inherited."
    - "The doc incorporates all four RESEARCH corrections: (i) contents expected-role Some(&[\"path\"]); (ii) HARDEN-01 workspace_rel plumbing budget; (iii) X-04 ruling; (iv) X-02 reframed to the confirm/deny process + pending_confirmations."
    - "The doc reads as decisions-not-options, mirroring DESIGN-slot-type-binding.md's rigor (§-per-mechanism, Adversarial-Review-Preemption §, Accepted Residual Risks §)."
  artifacts:
    - planning-docs/DESIGN-security-hardening.md
  key_links:
    - "Each § cites the exact current-code anchor from 26-RESEARCH.md (server.rs / audit.rs / sink_sensitivity.rs / quarantine.rs / confirmation.rs file:line) so Phases 27-29 are mechanical realizations."
    - "§e's expected-role ruling ties to the s9_live_file_create_clean_allow regression canary (must not Deny the only live file.create flow)."
---

<objective>
Author `planning-docs/DESIGN-security-hardening.md` — the v1.6 design-gate deliverable. This is a
DECISIONS doc (not an options survey) that pins the mechanism + fail-closed default for all five
TCB-local residuals (HARDEN-01..05), the three cross-cutting rulings (X-01/X-02/X-03), and an
explicit ruling on the RESEARCH-surfaced NEW finding (proposed X-04). It mirrors the shape and rigor
of `planning-docs/DESIGN-slot-type-binding.md` (the v1.5 gate doc).

Purpose: This doc HARD-BLOCKS Phases 27-30. No `crates/executor`, `crates/brokerd`, or
`crates/runtime-core` hardening code may be written until this doc clears a fresh adversarial review
(Plan 26-02). Every downstream implementation phase is a mechanical realization of what §a-§e pin.

Output: `planning-docs/DESIGN-security-hardening.md` (satisfies DESIGN-11). This plan writes NO
code — it produces one markdown design document.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-security-hardening-design-gate/26-CONTEXT.md
@.planning/phases/26-security-hardening-design-gate/26-RESEARCH.md

# Shape/format precedent — MIRROR this doc's structure and rigor (decisions-not-options):
@planning-docs/DESIGN-slot-type-binding.md

# The pinned prior design §b/§a must reconcile with (D-02 amends its :80-81 clause; §12 residual #1):
@planning-docs/DESIGN-session-trust-state.md

# Project TCB constraints (TCB-is-Rust, no swappable policy file, check-invariants gates, PLAN.md wins):
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author §0 (Purpose/Scope) and §a-§e (the five residual mechanisms)</name>
  <files>planning-docs/DESIGN-security-hardening.md</files>
  <action>
Create `planning-docs/DESIGN-security-hardening.md`. Open with a title, a metadata block (milestone
v1.6, requirements gated DESIGN-11/12, date), then §0 Purpose & Scope mirroring
DESIGN-slot-type-binding.md §0 (state: five TCB-local residuals; no new external-effect surface;
this doc hard-gates Phases 27-30; decisions-not-options discipline; every file:line re-verified at
Phase 27+ if commits intervene).

Before writing each §, re-run the RESEARCH's own recommended greps to confirm the file:line anchors
are still live (per 26-RESEARCH.md "Valid until"): `grep -n "CAPRUN_ENABLE_IPC_CREATE_SESSION"
crates/brokerd/src/server.rs`, `grep -n "session_status" crates/brokerd/src/server.rs`, `grep -n
"contents" cli/caprun/src/planner.rs`, `grep -n "contents" crates/executor/src/sink_sensitivity.rs`.
Cite the confirmed line numbers in the doc.

Then author one § per residual, each with: (1) the exact current-code anchor (file:line from
26-RESEARCH.md §a-§e), (2) the MECHANISM (pinned, not optioned), (3) the FAIL-CLOSED DEFAULT, (4) the
risk/false-positive surface, (5) an informative blast-radius note (files/functions Phase 27-29 will
touch). Pin per residual:

- **§a — HARDEN-01 demote-at-RequestFd (D-01/D-02/D-03):** demotion commits BEFORE the fd is released
  (fail-closed ordering, D-01); worker evidence may only DEMOTE, never keep Active (broker-derived
  keep-Active only). Reconcile with DESIGN-session-trust-state.md:80-81 per D-02 (status quo violates
  its own anti-self-declaration rationale because mint_from_read is only reached via the
  worker-optional ReportClaims path; relocating demotion to fd-grant is the broker's own act; cite
  the fd_requested=true broker-side precedent at server.rs:1001). Pin D-03's trusted-label criterion
  (shift from "fragment-free" to "trusted-labeled file"), the fail-closed default for an unlabeled
  file = demote, and the label must be broker-derivable WITHOUT parsing worker bytes (TOCTOU).
  RESEARCH CORRECTION (ii): the trusted-label signal (path-identity compare vs the CLI's own
  workspace_rel argument) has NO existing broker-side plumbing — workspace_rel is forwarded only to
  the worker via WORKSPACE_FILE env today (main.rs:187-189/:301), never into run_broker_server. The
  doc MUST state Phase 27 budgets a genuinely NEW threading hop (workspace_rel into
  run_broker_server/dispatch_request as broker-owned per-connection state, mirroring how
  session_status/fd_requested are threaded), not merely a new `if` branch. Pin the causal-edge target
  for the fd-grant-time session_demoted event (parents on fd_granted's id / current chain head, since
  no file_read event exists yet at fd-grant — DESIGN-session-trust-state.md §5 does not anticipate
  this) and note mint_from_read's "SOLE I1 trust-flip site" doc comment becomes false and must be
  corrected in the same Phase-27 PR.
- **§b — HARDEN-02 authenticated audit chain (D-04/D-05/D-06):** keyed MAC (HMAC-SHA256 over the
  existing compute_event_hash input shape; sha2/hex already workspace deps) with the key held by the
  broker OUTSIDE the confined worker's Landlock scope and OUTSIDE the SQLite file (D-04 threat ceiling
  = in-host DB-writer, NOT full host/root). Add an anchored/monotonic chain head to catch
  tail-truncation + restore-from-backup rollback (confirm from RESEARCH §b that verify_chain returns
  true on a truncated tail today — audit.rs:477-539). Pin the KEY-CUSTODY-ACROSS-PROCESSES answer
  (the single hardest question, RESEARCH §b/Open-Q2): caprun is single-shot-per-session, and
  confirm()/deny() run in a SEPARATE later OS process that must verify the SAME chain — so a
  per-process fresh key breaks confirm()'s verify_chain gate (confirmation.rs:599). Pin one concrete
  stable-secret source (e.g. a sibling `<audit_path>.key` outside the workspace root, restrictive host
  perms, read by both caprun and caprun confirm/deny). Pin D-06: RESEARCH §b confirms blocked_literals
  already lives OUTSIDE the hash (only literal_sha256 is anchored) so a whole-row MAC over the same
  fields compute_event_hash covers today already satisfies D-06 — state this explicitly (no salted
  commitment needed for blocked_literals). RESEARCH SECOND mutable-field finding: pending_confirmations
  is a SECOND mutable table with NO hash/MAC column of its own (state mutated via transition_state);
  the doc MUST either fold it into the MAC scheme (recommended, re-checked at confirm/deny entry) OR
  name it a second Accepted Residual Risk — do not silently assume it is covered. Record D-05 as a
  named Accepted Residual Risk (full host/root that reads the key is out of scope; candidate = external
  out-of-store notarization later); do not claim tamper-evidence beyond the in-host-DB-writer model.
- **§c — HARDEN-03 Allowed-path replay CAS (D-07/D-08):** idempotency key / CAS on the trusted
  email.send Allowed path (server.rs:791-866), in the SAME transaction-then-commit-then-effect
  discipline as the confirm path's SEND-01 pattern (confirmation.rs:694-738/291-302). Pin the
  load-bearing RESEARCH §c finding: effect_id is minted FRESH per SubmitPlanNode call at
  server.rs:562, so an effect_id-keyed CAS does NOTHING against replay — the idempotency key MUST be
  derived from the resolved plan-node content (candidate: SHA256 over sink.0 + sorted (arg_name,
  value_id) pairs, mirroring combined_digest's shape; value_id-scope is the correct D-08 per-plan-node
  scope). Pin the new table shape (a sent_plan_nodes-style PRIMARY-KEY-on-idempotency-key table) and
  the CAS idiom (INSERT-constraint-violation-as-signal vs UPDATE...WHERE) explicitly. Restate D-08's
  scope caveat UNSOFTENED: CAS buys at-most-once PER PLAN NODE, not bounded sends per session; a
  statically-compromised worker minting fresh distinct plan nodes still sends N times. File the
  effects-budget/rate-limit as named future defense-in-depth (out of v1.6 scope).
- **§d — HARDEN-04 compile-out forced-Active mint (D-09/D-10):** replace the runtime
  CAPRUN_ENABLE_IPC_CREATE_SESSION env gate (server.rs:918-932) with a dedicated Cargo feature
  (test-fixtures) so the forced-Active CreateSession arm is PHYSICALLY ABSENT from a default release
  build. Cite the RESEARCH §d live precedent: crates/executor/Cargo.toml:22-28 already ships the exact
  `test-fixtures = []` + self dev-dependency + `#[cfg(any(test, feature = "test-fixtures"))]` /
  `#[cfg(not(...))]` dual-arm idiom (sink_schema.rs:75-101); crates/brokerd/Cargo.toml has NO
  [features] section today. Pin the `#[cfg(not(...))]` sibling arm returning the SAME Error response
  the runtime flag returns today (identical wire behavior, code path physically absent). Pin D-10's
  OWN NEGATIVE GATE (a genuinely new discipline, no precedent in the codebase): the RESEARCH-preferred
  option (c) — a featureless-build BEHAVIORAL negative test that hits CreateSession over a real socket
  and asserts it always returns fail-closed Error (no env var to set, no feature to enable), pinned as
  primary; binary symbol-inspection (a) as optional defense-in-depth only. Confirm Phase 30's live
  proof runs a FEATURELESS release binary (cargo build --workspace --release, NOT the default
  cargo test which unifies dev-dep features).
- **§e — HARDEN-05 file.create contents slot (D-11/D-12):** RESEARCH CORRECTION (i), the single most
  load-bearing pin — expected_role(file.create, "contents") MUST be `Some(&["path"])`, explicitly
  justified by the end-to-end trace (worker.rs:293-312 reports only RelativePath claims;
  server.rs:1300-1313 hardcodes primary_role "path"; planner.rs:200-209 places intent_value_id (role
  "path") into BOTH the path AND contents slots) — the ONLY production value ever bound to contents
  today is the reused path-role handle, so any list NOT including "path" hard-Denies the only live
  file.create flow (s9_live_file_create_clean_allow, ROADMAP SC-3 regression). Pin BOTH sink_sensitivity.rs
  edits per RESEARCH §e: (a) add a `"file.create" => FILE_CREATE_CONTENT_SENSITIVE.contains(&arg_name)`
  arm to is_content_sensitive with FILE_CREATE_CONTENT_SENSITIVE = &["contents"] (new — wires a FUTURE
  tainted contents value into the I2 collect-then-Block path), scoped to &["contents"] ONLY so `path`
  never becomes content-sensitive; AND (b) change expected_role's :157 `"contents" => None` to
  `Some(&["path"])`. Note this is presently a no-op on the live path (no tainted-contents mint site
  exists yet) but is NOT dead code — it fires the moment a real content-extraction pipeline lands.
  Note the existing :315 unit assertion (expected_role file.create contents == None) INVERTS under the
  change and must be updated deliberately in Phase 29.

Author in directive prose. Do NOT paste Rust code blocks into the doc body beyond the short
`#[cfg(...)]` / table-entry identifier snippets already used by DESIGN-slot-type-binding.md as
precedent — name identifiers, signatures, file:line, and behavior.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-security-hardening.md && grep -q '## §a' planning-docs/DESIGN-security-hardening.md && grep -q '## §b' planning-docs/DESIGN-security-hardening.md && grep -q '## §c' planning-docs/DESIGN-security-hardening.md && grep -q '## §d' planning-docs/DESIGN-security-hardening.md && grep -q '## §e' planning-docs/DESIGN-security-hardening.md && grep -q 'Some(&\["path"\])' planning-docs/DESIGN-security-hardening.md && grep -qi 'fail-closed' planning-docs/DESIGN-security-hardening.md</automated>
  </verify>
  <done>
The doc exists with §0 + §a-§e. Each residual § names its current-code file:line anchor, its pinned
mechanism, and its fail-closed default. §a pins the workspace_rel new-plumbing budget and the
fd-grant causal-edge target. §b pins the keyed-MAC key-custody-across-processes answer, the anchored
head, AND rules on the second mutable table (pending_confirmations). §c pins that effect_id is
useless as a replay key and derives the idempotency key from plan-node content. §d pins the
test-fixtures feature + the featureless-build negative gate. §e pins contents expected-role =
Some(&["path"]) with the regression-canary justification. Automated grep confirms all five section
markers, the load-bearing Some(&["path"]) pin, and the fail-closed language.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author §f (cross-cutting X-01/X-02/X-03 + X-04 ruling), Adversarial-Review-Preemption, Accepted Residual Risks, Phase 27-30 Implementation Map, Proof-plan note, and Acceptance Predicate</name>
  <files>planning-docs/DESIGN-security-hardening.md</files>
  <action>
Append the cross-cutting and closing sections to the doc.

**§f — Cross-Cutting rulings.** Pin ONE uniform rule for each, per CONTEXT.md X-01/X-02/X-03 and the
RESEARCH §f groundings/corrections:
- **X-01 (label continuity):** ONE label-provenance model answers both HARDEN-01 and HARDEN-05. Per
  RESEARCH §f: a path is "trusted-labeled" ONLY if it equals the CLI's designated `<workspace-file>`
  argument (workspace_rel); a broker-WRITTEN file (via file.create) is by this rule automatically NOT
  trusted-labeled, so a later RequestFd on it fail-closed-demotes by default — this gives X-01 a
  fail-closed answer for v1.6 without new xattr/sidecar machinery. Record "broker-written files carry
  no explicit label; treated untrusted-by-default via the same is_trusted_labeled fail-closed rule"
  as the mechanism, and note the write→re-read laundering loop (D-12) is thereby closed for v1.6 and
  the full output-file provenance model is explicitly deferred.
- **X-02 (shared-store recovery authority):** RESEARCH CORRECTION (iv) — REFRAME. There is NO
  persistent broker daemon that restarts mid-session; run_broker_server is spawned fresh once per
  caprun invocation and never re-reads sessions.status after creation. The REAL restart actor is the
  `caprun confirm`/`caprun deny`/`caprun review` process (a fresh OS process reading audit.db fresh —
  audit.rs:69-71). Pin ONE rule applying uniformly to BOTH the events chain (via HARDEN-02's anchor)
  AND pending_confirmations (which has no MAC of its own today, §b): on every confirm/deny process
  start, recovered security state (PendingConfirmation.state/resolved_args/combined_digest and the
  sessions.status/events chain) is either MAC-re-verified (ties to HARDEN-02) or fail-closed
  re-derived. State the choice explicitly.
- **X-03 (TOCTOU / atomic ordering):** ONE uniform atomicity rule — the status transition commits
  BEFORE effect dispatch (same discipline as demote-before-fd-release in D-01). Note RESEARCH §f
  confirms the submit-side ordering already follows authorize-before-effect (event appended under
  lock before any sink invoke, server.rs:709-751 before :761-866); the genuine gap is the staleness
  finding below.

**X-04 RULING (mandatory — the RESEARCH §f NEW finding; do NOT leave it unstated).** Author a
dedicated named subsection ruling on the Planner-connection session_status staleness: every
connection seeds its per-connection session_status local from the SAME initial_session_status
function parameter (server.rs:202/:231), never re-read from the sessions table; a Worker-connection
ReportClaims demotion mutates only the WORKER connection's own `&mut SessionStatus` local
(server.rs:1131), invisible to a later Planner connection, so the Planner's SubmitPlanNode evaluates
Step 0.5 (I0 class-level Draft+CommitIrreversible deny) against a STALE Active status. Explicitly
DECIDE one of: (a) fold into HARDEN-01's Phase-27 scope as a required fix — make session_status a
shared Arc<Mutex<SessionStatus>> re-read per dispatch_request, mirroring the existing
planner_slot_occupied: Arc<AtomicBool> pattern (server.rs:185) [RESEARCH's recommended, more
consistent with X-03]; or (b) name and accept it as a residual with justification. If (a), state
whether REQUIREMENTS.md needs a new HARDEN-0X id or it is in-scope-by-extension of HARDEN-01. Give
the reviewer a clean target — do not bury the ruling in §a prose.

**Adversarial-Review-Preemption §.** Mirror DESIGN-slot-type-binding.md §8: list the probes a fresh
non-self reviewer will raise (one per residual + cross-cutting), each answerable by tracing the cited
code, not by trusting this doc. Include at minimum: can a demotion be skipped by a silent worker
(§a)? can a bare DB-writer forge a chain / does key custody leak to the worker (§b, RESEARCH
Assumption A2 = highest-stakes)? does an effect_id-keyed CAS actually stop replay (§c)? does a plain
cargo test silently build WITH the feature, defeating the gate (§d)? does the contents role list
break the only live flow (§e)? is the X-04 staleness fix (or its acceptance) sound?

**Accepted Residual Risks §.** Mirror DESIGN-slot-type-binding.md §10. Record at least: D-05 (full
host/root compromise reading the broker key — out of scope, candidate external notarization); D-08
(effects-budget beyond per-plan-node CAS — statically-compromised worker minting N distinct nodes
still sends N times; named future defense-in-depth); D-12 (full output-file provenance labeling —
deferred; v1.6 uses input-role treatment + X-01 continuity); and, if X-04 is ruled (b), that residual.

**Phase 27-30 Implementation Map (informative — not part of the gate).** Mirror
DESIGN-slot-type-binding.md §9: map §a-§e to Phases 27 (HARDEN-01+04), 28 (HARDEN-02), 29
(HARDEN-03+05), 30 (HARDEN-06). Note the check-invariants.sh discipline (Gate 1 no EffectRequest,
Gate 3 mint-call-site restriction) and that Phase 27+ must re-verify file:line if commits intervene.

**Proof-plan note (for Phase 30).** Pin the standing close-gate disciplines: Phase 30 uses the BARE
`scripts/mailpit-verify.sh` recipe; capture `$?` BEFORE any pipe; assert on the PASSED sentinel +
named test counts, never exit-0-through-a-pipe; map one negative test per closed residual (forged/
truncated chain rejected; replayed Allowed send delivers exactly once; forced-Active arm absent from
the featureless release binary; RequestFd-alone demotes while CONTROL-01 clean path still sends;
tainted contents blocks). Note Phase 30 must run a FEATURELESS release binary (§d).

**Acceptance Predicate — Done When.** Mirror DESIGN-slot-type-binding.md's closing predicate: list
the conditions under which Phase 26's gate is cleared (doc pins all five mechanisms+defaults; pins
the three cross-cutting rulings; rules explicitly on X-04; has an Adversarial-Review-Preemption § and
Accepted Residual Risks §; AND has cleared a fresh non-self adversarial review recorded in
DESIGN-GATE-RECORD-v1.6.md with no crates/executor|brokerd|runtime-core hardening code yet — the last
condition is satisfied by Plan 26-02). Add an empty "## Amendments (post-review)" section header at
the end for Plan 26-02 to fold round-tagged review amendments into.
  </action>
  <verify>
    <automated>grep -q '## §f' planning-docs/DESIGN-security-hardening.md && grep -qi 'X-04' planning-docs/DESIGN-security-hardening.md && grep -qi 'Adversarial-Review' planning-docs/DESIGN-security-hardening.md && grep -qi 'Accepted Residual' planning-docs/DESIGN-security-hardening.md && grep -qi 'Implementation Map' planning-docs/DESIGN-security-hardening.md && grep -qi 'Acceptance Predicate' planning-docs/DESIGN-security-hardening.md && grep -q 'Amendments' planning-docs/DESIGN-security-hardening.md</automated>
  </verify>
  <done>
The doc's §f pins X-01/X-02/X-03 as one uniform rule each (X-02 reframed to the confirm/deny process
+ pending_confirmations per RESEARCH correction iv). A dedicated named X-04 subsection explicitly
RULES on the Planner-connection staleness finding (fold vs accept). The Adversarial-Review-Preemption,
Accepted Residual Risks, Phase 27-30 Implementation Map, Proof-plan note, and Acceptance Predicate
sections all exist, mirroring DESIGN-slot-type-binding.md. An empty "## Amendments (post-review)"
header is present for Plan 26-02. Automated grep confirms §f, X-04, and all five closing section
markers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner artifact → downstream executor | This plan produces ONLY a markdown design doc under `planning-docs/`. It crosses no runtime trust boundary and writes no code. The threats it must adequately DESCRIBE are the five TCB-local residuals themselves (catalogued in the doc's own STRIDE-aligned §§). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | The design doc under-specifies a mechanism, letting a downstream implementer build a bypassable fix | high | mitigate | Decisions-not-options discipline + every § grounded in re-verified file:line; the fresh non-self adversarial review (Plan 26-02, DESIGN-12) is the backstop gate before any code |
| T-26-02 | Elevation of Privilege | The X-04 staleness finding is left unstated and inherited silently as a bypass of the I0 class-deny | high | mitigate | Mandatory dedicated X-04 ruling subsection (Task 2) — the doc must decide fold-vs-accept, giving the reviewer a clean target |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — doc-only; 26-RESEARCH.md Package Legitimacy Audit records N/A. No install tasks exist. |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-security-hardening.md` — the doc exists.
- `grep -c '^## §' planning-docs/DESIGN-security-hardening.md` returns ≥ 6 (§0 uses a different marker; §a-§f present).
- All five residual section markers (`## §a`..`## §e`), `## §f`, and the closing sections resolve via the per-task automated greps.
- The load-bearing pins are literally present: `Some(&["path"])` (§e), `X-04` ruling (§f), fail-closed language throughout.
- No code written: `git status --porcelain crates/ cli/` is empty after this plan (re-confirmed formally in Plan 26-02's hard-gate task).
</verification>

<success_criteria>
`planning-docs/DESIGN-security-hardening.md` exists and, for each of the five residuals, pins a
mechanism + fail-closed default grounded in a re-verified file:line; pins the three cross-cutting
rulings (with X-02 reframed to the confirm/deny process); contains an explicit X-04 ruling; and
carries the Adversarial-Review-Preemption, Accepted Residual Risks, Phase 27-30 Implementation Map,
Proof-plan, and Acceptance Predicate sections plus an empty Amendments header — mirroring
DESIGN-slot-type-binding.md. All four RESEARCH corrections are incorporated. DESIGN-11 is satisfied
(subject to the Plan 26-02 review clearing DESIGN-12). No hardening code is written.
</success_criteria>

<output>
Create `.planning/phases/26-security-hardening-design-gate/26-01-SUMMARY.md` when done.
</output>
