---
phase: 26-security-hardening-design-gate
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - planning-docs/DESIGN-GATE-RECORD-v1.6.md
  - planning-docs/DESIGN-security-hardening.md
  - planning-docs/DESIGN-session-trust-state.md
autonomous: true
requirements: [DESIGN-12]
must_haves:
  truths:
    - "A FRESH, NON-SELF reviewer (a separate Agent, model fable — advisor is unavailable this session) TRACES THE CODE and re-runs the RESEARCH greps against live source, not a self-read."
    - "Every review finding is resolved and folded into DESIGN-security-hardening.md as round-tagged amendments; no finding is resolved by weakening a ruling."
    - "DESIGN-GATE-RECORD-v1.6.md records the reviewer identity/independence, per-residual checklist, findings & disposition, and the hard-gate re-confirmation — mirroring DESIGN-GATE-RECORD-v1.5.md."
    - "DESIGN-session-trust-state.md is amended to name RequestFd as a second broker-side I1 demotion site (the D-02 reconciliation)."
    - "NO hardening code exists in crates/executor, crates/brokerd, or crates/runtime-core — grep + git-status + check-invariants.sh evidence recorded in the GATE-RECORD."
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.6.md
  key_links:
    - "The reviewer opens and traces the actual server.rs / audit.rs / sink_sensitivity.rs / quarantine.rs / confirmation.rs surfaces the doc cites, and independently re-runs the RESEARCH greps — a self-read is explicitly insufficient (fresh-context-adversarial-review discipline)."
    - "The GATE-RECORD's hard-gate section ties clearance to empty git status on crates/ + cli/ and a green check-invariants.sh."
---

<objective>
Clear the DESIGN-12 gate: subject `planning-docs/DESIGN-security-hardening.md` (authored in Plan
26-01) to a FRESH, NON-SELF adversarial review by a separate reviewer agent that TRACES THE CODE
(not a self-read), resolve every finding as a round-tagged amendment folded into the doc, amend
`planning-docs/DESIGN-session-trust-state.md` per the D-02 reconciliation, and record the whole
review in `planning-docs/DESIGN-GATE-RECORD-v1.6.md` (mirroring `DESIGN-GATE-RECORD-v1.5.md`). Then
formally confirm the hard gate: NO hardening code exists in the three TCB crates yet.

Purpose: This is the gate that unblocks Phases 27-30. The review must be GENUINELY non-self — the
project's standing `fresh-context-adversarial-review` discipline holds that a self-read caught 0 of 5
blockers a fresh code-tracing reviewer caught in a prior milestone. Advisor is unavailable this
session, so the reviewer is a separate `Agent(model:"fable")` (the standing fallback that has caught
real blockers), spawned by the executor with a self-contained code-tracing prompt.

Output: `planning-docs/DESIGN-GATE-RECORD-v1.6.md` (satisfies DESIGN-12), round-tagged amendments in
`DESIGN-security-hardening.md`, and the D-02 amendment to `DESIGN-session-trust-state.md`. NO code.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-security-hardening-design-gate/26-CONTEXT.md
@.planning/phases/26-security-hardening-design-gate/26-RESEARCH.md

# The doc under review (authored by Plan 26-01):
@planning-docs/DESIGN-security-hardening.md

# GATE-RECORD format precedent — MIRROR this exactly (reviewer identity, checklist, findings, hard-gate):
@planning-docs/DESIGN-GATE-RECORD-v1.5.md

# The doc §a's D-02 reconciliation amends (its :80-81 clause + :84-87 anti-self-declaration rationale):
@planning-docs/DESIGN-session-trust-state.md

# TCB constraints + check-invariants gates (hard-gate re-confirmation):
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spawn a fresh, non-self code-tracing adversarial reviewer and capture findings</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.6.md</files>
  <action>
Spawn a SEPARATE reviewer agent — `Agent(model:"fable")` (advisor is unavailable this session; this
is the standing fallback per the project's `fresh-context-adversarial-review` and
`advisor-tool-unavailable-fable-fallback` disciplines). The reviewer prompt MUST be self-contained
and MUST require the reviewer to:
- Read `planning-docs/DESIGN-security-hardening.md` in full.
- INDEPENDENTLY OPEN AND TRACE the live code the doc cites — at minimum `crates/brokerd/src/server.rs`
  (RequestFd arm, evaluate_plan_node_and_record email.send Allowed block, CreateSession arm,
  run_broker_server/classify_second_connection/dispatch_request, the per-connection session_status
  seeding at :202/:231/:1131), `crates/brokerd/src/audit.rs` (compute_event_hash, append_event,
  verify_chain, blocked_literals, the pending_confirmations table), `crates/brokerd/src/confirmation.rs`
  (transition_state, the SEND-01 CAS pattern), `crates/brokerd/src/quarantine.rs` (mint_from_read
  demotion), `crates/executor/src/sink_sensitivity.rs` (is_content_sensitive, expected_role :157),
  `cli/caprun/src/planner.rs` (the contents/path handle reuse), `cli/caprun/src/worker.rs`,
  `cli/caprun/src/main.rs` (workspace_rel / WORKSPACE_FILE threading).
- Independently RE-RUN the key greps and report the results: `grep -n "CAPRUN_ENABLE_IPC_CREATE_SESSION"
  crates/brokerd/src/server.rs`, `grep -n "session_status" crates/brokerd/src/server.rs`,
  `grep -n "contents" cli/caprun/src/planner.rs`, `grep -n "contents"
  crates/executor/src/sink_sensitivity.rs`, and a blast-radius grep for any new enum variant the doc
  implies (`grep -rn "DenyReason" crates/ cli/`).
- Adversarially probe EACH of the doc's Adversarial-Review-Preemption items against the code it
  traced — especially the highest-stakes ones flagged in 26-RESEARCH.md: §b key-custody-across-
  processes (Assumption A2 — can the confined worker or a bare DB-writer reach the key?), §c
  effect_id-is-fresh-per-call (does the doc's idempotency key actually defeat replay?), §e contents
  expected-role = Some(&["path"]) (does it regress the only live file.create flow, or over-broaden?),
  §d the featureless-build negative gate (does a plain cargo test defeat it?), and the X-04
  Planner-connection staleness ruling (is fold-or-accept sound and does the fix mirror
  planner_slot_occupied correctly?).
- Return findings classified by severity (BLOCKER / MAJOR / MINOR / NIT), each with file:line
  evidence and a concrete resolution, and an explicit cross-cutting verdict on whether anything
  undermines the security design itself.

Do NOT self-review. Record the reviewer's identity, model, independence rationale, effort (token/tool
counts if available), the re-run grep results, and the raw findings list into a NEW file
`planning-docs/DESIGN-GATE-RECORD-v1.6.md` under a "Reviewer identity & independence", "Independent
re-verification", and "Findings & Disposition" scaffold mirroring DESIGN-GATE-RECORD-v1.5.md. Leave
dispositions to Task 2. If the reviewer returns ANY finding, this task does not clear the gate on its
own — Task 2 resolves them.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.6.md && grep -qi 'fable' planning-docs/DESIGN-GATE-RECORD-v1.6.md && grep -qi 'independen' planning-docs/DESIGN-GATE-RECORD-v1.6.md && grep -qiE 'finding|BLOCKER|MAJOR|MINOR|NIT' planning-docs/DESIGN-GATE-RECORD-v1.6.md</automated>
  </verify>
  <done>
A fresh, non-self `Agent(model:"fable")` reviewer read the doc, INDEPENDENTLY opened and traced the
cited live code, and re-ran the RESEARCH greps. `DESIGN-GATE-RECORD-v1.6.md` exists and records the
reviewer identity/model, the independence rationale (not the authoring context), the re-run grep
results, and every finding classified by severity with file:line evidence and a cross-cutting
verdict. Automated grep confirms the file exists and carries the reviewer (fable), independence, and
findings scaffold.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve every finding, fold round-tagged amendments into the doc, amend DESIGN-session-trust-state.md, and complete the GATE-RECORD</name>
  <files>planning-docs/DESIGN-security-hardening.md, planning-docs/DESIGN-session-trust-state.md, planning-docs/DESIGN-GATE-RECORD-v1.6.md</files>
  <action>
For EACH finding the reviewer returned, resolve it by amending the design doc — NEVER by weakening a
locked ruling or by designing a mechanism that is locked out. Fold each resolution into the relevant
§ of `planning-docs/DESIGN-security-hardening.md` as a round-tagged amendment, and summarize them
under the doc's "## Amendments (post-review)" section (mirroring DESIGN-slot-type-binding.md's
"Round 1 (DESIGN-GATE-RECORD-v1.5 ...)" convention: tag each as
`Round N (DESIGN-GATE-RECORD-v1.6, fresh independent Fable reviewer, <date>)` with severity and the
§ it touched). If any finding conflicts with a CONTEXT.md locked decision (D-01..D-12, X-01..X-03),
honor the locked decision and note the reviewer's concern as an Accepted Residual Risk instead of
overriding the lock. If the reviewer raises a BLOCKER that cannot be resolved without a new locked
decision, STOP and surface it — do not clear the gate.

Amend `planning-docs/DESIGN-session-trust-state.md` per D-02 (the reconciliation): add one sentence to
its :80-81 clause naming the new `RequestFd`-entry demotion as a SECOND, broker-side, trusted-path-only
I1 demotion site, and add a second causal-edge case to its §5 for the `fd_granted`-parented demotion —
preserving (not violating) the :84-87 anti-self-declaration invariant (both demotion sites remain
broker-only, never worker-asserted). Keep this amendment surgical; do not restructure the doc.

Complete `planning-docs/DESIGN-GATE-RECORD-v1.6.md` mirroring DESIGN-GATE-RECORD-v1.5.md's full
structure: Gate status (CLEARED with date/round only after ALL findings are resolved), Reviewer
identity & independence, Revision History table, Independent re-verification (the grep results),
Per-Residual Checklist (one row per §a-§e + the cross-cutting rulings + X-04, each with a
PASS/verdict + evidence), Findings & Disposition table (each finding marked Resolved with how), a
"How to Verify (human review steps)" section, and the Decision. Every finding must be marked resolved
and folded, and the record must state that no ruling was weakened.
  </action>
  <verify>
    <automated>grep -qi 'DESIGN-GATE-RECORD-v1.6' planning-docs/DESIGN-security-hardening.md && grep -qiE 'RequestFd' planning-docs/DESIGN-session-trust-state.md && grep -qi 'CLEARED' planning-docs/DESIGN-GATE-RECORD-v1.6.md && grep -qiE 'resolved' planning-docs/DESIGN-GATE-RECORD-v1.6.md</automated>
  </verify>
  <done>
Every reviewer finding is resolved and folded into DESIGN-security-hardening.md as a round-tagged
amendment under "## Amendments (post-review)"; no ruling was weakened (or a BLOCKER was surfaced
rather than clearing the gate). DESIGN-session-trust-state.md is amended to name RequestFd as a
second broker-side I1 demotion site with the fd_granted-parented causal edge (D-02). The GATE-RECORD
is complete with a per-residual checklist, findings-all-resolved disposition, and a CLEARED decision
mirroring DESIGN-GATE-RECORD-v1.5.md. Automated grep confirms the amendment cross-reference in the
doc, the RequestFd amendment in the session-trust doc, and CLEARED/resolved in the GATE-RECORD.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hard-gate re-confirmation — prove NO hardening code exists in the three TCB crates</name>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.6.md</files>
  <action>
Formally confirm the DESIGN-12 hard gate: no `crates/executor`, `crates/brokerd`, or
`crates/runtime-core` hardening code was written this phase. Run and record the evidence into the
GATE-RECORD's "Hard-gate re-confirmation (no TCB code this phase)" section (mirroring
DESIGN-GATE-RECORD-v1.5.md):
- `git status --porcelain crates/ cli/` → must be EMPTY (the only files changed this phase are
  `planning-docs/*.md` and `.planning/**`).
- `grep -rniE 'hmac|verify_chain.*key|chain_anchor|sent_plan_nodes|is_trusted_labeled|test-fixtures' crates/`
  → must show NO new hardening mechanism symbols (the five residual fixes land in Phases 27-29, not
  here). Record the actual output (expected: only pre-existing incidental matches, if any, none of
  which are the new v1.6 mechanisms).
- `./scripts/check-invariants.sh` → Gate 1 (no raw EffectRequest), Gate 2 (runtime-core purity),
  Gate 3 (mint-call-site restriction) all PASS.
Record all three results verbatim in the GATE-RECORD and state the Decision (CLEARED) only if git
status is empty, no new hardening symbols exist, and check-invariants passes. This is the condition
that unblocks Phases 27-30.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain crates/ cli/)" && ./scripts/check-invariants.sh && grep -qi 'Hard-gate' planning-docs/DESIGN-GATE-RECORD-v1.6.md</automated>
  </verify>
  <done>
`git status --porcelain crates/ cli/` is empty; the hardening-symbol grep shows no new v1.6 mechanism
code in the three TCB crates; `./scripts/check-invariants.sh` passes all three gates. All three
results are recorded verbatim in the GATE-RECORD's Hard-gate re-confirmation section, and the
Decision is CLEARED. Phases 27-30 are unblocked. Automated verify confirms the empty crates/cli
working tree, a green check-invariants.sh, and the Hard-gate section present in the record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-authored design → independent reviewer | The gate's entire value is that the reviewer is NON-SELF and code-tracing. A self-read (or a prose-only read) does not cross this boundary and does not satisfy DESIGN-12. |
| planning artifact → TCB crates | The hard gate: no `crates/executor`/`crates/brokerd`/`crates/runtime-core` code may change this phase. Enforced by git-status + check-invariants evidence in Task 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Spoofing | A self-read masquerading as an independent review clears the gate on a design that a real code-tracing reviewer would reject | high | mitigate | Task 1 mandates a separate `Agent(model:"fable")` that independently opens/traces the cited code and re-runs the greps; the GATE-RECORD records identity/independence + the re-run grep output as evidence |
| T-26-04 | Tampering | A reviewer finding is silently "resolved" by weakening a locked ruling, hollowing the fix | high | mitigate | Task 2 forbids resolving-by-weakening; conflicts with a locked decision become Accepted Residual Risks, and an unresolvable BLOCKER is surfaced rather than cleared |
| T-26-05 | Elevation of Privilege | Hardening code sneaks into a TCB crate under cover of the design phase, bypassing the review gate | medium | mitigate | Task 3 hard-gate: empty `git status` on crates/+cli/, a no-new-symbols grep, and a green check-invariants.sh recorded before CLEARED |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — doc/review-only; 26-RESEARCH.md Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-GATE-RECORD-v1.6.md` — the gate record exists.
- The reviewer is non-self and code-tracing: GATE-RECORD records the fable model, independence
  rationale, and re-run grep results (Task 1 automated grep).
- Every finding resolved and folded: `grep -qi 'resolved' planning-docs/DESIGN-GATE-RECORD-v1.6.md`
  and the doc's "## Amendments (post-review)" section carries round-tagged entries (Task 2).
- D-02 amendment present: `grep -qiE 'RequestFd' planning-docs/DESIGN-session-trust-state.md`.
- Hard gate proven: `git status --porcelain crates/ cli/` empty AND `./scripts/check-invariants.sh`
  passes (Task 3 automated).
</verification>

<success_criteria>
`planning-docs/DESIGN-security-hardening.md` has cleared a FRESH, NON-SELF, code-tracing adversarial
review with every finding resolved and folded as round-tagged amendments (no ruling weakened);
`planning-docs/DESIGN-GATE-RECORD-v1.6.md` records the review mirroring DESIGN-GATE-RECORD-v1.5.md
and is marked CLEARED; `planning-docs/DESIGN-session-trust-state.md` is amended to name RequestFd as
a second broker-side I1 demotion site (D-02); and the hard gate is proven — no hardening code in
crates/executor|brokerd|runtime-core (empty git status on crates/+cli/, no new mechanism symbols,
green check-invariants.sh). DESIGN-12 is satisfied and Phases 27-30 are unblocked. No code written.
</success_criteria>

<output>
Create `.planning/phases/26-security-hardening-design-gate/26-02-SUMMARY.md` when done.
</output>
