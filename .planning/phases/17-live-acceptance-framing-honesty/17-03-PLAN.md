---
phase: 17-live-acceptance-framing-honesty
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - cli/caprun/tests/live_acceptance_v1_3.rs
autonomous: true
requirements: [ACCEPT-01]

must_haves:
  truths:
    - "Tooth #2 (HARD GATE, milestone-failing): Phase 15's genuine-taint proof is RE-RUN against BOTH hostile sessions' to+body anchors produced by THIS composed live run — using the promoted brokerd::provenance_proof functions, not a reimplementation (COORD-T2)."
    - "The proof holds for EVERY blocked arg in each 2-anchor block (both `to` AND `body`) — a block with only one edge proven is a partial pass, i.e. a FAIL."
    - "The derived `to` anchor additionally satisfies the payload-bound genuine-derivation predicate (a real derivation event binds its value_id to its input read chains)."
    - "Both anti-staple controls resolve against the anchors THESE invocations produced: (A) a fabricated root is REJECTED; (B) a naive re-anchored staple minted into the live session is REJECTED specifically on the payload-binding predicate, not on session-wide existence."
    - "The phase SUMMARY documents that 'one unbroken DAG' means per-session verify_chain integrity across a SHARED audit log, NOT a cross-session linked chain (COORD-SUMMARY, COORD-INTERP)."
  artifacts:
    - "cli/caprun/tests/live_acceptance_v1_3.rs — tooth #2 assertion block appended at the Plan-02 sentinel"
    - "17-03-SUMMARY.md — carries the COORD-SUMMARY interpretation sentence verbatim"
  key_links:
    - "brokerd::provenance_proof::{assert_unbroken_edge, genuine_derivation_binds} (Plan 01) → the live hostile anchors (Plan 02 run) — the reuse edge proving descent, not staple"
    - "the `to` value's `derivation` event input_provenance_chains → independently-reconstructed expected roots → identity-pinned against the anchor's provenance_chain"
---

<objective>
Append the milestone's HARD GATE — the genuine-taint re-proof (assertion tooth
#2) — to the composed live test `live_acceptance_v1_3_composed`, so the live run
does not merely pass on the presence of a block but proves the block's taint was
GENUINELY propagated from the raw doc reads through the extractor to the sink
args, for EVERY blocked arg, with both anti-staple controls rejecting fabricated
and re-anchored values against the anchors this run actually produced.

Purpose: Phase 17 success criterion #3 and CLAUDE.md's single non-negotiable
invariant ("taint stapled at the sink proves nothing") make this a
milestone-failing gate. The coordinator's tooth #2 explicitly requires
re-running `assert_unbroken_edge`/`genuine_derivation_binds` (promoted to
`brokerd::provenance_proof` in Plan 01) against the hostile sessions' `to`+`body`
anchors IN THIS composed run — "Do not assume Phase 15's own test coverage
suffices — the composition must re-prove descent here, or the 'one DAG' claim is
decorative."

Output: the tooth-#2 assertion block inside `live_acceptance_v1_3_composed`, and
the 17-03-SUMMARY.md interpretation sentence (COORD-SUMMARY).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-live-acceptance-framing-honesty/17-RESEARCH.md
@crates/brokerd/tests/extract_provenance_threading.rs
@cli/caprun/tests/live_acceptance_v1_3.rs
</context>

<coordinator_gate>
Executed under caprun-opus-77's locked ruling. Tooth #2 is the HARD GATE — if
it can be satisfied by a value stapled fresh at the sink, the milestone is NOT
done. Any FORCED design deviation → STOP and flag caprun-opus-77. This is the
last code wave of the final phase — do NOT let the doc-completion commit flip
ROADMAP.md's phase-level checkbox (the recurring 2-for-2 GSD bug); mark only
this plan's own line.
</coordinator_gate>

<tasks>

<task type="auto">
  <name>Task 1: Append the tooth-#2 genuine-taint re-proof to the composed live test</name>
  <read_first>
    - crates/brokerd/tests/extract_provenance_threading.rs (the CANONICAL usage of the proof functions: `extract_02_and_03_positive_proof_both_anchors` for the positive per-anchor walk + `genuine_derivation_binds`; `extract_02_anti_staple_control_a_fabricated_root_is_rejected` and `..._control_b_reanchored_staple_is_rejected` for the two controls — the exact call shapes and the control-B `mint_from_read`-onto-sink_blocked-hash re-anchor pattern)
    - crates/brokerd/src/provenance_proof.rs (the promoted `pub fn`s this task calls — signatures + the identity-pin/per-element/payload-bound contracts)
    - crates/brokerd/src/audit.rs (`find_event_by_id`, `find_event_by_type`, `event_hash_by_id` — all pub; note find_event_by_type is LIMIT-1 and unsafe for multi-instance event types, so scan `derivation`/`file_read` sets explicitly)
    - cli/caprun/tests/live_acceptance_v1_3.rs (the composed function + the `// TEETH-2-INSERTION-POINT` sentinel left by Plan 02; the two hostile sessions to target)
    - crates/brokerd/src/quarantine.rs (`Claim`, `mint_from_read` — needed for anti-staple control B's naive re-anchor; note it is exempt from check-invariants Gate 3 inside a tests/ file)
  </read_first>
  <files>cli/caprun/tests/live_acceptance_v1_3.rs</files>
  <action>
    Replace the `// TEETH-2-INSERTION-POINT ...` sentinel in
    `live_acceptance_v1_3_composed` with the genuine-taint re-proof, importing
    `use brokerd::provenance_proof::{assert_unbroken_edge, genuine_derivation_binds};`
    at the top of the file. Run the proof against BOTH hostile sessions (the
    confirm-leg session and the deny-leg session) — capture each session_id when
    its leg runs (Plan 02 already discovers them). For EACH hostile session:

    1. Load its durable `sink_blocked` event (find_event_by_type on that session)
       and split its anchors into the `to` anchor and the `body` anchor
       (`anchors.iter().find(|a| a.arg == "to"/"body")`). Assert exactly two
       anchors with arg names `{"body","to"}` (sanity — collect-then-Block).

    2. POSITIVE per-anchor unbroken edge (both anchors — a one-edge pass is a
       FAIL). Reconstruct the `to` anchor's expected roots INDEPENDENTLY of the
       anchor itself: scan the session's `derivation` events (NO-LIMIT inline
       SELECT of `payload` where event_type='derivation'), find the one whose
       `derived_value_id == to_anchor.value_id`, and take
       `union_of(input_provenance_chains)` as `expected_roots_to`. For the `body`
       anchor (a direct read, no derivation), use
       `expected_roots_body = vec![body_anchor.read_event_id]`. Then:
         - `assert_unbroken_edge(&conn, &sid, &to_anchor.provenance_chain, &expected_roots_to).expect(...)`
         - `assert_unbroken_edge(&conn, &sid, &body_anchor.provenance_chain, &expected_roots_body).expect(...)`
       (each internally re-checks every element resolves to a REAL untrusted
       `file_read` and identity-pins the chain — see provenance_proof doc).

    3. PAYLOAD-BOUND genuine-derivation for the derived `to` (finding #2):
       `assert!(genuine_derivation_binds(&conn, &sid, &to_anchor.value_id, &to_anchor.provenance_chain), ...)`.

    4. EXTRACT-03 survival: `assert!(to_anchor.taint.iter().any(|t| t.is_untrusted()))`
       (the concat-derived recipient still carries untrusted taint after the
       transform).

    5. ANTI-STAPLE CONTROL A (fabricated root REJECTED): build
       `let fab = vec![uuid::Uuid::new_v4()];` and assert
       `assert_unbroken_edge(&conn, &sid, &fab, &fab).is_err()` with the error
       mentioning "does not resolve".

    6. ANTI-STAPLE CONTROL B (re-anchored staple REJECTED) — run LAST for this
       session because it MUTATES the DAG. Mirror
       `extract_02_anti_staple_control_b...`: mint the already-assembled recipient
       literal (`expected_recipient(&<that leg's nonce>)`) into THIS session via a
       PLAIN `mint_from_read` with an `email_address`-shaped `Claim`, chained onto
       the session's `sink_blocked` event hash (`event_hash_by_id`). Assert (a) a
       `derivation` event DOES exist in this session (the genuine block's own — so
       the check is not vacuously "no derivation events"), and (b)
       `!genuine_derivation_binds(&conn, &sid, &naive_value_id, &vec![naive_read_id])`
       — rejected on the payload-binding predicate, not on existence. Then assert
       `verify_chain(&conn, &sid)` still holds (single linear chain preserved).

    Apply steps 1-5 to BOTH hostile sessions; step 6 (mutating) may be applied to
    the confirm-leg hostile session (running it on both is acceptable but not
    required — the control proves the predicate's teeth, session-independent).
    Do NOT place any fenced code block, nor the literal `accounts@ev1l.com`, in
    the file (nonced `.ev1l.test` recipients only — COORD-N2).
  </action>
  <verify>
    <automated>MAILPIT_VERIFY_CMD='cargo test -p caprun --test live_acceptance_v1_3 live_acceptance_v1_3_composed' bash scripts/mailpit-verify.sh</automated>
  </verify>
  <done>Under scripts/mailpit-verify.sh on Linux (capture `$?` BEFORE any pipe), `live_acceptance_v1_3_composed` still passes WITH the tooth-#2 block: both hostile sessions' `to` and `body` anchors pass the per-anchor unbroken-edge proof; the derived `to` passes genuine_derivation_binds; anti-staple control A is rejected (unresolvable root); anti-staple control B is rejected on the payload-binding predicate while a derivation event provably exists; verify_chain remains true after control B's append. The `// TEETH-2-INSERTION-POINT` sentinel no longer appears in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Record the "one unbroken DAG" interpretation sentence in the plan SUMMARY (COORD-SUMMARY)</name>
  <read_first>
    - .planning/phases/17-live-acceptance-framing-honesty/17-RESEARCH.md (Open Questions #1 + Assumptions A1 — the shared-DB-multiple-sessions interpretation the coordinator ratified)
    - /Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md (SUMMARY structure)
  </read_first>
  <files>.planning/phases/17-live-acceptance-framing-honesty/17-03-SUMMARY.md</files>
  <action>
    When writing 17-03-SUMMARY.md for this plan, include — as a clearly-labeled,
    standalone sentence (not buried) — the COORD-SUMMARY clarification: that in
    ACCEPT-01, "one unbroken audit DAG" means per-session `verify_chain` integrity
    across a SHARED audit.db log (three sessions in one file, each independently
    chain-verified, plus a re-proven genuine-taint descent for the hostile
    anchors), and explicitly NOT a single cross-session `parent_id` linked chain
    spanning confirm/deny/clean — the latter would contradict
    `DESIGN-session-trust-state.md`'s pinned one-invocation-one-session model.
    State this so a future reader does not mistake the scope. This sentence is
    itself a DOC-01 line (Plan 04 carries the PROJECT.md-facing version).
  </action>
  <verify>
    <automated>grep -qi "per-session" .planning/phases/17-live-acceptance-framing-honesty/17-03-SUMMARY.md && grep -qi "cross-session" .planning/phases/17-live-acceptance-framing-honesty/17-03-SUMMARY.md && echo PRESENT</automated>
  </verify>
  <done>17-03-SUMMARY.md contains a standalone, labeled sentence stating "one unbroken audit DAG" = per-session verify_chain integrity across a shared audit log, explicitly NOT a cross-session linked chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live hostile anchors → genuine-taint verifier | the audit-DAG evidence a skeptic uses to decide whether the block proved anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-06 | Tampering | A value stapled fresh at the sink satisfying the live taint proof (the milestone-failing failure mode) | critical | mitigate | Re-run the promoted assert_unbroken_edge (per-element real-untrusted-file_read + identity-pin to independently-reconstructed roots) + genuine_derivation_binds for every blocked arg; anti-staple controls A and B must both reject |
| T-17-07 | Repudiation | Anti-staple control B passing vacuously because a derivation event merely exists in the session | high | mitigate | Assert a derivation event DOES exist AND the payload-bound predicate still rejects the re-anchored value (payload-bound, not existence-based) |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency; test-only additions |
</threat_model>

<verification>
- This plan's live gate: `MAILPIT_VERIFY_CMD='cargo test -p caprun --test live_acceptance_v1_3 live_acceptance_v1_3_composed' bash scripts/mailpit-verify.sh` on Linux, `$?` captured BEFORE any pipe, asserting on the named test and pass count.
- 17-03-SUMMARY.md carries the COORD-SUMMARY interpretation sentence.
</verification>

<success_criteria>
The composed live test proves genuine taint descent for every blocked arg of both
hostile sessions and rejects both fabricated and re-anchored staples; the phase
SUMMARY documents the shared-DB per-session interpretation of "one unbroken DAG."
</success_criteria>

<output>
Create `.planning/phases/17-live-acceptance-framing-honesty/17-03-SUMMARY.md` when done.
</output>