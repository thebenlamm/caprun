---
phase: 17-live-acceptance-framing-honesty
plan: 04
type: execute
wave: 4
depends_on: ["17-02", "17-03"]
files_modified:
  - .planning/PROJECT.md
autonomous: false   # Task 2 (point 7) is gated on caprun-opus-77's OWN independent live-proof re-run — see the plan-level and Task-2 coordinator_gate (COORD MED-4). This plan MUST NOT auto-advance past Task 2.
requirements: [DOC-01]

must_haves:
  truths:
    - "DOC-01 point 1: PROJECT.md states CONTROL-01 proves 'trusted-intent send Allowed AND delivered vs doc-derived send Blocked' — NOT 'same doc, taint flipped'; the benign doc is decorative on the clean path."
    - "DOC-01 point 2 [HOLD THE LINE]: PROJECT.md states I1 draft-only triggers on a REPORTED read via mint_from_read, NOT on fd release — a worker that reads the doc and reports nothing stays Active (v2: demote at RequestFd)."
    - "DOC-01 point 3 [HOLD THE LINE]: PROJECT.md states verify_chain detects single-store and non-recomputing multi-store tampering but is NOT externally anchored — an actor with events write access can forge it end-to-end (accepted residual risk; v2 keyed-MAC/head-pin)."
    - "DOC-01 point 4: PROJECT.md states guard-(c) (CAPRUN_ENABLE_IPC_CREATE_SESSION) is a runtime default-deny flag, not a compile-time exclusion — the forced-Active mint code ships in the production binary (v2: build-exclude)."
    - "DOC-01 point 5: PROJECT.md states the Allowed-path send has no CAS — a replayed SubmitPlanNode sends N emails (accepted residual risk)."
    - "DOC-01 point 6: PROJECT.md states ProvideIntent mints worker-declared intent as UserTrusted only before any fd read, exactly once, broker-ENFORCED (not assumed)."
    - "DOC-01 point 7 (LATE, COORD MED-4): PROJECT.md states CONFIRM-01's verbatim narration is proven end-to-end for the FIRST time in Phase 17 (synthetic fixture at Phase 16) — added ONLY after caprun-opus-77 has INDEPENDENTLY re-run the live proof (scripts/mailpit-verify.sh, true exit code captured, named test live_acceptance_v1_3_composed passing under opus's OWN execution) and authorized it. Executor SUMMARY.md files are informative context only, NEVER the authorizing evidence for this claim (per the documented false-PASS-through-a-pipe and executor-self-marks-complete incidents)."
    - "DOC-01 point 8: PROJECT.md states the confined worker links brokerd → lettre → native-tls (factual dependency-chain note)."
    - "The revised controlled-experiment framing is stated truthfully: two hostile docs with identical injection text and identical derivation structure, differing only in a per-run test-isolation recipient token; both blocked identically; operator confirms one, denies the other (COORD-A)."
    - "The nonce honesty line is present: the per-run UUID recipient domain exists purely for live-Mailpit test isolation, NOT because the two docs differ in any way the taint mechanism sees (COORD)."
    - "The 'one unbroken DAG' scope sentence is present: per-session verify_chain integrity across a shared audit log, NOT a cross-session linked chain (COORD-SUMMARY)."
    - "The self-consistency honesty clause is present (COORD MED-1): PROJECT.md does NOT claim the live composed run's to/body anchor pin is independently-sourced/independently-reconstructed ground truth — it is a self-consistency reconstruction from the same derivation record; independent ground truth lives only in Phase-15's DB-alone test; the anti-staple teeth hold independently of this nuance."
    - "The residual-risks headline pointer is present (COORD MED-3): the top v1.3 DOC-01 paragraph explicitly routes the reader to the three residual risks below (verify_chain forgeable head, guard-(c) runtime-vs-compile-time gap, replay exposure) via the literal 'residual-risks clause below' — so a reader who stops at the top paragraph is still directed to points 3/4/5."
  artifacts:
    - ".planning/PROJECT.md — 8 DOC-01 points as standalone legible sentences + revised framing + nonce honesty line + scope sentence, anchored in the existing v1.3 + Residual-risks sections (no new top-level section)"
  key_links:
    - "each of the 8 points → its already-committed Phase-16/17 source (16-02/16-04 SUMMARY, v2-obligations todo, s9_live_block doc comments) — transcription, not invention"
    - "point 7 → the green ACCEPT-01 live test (Plans 02/03) — the fact that makes the claim TRUE, hence late-sequenced"
---

<objective>
Consolidate all 8 coordinator-mandated framing points into `.planning/PROJECT.md`
so a skeptical reader sees exactly what v1.3 does and does NOT prove — honest
scope for CONTROL-01, I1, verify_chain, the guard-(c) runtime flag, the
replay/no-CAS residual risk, ProvideIntent enforcement, CONFIRM-01's newly-live
status, and the brokerd→lettre→native-tls dependency chain — plus the revised
controlled-experiment framing, the nonce honesty line, and the "one unbroken
DAG" scope clarification.

Purpose: DOC-01 is the milestone's framing-honesty requirement — "No external
claim may say otherwise." 17-RESEARCH.md confirms all 8 points already exist
verbatim/near-verbatim across committed Phase-16/17 artifacts; this is
transcription + correct anchoring, not investigation. Points 2 and 3 are the
ones "a demo is tempted to soften" — they must be stated in full, never as a
subordinate clause. Point 7 must be added ONLY after Phase 17's own live test
passes, so it never prematurely claims something unproven.

Output: an edited PROJECT.md carrying the 8 points + revised framing + honesty
lines, anchored in the existing v1.3 and Residual-risks sections.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-live-acceptance-framing-honesty/17-RESEARCH.md
</context>

<coordinator_gate>
Executed under caprun-opus-77's locked ruling. Points 2 and 3 are [HOLD THE
LINE] — do not soften. The revised (nonce, not byte-identical) fixture framing
is mandatory; do NOT write "byte-identical hostile documents" (or any "byte
identical" variant) anywhere. Task 2 (point 7) is GATED on caprun-opus-77's OWN
independent re-run of the live proof — NOT on the executor SUMMARY.md files
(COORD MED-4); the plan is `autonomous: false` so an autonomous executor pauses
at Task 2 for the coordinator's explicit authorization. At the Phase-17 DONE
gate, caprun-opus-77 ALSO personally reads the final DOC-01 paragraph and
confirms points 2 and 3 are standalone, unsoftened, legible sentences — in
ADDITION to the automated checks and the independent live-proof re-run (COORD
MED-5). This is the final plan of the final phase — after it completes,
diff-check ROADMAP.md and do NOT let the doc-completion commit flip the
PHASE-level checkbox (the recurring 2-for-2 GSD bug); the coordinator marks the
phase DONE after the independent re-run of the live proof.
</coordinator_gate>

<tasks>

<task type="auto">
  <name>Task 1: Anchor DOC-01 points 1, 2, 6, 8 + revised framing + honesty lines in the v1.3 section; points 3, 5, 4 in a new v1.3 residual-risks clause</name>
  <read_first>
    - .planning/PROJECT.md lines 19-59 ("Current Milestone: v1.3" — the existing LLM-planner non-reopening sentence at 43-46 is the anchor for points 1/2/6/8) and lines 237-242 (the v0-era "Residual risks" bullet — the anchor to EXTEND with a v1.3 clause for points 3/5/4)
    - .planning/phases/17-live-acceptance-framing-honesty/17-RESEARCH.md "DOC-01 — Exact Content and Anchor Point" (the exact source citations + recommended anchor structure)
    - .planning/phases/16-confirm-ux-literal-binding-negative-controls/16-02-SUMMARY.md and 16-04-SUMMARY.md (verbatim source language for points 3, 5, 6, 2) — read the specific lines RESEARCH cites (16-02:39/150/159 for verify_chain; 16-04:210-212 for replay; 16-04:17/141 for ProvideIntent)
    - .planning/todos/pending/2026-07-08-v1.3-phase16-v2-security-obligations.md (items 1, 3, 4 — I1 scope, verify_chain scope, guard-c runtime-flag)
    - cli/caprun/tests/s9_live_block.rs lines 199-201, 790-794 (the verbatim CONTROL-01 honest-framing language for point 1)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Edit (do NOT rewrite) PROJECT.md. Use scoped Edit calls; never a whole-file
    Write. Do NOT create a new top-level section (17-RESEARCH.md is explicit on
    this). Keep the existing headline DOC-01 sentence (lines 43-46) intact.

    In the "Current Milestone: v1.3" section, immediately after the existing
    "Explicitly not reopened…/laundering…" paragraph, add a new paragraph
    "**What v1.3's live proof does and does not claim (DOC-01):**" containing
    these as STANDALONE, legible sentences (each its own sentence, not a
    subordinate clause; points 2 and 3 must NOT be softened):
      - Point 1: CONTROL-01 proves a send built from TRUSTED intent is Allowed and
        delivers, and a send whose args are DOC-DERIVED is Blocked — it does NOT
        prove "same doc, taint flipped"; the benign doc is decorative on the clean
        path.
      - Point 2 [HOLD THE LINE]: I1's draft-only demotion triggers when the broker
        mints untrusted taint from a REPORTED read (mint_from_read) — NOT on fd
        release; a worker that reads the doc and reports nothing stays Active.
        (v2 obligation: demote at RequestFd.)
      - Point 6: ProvideIntent mints worker-declared intent as UserTrusted only
        BEFORE any fd read, exactly once — broker-ENFORCED, not assumed.
      - Point 8: the confined worker's send path links brokerd → lettre →
        native-tls (a factual dependency-chain note).
      - Residual-risks pointer (COORD MED-3): add the sentence "Three additional
        accepted residual risks (verify_chain's forgeable chain head, guard-(c)'s
        runtime-vs-compile-time gap, and the Allowed-path's replay exposure) are
        detailed in the v1.3 residual-risks clause below — do not stop reading at
        this paragraph." This routes a skeptic who reads the top v1.3 section and
        stops straight to points 3/4/5, which otherwise sit deep in the
        Residual-risks bullet. The literal phrase "residual-risks clause below"
        MUST appear in PROJECT.md (it is a grep-checked token).
      - Revised controlled-experiment framing (COORD-A): the hostile confirm and
        deny legs use two documents with IDENTICAL injection text and IDENTICAL
        derivation structure, differing ONLY in a per-run test-isolation recipient
        token; both are blocked identically as doc-derived tainted recipients; the
        operator confirms one and denies the other; confirm sends exactly once,
        deny sends nothing. The controlled variable is the OPERATOR'S DECISION.
      - Nonce honesty line: that per-run recipient token is a UUID in the domain
        fragment that exists PURELY so the live Mailpit assertions can isolate each
        leg on a shared listener — not because the two docs differ in any way the
        taint mechanism sees.
      - Scope sentence (COORD-SUMMARY): "one unbroken audit DAG" here means
        per-session verify_chain integrity across a SHARED audit.db log (three
        sessions in one file, each independently chain-verified, with genuine-taint
        descent re-proven for the hostile anchors) — NOT a single cross-session
        parent_id chain spanning confirm/deny/clean.
      - Self-consistency honesty clause (COORD MED-1): the live composed run's
        `to`/`body` anchor pin is a SELF-CONSISTENCY reconstruction (expected roots
        rebuilt from the same derivation record being checked), NOT an
        independently-sourced ground-truth pin. Independent ground-truth root
        pinning (via out-of-band mint-return values) lives only in Phase-15's
        still-green DB-alone test — the one source of truth for that property. The
        substantive anti-staple teeth (per-element real-file_read check,
        genuine_derivation_binds, both anti-staple controls) hold independently of
        this nuance. Do NOT describe the live anchor pin as "independently
        reconstructed" anywhere in PROJECT.md.

    Then EXTEND the existing "Residual risks (acknowledged, not solved in v0)"
    bullet (lines 237-242): keep the v0 items unchanged, and append a new
    "v1.3 residual risks (Phase 16/17, DOC-01):" clause with:
      - Point 3 [HOLD THE LINE]: verify_chain detects single-store and
        non-recomputing multi-store tampering, but the chain head is NOT
        externally anchored — an actor with `events` write access can forge it
        end-to-end. Accepted residual risk; v2 keyed-MAC / head-pin.
      - Point 5: the Allowed email.send path has NO CAS — a replayed
        SubmitPlanNode sends N emails; the durable per-attempt ledger makes each
        send auditable but does not prevent duplication. Accepted residual risk.
      - Point 4: guard-(c) (CAPRUN_ENABLE_IPC_CREATE_SESSION) is a runtime
        default-deny flag, not a compile-time exclusion — the forced-Active mint
        code ships in the production binary. v2: build-exclude it.

    Do NOT add point 7 in this task (it is the LATE task, Task 2). Do NOT touch
    REQUIREMENTS.md's DOC-01 checkbox (verification flow owns status flips).
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/AgentOS && for tok in "decorative" "mint_from_read" "externally anchored" "CAPRUN_ENABLE_IPC_CREATE_SESSION" "replayed" "ProvideIntent" "native-tls" "test-isolation" "per-session" "residual-risks clause below"; do grep -qi "$tok" .planning/PROJECT.md || { echo "MISSING: $tok"; exit 1; }; done; echo ALL-PRESENT</automated>
  </verify>
  <done>PROJECT.md carries DOC-01 points 1, 2, 3, 4, 5, 6, 8 as standalone sentences plus the revised controlled-experiment framing, the nonce honesty line, and the one-unbroken-DAG scope sentence; the automated token check prints ALL-PRESENT; no "byte identical" framing appears in any form (`grep -niE 'byte[- ]?identical' .planning/PROJECT.md` prints nothing / exits non-zero — zero matches is the pass condition); no new top-level section was created.</done>
</task>

<task type="auto">
  <name>Task 2 (LATE, GATED): Add DOC-01 point 7 — CONFIRM-01 proven live for the first time — only after caprun-opus-77's OWN independent live-proof re-run is green</name>
  <read_first>
    - .planning/PROJECT.md (the DOC-01 paragraph added in Task 1 — the anchor for point 7)
    - .planning/phases/17-live-acceptance-framing-honesty/17-02-SUMMARY.md and 17-03-SUMMARY.md (INFORMATIVE CONTEXT ONLY — these describe what the plans intended. They are NOT the authorizing evidence for point 7. A self-reported executor green cannot back a "proven for the first time" claim; per this project's documented false-PASS-through-a-pipe and 2-for-2 executor-self-marks-complete incidents, the authorizing evidence is caprun-opus-77's OWN independent re-run — see the coordinator_gate below.)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    GATED TASK — do NOT run or commit this task until caprun-opus-77 has
    INDEPENDENTLY re-run the live proof (`scripts/mailpit-verify.sh` with
    `MAILPIT_VERIFY_CMD='cargo test -p caprun --test live_acceptance_v1_3 live_acceptance_v1_3_composed'`,
    the TRUE exit code captured BEFORE any pipe, and the named test
    `live_acceptance_v1_3_composed` passing under opus's OWN execution) and has
    explicitly authorized this task. Treat the Plan-02/03 SUMMARY.md files as
    informative context only, never as the authorizing evidence. Reading a
    self-reported green SUMMARY is NOT sufficient to run this task.

    ONLY after that independent authorization, add DOC-01 point 7 as a standalone
    sentence in the same DOC-01 paragraph: that CONFIRM-01's verbatim
    recipient+body narration is proven END-TO-END in Phase 17's live composed run
    for the FIRST time — at Phase 16 it was exercised against a synthetic fixture,
    and Phase 17's live doc→block→confirm/deny run is what makes the end-to-end
    claim true. Sequenced LAST (COORD point 7) so PROJECT.md never claims this
    before the live proof independently exists.
  </action>
  <coordinator_gate>
    BLOCKING — this task is gated on caprun-opus-77's OWN re-run, NOT on executor
    SUMMARY.md files. It must not be executed or committed until caprun-opus-77
    has independently re-run `scripts/mailpit-verify.sh` (true exit code captured,
    `live_acceptance_v1_3_composed` passing under opus's own execution) and
    confirmed it green. This is why the plan is `autonomous: false` — an
    autonomous executor MUST pause here for the coordinator's explicit
    authorization. PLAN NOTE FOR ORCHESTRATOR: this is a deliberate non-autonomous
    checkpoint; do not auto-advance past Task 2.
  </coordinator_gate>
  <verify>
    <automated>cd /Users/benlamm/Workspace/AgentOS && grep -qi "first time" .planning/PROJECT.md && grep -qi "synthetic fixture" .planning/PROJECT.md && echo POINT-7-PRESENT</automated>
  </verify>
  <done>PROJECT.md's DOC-01 paragraph states CONFIRM-01's end-to-end narration is proven live for the first time in Phase 17 (synthetic at Phase 16); the sentence was added ONLY after caprun-opus-77 independently re-ran the live proof (true exit code captured, live_acceptance_v1_3_composed green under opus's own execution) and authorized it — NOT on the strength of the Plan-02/03 SUMMARY.md files, which are informative context only. (The grep is a presence check; it is necessary but NOT sufficient — the authorizing evidence is the coordinator's independent re-run.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROJECT.md claims → external readers / future maintainers | the document is the milestone's public honesty surface; an overclaim here is the threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-08 | Repudiation | Overclaiming — softening points 2/3 into subordinate clauses, or claiming point 7 before the live proof exists | high | mitigate | Points stated as standalone sentences; point 7 late-sequenced behind the green ACCEPT-01 gate; token grep + no-"byte-identical hostile" gate |
| T-17-09 | Tampering | Whole-file Write to PROJECT.md destroying content outside the edit window | medium | mitigate | Scoped Edit calls only; never Write on the existing PROJECT.md |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | Documentation-only plan; no dependencies |
</threat_model>

<verification>
- Task 1 automated token check prints ALL-PRESENT; `grep -niE 'byte[- ]?identical' .planning/PROJECT.md` prints nothing and exits non-zero (zero matches = pass — catches "byte-identical", "byte identical", "byte-identical documents", etc.).
- Task 2 automated check prints POINT-7-PRESENT (run only after caprun-opus-77's OWN independent live-proof re-run is green — NOT after reading the Plan-02/03 SUMMARYs; the grep is a presence check, necessary but not sufficient, per COORD MED-4).
- Manual/adversarial read of the edited PROJECT.md confirms points 2 and 3 read as standalone, unsoftened sentences (DOC-01 is fundamentally a legibility requirement — the automated greps are presence scaffolding, not a substitute for the read).
- Coordinator-owned legibility gate (COORD MED-5): at the Phase-17 DONE gate, caprun-opus-77 PERSONALLY reads the final DOC-01 paragraph in PROJECT.md and confirms points 2 and 3 are standalone, unsoftened, legible sentences — in ADDITION to (not instead of) the automated grep/token checks and the independent live-proof re-run. This prose read is a phase-closing gate owned by the coordinator, not solely automatable.
</verification>

<success_criteria>
PROJECT.md honestly scopes v1.3: all 8 DOC-01 points present as legible
standalone sentences, the revised nonce/controlled-experiment framing stated
truthfully, point 7 added only after the live proof, no overclaim, no new
top-level section, no destructive rewrite.
</success_criteria>

<output>
Create `.planning/phases/17-live-acceptance-framing-honesty/17-04-SUMMARY.md` when done.
</output>
