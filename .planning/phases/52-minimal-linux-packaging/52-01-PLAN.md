---
phase: 52-minimal-linux-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-linux.sh
  - docs/GETTING-STARTED.md
autonomous: true
requirements:
  - PKG-01

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `bash scripts/install-linux.sh` on Linux from the workspace root places exactly `caprun`, `caprun-worker`, and `caprun-exec-launcher` in ONE destination directory, all executable (PKG-01 criterion 1; D-02, D-05, D-07, D-09)."
    - "The destination defaults to `${HOME}/.local/bin` and is overridable by an explicit operator-supplied destination, with no privilege escalation on any path (D-04)."
    - "A non-Linux host, a missing build toolchain, a failed release build, a missing expected binary, or an unusable destination stops the install with a message that names the specific missing or broken thing (D-03)."
    - "An interrupted or failed install leaves the destination unmodified: all three binaries are staged and proven executable in a staging directory before any is moved into the destination, and the staging directory is removed on every exit path (D-06)."
    - "`docs/GETTING-STARTED.md` documents BOTH the script path and the equivalent manual release-build/copy commands, so a design partner who declines to run the repository script still has a sufficient install path (D-01)."
    - "`docs/GETTING-STARTED.md` states that `cargo install --path cli/caprun` alone is not sufficient and names `caprun-exec-launcher` as the binary that command can never produce because it is a separate Cargo package (PKG-01 criterion 1; D-08)."
    - "`docs/GETTING-STARTED.md` no longer claims the workspace build produces only the orchestrator and the worker; it names the three required binaries plus the optional planner sidecar (D-07)."
    - "The install path is a source-tree build and copy only: no remote artifact fetch, no privileged package manager, no repository mutation (D-02, D-10)."
    - statement: "Concurrent installs into the same destination directory are not serialized, and a process kill landing between the three back-to-back renames can still leave a set mixed across two builds; the documented recovery is re-running the installer, which is idempotent (D-06 residual)."
      verification: backstop
  artifacts:
    - path: scripts/install-linux.sh
      provides: "Thin Linux source-build installer: build release targets, stage, install three siblings into one destination, verify layout"
      contains: "caprun-exec-launcher"
      min_lines: 90
    - path: docs/GETTING-STARTED.md
      provides: "Install (Linux) walkthrough — script path plus manual equivalent, three-binary layout, exec-launcher failure mode"
      contains: "install-linux.sh"
  key_links:
    - from: "scripts/install-linux.sh"
      to: "target/release/{caprun,caprun-worker,caprun-exec-launcher}"
      via: "cargo build --workspace --release"
      pattern: "cargo build --workspace --release"
    - from: "scripts/install-linux.sh"
      to: "cli/caprun/src/main.rs:607-611 single-hop current_exe().parent().join(\"caprun-worker\")"
      via: "all three binaries land in ONE flat destination directory, so the zero-tolerance worker lookup resolves at depth 0"
      pattern: "caprun-worker"
    - from: "scripts/install-linux.sh"
      to: "crates/brokerd/src/sinks/process_exec.rs:736-756 resolve_launcher_path()"
      via: "caprun-exec-launcher installed as a direct sibling, satisfying the bounded ancestor walk at depth 0"
      pattern: "caprun-exec-launcher"
    - from: "docs/GETTING-STARTED.md"
      to: "scripts/install-linux.sh"
      via: "documented install command in the Install (Linux) section"
      pattern: "install-linux\\.sh"
  assumptions:
    - "FLAGGED (edge-probe PKG-01/concurrency, status `unresolved`, reconciled against D-06 — recorded, not dismissed): the installer's interruption/parallelism guarantee is BOUNDED, not total. Stage-then-rename guarantees no mixed set from a partial BUILD or partial COPY (nothing enters the destination until all three are staged and executable), but three POSIX renames are atomic individually and NOT as a set, and nothing serializes two concurrent installs into one destination. Per RESEARCH.md 'Don't Hand-Roll', a custom lockfile protocol is explicitly out of scope. Resolution: adopt stage-then-rename, DOCUMENT the residual in the script header and the doc, and name re-running the installer as the recovery. See the backstop truth above."
    - "A1 (RESEARCH): design partners have `cargo`/`rustup` available or install it themselves; the script fails fast naming the toolchain rather than bootstrapping it."
    - "A2 (RESEARCH): `${HOME}/.local/bin` is writable or safely creatable; if not, the unusable-destination fail-fast path (D-03) covers it."
    - "A3 (RESEARCH): no shell linter is installed on this host; `bash -n` is the available local syntax gate."
  prohibitions:
    - "MUST NOT escalate privileges on any path — no privilege-escalation command, no re-exec as a different user, no privileged package-manager invocation, not even to create or write the destination (D-02, D-04)."
    - "MUST NOT download or execute opaque remote artifacts; the install path builds only from the checked-out source tree (D-02)."
    - "MUST NOT claim or imply that the install-time layout check constitutes the Linux security proof (Landlock/seccomp/e2e confinement) or substitutes for scripts/compose-verify.sh or the retained LIVE evidence (D-09)."
    - "MUST NOT mutate the repository or its git state during install, and MUST NOT delete or overwrite anything in the destination other than the three named binary paths (D-02)."
    - "MUST NOT reimplement, second-guess, or add an alternative to the shipped current_exe() sibling-resolution algorithm (no PATH search, no symlink farm, no lookup-override env var) (D-10)."
---

<objective>
Deliver the working minimal Linux install path end-to-end: a thin `scripts/install-linux.sh` that builds the release targets, stages them, installs exactly the three required sibling binaries into one destination, and verifies the layout — plus the `docs/GETTING-STARTED.md` walkthrough that names it and documents the equivalent manual commands.

Purpose: PKG-01's whole value is that a design partner ends up with `caprun`, `caprun-worker`, and `caprun-exec-launcher` in ONE directory. `caprun-worker` resolution is a SINGLE `.parent()` hop with no fallback search (`cli/caprun/src/main.rs:607-611`), so co-location is a functional deployment invariant, not a documentation preference.

Output: `scripts/install-linux.sh`; a new `## Install (Linux)` section and corrected binary/CLI/troubleshooting content in `docs/GETTING-STARTED.md`.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-minimal-linux-packaging/52-CONTEXT.md
@.planning/phases/52-minimal-linux-packaging/52-RESEARCH.md
@.planning/phases/52-minimal-linux-packaging/52-PATTERNS.md
@.planning/phases/52-minimal-linux-packaging/52-VALIDATION.md
@CLAUDE.md
@docs/GETTING-STARTED.md
</context>

<artifacts_this_phase_produces>
Phase 52 overall produces these NEW symbols (nothing here pre-exists; drift verification must not flag them as unacknowledged):

1. **`scripts/install-linux.sh`** — new file, the phase's only executable deliverable.
2. **Install-script CLI surface** — `--dest <dir>` flag and `--help`; positional-free otherwise.
3. **Install-script env override** — `INSTALL_DEST` (script-level only; NOT a runtime `caprun` variable).
4. **New doc section headings** — `## Install (Linux)` in `docs/GETTING-STARTED.md`; `## Operator Configuration Checklist` with `### Tier 1 — Always needed`, `### Tier 2 — Sink-specific credentials and settings`, `### Tier 3 — Internal and test-only (do not set)` in `docs/CONFIGURATION.md` (Plan 02).
5. **Newly-documented-as-contract env vars** — the `CAPRUN_*` names already read by shipped code but never previously documented (Plan 02).

This plan (52-01) owns items 1, 2, 3 and the `docs/GETTING-STARTED.md` half of item 4.

**Explicitly NOT produced:** no new crate, no new Cargo dependency, no change to any file under `crates/` or `cli/`, no `.deb`/`.snap`/cargo-dist/Homebrew artifact, no macOS install path.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: sudo -->
<!-- planner-discipline-allow: curl -->
<!-- planner-discipline-allow: wget -->
<!-- planner-discipline-allow: apt-get -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end install — build, stage, co-locate three siblings, verify, and document the one command</name>
  <precondition>`cargo --version` succeeds on this host and `cargo build --workspace` completes at HEAD (the task's verify runs a real `--release` build; the workspace has no pinned toolchain).</precondition>
  <reversibility rating="costly">The script path, its `--dest` flag spelling, and the `INSTALL_DEST` name become the documented surface design partners script against; renaming them later means a coordinated doc + partner-instruction change, though no data migration and no broken wire contract.</reversibility>
  <files>scripts/install-linux.sh, docs/GETTING-STARTED.md</files>
  <read_first>
    - scripts/compose-verify.sh lines 1-120 — house header doc-comment shape (one-line purpose + phase/req tag, prose, Usage line, "Run from the workspace root" note, "Env overrides (rarely needed):" comment table), `set -euo pipefail`, `${VAR:-default}` idiom, `trap cleanup EXIT`
    - scripts/docker-cache.sh lines 1-65 and 105-157 — house `case` argument dispatch plus the usage-fallback error arm
    - scripts/check-invariants.sh lines 1-21 and 350-363 — house PASS/FAIL summary echo convention and non-zero exit on failure
    - .planning/phases/52-minimal-linux-packaging/52-PATTERNS.md — "Shell script house style" and the post-install check shape
    - .planning/phases/52-minimal-linux-packaging/52-RESEARCH.md — "Architecture Patterns / Pattern 1" (both resolution functions), "Don't Hand-Roll" (stage-then-rename; do NOT build a lockfile protocol), "Anti-Patterns to Avoid"
    - cli/caprun/src/main.rs lines 600-615 — the single-hop `current_exe().parent().join("caprun-worker")` with no fallback search
    - crates/brokerd/src/sinks/process_exec.rs lines 695-760 — `resolve_launcher_path()` bounded ancestor walk and its actionable error string
    - Cargo.toml lines 1-4 — workspace members confirming `cli/caprun-exec-launcher` is a separate package
    - docs/GETTING-STARTED.md lines 32-53 — the "Clone and build" section this task inserts after
  </read_first>
  <action>
    Create `scripts/install-linux.sh` as an executable (`chmod +x`) bash script in the established house style, and add the `## Install (Linux)` section to `docs/GETTING-STARTED.md` that names it.

    **Script header doc-comment** (mirror `scripts/compose-verify.sh` lines 1-40): first line `#!/usr/bin/env bash`; then `# install-linux.sh — minimal Linux source-build installer for caprun (Phase 52, PKG-01)`; prose explaining that it builds the release targets and co-locates the three required sibling binaries in one directory because `caprun`'s worker lookup is a single `current_exe().parent()` hop with no fallback; a numbered step list; a `# Usage:` block showing `bash scripts/install-linux.sh [--dest <dir>]` and `# Run from the workspace root (same directory as Cargo.toml).`; an `# Env overrides:` comment table naming `INSTALL_DEST` with default `${HOME}/.local/bin`. The header MUST also state the bounded interruption guarantee in plain terms: nothing enters the destination until all three binaries are staged and executable; the three final renames are atomic individually but not as a set; two installs run at the same time into one destination are not serialized; the recovery for any mixed set is to re-run the installer.

    **Script body, in this order:**
    1. `set -euo pipefail` as the first executable line.
    2. Resolve the repo root from `$(dirname "${BASH_SOURCE[0]}")/..` and require `Cargo.toml` there; otherwise fail naming the workspace-root requirement.
    3. Argument dispatch via `case`: `--dest <dir>` sets the destination; `--help`/`-h` prints the usage line and exits 0; any other argument prints the usage line to stderr and exits 1.
    4. Destination resolution with flag precedence over env: `DEST="${DEST_FLAG:-${INSTALL_DEST:-$HOME/.local/bin}}"`. Quote every path expansion; never `eval` and never `source` the operator-supplied value.
    5. `cargo build --workspace --release` from the repo root. `set -e` propagates a build failure; add an explicit trailing FAIL message naming the build step.
    6. Assert each of `target/release/caprun`, `target/release/caprun-worker`, `target/release/caprun-exec-launcher` exists; on absence emit `FAIL — <path> missing after the release build` to stderr and exit 1.
    7. Staging: `mkdir -p "$DEST"`; create the staging directory INSIDE the destination so the later moves are same-filesystem renames — `STAGE_DIR="$(mktemp -d "${DEST}/.caprun-install.XXXXXX")"`; register `trap 'rm -rf "$STAGE_DIR"' EXIT` immediately after creating it.
    8. Copy all three release binaries into `$STAGE_DIR` with mode 755 (use `install -m 755`), then assert `test -x` on each staged path before touching the destination.
    9. Move the three staged files into `$DEST` back to back, in the fixed order `caprun-worker`, `caprun-exec-launcher`, `caprun` — the orchestrator last, so a kill mid-sequence leaves an already-present orchestrator paired with fresh helpers rather than the reverse.
    10. Post-install check (D-09, non-destructive): loop the three names asserting `test -x "$DEST/$name"`; on failure emit `FAIL — ${DEST}/<name> missing or not executable` and exit 1. Do NOT execute any installed binary, do NOT read any credential, do NOT open a Session.
    11. Report what was installed (D-02): echo each installed absolute path on its own line, then a single `PASS — ...` summary line naming the destination.
    12. PATH hint (D-05): `case ":$PATH:" in *":$DEST:"*) ;; *) echo "...";; esac` printing a one-line hint naming `$DEST`.

    `caprun-planner` is built by the workspace release build but is NOT copied — the required installed set is exactly the three names (D-07).

    **`docs/GETTING-STARTED.md`** — insert a new `## Install (Linux)` H2 immediately after the existing "Clone and build" section (before "Running the substrate demo (Linux)"), separated by the file's `---` horizontal-rule convention. It must contain: the one-line install command `bash scripts/install-linux.sh`; a statement that the default destination is `${HOME}/.local/bin` and that `--dest <dir>` overrides it; a sentence that the installer needs no elevated privileges and performs no download beyond what the build itself fetches; the list of the three installed names with a one-line role each; a note that `caprun-planner` is also built but is optional LLM-sidecar functionality that this path does not install; and the bounded-interruption statement plus "re-run the installer" as both the upgrade path and the recovery.

    Do NOT touch the "Running from macOS (Colima)" section — it is independently flagged stale and is out of this phase's scope; new text must simply not contradict it (the install path requires no container).
  </action>
  <verify>
    <automated>bash -n scripts/install-linux.sh &amp;&amp; test -x scripts/install-linux.sh &amp;&amp; D="$(mktemp -d)" &amp;&amp; INSTALL_DEST="$D" bash scripts/install-linux.sh &amp;&amp; test "$(ls -A1 "$D" | sort | tr '\n' ' ')" = "caprun caprun-exec-launcher caprun-worker " &amp;&amp; test -x "$D/caprun" &amp;&amp; test -x "$D/caprun-worker" &amp;&amp; test -x "$D/caprun-exec-launcher" &amp;&amp; grep -q 'install-linux.sh' docs/GETTING-STARTED.md &amp;&amp; grep -q '## Install (Linux)' docs/GETTING-STARTED.md &amp;&amp; echo TRACER-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/install-linux.sh` exists, is executable, and `bash -n scripts/install-linux.sh` exits 0
    - First line of the script is `#!/usr/bin/env bash`; `grep -q '^set -euo pipefail' scripts/install-linux.sh`
    - `grep -q 'cargo build --workspace --release' scripts/install-linux.sh`
    - `grep -c 'caprun-exec-launcher' scripts/install-linux.sh` is at least 2 (staged and verified)
    - `grep -qE 'trap .*EXIT' scripts/install-linux.sh` and `grep -q 'mktemp -d' scripts/install-linux.sh`
    - `grep -q 'INSTALL_DEST' scripts/install-linux.sh` and `grep -q '.local/bin' scripts/install-linux.sh`
    - `test "$(grep -v '^[[:space:]]*#' scripts/install-linux.sh | grep -c 'caprun-planner' || true)" -eq 0` — the optional sidecar is never copied by the script body
    - Behavior: `D="$(mktemp -d)"; INSTALL_DEST="$D" bash scripts/install-linux.sh` exits 0 and `ls -A1 "$D"` lists exactly `caprun`, `caprun-exec-launcher`, `caprun-worker` — no leftover staging directory
    - Behavior: all three installed paths pass `test -x`
    - Behavior: the run prints a line containing `PASS` naming the destination
    - `grep -q '## Install (Linux)' docs/GETTING-STARTED.md` and `grep -q 'install-linux.sh' docs/GETTING-STARTED.md`
    - `grep -q 'caprun-planner' docs/GETTING-STARTED.md` — the optional sidecar is named as not-installed
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0 — no TCB, manifest, or lockfile change
  </acceptance_criteria>
  <done>A single `bash scripts/install-linux.sh` invocation produces a destination directory holding exactly the three required executables, and `docs/GETTING-STARTED.md` names that command in a dedicated Install section.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-fast guards, explicit destination override, and the PATH/kernel advisories</name>
  <files>scripts/install-linux.sh</files>
  <read_first>
    - scripts/install-linux.sh — the file as written by Task 1 (current state, not assumptions)
    - scripts/compose-verify.sh lines 120-150 — the `echo "FAIL — <what> <why>" >&amp;2; exit 1` house error shape
    - .planning/phases/52-minimal-linux-packaging/52-RESEARCH.md — "Don't Hand-Roll" rows for the PATH substring test and the kernel advisory; "Environment Availability" for the kernel/toolchain facts
    - docs/GETTING-STARTED.md lines 20-29 and 156-169 — the platform table and "Common setup issues" kernel wording this advisory must stay consistent with
  </read_first>
  <action>
    Harden `scripts/install-linux.sh` with the D-03 fail-fast preconditions and the D-04/D-05 destination and PATH behavior. Every new guard uses the house `echo "FAIL — ..." >&2; exit 1` shape and names the specific missing or broken thing.

    Add, before the build step:
    1. **Operating-system guard:** if `uname -s` is not `Linux`, fail naming the detected system and stating that this install path is Linux-only. Reference the platform table in `docs/GETTING-STARTED.md` rather than restating it.
    2. **Toolchain guard:** if `command -v cargo` finds nothing, fail naming `cargo` and pointing at `rustup` as the way to obtain it. The script MUST NOT attempt to obtain a toolchain itself.
    3. **Destination usability guard:** after resolving `$DEST`, attempt `mkdir -p "$DEST"`; if that fails, or if the resulting directory is not writable (`test -w`), fail naming the exact path and stating that the operator must choose a writable destination or create it themselves. A destination that requires elevated privileges is an operator-chosen override, never something this script obtains.

    Add, after the post-install check:
    4. **Kernel advisory (warn only, never blocks):** read `uname -r`; if the major.minor is below 5.13, print a warning line naming the detected release and stating that Landlock needs Linux 5.13 or newer for the full confinement guarantee. Compare with a plain shell numeric split of the first two dot-separated fields — do not implement general semver parsing. This is advisory because `crates/sandbox` already negotiates the Landlock ABI down at runtime; the install itself still succeeds and still exits 0.

    Also confirm (adding if Task 1 left any gap): the `--dest <dir>` flag takes precedence over `INSTALL_DEST`, which takes precedence over the `${HOME}/.local/bin` default; a `--dest` with no following value fails with the usage line; every `$DEST`, `$STAGE_DIR`, and `target/release/...` expansion in the file is double-quoted.

    **The destination usability guard must fire before anything in the destination is touched.** This is the D-06 guarantee at the re-install boundary: a re-run that cannot proceed must leave the previously installed set byte-identical, never partially replaced. The verify below proves this by fault injection — it installs once, records a checksum of all three binaries, makes the destination non-writable, confirms the re-run exits non-zero, then confirms the checksum is unchanged. Order the guard accordingly: the writability probe precedes the build, the staging directory creation, and every copy.

    The script must remain free of any privilege-escalation command, any system package-manager invocation, any network-fetch utility, and any repository-mutating version-control subcommand — the verify gate below asserts all of these mechanically against the non-comment body.
  </action>
  <verify>
    <automated>bash -n scripts/install-linux.sh &amp;&amp; test "$(grep -v '^[[:space:]]*#' scripts/install-linux.sh | grep -cE '(^|[^A-Za-z0-9_])(sudo|doas|apt-get|yum|dnf|pacman|brew|curl|wget)([^A-Za-z0-9_]|$)' || true)" -eq 0 &amp;&amp; test "$(grep -v '^[[:space:]]*#' scripts/install-linux.sh | grep -cE '(^|[^A-Za-z0-9_])git[[:space:]]+(add|commit|checkout|reset|clean|push)' || true)" -eq 0 &amp;&amp; D2="$(mktemp -d)" &amp;&amp; bash scripts/install-linux.sh --dest "$D2" &amp;&amp; test -x "$D2/caprun-exec-launcher" &amp;&amp; D3="$(mktemp -d)" &amp;&amp; chmod 500 "$D3" &amp;&amp; ! bash scripts/install-linux.sh --dest "$D3/nested" &amp;&amp; chmod 700 "$D3" &amp;&amp; D4="$(mktemp -d)" &amp;&amp; INSTALL_DEST="$D4" bash scripts/install-linux.sh &gt;/dev/null &amp;&amp; B4="$(cd "$D4" &amp;&amp; sha256sum caprun caprun-worker caprun-exec-launcher)" &amp;&amp; chmod 555 "$D4" &amp;&amp; ! INSTALL_DEST="$D4" bash scripts/install-linux.sh &gt;/dev/null 2&gt;&amp;1 &amp;&amp; chmod 755 "$D4" &amp;&amp; test "$(cd "$D4" &amp;&amp; sha256sum caprun caprun-worker caprun-exec-launcher)" = "$B4" &amp;&amp; echo GUARDS-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install-linux.sh` exits 0
    - Source assertion: `grep -q 'uname -s' scripts/install-linux.sh` and `grep -q 'uname -r' scripts/install-linux.sh`
    - Source assertion: `grep -q 'command -v cargo' scripts/install-linux.sh`
    - Source assertion: `grep -qE 'test -w|\[ -w ' scripts/install-linux.sh`
    - Source assertion: `grep -q '5.13' scripts/install-linux.sh`
    - Source assertion: `grep -q -- '--dest' scripts/install-linux.sh`
    - Source assertion (region-scoped to the non-comment body): the escalation/package-manager/download token count is 0 and the repository-mutating version-control subcommand count is 0, exactly as the verify command asserts
    - Behavior: `bash scripts/install-linux.sh --dest "$(mktemp -d)"` exits 0 and installs all three executables into that directory
    - Behavior: a destination whose parent is not writable causes a non-zero exit with a stderr line containing `FAIL` and the exact offending path
    - Behavior (D-06 fault injection): after a successful install, making the destination non-writable and re-running produces a non-zero exit AND leaves all three installed binaries with an unchanged `sha256sum` — a refused re-install never partially replaces the previous set
    - Behavior: `bash scripts/install-linux.sh --bogus-flag` exits non-zero and prints the usage line
    - Behavior: `bash scripts/install-linux.sh --help` exits 0 and prints the usage line
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0
  </acceptance_criteria>
  <done>The installer refuses to proceed with a named, actionable message on a non-Linux host, a missing toolchain, or an unusable destination; installs to an explicitly supplied destination without privilege escalation; and warns without blocking on an older kernel.</done>
</task>

<task type="auto">
  <name>Task 3: Manual-equivalent commands, the cargo-install insufficiency warning, and corrected CLI/troubleshooting content</name>
  <files>docs/GETTING-STARTED.md</files>
  <read_first>
    - docs/GETTING-STARTED.md — the file as it stands after Task 1, in full (176 lines); specifically the "Clone and build" binary list at lines 40-43, the usage fence at lines 59-61, the sibling note at line 70, and the "Common setup issues" entries at lines 156-169
    - .planning/phases/52-minimal-linux-packaging/52-RESEARCH.md — "Common Pitfalls" Pitfall 1 (the exact `cargo install` insufficiency and its root cause), Pitfall 4 (do not re-litigate the container section), and "Code Examples / CLI invocation shape"
    - .planning/phases/52-minimal-linux-packaging/52-PATTERNS.md — the "Common setup issues" bold-question / one-paragraph-answer convention and the bare-usage-line fence convention
    - cli/caprun/src/main.rs lines 9-30 — the authoritative usage line and the intent-kind list
    - cli/caprun/src/main.rs lines 220-280 — verb handling (`run`) and the `--policy` / `--seed-from-file` flag loop
    - crates/brokerd/src/sinks/process_exec.rs lines 736-756 — the exact runtime failure an incomplete install produces
    - Cargo.toml line 2 — workspace members, evidence that `cli/caprun-exec-launcher` is its own package
  </read_first>
  <action>
    Complete the `docs/GETTING-STARTED.md` half of PKG-01 with the manual path (D-01), the insufficiency warning (D-08), and the corrected binary/CLI/troubleshooting content.

    1. **Manual equivalent inside `## Install (Linux)`** — under a bolded lead-in in this file's existing style, give the fenced bash commands a design partner can run instead of the script: `cargo build --workspace --release`, `mkdir -p "$HOME/.local/bin"`, and a single `install -m 755` (or `cp` plus `chmod +x`) copying exactly `target/release/caprun`, `target/release/caprun-worker`, and `target/release/caprun-exec-launcher` into that one directory. Follow it with the manual layout check — a short loop or three `test -x` lines over the same three paths — so the manual path carries the same D-09 verification the script performs. State plainly that these commands are the script's entire behavior, so the documentation is sufficient without running the repository script.

    2. **Insufficiency warning (D-08)** — a clearly marked warning inside `## Install (Linux)` stating that `cargo install --path cli/caprun` alone is NOT a sufficient install, giving the root cause: `caprun-exec-launcher` lives in a separate Cargo package (`cli/caprun-exec-launcher`), not as a second binary target of the `cli/caprun` package, so no number of `--bin` flags on that command can ever produce it. Name the observable consequence: everything appears to work until a plan node reaches the `process.exec` sink, which then fails to locate the launcher.

    3. **Correct the "Clone and build" binary claim** — rewrite the sentence and bullet list at `docs/GETTING-STARTED.md:40-43` so it names all three required binaries (`caprun` the orchestrator, `caprun-worker` the self-confining worker, `caprun-exec-launcher` the self-confining exec helper the broker spawns for `process.exec`) plus `caprun-planner` as an optional LLM sidecar that the minimal path does not require. Keep the existing `target/debug/` framing for the contributor build; the release layout belongs in `## Install (Linux)`.

    4. **Correct the CLI usage fence** — replace the bare-positional usage line at `docs/GETTING-STARTED.md:59-61` with the verified shape from `cli/caprun/src/main.rs:9`: `caprun [run] [--policy <path>] <intent-kind> <intent-param> <workspace-file> [audit-db-path]`. Keep the same plain fenced-block convention (no shell prompt). Update the adjacent runnable example so it passes an intent kind and intent parameter rather than a bare workspace path, and add a one-line pointer to `CONFIGURATION.md` for the full verb and flag reference. Do not enumerate the `confirm`/`deny`/`review`/`grant`/`audit` verbs here — that surface belongs to `docs/CONFIGURATION.md` (Plan 02).

    5. **Extend the sibling note at line 70** — state that all three required binaries must live in the same directory, and that `caprun`'s worker lookup is a single parent-directory hop with no fallback search, so a partial copy fails at spawn time with a bare operating-system "no such file" error and no descriptive message.

    6. **New "Common setup issues" entry** — following the existing bold-question / one-paragraph-answer convention, add an entry for the exec-launcher failure mode: the broker could not locate the sibling launcher near its own binary path; the fix is to install all three together via `scripts/install-linux.sh` or the manual commands above; and this is exactly the state `cargo install --path cli/caprun` leaves behind.

    Leave the "Running from macOS (Colima)" section untouched. Leave the "Running tests" and "Next steps" sections untouched except that "Next steps" may gain a pointer to the new Install section.
  </action>
  <verify>
    <automated>test "$(grep -c 'caprun-exec-launcher' docs/GETTING-STARTED.md)" -ge 4 &amp;&amp; grep -q 'cargo install --path cli/caprun' docs/GETTING-STARTED.md &amp;&amp; grep -q 'cargo build --workspace --release' docs/GETTING-STARTED.md &amp;&amp; grep -q 'caprun \[run\] \[--policy <path>\] <intent-kind> <intent-param> <workspace-file> \[audit-db-path\]' docs/GETTING-STARTED.md &amp;&amp; test "$(grep -c 'two binaries' docs/GETTING-STARTED.md || true)" -eq 0 &amp;&amp; test "$(grep -c '^caprun <workspace-file> \[audit-db-path\]$' docs/GETTING-STARTED.md || true)" -eq 0 &amp;&amp; grep -q 'colima start' docs/GETTING-STARTED.md &amp;&amp; echo DOCS-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'caprun-exec-launcher' docs/GETTING-STARTED.md` is at least 4 (build list, install command set, warning, troubleshooting entry)
    - `grep -q 'cargo install --path cli/caprun' docs/GETTING-STARTED.md` — the insufficient command is named verbatim so a reader searching for it finds the warning
    - `grep -q 'cli/caprun-exec-launcher' docs/GETTING-STARTED.md` — the separate-package root cause is stated
    - `grep -q 'process.exec' docs/GETTING-STARTED.md` — the observable failure surface is named
    - `grep -q 'cargo build --workspace --release' docs/GETTING-STARTED.md` and `grep -qE 'install -m 755|chmod \+x' docs/GETTING-STARTED.md` — the manual equivalent is runnable
    - The corrected usage line from `cli/caprun/src/main.rs:9` appears verbatim in the file
    - The superseded binary-count sentence and the superseded bare-positional usage line are both absent, exactly as the verify command asserts
    - `grep -q 'colima start' docs/GETTING-STARTED.md` — the container section was not removed or rewritten by this phase
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0
  </acceptance_criteria>
  <done>A design partner reading `docs/GETTING-STARTED.md` can install either by running the script or by copying the documented commands, knows why `cargo install --path cli/caprun` is not enough, sees the real CLI shape, and has a named troubleshooting entry for the exec-launcher failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design partner's shell → `scripts/install-linux.sh` | Operator-supplied destination path and argv cross into the script; unvalidated handling could write outside the intended directory |
| repository source tree → installed release binaries | Everything installed must originate from the checked-out source tree; any other origin is an unaudited supply-chain input |
| `docs/GETTING-STARTED.md` → operator's machine | Documented commands are executed verbatim by a reader; a wrong or over-privileged instruction is an effect on their host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Elevation of Privilege | `scripts/install-linux.sh` | high | mitigate | User-local default destination (`${HOME}/.local/bin`); system-wide reachable only through an explicit operator-supplied `--dest`; no privilege-escalation command, no privileged package manager anywhere in the file. Task 2 verify greps the non-comment body for the escalation/package-manager token set and requires a count of 0 |
| T-52-02 | Tampering | `scripts/install-linux.sh` destination-path handling | medium | mitigate | Every `$DEST`/`$STAGE_DIR`/target-path expansion double-quoted; the operator-supplied value is never `eval`'d or `source`'d; `mkdir -p` plus `test -w` fail-fast before any write (Task 2) |
| T-52-05 | Spoofing / Tampering | supply chain (build and install inputs) | medium | mitigate | Source-tree build only — no remote artifact fetch and no privileged installer. Task 2 verify requires a 0 count for network-fetch utilities in the non-comment body; every task asserts `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` |
| T-52-06 | Tampering | repository state during install | medium | mitigate | The script performs no repository-mutating version-control subcommand; Task 2 verify greps the non-comment body for that subcommand set and requires a count of 0 |
| T-52-07 | Denial of Service | interrupted or concurrent install | medium | accept | Stage-then-rename bounds the exposure to the three back-to-back renames instead of the whole build-and-copy window; per RESEARCH "Don't Hand-Roll" a custom lock protocol is out of scope. Accepted with the residual documented in the script header, in the doc, and as the plan's flagged assumption plus backstop truth; recovery is re-running the idempotent installer |
| T-52-08 | Repudiation | install-time layout check | low | accept | The D-09 check is deliberately non-destructive and produces no audit record; the authoritative records remain the SQLite audit DAG and the retained LIVE evidence. Accepted because manufacturing an install-time audit record would overstate what the check proves |
| T-52-SC | Tampering | Cargo dependency surface | medium | mitigate | Zero new packages are installed by this phase; RESEARCH "Package Legitimacy Audit" records none proposed, none `[SUS]`, none `[SLOP]`, so no legitimacy checkpoint is required. Gate: `git diff --exit-code -- Cargo.toml Cargo.lock` in every task's acceptance criteria |

All `high`-severity threats carry a `mitigate` disposition, satisfying the configured block-on-high policy at ASVS L1.
</threat_model>

<verification>
1. `bash -n scripts/install-linux.sh` exits 0 and the file is executable.
2. `D="$(mktemp -d)"; INSTALL_DEST="$D" bash scripts/install-linux.sh` exits 0 and leaves exactly `caprun`, `caprun-exec-launcher`, `caprun-worker` in `$D`, all passing `test -x`, with no leftover staging directory.
3. `bash scripts/install-linux.sh --dest <dir>` behaves identically for an explicitly supplied destination.
4. A non-writable destination, a bogus flag, and (on a non-Linux host) the OS guard each exit non-zero with a stderr line containing `FAIL` or the usage line.
5. The non-comment script body contains zero escalation, privileged-package-manager, network-fetch, and repository-mutating version-control tokens.
6. `./scripts/check-invariants.sh` exits 0.
7. `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0 — this plan changes no TCB code and adds no dependency.
8. `docs/GETTING-STARTED.md` contains the `## Install (Linux)` section, the script command, the manual command set, the `cargo install --path cli/caprun` warning with its separate-package root cause, the corrected usage line, and the new exec-launcher troubleshooting entry; the container section is unchanged.
</verification>

<success_criteria>
- A single documented command produces one directory holding exactly `caprun`, `caprun-worker`, and `caprun-exec-launcher`, all executable (PKG-01 criterion 1).
- The equivalent manual commands are documented and runnable without the repository script (D-01).
- `cargo install --path cli/caprun` is documented as insufficient, with the separate-package root cause named (PKG-01 criterion 1, D-08).
- The installer is thin: one shell file, no packaging framework, no new crate, no new dependency, no privileged operation (PKG-01 criterion 3, D-02, D-10).
- The bounded interruption/concurrency guarantee is stated honestly in the script header, in the doc, and in this plan's flagged assumption — no overclaim of atomicity.
</success_criteria>

<output>
Create `.planning/phases/52-minimal-linux-packaging/52-01-SUMMARY.md` when done.
</output>
