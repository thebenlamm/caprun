---
phase: 52-minimal-linux-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PKG-01

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs/CONFIGURATION.md` presents the operator configuration surface as a three-tier checklist — always-needed inputs for the chosen command, sink-specific credentials and settings, and internal/test-only variables marked do-not-set (PKG-01 criterion 2; D-11)."
    - "The always-needed tier shows the `--policy <path>` CLI flag as the runnable path and documents `CAPRUN_POLICY` as its env fallback feeding the same binder, with the broker default allowlist named as the behavior when neither is supplied (D-12)."
    - "A minimal policy-file example with the exact `allowed_sinks` / `arg_constraints` JSON keys is present, alongside the nine production sink ids and the constraint that the policy file must live outside the workspace root because the broker refuses a policy at or beneath it (D-12)."
    - "The sink-specific tier names every credential and setting the broker actually reads — `CAPRUN_GITHUB_TOKEN`, `CAPRUN_GIT_PUSH_TOKEN`, `CAPRUN_HTTP_WRITE_TOKEN`, `CAPRUN_SMTP_HOST`, `CAPRUN_SMTP_PORT`, `CAPRUN_SMTP_FROM`, `CAPRUN_GITHUB_API_BASE`, `CAPRUN_CONFIRM`, `CAPRUN_CONFIRM_TIMEOUT_SECS` — each bound to the sink or behavior that consumes it (PKG-01 criterion 2)."
    - "Credential guidance is placeholder-only with least-scope framing; no value resembling a real token appears anywhere in the documentation this phase writes (D-13)."
    - "The internal/test-only tier explicitly instructs operators NOT to set `BROKER_SOCK`, `WORKSPACE_FILE`, `INTENT`, `PRIMARY_SEED_FILE_DERIVED`, `PLANNER_SOCK`, `CAPRUN_CODING_I2_PROOF`, or `CAPRUN_ENABLE_IPC_CREATE_SESSION` for normal operation (D-14)."
    - "The documented CLI surface matches the shipped argv parsing: the `run` verb with `--policy` and `--seed-from-file` flags, plus the `confirm`, `deny`, `review`, `grant`, and `audit` verbs (PKG-01 criterion 2)."
    - "Every `CAPRUN_*` name the document presents as operator-facing is actually read by shipped code, and no name is presented that the code does not read (D-14)."
    - statement: "Broker-local credential variables are never forwarded into the confined worker or exec child — the documentation states this custody boundary rather than leaving a reader to assume the worker inherits them."
      verification: backstop
  artifacts:
    - path: docs/CONFIGURATION.md
      provides: "Corrected CLI reference, minimal policy example, and the three-tier operator configuration checklist"
      contains: "## Operator Configuration Checklist"
  key_links:
    - from: "docs/CONFIGURATION.md"
      to: "cli/caprun/src/main.rs:9,434-439"
      via: "documented `--policy` flag / `CAPRUN_POLICY` fallback feeding the same bind_policy call"
      pattern: "CAPRUN_POLICY"
    - from: "docs/CONFIGURATION.md"
      to: "crates/runtime-core/src/policy.rs:175-184"
      via: "policy JSON example using the exact serde field names of SessionPolicy"
      pattern: "allowed_sinks"
    - from: "docs/CONFIGURATION.md"
      to: "crates/brokerd/src/sinks/{github_pr,git_push,http_write,email_smtp}.rs"
      via: "sink-specific credential tier naming the broker-local env reads per sink"
      pattern: "CAPRUN_GIT_PUSH_TOKEN"
    - from: "docs/CONFIGURATION.md"
      to: "crates/brokerd/src/server.rs:1766-1772"
      via: "do-not-set tier naming the internal IPC session-creation gate"
      pattern: "CAPRUN_ENABLE_IPC_CREATE_SESSION"
  assumptions:
    - "`CAPRUN_GITHUB_API_BASE` is presented as a sink-specific override whose production default is the real GitHub API, because the code reads it in both the PR sink and the grant/confirmation path — it is not treated as test-only despite the verification harness being its main consumer today."
    - "`CAPRUN_PLANNER` / `CAPRUN_PLANNER_MODEL` / `OPENAI_API_KEY` are documented as optional sidecar configuration outside the PKG-01 minimal deterministic path (D-07), not as required operator setup."
    - "`SESSION_ID` is retained in the do-not-set tier with an explicit statement that no current code path reads it — this reconciles D-14 (which names it) with the verified grep showing it was removed, instead of silently dropping either."
  prohibitions:
    - "MUST NOT write a real credential value into any file, command example, log line, or planning artifact; every credential appears only as an angle-bracket placeholder with least-scope framing (D-13)."
    - "MUST NOT instruct operators to set worker-protocol-internal or test-only variables for normal operation — in particular the internal IPC session-creation gate, whose default-disabled state guards a privileged session-creation arm (D-14)."
    - "MUST NOT document a configuration variable that no shipped code reads, and MUST NOT omit an operator-facing variable that shipped code does read — stale documentation drift is itself the failure this section exists to correct."
    - "MUST NOT present the per-session policy as capable of relaxing or disabling the hardcoded value-injection enforcement in the Rust trusted computing base; policy narrows which sinks and arguments are callable and can never override it."
    - "MUST NOT claim or imply that any configuration described here substitutes for the authoritative Linux verification harness or the retained LIVE evidence."
---

<objective>
Replace the stale configuration surface in `docs/CONFIGURATION.md` with what the shipped code actually reads: the real verb-and-flag CLI, a minimal policy-file example, and a three-tier operator checklist that separates always-needed inputs from sink-specific credentials from internal variables an operator must never set.

Purpose: PKG-01 criterion 2 is the env/credential checklist. The existing "Worker Environment Variables" table documents a variable no current code reads and omits every `CAPRUN_*` variable that exists — a design partner following it today configures nothing correctly and may set an internal privilege gate. This is a replacement, not a patch.

Output: a corrected `## caprun CLI Arguments` section and a new `## Operator Configuration Checklist` in `docs/CONFIGURATION.md`.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-minimal-linux-packaging/52-CONTEXT.md
@.planning/phases/52-minimal-linux-packaging/52-RESEARCH.md
@.planning/phases/52-minimal-linux-packaging/52-PATTERNS.md
@.planning/phases/52-minimal-linux-packaging/52-VALIDATION.md
@CLAUDE.md
@docs/CONFIGURATION.md
</context>

<artifacts_this_phase_produces>
Phase 52 overall produces these NEW symbols (nothing here pre-exists; drift verification must not flag them as unacknowledged):

1. **`scripts/install-linux.sh`** — new file (Plan 01).
2. **Install-script CLI surface** — `--dest <dir>`, `--help` (Plan 01).
3. **Install-script env override** — `INSTALL_DEST` (script-level only; NOT a runtime `caprun` variable) (Plan 01).
4. **New doc section headings** — `## Install (Linux)` in `docs/GETTING-STARTED.md` (Plan 01); `## Operator Configuration Checklist` with `### Tier 1 — Always needed`, `### Tier 2 — Sink-specific credentials and settings`, `### Tier 3 — Internal and test-only (do not set)` in `docs/CONFIGURATION.md` (this plan).
5. **Newly-documented-as-contract env vars** — `CAPRUN_POLICY`, `CAPRUN_GITHUB_TOKEN`, `CAPRUN_GIT_PUSH_TOKEN`, `CAPRUN_HTTP_WRITE_TOKEN`, `CAPRUN_SMTP_HOST`, `CAPRUN_SMTP_PORT`, `CAPRUN_SMTP_FROM`, `CAPRUN_GITHUB_API_BASE`, `CAPRUN_CONFIRM`, `CAPRUN_CONFIRM_TIMEOUT_SECS`, `CAPRUN_PLANNER`, `CAPRUN_PLANNER_MODEL`, `CAPRUN_CODING_I2_PROOF`, `CAPRUN_ENABLE_IPC_CREATE_SESSION`, `INTENT`, `PRIMARY_SEED_FILE_DERIVED`, `PLANNER_SOCK` — all already read by shipped code, newly documented as contract by this plan.

This plan (52-02) owns the `docs/CONFIGURATION.md` half of item 4 and all of item 5.

**Explicitly NOT produced:** no new crate, no new Cargo dependency, no change to any file under `crates/` or `cli/`, no new runtime variable.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the CLI reference and add the minimal policy-file example</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>
    - docs/CONFIGURATION.md lines 1-45 — the current `## caprun CLI Arguments` section, its table shape, its `**Example — <label>:**` bold-lead-in convention, and the section it precedes
    - cli/caprun/src/main.rs lines 9-30 — the authoritative usage line and the three intent kinds with their parameter meanings
    - cli/caprun/src/main.rs lines 85-180 — the `confirm` / `deny` / `review` / `grant` / `audit` verb arms and which of them require an explicit audit database path
    - cli/caprun/src/main.rs lines 220-290 — the `run` verb alias, the `--policy` and `--seed-from-file` flag loop, and the fact that a seed file replaces the `<intent-param>` positional entirely
    - cli/caprun/src/main.rs lines 430-440 — flag-over-env precedence and the broker default when neither is supplied
    - crates/runtime-core/src/policy.rs lines 34-55 and 170-235 — the nine production sink id strings and the exact `SessionPolicy` serde field names (no rename attributes)
    - crates/brokerd/src/policy.rs lines 1-120 — `bind_policy` semantics and the containment refusal for a policy path at or beneath the workspace root
  </read_first>
  <action>
    Rewrite the `## caprun CLI Arguments` section of `docs/CONFIGURATION.md` against the shipped argv parsing, preserving the file's existing conventions: `<!-- generated-by: gsd-doc-writer -->` marker at the top, `---` rules between H2 sections, markdown tables with header rows, and `**Example — <label>:**` bold lead-ins before fenced bash blocks.

    1. **Replace the usage fence** with the full verified surface, one invocation shape per line in a single plain fenced block (no shell prompt):
       - the run form: `caprun [run] [--policy <path>] [--seed-from-file <path>] <intent-kind> <intent-param> <workspace-file> [audit-db-path]`
       - `caprun confirm <effect_id> [audit-db-path]`
       - `caprun deny <effect_id> [audit-db-path]`
       - `caprun review <effect_id> [audit-db-path]`
       - `caprun grant <session_id> [audit-db-path]`
       - `caprun audit <session_id> <audit-db-path>` — noting in the table that this verb requires an explicit database path and has no in-memory default

    2. **Replace the argument table** with rows for `<intent-kind>`, `<intent-param>`, `<workspace-file>`, `[audit-db-path]`, `--policy <path>`, and `--seed-from-file <path>`. Keep the existing four columns. State in the `--seed-from-file` row that the seed file replaces the `<intent-param>` positional rather than adding to it, and that a missing or unreadable seed file is a hard error with no fallback.

    3. **Add an intent-kind table** listing `send-email-summary` (parameter: recipient address), `create-file-from-report` (parameter: path created under the workspace root), and `safe-coding-workflow` (parameter: path to a JSON coding-intent file).

    4. **Correct the runnable examples** so each passes an intent kind and intent parameter rather than a bare workspace path, and add one example using `--policy` against a policy file located outside the workspace directory.

    5. **Add a `### Session policy file` subsection** documenting: `--policy <path>` as the preferred runnable path and `CAPRUN_POLICY` as the env fallback feeding the same binder with the flag taking precedence; the behavior when neither is set (the broker binds its default allowlist of the nine production sinks with no argument constraints); a fenced JSON example whose only two keys are `allowed_sinks` (an array of sink ids) and `arg_constraints` (an empty object in the minimal case); the full list of the nine production sink id strings read from `crates/runtime-core/src/policy.rs`; and a plainly stated constraint that the policy file must live OUTSIDE the workspace directory being operated on because the broker refuses to bind a policy at or beneath the workspace root.

    6. **Add one sentence** stating that the policy narrows which sinks and arguments are callable and can never relax the value-injection enforcement hardcoded in the Rust trusted computing base.

    Do not touch the `## Audit Database`, `## Hardcoded Confinement Parameters`, `## Cargo Build Configuration`, `## Platform Requirements`, or the container-testing section in this task.
  </action>
  <verify>
    <automated>grep -q 'caprun \[run\] \[--policy <path>\] \[--seed-from-file <path>\] <intent-kind> <intent-param> <workspace-file> \[audit-db-path\]' docs/CONFIGURATION.md &amp;&amp; grep -q 'caprun audit <session_id> <audit-db-path>' docs/CONFIGURATION.md &amp;&amp; grep -q 'allowed_sinks' docs/CONFIGURATION.md &amp;&amp; grep -q 'arg_constraints' docs/CONFIGURATION.md &amp;&amp; grep -q 'http.request.write' docs/CONFIGURATION.md &amp;&amp; grep -q 'safe-coding-workflow' docs/CONFIGURATION.md &amp;&amp; test "$(grep -c '^caprun <workspace-file> \[audit-db-path\]$' docs/CONFIGURATION.md || true)" -eq 0 &amp;&amp; echo CLI-PASS</automated>
  </verify>
  <acceptance_criteria>
    - The full run-form usage line appears verbatim in `docs/CONFIGURATION.md`
    - All five non-run verbs appear: `grep -q 'caprun confirm'`, `grep -q 'caprun deny'`, `grep -q 'caprun review'`, `grep -q 'caprun grant'`, `grep -q 'caprun audit'`
    - The superseded bare-positional usage line is absent, exactly as the verify command asserts
    - All three intent kinds are named: `send-email-summary`, `create-file-from-report`, `safe-coding-workflow`
    - `grep -q 'allowed_sinks' docs/CONFIGURATION.md` and `grep -q 'arg_constraints' docs/CONFIGURATION.md` — the policy example uses the real serde field names
    - All nine production sink ids appear: `email.send`, `file.create`, `file.write`, `process.exec`, `git.commit`, `http.request`, `github.pr`, `http.request.write`, `git.push`
    - `grep -q 'CAPRUN_POLICY' docs/CONFIGURATION.md` and `grep -q -- '--policy' docs/CONFIGURATION.md`
    - `grep -qi 'outside the workspace' docs/CONFIGURATION.md` — the containment constraint on the policy file's own location is stated
    - `grep -q '<!-- generated-by: gsd-doc-writer -->' docs/CONFIGURATION.md` — the file marker is preserved
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0
  </acceptance_criteria>
  <done>The CLI section matches the shipped argv parsing verb for verb and flag for flag, and a design partner can copy a working minimal policy file and know where to put it.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stale environment table with the three-tier operator configuration checklist</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>
    - docs/CONFIGURATION.md lines 30-46 — the current `## Worker Environment Variables` section being replaced wholesale, its three-column table shape, and the trailing prose
    - .planning/phases/52-minimal-linux-packaging/52-RESEARCH.md — "Common Pitfalls / Pitfall 3" (the exact drift being corrected) and "Security Domain / Known Threat Patterns" (which variables are operator-facing and which are internal)
    - .planning/phases/52-minimal-linux-packaging/52-PATTERNS.md — the `| Variable | Set by | Description |` table shape to keep and the prose-then-table convention for the credential guidance
    - crates/brokerd/src/sinks/github_pr.rs lines 60-95 — `CAPRUN_GITHUB_TOKEN` and `CAPRUN_GITHUB_API_BASE` reads and their required/optional semantics
    - crates/brokerd/src/sinks/git_push.rs lines 610-640 — the `CAPRUN_GIT_PUSH_TOKEN` read
    - crates/brokerd/src/sinks/http_write.rs lines 65-95 — the `CAPRUN_HTTP_WRITE_TOKEN` read
    - crates/brokerd/src/sinks/email_smtp.rs — the `CAPRUN_SMTP_HOST` / `CAPRUN_SMTP_PORT` / `CAPRUN_SMTP_FROM` reads and their defaults
    - crates/brokerd/src/confirmation.rs — the `CAPRUN_GITHUB_TOKEN` / `CAPRUN_GITHUB_API_BASE` reads on the grant path
    - crates/brokerd/src/server.rs lines 1755-1775 — `CAPRUN_ENABLE_IPC_CREATE_SESSION`, default-disabled, gating an internal session-creation arm
    - cli/caprun/src/main.rs lines 355-360, 560-600, 830-840, 1030-1045 — `CAPRUN_CODING_I2_PROOF`, `CAPRUN_PLANNER`, `CAPRUN_PLANNER_MODEL`, `CAPRUN_CONFIRM`, `CAPRUN_CONFIRM_TIMEOUT_SECS`
    - cli/caprun/src/worker.rs lines 95-125 and 380-400 — the complete broker-set worker-protocol variable list: `BROKER_SOCK`, `WORKSPACE_FILE`, `INTENT`, `PRIMARY_SEED_FILE_DERIVED`, `CAPRUN_CODING_I2_PROOF`, `CAPRUN_PLANNER`, `PLANNER_SOCK`
    - crates/brokerd/src/sinks/process_exec.rs — the environment-scrubbing behavior that keeps broker credentials out of confined children
  </read_first>
  <action>
    Replace the `## Worker Environment Variables` section wholesale with a `## Operator Configuration Checklist` H2 containing three `###` subsections, then a short retained subsection for the worker protocol. Keep the file's `| Variable | Set by | Description |` table shape where it fits, adding a `Required when` column for the credential tier.

    **`### Tier 1 — Always needed`** — the inputs every run requires regardless of sink: the `<workspace-file>` positional, the `[audit-db-path]` positional (with its in-memory default), and the policy input via `--policy <path>` with `CAPRUN_POLICY` as the fallback. Cross-reference the `### Session policy file` subsection rather than duplicating the JSON example.

    **`### Tier 2 — Sink-specific credentials and settings`** — one row per variable, each stating the sink or behavior that consumes it and when it becomes required:
      - `CAPRUN_GITHUB_TOKEN` — the `github.pr` sink and the `caprun grant` path
      - `CAPRUN_GIT_PUSH_TOKEN` — the `git.push` sink
      - `CAPRUN_HTTP_WRITE_TOKEN` — the `http.request.write` sink
      - `CAPRUN_SMTP_HOST`, `CAPRUN_SMTP_PORT`, `CAPRUN_SMTP_FROM` — the `email.send` sink
      - `CAPRUN_GITHUB_API_BASE` — the GitHub API base used by the PR sink and the grant path; state that the production default is the real GitHub API and that overriding it is for an alternate or mock API endpoint
      - `CAPRUN_CONFIRM` — set to `external` to force the out-of-band confirmation poll instead of the interactive prompt
      - `CAPRUN_CONFIRM_TIMEOUT_SECS` — how long a pending confirmation waits
      - `CAPRUN_PLANNER` (value `llm`), `CAPRUN_PLANNER_MODEL`, and `OPENAI_API_KEY` — clearly marked as the OPTIONAL language-model sidecar, explicitly outside the minimal deterministic install path this document's install section describes

    Precede the Tier 2 table with a short prose paragraph, following this file's existing "state the constraint plainly in one sentence, then a table" convention, covering three points: these variables are read by the broker process only and are never forwarded into the confined worker or exec child; every example uses an angle-bracket placeholder and never a real value; and each token should be minted at the least scope that lets its sink function. Follow the table with a fenced bash block of placeholder exports using angle-bracket placeholders such as `export CAPRUN_GITHUB_TOKEN='<paste-your-least-scope-token>'` — never a value shaped like a real credential.

    **`### Tier 3 — Internal and test-only (do not set)`** — a table whose leading sentence states that an operator should never set any of these for normal operation, with one row each for:
      - `BROKER_SOCK`, `WORKSPACE_FILE`, `INTENT`, `PRIMARY_SEED_FILE_DERIVED`, `PLANNER_SOCK` — set by the orchestrator when it spawns the worker or sidecar
      - `CAPRUN_CODING_I2_PROOF` — a test-only proof selector
      - `CAPRUN_ENABLE_IPC_CREATE_SESSION` — an internal gate that defaults to disabled and guards a privileged session-creation arm; setting it speculatively widens the attack surface
      - `SESSION_ID` — a legacy name retained here only so it is not reintroduced; its description MUST contain the exact phrase `not read by any current code path`
      - the `CAPRUN_ENV_LEAK_SENTINEL_` prefix — used by environment-scrub assertions in the test suite

    **Retain a short `### Worker protocol variables (reference)` subsection** if useful for contributors, but it must not present any of these as operator-settable, and it must not reintroduce a row for a variable no code reads.

    Every `CAPRUN_*` name written into this document must be one the code actually reads — the phase-level cross-check in Plan 03 asserts this mechanically in both directions.
  </action>
  <verify>
    <automated>grep -q '## Operator Configuration Checklist' docs/CONFIGURATION.md &amp;&amp; grep -q '### Tier 1 — Always needed' docs/CONFIGURATION.md &amp;&amp; grep -q '### Tier 2 — Sink-specific credentials and settings' docs/CONFIGURATION.md &amp;&amp; grep -q '### Tier 3 — Internal and test-only (do not set)' docs/CONFIGURATION.md &amp;&amp; for v in CAPRUN_POLICY CAPRUN_GITHUB_TOKEN CAPRUN_GIT_PUSH_TOKEN CAPRUN_HTTP_WRITE_TOKEN CAPRUN_SMTP_HOST CAPRUN_SMTP_PORT CAPRUN_SMTP_FROM CAPRUN_GITHUB_API_BASE CAPRUN_CONFIRM CAPRUN_CONFIRM_TIMEOUT_SECS CAPRUN_PLANNER CAPRUN_PLANNER_MODEL CAPRUN_CODING_I2_PROOF CAPRUN_ENABLE_IPC_CREATE_SESSION BROKER_SOCK WORKSPACE_FILE PRIMARY_SEED_FILE_DERIVED PLANNER_SOCK; do grep -qE "${v}([^A-Z0-9_]|$)" docs/CONFIGURATION.md || { echo "MISSING FROM DOC: $v"; exit 1; }; done &amp;&amp; test "$(grep -c 'SESSION_ID' docs/CONFIGURATION.md)" -ge 1 &amp;&amp; test "$(grep -c 'SESSION_ID' docs/CONFIGURATION.md)" -eq "$(grep 'SESSION_ID' docs/CONFIGURATION.md | grep -c 'not read by any current code path')" &amp;&amp; test "$(cat docs/CONFIGURATION.md | grep -cE 'gh[pousr]_[A-Za-z0-9]{20,}' || true)" -eq 0 &amp;&amp; echo CHECKLIST-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Operator Configuration Checklist' docs/CONFIGURATION.md` and all three `### Tier ...` headings are present verbatim
    - Every variable in the Tier 2 and Tier 3 lists above appears in the document with a word-boundary match, exactly as the verify loop asserts
    - `SESSION_ID` appears at least once, and every line mentioning it also contains `not read by any current code path` — the count-equality assertion in the verify command
    - `test "$(cat docs/CONFIGURATION.md | grep -cE 'gh[pousr]_[A-Za-z0-9]{20,}' || true)" -eq 0` — no value shaped like a real credential
    - `grep -qE "export CAPRUN_GITHUB_TOKEN='<" docs/CONFIGURATION.md` — placeholder exports use the angle-bracket form
    - `grep -qiE 'never forwarded|not forwarded' docs/CONFIGURATION.md` — the broker-local credential custody boundary is stated
    - `grep -qi 'least' docs/CONFIGURATION.md` — least-scope guidance is present
    - `grep -q 'OPENAI_API_KEY' docs/CONFIGURATION.md` and the sidecar is marked optional
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0
  </acceptance_criteria>
  <done>A design partner can walk one tiered checklist to know exactly what to set for their chosen command, what to set only if they use a given sink, and what to never set — with every name matching what the shipped code reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/CONFIGURATION.md` → operator's environment | Documented variables are exported verbatim by a reader; a wrong instruction changes the runtime's security posture on their host |
| documentation examples → version control / logs | Any credential-shaped value written into a doc becomes a committed, greppable secret |
| operator-supplied policy file → broker `bind_policy` | The policy path is a trusted input bound at session creation; documenting the wrong location invites the containment refusal or a worker-reachable policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Information Disclosure | credential guidance in `docs/CONFIGURATION.md` | high | mitigate | Angle-bracket placeholders only, least-scope framing, explicit statement that these are broker-local reads never forwarded to confined children. Task 2 verify greps the whole file for values matching a real token shape and requires a count of 0 |
| T-52-04 | Elevation of Privilege | do-not-set tier in `docs/CONFIGURATION.md` | high | mitigate | Tier 3 names the internal IPC session-creation gate and the test-only proof selector explicitly as never-set-by-operators, with the default-disabled state and the privileged arm it guards stated. Task 2 verify requires both names to be present |
| T-52-09 | Tampering | stale configuration documentation | medium | mitigate | The stale table is replaced wholesale rather than patched; the legacy variable is retained only with the phrase asserting no code reads it, enforced by the count-equality gate; Plan 03 adds the bidirectional documented-vs-source cross-check |
| T-52-10 | Elevation of Privilege | policy-file guidance | medium | mitigate | The policy file's required location outside the workspace root is stated, matching the broker's containment refusal; one sentence states that policy narrows the callable surface and can never relax the value-injection enforcement in the trusted computing base |
| T-52-SC | Tampering | Cargo dependency surface | medium | mitigate | Documentation-only plan; gate `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` in both tasks' acceptance criteria |

All `high`-severity threats carry a `mitigate` disposition, satisfying the configured block-on-high policy at ASVS L1.
</threat_model>

<verification>
1. The full run-form usage line and all five non-run verbs appear verbatim in `docs/CONFIGURATION.md`; the superseded bare-positional usage line is gone.
2. The policy example uses the real `allowed_sinks` / `arg_constraints` keys, all nine production sink ids are listed, and the outside-the-workspace constraint is stated.
3. `## Operator Configuration Checklist` exists with all three `### Tier ...` subsections.
4. Every variable in the Tier 2 and Tier 3 lists is present with a word-boundary match.
5. Every line mentioning the legacy session variable also contains `not read by any current code path`, and it appears at least once.
6. The document contains no value matching a real token shape; placeholder exports use the angle-bracket form.
7. `./scripts/check-invariants.sh` exits 0.
8. `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0.
</verification>

<success_criteria>
- The env/credential checklist covers `CAPRUN_*`, the policy file, and the GitHub grant token as applicable (PKG-01 criterion 2).
- Operator-facing configuration is separated from worker protocol variables and proof/test switches, with the latter marked do-not-set (D-11, D-14).
- Credentials appear only as placeholders with least-scope guidance (D-13).
- `--policy` is the runnable path, `CAPRUN_POLICY` the documented fallback, with a minimal policy example and its location constraint (D-12).
- Nothing under `crates/` or `cli/` changes and no dependency is added (D-10, HYG-02).
</success_criteria>

<output>
Create `.planning/phases/52-minimal-linux-packaging/52-02-SUMMARY.md` when done.
</output>
