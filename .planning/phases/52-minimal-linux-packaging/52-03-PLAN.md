---
phase: 52-minimal-linux-packaging
plan: 03
type: execute
wave: 2
depends_on:
  - 52-01
  - 52-02
files_modified:
  - README.md
  - docs/GETTING-STARTED.md
  - docs/CONFIGURATION.md
autonomous: false
requirements:
  - PKG-01

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`README.md`'s repository-layout tree lists `scripts/install-linux.sh` and stops describing the CLI tree as producing only the orchestrator and the worker — it names the separately packaged `caprun-exec-launcher` (PKG-01 criterion 1)."
    - "`README.md`'s build section points a reader at the install path without duplicating it, and without re-asserting the container recipe as the way to install (PKG-01 criterion 3)."
    - "Every `CAPRUN_*` name appearing anywhere in `README.md`, `docs/GETTING-STARTED.md`, or `docs/CONFIGURATION.md` is a name shipped code actually reads — no documented variable is invented and none is stale."
    - "The three required binary names are consistent across `README.md` and `docs/GETTING-STARTED.md`; no document still implies a two-binary install."
    - "The install documentation states explicitly that the install-time layout check is NOT the Linux security verification, and names `scripts/compose-verify.sh` as the authoritative harness (D-09)."
    - "No file this phase writes contains a value matching a real credential shape (D-13)."
    - "`./scripts/check-invariants.sh` exits 0 and `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0 at the end of the phase — the packaging phase changed no trusted-computing-base code and added no dependency (D-10, HYG-02; PKG-01 criterion 3)."
    - "The container/Colima sections in `README.md` and `docs/GETTING-STARTED.md` survive this phase unrewritten — new install text coexists with them rather than re-litigating them."
    - statement: "A first-time design partner can follow the install documentation top to bottom without consulting source, and the resulting install works on a host that is not this development box."
      verification: backstop
  artifacts:
    - path: README.md
      provides: "Repository-layout entry for the installer, the separately packaged launcher, and a build-section pointer to the install path"
      contains: "install-linux.sh"
  key_links:
    - from: "README.md"
      to: "scripts/install-linux.sh"
      via: "repository-layout tree entry and build-section pointer"
      pattern: "install-linux\\.sh"
    - from: "README.md"
      to: "docs/GETTING-STARTED.md"
      via: "build-section pointer to the Install (Linux) walkthrough"
      pattern: "GETTING-STARTED"
    - from: "docs/GETTING-STARTED.md"
      to: "scripts/compose-verify.sh"
      via: "explicit statement that the layout check is not the Linux security verification"
      pattern: "compose-verify\\.sh"
  assumptions:
    - "FLAGGED (carried from Plan 01, edge-probe PKG-01/concurrency, status `unresolved`): the installer's interruption/parallelism guarantee is bounded — no mixed set from a partial build or partial copy, but the three final renames are not atomic as a set and concurrent installs into one destination are not serialized. Recorded, documented, and recovered by re-running; NOT closed by a lock protocol (out of scope per RESEARCH 'Don't Hand-Roll')."
    - "`docs/ARCHITECTURE.md`, `docs/DEVELOPMENT.md`, and `docs/TESTING.md` are intentionally untouched by this phase — they describe architecture, contributor workflow, and test strategy rather than the install path, and none of them makes a binary-count claim that contradicts it."
    - "The clean-host install and first-time-reader clarity checks are human-only (see the phase validation strategy); the automated gates below prove layout and consistency, not prose quality."
  prohibitions:
    - "MUST NOT rewrite or re-litigate the container/Colima guidance in `README.md` or `docs/GETTING-STARTED.md` — this phase's scope is the install path, and that guidance is tracked as stale elsewhere."
    - "MUST NOT claim that any check performed by the install path constitutes or substitutes for the Linux security verification or the retained LIVE evidence (D-09)."
    - "MUST NOT introduce a package format, release-distribution service, checksum/signature distribution, auto-updater, or macOS install path — all are deferred beyond this phase."
    - "MUST NOT modify any file under `crates/` or `cli/`, and MUST NOT add a Cargo dependency or a crate, to make the documentation true (D-10)."
    - "MUST NOT let a human sign-off substitute for the mechanical gates — the checkpoint runs after the automated verification passes, never in place of it."
---

<objective>
Close the phase: bring `README.md` into agreement with the three-binary install path, run the cross-document and regression gates that prove the whole phase is internally consistent and touched no trusted-computing-base code, and take a single human sign-off on the design-partner-facing walkthrough.

Purpose: PKG-01's deliverable is a documented path a design partner follows. Three files now describe it; drift between them is the failure mode that made the pre-existing configuration documentation useless. This plan is the reconciliation and the honest boundary statement about what the install-time check does and does not prove.

Output: a narrowly updated `README.md`, any drift corrections needed in the two documents from Wave 1, and a recorded human verification.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-minimal-linux-packaging/52-CONTEXT.md
@.planning/phases/52-minimal-linux-packaging/52-RESEARCH.md
@.planning/phases/52-minimal-linux-packaging/52-PATTERNS.md
@.planning/phases/52-minimal-linux-packaging/52-VALIDATION.md
@.planning/phases/52-minimal-linux-packaging/52-01-SUMMARY.md
@.planning/phases/52-minimal-linux-packaging/52-02-SUMMARY.md
@CLAUDE.md
@README.md
</context>

<artifacts_this_phase_produces>
Phase 52 overall produces these NEW symbols (nothing here pre-exists; drift verification must not flag them as unacknowledged):

1. **`scripts/install-linux.sh`** — new file (Plan 01).
2. **Install-script CLI surface** — `--dest <dir>`, `--help` (Plan 01).
3. **Install-script env override** — `INSTALL_DEST` (script-level only; NOT a runtime `caprun` variable) (Plan 01).
4. **New doc section headings** — `## Install (Linux)` in `docs/GETTING-STARTED.md` (Plan 01); `## Operator Configuration Checklist` with `### Tier 1 — Always needed`, `### Tier 2 — Sink-specific credentials and settings`, `### Tier 3 — Internal and test-only (do not set)` in `docs/CONFIGURATION.md` (Plan 02).
5. **Newly-documented-as-contract env vars** — the `CAPRUN_*` names already read by shipped code but never previously documented (Plan 02).
6. **New `README.md` repository-layout entries** — `scripts/install-linux.sh` and `cli/caprun-exec-launcher/` in the layout tree (this plan).

This plan (52-03) owns item 6 and the phase-level reconciliation of items 1-5.

**Explicitly NOT produced:** no new crate, no new Cargo dependency, no change to any file under `crates/` or `cli/`, no `.deb`/`.snap`/cargo-dist/Homebrew artifact, no macOS install path, no new script beyond item 1.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Narrow README update — layout tree entries and a build-section pointer</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 56-92 — the "Build & test" section, its `# comment above command` fenced-bash convention, and the container subsection that must remain untouched
    - README.md lines 118-138 — the repository-layout fenced tree and its `path   # one-line trailing comment` alignment style
    - .planning/phases/52-minimal-linux-packaging/52-PATTERNS.md — the README analysis confirming the narrow scope and the tree alignment convention
    - Cargo.toml line 2 — the workspace members list, the authority for which CLI packages exist
    - docs/GETTING-STARTED.md — the `## Install (Linux)` section produced by Plan 01, so the pointer names the right heading
  </read_first>
  <action>
    Make two narrow edits to `README.md`, matching its existing conventions exactly. Do not touch the "Security model", "Architecture", "Linux-only security tests", "Docker cache policy", "Source of truth", or "License" sections.

    1. **Repository-layout tree** (the fenced block starting at `caprun/`):
       - Add an `install-linux.sh` entry under the `scripts/` subtree, using the same `path   # one-line trailing comment` alignment as the neighbouring `check-invariants.sh` and `docker-cache.sh` lines. The comment should read as the minimal Linux source-build installer producing the three co-located binaries.
       - Correct the `cli/` subtree so it reflects the actual workspace members: `caprun/` (the orchestrator plus the worker binary and the end-to-end tests), `caprun-exec-launcher/` (the separately packaged self-confining exec helper the broker spawns), and `caprun-planner/` (the optional language-model sidecar). The current single-entry form asserts the CLI tree yields only the orchestrator and the worker, which contradicts the install path.

    2. **Build & test section** — add a short pointer, in this file's `# comment above command` fenced-bash style, showing `bash scripts/install-linux.sh` as the install path and naming `docs/GETTING-STARTED.md` as the walkthrough with the manual equivalent. Keep it to a couple of lines: the README points, `docs/GETTING-STARTED.md` explains. Place it after the existing build/test commands and before the "Linux-only security tests" subheading, so it does not disturb the container guidance that follows.
  </action>
  <verify>
    <automated>grep -q 'install-linux.sh' README.md &amp;&amp; grep -q 'caprun-exec-launcher' README.md &amp;&amp; grep -q 'GETTING-STARTED' README.md &amp;&amp; grep -q 'colima start' README.md &amp;&amp; grep -q 'docker-cache.sh' README.md &amp;&amp; grep -q '<!-- generated-by: gsd-doc-writer -->' README.md &amp;&amp; echo README-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'install-linux.sh' README.md` is at least 2 (layout tree entry and build-section pointer)
    - `grep -q 'caprun-exec-launcher' README.md` — the separately packaged launcher is named in the layout tree
    - `grep -q 'caprun-planner' README.md` — the optional sidecar package is named
    - `grep -q 'GETTING-STARTED' README.md` — the pointer names the walkthrough document
    - `grep -q 'colima start' README.md` and `grep -q 'docker-cache.sh' README.md` — the container guidance and cache-policy sections were not removed or rewritten
    - `grep -q '<!-- generated-by: gsd-doc-writer -->' README.md` — the file marker is preserved
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0
  </acceptance_criteria>
  <done>`README.md` names the installer and the separately packaged launcher, points at the walkthrough, and leaves every out-of-scope section byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-document consistency gate, the not-a-security-proof statement, and the phase regression check</name>
  <files>docs/GETTING-STARTED.md, docs/CONFIGURATION.md, README.md</files>
  <read_first>
    - README.md — as written by Task 1
    - docs/GETTING-STARTED.md — as written by Plan 01, in full
    - docs/CONFIGURATION.md — as written by Plan 02, in full
    - scripts/install-linux.sh — as written by Plan 01, in full
    - .planning/phases/52-minimal-linux-packaging/52-RESEARCH.md — "Validation Architecture / Phase Requirements → Test Map" for the bidirectional environment-variable cross-check this task mechanizes
    - .planning/phases/52-minimal-linux-packaging/52-VALIDATION.md — the sampling contract and the two human-only verifications this gate deliberately does not attempt
    - CLAUDE.md — the statement that `scripts/compose-verify.sh` and `scripts/mailpit-verify.sh` are the authoritative Linux verification harnesses and require Docker on a provisioned host
  </read_first>
  <action>
    Reconcile the three documents and the script into one consistent story, then run the phase regression gate. This task edits only where a gate below fails; a clean pass means no edit is needed in that file.

    1. **Add the boundary statement (D-09).** In the `## Install (Linux)` section of `docs/GETTING-STARTED.md`, ensure there is one plainly worded sentence stating that the installer's post-install check confirms only that the three binaries are present and executable in one directory — it does not exercise confinement, does not open a Session, and is not a substitute for the Linux verification harness. Name `scripts/compose-verify.sh` as that harness and state that it requires Docker on a provisioned Linux host. If Plan 01 already produced an equivalent sentence, verify it names the harness by filename and leave it.

    2. **Run the bidirectional environment-variable cross-check.** Every `CAPRUN_*` token appearing in any of the three documents must be a name shipped code actually reads. Where the gate reports a name absent from source, fix the document — never fix the gate, and never add code to make a document true.

    3. **Run the three-binary consistency check.** `README.md` and `docs/GETTING-STARTED.md` must each name `caprun`, `caprun-worker`, and `caprun-exec-launcher`, and no document may still describe an install as complete without the launcher.

    4. **Run the credential-shape check** across `README.md`, `docs/GETTING-STARTED.md`, `docs/CONFIGURATION.md`, and `scripts/install-linux.sh`: nothing matching a real token shape may appear in any of them.

    5. **Run the phase regression gate:** `./scripts/check-invariants.sh` must exit 0, `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` must exit 0, `bash -n scripts/install-linux.sh` must exit 0, and a fresh dry-run install into a temporary destination must produce exactly the three executables.

    6. **Record the phase's verification boundary in the summary**, not by weakening any gate: state which checks ran on this host and that the clean-host install and first-time-reader clarity checks remain the human-only items named in the phase validation strategy.

    If any gate fails, correct the offending document. Do not relax a gate, do not add a suppression, and do not change any file under `crates/` or `cli/`.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; git diff --exit-code -- Cargo.toml Cargo.lock crates cli &amp;&amp; bash -n scripts/install-linux.sh &amp;&amp; for v in $(grep -ohE 'CAPRUN_[A-Z0-9_]+' README.md docs/GETTING-STARTED.md docs/CONFIGURATION.md | sort -u); do grep -rqE "$v" crates/ cli/ --include=*.rs || { echo "NOT READ BY ANY SOURCE: $v"; exit 1; }; done &amp;&amp; for f in README.md docs/GETTING-STARTED.md; do for b in caprun-worker caprun-exec-launcher; do grep -q "$b" "$f" || { echo "MISSING $b IN $f"; exit 1; }; done; done &amp;&amp; test "$(cat README.md docs/GETTING-STARTED.md docs/CONFIGURATION.md scripts/install-linux.sh | grep -cE 'gh[pousr]_[A-Za-z0-9]{20,}' || true)" -eq 0 &amp;&amp; grep -q 'compose-verify.sh' docs/GETTING-STARTED.md &amp;&amp; D="$(mktemp -d)" &amp;&amp; INSTALL_DEST="$D" bash scripts/install-linux.sh &amp;&amp; test "$(ls -A1 "$D" | sort | tr '\n' ' ')" = "caprun caprun-exec-launcher caprun-worker " &amp;&amp; echo PHASE-GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/check-invariants.sh` exits 0
    - `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0 — the phase changed no trusted-computing-base code, no manifest, and no lockfile
    - `bash -n scripts/install-linux.sh` exits 0
    - Every `CAPRUN_*` token found in the three documents is matched by a grep against `crates/` and `cli/` Rust sources, exactly as the verify loop asserts
    - `caprun-worker` and `caprun-exec-launcher` each appear in both `README.md` and `docs/GETTING-STARTED.md`
    - `test "$(cat README.md docs/GETTING-STARTED.md docs/CONFIGURATION.md scripts/install-linux.sh | grep -cE 'gh[pousr]_[A-Za-z0-9]{20,}' || true)" -eq 0`
    - `grep -q 'compose-verify.sh' docs/GETTING-STARTED.md` — the authoritative harness is named where the boundary statement lives
    - Behavior: `D="$(mktemp -d)"; INSTALL_DEST="$D" bash scripts/install-linux.sh` exits 0 and `ls -A1 "$D"` lists exactly the three required names
    - The summary records which checks ran on this host and names the two human-only verifications that did not
  </acceptance_criteria>
  <done>All three documents, the script, and the shipped source agree; the phase regression gate is green; and the install-time check is documented as a layout check rather than a security proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the design-partner install walkthrough</name>
  <what-built>
    The complete PKG-01 install path: `scripts/install-linux.sh` (build the release targets, stage, install exactly `caprun`, `caprun-worker`, and `caprun-exec-launcher` into one destination, verify the layout), the `## Install (Linux)` walkthrough with the manual equivalent and the `cargo install --path cli/caprun` insufficiency warning in `docs/GETTING-STARTED.md`, the three-tier operator configuration checklist in `docs/CONFIGURATION.md`, and the narrow `README.md` layout/pointer updates. All automated gates in Tasks 1 and 2 have already passed.
  </what-built>
  <how-to-verify>
    1. Read `docs/GETTING-STARTED.md` from the top of `## Install (Linux)` through the end of its manual-equivalent commands. Confirm you could follow it on a fresh Linux host without opening any source file.
    2. Confirm the section states clearly that `cargo install --path cli/caprun` alone is not enough, and that the reason given (`caprun-exec-launcher` is a separate Cargo package) is stated as the cause rather than just asserted.
    3. Run `bash scripts/install-linux.sh --dest "$(mktemp -d)"` yourself. Confirm the output reports each installed path and ends in a line containing `PASS`, and that the destination it names holds exactly three executables.
    4. Read `docs/CONFIGURATION.md` `## Operator Configuration Checklist`. Confirm the three tiers are unambiguous about which variables you set for your chosen command, which you set only for a specific sink, and which you must never set. Confirm every credential shown is an obvious placeholder.
    5. Confirm the install documentation does not claim the post-install check proves confinement, and that it names `scripts/compose-verify.sh` as the authoritative Linux verification.
    6. Accept the recorded residual: an install interrupted between the final three renames, or two installs run concurrently into the same destination, can still leave a set mixed across two builds; the documented recovery is to re-run the installer. No lock protocol was built (out of scope).
    7. Note the two verifications this phase could not perform on this host: an install on a clean design-partner machine, and a first-time reader following the doc cold. Decide whether either should gate the milestone.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which section reads wrong and what it should say instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `README.md` / `docs/*.md` → operator's machine | Documented commands are executed verbatim by a reader; drift between documents means one of them is instructing the reader wrongly |
| install-time layout check → reader's belief about security posture | A check that verifies file presence can be mistaken for a check that verifies confinement; the gap between them is a false-assurance surface |
| phase diff → trusted computing base | A documentation phase that silently touches enforcement code or the dependency surface would ship an unreviewed change under a packaging label |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-11 | Spoofing (false assurance) | install-time layout check vs. the Linux security verification | high | mitigate | Task 2 requires an explicit statement that the post-install check confirms presence and executability only, naming `scripts/compose-verify.sh` as the authoritative harness; gate asserts the filename appears in `docs/GETTING-STARTED.md`, and the human checkpoint re-reads the wording |
| T-52-03 | Information Disclosure | credential examples across all phase-written files | high | mitigate | Task 2 runs the real-token-shape check across all four files this phase writes, not just the configuration document, and requires a count of 0 |
| T-52-09 | Tampering | cross-document configuration drift | medium | mitigate | Task 2 runs the bidirectional check requiring every documented `CAPRUN_*` name to be read by shipped source, plus the three-binary consistency check across `README.md` and `docs/GETTING-STARTED.md`; a failure is fixed in the document, never by relaxing the gate |
| T-52-13 | Tampering | trusted-computing-base and dependency surface | high | mitigate | Every task asserts `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0 and `./scripts/check-invariants.sh` exits 0, so a packaging phase cannot smuggle an enforcement or dependency change |
| T-52-07 | Denial of Service | interrupted or concurrent install | medium | accept | Carried from Plan 01: stage-then-rename bounds the exposure; the residual is documented in the script header, the walkthrough, and the plan's flagged assumption, and is explicitly surfaced to the human at the checkpoint rather than buried |
| T-52-SC | Tampering | Cargo dependency surface | medium | mitigate | Zero new packages; RESEARCH "Package Legitimacy Audit" records none proposed, none `[SUS]`, none `[SLOP]`, so no legitimacy checkpoint applies. Gate: the lockfile/manifest diff assertion above |

All `high`-severity threats carry a `mitigate` disposition, satisfying the configured block-on-high policy at ASVS L1.
</threat_model>

<verification>
1. `README.md` names `install-linux.sh` at least twice, names `caprun-exec-launcher` and `caprun-planner`, points at `docs/GETTING-STARTED.md`, and still contains its container and cache-policy sections.
2. Every `CAPRUN_*` token in the three documents matches a name read by Rust source under `crates/` or `cli/`.
3. `caprun-worker` and `caprun-exec-launcher` each appear in both `README.md` and `docs/GETTING-STARTED.md`.
4. No value matching a real credential shape appears in `README.md`, `docs/GETTING-STARTED.md`, `docs/CONFIGURATION.md`, or `scripts/install-linux.sh`.
5. `docs/GETTING-STARTED.md` names `scripts/compose-verify.sh` in the boundary statement about what the install-time check does not prove.
6. `./scripts/check-invariants.sh` exits 0.
7. `git diff --exit-code -- Cargo.toml Cargo.lock crates cli` exits 0.
8. A fresh dry-run install into a temporary destination exits 0 and yields exactly `caprun`, `caprun-exec-launcher`, `caprun-worker`.
9. The human checkpoint is recorded with an explicit approval or a list of requested wording changes.
</verification>

<success_criteria>
- The documented design-partner install path is internally consistent across `README.md`, `docs/GETTING-STARTED.md`, and `docs/CONFIGURATION.md` (PKG-01, all three criteria).
- The install-time layout check is documented as exactly that, with the authoritative Linux verification named (D-09).
- The phase demonstrably changed no code under `crates/` or `cli/`, added no crate and no dependency, and left `check-invariants.sh` green (D-10, HYG-02).
- The bounded interruption/concurrency residual is surfaced to a human rather than buried.
- A human has read the design-partner-facing walkthrough and signed off or named the corrections.
</success_criteria>

<output>
Create `.planning/phases/52-minimal-linux-packaging/52-03-SUMMARY.md` when done.
</output>
