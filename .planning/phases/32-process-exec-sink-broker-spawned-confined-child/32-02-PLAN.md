---
phase: 32-process-exec-sink-broker-spawned-confined-child
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/sandbox/src/landlock.rs
  - crates/sandbox/src/seccomp.rs
  - crates/sandbox/src/lib.rs
autonomous: true
requirements: [EXEC-04]

must_haves:
  truths:
    - "`exec_child_ruleset(workspace_root)` builds a Landlock ruleset that ALLOWS ReadFile+Execute on enumerated system paths and ReadFile+WriteFile on WorkspaceRoot only, and denies everything else — so the target binary can load but no arbitrary path is reachable."
    - "`exec_child_filter()` reuses the worker's socket(AF_INET/AF_INET6) deny UNCHANGED but does NOT deny execve/execveat — the launcher's one legitimate execve must succeed."
    - "Both new functions are `#[cfg(target_os=\"linux\")]`; a `#[cfg(not(target_os=\"linux\"))]` no-op stub returns Ok(()) so the crate builds on Mac."
  artifacts:
    - crates/sandbox/src/landlock.rs
    - crates/sandbox/src/seccomp.rs
  key_links:
    - "exec_child_ruleset is NOT deny_all_filesystem() reused — reusing the zero-allow ruleset would block the target binary from loading (EACCES/ENOEXEC)."
    - "exec_child_filter drops ONLY the two execve/execveat deny entries from apply_worker_filter; the socket deny is byte-for-byte identical and persists across execve."
---

<objective>
Add the two kernel-confinement primitives the exec child needs, as NEW
constructors beside the worker's existing ones — a narrow-allow Landlock ruleset
(`exec_child_ruleset`) and an execve-permitting, net-denying seccomp filter
(`exec_child_filter`). No change to the worker's `deny_all_filesystem()` /
`apply_worker_filter()` / `apply_confinement()`.

Purpose: The `caprun-exec-launcher` (32-03) applies these to ITSELF post-fork,
before its `execve` of the target (DESIGN §1.3 Option B). They must exist and
compile before the launcher can call them.
Output: `sandbox::landlock::exec_child_ruleset`, `sandbox::seccomp::exec_child_filter`,
re-exported from `sandbox`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: exec_child_ruleset() — Landlock narrow allow-list</name>
  <files>crates/sandbox/src/landlock.rs, crates/sandbox/src/lib.rs</files>
  <read_first>
    - crates/sandbox/src/landlock.rs (full — the `deny_all_filesystem()` shape at 16-38 and its `#[cfg]` stub; REUSE `handle_access(AccessFs::from_all(abi))` verbatim, then ADD allow-rules).
    - crates/sandbox/src/lib.rs lines 38-44 (module + `pub use` re-exports).
    - DESIGN §1.4 "Landlock — a NEW narrow-allow-list ruleset, NOT deny_all_filesystem() reused verbatim" (ReadFile+Execute on /usr,/bin,/lib,/lib64-equivalent; ReadFile+WriteFile on WorkspaceRoot only; deny ~/.ssh etc.) and §8 item 1 (exact path strings are a deployment constant, resolved against the container).
    - 32-RESEARCH.md "Pattern 2" (the VERIFIED `path_beneath_rules`/`PathFd`/`add_rules` API from the vendored landlock-0.4.5 example — the exact call shape) and Pitfall 1 (deny_all_filesystem reused verbatim blocks the target load).
  </read_first>
  <action>
    Add `#[cfg(target_os="linux")] pub fn exec_child_ruleset(workspace_root:
    &std::path::Path) -> std::io::Result<()>` beside `deny_all_filesystem()`,
    following 32-RESEARCH Pattern 2 exactly: `Ruleset::default()
    .handle_access(AccessFs::from_all(ABI::V3))` (verbatim from deny_all_filesystem),
    `.create()`, then `.add_rules(path_beneath_rules(system_paths.iter(),
    AccessFs::ReadFile | AccessFs::Execute))` and `.add_rules(path_beneath_rules(
    std::iter::once(PathFd::new(workspace_root)?), AccessFs::ReadFile |
    AccessFs::WriteFile))`, then `.restrict_self()`. Hardcode the system allow-list
    as `["/usr","/bin","/lib","/lib64"]` (DESIGN §8 item 1 defers the EXACT strings
    to in-container verification — 32-06 confirms via `ldd`/`which`; leave a doc
    comment marking this an enumerated, explicitly-hardcoded allow-list, never a
    PATH walk, per the "no dynamic registry" discipline). Do NOT grant Execute on
    WorkspaceRoot (never run a worker-planted binary). Map all landlock errors to
    `std::io::Error` as the existing function does. Add a
    `#[cfg(not(target_os="linux"))] pub fn exec_child_ruleset(_: &std::path::Path)
    -> std::io::Result<()> { Ok(()) }` no-op stub. Re-export from lib.rs alongside
    `deny_all_filesystem`.
  </action>
  <verify>
    <automated>cargo build -p sandbox && cargo build -p sandbox --tests</automated>
  </verify>
  <done>
    `exec_child_ruleset` compiles on Mac (stub) and links the landlock API on
    Linux (the container compile-check in 32-06 confirms the Linux path). It grants
    Execute only on the enumerated system paths and ReadFile+WriteFile only on
    WorkspaceRoot; it is distinct from `deny_all_filesystem` (which is unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 2: exec_child_filter() — seccomp net-deny, no execve-deny</name>
  <files>crates/sandbox/src/seccomp.rs, crates/sandbox/src/lib.rs</files>
  <read_first>
    - crates/sandbox/src/seccomp.rs (full — `apply_worker_filter()` at 54-112; the socket(AF_INET/AF_INET6) deny block at 68-93; the NO_NEW_PRIVS note at 20-30).
    - DESIGN §1.4 "seccomp — reused network-deny, no execve-deny" and the B1 resolution (a stateless BPF cannot allow-first-then-deny execve; grandchild egress is bounded by the persistent net-deny + Landlock Execute allow-list, NOT a seccomp recursion-deny).
    - 32-RESEARCH.md "Pattern 3" (the exact filter — same socket rules, the two SYS_execve/SYS_execveat entries dropped).
  </read_first>
  <action>
    Add `#[cfg(target_os="linux")] pub fn exec_child_filter() -> std::io::Result<()>`
    beside `apply_worker_filter()`. Copy `apply_worker_filter`'s body EXACTLY,
    then remove ONLY the two `(libc::SYS_execve, vec![])` and
    `(libc::SYS_execveat, vec![])` deny entries — keep the `(libc::SYS_socket,
    vec![af_inet_rule, af_inet6_rule])` block byte-for-byte, the same
    `SeccompAction::Allow` mismatch / `Errno(EPERM)` match actions, the same
    `try_into` BpfProgram conversion, and the same `seccompiler::apply_filter`
    (which still sets NO_NEW_PRIVS internally). Document at the function head WHY
    execve is NOT denied (the launcher must perform its one legitimate execve of
    the target; recursion-deny is unrealizable with a stateless BPF — DESIGN §1.4
    B1) and that the socket deny persists across that execve (DESIGN §9 A5, the
    single load-bearing kernel-semantics assumption, empirically confirmed by the
    32-06 confinement negative test). Add a `#[cfg(not(target_os="linux"))]` no-op
    stub. Re-export from lib.rs alongside `apply_worker_filter`.
  </action>
  <verify>
    <automated>cargo build -p sandbox && cargo build -p sandbox --tests</automated>
  </verify>
  <done>
    `exec_child_filter` compiles; its socket-deny block is identical to
    `apply_worker_filter`'s and it contains NO SYS_execve/SYS_execveat deny entry;
    `apply_worker_filter` is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher process → kernel | Confinement primitives applied to self before executing arbitrary target code. |
| exec child → filesystem / network | The confined child (and any grandchild) attempts fs/network access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Information Disclosure | exec child reads ~/.ssh or paths outside WorkspaceRoot | critical | mitigate | exec_child_ruleset allows ReadFile only on enumerated system paths + WorkspaceRoot; everything else denied (verified negative test 32-06) |
| T-32-06 | Information Disclosure | network exfiltration from the child or a grandchild | critical | mitigate | exec_child_filter reuses socket(AF_INET/AF_INET6) deny; persists across execve (A5) |
| T-32-07 | Elevation of Privilege | grandchild execve of an un-enumerated binary | high | mitigate | Landlock Execute granted ONLY on enumerated system paths; no arbitrary binary is runnable (no seccomp recursion-deny needed — B1) |
| T-32-08 | Tampering | reusing deny_all_filesystem() verbatim blocks the target load (fail-open temptation to loosen) | medium | mitigate | distinct exec_child_ruleset with explicit Execute allow — never loosen deny_all_filesystem itself |
</threat_model>

<verification>
- `cargo build -p sandbox` and `cargo build -p sandbox --tests` green on Mac (Linux
  bodies are cfg-gated; the container compile-check in 32-06 exercises them).
- `deny_all_filesystem` and `apply_worker_filter` diffs are empty (grep) — the worker
  path is untouched.
</verification>

<success_criteria>
Two new confinement constructors exist that (a) let an arbitrary target binary
load and run under a narrow Landlock allow-list and (b) deny network egress
without denying the child's own one execve — ready for the launcher (32-03) to
apply to itself post-fork.
</success_criteria>

<output>
Create `.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-02-SUMMARY.md` when done
</output>
