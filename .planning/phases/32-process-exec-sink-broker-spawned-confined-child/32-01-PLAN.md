---
phase: 32-process-exec-sink-broker-spawned-confined-child
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/plan_node.rs
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04]

must_haves:
  truths:
    - "`process.exec` is a callable sink: its arg set {command,args,cwd} passes the Step-0 schema gate; command is required; an unknown/duplicate/missing arg Denies at Step 0."
    - "A tainted `command` or `args` value Blocks (routing- AND content-sensitive); `command`/`args` carry expected_role=None so a legitimate command is not fail-closed-Denied at Step 1c."
    - "`process.exec` is CommitIrreversible (a Draft session cannot invoke it)."
    - "`TaintLabel::ExecRaw` exists and `is_untrusted()` returns true for it (compiler-enforced exhaustive match)."
  artifacts:
    - crates/runtime-core/src/plan_node.rs
    - crates/executor/src/sink_schema.rs
    - crates/executor/src/sink_sensitivity.rs
  key_links:
    - "`is_untrusted()` exhaustive match includes ExecRaw — a missed arm is a compile error, never a silent false-allow."
    - "`command`/`args` appear in BOTH PROCESS_EXEC_ROUTING_SENSITIVE and PROCESS_EXEC_CONTENT_SENSITIVE so neither classifier returns false for them."
---

<objective>
Lay the foundational, purely-additive types for the `process.exec` sink: the
`TaintLabel::ExecRaw` variant (runtime-core) and the executor sink-table entries
(schema + sensitivity + expected_role) that make `process.exec` a fail-closed,
I2-governed sink — with **zero new enforcement logic** (table entries only, per
DESIGN §4.1).

Purpose: Everything downstream (the mint, the broker dispatch, the acceptance
test) needs these types to exist and compile first. This plan is additive — it
introduces no breaking protocol change and no runtime behavior beyond the new
table rows.
Output: `TaintLabel::ExecRaw`; the `KNOWN_SINKS` `process.exec` row; the
`process.exec` arms in `sink_effect_class` / `is_routing_sensitive` /
`is_content_sensitive` / `expected_role`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Consolidated register of every NEW symbol/file across Phase 32 (for cross-plan
reference; this plan produces the first three):
- `TaintLabel::ExecRaw` (this plan) — runtime-core taint variant, untrusted.
- `process.exec` sink id + `KNOWN_SINKS` row + sensitivity/expected_role arms (this plan).
- `sandbox::landlock::exec_child_ruleset` / `sandbox::seccomp::exec_child_filter` (32-02).
- `caprun-exec-launcher` binary crate (32-03).
- `sinks::process_exec::invoke_process_exec` + `process_exited`/`process_spawn_failed` Events (32-04).
- `quarantine::mint_from_exec` + Gate-3 line + `BrokerResponse::PlanNodeDecision.output_value_id` (32-05).
- EXEC-03 acceptance + exec-child confinement negative tests (32-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TaintLabel::ExecRaw and place it in the untrusted arm</name>
  <files>crates/runtime-core/src/plan_node.rs</files>
  <read_first>
    - crates/runtime-core/src/plan_node.rs lines 11-51 (the `TaintLabel` enum + `is_untrusted()` exhaustive-match invariant, no wildcard arm).
    - DESIGN §2.2 (the ExecRaw label + origin_role decision); DESIGN §11 n1 (adding ExecRaw forces updating EVERY non-wildcard TaintLabel match, not only is_untrusted — compiler-caught).
    - 32-RESEARCH.md "File-by-file" row for plan_node.rs and Pitfall/n1 note.
  </read_first>
  <behavior>
    - `TaintLabel::ExecRaw` is a new enum variant (placed after `PathRaw`, mirroring the PathRaw/EmailRaw/PdfRaw naming convention).
    - `is_untrusted()` returns `true` for `ExecRaw` (it joins the untrusted arm alongside ExternalUntrusted/EmailRaw/PdfRaw/LlmGenerated/WorkerExtracted/PathRaw).
    - The enum still compiles with no wildcard arm anywhere — every non-wildcard `match` over `TaintLabel` in the whole tree gains an `ExecRaw` arm.
  </behavior>
  <action>
    Add `ExecRaw` to the `TaintLabel` enum in plan_node.rs (after `PathRaw`), with
    a doc line describing it as raw stdout/stderr captured from a broker-spawned
    confined exec child (untrusted origin). Add `TaintLabel::ExecRaw` to the
    untrusted arm of `is_untrusted()` (the `| TaintLabel::PathRaw => true` group)
    and update that method's doc comment to reference seven untrusted labels.
    Then run `cargo build --workspace` and add an `ExecRaw` arm to EVERY OTHER
    non-wildcard match over `TaintLabel` the compiler flags (DESIGN §11 n1 — grep
    `TaintLabel::` across crates/ and cli/ to anticipate; the compiler is
    authoritative). Do NOT introduce a wildcard `_` arm to silence them — each
    must be an explicit arm (the whole point of the invariant at plan_node.rs:34-39).
    ExecRaw is always minted paired with ExternalUntrusted downstream (32-05), so
    treat it exactly like the other untrusted origin labels wherever matched.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p runtime-core</automated>
  </verify>
  <done>
    `TaintLabel::ExecRaw` exists; `is_untrusted(&ExecRaw)` is true; the workspace
    compiles with no wildcard arm added to any TaintLabel match; `cargo test -p
    runtime-core` green (add a unit assertion that `TaintLabel::ExecRaw.is_untrusted()`
    is true if runtime-core has an inline test module for this enum).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register process.exec in the schema + sensitivity + expected_role tables</name>
  <files>crates/executor/src/sink_schema.rs, crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_schema.rs lines 27-58 (`SinkSchema`, `KNOWN_SINKS`, the `file.create` row shape).
    - crates/executor/src/sink_sensitivity.rs lines 40-181 (sink_effect_class, the ROUTING/CONTENT consts, is_routing_sensitive, is_content_sensitive, expected_role, and the file.create arms) and the inline test module 183-377.
    - DESIGN §4.2 (command/args classified BOTH routing- AND content-sensitive; expected_role=None for command/args — Round-1 M2; cwd routing-sensitive, not content-sensitive), DESIGN §5 fail-closed table.
    - 32-RESEARCH.md "Code Examples" (exact KNOWN_SINKS row + sensitivity arms + the expected_role arm with the cwd `[ASSUMED]` note) and Pitfall (the "exec.shell" fixture at sink_schema.rs:193-198 is a permanently-rejected UnknownSink test string — do NOT collide with it; the real id is `process.exec`).
  </read_first>
  <behavior>
    - `validate_schema` accepts `process.exec` with args subset of {command,args,cwd}, requires `command`, and Denies an unknown/duplicate/missing arg at Step 0.
    - `sink_effect_class(process.exec)` == CommitIrreversible.
    - `is_routing_sensitive(process.exec, x)` is true for command, args, cwd.
    - `is_content_sensitive(process.exec, x)` is true for command, args; false for cwd.
    - `expected_role(process.exec, "command")` and `(…, "args")` are None; `(…, "cwd")` is Some(&["path","relative_path"]).
  </behavior>
  <action>
    In sink_schema.rs, add a `SinkSchema { sink: "process.exec", allowed:
    &["command","args","cwd"], required: &["command"] }` row to `KNOWN_SINKS`
    (mirror the file.create row at 53-57). In sink_sensitivity.rs add two consts
    `PROCESS_EXEC_ROUTING_SENSITIVE = &["command","args","cwd"]` and
    `PROCESS_EXEC_CONTENT_SENSITIVE = &["command","args"]`, and add a
    `"process.exec"` arm to each of: `sink_effect_class` (=> CommitIrreversible),
    `is_routing_sensitive` (=> PROCESS_EXEC_ROUTING_SENSITIVE.contains), and
    `is_content_sensitive` (=> PROCESS_EXEC_CONTENT_SENSITIVE.contains). Add a
    `"process.exec"` arm to `expected_role`: `command | args => None` (per DESIGN
    §4.2/M2 — no origin_role-producing mint site for a trusted-authored command;
    Some(...) would fail-closed-Deny the legitimate command at Step 1c; the Block
    comes from routing+content sensitivity + the untrusted-taint check, not this
    gate), `cwd => Some(&["path","relative_path"])` (reuse file.create's path role
    vocabulary — DESIGN pins cwd routing-sensitive but not its role list; this is
    the RESEARCH A3 recommendation), `_ => None`. Add inline `#[cfg(test)]`
    assertions mirroring the existing file.create tests: process_exec present in
    KNOWN_SINKS with the right allowed/required sets; command & args each
    routing+content sensitive; cwd routing- but not content-sensitive; effect_class
    CommitIrreversible; expected_role None for command/args and Some(path list) for
    cwd.
  </action>
  <verify>
    <automated>cargo test -p executor sink_schema && cargo test -p executor sink_sensitivity</automated>
  </verify>
  <done>
    Both crates compile; the new inline unit tests pass; a plan node with sink
    `process.exec` and args exactly {command} (or {command,args,cwd}) passes
    validate_schema, and one with an unknown arg or missing `command` Denies at
    Step 0. `command`/`args` classify routing+content-sensitive with
    expected_role None; cwd routing-sensitive with expected_role Some(&["path","relative_path"]).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → executor sink tables | Untrusted plan-node arg names + taint cross into the schema/sensitivity gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Elevation of Privilege | tainted `command`/`args` reaching a spawn | critical | mitigate | command & args in BOTH PROCESS_EXEC_ROUTING_SENSITIVE and PROCESS_EXEC_CONTENT_SENSITIVE → tainted value Blocks at Step 2/3 before any spawn (32-04/05) |
| T-32-02 | Tampering | unknown/duplicate/missing exec arg smuggled to the sink | high | mitigate | fail-closed `KNOWN_SINKS` exact-match schema; Deny at Step 0 before resolve/sensitivity |
| T-32-03 | Spoofing | new untrusted label silently treated as trusted | high | mitigate | `is_untrusted()` exhaustive match (no wildcard) — ExecRaw omission is a compile error |
| T-32-04 | Elevation of Privilege | legitimate command fail-closed-Denied, then loosened by a maintainer | medium | mitigate | expected_role=None for command/args is documented (DESIGN §4.2/M2) so the Block source stays the sensitivity+taint check, never a loosened role gate |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | No new external package this phase — only a tokio feature flag (32-04) on already-audited tokio; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `cargo build --workspace` compiles with no wildcard TaintLabel arm anywhere.
- `cargo test -p runtime-core` and `cargo test -p executor sink_schema sink_sensitivity` green on Mac.
- No new enforcement logic added to `submit_plan_node` — grep confirms only table
  rows/arms changed in executor (DESIGN §4.1: table entries only).
</verification>

<success_criteria>
`TaintLabel::ExecRaw` (untrusted) exists; `process.exec` is a fail-closed,
CommitIrreversible, I2-governed sink whose command/args Block on taint and whose
schema Denies malformed arg sets — all via table entries only, zero new mechanism.
</success_criteria>

<output>
Create `.planning/phases/32-process-exec-sink-broker-spawned-confined-child/32-01-SUMMARY.md` when done
</output>
