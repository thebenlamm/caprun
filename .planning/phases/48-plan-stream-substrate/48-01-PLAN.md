---
phase: 48-plan-stream-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/src/planner.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/planner.rs
  - crates/brokerd/tests/stream_multi_submit.rs
autonomous: true
requirements:
  - STREAM-01
  - STREAM-02

must_haves:
  truths:
    - "In one Session on one worker connection, N sequential SubmitPlanNode calls each run independent policy-pre-I2 then I2 (STREAM-01; DESIGN §1.1)."
    - "Every multi-node decision/event lands on the same audit DAG with verify_chain true for that Session (STREAM-01 chain-head continuity)."
    - "Worker handle bag stores only opaque ValueIds; any Some(output_value_id) is inserted regardless of sink id (STREAM-02; DESIGN §2.2 F-01)."
    - "Planner multi-node surface places offered handles only — never literals, taint labels, or ValueRecords; task_instruction stays Option String not ValueId (STREAM-02; PLAN-03; DESIGN §5)."
    - "ProvideIntent runs exactly once before RequestFd; a second ProvideIntent mid-stream is rejected by broker (STREAM-02; DESIGN §2.3)."
    - "Empty multi-node stream (submitted == 0) fails closed — not exit 0 (DESIGN §8.2 empty probe)."
    - "Sequential submit order only; one Session occupancy on the product multi-step path (DESIGN §6.3 concurrency probe)."
    - "One-shot email/file DeterministicPlanner and LlmPlanner plan() paths remain green via default plan_next one-shot adapter (Pitfall 6)."
    - "Zero new crates; Gate 3 mint allowlist unchanged; check-invariants Gates 1+3 green (HYG-02)."
  artifacts:
    - path: cli/caprun/src/planner.rs
      provides: "Additive plan_next + PlanStreamContext (opaque handles + step_index); default one-shot adapter"
      contains: "plan_next"
    - path: cli/caprun/src/worker.rs
      provides: "Sequential submit loop + opaque handle bag + decision branch table"
      contains: "plan_next"
    - path: cli/caprun/tests/planner.rs
      provides: "Unit proofs for plan_next one-shot adapter + multi-node test planner bag placement"
    - path: crates/brokerd/tests/stream_multi_submit.rs
      provides: "Linux same-connection N× SubmitPlanNode + verify_chain + ProvideIntent-once mid-stream"
  key_links:
    - "worker plan_next → SubmitPlanNode × N → PlanNodeDecision → bag.insert(any Some(output_value_id)) → next plan_next offered handles"
    - "broker handle_connection loop already multi-submit; stream_multi_submit proves DIFFERENT nodes + verify_chain on same session_id"
    - "ProvideIntent once at stream start; loop body never sends ProvideIntent"
  assumptions:
    - "Static step_index / plan_next with default one-shot adapter is the additive API (DESIGN §1.3; RESEARCH A1)."
    - "PlanStreamContext stays cli-local in Phase 48 (RESEARCH Open Q4 / A2 — no CaprunIntent coding variant)."
    - "Substrate stop-on-Block (exit 1, no re-submit) is acceptable; full stay-connected hold is Phase 50 (DESIGN §3; RESEARCH A3)."
    - "No new mint site required; consume existing output_value_id only (RESEARCH A4 hard stop)."
---

<objective>
Deliver the **plan-stream substrate tracer**: one production-quality end-to-end path where an additive multi-node Planner surface plus a worker sequential submit loop with an opaque ValueId handle bag evaluates N nodes on one Session/connection, stores any Some(output_value_id), and proves chain-head continuity with verify_chain — without batch authorize, without coding CaprunIntent (Phase 49), and without full CLI multi-node productization (Phase 50).

Purpose: Close STREAM-01/02 composition gap (worker was one-shot and discarded output_value_id). Broker multi-submit is already legal; this plan makes the worker loop + bag + planner surface real and proves them.

Output: plan_next/PlanStreamContext; worker sequential loop + bag; planner unit tests; brokerd stream_multi_submit integration test.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.claude/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-plan-stream-substrate/48-RESEARCH.md
@.planning/phases/48-plan-stream-substrate/48-PATTERNS.md
@.planning/phases/48-plan-stream-substrate/48-VALIDATION.md
@planning-docs/DESIGN-multi-step-plan-stream.md
@planning-docs/DESIGN-GATE-RECORD-v1.10.md
@CLAUDE.md
@cli/caprun/src/planner.rs
@cli/caprun/src/worker.rs
@cli/caprun/tests/planner.rs
@crates/brokerd/tests/replay_cas.rs
@crates/runtime-core/src/plan_node.rs
</context>

<artifacts_this_phase_produces>
Phase 48 overall produces:

1. **`cli/caprun/src/planner.rs`** — additive multi-node Planner surface (`PlanStreamContext`, `plan_next`) keeping one-shot `plan()`.
2. **`cli/caprun/src/worker.rs`** — sequential N× SubmitPlanNode loop + opaque handle bag + decision branch table (Allowed bag+continue; Deny abort remaining; Block stop without re-submit).
3. **`cli/caprun/tests/planner.rs`** — plan_next one-shot adapter + multi-node bag placement unit tests.
4. **`crates/brokerd/tests/stream_multi_submit.rs`** — same-connection N sequential DIFFERENT nodes + verify_chain + ProvideIntent-once under multi-submit (STREAM-01).
5. **`cli/caprun/tests/stream_substrate.rs`** (Plan 02) — deny-abort remaining, Block no re-submit, Linux taint-via-bag leg (STREAM-02).
6. **F-01 docs drift fixes** in worker/proto/server comments (Plan 02).
7. **`.planning/phases/48-plan-stream-substrate/COVERAGE.md`** — no external API (Plan 02).

This plan (48-01) owns items 1–4 (tracer spine).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end plan stream — plan_next + bag + sequential worker loop</name>
  <reversibility rating="costly">Additive plan_next / PlanStreamContext becomes the Phase 49–50 multi-node seam; wrong PLAN-03 shape forces coordinated worker+planner+coding-recipe rework (not a one-way wire format).</reversibility>
  <files>cli/caprun/src/planner.rs, cli/caprun/src/worker.rs, cli/caprun/tests/planner.rs</files>
  <read_first>
    - planning-docs/DESIGN-multi-step-plan-stream.md §1–§2, §5–§6, §8.2 — sequential stream, handle bag (any Some), ProvideIntent once, PLAN-03, branch table, empty-stream reject
    - planning-docs/DESIGN-GATE-RECORD-v1.10.md — F-01 (bag any Some), F-02 (post-confirm out-of-bag / no re-submit), re-run triggers
    - .planning/phases/48-plan-stream-substrate/48-RESEARCH.md — Pattern 1–4, Minimal Tracer T2/T5/T6, recommended plan_next shape, Architectural Responsibility Map
    - .planning/phases/48-plan-stream-substrate/48-PATTERNS.md — worker one-shot gap lines, planner trait seam, bag seed keys, decision branch table
    - cli/caprun/src/planner.rs — full Planner trait, DeterministicPlanner, plan_from_intent, PLAN-03 module docs, LlmPlanner impl of plan()
    - cli/caprun/src/worker.rs — ProvideIntent→claims→plan→SubmitPlanNode→discard output_value_id tail (≈361–414), send_framed/recv_framed, confinement order docs
    - cli/caprun/tests/planner.rs — #[path] include pattern, arg() helper, existing plan_from_intent cases
    - crates/runtime-core/src/plan_node.rs — ValueId Hash+Eq, PlanArg, PlanNode
  </read_first>
  <behavior>
    - plan_next at step_index 0 with default adapter returns Some(node) matching DeterministicPlanner::plan for email and file intents
    - plan_next at step_index ≥ 1 with default adapter returns None (one-shot)
    - Test-only multi-node planner: step 0 emits node_a; step 1 emits node_b whose PlanArg value_id equals a bag handle offered in PlanStreamContext; step ≥ 2 returns None
    - Handle bag type admits only ValueId values (opaque); no String/literal payload field on the bag
    - After Allowed with Some(output_value_id), bag contains that id under a stable key (e.g. out_{step}) regardless of sink id — insert path must not filter by process.exec only (F-01)
    - After Allowed with None, bag is unchanged and loop continues
    - Empty stream: if plan_next returns None before any successful submit, worker fails closed (non-zero / bail), not success
    - Existing plan_from_intent_* unit tests remain green (email/file regression)
  </behavior>
  <action>
    Implement the STREAM substrate tracer end-to-end on the production worker/planner path (DESIGN §1.2–§2.2, STREAM-01/02).

    **Planner (cli/caprun/src/planner.rs) — additive only:**
    - Add cli-local `PlanStreamContext` with at least: `intent: CaprunIntent`, `step_index: usize`, named-slot handles as `HashMap&lt;String, ValueId&gt;` (or equivalent structured named fields that remain ValueId-only), and `task_instruction: Option&lt;String&gt;` (never ValueId — DESIGN §5).
    - Add `fn plan_next(&amp;self, ctx: &amp;PlanStreamContext) -&gt; Option&lt;PlanNode&gt;` on `Planner` with a **default** one-shot adapter: when `step_index == 0`, pull the same named handles the existing `plan()` signature expects from `ctx.handles` (keys must match worker seed convention: intent, derived_recipient, body, trusted_subject, trusted_body — document the convention in module docs), call `self.plan(...)`, return `Some`; when `step_index &gt; 0`, return `None`. DeterministicPlanner and LlmPlanner inherit the default — do not rewrite their `plan()` bodies.
    - Do **not** add a CaprunIntent coding variant (Phase 49). Do **not** mint, strip taint, or accept ValueRecord.

    **Worker (cli/caprun/src/worker.rs) — replace one-shot tail after planner construction:**
    - Seed an opaque bag (`HashMap&lt;String, ValueId&gt;`) from existing ProvideIntent + claim locals (intent_value_id, derived_recipient, body, trusted_subject_handle, trusted_body_handle) using the documented key convention. Preserve connect → self-confine → ProvideIntent **once** → RequestFd → claims order unchanged.
    - Loop: build `PlanStreamContext { intent, step_index, handles: bag clone/refs, task_instruction }` → `planner.plan_next(&amp;ctx)`. On `None`, break. On `Some(plan_node)`, send `BrokerRequest::SubmitPlanNode { plan_node }` (HARD-03: no session_id field), recv `PlanNodeDecision`.
    - Branch table (DESIGN §6; Phase 48 substrate, not Phase 50 product hold):
      - Allowed: if `output_value_id` is Some, insert into bag under `out_{step}` (or equivalent) for **any** sink — no sink-id filter (F-01). Increment step; continue.
      - BlockedPendingConfirmation: stop fail-closed (exit 1); **do not** re-submit the blocked node; **do not** ProvideIntent again (leave ready for Phase 50 hold).
      - Denied / NotImplemented: abort remaining (exit 1); no further SubmitPlanNode.
    - If zero nodes were submitted when the loop ends, fail closed (bail / non-zero) — DESIGN §8.2.
    - Fix the stale comment that claims output_value_id is process.exec-only at the former discard site so it states any Allowed mint (process.exec / git.commit / http.request) — docs aligned with behavior (F-01 partial; full proto/server comment sweep is Plan 02).
    - Reuse existing `send_framed` / `recv_framed`. No new IPC verbs. No new crates. No new mint sites.

    **Tests (cli/caprun/tests/planner.rs) — write/run first where practical (tdd):**
    - Default plan_next step 0 / step ≥1 cases above.
    - A **test-only** multi-node planner struct (private to tests or cfg-test helper in planner module if needed for trait object) emitting a fixed 2-node sequence that places a bag handle into the second node's PlanArg — proves STREAM-02 placement without CaprunIntent coding.
    - Keep all existing planner unit tests green.

    Reversibility note: plan_next shape is costly-shared; stay within DESIGN pins. If stream shape, confirm-hold, or trusted-arg mint path would need to pivot, stop and re-run adversarial design-trace (GATE-RECORD re-run triggers) — do not silent-pivot DESIGN.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; cargo test -p caprun --test planner -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - planner.rs defines PlanStreamContext and Planner::plan_next with default one-shot adapter
    - worker.rs sequential loop calls plan_next and SubmitPlanNode in a loop; bag stores any Some(output_value_id)
    - planner tests cover default plan_next one-shot + multi-node bag placement
    - empty stream fails closed; Block/Deny paths do not re-submit
    - check-invariants.sh exits 0; cargo test -p caprun --test planner passes
    - no CaprunIntent coding variant; no new crate; no new mint site under cli/ or crates/
  </acceptance_criteria>
  <done>Production worker can evaluate N sequential plan nodes with opaque bag continuity; one-shot planners stay green via default plan_next; unit proofs for STREAM-02 handle placement pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Broker multi-submit + verify_chain + ProvideIntent-once (STREAM-01)</name>
  <files>crates/brokerd/tests/stream_multi_submit.rs</files>
  <read_first>
    - crates/brokerd/tests/replay_cas.rs — spawn_fresh_broker, send_req/read_resp, ProvideIntent once, 2× SubmitPlanNode framing, Linux cfg + mailpit-verify header
    - crates/brokerd/src/server.rs — handle_connection multi-request loop; ProvideIntent-once reject message; evaluate_plan_node_and_record path
    - crates/brokerd/src/proto.rs — SubmitPlanNode, PlanNodeDecision, ProvideIntent
    - .planning/phases/48-plan-stream-substrate/48-RESEARCH.md — Minimal Tracer T1/T4; broker multi-submit harness shape
    - .planning/phases/48-plan-stream-substrate/48-PATTERNS.md — stream_multi_submit analog section
    - brokerd::audit::verify_chain usage in cli/caprun/tests/s9_process_exec_block.rs or durable_anchor.rs
  </read_first>
  <behavior>
    - Same connection/session: ProvideIntent once → SubmitPlanNode(node_1) → SubmitPlanNode(node_2) where node_2 is a DIFFERENT PlanNode than node_1 (not identical CAS replay)
    - Both decisions received as PlanNodeDecision; both evaluations attributed to the same session_id
    - After drop(stream), open audit DB and verify_chain(conn, session_id, key) is true
    - At least two plan-node evaluation / sink-related durable events on that session_id
    - After first ProvideIntent (+ optional first submit), a second ProvideIntent returns BrokerResponse::Error with the existing once-before-RequestFd fail-closed message; no second UserTrusted mint success
    - Tests are Linux-gated (abstract UDS) with mailpit-verify run instructions in the module header when SMTP sinks are used
  </behavior>
  <action>
    Create `crates/brokerd/tests/stream_multi_submit.rs` (STREAM-01 proof at broker layer; hybrid harness OK — frame as substrate, not LIVE-07 CLI DONE).

    Pattern after `replay_cas.rs`: spawn_fresh_broker (in-memory audit DB, SessionPolicy::allow_all, abstract sock), connect once, ProvideIntent **once**, submit **N≥2 DIFFERENT** plan nodes sequentially on that stream.

    **Preferred sink pair for T1 (discretion within DESIGN):** two sequential Allowed `file.create` nodes with distinct trusted path handles if ProvideIntent / mint shape supports it cleanly; otherwise two trusted `email.send` with distinct recipients (requires mailpit-verify) or two trusted process.exec on Linux. The critical asserts are same session_id, independent evaluations, and `verify_chain` true — not coding-recipe sinks.

    When first node returns Some(output_value_id), optionally thread that opaque handle into node_2 as a PlanArg only if the second sink accepts it without inventing a new mint — otherwise keep both nodes independently trusted to isolate STREAM-01 chain continuity (STREAM-02 bag threading is proven in planner units + Plan 02 taint leg).

    Add a dedicated test for mid-stream second ProvideIntent → Error (T4). Do not weaken broker guards.

    Module header: `#![cfg(target_os = "linux")]` and document:
    `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test stream_multi_submit -- --nocapture' bash scripts/mailpit-verify.sh`
    when the chosen sinks can touch SMTP; if sinks are non-SMTP, still prefer mailpit-verify for Linux authority consistency with CLAUDE.md Phase 16+.

    No production server.rs behavior changes required for multi-submit (already legal). Do not add batch IPC. Do not claim CLI multi-step DONE.
  </action>
  <verify>
    <automated>./scripts/check-invariants.sh &amp;&amp; (test "$(uname -s)" = Linux &amp;&amp; cargo test -p brokerd --test stream_multi_submit -- --nocapture || echo "host non-Linux: stream_multi_submit cfg-gated; run via mailpit-verify when Docker available")</automated>
  </verify>
  <acceptance_criteria>
    - crates/brokerd/tests/stream_multi_submit.rs exists with N≥2 sequential different SubmitPlanNode on one connection
    - verify_chain true on the session after multi-submit
    - second ProvideIntent mid-stream rejected
    - check-invariants.sh exits 0
    - Linux: cargo test -p brokerd --test stream_multi_submit passes under mailpit-verify when Docker available
  </acceptance_criteria>
  <done>STREAM-01 chain-head continuity proven at broker multi-submit layer; ProvideIntent-once holds under multi-submit context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker process → broker UDS | Confined worker is sole product egress; framed SubmitPlanNode only; no ambient net/exec after self-confine |
| Planner trait → PlanNode | PLAN-03: opaque ValueIds + typed intent only; instruction channel is String not bindable handle |
| Broker ValueStore → worker bag | Bag is routing table of opaque ids; literals/taint stay broker-owned |
| Session occupancy latch | One worker connection product path; reconnect remint forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering / Elevation | handle bag / output_value_id | high | mitigate | Opaque ValueIds only; store any Some without trust upgrade; per-node I2; planner never mints/strips (DESIGN §2, §9 threat 1) |
| T-48-02 | Elevation | mid-stream ProvideIntent | high | mitigate | Broker once-before-RequestFd guards; worker loop never re-sends ProvideIntent; stream_multi_submit T4 (DESIGN §2.3) |
| T-48-03 | Tampering | batch / multi-node one-shot I2 | high | mitigate | Sequential N× SubmitPlanNode only; no new batch verb; Gate 1 unchanged (DESIGN §1.4) |
| T-48-04 | Tampering | re-submit blocked node | high | mitigate | Block branch exits without re-submit; F-02 post-confirm out-of-bag (substrate leave-ready for Phase 50) |
| T-48-05 | Tampering | instruction→value collapse | medium | mitigate | task_instruction remains Option&lt;String&gt; on PlanStreamContext; never ValueId |
| T-48-06 | Elevation | new mint outside Gate 3 | high | mitigate | Consume existing output_value_id only; check-invariants Gate 3 green; zero new mint sites |
| T-48-07 | Integrity of claims | hybrid sold as CLI DONE | medium | mitigate | Frame tests as STREAM substrate; no LIVE-07 claim in this plan |
| T-48-08 | Repudiation | chain split across nodes | high | mitigate | Same session_id; verify_chain true after N submits |
| T-48-SC | Tampering | package installs | high | accept | No npm/pip/cargo new deps this phase — Package Legitimacy N/A |
</threat_model>

<verification>
- `./scripts/check-invariants.sh` green (Gates 1+3 especially)
- `cargo test -p caprun --test planner -- --nocapture`
- Linux: `MAILPIT_VERIFY_CMD='cargo test -p brokerd --test stream_multi_submit -- --nocapture' bash scripts/mailpit-verify.sh` when Docker available
- Worker production path uses plan_next loop (not one-shot discard)
</verification>

<success_criteria>
- STREAM-01: N sequential SubmitPlanNode same Session/connection with verify_chain true
- STREAM-02: opaque bag + any Some(output_value_id) + planner places handles only + ProvideIntent once
- One-shot email/file planner regression green
- Empty stream fail-closed; sequential order only
- HYG-02: zero new crates / zero new mint sites / invariants green
</success_criteria>

<output>
Create `.planning/phases/48-plan-stream-substrate/48-01-SUMMARY.md` when done
</output>
