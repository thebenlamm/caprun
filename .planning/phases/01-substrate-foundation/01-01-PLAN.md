---
phase: 01-substrate-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - cli/caprun/Cargo.toml
  - cli/caprun/src/main.rs
  - crates/runtime-core/Cargo.toml
  - crates/runtime-core/src/lib.rs
  - crates/runtime-core/src/intent.rs
  - crates/runtime-core/src/session.rs
  - crates/runtime-core/src/effect.rs
  - crates/runtime-core/src/artifact.rs
  - crates/runtime-core/src/event.rs
  - crates/runtime-core/src/plan_node.rs
  - crates/runtime-core/src/executor_decision.rs
  - crates/runtime-core/tests/types_compile.rs
autonomous: true
requirements:
  - REQ-runtime-core
must_haves:
  truths:
    - "`cargo build --workspace` succeeds from a clean checkout"
    - "Intent, Session, Event, Artifact, and the 3-class Effect enum compile in runtime-core with no I/O"
    - "ValueNode carries literal + provenance + taint fields in its type definition from the first commit"
    - "PlanNode, ValueNode, SinkId, TaintLabel, Provenance, and ExecutorDecision::NotImplemented are all defined and publicly re-exported from runtime-core"
  artifacts:
    - "Cargo.toml — virtual workspace manifest (no [package]), resolver = \"3\", pinned [workspace.dependencies]"
    - "crates/runtime-core/src/plan_node.rs — PlanNode, ValueNode, SinkId, TaintLabel, Provenance"
    - "crates/runtime-core/src/executor_decision.rs — ExecutorDecision enum incl. NotImplemented variant"
    - "crates/runtime-core/src/effect.rs — Effect 3-class enum + ObserveEffect/ReversibleEffect/IrreversibleEffect"
    - "crates/runtime-core/tests/types_compile.rs — field-presence + serde round-trip test for ValueNode taint"
    - "cli/caprun/ — minimal binary stub so the explicit workspace member resolves"
  key_links:
    - "runtime-core/src/event.rs Event.taint references plan_node::TaintLabel (same crate, defined before use)"
    - "lib.rs re-exports every public domain type so downstream crates (brokerd, executor) import from runtime-core, never duplicate"
  prohibitions:
    - "No std::io, std::fs, std::net, tokio, or `async fn` anywhere under crates/runtime-core/src/ (no runtime I/O; verified by negative grep)"
---

<objective>
Stand up the single Cargo virtual workspace and implement the `runtime-core`
crate of pure domain types with zero I/O. This is the foundation every later
crate depends on. All types are specified verbatim in 01-RESEARCH.md — there is
no design freedom; the value of this plan is locking the *shape*, especially
ValueNode's literal+provenance+taint triple (a Success Criterion 3 invariant
that becomes a breaking change if added later).

Purpose: Satisfy REQ-runtime-core and Phase 1 Success Criteria 1 and 3.
Output: Compiling workspace + runtime-core crate with all domain types and a
field-presence test for the taint invariant.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-substrate-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Virtual workspace skeleton + caprun stub</name>
  <files>Cargo.toml, .gitignore, crates/runtime-core/Cargo.toml, crates/runtime-core/src/lib.rs, cli/caprun/Cargo.toml, cli/caprun/src/main.rs</files>
  <read_first>
    - 01-RESEARCH.md "Recommended Project Structure", "Pattern 1: Virtual Workspace Manifest", "Code Examples > Workspace Root Cargo.toml", and "Pitfall 4: Non-Virtual Workspace Root"
    - Existing .gitignore at repo root (extend it; do not clobber unrelated entries)
  </read_first>
  <action>
    Create the repo-root `Cargo.toml` as a VIRTUAL manifest only: a `[workspace]`
    table with `members = ["crates/*", "cli/caprun"]` and `resolver = "3"`; a
    `[workspace.package]` table with `version = "0.1.0"`, `edition = "2021"`,
    `license = "MIT OR Apache-2.0"`; and a `[workspace.dependencies]` table
    pinning EXACTLY the verified versions from 01-RESEARCH.md: serde 1.0.228
    (features ["derive"]), serde_json 1.0.150, uuid 1.23.4 (features ["v4","serde"]),
    thiserror 2.0.18, chrono 0.4.45 (features ["serde"]), anyhow 1.0.103. The root
    manifest MUST NOT contain a `[package]` section (per DEC-repo-layout and
    Pitfall 4 — the repo root is the workspace, not a crate).
    Create `crates/runtime-core/Cargo.toml` inheriting workspace deps via
    `{ workspace = true }` for serde, serde_json, uuid, thiserror, chrono, and an
    empty `crates/runtime-core/src/lib.rs` (Task 2/3 fill it).
    Create the `cli/caprun` member as a minimal binary stub (Cargo.toml inheriting
    workspace package keys; `src/main.rs` with an empty `fn main() {}`) so the
    explicit `cli/caprun` workspace member resolves and `cargo build --workspace`
    never fails on a missing member (resolves RESEARCH Open Question 2).
    Append `/target` and `Cargo.lock` handling to .gitignore as appropriate for a
    workspace that will ship binaries (commit Cargo.lock — keep it tracked).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5; grep -c '^\[package\]' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0 (empty runtime-core lib + caprun stub compile)
    - `grep -c '^\[package\]' Cargo.toml` returns 0 (root is a pure virtual manifest)
    - `cargo metadata --no-deps --format-version 1` lists members `runtime-core` and `caprun`
    - All six workspace dependency versions match 01-RESEARCH.md exactly (serde 1.0.228, serde_json 1.0.150, uuid 1.23.4, thiserror 2.0.18, chrono 0.4.45, anyhow 1.0.103)
  </acceptance_criteria>
  <done>Workspace builds clean; root is virtual-manifest-only; caprun + runtime-core members resolve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Locked plan-node, taint, effect, and decision types</name>
  <files>crates/runtime-core/src/plan_node.rs, crates/runtime-core/src/effect.rs, crates/runtime-core/src/executor_decision.rs, crates/runtime-core/src/lib.rs</files>
  <read_first>
    - 01-RESEARCH.md "Pattern 3: The 3-Class Effect Enum", "Pattern 4: ValueNode with Literal + Provenance + Taint", "Pattern 5: submit_plan_node Stub" (the ExecutorDecision enum block), and "Pitfall 3: ValueNode Missing Taint Field"
    - 01-RESEARCH.md "Pitfall 5: serde_json in runtime-core" (serde_json::Value IS acceptable here — no I/O)
  </read_first>
  <behavior>
    - ValueNode struct has three public fields named exactly `literal` (serde_json::Value), `provenance` (Provenance), `taint` (Vec<TaintLabel>) — verified by a test that constructs a ValueNode and serde round-trips it
    - Effect is a 3-variant enum: Observe(ObserveEffect), MutateReversible(ReversibleEffect), CommitIrreversible(IrreversibleEffect) — not a flat list
    - ExecutorDecision has a `NotImplemented` variant plus Allowed, BlockedPendingConfirmation { literal_value, sink, arg_name }, Denied { reason }
  </behavior>
  <action>
    Create `crates/runtime-core/src/plan_node.rs` defining, verbatim per RESEARCH
    Pattern 4: `TaintLabel` enum (UserTrusted, LocalWorkspace, ExternalUntrusted,
    EmailRaw, PdfRaw, LlmGenerated, WorkerExtracted); `Provenance` struct
    (source_event_id: Option<Uuid>, source_artifact_id: Option<Uuid>, description:
    String); `ValueNode` struct with the three fields `literal: serde_json::Value`,
    `provenance: Provenance`, `taint: Vec<TaintLabel>` (all three present from this
    first commit — DEC-architectural-lock-plan-nodes, Success Criterion 3);
    `SinkId(pub String)`; and `PlanNode { sink: SinkId, args: Vec<ValueNode> }`.
    Create `crates/runtime-core/src/effect.rs` with the 3-class `Effect` enum and
    its `ObserveEffect`, `ReversibleEffect`, `IrreversibleEffect` sub-enums exactly
    as in Pattern 3 (grow later by adding sub-enum variants, never new top-level
    classes).
    Create `crates/runtime-core/src/executor_decision.rs` with the `ExecutorDecision`
    enum from Pattern 5, including the `NotImplemented` variant (Plan 02's stub
    returns this — a typed value, never `todo!()`/panic).
    Derive `Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize` on every
    type. Do NOT import or reference std::io / std::fs / std::net / tokio, and do
    NOT write any `async fn` — runtime-core is pure types. Do not name those tokens
    in source comments either (they trip the purity grep gate in Task 3).
    <!-- planner-discipline-allow: std::io -->
    <!-- planner-discipline-allow: std::fs -->
    <!-- planner-discipline-allow: std::net -->
    <!-- planner-discipline-allow: async fn -->
  </action>
  <verify>
    <automated>cargo build -p runtime-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p runtime-core` exits 0 with plan_node.rs, effect.rs, executor_decision.rs declared as modules in lib.rs (declare them in lib.rs as part of this task so the crate compiles)
    - ValueNode has exactly the public fields `literal`, `provenance`, `taint`; removing `taint` would be a compile-breaking change (the invariant is structural)
    - Effect enum has exactly the three variants Observe / MutateReversible / CommitIrreversible
    - ExecutorDecision exposes a `NotImplemented` variant
  </acceptance_criteria>
  <done>The architecturally-locked types compile; ValueNode carries the literal+provenance+taint triple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remaining domain types, lib re-exports, purity gate + tests</name>
  <files>crates/runtime-core/src/intent.rs, crates/runtime-core/src/session.rs, crates/runtime-core/src/artifact.rs, crates/runtime-core/src/event.rs, crates/runtime-core/src/lib.rs, crates/runtime-core/tests/types_compile.rs</files>
  <read_first>
    - 01-RESEARCH.md "Intent Struct", "Session Struct", "Event Struct (Audit DAG Node)", "runtime-core/src/lib.rs Skeleton"
    - 01-RESEARCH.md "Pitfall 1: I/O Leaking into runtime-core" (the negative-grep gate)
    - crates/runtime-core/src/plan_node.rs from Task 2 (Event.taint reuses TaintLabel defined there)
  </read_first>
  <behavior>
    - Intent, Session, Artifact, Event all construct and serde round-trip without panics
    - Event.taint is a Vec<TaintLabel> reusing the type from plan_node (no duplicate TaintLabel definition)
    - A negative-grep test/gate confirms runtime-core/src contains no runtime-I/O tokens
  </behavior>
  <action>
    Create `crates/runtime-core/src/intent.rs` (Intent + IntentStatus), `session.rs`
    (Session + SessionStatus), `artifact.rs` (Artifact + ArtifactRef — derive a
    minimal Artifact/ArtifactRef per the lib.rs skeleton re-export list; use Uuid id,
    String name/type, content_hash String, DateTime<Utc> created_at), and `event.rs`
    (Event with id, parent_id: Option<Uuid>, session_id, actor: String, event_type:
    String, timestamp: DateTime<Utc>, taint: Vec<plan_node::TaintLabel>) exactly per
    the RESEARCH struct blocks. Note Session uses the public name `Session`
    (ExecutionContext is internal-only per DEC-terminology — do not expose it).
    Fill `crates/runtime-core/src/lib.rs` with the module declarations and public
    re-exports from the RESEARCH "lib.rs Skeleton" (intent, session, effect, artifact,
    event, plan_node, executor_decision; re-export Intent/IntentStatus, Session/
    SessionStatus, Effect/ObserveEffect/ReversibleEffect/IrreversibleEffect, Artifact/
    ArtifactRef, Event, PlanNode/ValueNode/SinkId/TaintLabel/Provenance, ExecutorDecision).
    Create `crates/runtime-core/tests/types_compile.rs` with a test that builds a
    ValueNode { literal, provenance, taint }, asserts the taint Vec is preserved
    through a serde_json round-trip, and constructs one Intent, Session, Event, and
    Artifact (compile + smoke coverage of every domain type).
    Keep the crate free of std::io / std::fs / std::net / tokio / `async fn` in both
    code and comments so the purity grep gate returns clean.
    <!-- planner-discipline-allow: std::io -->
    <!-- planner-discipline-allow: std::fs -->
    <!-- planner-discipline-allow: std::net -->
    <!-- planner-discipline-allow: async fn -->
  </action>
  <verify>
    <automated>cargo test -p runtime-core 2>&1 | tail -8 && ! grep -rE "std::io|std::fs|std::net|tokio|async fn" crates/runtime-core/src/</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p runtime-core` passes (types_compile.rs green, taint survives serde round-trip)
    - `grep -rE "std::io|std::fs|std::net|tokio|async fn" crates/runtime-core/src/` returns no matches (exit 1 — proves no runtime I/O; Success Criterion 1)
    - `cargo build --workspace` still exits 0
    - lib.rs publicly re-exports Intent, Session, Effect (3-class), Artifact, Event, PlanNode, ValueNode, SinkId, TaintLabel, Provenance, ExecutorDecision
  </acceptance_criteria>
  <done>All runtime-core domain types compile, are re-exported, tested, and the crate is provably I/O-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → build | Third-party crate sources pulled during `cargo build` (supply chain) |
| (no runtime boundaries in Phase 1) | runtime-core is pure types; no network/fs/process boundary is crossed at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | ValueNode type definition | high | mitigate | ValueNode carries literal+provenance+taint from the first commit; field presence is compile-enforced and serde-round-trip tested. Omitting taint would be a breaking schema change that defeats Phase 4 I2 (Success Criterion 3). |
| T-01-03 | Information Disclosure | runtime-core purity | medium | mitigate | No std::io/std::fs/std::net/tokio/async in crates/runtime-core/src/, enforced by a negative-grep gate (Task 3). Pure types cannot leak data at runtime. |
| T-01-SC | Tampering | crates.io installs (serde/uuid/thiserror/chrono/anyhow/serde_json) | high | mitigate | All six packages slopchecked OK in 01-RESEARCH.md "Package Legitimacy Audit" (no [ASSUMED]/[SUS]/[SLOP]); versions pinned in [workspace.dependencies]; Cargo.lock committed. No human checkpoint required (all verdicts OK). |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test -p runtime-core` green
- `grep -rE "std::io|std::fs|std::net|tokio|async fn" crates/runtime-core/src/` returns nothing
- `grep -c '^\[package\]' Cargo.toml` returns 0
</verification>

<success_criteria>
REQ-runtime-core is satisfied: Intent, Session, Event, Artifact, and the 3-class
Effect enum compile in runtime-core with no I/O (Success Criterion 1), and
ValueNode carries literal + provenance + taint in its type definition (Success
Criterion 3). Workspace builds clean from a fresh checkout.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols and paths (source-grounding exclusion list):

Structs/enums (in crates/runtime-core): Intent, IntentStatus, Session, SessionStatus,
Effect, ObserveEffect, ReversibleEffect, IrreversibleEffect, Artifact, ArtifactRef,
Event, PlanNode, ValueNode, SinkId, TaintLabel, Provenance, ExecutorDecision (incl.
NotImplemented, Allowed, BlockedPendingConfirmation, Denied variants).

New paths: Cargo.toml (workspace), crates/runtime-core/ (Cargo.toml, src/lib.rs,
intent.rs, session.rs, effect.rs, artifact.rs, event.rs, plan_node.rs,
executor_decision.rs, tests/types_compile.rs), cli/caprun/ (Cargo.toml, src/main.rs stub).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-substrate-foundation/01-01-SUMMARY.md` when done.
</output>
