---
phase: 29-sink-path-hardening-replay-cas-contents-slot
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [HARDEN-05]

must_haves:
  truths:
    - "`file.create`'s `contents` arg is content-sensitive (`is_content_sensitive` returns true for it) AND role-checked (`expected_role` returns `Some(&[\"path\"])`) — so a FUTURE tainted `contents` value routes into the I2 collect-then-Block / slot-type path like other sensitive args."
    - "`file.create`'s `path` arg stays routing-sensitive ONLY — it does NOT become content-sensitive (no over-widening)."
    - "The only live `file.create` flow (`s9_live_file_create_clean_allow`) still Allows — no false-positive block — because `contents` today carries the reused trusted `\"path\"`-role literal, which `Some(&[\"path\"])` accepts."
  artifacts:
    - "crates/executor/src/sink_sensitivity.rs: `FILE_CREATE_CONTENT_SENSITIVE` const + `file.create` arm in `is_content_sensitive`"
    - "crates/executor/src/sink_sensitivity.rs: `expected_role` `\"contents\" => Some(&[\"path\"])`"
    - "crates/executor/src/sink_sensitivity.rs: inverted test `file_create_contents_expects_path` + new `path`-not-content-sensitive guard test"
  key_links:
    - "`expected_role(file.create, contents) == Some(&[\"path\"])` accepts the planner's reused trusted `\"path\"`-role `intent_value_id` (planner.rs:208) — the load-bearing pin that keeps the live flow green."
    - "TCB change in the executor (I2 non-bypassable); policy may gate which sinks are callable but never disables this."
---

<objective>
Close HARDEN-05: give `file.create`'s `contents` arg an expected-role + content-sensitivity treatment in the executor TCB (`crates/executor/src/sink_sensitivity.rs`), so a future tainted `contents` value is handled under the same I2/slot-type discipline as other sensitive sink args — without regressing the only live `file.create` flow.

Purpose: `contents` is currently a role-UNCONSTRAINED slot (`None`) — the last open gap from v1.5's slot-type-binding work. This wires it into I2 while remaining a present no-op on the live path (which only ever carries a trusted `"path"`-role literal).

Output: two precise `sink_sensitivity.rs` edits + one inverted unit test + one over-widening guard test. All macOS-runnable; the Linux regression canary (`s9_live_file_create_clean_allow`) confirms no false-positive block.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-security-hardening.md
@.planning/phases/29-sink-path-hardening-replay-cas-contents-slot/29-RESEARCH.md
@.planning/phases/29-sink-path-hardening-replay-cas-contents-slot/29-PATTERNS.md

# Anchors (read the cited spans only):
# crates/executor/src/sink_sensitivity.rs:66,78 (FILE_CREATE_ROUTING_SENSITIVE / EMAIL_SEND_CONTENT_SENSITIVE const shape)
# crates/executor/src/sink_sensitivity.rs:102-107 (is_content_sensitive — add file.create arm)
# crates/executor/src/sink_sensitivity.rs:122-124 (doc-comment: NEVER construct Some(&[]))
# crates/executor/src/sink_sensitivity.rs:147-162 (expected_role — file.create arm, "contents" => None at :157)
# crates/executor/src/sink_sensitivity.rs:307-323 (existing file.create tests incl. file_create_contents_is_unconstrained at :313 — INVERTS)
# planning-docs/DESIGN-security-hardening.md §e (the pinned two edits + load-bearing Some(&["path"]) trace to planner.rs:208)
</context>

<interface_context>
Signatures (unchanged by this plan):
- `pub fn is_content_sensitive(sink: &SinkId, arg_name: &str) -> bool`
- `pub fn expected_role(sink: &SinkId, arg_name: &str) -> Option<&'static [&'static str]>`
Existing `file.create` arms: `is_routing_sensitive` matches `FILE_CREATE_ROUTING_SENSITIVE = &["path"]` (:66); `expected_role`'s `file.create` arm has `"path" => Some(&["path","relative_path"])`, `"contents" => None` (:157), `_ => None`.
Contract (v1.5, load-bearing): `Some(&[])` must NEVER be constructed (a zero-valid-role slot is a design bug). Do NOT implement the lookup as `.unwrap_or(&[])`.
Why `Some(&["path"])` and not a new role name: the planner (`cli/caprun/src/planner.rs:208`) reuses the SAME trusted `"path"`-role `intent_value_id` in BOTH the `path` and `contents` slots; no `"contents"`/`"file_body"` role-producing mint site exists in the codebase. Any list omitting `"path"` hard-`Deny`s the only live flow.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constrain file.create contents to role Some(&["path"]) + content-sensitive (HARDEN-05)</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    crates/executor/src/sink_sensitivity.rs:66,78 (const declaration shape to mirror);
    crates/executor/src/sink_sensitivity.rs:102-107 (`is_content_sensitive` — the `email.send` `.contains()` arm to mirror EXACTLY, not an unconditional `true`);
    crates/executor/src/sink_sensitivity.rs:147-162 (`expected_role` `file.create` arm — flip `"contents" => None` at :157);
    crates/executor/src/sink_sensitivity.rs:307-323 (existing file.create tests; `file_create_contents_is_unconstrained` at :313 INVERTS).
  </read_first>
  <behavior>
    - `is_content_sensitive(file.create, "contents")` == true (NEW).
    - `is_content_sensitive(file.create, "path")` == false (guard — no over-widening).
    - `expected_role(file.create, "contents")` == `Some(&["path"][..])` (was `None`).
    - `expected_role(file.create, "path")` == `Some(&["path","relative_path"])` (UNCHANGED).
    - `is_routing_sensitive(file.create, "path")` == true (UNCHANGED).
  </behavior>
  <action>
    Edit 1 (a): add `pub const FILE_CREATE_CONTENT_SENSITIVE: &[&str] = &["contents"];` mirroring `EMAIL_SEND_CONTENT_SENSITIVE`'s shape (:78), scoped to `&["contents"]` ONLY. Add a `"file.create" => FILE_CREATE_CONTENT_SENSITIVE.contains(&arg_name)` arm to `is_content_sensitive` — mirror the `email.send` `.contains()` arm EXACTLY (Pitfall 5: do NOT write `"file.create" => true`, which would wrongly make `path` content-sensitive).
    Edit 2 (b): in `expected_role`'s `file.create` arm, change `"contents" => None` (:157) to `"contents" => Some(&["path"])`. This exact value is the load-bearing, non-negotiable pin (DESIGN §e, traced to `planner.rs:208`'s reuse of the trusted `"path"`-role handle in the `contents` slot). Do NOT use `.unwrap_or(&[])`; never construct `Some(&[])`.
    Invert the existing test: rename `file_create_contents_is_unconstrained` (:313) to `file_create_contents_expects_path` and change its assertion to `assert_eq!(expected_role(&file_create(), "contents"), Some(&["path"][..]), ...)` with an updated message noting this is HARDEN-05 (the only live production value is the reused trusted `path`-role literal).
    Add a defense-in-depth guard test (Pitfall 5) e.g. `file_create_path_not_content_sensitive`: assert `!is_content_sensitive(&file_create(), "path")` — guards against the over-widening failure mode. Keep the existing `file_create_contents_not_routing_sensitive` / `file_create_path_is_routing_sensitive` pair green (do not touch `is_routing_sensitive`).
    Honesty note (in a comment or the SUMMARY, not overclaimed): this fires on nothing on the live path today (`contents` only carries a `"path"`-role value); it wires I2 to fire the moment a real content-extraction pipeline (D-12 deferred) mints a doc-derived `contents` claim. It is NOT a T2 violation (the value's role genuinely IS `"path"`, now an accepted role for the slot).
  </action>
  <verify>
    <automated>cargo test -p executor sink_sensitivity</automated>
  </verify>
  <acceptance_criteria>
    - `is_content_sensitive(file.create, "contents")` is true; `is_content_sensitive(file.create, "path")` is false (guard test green).
    - `expected_role(file.create, "contents")` == `Some(&["path"][..])`; `expected_role(file.create, "path")` unchanged.
    - `file_create_contents_is_unconstrained` is renamed/inverted to `file_create_contents_expects_path` (the stale assertion is gone, updated deliberately — not left failing).
    - No `Some(&[])` / `.unwrap_or(&[])` introduced; the existing routing-sensitive test pair stays green.
    - `cargo test -p executor sink_sensitivity` passes on macOS.
  </acceptance_criteria>
  <done>`file.create` `contents` is role-checked to `Some(&["path"])` and content-sensitive; `path` is unchanged; the executor unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolved plan-node value → `file.create` `contents` sink arg | A tainted value must not occupy an unconstrained sink slot; it must be subject to the same I2/slot-type discipline as other sensitive args. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-06 | Tampering / Information Disclosure | `file.create` `contents` slot (`sink_sensitivity.rs`, I2 TCB) | medium | mitigate | Add `contents` to `is_content_sensitive` (`FILE_CREATE_CONTENT_SENSITIVE`) + `expected_role` `Some(&["path"])` — routes a future tainted `contents` into the I2 collect-then-Block / SlotTypeMismatch path. Hardcoded in the executor TCB, never a policy file. |
| T-29-07 | (availability/correctness, not STRIDE) | the only live `file.create` flow | high | mitigate | Role list pinned to `Some(&["path"])` (end-to-end-traced to `planner.rs:208`) so `s9_live_file_create_clean_allow` still Allows; regression canary re-run on Linux. |
| T-29-08 | Tampering (over-widening regression) | `path` arg sensitivity | medium | mitigate | `FILE_CREATE_CONTENT_SENSITIVE = &["contents"]` only + `.contains()` arm (not unconditional `true`); dedicated `path`-not-content-sensitive guard test. |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase; no install task, no legitimacy gate needed. |
</threat_model>

<verification>
- `cargo test -p executor sink_sensitivity` — green on macOS (all HARDEN-05 unit behavior, incl. the inverted test + over-widening guard).
- `cargo build --workspace` — compiles clean.
- `./scripts/check-invariants.sh` — architectural invariants hold (I2 stays hardcoded in the executor TCB; no policy-file indirection introduced).
- Regression canary (Linux, cfg(linux) test-blindness warning applies — a green macOS build proves NOTHING here): `MAILPIT_VERIFY_CMD='cargo test -p caprun --test s9_live_block s9_live_file_create_clean_allow' bash scripts/mailpit-verify.sh` — `s9_live_file_create_clean_allow` must still Allow (no false-positive `SlotTypeMismatch`/Deny). Capture true exit before any pipe.
</verification>

<success_criteria>
- `file.create` `contents` is content-sensitive + role-checked to `Some(&["path"])`; `path` unchanged (routing-sensitive only).
- The inverted unit test + over-widening guard test are green.
- `s9_live_file_create_clean_allow` still Allows on real Linux — no regression to the live flow.
- `./scripts/check-invariants.sh` passes.
</success_criteria>

<output>
Create `.planning/phases/29-sink-path-hardening-replay-cas-contents-slot/29-03-SUMMARY.md` when done.
</output>
