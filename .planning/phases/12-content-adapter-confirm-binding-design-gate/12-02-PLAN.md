---
phase: 12-content-adapter-confirm-binding-design-gate
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - planning-docs/DESIGN-confirm-binding.md
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - "A DESIGN doc defines confirm-binding: caprun confirm binds to ONE combined SHA-256 hash over the FULL SET of blocked args' resolved literals (recipient AND body together), computed over post-transformation bytes (CONFIRM-03, D-08/D-19)."
    - "The doc forbids any transform between ValueRecord mint and Block, and between Block and adapter send — the confirmed bytes provably equal the sent bytes (D-08, Pitfall 2)."
    - "The doc forbids truncated display of long bodies at confirm time — the human sees the verbatim literal (D-09)."
    - "The doc extends the existing PendingConfirmation resolved_args store rather than replacing it, and mandates block narration for EVERY blocked arg in the set (D-10, D-20)."
  artifacts:
    - planning-docs/DESIGN-confirm-binding.md
  key_links:
    - "The combined-digest binding (D-19) must cover exactly the blocked-arg set defined by DESIGN-content-adapter-mediation.md's collect-then-Block section (D-14) — the two docs must agree on the set shape."
    - "Extends crates/brokerd/src/confirmation.rs PendingConfirmation.resolved_args (v1.2) — do not replace it."
---

<objective>
Author `planning-docs/DESIGN-confirm-binding.md`: the DESIGN doc extending v1.2's `DESIGN-confirmation-release.md` / `PendingConfirmation` mechanism with confirm-binding to the exact resolved literals of the FULL blocked-arg set (CONFIRM-03, D-08/D-09/D-10/D-19), plus the block-narration requirement covering every blocked arg (CONFIRM-04, D-20).

Purpose: Second half of the DESIGN-01 design gate. This doc binds `caprun confirm` to a single combined hash over the recipient+body literals TOGETHER — the only shape that satisfies D-02 (both surface as Blocked) and D-08 (hash of resolved recipient+body) simultaneously without a second confirm round-trip — and closes the pre-vs-post-transformation-bytes hazard (D-12b / Pitfall 2). No executor/TCB/brokerd code for CONFIRM-03 may be written until this doc plus DESIGN-content-adapter-mediation.md is adversarially reviewed and the gate record shows APPROVED/UNBLOCKED.

Output: `planning-docs/DESIGN-confirm-binding.md`.

## Artifacts this phase produces
- `planning-docs/DESIGN-content-adapter-mediation.md` (plan 01)
- `planning-docs/DESIGN-confirm-binding.md` (this plan)
- `planning-docs/DESIGN-GATE-RECORD-v1.3.md` (plan 03)
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-RESEARCH.md
@.planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md
@CLAUDE.md

# The v1.2 doc this one extends, plus its backing mechanism
@planning-docs/DESIGN-confirmation-release.md
@crates/brokerd/src/confirmation.rs

# The companion doc that defines the blocked-arg set this binding covers
@planning-docs/DESIGN-content-adapter-mediation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm-binding + combined-digest + block-narration sections</name>
  <files>planning-docs/DESIGN-confirm-binding.md</files>
  <read_first>
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-CONTEXT.md (D-08, D-09, D-10, D-19, D-20)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-RESEARCH.md (Pitfall 2 hash-over-pre-transformation-bytes; SHA-256 digest reuse; Confirm-MUST-NOT-re-invoke pattern)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md (Schema-extension pattern; MUST-NOT-re-invoke-submit_plan_node section pattern; Done-When pattern)
    - planning-docs/DESIGN-confirmation-release.md (PendingConfirmation Schema §47; Confirm MUST NOT Re-Invoke §190; Single-Shot §290; Done-When §378)
    - crates/brokerd/src/confirmation.rs (ResolvedArg struct + PendingConfirmation.resolved_args, lines 38-124; the "frozen at Block time" doc-comment convention)
    - planning-docs/DESIGN-content-adapter-mediation.md (the collect-then-Block blocked-arg set this binding must cover)
  </read_first>
  <action>
    Create `planning-docs/DESIGN-confirm-binding.md` with title line `# DESIGN-confirm-binding.md — caprun Confirm-Binding to Resolved Literals (v1.3)` and an opening `## The Problem Being Solved` section framing this as an extension of v1.2's `PendingConfirmation`/`resolved_args` mechanism (D-10), not a replacement. Then author these named sections:

    `## Combined-Digest Binding (CONFIRM-03, D-08/D-19)` — State as MUSTs: `caprun confirm` binds to ONE combined SHA-256 digest covering the FULL SET of blocked args' exact resolved literals — recipient AND body TOGETHER as one set — matching the collect-then-Block blocked-arg set defined in `DESIGN-content-adapter-mediation.md` (reference it by name). This single combined-digest shape is the only one that satisfies D-02 (both args surface as Blocked, confirmable through the single-shot mechanism) and D-08 (hash of resolved recipient+body) simultaneously without a second confirm round-trip. Reuse SHA-256 (matching the existing `literal_sha256` pattern in `crates/executor/src/lib.rs:112-116`) — do NOT introduce a new hash primitive. Describe the schema extension using the PATTERNS.md convention: a combined-digest field alongside `resolved_args: Vec<ResolvedArg>` in `PendingConfirmation` (`crates/brokerd/src/confirmation.rs`), with the "frozen at Block time, never re-derived" doc-comment discipline. Mark any Rust snippet "illustrative shape, not literal code."

    `## Post-Transformation Bytes — No Drift Between Confirm and Send (CONFIRM-03, D-08, Pitfall 2)` — State as MUSTs: the confined-worker extractor mints the `ValueRecord` (via `mint_from_read` or its Phase-15 successor) ONLY AFTER applying any transformation to the raw read bytes (concatenation, base64-decode — the EXTRACT-03 manipulation variants); there MUST be NO transformation step between `ValueRecord` mint and executor Block, and NONE between Block (frozen into `ResolvedArg.literal`) and adapter invocation. Therefore the combined digest, computed over the frozen `ResolvedArg.literal` fields, is guaranteed to equal the exact bytes the adapter transmits — the bytes the human read are provably the bytes sent. This directly answers attack vector D-12(b) (hash over pre- vs post-transformation bytes).

    `## Verbatim Display — No Truncation (CONFIRM-03, D-09)` — State as a MUST that the confirm/deny display shows the verbatim resolved literal for every blocked arg — no summary, no truncation of long bodies. The human MUST see the full bytes the digest attests to.

    `## Block Narration for Every Arg (CONFIRM-04, D-20)` — State as a MUST that the Block moment narrates provenance for EVERY blocked arg in the set — recipient/body to untrusted doc to these bytes to this sink arg, for each — never a bare "Error: blocked" and never only the first-matched arg. This is the human-legibility counterpart to collect-then-Block: if the set carries two tainted args, the human sees both literals + both provenance chains before deciding.

    `## Single-Shot Over the Whole Set — Confirm MUST NOT Re-Invoke submit_plan_node (CONFIRM-03, D-17/D-19)` — Model this section on `DESIGN-confirmation-release.md`'s `## Confirm MUST NOT Re-Invoke submit_plan_node`. State as MUSTs: confirm authorizes the WHOLE (sink, {all blocked args}, combined-digest) set atomically or denies ALL; there is NO partial-set confirm; confirm/deny operates over the frozen `resolved_args` snapshot and MUST NOT re-invoke `submit_plan_node`.

    End with a `## Done-When (Acceptance Predicate)` section: a named, falsifiable list — combined digest over the full set present; post-transform mint rule stated; no-truncation stated; every-arg narration stated; extends (not replaces) PendingConfirmation — matching the analog docs' terminal Acceptance-Predicate discipline. Author every invariant as a hard MUST/MUST NOT.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-confirm-binding.md && grep -qi 'combined.*digest\|combined.*hash' planning-docs/DESIGN-confirm-binding.md && grep -qi 'post-transform\|post-transformation' planning-docs/DESIGN-confirm-binding.md && grep -q 'resolved_args' planning-docs/DESIGN-confirm-binding.md && grep -q 'submit_plan_node' planning-docs/DESIGN-confirm-binding.md && grep -qi 'Done-When\|Acceptance Predicate' planning-docs/DESIGN-confirm-binding.md</automated>
  </verify>
  <acceptance_criteria>
    - File `planning-docs/DESIGN-confirm-binding.md` exists.
    - Contains `## Combined-Digest Binding (CONFIRM-03, D-08/D-19)` stating ONE combined SHA-256 digest over the full recipient+body blocked-arg set, referencing DESIGN-content-adapter-mediation.md's collect-then-Block set and reusing the existing `literal_sha256` SHA-256 pattern (D-08, D-19).
    - Contains `## Post-Transformation Bytes — No Drift Between Confirm and Send (CONFIRM-03, D-08, Pitfall 2)` requiring mint-after-transform and no transform between mint/Block/send (answers D-12b).
    - Contains `## Verbatim Display — No Truncation (CONFIRM-03, D-09)`.
    - Contains `## Block Narration for Every Arg (CONFIRM-04, D-20)`.
    - Contains a `## Single-Shot Over the Whole Set — Confirm MUST NOT Re-Invoke submit_plan_node (CONFIRM-03, D-17/D-19)` section forbidding partial confirm and re-invocation.
    - Contains a terminal `## Done-When (Acceptance Predicate)` section.
    - grep verify passes.
  </acceptance_criteria>
  <done>DESIGN-confirm-binding.md exists with the combined-digest, post-transform, no-truncation, every-arg-narration, and single-shot/no-re-invoke sections plus a Done-When predicate; all cited D-IDs resolved as MUSTs; grep verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Completeness self-check against D-08/09/10/19/20 and cross-doc set agreement</name>
  <files>planning-docs/DESIGN-confirm-binding.md</files>
  <read_first>
    - planning-docs/DESIGN-confirm-binding.md (the full authored doc)
    - planning-docs/DESIGN-content-adapter-mediation.md (verify the blocked-arg set shape referenced here matches the collect-then-Block set defined there)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-VALIDATION.md (grep tokens)
    - .planning/phases/12-content-adapter-confirm-binding-design-gate/12-PATTERNS.md (MUST-density expectation)
  </read_first>
  <action>
    Run the completeness grep checklist over `planning-docs/DESIGN-confirm-binding.md` and repair any gap in-place before committing. The doc MUST resolve every one of D-08, D-09, D-10, D-19, D-20. Confirm the combined-digest binding covers exactly the collect-then-Block blocked-arg set defined in `DESIGN-content-adapter-mediation.md` (the two docs MUST agree on the set shape — if they diverge, fix this doc's reference; do not edit the plan-01 doc here). Confirm no reliance on "should" for the binding rules and that MUST-density is comparable to the analog docs. Do not introduce any executor/TCB/brokerd source file — documentation only.
  </action>
  <verify>
    <automated>for d in D-08 D-09 D-10 D-19 D-20; do grep -q "$d" planning-docs/DESIGN-confirm-binding.md || { echo "MISSING $d"; exit 1; }; done; grep -q 'DESIGN-content-adapter-mediation' planning-docs/DESIGN-confirm-binding.md && test "$(grep -c 'MUST' planning-docs/DESIGN-confirm-binding.md)" -ge 15 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every D-ID in {D-08, D-09, D-10, D-19, D-20} appears verbatim in the doc (the grep loop exits 0).
    - The doc references `DESIGN-content-adapter-mediation.md` and its combined-digest binding covers the same recipient+body set defined by that doc's collect-then-Block section.
    - `grep -c 'MUST'` on the doc is >= 15.
    - No new file exists under `crates/` (documentation-only phase preserved).
  </acceptance_criteria>
  <done>All 5 cited D-IDs present, cross-doc set agreement confirmed, MUST-density >= 15, no source code added; both DESIGN docs are ready for the plan-03 adversarial review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author session → design gate | The session authoring this DESIGN doc is untrusted to also approve it (D-11); soundness is established only by plan 03's fresh-context adversarial review. |
| (design-time, forward-looking) confirm-time bytes → send-time bytes | The doc specifies that the digest-attested resolved literals provably equal the bytes the adapter transmits (no drift). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | confirm-binding hash scope | critical | mitigate | Doc mandates ONE combined digest over the FULL blocked-arg set (D-19), so confirming a subset can never silently release an unconfirmed tainted arg. |
| T-12-06 | Tampering | confirm-time vs send-time byte drift (TOCTOU via EXTRACT-03 transforms) | high | mitigate | Doc requires mint-after-transform and forbids any transform between mint/Block/send (D-08, Pitfall 2) — answers D-12(b). |
| T-12-07 | Information Disclosure / legibility failure | truncated or single-arg block narration | medium | mitigate | Doc forbids truncation (D-09) and mandates provenance narration for every blocked arg (D-20). |
| T-12-08 | Repudiation | doc authored and self-approved by one session | high | transfer | Approval deferred to plan 03's fresh-context adversarial review arranged by caprun-opus-77 (D-11). |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this documentation-only phase. |
</threat_model>

<verification>
- `planning-docs/DESIGN-confirm-binding.md` exists and resolves every cited D-ID (D-08/09/10/19/20) as a hard MUST/MUST NOT.
- The combined-digest binding references and agrees with the collect-then-Block set in `DESIGN-content-adapter-mediation.md`.
- No source file under `crates/` was created or modified (documentation-only phase preserved).
- Both task-level `<automated>` greps pass.
</verification>

<success_criteria>
The confirm-binding DESIGN doc exists with explicit, greppable sections mandating a single combined SHA-256 digest over the full recipient+body blocked-arg set (D-19), computed over post-transformation bytes with no confirm→send drift (D-08), verbatim non-truncated display (D-09), every-arg block narration (D-20), and extension of (not replacement of) the existing PendingConfirmation mechanism (D-10) — ready for the plan-03 adversarial review.
</success_criteria>

<output>
Create `.planning/phases/12-content-adapter-confirm-binding-design-gate/12-02-SUMMARY.md` when done.
</output>
