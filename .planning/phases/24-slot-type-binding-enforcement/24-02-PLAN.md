---
phase: 24-slot-type-binding-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/executor_decision.rs
autonomous: true
requirements: [T2-04]

must_haves:
  truths:
    - "A new exhaustive DenyReason variant SlotTypeMismatch exists with no wildcard arm (SC3)"
    - "Both exhaustive matches over DenyReason (code() and Display) cover SlotTypeMismatch; the workspace compiles (a missed match is a compile error, not a runtime bug) (SC3)"
    - "The variant's fields are owned (Vec<String> / Option<String>), serde-Deserializable across the IPC wire (SC3)"
  artifacts:
    - "crates/runtime-core/src/executor_decision.rs — DenyReason::SlotTypeMismatch { sink: String, arg: String, expected: Vec<String>, found: Option<String> }"
    - "crates/runtime-core/src/executor_decision.rs — new arm in DenyReason::code() returning \"slot_type_mismatch\""
    - "crates/runtime-core/src/executor_decision.rs — new arm in Display for DenyReason"
  key_links:
    - "Field types MUST be owned (Vec<String>/Option<String>), NEVER &'static — DenyReason derives Deserialize and crosses the IPC wire; &'static refs are not deserializable (DESIGN F1)"
    - "Reuse the existing ExecutorDecision::Denied { reason } carrier — do NOT add a new ExecutorDecision variant (the outer enum's only non-test match at server.rs:671 has a wildcard _ arm; A3 discharged)"
---

<objective>
Add the new exhaustive `DenyReason::SlotTypeMismatch` variant (T2-04) and update both exhaustive matches over `DenyReason` (`code()` and `Display`). This is an independent, purely additive change to a single file (`crates/runtime-core/src/executor_decision.rs`), disjoint from Plan 01 — it runs in parallel in Wave 1.

Purpose: Provide the deny reason the Wave-2 Step 1c enforcement gate will construct. The variant is defined and rendered here; its construction site (Step 1c) is added in Plan 03.

Output: The `SlotTypeMismatch` variant, its two match arms, and a runtime-core test proving `code()` and `Display` both cover it.
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (exclude from any drift verification — they do not exist yet):
- `DenyReason::SlotTypeMismatch { sink: String, arg: String, expected: Vec<String>, found: Option<String> }` (crates/runtime-core/src/executor_decision.rs)
- The `"slot_type_mismatch"` arm in `DenyReason::code()`
- The Display arm for `SlotTypeMismatch`
- A new runtime-core test asserting the above (test name at executor's discretion, e.g. `slot_type_mismatch_code_and_display`)
</artifacts_this_phase_produces>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-slot-type-binding.md
@.planning/phases/24-slot-type-binding-enforcement/24-RESEARCH.md
@.planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DenyReason::SlotTypeMismatch variant + update both exhaustive matches + coverage evidence</name>
  <files>crates/runtime-core/src/executor_decision.rs</files>
  <read_first>
    - crates/runtime-core/src/executor_decision.rs (the DenyReason enum :14-60; DenyReason::code() match :64-80; Display for DenyReason match :83-112; the sibling DraftOnlySessionDeniesCommitIrreversible / NonLiveSessionDeniesCommitIrreversible variants + their arms as the analog)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (executor_decision.rs section — exact variant shape and both match arms; the deliberate type deviation from the SinkId-typed siblings)
    - planning-docs/DESIGN-slot-type-binding.md §5 (variant shape, F1 owned-types correction, blast-radius = exactly 2 matches)
  </read_first>
  <behavior>
    - DenyReason::SlotTypeMismatch { .. }.code() returns "slot_type_mismatch".
    - Display for a SlotTypeMismatch value renders a non-empty human-readable string naming the sink, arg, expected roles, and found role (does not panic).
    - The crate compiles with no wildcard arm in either match (a future variant would force a compile-time decision).
  </behavior>
  <action>
    Add ONE variant to the exhaustive `DenyReason` enum (executor_decision.rs:14-60), no wildcard arm:
    `SlotTypeMismatch { sink: String, arg: String, expected: Vec<String>, found: Option<String> }`.

    The field types MUST be owned — `String`, `Vec<String>`, `Option<String>` — NEVER `&'static [&'static str]` (DESIGN F1, MAJOR). `DenyReason` derives `serde::Deserialize` (:14) and the decision crosses the IPC wire (the worker deserializes it, worker.rs:370-381); serde cannot deserialize into `&'static` references. This is a deliberate deviation from the sibling variants that carry a typed `SinkId` wrapper — do NOT copy the `SinkId`-typed convention here; use plain `String`. The `expected` Vec is populated FROM the static expected-role table into an owned Vec at construction time (that construction happens in Plan 03's Step 1c; the table itself stays &'static). Name the variant `SlotTypeMismatch` verbatim to match ROADMAP / REQUIREMENTS vocabulary.

    Do NOT add a new `ExecutorDecision` variant — reuse the existing `ExecutorDecision::Denied { reason }` carrier (A3: the only non-test outer-enum match, server.rs:650-683, carries a `_ =>` wildcard arm, so it needs no update).

    Update the exactly-2 exhaustive matches, both co-located in this file:
    1. `DenyReason::code()` (:64-80) — add an arm returning `"slot_type_mismatch"`.
    2. `Display for DenyReason` (:83-112) — add a human-readable arm naming sink / arg / expected / found.

    Coverage evidence (T2-04's enumerated-sites requirement): run `grep -rn "DenyReason" crates/ cli/` and RECORD the result in the SUMMARY. That grep IS the coverage proof — it confirms exactly 2 exhaustive matches (both here) and that every other DenyReason reference is a CONSTRUCTION site, not a match. Do NOT invent hand-written CLI match arms: `cli/caprun/src/worker.rs:381` uses `matches!(decision, ExecutorDecision::Allowed)` + `eprintln!("{decision:?}")` (Debug, auto-derived) — NOT an exhaustive DenyReason match, needs NO update; audit persistence uses `#[derive(Serialize)]` — needs NO update. The no-wildcard `check-invariants.sh` discipline is the compile-time backstop: a missed 3rd match fails the build rather than silently mis-rendering.

    Add a `#[cfg(test)]` test in this file (RESEARCH flags this file currently has NO test module — create one, following the sibling-crate bottom-of-file `#[cfg(test)] mod tests` convention, do NOT invent a new test-file location) that constructs a `SlotTypeMismatch` value and asserts `.code() == "slot_type_mismatch"` and that its `Display` output is non-empty. This test also ensures the new variant is not dead code.
  </action>
  <verify>
    <automated>cargo test -p runtime-core</automated>
    <automated>cargo build --workspace</automated>
    <automated>./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'SlotTypeMismatch { sink: String, arg: String, expected: Vec<String>, found: Option<String> }' crates/runtime-core/src/executor_decision.rs returns 1
    - grep -c '&.static \[&.static str\]' inside the SlotTypeMismatch variant definition returns 0 (owned types only, per F1)
    - grep -c 'slot_type_mismatch' crates/runtime-core/src/executor_decision.rs returns >= 2 (code() arm + the test assertion)
    - Both `code()` and `Display` matches contain a `SlotTypeMismatch` arm and neither match has a `_ =>` wildcard arm
    - `cargo test -p runtime-core` exits 0
    - `cargo build --workspace` exits 0
    - `./scripts/check-invariants.sh` exits 0
    - The SUMMARY records the `grep -rn "DenyReason" crates/ cli/` output as T2-04 coverage evidence
  </acceptance_criteria>
  <done>DenyReason::SlotTypeMismatch exists with owned field types; both exhaustive matches cover it with no wildcard; a runtime-core test proves code()/Display; the grep coverage evidence is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → worker (IPC) | ExecutorDecision (carrying DenyReason) is serialized and deserialized across the UDS wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Tampering | DenyReason serialization across IPC | medium | mitigate | Field types are owned (Vec<String>/Option<String>), serde-Deserializable; using &'static refs would fail deserialization and could drop/garble a deny reason on the wire (DESIGN F1). Enforced by the owned-types acceptance criterion. |
| T-24-05 | Repudiation | missed exhaustive match silently mis-renders a deny | low | mitigate | No-wildcard discipline + check-invariants.sh makes a missed match a compile error, not a silent runtime mis-render; grep coverage evidence recorded in SUMMARY. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `cargo test -p runtime-core` green (new SlotTypeMismatch code()/Display test passes).
- `cargo build --workspace` green.
- `./scripts/check-invariants.sh` passes.
- `grep -rn "DenyReason" crates/ cli/` confirms exactly 2 exhaustive matches, both in executor_decision.rs; recorded in SUMMARY.
</verification>

<success_criteria>
A new exhaustive DenyReason variant (SlotTypeMismatch) exists with no wildcard arm; every existing exhaustive match over DenyReason (exactly the 2 in executor_decision.rs — code() and Display) is updated; owned field types cross the IPC wire; the grep coverage evidence confirms no CLI/audit match sites were missed (SC3).
</success_criteria>

<output>
Create `.planning/phases/24-slot-type-binding-enforcement/24-02-SUMMARY.md` when done.
</output>
