---
phase: 24-slot-type-binding-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/value_record.rs
  - crates/executor/src/value_store.rs
  - crates/brokerd/src/quarantine.rs
  - crates/brokerd/src/server.rs
  - crates/runtime-core/tests/types_compile.rs
  - crates/executor/tests/executor_decision.rs
  - crates/brokerd/tests/extract_provenance_threading.rs
  - crates/brokerd/tests/s9_acceptance.rs
  - crates/brokerd/tests/durable_anchor.rs
  - crates/brokerd/tests/phase5_dispatch.rs
  - crates/brokerd/src/sinks/file_create.rs
  - cli/caprun/tests/live_acceptance_v1_3.rs
autonomous: true
requirements: [T2-02]

must_haves:
  truths:
    - "Every value minted via mint_from_intent / mint_from_read / mint_from_derivation carries an origin_role tag on its ValueRecord (SC1)"
    - "I0/I1 trust classification (which values become UserTrusted vs untrusted) is UNCHANGED by this plan — origin_role is a parallel, additive field, never a TaintLabel variant (SC1)"
    - "cargo build --workspace and cargo test --workspace --no-fail-fast are both GREEN on the Mac after this plan (workspace-green boundary)"
  artifacts:
    - "crates/runtime-core/src/value_record.rs — new field: #[serde(default)] pub origin_role: Option<String>"
    - "crates/executor/src/value_store.rs — ValueStore::mint gains a 4th parameter origin_role: Option<String>, threaded into the ValueRecord{} literal"
    - "crates/brokerd/src/server.rs — primary_role selected INSIDE the intent-variant match at :1294, threaded to the shared mint_from_intent call"
  key_links:
    - "ValueStore::mint (value_store.rs:61) is the SOLE production constructor of ValueRecord — if origin_role is not threaded THERE, the tag never reaches any record (the 4th, undisclosed threading site)"
    - "Role is selected inside the server.rs:1294 intent-variant match (recipient vs path), NOT hardcoded at the shared :1317 call site (DESIGN F3)"
    - "mint_from_derivation's role is a function of transform_kind only, never read from inputs[*].origin_role (DESIGN §4 anti-laundering)"
---

<objective>
Land the additive origin-role tag mechanism (T2-02): add `origin_role: Option<String>` to `ValueRecord`, thread it through the sole production constructor `ValueStore::mint`, through the three `mint_from_*` broker wrappers, and through the five `server.rs` dispatch call sites — then update every compilation-forced test call site so the Mac workspace stays green. This is an atomic unit: changing `ValueStore::mint`'s signature breaks ~63 test call sites across 8 files at once, so all of them land in this plan.

Purpose: Give every minted value a semantic origin-role tag that the Wave-2 executor gate (Step 1c) will check. This plan adds NO enforcement and NO behavior change — the tag is carried but not yet consumed. I0/I1 trust classification is unaffected.

Output: `ValueRecord.origin_role` field, an `origin_role` parameter on `ValueStore::mint` and the three `mint_from_*` wrappers, role selection at the `server.rs` dispatch sites, and a green workspace.
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (exclude from any drift / "symbol not found" verification — they do not exist yet in the codebase):
- `ValueRecord::origin_role` — field, `#[serde(default)] pub origin_role: Option<String>` (crates/runtime-core/src/value_record.rs)
- `ValueStore::mint`'s new 4th parameter `origin_role: Option<String>` (crates/executor/src/value_store.rs)
- New trailing `origin_role`/`role` parameters on `mint_from_read`, `mint_from_intent`, `mint_from_derivation` (crates/brokerd/src/quarantine.rs) — see per-mint-site rule below
- `primary_role` local (`&'static str`) selected in the `server.rs:1294` intent-variant match
</artifacts_this_phase_produces>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-slot-type-binding.md
@.planning/phases/24-slot-type-binding-enforcement/24-RESEARCH.md
@.planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add origin_role to ValueRecord + thread through ValueStore::mint</name>
  <files>crates/runtime-core/src/value_record.rs, crates/executor/src/value_store.rs, crates/runtime-core/tests/types_compile.rs</files>
  <read_first>
    - crates/runtime-core/src/value_record.rs (the struct being modified, :20-31)
    - crates/executor/src/value_store.rs (ValueStore::mint, :61-82 — the sole production ValueRecord constructor, and its own #[cfg(test)] mod tests at :111,146,158,179)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (analog: the struct's own taint/provenance_chain fields; ValueStore::mint's own 3-param signature)
    - planning-docs/DESIGN-slot-type-binding.md §1 (tag mechanism, Option<String>, serde default)
  </read_first>
  <behavior>
    - After adding the field, a ValueRecord round-trips origin_role through serde (Serialize then Deserialize yields the same Option<String>).
    - A record serialized BEFORE the field existed still deserializes, yielding origin_role == None (the #[serde(default)] guarantee).
    - ValueStore::mint stores the passed origin_role verbatim onto the record; a later resolve() returns it unchanged.
  </behavior>
  <action>
    Add a fifth field to `ValueRecord` (value_record.rs:20-31): `#[serde(default)] pub origin_role: Option<String>`. Use `Option<String>`, NOT bare `String` — `None` must be representable as a state distinct from every valid role tag, because the Wave-2 fail-closed default keys off exactly that bit (DESIGN §1). The `#[serde(default)]` attribute is MANDATORY per DESIGN F6 so pre-field serialized records still deserialize.

    Add a 4th parameter `origin_role: Option<String>` to `ValueStore::mint` (value_store.rs:61) and thread it straight into the `ValueRecord { .. }` literal at :74-79. This is the 4th, undisclosed threading site (RESEARCH A4): `ValueStore::mint` is the ONLY production code path that constructs a `ValueRecord`, so the tag cannot reach any record without passing through here. This edit is Gate-3-legal (`value_store.rs` is a sanctioned `.mint(` locus). Do NOT add any validation / empty-check on origin_role — the existing early-return guards on empty taint / empty provenance are UNCHANGED; the fail-closed role check lives entirely in the Wave-2 Step 1c, not here.

    `ValueRecord` has NO `#[derive(Default)]`, so every direct struct-literal site must be updated or the build fails. This task fixes 2 of the 3 direct-literal sites: `value_store.rs:74-79` (the production constructor, above) and `crates/runtime-core/tests/types_compile.rs:42-47` (the serde/shape compile-check — add `origin_role: None` or a `Some("...")` matching that test's intent). Do NOT introduce a `#[derive(Default)]` + `..Default::default()` shortcut (DESIGN "Don't Hand-Roll" — it would mask which fixtures intentionally carry a role). Update the 4 `.mint(` call sites in `value_store.rs`'s own test module (:111,146,158,179) with an explicit `None` or `Some("...")` 4th arg. (The 2 quarantine.rs direct-literal test sites are handled in Task 3.)
  </action>
  <verify>
    <automated>cargo test -p executor value_store</automated>
    <automated>cargo test -p runtime-core</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'pub origin_role: Option<String>' crates/runtime-core/src/value_record.rs returns 1
    - crates/runtime-core/src/value_record.rs contains `#[serde(default)]` immediately preceding the origin_role field
    - ValueStore::mint's signature in crates/executor/src/value_store.rs lists an `origin_role: Option<String>` parameter
    - grep -c 'Default::default' crates/runtime-core/src/value_record.rs returns 0 (no Default shortcut)
    - `cargo test -p executor value_store` exits 0
    - `cargo test -p runtime-core` exits 0
  </acceptance_criteria>
  <done>ValueRecord carries origin_role (serde-default), ValueStore::mint threads it onto the record, and runtime-core + executor value_store tests compile and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Thread origin_role through the 3 mint_from_* wrappers + 5 server.rs dispatch sites</name>
  <files>crates/brokerd/src/quarantine.rs, crates/brokerd/src/server.rs</files>
  <read_first>
    - crates/brokerd/src/quarantine.rs (mint_from_read :284, mint_from_intent :435, mint_from_derivation :574; the `match transform_kind { "concat" => ... }` block at :670-692; the store.mint delegations at :365/:470/:703)
    - crates/brokerd/src/server.rs (the intent-variant match at :1294-1300; the 5 mint call sites at :1084 ReportClaims, :1217 ReportDerivedClaim, :1317/:1330/:1347 mint_from_intent)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (server.rs analog: the intent-variant match extended to produce primary_role; quarantine concat-arm arity-guard idiom)
    - planning-docs/DESIGN-slot-type-binding.md §2 (role vocabulary table), §4 (derivation role from transform_kind), §9 (mint-site signature map)
  </read_first>
  <action>
    Thread the role through all three wrappers, honoring the per-mint-site rule (DESIGN §9 + the T2-02 signatures rule): ONLY `mint_from_intent` gains a NEW caller-supplied role parameter. The other two derive their role INSIDE the function — do NOT push role authority out to their callers.

    `mint_from_intent` (quarantine.rs:435): add a trailing parameter (e.g. `origin_role: Option<String>` or `role: &str`) and pass it into the `store.mint(...)` delegation at :470.

    `mint_from_read` (quarantine.rs:284): role source is `claim.claim_type` VERBATIM — already resolved inside the function body (:315-341, one of `"email_address"` / `"relative_path"` / `"doc_fragment"`). Reuse it; do NOT add a caller parameter and do NOT re-derive. Pass `Some(claim.claim_type.clone())` into the `store.mint(...)` delegation at :365. Any other claim_type is already a fail-closed mint error (:336-340), so no untrusted value can carry an unknown role.

    `mint_from_derivation` (quarantine.rs:574): role is a deterministic function of `transform_kind`, NEVER inherited or unioned from `inputs[*].origin_role` (DESIGN §4 anti-laundering — a naive implementer will reach for the taint-union shape sitting right above; do NOT). Inside the existing `match transform_kind { "concat" => ... }` block (:670-692), for the `"concat"` arm pin option (a) from DESIGN §4 F2: guard on arity — `let origin_role = if inputs.len() == 2 { Some("recipient".to_string()) } else { None };` — because the `local@domain` email shape only holds for exactly 2 inputs (Concat joins N≥1 inputs on '@'; 1 input is verbatim, 3+ is `a@b@c`). Do NOT skip the length guard and lean on I2 alone. Pass the resulting origin_role into the `store.mint(...)` delegation at :703.

    In `server.rs`, select the role INSIDE the intent-variant match at :1294-1300 — never hardcode at the shared call site. The match currently binds `(primary_literal, subject_literal, body_literal): (String, Option<String>, Option<String>)`. Extend it to also bind a `primary_role: &'static str` in the SAME arm that produces `primary_literal`: `SendEmailSummary` → `"recipient"`, `CreateFileFromReport` → `"path"` (widen the tuple to a 4-tuple and update its type annotation). Thread `primary_role` into the shared `mint_from_intent` call at :1317. Hardcoding `"recipient"` at :1317 (which is reached by BOTH intent variants) would mistag every `file.create` path and cause a fail-closed regression at the Wave-2 `path` slot (DESIGN F3 — a real correctness bug, not a security hole, but must not ship). The `subject` call at :1330 gets literal `"subject"`; the `body` call at :1347 gets literal `"body"` (each reachable only from `SendEmailSummary`, no ambiguity). The `ReportClaims` (:1084) and `ReportDerivedClaim` (:1217) call sites need NO role argument added at the call site — role is resolved inside the callee per the mint_from_read / mint_from_derivation rules above.

    Mint-ordering landmine (DESIGN §9): `mint_from_read` appends its file_read Event BEFORE minting (:342-366); `mint_from_derivation` mints FIRST then appends its derivation Event (:702-729). Do NOT assume a uniform mint-then-event order when threading.
  </action>
  <verify>
    <automated>cargo test -p brokerd quarantine</automated>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - crates/brokerd/src/server.rs contains a match arm producing `"recipient"` and one producing `"path"` (grep -c '"recipient"' and '"path"' inside the :1294 match region each >= 1)
    - grep -c 'inputs.len() == 2' crates/brokerd/src/quarantine.rs returns >= 1 (the concat arity guard)
    - grep -n 'inputs\[' crates/brokerd/src/quarantine.rs shows NO read of inputs[*].origin_role inside mint_from_derivation's role assignment (role never inherited from inputs)
    - `cargo build --workspace` exits 0
    - `cargo test -p brokerd quarantine` exits 0
  </acceptance_criteria>
  <done>All three mint_from_* wrappers thread a role (intent: caller-supplied; read: claim_type; derivation: transform_kind with the 2-input concat guard); server.rs selects primary_role inside the intent-variant match and threads it to the shared mint_from_intent call.</done>
</task>

<task type="auto">
  <name>Task 3: Update all compilation-forced test fixtures to keep the workspace green</name>
  <files>crates/brokerd/src/quarantine.rs, crates/executor/tests/executor_decision.rs, crates/brokerd/tests/extract_provenance_threading.rs, crates/brokerd/tests/s9_acceptance.rs, crates/brokerd/tests/durable_anchor.rs, crates/brokerd/tests/phase5_dispatch.rs, crates/brokerd/src/sinks/file_create.rs, cli/caprun/tests/live_acceptance_v1_3.rs</files>
  <read_first>
    - crates/brokerd/src/quarantine.rs (its own #[cfg(test)] mod tests, ~44 mint_from_* call sites; the 2 direct ValueRecord{} literal sites at :1589-1600)
    - crates/executor/tests/executor_decision.rs (16 .mint() call sites: lines 72,133,184,215,222,250,286,314,340,347,415,441,448,494,501,559,566)
    - .planning/phases/24-slot-type-binding-enforcement/24-RESEARCH.md §3 (full file-by-file call-site inventory) and Open Question 2 (role-assignment discipline for existing fixtures)
    - .planning/phases/24-slot-type-binding-enforcement/24-PATTERNS.md (test-files section, the mechanical None/Some pattern)
  </read_first>
  <action>
    Update every test call site that the Task 1/2 signature changes broke, so `cargo build --workspace` and `cargo test --workspace --no-fail-fast` are GREEN on the Mac. This is the workspace-green boundary: these fixture updates are direct compilation-forced consequences of the type change and are IN SCOPE for Phase 24. The SYSTEMATIC audit of permissive-UserTrusted tests is Phase 25 (T2-07) and is OUT OF SCOPE here — only fix what the compiler forces plus what the new field's presence requires.

    NEVER weaken, disable, or `#[ignore]` a test to make it compile/pass, and never soften the new mechanism (violates CLAUDE.md Critical Rules). This plan adds no enforcement, so no existing assertion should newly fail on behavior — only on missing arguments / missing struct field.

    Role-assignment discipline (RESEARCH Open Question 2): assign each fixture a role that keeps its EXISTING assertion valid. A fixture asserting a `to`-slot `Allowed` outcome with a `UserTrusted` value should get `origin_role: Some("recipient".to_string())` (NOT `None` or a mismatched role) so it will still pass once Wave-2 enforcement lands. For fixtures that do not exercise a role-checked slot, an explicit `None` (or `Some("...")` matching the fixture's semantic intent) is correct. Do NOT use a blanket default. If the correct role for a fixture is genuinely ambiguous, leave a `// TODO(24): role assignment — Phase 25 audit` comment and pick the value that preserves the current assertion, rather than guessing silently.

    Site inventory (from RESEARCH §3, re-confirm counts before editing): quarantine.rs own test module ~44 `mint_from_*` calls + the 2 direct `ValueRecord{}` literals at :1589-1600; executor/tests/executor_decision.rs 16 `.mint()` calls; brokerd/tests/{extract_provenance_threading (5), s9_acceptance (3+1 direct .mint at :433), durable_anchor (1+1 direct .mint at :128), phase5_dispatch (4)}; brokerd/src/sinks/file_create.rs `.mint(` at :255/:258 (inside that file's own #[cfg(test)] block); cli/caprun/tests/live_acceptance_v1_3.rs (1). All are Gate-3-exempt (tests / #[cfg(test)]).
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
    <automated>cargo test --workspace --no-fail-fast</automated>
    <automated>./scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0
    - `cargo test --workspace --no-fail-fast` exits 0 (Mac: Linux-only security tests report "0 passed" by design — that is expected, not a failure)
    - `./scripts/check-invariants.sh` exits 0 (Gate 1 EffectRequest, Gate 2 runtime-core I/O-free, Gate 3 mint-token loci all pass)
    - grep -rn '#\[ignore\]' on the touched test files shows no NEW ignore attributes added by this task
    - No `mint_from_*` or `.mint(` call-site token was added OUTSIDE quarantine.rs / server.rs / value_store.rs production code (Gate 3 backstop)
  </acceptance_criteria>
  <done>The full Mac workspace builds and tests green with origin_role threaded through every call site; check-invariants.sh passes; no test was weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → broker (IPC/UDS) | Worker submits ReportClaims / ReportDerivedClaim / ProvideIntent; broker is sole mint authority |
| broker mint sites → ValueRecord | The only place origin_role is assigned; must be atomic with taint/provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | mint_from_derivation role assignment | high | mitigate | Role derived from transform_kind's verified output shape only; NEVER read/unioned from inputs[*].origin_role — closes the derivation role-laundering path (DESIGN §4). Enforced by acceptance criterion (no inputs[*].origin_role read). |
| T-24-02 | Spoofing | mistagged file.create path | medium | mitigate | primary_role selected inside the server.rs:1294 intent-variant match, never hardcoded at the shared :1317 call site; prevents a `file.create` path being tagged "recipient" (DESIGN F3). |
| T-24-03 | Tampering | untrusted-origin role tag is worker-influenced | high | accept | A hostile worker can mint an arbitrary string tagged "email_address"/"relative_path" (mint_from_read shape-validates only doc_fragment). Accepted because the role tag is NOT a trust signal on the untrusted side — such values carry untrusted taint and remain governed by I2's per-arg Block. The Wave-2 table-construction invariant (untrusted role appears in a slot's list only if that slot is also I2-sensitive) preserves this. No mitigation in THIS plan; carried to Plan 03's table. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies introduced this phase (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `cargo build --workspace` green.
- `cargo test --workspace --no-fail-fast` green on the Mac (Linux-only security tests report 0 passed by design).
- `./scripts/check-invariants.sh` passes (Gates 1/2/3).
- origin_role present on ValueRecord with `#[serde(default)]`; threaded through ValueStore::mint and all three mint_from_* wrappers; role selected in the server.rs intent-variant match.
</verification>

<success_criteria>
Every value minted via the three mint paths carries an origin_role tag, added as an additive mechanical signature change; I0/I1 trust classification is unaffected; the Mac workspace is green (SC1). No enforcement is added by this plan — the tag is carried, not yet consumed.
</success_criteria>

<output>
Create `.planning/phases/24-slot-type-binding-enforcement/24-01-SUMMARY.md` when done.
</output>
