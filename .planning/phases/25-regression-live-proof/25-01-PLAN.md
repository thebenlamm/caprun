---
phase: 25-regression-live-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/tests/s9_acceptance.rs
autonomous: true
requirements: [T2-06]
must_haves:
  truths:
    - "A plan node routing a subject-role UserTrusted value into `to` and a recipient-role UserTrusted value into `subject` returns ExecutorDecision::Denied { reason: DenyReason::SlotTypeMismatch { .. } } (T2-06)"
    - "The deny is attributable to Step 1c ALONE: the same two UserTrusted values routed into their correct slots (subject->subject, recipient->to) evaluate to ExecutorDecision::Allowed — proving neither I0 (class-deny) nor I2 (untrusted per-arg Block) could have produced the swapped deny (T2-06 isolation)"
    - "The Denied evaluation is durably recorded as a `plan_node_evaluated` audit-DAG event (anchors: vec![]) causally parented on the last mint's event id, and verify_chain returns true across it (T2-06)"
  artifacts:
    - "crates/brokerd/tests/s9_acceptance.rs contains #[test] fn slot_type_binding_swapped_subject_recipient_denies"
    - "crates/brokerd/tests/s9_acceptance.rs contains #[test] fn slot_type_binding_correctly_routed_allows"
  key_links:
    - "mint_from_intent (genuine origin_role tag at mint time) -> submit_plan_node (Step 1c role check) -> Denied{SlotTypeMismatch}"
    - "hand-mirrored append_event(plan_node_evaluated) -> find_event_by_type -> verify_chain true"
---

<objective>
Write the held-out T2-06 acceptance test that proves the shipped Phase 24 slot-type
binding enforcement (Step 1c) catches a deliberately SWAPPED subject↔recipient handle pair
through the REAL broker path — with a genuinely-propagated origin_role chain (never a stapled
field), a corresponding audit-DAG event, and verify_chain still true.

Purpose: T2-06 is tripwire #1 (the genuine-chain proof). Phase 24 already shipped the
enforcement CODE and unit-tested it in-process (`role_mismatch_denies` etc.). Those unit tests
call `store.mint()` directly, bypass the audit DAG, and only swap body→to — they are NOT the
T2-06 proof. This plan adds the missing FULL-broker-path + audit-DAG layer: mint via
`mint_from_intent` (real origin_role assignment), submit via `submit_plan_node`, assert
`Denied{SlotTypeMismatch}`, hand-mirror the `plan_node_evaluated` DAG append exactly as the
existing `clean_path_intent_value_evaluates_to_allowed` test does, and assert `verify_chain` true.

Output: two new `#[test] fn`s in `crates/brokerd/tests/s9_acceptance.rs` (the swapped-deny proof
and its correctly-routed Allowed control). NO production code changes. NO new files.

DESIGN §9 note (deliberate, non-silent deviation): DESIGN §9's informative map says the held-out
test "must live behind `#[cfg(target_os = "linux")]`". Per Phase-25 RESEARCH (HIGH) the correct
test shape is IN-PROCESS (`mint_from_intent` + `submit_plan_node`) whose behavior is identical on
Mac and Linux and needs no kernel — cfg-gating it would only slow feedback and gain nothing.
Because `s9_acceptance.rs` is NOT cfg-gated, it runs under `bash scripts/mailpit-verify.sh` on real
Linux at the T2-08 gate (Plan 03), which asserts this test's name appears in the Linux log with 0
failures — satisfying DESIGN §9's actual intent (proven on real Linux, not Mac-only). DESIGN §9 is
explicitly labeled "informative — not part of the gate", so this is a sanctioned reading, recorded
here so the executor and verifier see the reasoning.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-regression-live-proof/25-RESEARCH.md
@.planning/phases/25-regression-live-proof/25-PATTERNS.md
@crates/brokerd/tests/s9_acceptance.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Held-out swapped subject↔recipient deny test (the T2-06 genuine-chain proof)</name>
  <files>crates/brokerd/tests/s9_acceptance.rs</files>
  <read_first>
    - crates/brokerd/tests/s9_acceptance.rs lines 276-404 — `clean_path_intent_value_evaluates_to_allowed`, the EXACT analog to mirror (same file, same helper set, inverted assertion). Copy its structure: `open_audit_db(":memory:")`, `ValueStore::default()`, `Uuid::new_v4()` session, `mint_from_intent` call at :297-306 (the 7-param form with `origin_role` as the 7th arg), the local `use brokerd::audit::append_event; use runtime_core::Event;` idiom at :355-356, the `Event::new(.., Some(intent_event_id), session_id, "executor".into(), "plan_node_evaluated".into(), chrono::Utc::now(), vec![])` append at :358-367, and the `find_event_by_type` + `verify_chain` assertions at :388-403.
    - crates/executor/tests/executor_decision.rs lines 689-721 — `role_mismatch_denies` — borrow ONLY the `match` arm shape `ExecutorDecision::Denied { reason: DenyReason::SlotTypeMismatch { sink, arg, expected, found } }`. Do NOT copy its `store.mint()` mint path (bypasses the audit DAG — the exact anti-pattern T2-06 must avoid).
    - .planning/phases/25-regression-live-proof/25-RESEARCH.md "Pattern 1" (lines 157-251) and Anti-Patterns (lines 253-267) — the full worked shape and the three forbidden shortcuts.
    - planning-docs/DESIGN-slot-type-binding.md §3 (lines 162-206) — the expected-role table: `to`/`cc`/`bcc` expect `["recipient","email_address"]`, `subject` expects `["subject"]`.
  </read_first>
  <action>
    Add a new `#[test] fn slot_type_binding_swapped_subject_recipient_denies` to
    crates/brokerd/tests/s9_acceptance.rs (NOT cfg-gated). Mirror
    `clean_path_intent_value_evaluates_to_allowed` structurally. Steps:

    (1) Open an in-memory audit DB, a default ValueStore, and a fresh session_id.

    (2) Call `mint_from_intent` TWICE, each with a GENUINE origin_role tag assigned at mint time
    (this is the genuine-chain discipline — origin_role must be a real mint-assigned tag, never a
    hand-set field at the sink). First mint: the literal "Re: quarterly report" tagged
    Some("subject".to_string()) — a legitimately subject-role value. Second mint: the literal
    "boss@company.com" tagged Some("recipient".to_string()) — a legitimately recipient-role value.
    Chain them genuinely: the second call's parent_id/parent_hash MUST be the first call's returned
    (event_id, hash), NOT None/None — so the two intent_received events form one real single-session
    chain (mirrors server.rs's sequential SendEmailSummary mint calls), not siblings.

    (3) Build a PlanNode with sink SinkId("email.send".into()) and args that SWAP the handles:
    PlanArg { name: "to", value_id: <the subject-tagged value_id> } and
    PlanArg { name: "subject", value_id: <the recipient-tagged value_id> }. Both values are
    UserTrusted and untainted, so neither I0 (Step 0.5 class-deny) nor I2 (untrusted per-arg Block)
    can fire — ONLY Step 1c catches the mismatch. State this isolation rationale in a doc-comment
    above the test.

    (4) Call `executor::submit_plan_node(session_id, Uuid::new_v4(), &plan_node, &store,
    &SessionStatus::Active)`. Match the result against
    `ExecutorDecision::Denied { reason: DenyReason::SlotTypeMismatch { sink, arg, expected, found } }`
    (DenyReason lives in runtime_core::executor_decision — add the import if not already present);
    panic on any other decision. Assert sink == "email.send", arg == "to",
    expected == vec!["recipient".to_string(), "email_address".to_string()],
    found == Some("subject".to_string()).

    (5) Durably record the Denied evaluation EXACTLY as the broker's private
    `evaluate_plan_node_and_record` catch-all `_` arm does for any non-Blocked decision (confirmed
    at server.rs:670-682 — `evaluate_plan_node_and_record` is a private async fn, UNREACHABLE from
    crates/brokerd/tests/, so this hand-mirror is the ONLY option and must stay faithful to that arm):
    construct `Event::new(Uuid::new_v4(), Some(<recipient mint event_id>), session_id,
    "executor".into(), "plan_node_evaluated".into(), chrono::Utc::now(), vec![])` and
    `append_event(&conn, &eval_event, Some(&<recipient mint hash>))`. Use the same local-`use`
    idiom as the analog (`use brokerd::audit::append_event; use runtime_core::Event;` inside the fn),
    keeping the diff surgical — do not hoist to file scope.

    (6) Assert the durable record: `find_event_by_type(&conn, &session_id.to_string(),
    "plan_node_evaluated")` returns Some, its parent_id == Some(<recipient mint event_id>), and
    `verify_chain(&conn, &session_id.to_string())` returns true.

    Do NOT construct DenyReason::SlotTypeMismatch by hand; do NOT call ValueStore::mint() directly;
    do NOT assert a `sink_blocked` event or a reason-rich DAG payload (the SlotTypeMismatch reason
    is NOT persisted in the DAG — only the bare plan_node_evaluated event is; assert only what is
    actually recorded).
  </action>
  <acceptance_criteria>
    - crates/brokerd/tests/s9_acceptance.rs contains `fn slot_type_binding_swapped_subject_recipient_denies`.
    - The test obtains its origin_role tags via `mint_from_intent` (genuine mint-time assignment), NOT via `store.mint()` or a hand-set field, and NOT by constructing DenyReason::SlotTypeMismatch directly.
    - `cargo test -p brokerd --test s9_acceptance slot_type_binding_swapped_subject_recipient_denies` exits 0.
    - The test asserts the returned decision is Denied{SlotTypeMismatch} with arg=="to", found==Some("subject"), expected==["recipient","email_address"].
    - The test asserts a `plan_node_evaluated` event is present, parented on the second mint's event_id, and `verify_chain` returns true — and does NOT assert a `sink_blocked` event.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd --test s9_acceptance slot_type_binding_swapped_subject_recipient_denies -- --exact</automated>
  </verify>
  <done>The swapped subject↔recipient plan node denies with SlotTypeMismatch through the real broker path, a plan_node_evaluated audit-DAG event is durably recorded and chained, and verify_chain stays true — proven by a passing test that uses genuine mint-time origin_role tags.</done>
</task>

<task type="auto">
  <name>Task 2: Correctly-routed Allowed control (proves the deny is Step-1c-attributable, not I0/I2)</name>
  <files>crates/brokerd/tests/s9_acceptance.rs</files>
  <read_first>
    - crates/brokerd/tests/s9_acceptance.rs lines 336-348 — the `assert!(matches!(decision, ExecutorDecision::Allowed), ...)` shape from `clean_path_intent_value_evaluates_to_allowed`, to mirror for the Allowed assertion.
    - Task 1 above (the sibling swapped-deny test just written) — reuse its exact two `mint_from_intent` calls (same literals, same genuine roles, same causal chaining) so this control provably exercises the SAME two values.
    - .planning/phases/25-regression-live-proof/25-VALIDATION.md "Genuine-Chain Discipline" (lines 57-64) — the both-UserTrusted isolation requirement this control makes mechanical.
  </read_first>
  <action>
    Add a second `#[test] fn slot_type_binding_correctly_routed_allows` to
    crates/brokerd/tests/s9_acceptance.rs. Mint the SAME two UserTrusted values as Task 1
    ("Re: quarterly report" tagged Some("subject"), "boss@company.com" tagged Some("recipient"),
    genuinely chained via mint_from_intent), but this time route each handle into its CORRECT slot:
    PlanArg { name: "subject", value_id: <subject-tagged id> } and
    PlanArg { name: "to", value_id: <recipient-tagged id> }. Call `submit_plan_node` with the same
    args as Task 1's step (4) and assert the decision is `ExecutorDecision::Allowed`.

    This control is the mechanical (not comment-only) proof of T2-06's isolation claim: because the
    SAME two values, when correctly routed, are Allowed, they are demonstrably valid and untainted —
    so I2 (untrusted per-arg Block) cannot fire on them, and the session being Active means I0
    (class-deny) cannot fire either. Therefore the Denied outcome in Task 1 is attributable to Step
    1c (the slot-type role check) ALONE, and to nothing else. Add a doc-comment stating exactly this
    reasoning. Keep the diff surgical; no production code changes.
  </action>
  <acceptance_criteria>
    - crates/brokerd/tests/s9_acceptance.rs contains `fn slot_type_binding_correctly_routed_allows`.
    - The control mints the same two literals with the same genuine roles as Task 1, routes them into their correct slots (subject->subject, recipient->to), and asserts ExecutorDecision::Allowed.
    - `cargo test -p brokerd --test s9_acceptance slot_type_binding_correctly_routed_allows` exits 0.
    - A doc-comment states that this Allowed outcome proves the Task-1 deny is Step-1c-attributable (neither I0 nor I2 could have produced it).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd --test s9_acceptance slot_type_binding -- --nocapture</automated>
  </verify>
  <done>The same two UserTrusted values that deny when swapped are Allowed when correctly routed — mechanically proving the swapped deny is caused by Step 1c and not by I0/I2, closing the T2-06 isolation requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/worker → broker plan node | A (possibly buggy or hostile) plan routes a legitimately-trusted value into the wrong semantic slot; the executor must catch it structurally. This test PROVES that catch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Spoofing/Tampering | Confused-deputy slot misrouting (subject↔recipient swap) at `email.send` | high | mitigate | Step 1c (shipped Phase 24) hard-Denies role↔slot mismatch; THIS plan proves it end-to-end through the real broker path with a genuine mint-time origin_role chain — closing the v1.4 T2 residual with an auditable proof, not a stapled assertion. |
| T-25-02 | Repudiation | The T2-06 proof itself being stapled (test constructs the deny by hand instead of driving production code) | high | mitigate | Genuine-chain discipline enforced in the task: mint via `mint_from_intent`, submit via `submit_plan_node`, never construct DenyReason directly nor call `ValueStore::mint()`; a correctly-routed Allowed control proves the deny is Step-1c-attributable. |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — zero new dependencies (RESEARCH Package Legitimacy Audit: N/A). No install tasks, no legitimacy checkpoints required. |
</threat_model>

<verification>
- `cargo test -p brokerd --test s9_acceptance slot_type_binding` — both new tests green (Mac-buildable, seconds).
- `cargo test -p brokerd --test s9_acceptance` — the whole acceptance file still green (no regression to the existing clean_path/blocked tests).
- Real-Linux confirmation is delivered by Plan 03 (T2-08): `bash scripts/mailpit-verify.sh` runs this non-gated test on real Linux and asserts its name appears in the log with 0 failures.
</verification>

<success_criteria>
Both `slot_type_binding_swapped_subject_recipient_denies` and
`slot_type_binding_correctly_routed_allows` exist in s9_acceptance.rs and pass; the swapped test
proves Denied{SlotTypeMismatch} + plan_node_evaluated event + verify_chain true through genuine
mint-time origin_role tags; the control proves the deny is Step-1c-attributable. No production code
changed.
</success_criteria>

## Artifacts this plan produces
- `crates/brokerd/tests/s9_acceptance.rs`::`slot_type_binding_swapped_subject_recipient_denies` (new `#[test] fn`)
- `crates/brokerd/tests/s9_acceptance.rs`::`slot_type_binding_correctly_routed_allows` (new `#[test] fn`)

<output>
Create `.planning/phases/25-regression-live-proof/25-01-SUMMARY.md` when done.
</output>
