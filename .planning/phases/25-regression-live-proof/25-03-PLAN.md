---
phase: 25-regression-live-proof
plan: 03
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified: []
autonomous: false
requirements: [T2-08]
user_setup:
  - service: colima-docker
    why: "Linux-only Landlock/seccomp + real SMTP verification runs in an unprivileged rust:1 container on a Colima-hosted Docker network with a Mailpit sidecar"
    dashboard_config:
      - task: "Ensure Colima is running (`colima status` reports running) and Docker is reachable (`docker info` succeeds) before the gate run"
        location: "local Mac — `colima start` if not running"
must_haves:
  truths:
    - "`bash scripts/mailpit-verify.sh` (bare default recipe, NO MAILPIT_VERIFY_CMD override) is independently re-run and reports the terminal sentinel line 'Mailpit-backed Linux verification suite PASSED.' with a true captured exit code of 0 and 0 FAILED test-result blocks (T2-08)"
    - "The Plan-01 held-out test `slot_type_binding_swapped_subject_recipient_denies` appears in the Linux verification log as run (not silently skipped) — proving T2-06's swapped-handle deny holds on real Linux under the confined-worker suite (T2-06 real-Linux leg / T2-08)"
    - "A human confirms the v1.5 DONE milestone-close: the full workspace regression is green on real Linux, independently re-run, not assumed from any prior or scoped pass (T2-08)"
  artifacts:
    - "25-03-SUMMARY.md records the true pre-pipe exit code, the PASSED sentinel line verbatim, the test-result-ok / FAILED counts, and confirmation the held-out test name appeared in the log"
  key_links:
    - "bare `bash scripts/mailpit-verify.sh` -> capture RESULT=$? BEFORE any pipe -> assert exit 0 + sentinel + 0 FAILED + held-out test ran"
    - "non-cfg-gated s9_acceptance.rs held-out test -> executed inside mailpit-verify.sh's cargo test --workspace on real Linux"
---

<objective>
The v1.5 DONE gate: independently re-run the full workspace regression on real Linux via the bare
default `scripts/mailpit-verify.sh` recipe, prove it green (0 failures), confirm the Plan-01 held-out
test ran on real Linux, and take human sign-off on the milestone close.

Purpose: T2-08 is the milestone-close discipline. It must NOT be assumed from a prior or scoped pass
— scoped `MAILPIT_VERIFY_CMD` runs have masked real bugs before (the v1.4 caprun-planner
binary-placement bug, caught only by a full unscoped re-run). This plan runs the ACTUAL default
recipe, captures the true exit code BEFORE any pipe (a documented prior project incident nearly
shipped a false PASS by checking `$?` after a `| tail`), and asserts on named counts + the PASSED
sentinel — never on exit-0-through-a-pipe. The same unscoped run independently confirms Phase 24's
body/doc_fragment deviation does not break the live flow, and that Plan 01's non-cfg-gated held-out
test passes on real Linux (T2-06's real-Linux leg).

Output: a green Linux verification, a SUMMARY recording the evidence, and human milestone-close
sign-off. NO code changes — this plan only runs and verifies. Runs AFTER Plan 01 (held-out test) and
Plan 02 (regression audit) land — it must not be run against a tree missing the new test.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-regression-live-proof/25-RESEARCH.md
@.planning/phases/25-regression-live-proof/25-VALIDATION.md
@CLAUDE.md
@scripts/mailpit-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment precheck + run the bare default mailpit-verify.sh gate</name>
  <files>(none — runs and verifies; no files modified)</files>
  <read_first>
    - scripts/mailpit-verify.sh (full) — the invocation contract: default `MAILPIT_VERIFY_CMD` is `cargo build --workspace && cargo test --workspace --no-fail-fast` (do NOT override); the success sentinel is the terminal line "Mailpit-backed Linux verification suite PASSED." printed only after a non-error `docker run` exit under `set -euo pipefail`.
    - CLAUDE.md "Linux-only security tests" + the mailpit-verify.sh paragraph — why the bare recipe (not the raw `docker run rust:1`) is mandatory from Phase 16 onward (a real email.send dispatch needs the Mailpit sidecar).
    - .planning/phases/25-regression-live-proof/25-RESEARCH.md "T2-08: Live Re-Run Discipline" (lines 397-417) and Pitfall 4 (lines 321-330) — capture `$?` BEFORE any pipe; assert on the sentinel + named counts, never on exit-0-through-a-pipe.
    - ~/.claude memory "Verification exit code through a pipe" — the prior project incident this discipline exists to prevent.
  </read_first>
  <action>
    First confirm the environment: run `colima status` (expect "colima is running") and `docker info`
    (expect success). If Colima is not running, run `colima start` and re-check. Do not proceed until
    both pass.

    Then run the gate EXACTLY as the default recipe, with NO `MAILPIT_VERIFY_CMD` override:
    invoke `bash scripts/mailpit-verify.sh` and capture its exit status into a variable on the VERY
    NEXT line (RESULT=$?), BEFORE any pipe, tee, grep, or tail. Redirect the full combined output to a
    log file (e.g. via `bash scripts/mailpit-verify.sh > /tmp/mailpit-verify-25.log 2>&1; RESULT=$?`)
    so the exit code is the SCRIPT's, not a pipe's.

    Assert ALL of the following on the captured artifacts (fail the task if any is not met):
    (1) RESULT == 0.
    (2) The log's terminal region contains the exact sentinel line
        "Mailpit-backed Linux verification suite PASSED."
    (3) The log contains one or more "test result: ok" blocks and ZERO "test result: FAILED" and zero
        "error: " compilation-abort lines.
    (4) The log contains the string "slot_type_binding_swapped_subject_recipient_denies" — proving
        Plan 01's non-cfg-gated held-out test actually RAN on real Linux (not silently skipped),
        satisfying T2-06's real-Linux leg.

    Record all four results (the true exit code, the verbatim sentinel line, the ok/FAILED counts, and
    the held-out-test presence) into 25-03-SUMMARY.md. If ANY assertion fails, do NOT declare pass —
    capture the failing evidence, do not weaken any assertion, and surface the failure (a red gate here
    is a real bug, per milestone-close discipline).
  </action>
  <acceptance_criteria>
    - The gate was run as bare `bash scripts/mailpit-verify.sh` with NO MAILPIT_VERIFY_CMD override, and the exit code was captured on the line immediately after the invocation (before any pipe).
    - Captured true exit code == 0.
    - The log contains the verbatim sentinel "Mailpit-backed Linux verification suite PASSED." and zero "test result: FAILED".
    - The log contains "slot_type_binding_swapped_subject_recipient_denies" (the held-out test ran on real Linux).
    - 25-03-SUMMARY.md records the exit code, sentinel, ok/FAILED counts, and held-out-test presence.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/mailpit-verify.sh > /tmp/mailpit-verify-25.log 2>&1; RESULT=$?; grep -q "Mailpit-backed Linux verification suite PASSED." /tmp/mailpit-verify-25.log && grep -q "slot_type_binding_swapped_subject_recipient_denies" /tmp/mailpit-verify-25.log && ! grep -q "test result: FAILED" /tmp/mailpit-verify-25.log && [ "$RESULT" -eq 0 ]</automated>
  </verify>
  <done>The full workspace regression is independently green on real Linux via the bare default recipe, with the true pre-pipe exit code 0, the PASSED sentinel, 0 failures, and the Plan-01 held-out test proven to have run on Linux — all evidence durably recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — v1.5 DONE milestone-close gate</name>
  <action>STOP. This is a blocking milestone-close gate. Present the Task 1 evidence to the human and wait for explicit approval before marking the phase complete. Do NOT self-approve regardless of auto_advance — this is the v1.5 DONE sign-off.</action>
  <what-built>
    The v1.5 milestone-close proof: Plan 01's held-out swapped subject↔recipient deny test, Plan 02's
    regression audit (no silent bypass), and Task 1's independent bare `mailpit-verify.sh` re-run —
    green on real Linux, 0 failures, held-out test confirmed run on Linux, exit code captured before
    any pipe.
  </what-built>
  <how-to-verify>
    1. Open 25-03-SUMMARY.md and confirm it records: true exit code 0, the verbatim sentinel line
       "Mailpit-backed Linux verification suite PASSED.", the "test result: ok" count with 0
       "test result: FAILED", and the presence of "slot_type_binding_swapped_subject_recipient_denies"
       in the Linux log.
    2. Confirm the run used the BARE default recipe (no MAILPIT_VERIFY_CMD override) and the exit code
       was captured before any pipe (not exit-0-through-a-pipe).
    3. Confirm Plan 02's 25-REGRESSION-AUDIT.md carries a final T2-07 PASS verdict with an explicit
       NEEDS-FIX count.
    4. This is the v1.5 DONE gate (T2 slot-type binding fully proven). Confirm you are satisfied the
       milestone is closeable, or describe any concern.
  </how-to-verify>
  <resume-signal>Type "approved" to close the v1.5 milestone gate, or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/verification → milestone-close truth claim | A false-green (exit-0-through-a-pipe, or a scoped run that masks a bug) would let a broken build close the milestone. This gate defends against that. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Repudiation | False-green live verification (exit-0-through-a-pipe, trusting a prior/scoped pass) | high | mitigate | Bare default recipe only (no MAILPIT_VERIFY_CMD override); capture `$?` before any pipe; assert on the PASSED sentinel + named test counts + held-out-test presence, never on a piped exit code; independent re-run at gate time, not assumed. |
| T-25-06 | Tampering | A scoped run masking a build/placement bug (the v1.4 caprun-planner precedent) | medium | mitigate | Unscoped full-workspace `cargo build --workspace && cargo test --workspace --no-fail-fast` (the script default), which rebuilds all sibling binaries. |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — the verification container reuses pre-existing workspace dependencies only (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Bare `bash scripts/mailpit-verify.sh` — true exit code 0 (captured pre-pipe), PASSED sentinel present, 0 test-result-FAILED.
- Held-out test `slot_type_binding_swapped_subject_recipient_denies` present in the Linux log (ran, not skipped).
- Human milestone-close sign-off obtained (blocking checkpoint).
</verification>

<success_criteria>
The full v1.5 workspace regression is independently green on real Linux via the bare default recipe,
with the true pre-pipe exit code 0, the PASSED sentinel, 0 failures, and Plan 01's held-out test
proven to have run on Linux; a human has approved the v1.5 DONE milestone close.
</success_criteria>

## Artifacts this plan produces
- `.planning/phases/25-regression-live-proof/25-03-SUMMARY.md` (records the Linux gate evidence: exit code, sentinel, counts, held-out-test presence, human sign-off)
- No code artifacts — this plan runs and verifies only.

<output>
Create `.planning/phases/25-regression-live-proof/25-03-SUMMARY.md` when done.
</output>
