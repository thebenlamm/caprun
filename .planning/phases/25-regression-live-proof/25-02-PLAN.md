---
phase: 25-regression-live-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md
autonomous: true
requirements: [T2-07]
must_haves:
  truths:
    - "Every Mac-buildable direct-mint site (`.mint(` / `ValueRecord {`) that routes a value into a role-checked slot (to/cc/bcc/subject/body/path) has its origin_role cross-referenced against sink_sensitivity.rs's expected_role table and given a verdict (correct / needs-fix) (T2-07)"
    - "No existing fixture silently bypasses the new Step 1c role check by assigning origin_role None (or a wrong-and-unintended role) at a role-checked slot while still asserting the old permissive Allowed behavior (T2-07)"
    - "The full Mac workspace regression `cargo test --workspace --no-fail-fast` is green (0 FAILED), confirming no fixture is broken by the new check before the expensive Linux run (T2-07)"
  artifacts:
    - ".planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md exists and catalogs every direct-mint site with its routed slot, origin_role, and verdict — explicitly recording '0 needs-fix' if the re-sweep confirms no bypass"
  key_links:
    - "independent re-grep (find linux-gated files; grep direct-mint sites) -> per-site cross-reference against expected_role table -> documented verdict"
---

<objective>
Independently re-confirm Phase 24's ~40-fixture role-assignment sweep: audit every Mac-buildable
direct-mint test site for any fixture that would now silently bypass (or be silently broken by) the
Step 1c role check, catalog the findings durably, fix any (expected: zero), and prove the full Mac
workspace is green.

Purpose: T2-07 defends against "test-suite laundering" — a regression test that silently accepts a
role-mismatched fixture, masking a real bypass. Phase 24's SUMMARY self-reports the fixtures were
updated, but 24-01's own "Next Phase Readiness" section explicitly asks Phase 25 to CONFIRM this
independently, not rubber-stamp it. RESEARCH already narrowed the real search space: the 21
`#[cfg(target_os = "linux")]`-gated e2e files have ZERO direct-mint sites (they drive the real
`caprun` binary, whose production origin_role Phase 24 fixed), so the audit target is exactly the
Mac-buildable in-process files that construct `.mint(` / `ValueRecord {` fixtures directly.

Output: `.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md` — a per-site catalog with
verdicts. Any fixture fix (unexpected — RESEARCH predicts 0) is a TEST-fixture role assignment only,
never a production/TCB logic change; it lands in `crates/executor/tests/executor_decision.rs`,
`crates/brokerd/tests/durable_anchor.rs`, `crates/brokerd/tests/file_create.rs`, or the
`#[cfg(test)]` module of `crates/brokerd/src/quarantine.rs` — all disjoint from Plan 01's
`s9_acceptance.rs`. If any candidate fix would touch PRODUCTION logic, STOP and flag to the user
(this is a proof-only phase — no production code changes).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-regression-live-proof/25-RESEARCH.md
@.planning/phases/25-regression-live-proof/25-PATTERNS.md
@.planning/phases/24-slot-type-binding-enforcement/24-01-SUMMARY.md
@crates/executor/src/sink_sensitivity.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Independent re-grep + per-site cross-reference audit</name>
  <files>.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md</files>
  <read_first>
    - .planning/phases/25-regression-live-proof/25-RESEARCH.md "T2-07: Regression Audit" (lines 362-395) — the exact search methodology and the confirmed-narrow blind spot (5 Mac-buildable files, 0 in the 21 Linux-gated files). Re-run the searches independently; do not cite the prior session's counts as final.
    - .planning/phases/25-regression-live-proof/25-PATTERNS.md "T2-07 Regression Audit" (lines 118-134) — the target file set and re-run commands.
    - crates/executor/src/sink_sensitivity.rs `expected_role` (around lines 147-162) — the authoritative table: `to`/`cc`/`bcc` -> ["recipient","email_address"], `subject` -> ["subject"], `body` -> ["body","doc_fragment"], `path` -> the file.create routing role; every other arg is role-unconstrained (returns None -> Step 1c skips it).
    - .planning/phases/24-slot-type-binding-enforcement/24-01-SUMMARY.md — Phase 24 Plan 01's file list (the files it touched when threading origin_role); these are the primary re-confirm targets.
  </read_first>
  <action>
    Run two independent searches from the repo root and capture their raw output:
    (a) `find crates cli -name '*.rs' -not -path '*/target/*' | xargs grep -l 'cfg(target_os = "linux")'`
    then for each result test whether it constructs a direct mint fixture
    (`grep -q '\.mint(\|ValueRecord {' "$f"`) — RESEARCH expects 0 such Linux-gated files; record the
    actual result. (b) `grep -rn '\.mint(\|ValueRecord {' crates/ --include='*.rs' | grep -v '/target/'`
    — the Mac-buildable direct-mint search space.

    For EVERY hit from (b), determine (by reading the surrounding fixture) the PlanArg slot the minted
    value is routed into (if any) and the origin_role it is minted with. Cross-reference against
    `sink_sensitivity.rs`'s `expected_role` table: a mint routed into a role-checked slot
    (to/cc/bcc/subject/body/path) MUST carry an origin_role in that slot's expected list; a mint
    routed into an unconstrained slot (e.g. file.create `contents`, or any non-role-checked arg) is
    unaffected. A fixture is a BYPASS/BREAK RISK if it routes a value into a role-checked slot with
    origin_role None or a role not in the expected list AND still asserts the pre-Phase-24 permissive
    Allowed outcome.

    Write `.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md` cataloging every hit:
    file:line, the routed slot (or "not routed to a sink"), the origin_role, the expected-role list
    for that slot, and a verdict of CORRECT or NEEDS-FIX. Per this project's Runtime-State-Inventory
    discipline, explicitly record "0 NEEDS-FIX" if the re-sweep confirms no bypass — leaving it blank
    is not acceptable. Note Plan 01's two new `s9_acceptance.rs` tests are out of this audit's scope
    (they are Phase-25 additions, self-verified by Plan 01, not pre-existing fixtures).
  </action>
  <acceptance_criteria>
    - `.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md` exists and lists every `.mint(` / `ValueRecord {` site found by the re-grep, each with file:line, routed slot, origin_role, expected-role list, and a CORRECT/NEEDS-FIX verdict.
    - The audit records the actual counts from the independently-run searches (both the Linux-gated-file check and the Mac-buildable direct-mint grep), not cited from RESEARCH.
    - The audit explicitly states the NEEDS-FIX count (including "0" if none), never leaving it blank.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md && grep -qiE 'needs-fix|needs_fix' .planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md && grep -qE 'mint|ValueRecord' .planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md</automated>
  </verify>
  <done>A durable audit record catalogs every Mac-buildable direct-mint site with a per-site verdict, independently re-run (not cited), with the NEEDS-FIX count explicitly stated — defending T2-07 against silent test-suite laundering.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile any findings + prove the full Mac workspace green</name>
  <files>.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md</files>
  <read_first>
    - Task 1's output `.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md` — the NEEDS-FIX list (expected empty per RESEARCH).
    - .planning/phases/25-regression-live-proof/25-RESEARCH.md Pitfall 3 (lines 309-320) — do not trust Phase 24's "~40 fixtures updated" claim; the workspace-green re-run is the independent confirmation.
    - crates/executor/src/sink_sensitivity.rs `expected_role` — the correct role vocabulary to assign if a fixture genuinely needs fixing.
  </read_first>
  <action>
    For each NEEDS-FIX site in the audit (RESEARCH predicts zero), fix ONLY the test fixture: assign
    the correct origin_role for the slot it routes into, per `expected_role`. NEVER weaken or delete
    a security assertion to make a test pass; NEVER change production/TCB logic. If a fixture asserts
    the old permissive Allowed for a genuinely-misrouted value, the correct fix is to assign the value
    its true role (so the fixture reflects a legitimate routing) — not to assert the deny away. If any
    candidate fix would require touching production logic (anything outside a test file or a
    `#[cfg(test)]` module), STOP and flag to the user: this is a proof-only phase.

    Then run the full Mac workspace regression `cargo build --workspace && cargo test --workspace
    --no-fail-fast` and confirm 0 FAILED (the Linux-only security tests correctly report 0-passed on
    Mac by design — that is expected, not a failure). Append a "Reconciliation" section to
    25-REGRESSION-AUDIT.md recording: the fixes applied (or "none"), the exact `cargo test` summary
    line counts (test-result-ok blocks, 0 FAILED), and a final T2-07 verdict of PASS. This green Mac
    run is the per-wave-merge gate before Plan 03's expensive Linux run.
  </action>
  <acceptance_criteria>
    - Any NEEDS-FIX fixture from Task 1 is corrected by assigning the true origin_role (never by weakening an assertion), and no production/TCB file is modified.
    - `cargo test --workspace --no-fail-fast` exits 0 with 0 FAILED on Mac.
    - 25-REGRESSION-AUDIT.md has a "Reconciliation" section recording fixes applied (or "none"), the cargo test count summary, and a final T2-07 PASS verdict.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace --no-fail-fast 2>&1 | grep -qE 'test result: ok' && ! (cargo test --workspace --no-fail-fast 2>&1 | grep -qE 'test result: FAILED')</automated>
  </verify>
  <done>Any fixture bypass is fixed by correct role assignment (expected: none), the full Mac workspace is green with 0 failures, and the audit record carries a final T2-07 PASS verdict — no production code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → executor role check | A regression fixture that assigns origin_role None (or a wrong role) at a role-checked slot could silently accept a bypass, masking the real security proof. This audit inventories every such site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Repudiation | Test-suite laundering — a fixture silently accepts a role-mismatched value, repudiating the security proof | high | mitigate | Independent per-site re-grep + cross-reference against expected_role, durable catalog with explicit NEEDS-FIX count, and a full-workspace-green re-run that is NOT trusted from Phase 24's self-report. |
| T-25-04 | Tampering | A "fix" that weakens a security assertion to make a test pass | medium | mitigate | Task 2 hard rule: fixes assign the true role only, never delete/weaken an assertion; any production-logic touch STOPS and flags to the user (proof-only phase). |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — zero new dependencies (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Task 1 produces `25-REGRESSION-AUDIT.md` with a complete per-site catalog and an explicit NEEDS-FIX count.
- `cargo test --workspace --no-fail-fast` green on Mac (0 FAILED) after any reconciliation.
- Deeper Linux regression confirmation is Plan 03 (T2-08).
</verification>

<success_criteria>
Every Mac-buildable direct-mint site is independently audited and given a verdict; no fixture
silently bypasses or is broken by the Step 1c role check; the full Mac workspace is green; the audit
record durably proves T2-07 with a PASS verdict — no production code changed.
</success_criteria>

## Artifacts this plan produces
- `.planning/phases/25-regression-live-proof/25-REGRESSION-AUDIT.md` (new — per-site catalog + Reconciliation section + T2-07 verdict)
- (Conditional, expected none) role-assignment fixes in `crates/executor/tests/executor_decision.rs`, `crates/brokerd/tests/durable_anchor.rs`, `crates/brokerd/tests/file_create.rs`, or `crates/brokerd/src/quarantine.rs` `#[cfg(test)]` module — all disjoint from Plan 01.

<output>
Create `.planning/phases/25-regression-live-proof/25-02-SUMMARY.md` when done.
</output>