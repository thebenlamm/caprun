---
phase: 15-deterministic-doc-action-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/src/quarantine.rs
  - crates/runtime-core/src/event.rs
  - scripts/check-invariants.sh
autonomous: true
requirements: [EXTRACT-01, EXTRACT-03]
must_haves:
  truths:
    - "A transform-derived value (e.g. two concatenated doc fragments) is minted with taint = the union of its inputs' taint (never narrowed) and provenance_chain = the order-stable, deduplicated concatenation of its inputs' read-rooted provenance chains — its root is an originating file_read Event, never a fresh transform-local event."
    - "mint_from_derivation fails closed (returns Err, mints nothing) when given zero inputs, mirroring the existing empty-taint / empty-provenance guards — it can never silently fall back to a fresh-rooted mint."
    - "mint_from_read is byte-for-byte unchanged except for one additive claim_type arm (doc_fragment); derived values are never minted through mint_from_read."
    - "The confined-worker-callable extraction+transform helpers (multi-fragment scan, concatenation) are pure, deterministic, dependency-free functions defined beside the existing extract_email_claims pattern."
    - "doc_fragment has a NAMED, TESTABLE shape predicate (looks_like_doc_fragment) enforced AT THE MINT: a fully-assembled recipient (a token containing '@') arriving as a doc_fragment is a fail-closed mint error — so a worker CANNOT re-enter the concat OUTPUT through mint_from_read as a fresh single-element chain (finding #1a; DESIGN-confirm-binding.md:249-254 fresh-single-element-derived-chain-is-a-mint-error mandate)."
    - "mint_from_derivation fails closed when the union taint is untrusted but provenance_chain[0] does NOT resolve (via a session-scoped audit lookup) to a genuine file_read Event — a worker cannot pick the audited origin by choosing input ORDER (e.g. putting an intent_received-rooted UserTrusted handle first) (finding #3)."
    - "When any untrusted label is present in the input union, UserTrusted is DROPPED from the derived value's taint — a [UserTrusted, WorkerExtracted] vector is self-contradictory and would make a future taint.contains(&UserTrusted) predicate fail OPEN (finding #3)."
    - "The derivation Event records the derivation edge in its HASHED PAYLOAD (derived_value_id, input_value_ids, input_provenance_chains, transform_kind — each serde skip_serializing_if, riding in the hashed payload exactly as `anchors` does, NO DB migration), so the value-lineage edge V←{A,B} is durable and DB-alone checkable; Event.parent_id stays single-parent (two-graphs-never-share-edges preserved) (finding #2)."
    - "A check-invariants.sh grep gate restricts mint_from_read AND mint_from_derivation call sites to quarantine.rs + the single ReportClaims/ReportDerivedClaim dispatch arm in server.rs — any OTHER module minting a fresh-rooted ValueRecord fails the build (finding #1b; DESIGN-confirm-binding.md:266-278 moved out of future-work because Phase 15 makes it load-bearing)."
  artifacts:
    - "crates/brokerd/src/quarantine.rs::mint_from_derivation (new sibling of mint_from_read)"
    - "crates/brokerd/src/quarantine.rs::extract_doc_fragments + concat transform helper (new, worker-callable, hand-rolled)"
    - "crates/brokerd/src/quarantine.rs::looks_like_doc_fragment (new NAMED shape predicate, mirroring looks_like_email; rejects a token containing '@' as a raw doc_fragment)"
    - "crates/brokerd/src/quarantine.rs::mint_from_read new doc_fragment claim_type arm -> [ExternalUntrusted], guarded by looks_like_doc_fragment"
    - "crates/runtime-core/src/event.rs: Event derivation-payload fields (derived_value_id: Option<Uuid>, input_value_ids: Vec<ValueId>, input_provenance_chains: Vec<Vec<Uuid>>, transform_kind: Option<String>) each #[serde(default, skip_serializing_if)] + a new Event::derivation constructor (sibling of Event::sink_blocked)"
    - "the 'derivation' Event.event_type string (first use)"
    - "TaintLabel::WorkerExtracted first mint site (added by mint_from_derivation to the taint union)"
    - "scripts/check-invariants.sh new gate restricting mint_from_read/mint_from_derivation call sites"
  key_links:
    - "mint_from_derivation.provenance_chain[i] -> each equals an originating file_read Event id threaded from an input record (EVERY element is a file_read; a derivation event NEVER appears in provenance_chain — finding #10) — the unbroken-edge anchor EXTRACT-02 walks"
    - "mint_from_derivation appends a durable 'derivation' Event whose parent_id is the causal chain head (NOT its inputs); the multi-input value-lineage edge rides in the derivation Event's HASHED PAYLOAD (input_provenance_chains / derived_value_id), NOT in parent_id and NOT as a provenance_chain element (two-graphs-never-share-edges)"
---

<objective>
Build the provenance-threading mint primitive that closes the milestone's #1 laundering BLOCKER, plus the confined-worker-callable multi-fragment extraction and concatenation helpers that feed it.

Purpose: A transform (concatenating two doc fields into a recipient) must NOT re-anchor the derived value's lineage at a fresh event. Copying the taint LABEL forward while rooting the derived value at a brand-new event is "taint stapled on at the sink" — it satisfies the letter of "an unbroken edge exists" while proving nothing (CLAUDE.md hard constraint #1; DESIGN-confirm-binding.md "Provenance-Threading for Transform-Derived Mints", D-16). This plan makes a genuinely-threaded mint the only mint a derived value can pass through.

Output: `mint_from_derivation` (thread inputs' chains, fail closed on a re-anchored chain), a `doc_fragment` raw claim type, and hand-rolled fragment-scan + concat helpers. Consumed by Plan 02 (the DB-alone EXTRACT-02 proof) and Plan 03/04 (the live confined-worker path).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deterministic-doc-action-extraction/15-RESEARCH.md
@planning-docs/DESIGN-confirm-binding.md
@CLAUDE.md

# The exact code to extend (do NOT modify mint_from_read's body beyond the additive arm)
@crates/brokerd/src/quarantine.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the doc_fragment raw claim type and the confined-worker multi-fragment extraction + concat helpers</name>
  <files>crates/brokerd/src/quarantine.rs</files>
  <read_first>
    - crates/brokerd/src/quarantine.rs:44-145 (extract_email_claims, extract_relative_path_claims, looks_like_email/looks_like_relative_path — the exact hand-rolled pattern to mirror)
    - crates/brokerd/src/quarantine.rs:235-243 (mint_from_read's claim_type -> taint match; the fail-closed "unknown claim_type" arm)
    - crates/runtime-core/src/plan_node.rs:13-48 (TaintLabel variants + is_untrusted — confirm ExternalUntrusted and WorkerExtracted both return true)
  </read_first>
  <behavior>
    - looks_like_doc_fragment(s): a NAMED, TESTABLE shape predicate mirroring looks_like_email's crispness (finding #8). It returns true iff s is a non-empty raw doc-fragment token that is NOT a fully-assembled recipient — concretely, it MUST reject any token containing '@' (an assembled recipient/email is never a valid RAW doc_fragment; it may only exist as a mint_from_derivation OUTPUT, never re-entered as a raw fragment). This is the crisp predicate that replaces the vague "any doc-marked token the fixture needs".
    - extract_doc_fragments(raw): returns an ordered Vec of typed fragments found in the raw doc bytes by a deterministic, dependency-free scanner (no regex crate, no LLM), mirroring extract_email_claims' split_whitespace + trim_matches shape. Fragments are the marker-anchored recipient-half tokens the fixture carries on INDEPENDENTLY PLAUSIBLE lines (finding #9): the value following a `Reply-To:` marker and the value following a `Domain:` marker — each of which satisfies looks_like_doc_fragment (neither contains '@'; they only become a recipient AFTER the concat transform joins them with a literal '@'). Preserves source order; discards surrounding prose (lossy guarantee).
    - The concat transform helper joins the two already-extracted fragment strings into one recipient string by deterministic String concatenation with a literal '@' separator (local-half + "@" + domain-half) — no parsing, no library. It operates on already-extracted fragment VALUES, never re-reads bytes.
    - mint_from_read gains ONE additive arm: claim_type "doc_fragment" -> FIRST assert looks_like_doc_fragment(value) (fail-closed Err if it contains '@' — the finding #1a mint-time guard, so a concatenated recipient can never re-enter here as a fresh single-element chain), THEN taint vec![TaintLabel::ExternalUntrusted]. Every other line of mint_from_read is unchanged; the existing unknown-claim_type fail-closed arm is preserved (a still-unknown type still errors).
    - Test: looks_like_doc_fragment("accounts") == true, looks_like_doc_fragment("ev1l.com") == true, looks_like_doc_fragment("accounts@ev1l.com") == false (assembled recipient rejected), looks_like_doc_fragment("") == false.
    - Test: extract_doc_fragments over the intended `Reply-To:`/`Domain:` fixture returns the two expected ordered fragment values and discards surrounding prose (lossy guarantee).
    - Test: the concat helper joins the two fragments into the exact expected recipient literal (e.g. "accounts@ev1l.com").
    - Test: mint_from_read with claim_type "doc_fragment" and a valid fragment mints a ValueRecord whose taint is [ExternalUntrusted] and whose provenance_chain has length 1 rooted at the file_read event it just appended (single-source read semantics unchanged).
    - Test (finding #1a mint guard): mint_from_read with claim_type "doc_fragment" and a value containing '@' (the concatenated recipient) returns Err and mints nothing — the laundering path is closed AT THE MINT, not merely in a test walk.
    - Test: mint_from_read with a genuinely-unknown claim_type still returns Err (fail-closed arm intact).
  </behavior>
  <action>Extend crates/brokerd/src/quarantine.rs following its own hand-rolled scanner discipline (the file's doc comments state the regex-free, LLM-free choice is deliberate — do not introduce a regex or parser dependency). Add looks_like_doc_fragment as a named predicate beside looks_like_email/looks_like_relative_path with the crisp "non-empty AND does not contain '@'" shape (finding #8). Add extract_doc_fragments as a pure function returning the marker-anchored (`Reply-To:` / `Domain:`) fragments in source order, each satisfying looks_like_doc_fragment, and a concat transform helper that joins the already-extracted fragment values with a literal '@'. Fragment extraction and the transform are BOTH written here so the confined worker (Plan 04) can call them exactly as it already calls extract_email_claims — the transform MUST be able to run worker-side before any mint, per EXTRACT-01 and DESIGN-confirm-binding.md "Post-Transformation Bytes" (D-08). Add exactly one additive arm to mint_from_read's claim_type->taint match: doc_fragment FIRST enforces looks_like_doc_fragment (fail-closed Err on a token containing '@' — finding #1a) THEN yields [ExternalUntrusted] (a generic untrusted raw fragment carries no shape label like EmailRaw/PathRaw; ExternalUntrusted alone is non-empty and is_untrusted, sufficient to block downstream). Do not otherwise alter mint_from_read. Add the unit tests in the module's existing test section.</action>
  <verify>
    <automated>cargo test -p brokerd --lib quarantine</automated>
  </verify>
  <done>looks_like_doc_fragment + extract_doc_fragments + concat helper exist as pure, dependency-free functions with passing unit tests; mint_from_read's doc_fragment arm enforces looks_like_doc_fragment (a '@'-containing token fails closed at the mint) then yields [ExternalUntrusted]; the unknown-claim_type fail-closed arm still errors; no regex/parser dependency added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mint_from_derivation — the provenance-threading, fail-closed derived-value mint</name>
  <files>crates/brokerd/src/quarantine.rs</files>
  <read_first>
    - crates/brokerd/src/quarantine.rs:147-296 (mint_from_read — its atomicity discipline, append_event usage, chain-head return plumbing, and the SOLE-I1-TRUST-FLIP doc comment: mint_from_derivation MUST NOT duplicate the demotion)
    - crates/executor/src/value_store.rs:55-90 (ValueStore::mint — the EmptyTaint / EmptyProvenance fail-closed guards to mirror)
    - crates/runtime-core/src/value_record.rs:21-31 (ValueRecord fields: taint, provenance_chain)
    - crates/runtime-core/src/event.rs:17-100 (Event struct: {id,parent_id,session_id,actor,event_type,timestamp,taint,anchors}; Event::new; the Event::sink_blocked constructor + the `anchors` #[serde(default, skip_serializing_if = "Vec::is_empty")] payload pattern and the GOLDEN byte-fixture test — the derivation-payload fields MUST follow this exact skip_serializing_if pattern so pre-derivation events stay byte-identical)
    - crates/runtime-core/src/plan_node.rs (ValueId type — the derivation payload's derived_value_id/input_value_ids field types; TaintLabel::UserTrusted + is_untrusted)
    - crates/brokerd/src/audit.rs:287-320 (find_event_by_type query shape — mint_from_derivation's file_read-root guard needs a session-scoped SELECT event_type WHERE id = provenance_chain[0]; use an inline query of this exact shape, do NOT depend on Plan 02's find_event_by_id since this is Wave 1)
    - planning-docs/DESIGN-confirm-binding.md:241-264 "Provenance-Threading for Transform-Derived Mints" (the MUSTs, incl. the fresh-single-element-chain-is-a-mint-error mandate at 249-254 and the explicit-DAG-derivation-edge option at 243-244) and "Post-Transformation Bytes" (D-08)
    - 15-RESEARCH.md Pattern 2 (two-graphs-never-share-edges) and the illustrative mint_from_derivation shape
  </read_first>
  <behavior>
    - FIRST extend crates/runtime-core/src/event.rs: add four derivation-payload fields to Event, each #[serde(default, skip_serializing_if)] so pre-derivation events serialize byte-identically (the golden byte-fixture test at event.rs:112-148 MUST still pass — verify it does):
        * derived_value_id: Option<ValueId> (skip_serializing_if = "Option::is_none")
        * input_value_ids: Vec<ValueId> (skip_serializing_if = "Vec::is_empty")
        * input_provenance_chains: Vec<Vec<Uuid>> (skip_serializing_if = "Vec::is_empty")
        * transform_kind: Option<String> (skip_serializing_if = "Option::is_none")
      Add an Event::derivation(id, parent_id, session_id, timestamp, taint, derived_value_id, input_value_ids, input_provenance_chains, transform_kind) constructor (sibling of Event::sink_blocked), and set anchors: vec![] plus the derivation fields. Event::new keeps setting all four derivation fields to their empty defaults so every existing call site is unchanged. (This fixes finding #2: the old plan wrote transform_kind to a field that did not exist.)
    - mint_from_derivation(conn, store, session_id, transformed_literal, inputs: &[&ValueRecord], transform_kind, parent_id, parent_hash) mints a derived ValueRecord and appends a durable "derivation" Event in one atomic call, mirroring mint_from_read's discipline.
    - taint = the union of every input's taint (order-stable, deduplicated, never narrowed), PLUS TaintLabel::WorkerExtracted appended (its first mint site). Union monotonicity per D-16 point 1. THEN (finding #3): if the union contains ANY untrusted label, DROP TaintLabel::UserTrusted from the union — a [UserTrusted, WorkerExtracted] vector is self-contradictory and any future taint.contains(&UserTrusted) predicate would fail OPEN; document this at the drop site.
    - provenance_chain = the order-stable, deduplicated concatenation of every input's provenance_chain. EVERY element is a file_read event id; a derivation event NEVER appears in provenance_chain (finding #10).
    - GUARD (finding #3, asserted AT THE MINT): when the union taint is_untrusted, provenance_chain[0] MUST resolve via a session-scoped audit lookup (inline SELECT event_type WHERE id = ?1 AND session_id = ?2) to an event whose event_type == "file_read". If it resolves to anything else (e.g. an intent_received event, because a worker put a UserTrusted intent handle first in input order) OR does not resolve, return a fail-closed Err and mint nothing. This closes the "attacker picks the audited origin by choosing input ORDER" hole — durable_anchor.rs's §9 read_event_id = provenance_chain[0] can no longer attest a trusted intent_received event.
    - GUARD (finding #1/D-16): fresh single-element re-anchored chain — because inputs are &ValueRecord already read-rooted, and the file_read-root guard above holds, a derived value whose chain is a single fresh non-file_read root is rejected. Combined with looks_like_doc_fragment (Task 1) the concat output cannot re-enter via mint_from_read either.
    - A durable "derivation" Event is appended via Event::derivation (event_type "derivation"), parent_id = the causal chain head passed in (NOT any input read), taint = the union taint, AND its hashed payload carries derived_value_id = the minted value_id, input_value_ids = the inputs' ValueIds, input_provenance_chains = each input's provenance_chain, transform_kind = the transform tag. This IS the durable derivation edge (DESIGN-confirm-binding.md:243-244 explicit-DAG-derivation-edge option), DB-alone checkable by EXTRACT-02 (Plan 02) — the value-lineage relationship rides in the payload, NEVER in Event.parent_id (two-graphs-never-share-edges).
    - Fail-closed: inputs.is_empty() -> Err (a derived value must thread >=1 input's provenance; never fresh-root). Also propagate ValueStore::mint's EmptyTaint/EmptyProvenance Err rather than masking it.
    - mint_from_derivation MUST NOT demote the session (it is NOT an I1 trust-flip site; inputs were already demoted by their own mint_from_read). It only appends the derivation event and mints.
    - Returns the derivation Event id + hash + minted value_id + chain-head plumbing so callers can thread last_event_id/last_event_hash onward (mirroring mint_from_read's return contract).
    - Test (byte-identity): the event.rs golden byte-fixture test still passes (a non-derivation Event serializes with no derivation keys).
    - Test (payload): the appended derivation Event's payload carries derived_value_id == minted value_id, input_value_ids == the inputs' ids, input_provenance_chains == the inputs' chains, transform_kind == the tag; round-trips.
    - Test (threading): given two inputs each with provenance_chain [read_a] and [read_b] and taint [ExternalUntrusted], the derived record has provenance_chain [read_a, read_b] and taint containing ExternalUntrusted and WorkerExtracted; a "derivation" event exists in the DAG.
    - Test (drop UserTrusted): an input set mixing a [UserTrusted] record and an [ExternalUntrusted] record — where the untrusted record's read is placed at index 0 — yields a union that contains ExternalUntrusted + WorkerExtracted but NOT UserTrusted.
    - Test (file_read-root guard): an input set whose index-0 input is intent_received-rooted (UserTrusted) and index-1 is file_read-rooted, with an overall untrusted union, is REJECTED (Err) because provenance_chain[0] resolves to a non-file_read event.
    - Test (dedup / order): overlapping input chains dedup order-stably; provenance_chain[0] is an input read id, not the derivation event id.
    - Test (fail-closed): zero inputs -> Err, and no event/record is persisted.
    - Test (no re-anchor): the derived record's provenance_chain[0] is NOT the derivation event's own id (proves lineage is threaded, not re-rooted at the transform).
    - Test (no demotion side effect): calling mint_from_derivation does not change session status (only mint_from_read flips I1).
  </behavior>
  <action>First extend crates/runtime-core/src/event.rs with the four skip_serializing_if derivation-payload fields and the Event::derivation constructor as described in behavior (keep Event::new setting them to empty defaults; keep the golden byte-fixture test passing — this is a pure runtime-core type change, no I/O, check-invariants Gate 2 stays green). Then add mint_from_derivation as a sibling to mint_from_read in crates/brokerd/src/quarantine.rs, following the exact "append event + mint record in one call" atomicity mint_from_read uses. Take the already-minted input ValueRecords by reference (the caller resolves ValueIds to records before calling — the worker computed the transformed literal, the broker threads provenance). Build taint as the deduplicated union of inputs' taint plus WorkerExtracted, THEN drop UserTrusted when any untrusted label is present (finding #3, documented). Build provenance_chain as the deduplicated, order-stable concatenation of inputs' provenance_chains. Enforce the file_read-root guard (finding #3): when the union is_untrusted, run an inline session-scoped SELECT event_type WHERE id = provenance_chain[0] AND session_id = ?; fail closed unless it is "file_read". Reject inputs.is_empty() with a fail-closed Err. Append the durable derivation Event via Event::derivation with parent_id = the causal chain head argument and the hashed payload (derived_value_id/input_value_ids/input_provenance_chains/transform_kind) carrying the multi-input value-lineage — do NOT encode input parents in Event.parent_id (single-parent by design; Pattern 2) and do NOT put a derivation event into any provenance_chain (finding #10). Do NOT demote the session and do NOT call update_session_status — mint_from_read remains the sole I1 flip site. Do not re-apply the transform broker-side (the worker already applied it; re-applying it here would put hostile-byte manipulation in the control plane, the EXTRACT-01 violation). Return the derivation event id, its hash, the minted value_id, and the chain-head plumbing mirroring mint_from_read. Add the unit tests described in behavior.</action>
  <verify>
    <automated>cargo test -p brokerd --lib quarantine && cargo test -p runtime-core --lib event</automated>
  </verify>
  <done>Event carries the four skip_serializing_if derivation-payload fields + Event::derivation constructor with the golden byte-fixture still passing; mint_from_derivation threads inputs' chains (union taint incl. WorkerExtracted, UserTrusted dropped under untrusted union, concat+dedup provenance), enforces the file_read-root guard on an untrusted union, appends a durable derivation event whose HASHED PAYLOAD carries the derived/input value ids + input_provenance_chains + transform_kind, fails closed on zero inputs, never demotes the session, and never roots the derived value at its own derivation event — all proven by passing unit tests including the no-re-anchor, drop-UserTrusted, and file_read-root-guard assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Add the check-invariants.sh mint-call-site grep gate (moved out of future-work, finding #1b)</name>
  <files>scripts/check-invariants.sh</files>
  <read_first>
    - scripts/check-invariants.sh:7-35 (Gate 1 — the exact EffectRequest token-ban shape: grep -r under crates/, filter planner-discipline-allow, fail on any unannotated hit — the pattern to mirror)
    - planning-docs/DESIGN-confirm-binding.md:266-278 (the future check-invariants gate spec — Phase 15 is the phase that makes it load-bearing, so it moves out of future-work NOW)
    - crates/brokerd/src/quarantine.rs (the definition + tests of mint_from_read/mint_from_derivation — the SOLE legitimate locus) and crates/brokerd/src/server.rs (the single ReportClaims/ReportDerivedClaim dispatch arm that calls them on the live path)
  </read_first>
  <action>Add a new gate to scripts/check-invariants.sh mirroring Gate 1's EffectRequest token-ban shape: the tokens `mint_from_read` and `mint_from_derivation` MUST NOT appear under crates/ or cli/ outside crates/brokerd/src/quarantine.rs (their definition + unit tests) and crates/brokerd/src/server.rs (the sole dispatch call sites). Use the same grep-and-filter discipline as Gate 1 (grep -rn the token across the source tree, exclude the two allowed files, and any other hit fails the build), and allow an inline `<!-- planner-discipline-allow: mint_from_read -->` / `mint_from_derivation` annotation for any intentional mention (e.g. doc comments in other files), exactly as the EffectRequest gate does. This gives the "do not fabricate a fresh provenance root" rule a mechanical backstop: any NEW module attempting to mint a fresh-rooted ValueRecord fails ./scripts/check-invariants.sh. IMPORTANT: this modifies scripts/check-invariants.sh (a gate the DESIGN doc explicitly said a LATER phase would add — Phase 15 IS that phase because it makes the laundering path live); it does NOT modify any DESIGN doc.</action>
  <verify>
    <automated>./scripts/check-invariants.sh</automated>
  </verify>
  <done>check-invariants.sh has a new gate restricting mint_from_read/mint_from_derivation call sites to quarantine.rs + server.rs (with a planner-discipline-allow escape hatch mirroring Gate 1); the full workspace passes the gate; a hand-test hit in a third file would fail it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined worker -> broker mint | The worker supplies an already-transformed literal + input handles; the broker mints. The transformed bytes are untrusted (tainted) and will be blocked; the broker must not trust their provenance except as threaded from the inputs it holds. |
| raw doc bytes -> extraction helpers | Hostile bytes flow into extract_doc_fragments / concat; a parser bug here is attack surface, so it stays a hand-rolled, dependency-free scanner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Spoofing (of provenance) | mint_from_derivation | critical | mitigate | Laundering is closed by FOUR mint-time guards, NOT "by construction" (finding #1): (a) provenance_chain is the concat of inputs' read-rooted chains; (b) an untrusted union whose provenance_chain[0] is not a file_read event fails closed (blocks worker-chosen input ORDER, finding #3); (c) zero inputs fail closed; (d) looks_like_doc_fragment rejects a '@'-containing token at mint_from_read so the concat OUTPUT cannot re-enter as a fresh single-element chain (finding #1a; D-16 fresh-single-element-chain-is-a-mint-error). The check-invariants gate (Task 3) mechanically bars fresh-rooted mints from any other module. |
| T-15-02 | Tampering | mint_from_read | high | mitigate | mint_from_read is unchanged except an additive claim_type arm GUARDED by looks_like_doc_fragment; derived values never mint through it, and an assembled recipient reported as a raw doc_fragment fails closed. |
| T-15-03 | Elevation of Privilege | extraction/transform helpers | high | mitigate | Extraction + concat are pure, hand-rolled, dependency-free (no regex/parser lib); the broker never re-applies the transform. |
| T-15-04 | Tampering | derivation Event linkage | medium | mitigate | The derivation event's multi-input lineage rides only in provenance_chain (value-lineage graph), never conflated into single-parent Event.parent_id (two-graphs-never-share-edges). |
</threat_model>

<verification>
- `cargo test -p brokerd --lib quarantine` green (extraction, looks_like_doc_fragment, concat, doc_fragment mint + '@'-token mint guard, mint_from_derivation threading + drop-UserTrusted + file_read-root guard + fail-closed + no-re-anchor + no-demotion).
- `cargo test -p runtime-core --lib event` green (derivation-payload fields round-trip; golden byte-fixture for non-derivation events still byte-identical — no DB migration).
- `cargo test --workspace --no-fail-fast` green (no regression in mint_from_read / Event::new consumers).
- `./scripts/check-invariants.sh` passes, INCLUDING the new mint-call-site gate (Task 3) — no EffectRequest token, runtime-core purity (Gate 2) intact despite the additive Event fields.
- No new Cargo dependency added (concat transform is String-only; base64-decode variant is explicitly deferred — see success_criteria).
</verification>

<success_criteria>
- mint_from_derivation exists, threads provenance, fails closed on zero inputs, adds WorkerExtracted, drops UserTrusted under an untrusted union, enforces the file_read-root guard on an untrusted union, does not demote — proven by unit tests including a no-re-anchor, a zero-input rejection, a drop-UserTrusted, and a file_read-root-guard test.
- Event carries the four skip_serializing_if derivation-payload fields + Event::derivation constructor; the golden byte-fixture proves pre-derivation events stay byte-identical (no DB migration).
- looks_like_doc_fragment + doc_fragment claim type + extraction/concat helpers exist and are pure/dependency-free; an assembled recipient reported as a raw doc_fragment fails closed at the mint.
- The check-invariants.sh mint-call-site gate is live (moved out of DESIGN's future-work), restricting mint_from_read/mint_from_derivation to quarantine.rs + server.rs.
- EXTRACT-03 scope decision (documented, not a silent drop): Phase 15 satisfies EXTRACT-03's transform requirement with the CONCAT transform (zero new dependencies). The base64-decode transform variant is deferred as an optional bonus for a later pass because it would add the `base64` direct dependency (and its legitimacy checkpoint) without strengthening the EXTRACT-02 proof, which is transform-shape-agnostic. Concat alone fully satisfies "TRANSFORMS the tainted value before the sink."
</success_criteria>

<output>
Create `.planning/phases/15-deterministic-doc-action-extraction/15-01-SUMMARY.md` when done.
</output>
