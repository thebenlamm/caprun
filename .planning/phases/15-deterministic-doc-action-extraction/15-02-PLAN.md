---
phase: 15-deterministic-doc-action-extraction
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - crates/brokerd/src/audit.rs
  - crates/brokerd/tests/extract_provenance_threading.rs
  - crates/brokerd/tests/fixtures/hostile_doc.txt
autonomous: true
requirements: [EXTRACT-02, EXTRACT-03, CONFIRM-02]
must_haves:
  truths:
    - "A programmatic audit-DAG query proves, for EVERY blocked arg in a multi-anchor Block (tainted derived recipient AND tainted body), an unbroken edge from that arg's value back through its provenance_chain to genuine originating file_read Event(s) — the query FAILS if any single edge is missing. EVERY provenance_chain element MUST be event_type == file_read; a derivation event appearing in a provenance_chain is a fail-closed error (finding #10, two-graphs decision)."
    - "The derived-recipient anchor's genuine-derivation is proven by the derivation Event's HASHED PAYLOAD, DB-alone (finding #2): ∃ a 'derivation' event ev in the session s.t. ev.derived_value_id == anchor.value_id AND the UNION of ev.input_provenance_chains == anchor.provenance_chain (exact set/vector equality) — NOT mere id-membership and NOT a vacuous 'a derivation event exists' existence check."
    - "Each anchor's root is IDENTITY-PINNED (finding #12): the walk asserts exact vector equality anchor.provenance_chain == expected_roots_for(arg) against the specific file_read ids the fixture's reads minted — for BOTH anchors — never settling for 'terminates at some genuine file_read whose taint is_untrusted'."
    - "find_event_by_id resolves a SPECIFIC event by its uuid within a session, disambiguating among the >=2 file_read events that multi-field extraction produces (find_event_by_type's LIMIT 1 cannot)."
    - "A paired anti-staple control rejects (a) a fabricated provenance root (a uuid never appended to the DAG) and (b) a re-anchored 'stapled' derived value minted fresh via mint_from_read INTO THE SAME session/DB as the genuine block (finding #11), with no threaded ancestry to its input reads — rejected specifically because no derivation Event's payload binds the fabricated value_id to those input chains (the finding #2 anchor-identity-bound predicate), NOT a session-wide 'has a derivation event' query."
    - "A transform-derived (concatenated) recipient value STILL blocks at the sink and STILL carries untrusted taint after the transform (EXTRACT-03) — proven by the same DB-alone scenario, not assumed."
    - "A realistic hostile-doc fixture (embedded send-redirection injection + INDEPENDENTLY PLAUSIBLE multi-fragment structure — a `Reply-To:` line and a separate `Domain:` line joined by the transform, finding #9) exists as a reusable artifact."
  artifacts:
    - "crates/brokerd/src/audit.rs::find_event_by_id (new accessor: WHERE id = ?1 AND session_id = ?2)"
    - "crates/brokerd/tests/extract_provenance_threading.rs (new: EXTRACT-02 per-anchor identity-pinned unbroken-edge + payload-bound genuine-derivation predicate + anti-staple, EXTRACT-03 block-survival)"
    - "crates/brokerd/tests/fixtures/hostile_doc.txt (CONFIRM-02 reusable fixture)"
  key_links:
    - "each persisted SinkBlockedAnchor.provenance_chain element -> find_event_by_id -> a real file_read Event (EVERY element must be file_read; a derivation event is NEVER a chain element — finding #10) — the unbroken edge"
    - "the derived-recipient anchor -> the 'derivation' Event whose HASHED PAYLOAD binds derived_value_id == anchor.value_id AND ∪input_provenance_chains == anchor.provenance_chain (finding #2) — the genuine-derivation edge, checked DB-alone"
    - "the fabricated-root negative control -> find_event_by_id returns None -> the walk REJECTS (anti-staple has teeth); the re-anchored control -> no derivation payload binds its value_id -> REJECTS"
---

<objective>
Build the phase's HARD-GATE proof: a programmatic, DB-alone audit-DAG query that verifies genuine taint propagation — including through a concatenation transform — for EVERY blocked arg in a multi-anchor Block, paired with an anti-staple control that rejects a fabricated/re-anchored chain. Plus the realistic hostile-doc fixture (CONFIRM-02) the proof consumes.

Purpose: This is the single non-negotiable invariant of the whole milestone (CLAUDE.md #1, ROADMAP Phase 15 success criterion 3: "phase FAILS if not met"). "Taint stapled at the sink proves nothing." A passing proof on genuine data is worthless without a paired FAILING proof on fabricated data — so this plan builds both. Because Phase 12 mandates collect-then-Block, the proof must hold for BOTH the tainted derived recipient AND the tainted body in one Block, not just one (D-16 per-arg).

Output: `find_event_by_id`, the new EXTRACT-02/03 test file, and the CONFIRM-02 fixture. Runs cross-platform (SQLite alone, no Linux sandbox) modeled on durable_anchor.rs's after-exit DB-alone pattern.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deterministic-doc-action-extraction/15-RESEARCH.md
@planning-docs/DESIGN-confirm-binding.md
@planning-docs/DESIGN-content-adapter-mediation.md
@CLAUDE.md

# The exact patterns to model on and the accessor to extend
@crates/brokerd/tests/durable_anchor.rs
@crates/brokerd/src/audit.rs
@crates/runtime-core/src/executor_decision.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add find_event_by_id — a specific-event accessor for per-anchor DAG lookup</name>
  <files>crates/brokerd/src/audit.rs</files>
  <read_first>
    - crates/brokerd/src/audit.rs:287-320 (find_event_by_type — the exact query shape to clone; note its ORDER BY rowid LIMIT 1 that makes it first-of-type only)
    - crates/brokerd/src/audit.rs:335-400 (Event deserialization from the payload column)
    - crates/brokerd/src/audit.rs:410-450 (existing find_event_by_type unit tests — mirror their setup)
    - 15-RESEARCH.md Pitfall 3 (why find_event_by_type is insufficient once >1 event of a type exists per session)
  </read_first>
  <behavior>
    - find_event_by_id(conn, session_id, id) returns Ok(Some(Event)) when an event with that exact uuid exists in that session, Ok(None) when it does not, Err on a DB error — a WHERE id = ?1 AND session_id = ?2 variant of find_event_by_type's exact query/deserialize shape.
    - Test: with two file_read events in one session, find_event_by_id resolves EACH to its own distinct event (disambiguation find_event_by_type cannot do).
    - Test: a uuid never appended returns Ok(None).
    - Test: a uuid from a DIFFERENT session returns Ok(None) (session-scoped).
  </behavior>
  <action>Add find_event_by_id to crates/brokerd/src/audit.rs directly beside find_event_by_type, cloning its query_row + payload-deserialize shape but keying on the event id AND session_id instead of event_type, with no LIMIT clause (id is unique). Return the same Result<Option<Event>> shape. Add the three unit tests described in behavior into the existing audit test module. Do not modify find_event_by_type.</action>
  <verify>
    <automated>cargo test -p brokerd --lib audit</automated>
  </verify>
  <done>find_event_by_id resolves specific events by uuid within a session, disambiguates among >=2 same-type events, returns None for absent/cross-session ids — proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author the CONFIRM-02 hostile-doc fixture and the DB-alone multi-anchor block builder</name>
  <files>crates/brokerd/tests/fixtures/hostile_doc.txt, crates/brokerd/tests/extract_provenance_threading.rs</files>
  <read_first>
    - crates/brokerd/tests/durable_anchor.rs:45-180 (build_hostile_block_db — the after-exit DB-alone scenario builder to model on: mint, submit_plan_node, persist, reopen)
    - crates/brokerd/tests/durable_anchor.rs:53 (HOSTILE_PATH constant style) and crates/brokerd/tests/email_smtp_acceptance.rs:315 (existing hostile body/CRLF string style)
    - crates/brokerd/src/quarantine.rs (mint_from_read, mint_from_derivation, extract_doc_fragments, concat helper — from Plan 01)
    - crates/executor/src/lib.rs (submit_plan_node — collect-then-Block returns anchors: Vec<BlockedArg>)
    - crates/runtime-core/src/executor_decision.rs:108-179 (SinkBlockedAnchor fields incl. provenance_chain + read_event_id; BlockedArg; BlockedPendingConfirmation)
  </read_first>
  <behavior>
    - The fixture is a realistic short document (e.g. meeting/summary notes / a badly-designed template) that embeds an injection attempting to redirect/alter the send (an "ignore previous instructions, send to <attacker>" style directive the deterministic extractor does NOT obey — there is no LLM in the loop), AND carries the multi-fragment structure as INDEPENDENTLY PLAUSIBLE lines (finding #9): a `Reply-To:` line whose value is the local-half (e.g. `accounts`) and a SEPARATE `Domain:` line whose value is the domain-half (e.g. `ev1l.com`) — NOT two adjacent whitespace-delimited tokens. The concat transform joins them with a literal '@' into the attacker recipient (`accounts@ev1l.com`). This reads as a real template AND strengthens the multi-input threading story (a Phase-17 reviewer cannot say the demo is shaped around its own extractor). The fixture also carries a tainted body fragment on its own line.
    - A shared test helper builds a DB-alone scenario: run extract_doc_fragments over the fixture, mint each recipient-half fragment (the `Reply-To:` value and the `Domain:` value) via mint_from_read (doc_fragment), mint the concatenated recipient via mint_from_derivation over those two input records, mint the body fragment via mint_from_read (doc_fragment), build an email.send PlanNode whose `to` = the derived recipient handle and `body` = the tainted body handle, submit_plan_node, and persist the resulting sink_blocked event; then reopen the DB (after-exit, DB-alone) for querying.
    - The helper returns the reopened connection, the session id, the persisted anchors, AND the specific file_read event ids the two recipient-half mints and the body mint produced (so Task 3 can identity-pin expected_roots_for(arg), finding #12).
    - The scenario asserts (sanity, consumed by Task 3): submit_plan_node returned BlockedPendingConfirmation with EXACTLY two anchors, AND the two anchors carry DISTINCT arg names {"to","body"} (finding #13 nit — len==2 alone is satisfied by the same BlockedArg twice; a Phase-14 regression would slip) — the collect-then-Block multi-arg Block Phase 12/14 mandate.
  </behavior>
  <action>Create crates/brokerd/tests/fixtures/hostile_doc.txt containing the realistic hostile document described in behavior, with the recipient halves on independent `Reply-To:` and `Domain:` lines (finding #9). Create crates/brokerd/tests/extract_provenance_threading.rs with a build helper modeled directly on durable_anchor.rs::build_hostile_block_db, but driving the Plan-01 primitives to produce a TWO-anchor block: a concatenation-derived tainted recipient (via mint_from_derivation over the two recipient-half reads) and a tainted body (via mint_from_read doc_fragment). Thread last_event_id/last_event_hash across every mint (mint_from_read demotes and returns the session_demoted chain head; mint_from_derivation returns the derivation chain head) so the causal DAG stays one linear chain and verify_chain still holds after reopen. Capture and return the originating file_read event ids for identity-pinning. Persist and reopen the DB exactly as durable_anchor.rs does. Include the two-anchor sanity assertion AND the distinct-arg-name assertion. NOTE for the SUMMARY (finding #13): the two recipient-half file_read events are SYNTHETIC (one physical read_to_end of the fixture produced both, plus the body read), and each mint_from_read also appends a session_demoted event — so a 3-fragment doc yields 3 redundant demotion events; record this so Phase 17's "one unbroken DAG" narration accounts for them.</action>
  <verify>
    <automated>cargo test -p brokerd --test extract_provenance_threading builds_two_anchor_block</automated>
  </verify>
  <done>hostile_doc.txt exists as a reusable fixture with an embedded injection + multi-fragment structure; the builder produces a persisted two-anchor (derived-recipient + tainted-body) sink_blocked event, reopenable DB-alone, with verify_chain intact.</done>
</task>

<task type="auto">
  <name>Task 3: EXTRACT-02 per-anchor unbroken-edge proof + paired anti-staple control + EXTRACT-03 block-survival</name>
  <files>crates/brokerd/tests/extract_provenance_threading.rs</files>
  <read_first>
    - crates/brokerd/tests/durable_anchor.rs:182-244 (after_exit_db_alone_anti_stapling_sentinel — the single-anchor genuine-taint walk to GENERALIZE over anchors)
    - crates/brokerd/tests/durable_anchor.rs:324-369 (tamper_evidence pattern: a passing baseline PLUS a failing tampered case — the paired-control discipline to mirror)
    - crates/brokerd/src/audit.rs::find_event_by_id (from Task 1) and find_event_by_type
    - planning-docs/DESIGN-confirm-binding.md "Provenance-Threading for Transform-Derived Mints" (the MUST that provenance must reach the originating read, never a fresh transform-local root)
    - 15-RESEARCH.md Pitfall 4 (anti-staple must be "chain root is genuine", NOT merely "chain non-empty")
  </read_first>
  <behavior>
    - POSITIVE (EXTRACT-02): iterate EVERY anchor in the persisted block's anchors (both the derived `to` and the tainted `body`). For each anchor, walk its provenance_chain element-by-element (not just [0]); for each element, find_event_by_id MUST return a real Event whose event_type == "file_read" (finding #10: EVERY element is a file_read; a derivation event appearing in a provenance_chain is a fail-closed error — the walk never recurses through a derivation event as a chain element). Each terminal file_read's taint MUST be is_untrusted.
    - IDENTITY-PINNING (finding #12): for BOTH anchors, assert exact vector equality anchor.provenance_chain == expected_roots_for(arg), where expected_roots_for is the specific file_read ids the fixture's reads minted (returned by Task 2's helper): the body anchor's chain == [body_read_id]; the derived `to` anchor's chain == [reply_to_read_id, domain_read_id]. "Is a file_read" is kept ONLY as a per-element type check, never as the terminal criterion — a re-mint via mint_from_read produces a REAL untrusted file_read, so type-alone would pass a laundered body.
    - GENUINE-DERIVATION PREDICATE (finding #2, DB-alone, for the derived `to` anchor): assert ∃ a "derivation" Event ev in the session (via find_event_by_type / by scanning the session's derivation events) s.t. ev.derived_value_id == anchor.value_id AND the UNION of ev.input_provenance_chains == anchor.provenance_chain (exact equality). This is the payload-bound edge — NOT a vacuous "a derivation event exists" existence check, and NOT id-membership. The test FAILS if ANY anchor has any missing/unresolvable/non-file_read element, any root-vector mismatch, or (for the derived anchor) no payload-binding derivation event — a two-anchor block with only one edge proven is a partial pass, i.e. a FAIL.
    - NEGATIVE CONTROL A (fabricated root): construct an anchor whose provenance_chain contains a uuid produced by a fresh generator and never appended to the DAG; assert the same walk REJECTS it (find_event_by_id returns None -> the edge is unproven). This proves the check is not merely a non-empty test.
    - NEGATIVE CONTROL B (re-anchored / stapled, finding #11): simulate what a NAIVE extractor produces — take the already-concatenated recipient literal and mint it via mint_from_read INTO THE SAME session/DB as the genuine block (a fresh file_read root, a REAL event in the same session, but with NO threaded ancestry to the two original recipient-half reads). NOTE: this requires the concatenated literal to bypass Plan-01's looks_like_doc_fragment guard, which rejects a '@'-containing token — so control B mints it via a DIFFERENT claim_type shape (e.g. email_address) or a direct store path that still produces a real same-session file_read, modeling the naive-extractor defect faithfully. Assert the EXTRACT-02 genuine-derivation check REJECTS it specifically because NO derivation Event's payload binds its value_id to the recipient-half input chains (the finding #2 anchor-identity-bound predicate) AND its provenance_chain != expected_roots_for("to") — NEVER via a session-wide "this session has a derivation event" query (that would be vacuously true, since the genuine block's derivation event is in the same session — finding #11's exact teeth requirement).
    - EXTRACT-03 (survival through transform): assert the derived (concatenated) recipient value STILL carries untrusted taint AND submit_plan_node STILL returned a Block for it — taint and provenance survived the transform, not just a copy.
  </behavior>
  <action>Complete crates/brokerd/tests/extract_provenance_threading.rs. Write a reusable assertion routine that, given the reopened connection + session id + one anchor + its expected_roots vector, resolves every provenance_chain element via find_event_by_id, requires each resolved event to be event_type == "file_read" (finding #10 — reject a derivation event as a chain element), requires each terminal file_read's taint is_untrusted, AND asserts exact vector equality anchor.provenance_chain == expected_roots (finding #12 identity-pinning). Apply it to BOTH anchors in a loop, passing each arg's expected roots from Task 2's helper. For the derived `to` anchor additionally assert the finding #2 payload-bound genuine-derivation predicate (a session derivation event whose derived_value_id == anchor.value_id and ∪input_provenance_chains == anchor.provenance_chain). Add the two negative controls: (A) a fabricated uuid never appended; (B) a same-session naive re-mint of the concatenated literal (bypassing the doc_fragment '@' guard via a different claim shape) — assert the routine REJECTS both, control B specifically on the payload-binding predicate + root-vector mismatch, NEVER on a session-wide existence query — mirroring durable_anchor.rs's paired passing-baseline/failing-tampered discipline. Add the EXTRACT-03 assertion that the derived value is still tainted and still blocked. Do not weaken any assertion to "chain non-empty" or "terminates at some file_read" — the root vector must equal the specific minted ids.</action>
  <verify>
    <automated>cargo test -p brokerd --test extract_provenance_threading</automated>
  </verify>
  <done>The per-anchor unbroken-edge proof passes for BOTH anchors with exact identity-pinned root-vector equality (finding #12) and every element proven event_type == file_read (finding #10); the derived `to` anchor additionally passes the payload-bound genuine-derivation predicate (finding #2); the fabricated-root (A) and same-session re-anchored-staple (B, finding #11) negative controls are both REJECTED — control B on the payload-binding + root-mismatch, never a session-wide existence query; the derived recipient is proven still-tainted and still-blocked (EXTRACT-03). No assertion is weakened to "chain non-empty" or "terminates at some file_read".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted audit DAG -> verification query | The EXTRACT-02 proof reads the reopened SQLite DAG as ground truth; it must not trust in-memory ValueStore state (which does not survive process exit) — only durable events + persisted anchors. |
| genuine vs. fabricated provenance | The proof's entire value is its ability to tell a threaded chain from a stapled one; a proof that passes on both is worse than none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Spoofing (of provenance) | EXTRACT-02 walk | critical | mitigate | Each provenance_chain element is positively resolved via find_event_by_id to a real file_read; a fabricated uuid resolves to None and is rejected (negative control A). |
| T-15-06 | Tampering (taint stapling) | EXTRACT-02 walk | critical | mitigate | A same-session naive re-anchored mint (negative control B, finding #11) is rejected because NO derivation Event payload binds its value_id to the recipient-half input chains (finding #2 predicate) AND its provenance_chain != the identity-pinned expected roots (finding #12) — never via a vacuous session-wide "has a derivation event" query. |
| T-15-07 | Repudiation | multi-anchor coverage | high | mitigate | The walk iterates EVERY anchor with distinct arg names asserted (finding #13); a partial (one-of-two) proof fails, per D-16 per-arg + Phase 12 collect-then-Block. |
| T-15-08 | Information Disclosure | fixture content | low | accept | The hostile-doc fixture contains only synthetic attacker strings; no real secrets. |
</threat_model>

<verification>
- `cargo test -p brokerd --test extract_provenance_threading` green (positive multi-anchor walk + both negative controls + EXTRACT-03 survival).
- `cargo test -p brokerd --lib audit` green (find_event_by_id).
- `cargo test --workspace --no-fail-fast` green; `./scripts/check-invariants.sh` passes.
- Manual reviewer check: the test file contains at least one test that constructs a deliberately fabricated/re-anchored chain and asserts REJECTION (the anti-staple has a paired failing case).
</verification>

<success_criteria>
- HARD GATE (ROADMAP success criterion 3): a programmatic audit-DAG query proves an unbroken edge for EVERY blocked arg in a multi-anchor Block AND a paired anti-staple control rejects a fabricated/re-anchored chain. This composes into ACCEPT-01/Phase 17 as a milestone-failing gate.
- find_event_by_id exists and is used for per-anchor lookup (find_event_by_type is NOT used for the per-anchor walk).
- CONFIRM-02 fixture exists and is reused by the EXTRACT-02/03 tests (and available for later confirm-tests + live demo).
- EXTRACT-03 proven: the concatenation-derived recipient still carries untrusted taint and still blocks.
</success_criteria>

<output>
Create `.planning/phases/15-deterministic-doc-action-extraction/15-02-SUMMARY.md` when done.
</output>
