---
phase: 28-authenticated-audit-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/brokerd/Cargo.toml
  - cli/caprun/tests/s9_live_block.rs
  - cli/caprun/tests/e2e.rs
  - cli/caprun/tests/live_acceptance_tainted_session.rs
  - cli/caprun/tests/live_acceptance_v1_3.rs
  - cli/caprun/tests/live_acceptance_v1_4_composed.rs
  - cli/caprun/tests/llm_planner_live_accept.rs
  - cli/caprun/tests/origin_seed_provenance.rs
autonomous: true
requirements: [HARDEN-02]

must_haves:
  truths:
    - "All 7 previously-vulnerable live-test fixtures place the workspace file under its own subdirectory with audit.db a sibling of that subdirectory (never a direct child of the workspace root) — the F1-safe layout confirm.rs already uses."
    - "The fixture relocation is behaviorally a no-op: the full macOS `cargo test --workspace` set that passed before still passes after (same tests, relocated files only)."
    - "hmac 0.12.1 and getrandom 0.4 are direct dependencies of crates/brokerd and the crate builds."
  artifacts:
    - "crates/brokerd/Cargo.toml with hmac + getrandom entries"
    - "Cargo.toml [workspace.dependencies] hmac + getrandom pins"
    - "7 migrated fixture files under cli/caprun/tests/"
  key_links:
    - "Fixture layout must match the F1 containment rule Plan 02 enforces (workspace file NOT a co-located sibling of audit.db) — this plan is the prerequisite that keeps the Plan 03 signature change + Plan 02 F1 refusal from turning the live suite red."
---

<objective>
Land the two prerequisite, low-risk groundwork items for HARDEN-02 before any cryptographic or refusal logic exists: (1) migrate the 7 live-test fixtures that today co-locate `audit.db` as a direct child of the workspace root to an F1-safe layout, and (2) add the `hmac`/`getrandom` dependencies.

Purpose: The F1 fail-closed startup refusal (Plan 02/03) rejects any run where the audit DB or its `.key` sibling resolves beneath the workspace root. RESEARCH confirmed 7 of 8 live fixtures use exactly that vulnerable layout — landing the refusal before migrating them would turn the entire live suite red with a single "broker refused to start" error (Pitfall 1). This plan isolates the mechanical relocation from the security logic so a later red test unambiguously means "the refusal logic is wrong," not "a fixture was mislaid."
Output: 7 relocated fixtures (behaviorally identical), `hmac 0.12.1` + `getrandom 0.4` deps.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-authenticated-audit-chain/28-RESEARCH.md
@.planning/phases/28-authenticated-audit-chain/28-PATTERNS.md
@cli/caprun/tests/confirm.rs
</context>

## Artifacts this phase produces (whole-phase reference)

New symbols/tables introduced across Plans 01-05 (executors: do not duplicate; consult before naming):

- `hmac = "0.12.1"`, `getrandom = "0.4"` — new deps (Plan 01)
- `type HmacSha256 = Hmac<Sha256>` — audit.rs type alias (Plan 03)
- keyed `compute_event_hash(key: &[u8], parent_hash, id, session_id, event_type, payload, taint) -> String` — audit.rs (Plan 03)
- `verify_event_hash(key, expected_hex, ...) -> bool` — audit.rs constant-time (`Mac::verify_slice`) helper (Plan 03)
- `load_or_create_key(audit_path: &str, workspace_root: &Path) -> anyhow::Result<Vec<u8>>` — cli/caprun key custody + F1 refusal helper (Plan 02)
- `chain_anchor(session_id TEXT PRIMARY KEY, head_event_id TEXT NOT NULL, head_hash TEXT NOT NULL, event_count INTEGER NOT NULL, mac TEXT NOT NULL) STRICT` — new audit.rs table (Plan 04)
- new `mac TEXT NOT NULL DEFAULT ''` column on `pending_confirmations` (Plan 05)
- new tests: forgery-without-key, key-dependence (Plan 03); tail-truncation, legacy-DB-fail-closed (Plan 04); pending_confirmation flip-back caught, deny() gate, cross-process confirm positive control (Plan 05); F1 refusal + key-file idempotency (Plan 02)

<tasks>

<task type="auto">
  <name>Task 1: Migrate 7 live-test fixtures to the F1-safe directory layout</name>
  <files>cli/caprun/tests/s9_live_block.rs, cli/caprun/tests/e2e.rs, cli/caprun/tests/live_acceptance_tainted_session.rs, cli/caprun/tests/live_acceptance_v1_3.rs, cli/caprun/tests/live_acceptance_v1_4_composed.rs, cli/caprun/tests/llm_planner_live_accept.rs, cli/caprun/tests/origin_seed_provenance.rs</files>
  <read_first>
    - cli/caprun/tests/confirm.rs (the already-F1-safe analog — lines ~221-223: `workspace = tmp.join("workspace")` subdirectory, `db_path = tmp.join("audit.db")` sibling of that subdirectory)
    - Each of the 7 target files' fixture-construction code (grep each for `join("workspace` and `join("audit.db")` / `audit_db` to find the exact `let workspace_file = ...; let audit_db = ...;` pair)
    - 28-RESEARCH.md anchor-table row "CRITICAL FINDING — F1 refusal breaks 7 of 8 live test fixtures" and 28-PATTERNS.md "7 test fixtures — directory-layout migration"
  </read_first>
  <action>In each of the 7 files, change the fixture layout so the workspace file lives under its OWN subdirectory and the audit DB is a sibling of THAT subdirectory, not of the workspace file. Concretely: where a file today does the equivalent of `let workspace_file = tmp.join("workspace.txt"); let audit_db = tmp.join("audit.db");` (siblings in the same dir, making `workspace_file.parent() == audit_db.parent()` — the F1-vulnerable shape), relocate the workspace file into a subdirectory (e.g. `let ws_dir = tmp.join("workspace"); std::fs::create_dir_all(&ws_dir)?; let workspace_file = ws_dir.join("workspace.txt"); let audit_db = tmp.join("audit.db");`) so that `workspace_root_dir = workspace_file.parent()` is `ws_dir` and `audit_db` (in `tmp`) is NOT beneath it. Mirror confirm.rs's existing pattern exactly. Note the two variants RESEARCH flagged: live_acceptance_v1_3.rs (~line 101) and live_acceptance_v1_4_composed.rs (~line 265) derive the workspace path as `audit_db.parent().join(...)` — invert that so the workspace file gets its own child dir. Do NOT change any test assertion, intent text, marker content, or SMTP/recipient wiring — only the two path bindings and any `create_dir_all` needed for the new subdirectory. Ensure each fixture still writes the intended workspace-file contents into the NEW path.</action>
  <verify>
    <automated>cargo test --workspace --no-fail-fast 2>&1 | tail -30  # macOS: Linux-gated tests show 0 passed (expected); every host-side + previously-green test must still pass, none newly failing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'join("audit.db")\|join("audit_db")\|audit_db =' cli/caprun/tests/{7 files}` shows, for each file, that the audit DB path's parent directory differs from the workspace file's parent directory (audit DB is NOT a co-located sibling of the workspace file).
    - `cargo test --workspace --no-fail-fast` on macOS produces the SAME set of passing test names as before the change (no test newly fails or is newly skipped due to a missing-file/path error) — capture the before/after pass counts and confirm equality.
    - No assertion, marker, or intent string was altered (diff touches only path bindings + `create_dir_all`).
  </acceptance_criteria>
  <done>All 7 fixtures use the F1-safe layout (workspace file under its own subdirectory, audit DB a sibling of that subdirectory); macOS `cargo test --workspace` is behaviorally unchanged (same passes, zero new failures).</done>
</task>

<task type="auto">
  <name>Task 2: Add hmac 0.12.1 and getrandom 0.4 dependencies to brokerd</name>
  <files>Cargo.toml, crates/brokerd/Cargo.toml</files>
  <read_first>
    - Cargo.toml (root `[workspace.dependencies]` — see existing `sha2`/`hex`/`uuid` entries for the pin style)
    - crates/brokerd/Cargo.toml (existing `sha2`/`hex` dependency references, ~lines 23-24)
    - 28-RESEARCH.md Standard Stack + Package Legitimacy Audit (both packages verdict OK, compile-verified against `sha2 = "0.10"` this session; pin `hmac = "0.12.1"` NOT `0.13.0`)
  </read_first>
  <action>Add `hmac = "0.12.1"` and `getrandom = "0.4"` to the root `[workspace.dependencies]` in Cargo.toml (matching the existing pin style used for `sha2`/`hex`), then reference both from crates/brokerd/Cargo.toml's `[dependencies]` (e.g. `hmac = { workspace = true }` / `getrandom = { workspace = true }`, or plain version entries matching whatever convention the existing `sha2`/`hex` brokerd entries use). Pin `hmac` to `0.12.1` exactly — RESEARCH empirically verified `0.13.0` may not match the existing `sha2 = "0.10"` digest-trait version. `getrandom 0.4.3` is already transitively resolved via `uuid`'s v4 feature, so this adds zero new resolution. Do not add any other dependency and do not upgrade `sha2`/`hex`.</action>
  <verify>
    <automated>cargo build -p brokerd 2>&1 | tail -15  # compiles clean against existing sha2 0.10 pin; no new resolution conflicts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hmac' crates/brokerd/Cargo.toml` >= 1 and `grep -c 'getrandom' crates/brokerd/Cargo.toml` >= 1.
    - `grep '0.13' Cargo.toml | grep hmac` returns nothing (hmac pinned at 0.12.x).
    - `cargo build -p brokerd` exits 0.
    - `cargo tree -p brokerd -i hmac` resolves to a 0.12.x version.
  </acceptance_criteria>
  <done>brokerd depends on hmac 0.12.1 + getrandom 0.4; `cargo build -p brokerd` is green; no version-resolution conflict with the existing sha2/hex pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build inputs → compiled brokerd TCB | new crate dependencies enter the trusted computing base |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-SC | Tampering | crates.io `hmac`/`getrandom` installs | high | mitigate | RESEARCH `## Package Legitimacy Audit`: both verdict OK (hmac 8.3M/wk, getrandom 34.7M/wk downloads; canonical RustCrypto / rust-random repos), independently compile-verified against the live workspace graph this session. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package → no blocking-human checkpoint required. |
| T-28-01 | Tampering | test-fixture layout drift | low | mitigate | Task 1's before/after pass-count equality proves the relocation is a behavioral no-op, not a masked regression. |
</threat_model>

<verification>
- macOS `cargo test --workspace --no-fail-fast`: unchanged pass set after fixture migration.
- `cargo build -p brokerd`: green with new deps.
- `./scripts/check-invariants.sh`: still green (no TCB source touched — deps + tests only).
</verification>

<success_criteria>
- 7 fixtures relocated to F1-safe layout, behaviorally identical (no new test failures).
- hmac 0.12.1 + getrandom 0.4 present and building.
- Nothing in `crates/*/src` changed (no premature crypto logic).
</success_criteria>

<output>
Create `.planning/phases/28-authenticated-audit-chain/28-01-SUMMARY.md` when done.
</output>
