---
phase: 20-planner-seam-capability-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/caprun/src/planner.rs
  - cli/caprun/src/worker.rs
autonomous: true
requirements: [PLANNER-01, PLANNER-04]
must_haves:
  truths:
    - "A `Planner` trait exists in cli/caprun/src/planner.rs with a single planning method that maps a typed intent + opaque ValueId handles to a PlanNode."
    - "A concrete `DeterministicPlanner` implements `Planner`, delegating to the existing deterministic `plan_from_intent` logic (a real seam Phase 21's LlmPlanner will implement, not a rename)."
    - "The worker constructs a `DeterministicPlanner` and produces its PlanNode by calling the trait method — the bare-fn call site is replaced by the seam."
    - "The trait method signature exposes ONLY `&CaprunIntent` (typed, user-trusted) and `ValueId` handles — never a ValueRecord, raw byte slice, string from untrusted content, or taint label (PLANNER-04 type-level boundary preserved)."
    - "All pre-existing planner unit tests and the e2e/worker behavior pass unchanged through the new seam (no regression)."
  artifacts:
    - "cli/caprun/src/planner.rs — `pub trait Planner` + `pub struct DeterministicPlanner` impl; `plan_from_intent` retained as the delegated deterministic logic."
    - "cli/caprun/src/worker.rs — planner invocation routed through the `Planner` trait."
  key_links:
    - "worker.rs → `Planner` trait → `DeterministicPlanner` → `plan_from_intent` deterministic mapping."
    - "The trait method signature is the compile-time seam: Phase 21's `LlmPlanner` implements the same trait without changing the worker's call site."
---

<objective>
Introduce the `Planner` seam (PLANNER-01): a real trait abstraction that the existing
deterministic intent→PlanNode logic implements, and that Phase 21's LLM planner will
implement in turn. Preserve the compile-time structural guarantee (PLANNER-04) that the
planner only ever sees opaque `ValueId` handles and the typed intent — never raw untrusted
bytes, a `ValueRecord`, or a taint label.

Purpose: Today `cli/caprun/src/planner.rs` exposes a bare free function `plan_from_intent`.
Phase 21 needs a swappable seam to drop an adversarial LLM planner behind without touching
the worker's call site or the broker. This plan creates that seam and re-points the worker
at it, with zero behavior change.

Output: A `Planner` trait + `DeterministicPlanner` impl in `planner.rs`; the worker producing
its PlanNode via the trait.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-session-trust-coherence.md
@cli/caprun/src/planner.rs
@cli/caprun/src/worker.rs
@cli/caprun/tests/planner.rs
</context>

<artifacts>
This plan produces / modifies:
- NEW: `pub trait Planner` (cli/caprun/src/planner.rs) — the seam. One method mapping a
  typed `&CaprunIntent` + opaque `ValueId` handles → `PlanNode`.
- NEW: `pub struct DeterministicPlanner` + `impl Planner for DeterministicPlanner`
  (cli/caprun/src/planner.rs) — delegates to `plan_from_intent`.
- RETAINED (unchanged signature): `pub fn plan_from_intent(...)` — kept so
  `cli/caprun/tests/planner.rs` (which includes the module via `#[path]` and calls
  `plan_from_intent` directly) passes UNMODIFIED.
- MODIFIED: `cli/caprun/src/worker.rs` — planner invocation routed through the trait.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the `Planner` trait and `DeterministicPlanner` impl</name>
  <files>cli/caprun/src/planner.rs</files>
  <read_first>
    - cli/caprun/src/planner.rs — the current `plan_from_intent` free fn, its 6-param
      signature (`intent`, `intent_value_id`, `derived_recipient`, `body`,
      `trusted_subject_handle`, `trusted_body_handle`) and its PLAN-03/I2 module doc
      (the "holds ONLY opaque ValueId handles" contract that MUST be preserved).
    - cli/caprun/tests/planner.rs — note it does `#[path = "../src/planner.rs"] mod planner;`
      and calls `planner::plan_from_intent(...)` directly in every test. This is why
      `plan_from_intent` MUST remain a free fn with its exact current signature.
  </read_first>
  <action>
    Add a `pub trait Planner` to planner.rs whose single method (name it `plan`) accepts
    exactly the same inputs the current `plan_from_intent` free fn takes — a `&CaprunIntent`
    plus the five `ValueId`/`Option<ValueId>` handle parameters — and returns `PlanNode`.
    The method takes `&self`. Do NOT introduce any parameter typed as `ValueRecord`, a raw
    string/byte slice, or a taint label — the seam MUST preserve the compile-time PLAN-03/
    PLANNER-04 guarantee that the planner sees only opaque handles + the typed intent.

    Add `pub struct DeterministicPlanner;` and `impl Planner for DeterministicPlanner` whose
    `plan` body calls the existing `plan_from_intent(...)` free fn, forwarding every argument
    unchanged. Keep `plan_from_intent` as a `pub fn` with its EXACT current signature — the
    trait impl delegates to it; the deterministic logic itself is not rewritten.

    Extend the module doc header to state that `Planner` is the seam Phase 21's LLM planner
    implements, and that the trait method carries the same opaque-handle-only contract the
    free fn already documents. Reference PLANNER-01 (seam) and PLANNER-04 (structural
    handle-only boundary) in the doc.
  </action>
  <acceptance_criteria>
    - `grep -n 'pub trait Planner' cli/caprun/src/planner.rs` matches.
    - `grep -n 'impl Planner for DeterministicPlanner' cli/caprun/src/planner.rs` matches.
    - `grep -n 'pub fn plan_from_intent' cli/caprun/src/planner.rs` still matches (free fn
      retained).
    - `cargo test -p caprun --test planner` passes with ALL existing test bodies UNMODIFIED
      (the `plan_from_intent` signature is unchanged).
    - `cargo build --workspace` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p caprun --test planner && cargo build --workspace</automated>
  </verify>
  <done>The `Planner` trait and `DeterministicPlanner` impl exist; `plan_from_intent` is
  unchanged; the planner unit tests pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Route the worker's planner call through the `Planner` trait</name>
  <files>cli/caprun/src/worker.rs</files>
  <read_first>
    - cli/caprun/src/worker.rs — the current call site (~line 290) that invokes
      `crate::planner::plan_from_intent(&intent, intent_value_id, derived_recipient, body,
      trusted_subject_handle, trusted_body_handle)` and the surrounding comment block
      (PLAN-03/finding #4). `mod planner;` is declared at line 66.
    - cli/caprun/src/planner.rs (as modified by Task 1) — the `Planner` trait and
      `DeterministicPlanner`.
  </read_first>
  <action>
    Replace the direct `plan_from_intent(...)` call in worker.rs with construction of a
    `DeterministicPlanner` and a call to its trait method `plan(...)`, passing the identical
    arguments. Bring the trait into scope (e.g. `use crate::planner::Planner;`) so the method
    is callable. The produced `plan_node` value and every downstream line (the
    `SubmitPlanNode` send, the decision handling) are unchanged — only the construction of
    `plan_node` moves from a free-fn call to a trait-method call.

    Update the adjacent comment to note the planner is now invoked through the `Planner` seam
    (PLANNER-01); do NOT weaken or delete the existing PLAN-03 "opaque ValueId handles only"
    commentary.
  </action>
  <acceptance_criteria>
    - `grep -n 'DeterministicPlanner' cli/caprun/src/worker.rs` matches (worker constructs the
      concrete planner).
    - `grep -n '\.plan(' cli/caprun/src/worker.rs` matches (worker calls the trait method).
    - `cargo build --workspace` succeeds.
    - `cargo test --workspace --no-fail-fast` shows no NEW failures vs. the pre-change
      baseline (macOS: the Linux-gated e2e/security tests still report 0 passed, as expected
      per CLAUDE.md — that is not a regression).
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace && cargo test -p caprun --no-fail-fast</automated>
  </verify>
  <done>The worker produces its PlanNode via the `Planner` trait; the full workspace builds
  and no pre-existing test regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted doc bytes → planner inputs | The planner must never receive raw bytes; only typed intent + opaque handles cross into it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Information Disclosure | `Planner` trait method signature | medium | mitigate | Trait method accepts only `&CaprunIntent` + `ValueId` handles — no `ValueRecord`/raw-bytes/taint parameter is added, preserving the compile-time PLAN-03/PLANNER-04 boundary. Verified by the signature and the unchanged `plan_from_intent` contract. |
| T-20-07 | Tampering | deterministic mapping logic | low | mitigate | The seam delegates to the unchanged `plan_from_intent`; existing planner unit tests pass unmodified, proving the routing/mapping behavior is byte-identical. |

No package-manager installs in this plan (pure std Rust) — no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- `cargo build --workspace` succeeds.
- `cargo test -p caprun --test planner` passes with test bodies unmodified.
- `cargo test -p caprun --no-fail-fast` shows no new failures.
- `./scripts/check-invariants.sh` passes (no `EffectRequest` token introduced; planner still
  holds only opaque handles).
</verification>

<success_criteria>
A `Planner` trait exists and the existing deterministic logic implements it as a genuine
abstraction (not a rename); the worker invokes the planner through the trait; the trait method
is type-restricted to opaque handles + typed intent; all pre-existing planner tests pass
unmodified.
</success_criteria>

<output>
Create `.planning/phases/20-planner-seam-capability-split/20-01-SUMMARY.md` when done.
</output>
