---
phase: 04-value-injection-security-demo-v0-done
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - crates/brokerd/tests/s9_acceptance.rs
autonomous: true
requirements: [REQ-s9-acceptance-test]
must_haves:
  truths:
    - "The §9 scenario runs end-to-end with no LLM: hostile content is read under confinement, a typed claim is extracted, a scripted plan flows the value's handle into email.send's `to` arg, and the executor blocks."
    - "The block decision surfaces the exact literal recipient for confirmation."
    - "The audit DAG shows an UNBROKEN taint edge: the block decision's provenance_chain[0] equals the id of a real file_read Event present in the DAG, and the chain verifies."
  artifacts:
    - "crates/brokerd/tests/s9_acceptance.rs — the automated §9 integration test (the single gate for v0 DONE)"
  key_links:
    - "The held-out assertion chain[0] == read_event.id is the genuine-taint backstop: it fails if taint is stapled at the sink instead of propagated from the read Event."
    - "verify_chain(conn, session_id) == true proves the audit DAG hash-chain integrity from the read Event through the blocked evaluation."
---

<objective>
Author the §9 acceptance test — the single non-negotiable gate for v0 DONE. It runs the full value-injection scenario end-to-end in-process (no LLM, no interactive input) and asserts every REQUIREMENTS sub-criterion, above all the genuine-taint backstop: the block decision's provenance_chain[0] equals the id of a real file_read Event in the audit DAG. If taint were stapled at the sink, this assertion fails and the demo proves nothing.

Purpose: This test is the acceptance contract. It is written against REQUIREMENTS.md REQ-s9-acceptance-test sub-criteria 1-6 and the §9 spec in planning-docs/PLAN.md — NOT reverse-engineered to match the implementation. Assertions must not be weakened to make a stapled or shortcut implementation pass.
Output: crates/brokerd/tests/s9_acceptance.rs passing, proving v0 DONE.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-RESEARCH.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md
@planning-docs/PLAN.md
@planning-docs/DESIGN-plan-executor.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- `crates/brokerd/tests/s9_acceptance.rs` — integration test `s9_acceptance` (uses brokerd::{audit, quarantine, approval}, executor::{submit_plan_node, value_store::ValueStore}, runtime_core::{PlanNode, PlanArg, SinkId, ExecutorDecision, TaintLabel})
- No production symbols added; this plan is the held-out acceptance contract only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: §9 end-to-end acceptance test with genuine-taint backstop</name>
  <files>crates/brokerd/tests/s9_acceptance.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md REQ-s9-acceptance-test (the six sub-criteria — the assertions to encode, verbatim in intent)
    - planning-docs/PLAN.md §"v0 Acceptance Test (§9)" (steps 1-6 and the non-negotiable anti-stapling sub-criterion)
    - .planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md §"crates/executor/tests/s9_acceptance.rs" (the test scaffold pattern — note this plan places the test under crates/brokerd/tests/ because brokerd depends on executor, avoiding a dev-dependency cycle)
    - crates/brokerd/src/quarantine.rs (extract_email_claims, mint_from_read), crates/brokerd/src/audit.rs (open_audit_db, verify_chain, find_event_by_type), crates/brokerd/src/approval.rs (build_confirmation_prompt), crates/executor/src/lib.rs (submit_plan_node)
  </read_first>
  <action>
    Create crates/brokerd/tests/s9_acceptance.rs with a single `#[test] fn s9_acceptance()`. Drive the real production code (no re-implementation of taint logic in the test): open an in-memory audit DB via audit::open_audit_db(":memory:"); define the hostile content sentence containing accounts@ev1l.com; call quarantine::extract_email_claims on it and assert exactly one email_address claim equal to the attacker address and that the raw sentence is not stored on the claim (lossy). Call quarantine::mint_from_read(conn, &mut store, session_id, &claim, None) to append the file_read Event and mint the tainted ValueRecord, capturing (read_event_id, read_hash, value_id). Build a scripted PlanNode { sink: SinkId("email.send"), args: vec![PlanArg { name: "to", value_id }] } — the planner holds only the handle. Call executor::submit_plan_node(session_id, &plan_node, &store) and match BlockedPendingConfirmation, binding literal_value, sink, arg_name, taint, provenance_chain. Assert: literal_value == "accounts@ev1l.com"; sink == "email.send"; arg_name == "to"; taint contains TaintLabel::ExternalUntrusted; and the genuine-taint backstop provenance_chain[0] == read_event_id. Call approval::build_confirmation_prompt(literal_value, taint, read_event_id) and assert the prompt's raw_recipient equals the exact attacker address (literal-value confirmation surfaces the byte-exact recipient). Call audit::find_event_by_type(conn, session_id, "file_read") and assert it is Some with taint containing ExternalUntrusted and EmailRaw, and that its id equals provenance_chain[0] (the chain anchor is a real DAG event, not a fabricated uuid). Assert audit::verify_chain(conn, &session_id.to_string()) is true. Add a header comment stating that none of these assertions may be weakened and that a stapled-taint implementation MUST fail the provenance_chain[0] == read_event_id assertion. Do NOT set any taint field inside the test — taint must come only from mint_from_read. For the Linux-only no-send-cap sub-criterion (REQUIREMENTS sub-criterion 6, reader had no send cap), reference the Phase 3 sandbox negative assertions as the established evidence in a comment rather than re-running confinement in this in-process test (confinement is proven by Phase 3's 29/29).
  </action>
  <acceptance_criteria>
    - Source: `grep -c "provenance_chain\[0\]" crates/brokerd/tests/s9_acceptance.rs` >= 1 (the genuine-taint backstop assertion is present).
    - Source: the test asserts equality of provenance_chain[0] with the read_event_id returned by mint_from_read AND with the id of the file_read event from find_event_by_type.
    - Source (no stapling in the test): `grep -v '^[[:space:]]*//' crates/brokerd/tests/s9_acceptance.rs | grep -c "TaintLabel::ExternalUntrusted"` counts only assertion uses, with no `.mint(` or ValueRecord construction in the test body — taint originates solely from mint_from_read: `grep -v '^[[:space:]]*//' crates/brokerd/tests/s9_acceptance.rs | grep -c "store.mint"` returns 0.
    - Test: `cargo test -p brokerd --test s9_acceptance` passes.
    - Test (anti-stapling sanity, manual reasoning recorded in SUMMARY): replacing mint_from_read's provenance_chain with an empty/foreign uuid would make the chain[0] assertion fail — confirming the test is a genuine backstop, not a tautology.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd --test s9_acceptance</automated>
  </verify>
  <done>The §9 acceptance test passes end-to-end with a genuine, audited taint chain: a tainted value blocked in email.send's `to` arg, the exact literal surfaced for confirmation, and provenance_chain[0] proven equal to a real file_read Event id with verify_chain true. v0 is DONE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production code | The test exercises real production paths (extract, mint, executor decision, audit) and must not re-implement or shortcut taint logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Spoofing | §9 genuine-taint assertion | critical | mitigate | The held-out assertion provenance_chain[0] == real file_read Event id fails for any stapled-taint implementation; verify_chain confirms DAG integrity. This is the v0-DONE backstop. |
| T-04-06 | Tampering | test fidelity | high | mitigate | The test sets no taint itself (negative grep on store.mint and ValueRecord construction in the test body); all taint originates from mint_from_read in production code. |
| T-04-SC | Tampering | cargo dependencies | low | accept | Test-only file; adds no dependency. Uses brokerd dev-deps + the in-workspace executor crate already depended on by brokerd. |
</threat_model>

<verification>
- `cargo test -p brokerd --test s9_acceptance` passes.
- `cargo test --workspace` green (no regressions).
- The genuine-taint backstop assertion (provenance_chain[0] == read_event_id == file_read event id) is present and non-weakened.
</verification>

<success_criteria>
§9 passes: a kernel-confined-reader-sourced tainted value is deterministically blocked in email.send's routing-sensitive `to` argument, the exact literal is surfaced for human confirmation, and the audit DAG shows an unbroken taint edge from the raw-read Event to the blocked sink argument. This is v0 DONE.
</success_criteria>

<output>
Create `.planning/phases/04-value-injection-security-demo-v0-done/04-05-SUMMARY.md` when done.
</output>
