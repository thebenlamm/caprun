---
phase: 04-value-injection-security-demo-v0-done
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/brokerd/src/proto.rs
  - crates/brokerd/src/sinks.rs
  - crates/brokerd/src/sinks/email_send.rs
  - crates/brokerd/src/approval.rs
  - crates/brokerd/src/audit.rs
  - crates/brokerd/src/lib.rs
autonomous: true
requirements: [REQ-mediated-sink-stub, REQ-approval-hook]
must_haves:
  truths:
    - "A mediated email.send sink stub records its invocation to the audit DAG and never actually sends email."
    - "When the executor blocks, the broker can construct a literal-value confirmation prompt that shows the EXACT recipient address (raw and canonical) plus its source read Event — not a category."
    - "The audit DAG can be queried to find the file_read Event and read back its taint labels for the §9 chain assertion."
  artifacts:
    - "crates/brokerd/src/sinks/email_send.rs with invoke_email_send_stub recording an email_send_stub Event"
    - "crates/brokerd/src/approval.rs with build_confirmation_prompt producing the literal-value prompt payload"
    - "crates/brokerd/src/audit.rs with query_events_by_session and find_event_by_type"
    - "crates/brokerd/src/proto.rs with SubmitPlanNode request + PlanNodeDecision response variants"
  key_links:
    - "build_confirmation_prompt takes the Block payload (literal_value, taint, source event id) so the human confirms the byte-exact address (DESIGN-plan-executor §Literal-Value Confirmation UX)."
    - "find_event_by_type('file_read') returns the read Event with its taint intact so the §9 test can assert the chain anchor exists in the DAG."
---

<objective>
Add the broker-side pieces that surround the executor decision but do NOT depend on the executor crate, so this plan runs in parallel (Wave 2) with the executor build: the mediated email.send sink stub, the literal-value confirmation prompt builder (REQ-approval-hook), the SubmitPlanNode/PlanNodeDecision proto variants (closes RESEARCH Gap 3 data surface), and the audit-DAG query helpers the §9 test needs.

Purpose: REQ-mediated-sink-stub requires a sink whose sensitivity is hardcoded (the map lives in the executor; this is the post-confirmation dispatch target that records the invocation). REQ-approval-hook requires surfacing the literal value (DESIGN UX). The audit query helpers let the held-out §9 test prove the read Event with taint exists in the DAG.
Output: brokerd compiles with the new sink module, approval module, proto variants, and audit query helpers, each unit-tested. No executor dependency is added in this plan (the dispatch wiring lands in plan 04).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-RESEARCH.md
@.planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md
@planning-docs/DESIGN-plan-executor.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- `brokerd::proto::BrokerRequest::SubmitPlanNode { session_id: Uuid, plan_node: runtime_core::PlanNode }` — new request variant
- `brokerd::proto::BrokerResponse::PlanNodeDecision { decision: runtime_core::ExecutorDecision }` — new response variant
- `brokerd::sinks` module (crates/brokerd/src/sinks.rs) + `brokerd::sinks::email_send::invoke_email_send_stub(conn, session_id, plan_node, parent_hash) -> Result<String>` — records an `email_send_stub` Event, never sends
- `brokerd::approval::ConfirmationPrompt { raw_recipient, canonical_address, domain, known_contact, source_event_id, taint }` — prompt payload struct
- `brokerd::approval::build_confirmation_prompt(literal_value, taint, source_event_id) -> ConfirmationPrompt` — literal-value prompt builder
- `brokerd::audit::query_events_by_session(conn, session_id) -> Result<Vec<Event>>` — read events back from the DAG
- `brokerd::audit::find_event_by_type(conn, session_id, event_type) -> Result<Option<Event>>` — locate a typed event with its taint
- New event_type string literal: `email_send_stub`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Proto variants + audit-DAG query helpers</name>
  <files>crates/brokerd/src/proto.rs, crates/brokerd/src/audit.rs</files>
  <read_first>
    - crates/brokerd/src/proto.rs (BrokerRequest/BrokerResponse enums to extend)
    - crates/brokerd/src/audit.rs (open_audit_db, append_event, the events table columns id/parent_id/session_id/event_type/actor/payload/taint — the payload column stores the full serialized Event)
    - crates/runtime-core/src/event.rs (Event shape to reconstruct from the payload column) and src/plan_node.rs (TaintLabel)
    - .planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md §"crates/brokerd/src/proto.rs"
  </read_first>
  <action>
    In proto.rs add to BrokerRequest a `SubmitPlanNode { session_id: uuid::Uuid, plan_node: runtime_core::PlanNode }` variant, and to BrokerResponse a `PlanNodeDecision { decision: runtime_core::ExecutorDecision }` variant, matching the existing derive and doc-comment style (these are pure data — no executor call here). In audit.rs add `query_events_by_session(conn, session_id) -> anyhow::Result<Vec<Event>>` that selects the `payload` column for the session ordered by rowid and deserializes each payload back into a `runtime_core::Event` (the payload column already stores the full serialized Event per append_event). Add `find_event_by_type(conn, session_id, event_type) -> anyhow::Result<Option<Event>>` returning the first matching event. Reconstruct Event from the stored payload JSON so the returned Event.taint reflects what was persisted. Add a unit test that appends a file_read Event with taint [ExternalUntrusted, EmailRaw] then asserts find_event_by_type returns it with both labels intact, and that query_events_by_session returns it.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "SubmitPlanNode" crates/brokerd/src/proto.rs` >= 1 and `grep -c "PlanNodeDecision" crates/brokerd/src/proto.rs` >= 1.
    - Source: `grep -c "pub fn find_event_by_type" crates/brokerd/src/audit.rs` returns 1; `grep -c "pub fn query_events_by_session" crates/brokerd/src/audit.rs` returns 1.
    - Test: `cargo test -p brokerd audit` passes including the new find_event_by_type taint-roundtrip test.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd audit</automated>
  </verify>
  <done>Proto carries SubmitPlanNode/PlanNodeDecision; the audit DAG can be queried by session and event_type with taint preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mediated email.send sink stub + literal-value confirmation prompt builder</name>
  <files>crates/brokerd/src/sinks.rs, crates/brokerd/src/sinks/email_send.rs, crates/brokerd/src/approval.rs, crates/brokerd/src/lib.rs</files>
  <read_first>
    - crates/brokerd/src/session.rs and src/audit.rs (append_event pattern — the analog for recording an Event)
    - crates/runtime-core/src/event.rs (Event construction) and src/plan_node.rs (PlanNode, TaintLabel)
    - planning-docs/DESIGN-plan-executor.md §"Literal-Value Confirmation UX" (prompt MUST show raw AND canonical recipient, known-contact, source; confirm the EXACT address)
    - .planning/phases/04-value-injection-security-demo-v0-done/04-PATTERNS.md §"crates/brokerd/src/sinks/email_send.rs"
  </read_first>
  <behavior>
    - invoke_email_send_stub appends an event of type email_send_stub and returns its hash; it performs no network/send action.
    - build_confirmation_prompt(literal, taint, source_event_id) returns a ConfirmationPrompt whose raw_recipient equals the exact literal, canonical_address equals the canonicalized literal (for ASCII no-display-name input, equal to raw), domain is the substring after '@', known_contact is false, and source_event_id is carried through.
    - Test: invoke_email_send_stub records an email_send_stub event findable via find_event_by_type.
    - Test: build_confirmation_prompt for "accounts@ev1l.com" yields raw_recipient == canonical_address == "accounts@ev1l.com", domain "ev1l.com", known_contact false.
  </behavior>
  <action>
    Create sinks.rs declaring `pub mod email_send;` and add `pub mod sinks;` plus `pub mod approval;` to lib.rs. In sinks/email_send.rs implement `invoke_email_send_stub(conn: &rusqlite::Connection, session_id: Uuid, plan_node: &PlanNode, parent_hash: Option<&str>) -> anyhow::Result<String>` that constructs an Event with actor "sink-stub:email.send", event_type "email_send_stub", empty taint, and appends it via audit::append_event; document that the §9 test never reaches this function because the executor blocks first, and that no raw literals are written to the payload. In approval.rs define `ConfirmationPrompt` with fields raw_recipient (String), canonical_address (String), domain (String), known_contact (bool), source_event_id (uuid::Uuid), taint (Vec<TaintLabel>), and `build_confirmation_prompt(literal_value: String, taint: Vec<TaintLabel>, source_event_id: uuid::Uuid) -> ConfirmationPrompt`. For v0 canonicalization, trim surrounding whitespace and treat the trimmed ASCII literal as canonical (raw == canonical for the simple §9 case); set domain to the portion after the last '@'; set known_contact false. Document that punycode/homoglyph/RTL canonicalization is a post-v0 extension and that the prompt MUST show the exact literal for human confirmation, and that delivery is via FAMP with the broker triggering it on a Block decision (the FAMP wire is a thin seam exercised outside the automated §9 path). Write the two RED unit tests first, then implement to GREEN.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "pub fn invoke_email_send_stub" crates/brokerd/src/sinks/email_send.rs` returns 1.
    - Source: `grep -c "pub fn build_confirmation_prompt" crates/brokerd/src/approval.rs` returns 1.
    - Source: `grep -E "pub mod sinks|pub mod approval" crates/brokerd/src/lib.rs` shows both modules declared.
    - Test: `cargo test -p brokerd` passes including the sink-stub event-recording test and the prompt-builder literal-fidelity test (raw_recipient equals the exact input literal).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd</automated>
  </verify>
  <done>The email.send stub records an audit event without sending; the confirmation prompt builder surfaces the exact literal recipient (raw + canonical) with its source event — satisfying REQ-mediated-sink-stub and REQ-approval-hook for v0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → human (FAMP) | The confirmation prompt crosses to the human; it must carry the byte-exact literal, not a category, or the human cannot recognize the attack. |
| broker → audit DAG | The sink stub and query helpers read/write the append-only DAG; no raw hostile literals are written into sink payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | confirmation prompt | high | mitigate | The prompt shows the exact recipient literal (raw + canonical + domain + known-contact=false) so the human confirms the precise address rather than a vague category (DESIGN §Literal-Value Confirmation UX). |
| T-04-05 | Repudiation | email.send sink stub | medium | mitigate | Every (would-be) sink invocation records an email_send_stub Event in the append-only audit DAG, leaving a tamper-evident trace. |
| T-04-SC | Tampering | cargo dependencies | low | accept | No new external crate; this plan uses brokerd's existing workspace deps (rusqlite, serde_json, uuid, chrono) vetted in Phase 3. |
</threat_model>

<verification>
- `cargo test -p brokerd` passes (audit query helpers, sink stub, approval prompt).
- `cargo build --workspace` green.
- No executor dependency added to brokerd in this plan (kept parallel with the executor build).
</verification>

<success_criteria>
brokerd gains the SubmitPlanNode/PlanNodeDecision proto variants, the audit-DAG query helpers (with taint preserved), the mediated email.send stub, and the literal-value confirmation prompt builder — each unit-tested — without depending on the executor crate.
</success_criteria>

<output>
Create `.planning/phases/04-value-injection-security-demo-v0-done/04-03-SUMMARY.md` when done.
</output>
