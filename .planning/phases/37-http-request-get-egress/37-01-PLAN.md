---
phase: 37-http-request-get-egress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime-core/src/plan_node.rs
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [HTTP-01, HTTP-02]
must_haves:
  truths:
    - "TaintLabel::HttpRaw exists and is_untrusted() returns true for it, compile-forced into the exhaustive match (no wildcard)."
    - "http.request is a registered KNOWN_SINKS entry with an exact-match arg schema (url allowed AND required)."
    - "sink_effect_class(http.request) == Observe (the first real Observe sink)."
    - "http.request `url` is BOTH routing-sensitive and content-sensitive; its expected_role is None (unconstrained structural gate)."
  artifacts:
    - crates/runtime-core/src/plan_node.rs
    - crates/executor/src/sink_schema.rs
    - crates/executor/src/sink_sensitivity.rs
  key_links:
    - "is_untrusted()'s exhaustive `match self` with NO wildcard forces HttpRaw into the untrusted arm — omission is a compile error, not a silent false-allow."
---

<objective>
Add the pure-type and executor-table foundations for the read-only `http.request` GET sink, per DESIGN §3.2 / §3.4 and 37-CONTEXT decisions 2 and 4. This is the wave-1, no-I/O layer: a new `TaintLabel::HttpRaw` variant (compile-forced untrusted) and the hardcoded executor rows that make `http.request` a callable, `Observe`-classified, `url`-I2-gated sink.

Purpose: Establishes the taint label the new inbound mint (Plan 03) stamps, and the sink-sensitivity rows that make a tainted `url` Block and an untrusted `Observe` GET survive a Draft session — without any network, mint, or dispatch code (those are Plans 02/03).
Output: `HttpRaw` in `runtime-core`; `http.request` in `KNOWN_SINKS`, `sink_effect_class`, `is_routing_sensitive`, `is_content_sensitive`, `expected_role`; unit tests for each.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-http-request-get-egress/37-CONTEXT.md
@planning-docs/DESIGN-git-github-http-sinks.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TaintLabel::HttpRaw, compile-forced into is_untrusted()</name>
  <files>crates/runtime-core/src/plan_node.rs</files>
  <read_first>
    - crates/runtime-core/src/plan_node.rs lines 11-57 (the `TaintLabel` enum + the `is_untrusted()` exhaustive match — `ExecRaw` at line 28 is the exact naming/pairing convention to mirror).
    - DESIGN §3.4 (HttpRaw is compile-forced into the exhaustive match; no wildcard).
  </read_first>
  <behavior>
    - is_untrusted() returns true for TaintLabel::HttpRaw (it is an external/untrusted-origin label, paired with ExternalUntrusted at mint time in Plan 03).
    - is_untrusted() still returns false for UserTrusted and LocalWorkspace only.
    - Removing HttpRaw from the untrusted arm is a compile error (the match stays exhaustive, no `_` wildcard).
  </behavior>
  <action>
    Add a new `HttpRaw` variant to the `TaintLabel` enum in plan_node.rs, placed after `ExecRaw`, per DESIGN §3.4 and 37-CONTEXT decision 4. Mirror `ExecRaw`'s doc-comment convention: note it is raw response-body bytes fetched from an allowlisted external host over broker-mediated egress (untrusted origin), always minted paired with `ExternalUntrusted`. Add `HttpRaw` to the untrusted arm of `is_untrusted()`'s exhaustive `match self` (the `=> true` arm alongside `ExecRaw`). Do NOT add a wildcard arm and do NOT convert to `matches!()` — the exhaustive match is the compile-time enforcement (Pitfall 5). Update the method's doc comment count of untrusted labels ("seven"/"eight") to stay accurate. runtime-core must stay pure (check-invariants Gate 2 forbids std::io/std::fs/std::net/tokio/async here — adding an enum variant introduces none of these).
  </action>
  <verify>
    <automated>cargo test -p runtime-core --no-fail-fast 2>&1 | tail -20; cargo build -p runtime-core</automated>
  </verify>
  <done>HttpRaw compiles into TaintLabel; a unit test asserts is_untrusted(&HttpRaw)==true; runtime-core builds and check-invariants Gate 2 stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register http.request in the executor sink schema + sensitivity tables</name>
  <files>crates/executor/src/sink_schema.rs, crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_schema.rs lines 40-92 (KNOWN_SINKS; git.commit at 76-91 is the exact-match single-arg exemplar) + its tests 346-404.
    - crates/executor/src/sink_sensitivity.rs lines 40-168 (sink_effect_class, is_routing_sensitive, is_content_sensitive) + 208-286 (expected_role) + the git.commit tests 621-668 and process.exec tests 496-548.
    - DESIGN §3.2 (Observe), §8 fail-closed table row for `http.request` `url` (routing AND content-sensitive), and DESIGN-GATE-RECORD-v1.8 finding 6/NIT (url content-sensitive too).
    - 37-CONTEXT decisions 2 and 6.
  </read_first>
  <behavior>
    - schema_for("http.request") is Some; validate_schema accepts exactly {url} (url allowed AND required); unknown/duplicate/missing arg → the matching DenyReason.
    - sink_effect_class(http.request) == EffectClass::Observe.
    - is_routing_sensitive(http.request, "url") == true AND is_content_sensitive(http.request, "url") == true.
    - expected_role(http.request, "url") == None (unconstrained structural role gate — no origin_role mint site for a legit url; the Block comes from routing/content-sensitivity + taint, exactly like process.exec command/args and git.commit message).
  </behavior>
  <action>
    In sink_schema.rs, add a `SinkSchema` row to `KNOWN_SINKS` for sink `"http.request"` with `allowed: &["url"]` and `required: &["url"]` (exact-match single-arg, mirroring git.commit's shape at lines 88-91; GET only — no method/headers/body args this milestone, per DESIGN §0/§3.1). Add a doc comment citing HTTP-01, DESIGN §3.1/§3.2, and note the schema gate enforces only the arg NAME set — a tainted url Blocks downstream, it does not Deny here.

    In sink_sensitivity.rs: (a) add `"http.request" => EffectClass::Observe` to `sink_effect_class` (before the `_ => CommitIrreversible` fail-closed default), with a doc comment: this is the FIRST real `Observe` sink (only the test-only `test.observe` is Observe today), Allowed even in a Draft session, but its inbound response demotes the session at mint time (DESIGN §3.2, wired in Plan 03). (b) Add `HTTP_REQUEST_ROUTING_SENSITIVE: &[&str] = &["url"]` and `HTTP_REQUEST_CONTENT_SENSITIVE: &[&str] = &["url"]` consts, and wire both into `is_routing_sensitive` and `is_content_sensitive` (url is routing-sensitive — it decides WHERE the GET goes — AND content-sensitive per NIT-6/§8 defense-in-depth: a secret assembled into the query string is exfil). (c) In `expected_role`, add a `"http.request" => match arg_name { "url" => None, _ => None }` arm, with a doc comment reusing the process.exec/git.commit rationale (unconstrained structural gate; the Block comes entirely from routing/content-sensitivity + the untrusted-taint check — this None is NOT an I2 bypass). Add unit tests mirroring the git.commit test block (registered sink; exact-args ok; unknown/duplicate/missing arg denied; is_observe; url routing-AND-content-sensitive; url expected_role None).
  </action>
  <verify>
    <automated>cargo test -p executor --no-fail-fast 2>&1 | tail -30; cargo build -p executor</automated>
  </verify>
  <done>http.request is registered and Observe-classified; url is routing+content-sensitive with expected_role None; all new unit tests pass; executor builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | untrusted plan node names a sink + args; executor classifies via hardcoded tables only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-10 | Tampering | is_untrusted() exhaustive match | high | mitigate | HttpRaw compile-forced into the untrusted arm; no wildcard — a future omission is a build failure, not a silent false-allow (DESIGN §3.4) |
| T-37-11 | Elevation | http.request `url` sensitivity rows | high | mitigate | url is routing- AND content-sensitive (hardcoded in the Rust TCB, never a policy file) so a tainted url Blocks; Observe classification is a table row only, no new ExecutorDecision variant (I2 unweakened) |
| T-37-12 | Spoofing | KNOWN_SINKS schema gate | medium | mitigate | exact-match single-arg {url}; unknown/duplicate/missing arg → Deny at Step 0 before any resolve/sensitivity check |
</threat_model>

<verification>
- `cargo build --workspace` compiles (HttpRaw + http.request rows introduce no new errors).
- `cargo test -p runtime-core` and `cargo test -p executor` pass, including the new HttpRaw-untrusted and http.request table tests.
- `bash scripts/check-invariants.sh` exits 0 (Gate 2 runtime-core purity unaffected; Gate 1 no EffectRequest token introduced).
</verification>

<success_criteria>
HttpRaw is an untrusted TaintLabel (compile-forced). http.request is a callable, Observe-classified sink whose single `url` arg is I2-gated (routing + content-sensitive). No network, mint, or dispatch code exists in this plan (those are Plans 02/03). Partial coverage of HTTP-01 (sink rows) and HTTP-02 (the HttpRaw label the mint stamps).
</success_criteria>

<output>
Create `.planning/phases/37-http-request-get-egress/37-01-SUMMARY.md` when done.
</output>
