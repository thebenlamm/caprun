---
phase: 37-http-request-get-egress
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/Cargo.toml
  - crates/brokerd/src/sinks.rs
  - crates/brokerd/src/sinks/http_request.rs
autonomous: true
requirements: [HTTP-01, HTTP-03]
user_setup: []
must_haves:
  truths:
    - "A GET whose host is not on the hardcoded allowlist is rejected (Err → Deny) BEFORE any DNS resolve or socket — host-portable."
    - "The SSRF classifier denies loopback, RFC1918, link-local, CGNAT, cloud-metadata, ULA, and IPv6-mapped resolved IPs (host-portable unit tests over a pure IpAddr classifier)."
    - "url validation rejects userinfo@, any non-https scheme, and IP-encoding tricks (decimal/octal/hex) — host-portable."
    - "The reqwest client disables redirect following and connects to the SSRF-vetted resolved IP it pins (SNI/Host = original hostname), closing DNS-rebind TOCTOU."
  artifacts:
    - crates/brokerd/Cargo.toml
    - crates/brokerd/src/sinks/http_request.rs
    - crates/brokerd/src/sinks.rs
  key_links:
    - "ssrf_check runs on the SAME resolved IP that reqwest is then pinned to (.resolve) — the checked IP equals the connected IP (no re-resolve), the resolve-and-pin TOCTOU close."
---

<objective>
Build the broker-side, read-only `http.request` GET egress mechanism (Pattern A) and its SSRF resolve-and-pin defense, per DESIGN §3.1 / §3.6 and 37-CONTEXT decisions 1, 6, 7. This is the network + supply-chain layer: the `reqwest` (rustls/ring) dependency and an `http_request` sink module whose SSRF/url checks are a HOST-PORTABLE pure classifier (unit-testable on macOS), with only the real socket connect Linux-gated.

Purpose: Delivers the egress + SSRF surface that Plan 03's server dispatch calls. It performs NO minting and NO session demotion (Gate 3 stays clean for this module — DESIGN §10), so it can be built and largely tested independent of Plans 01/03.
Output: brokerd `reqwest`/`ring`/`webpki-roots` deps; `crates/brokerd/src/sinks/http_request.rs` (host allowlist, pure url-validation, pure `ssrf_check`, `invoke_http_get`); host-portable unit tests; live-HTTPS behavior deferred to Phase 40.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-http-request-get-egress/37-CONTEXT.md
@planning-docs/DESIGN-git-github-http-sinks.md
@crates/brokerd/src/sinks/email_smtp.rs
@cli/caprun-planner/Cargo.toml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reqwest (rustls/ring) + webpki-roots to brokerd Cargo.toml</name>
  <files>crates/brokerd/Cargo.toml</files>
  <read_first>
    - crates/brokerd/Cargo.toml (the full [dependencies] block, and the lettre comment at 38-41 warning against the boring-tls / native-tls backend — the SAME hazard class applies to reqwest).
    - cli/caprun-planner/Cargo.toml lines 14-22 (the ONLY existing workspace reqwest — `=0.13.4`, `default-features = false`, `features = ["rustls", "json"]`, with the recorded crates.io legitimacy evidence and the `rustls` (not `rustls-tls`) feature-name deviation note — mirror this exactly).
    - DESIGN §5.1 (crypto = lean `ring`, pure-Rust; aws-lc-rs acceptable only if provider-consistency is materially cleaner) and §5.2 (CA roots = compiled-in `webpki-roots 1.0.8` so env_clear() is hermetic — NOT rustls-platform-verifier, no SSL_CERT_* / system store).
  </read_first>
  <action>
    Add `reqwest = { version = "=0.13.4", default-features = false, features = [...] }` to brokerd's `[dependencies]`, selecting the feature set that yields the `ring` crypto provider + compiled-in webpki-roots and NO native-tls/openssl-sys transitive dependency (per DESIGN §5.1/§5.2). Confirm the exact reqwest 0.13.4 feature names via Context7 (resolve `reqwest`, query "0.13 rustls ring provider webpki-roots feature names") and/or `cargo add reqwest@0.13.4 --dry-run --no-default-features --features ...` BEFORE committing — reqwest 0.13's `rustls` feature may default to aws-lc-rs, so the ring provider may require a distinct feature (e.g. a `-ring` variant) or an explicit `rustls`/`webpki-roots` crate pin wired into the ClientBuilder in Task 2. If a clean ring selection is not expressible in reqwest features, DESIGN §5.1 permits aws-lc-rs — but lean ring first and record the exact choice + why in the SUMMARY. If reqwest's bundled roots are not `webpki-roots 1.0.8`, add `webpki-roots = "1.0.8"` as a direct dep and wire it into the client (Task 2). Add a doc comment on the dep line recording crates.io legitimacy evidence (reqwest is already vetted in-workspace at caprun-planner: seanmonstar/hyper, not yanked, ~573M downloads; ring and webpki-roots are the canonical rustls-ecosystem crates the Phase-35 DESIGN gate selected at §5.1/§5.2) and the "GET-only, broker-side egress" scope. Do NOT enable any native-tls / boring-tls / openssl feature (RUSTSEC hazard class, per the lettre comment). Only add the deps this task — no code yet (an unused dep is fine; Task 2 consumes it).
  </action>
  <verify>
    <automated>cargo build -p brokerd 2>&1 | tail -20; cargo tree -p brokerd -i openssl-sys 2>&1 | tail -5 || echo "no openssl-sys (expected)"</automated>
  </verify>
  <done>brokerd builds with reqwest (rustls/ring) + webpki-roots; `cargo tree` shows NO openssl-sys/native-tls in brokerd's dependency graph; crypto/roots choice + crates.io evidence recorded in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: http_request egress sink module — host allowlist, pure url-validation, pure ssrf_check, invoke_http_get</name>
  <files>crates/brokerd/src/sinks/http_request.rs, crates/brokerd/src/sinks.rs</files>
  <read_first>
    - crates/brokerd/src/sinks.rs lines 15-19 (the `pub mod` sink registrations — add `pub mod http_request;`).
    - crates/brokerd/src/sinks/email_smtp.rs lines 40-65 + 87-112 (Pattern A shape: broker-resident egress, D-04 trusted-config endpoint sourcing from broker-local env — NEVER from a plan node / ValueNode / audit DB; opaque payloads). The allowlist here is the analogous broker-owned trusted config.
    - DESIGN §3.1 (Pattern A, reqwest, GET only, NEVER in the worker) and §3.6 (resolve host → PIN destination IP → connect to pinned IP with SNI/Host = original; deny loopback 127/8 + ::1, RFC1918 10/8+172.16/12+192.168/16, link-local 169.254/16+fe80::/10, CGNAT 100.64/10, cloud-metadata 169.254.169.254, ULA fc00::/7, IPv6-mapped ::ffff:0:0/96; NO redirect following; reject userinfo@ / non-https / IP-encoding tricks; host allowlist).
    - DESIGN §8 fail-closed row: non-allowlisted host or SSRF-range resolution → Deny.
    - 37-CONTEXT decision 6 and the <specifics> block (structure the IP-range/userinfo/scheme/redirect checks as a host-portable pure classifier over a resolved IP; real socket connect Linux-gated; allowlist is a hardcoded/broker-config constant, NOT a policy file).
  </read_first>
  <behavior>
    - is_host_allowlisted(host) is true only for hosts in the hardcoded/broker-config allowlist; a non-allowlisted host is rejected before any resolve (host-portable).
    - ssrf_check(ip: IpAddr) returns Err for: loopback (127.0.0.0/8, ::1), RFC1918 (10/8, 172.16/12, 192.168/16), link-local (169.254.0.0/16, fe80::/10), CGNAT (100.64.0.0/10), cloud-metadata (169.254.169.254), ULA (fc00::/7), and IPv6-mapped-IPv4 (::ffff:0:0/96) whose embedded v4 is any denied range; Ok for an ordinary public IP.
    - validate_url(url) returns Err for a userinfo@ component, any scheme != "https", and IP-encoding tricks (decimal/octal/hex-packed host); Ok for a plain https URL with an allowlisted hostname.
    - invoke_http_get, on a non-allowlisted host or an SSRF-range resolution, returns Err (mapped to a broker-side Deny by the Plan-03 dispatch) and performs no request; on success it returns the response body String. It builds the reqwest client with redirect following DISABLED and the destination pinned to the SSRF-vetted resolved IP.
  </behavior>
  <action>
    Create `crates/brokerd/src/sinks/http_request.rs` and register it with `pub mod http_request;` in `crates/brokerd/src/sinks.rs`. Implement, structured for host-portability per 37-CONTEXT decision 6:

    1. A hardcoded `HOST_ALLOWLIST` constant (an operator-surfaced deployment constant, DESIGN §11 — a `&[&str]`, NOT a policy file; sourcing a single trusted default like `api.github.com` plus any test host is fine, mirroring email_smtp's broker-owned trusted config). Document that the allowlist is a security property, never runtime-configurable from a plan node / ValueNode / audit DB (D-04 discipline).

    2. `fn validate_url(&str) -> Result<url::Host/parsed, _>`: parse the URL; reject any userinfo@ component, any scheme other than `https`, and IP-encoding tricks (a host that parses as a raw/decimal/octal/hex-packed IP literal rather than a DNS name — reject non-DNS-name hosts). Pure, host-portable.

    3. `fn ssrf_check(ip: std::net::IpAddr) -> Result<(), _>`: deny every range in DESIGN §3.6 (enumerate them; for IPv6-mapped-IPv4 unwrap the embedded v4 and re-check the v4 ranges). Pure over `IpAddr`, host-portable — the load-bearing SSRF classifier.

    4. `async fn invoke_http_get(url: &str) -> Result<String>`: validate_url → confirm host is on HOST_ALLOWLIST (Err if not, BEFORE resolve) → resolve the host to IP(s) → run ssrf_check on the resolved IP that will be used (Err on any denied range) → build a reqwest `Client` with `redirect(Policy::none())`, the compiled-in webpki-roots trust anchors (Task 1), and the destination PINNED to the vetted resolved IP via reqwest's `.resolve(host, socket_addr)` (so the connect target equals the SSRF-checked IP — DNS-rebind TOCTOU close) with SNI/Host = the original hostname → issue the GET → return the response body text. This function does NOT append audit events, does NOT mint, does NOT touch session status (Plan 03 owns all of that — keeps this module out of Gate 3's mint-site restriction, DESIGN §10). Confirm the reqwest ClientBuilder `.resolve()`, `.redirect()`, and root-store API via Context7 before use.

    Gate the real DNS-resolve + socket-connect leg of `invoke_http_get` behind `#[cfg(target_os = "linux")]` where it needs a live socket, providing a macOS `#[cfg(not(target_os = "linux"))]` no-op/stub path (mirroring the project's Linux-only pattern in CLAUDE.md) so the crate builds and the pure classifiers test on the Mac; live-HTTPS behavior is deferred to Phase 40.

    Add HOST-PORTABLE unit tests (run on macOS): each SSRF-range denial (loopback/RFC1918/link-local/CGNAT/metadata/ULA/IPv6-mapped) individually; a public IP passes ssrf_check; validate_url rejects userinfo@, http:// (non-https), and a decimal/octal/hex IP-encoded host; is_host_allowlisted rejects a non-allowlisted host and accepts an allowlisted one; invoke_http_get on a non-allowlisted host returns Err without touching the network (assert the allowlist check precedes resolve).
  </action>
  <verify>
    <automated>cargo test -p brokerd http_request --no-fail-fast 2>&1 | tail -40; cargo test -p brokerd ssrf --no-fail-fast 2>&1 | tail -20; cargo build -p brokerd</automated>
  </verify>
  <done>ssrf_check, validate_url, and the host allowlist are pure and pass host-portable unit tests covering every §3.6 denial; invoke_http_get pins the SSRF-vetted resolved IP and disables redirects; the module performs no mint/event/demotion; brokerd builds; check-invariants Gate 3 stays green (no mint token in this module).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → external network | outbound GET leaves the trust boundary to an allowlisted host |
| external network → broker | untrusted response bytes arrive (minted in Plan 03) |
| supply chain | new broker deps: reqwest, ring, webpki-roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information disclosure | url → internal/metadata IP (SSRF) | critical | mitigate | pure ssrf_check denies loopback/RFC1918/link-local/CGNAT/metadata/ULA/IPv6-mapped; host allowlist gate before resolve; non-https/userinfo@/IP-encoding rejected (DESIGN §3.6) |
| T-37-03 | Tampering | DNS-rebind TOCTOU | high | mitigate | resolve-and-pin: connect to the SSRF-vetted resolved IP via reqwest `.resolve()`, SNI/Host = original — checked IP == connected IP |
| T-37-04 | Tampering | 30x redirect bounce to a denied range | high | mitigate | redirect following disabled (`redirect(Policy::none())`) — a redirect cannot re-target a denied IP |
| T-37-SC | Tampering | reqwest/ring/webpki-roots supply chain | high | mitigate | Phase-35 DESIGN-gate-selected crypto stack; reqwest already vetted in-workspace (crates.io evidence); rustls/ring only, NO native-tls/openssl (`cargo tree` asserted); recorded in SUMMARY |
</threat_model>

<verification>
- `cargo build -p brokerd` compiles with the new deps and module.
- `cargo test -p brokerd http_request` / `ssrf` pass on macOS (pure classifiers, host-portable).
- `cargo tree -p brokerd` shows no openssl-sys / native-tls.
- `bash scripts/check-invariants.sh` exits 0 — Gate 3 unaffected (this module never mints).
- Live-HTTPS resolve/connect behavior is intentionally NOT verified here; deferred to the Phase 40 live Linux proof.
</verification>

<success_criteria>
The broker can perform an allowlisted, redirect-free, IP-pinned GET, and rejects non-allowlisted hosts, SSRF ranges, userinfo@, non-https, and IP-encoding tricks — with the SSRF/url logic proven by host-portable unit tests on macOS. No minting or session demotion in this module. Covers HTTP-01 (allowlisted GET egress) and HTTP-03 (SSRF resolve-and-pin).
</success_criteria>

<output>
Create `.planning/phases/37-http-request-get-egress/37-02-SUMMARY.md` when done.
</output>
