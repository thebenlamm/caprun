---
phase: 03-confinement-mediation-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/sandbox/Cargo.toml
  - crates/sandbox/src/lib.rs
  - crates/sandbox/src/landlock.rs
  - crates/sandbox/src/seccomp.rs
  - crates/sandbox/src/rlimits.rs
  - crates/sandbox/src/bin/confine-probe.rs
  - crates/sandbox/tests/api_spike.rs
  - crates/sandbox/tests/confinement_integration.rs
  - crates/adapter-fs/Cargo.toml
  - crates/adapter-fs/src/lib.rs
  - crates/adapter-fs/src/protocol.rs
  - crates/adapter-fs/tests/fd_pass.rs
  - crates/brokerd/Cargo.toml
  - crates/brokerd/src/lib.rs
  - crates/brokerd/src/proto.rs
  - crates/brokerd/src/server.rs
  - crates/brokerd/src/session.rs
  - crates/brokerd/src/audit.rs
  - crates/brokerd/tests/uds_abstract_spike.rs
  - crates/brokerd/tests/uds_ipc.rs
  - crates/brokerd/tests/audit_dag.rs
  - cli/caprun/Cargo.toml
  - cli/caprun/src/main.rs
  - cli/caprun/src/worker.rs
  - cli/caprun/tests/e2e.rs
autonomous: true
requirements:
  - REQ-sandbox
  - REQ-brokerd-core
  - REQ-adapters-fs
  - REQ-substrate-demo

must_haves:
  truths:
    - "cargo build --workspace succeeds on macOS (Linux-only confinement is cfg-gated, never compiled on macOS)"
    - "cargo test --workspace --lib passes on macOS with all confinement integration tests cfg-gated out or #[ignore]"
    - "On Linux the seccompiler 0.5.0 deny-rule API is proven to compile and load (deny execve + deny socket(AF_INET))"
    - "On Linux the abstract-namespace UDS bind -> from_std -> accept round-trip is proven to work"
  artifacts:
    - "crates/sandbox/ crate skeleton (cfg-gated stubs)"
    - "crates/adapter-fs/ crate skeleton"
    - "crates/brokerd/src/proto.rs IPC message types (BrokerRequest, BrokerResponse)"
    - "All Wave-0 test scaffold files mapped to 03-VALIDATION.md"
  key_links:
    - "Root Cargo.toml [workspace.dependencies] gains nix, tokio, rusqlite, landlock, seccompiler, sha2, hex, tokio-util"
    - "seccompiler verified call pattern recorded in api_spike.rs comments for Wave 2 consumption"
    - "abstract-UDS from_std verified pattern recorded in uds_abstract_spike.rs comments for Wave 2 consumption"
---

<objective>
Stand up the entire buildable skeleton for Phase 3 — all new crates, all
dependency wiring, all test scaffold files — and verify the two highest-risk
APIs (seccompiler 0.5.0 deny rules, abstract-namespace UDS in tokio) BEFORE any
Wave 2 implementation depends on them. This is the Nyquist "Wave 0": after this
plan, `cargo build --workspace` and `cargo test --workspace --lib` are green on
macOS, every downstream test file exists, and the two risky APIs are proven on
Linux so Wave 2 builds on facts, not assumptions.

Purpose: De-risk seccompiler + abstract-UDS (the LOW-confidence items in
03-RESEARCH.md Assumptions Log A1/A2) and give Wave 2 a compiling foundation so
the three implementation plans (sandbox / brokerd / adapter-fs) can run in
parallel with no Cargo.toml contention.
Output: Compiling workspace skeleton, dependency wiring, spike proofs, test
scaffolds.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-confinement-mediation-substrate/03-RESEARCH.md
@.planning/phases/03-confinement-mediation-substrate/03-PATTERNS.md
@.planning/phases/03-confinement-mediation-substrate/03-VALIDATION.md
@Cargo.toml
@crates/brokerd/Cargo.toml
@crates/brokerd/src/lib.rs
@crates/runtime-core/src/lib.rs
@crates/runtime-core/src/plan_node.rs
</context>

<artifacts_this_plan_produces>
- New crate `sandbox` (Cargo.toml + src/lib.rs, landlock.rs, seccomp.rs, rlimits.rs as cfg-gated stubs; src/bin/confine-probe.rs stub)
- New crate `adapter-fs` (Cargo.toml + src/lib.rs, src/protocol.rs stubs)
- New module files in `brokerd`: `proto.rs` (real types: `BrokerRequest`, `BrokerResponse`), `server.rs`, `session.rs`, `audit.rs` (compiling stubs)
- `cli/caprun` second binary `caprun-worker` registered; deps wired
- Root `Cargo.toml` `[workspace.dependencies]`: `nix`, `tokio`, `rusqlite`, `landlock`, `seccompiler`, `sha2`, `hex`, `tokio-util`
- Spike test files: `crates/sandbox/tests/api_spike.rs`, `crates/brokerd/tests/uds_abstract_spike.rs`
- Test scaffolds (`#[ignore]` placeholders): `crates/sandbox/tests/confinement_integration.rs`, `crates/brokerd/tests/uds_ipc.rs`, `crates/brokerd/tests/audit_dag.rs`, `crates/adapter-fs/tests/fd_pass.rs`, `cli/caprun/tests/e2e.rs`
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Workspace dependency wiring + crate skeletons + test scaffolds</name>
  <files>Cargo.toml, crates/sandbox/Cargo.toml, crates/sandbox/src/lib.rs, crates/sandbox/src/landlock.rs, crates/sandbox/src/seccomp.rs, crates/sandbox/src/rlimits.rs, crates/sandbox/src/bin/confine-probe.rs, crates/adapter-fs/Cargo.toml, crates/adapter-fs/src/lib.rs, crates/adapter-fs/src/protocol.rs, crates/brokerd/Cargo.toml, crates/brokerd/src/lib.rs, crates/brokerd/src/proto.rs, crates/brokerd/src/server.rs, crates/brokerd/src/session.rs, crates/brokerd/src/audit.rs, cli/caprun/Cargo.toml, cli/caprun/src/main.rs, cli/caprun/src/worker.rs, crates/sandbox/tests/confinement_integration.rs, crates/brokerd/tests/uds_ipc.rs, crates/brokerd/tests/audit_dag.rs, crates/adapter-fs/tests/fd_pass.rs, cli/caprun/tests/e2e.rs</files>
  <read_first>
    - Cargo.toml (root — current [workspace.dependencies] and members glob)
    - crates/brokerd/Cargo.toml + crates/brokerd/src/lib.rs (preserve submit_plan_node; add module declarations only)
    - crates/runtime-core/src/lib.rs + crates/runtime-core/src/plan_node.rs (reuse TaintLabel, Session, Event; copy the derive stack `#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]`)
    - 03-PATTERNS.md §"crates/sandbox/Cargo.toml", §"crates/brokerd/src/proto.rs", §"Workspace [dependencies] additions required", §"cfg Linux-gate pattern"
    - 03-VALIDATION.md §"Per-Task Verification Map" and 03-RESEARCH.md §"Wave 0 Gaps" (the exact test file list)
  </read_first>
  <action>
    CRATE NAMING DECISION (resolves 03-RESEARCH.md Open Question 3): use Option A — name the fs adapter crate `crates/adapter-fs`. It is matched by the existing `members = ["crates/*", "cli/caprun"]` glob, so NO workspace members change is required. Do not create `crates/adapters/fs`.

    Root Cargo.toml: add to `[workspace.dependencies]` exactly these pins from 03-RESEARCH.md §Standard Stack: `nix = { version = "0.31.3", features = ["fs","socket","resource","process","signal"] }`, `tokio = { version = "1.52.3", features = ["net","io-util","rt-multi-thread","macros"] }`, `rusqlite = { version = "0.40.1", features = ["bundled"] }`, `landlock = "0.4.5"`, `seccompiler = "0.5.0"`, `sha2 = { version = "0.11.0", features = ["std"] }`, `hex = "0.4"`, `tokio-util = { version = "0.7.18", features = ["codec"] }`. Keep serde/serde_json/uuid/chrono/anyhow/thiserror as-is.

    Create crate `sandbox` (Cargo.toml copies workspace metadata inheritance from brokerd; put `landlock`, `seccompiler`, `nix` under `[target.'cfg(target_os = "linux")'.dependencies]` and `anyhow` under `[dependencies]`; register `[[bin]] name = "confine-probe" path = "src/bin/confine-probe.rs"`). src/lib.rs declares `pub mod landlock; pub mod seccomp; pub mod rlimits;` and defines `pub fn apply_confinement() -> std::io::Result<()>` with two cfg variants: on `not(target_os="linux")` it eprintln-warns and returns Ok(()); on `target_os="linux"` it calls rlimits then landlock then seccomp (stub bodies returning Ok for now — Wave 2 fills them). Each of landlock.rs/seccomp.rs/rlimits.rs exposes its public fn (`deny_all_filesystem`, `apply_worker_filter`, `apply_rlimits`) as a cfg-gated stub returning `std::io::Result<()>` = Ok(()) on Linux and a no-op on non-Linux. ALL sandbox public fns return `std::io::Result<()>` (pre_exec/self-confine compatibility — 03-PATTERNS.md §"std::io::Result for pre_exec compatibility"). confine-probe.rs is a stub `fn main()` that Wave 2 Plan 02 will flesh out. Include the lib.rs unit test `noop_on_macos_does_not_panic` from 03-PATTERNS.md.

    Create crate `adapter-fs` (Cargo.toml: anyhow + nix + runtime-core path dep + serde/serde_json/uuid for protocol types). src/lib.rs declares `pub mod protocol;` and stub signatures `pub fn pass_fd(socket_raw_fd: RawFd, file_raw_fd: RawFd) -> nix::Result<()>` and `pub fn recv_fd(socket_raw_fd: RawFd) -> nix::Result<RawFd>` (use `std::os::fd::RawFd`); stubs may `unimplemented!()` guarded so the lib still compiles — prefer returning a placeholder `Ok(())` / `Err(nix::errno::Errno::ENOSYS)` so no panic path ships. src/protocol.rs defines `RequestFd { path: String, session_id: uuid::Uuid }` and `FdGranted { path: String }` with the standard derive stack.

    brokerd: add `tokio`, `rusqlite`, `sha2`, `hex`, `serde`, `serde_json`, `chrono` (workspace deps) to crates/brokerd/Cargo.toml [dependencies] alongside existing runtime-core/uuid/anyhow. Edit crates/brokerd/src/lib.rs to ADD `pub mod proto; pub mod server; pub mod session; pub mod audit;` WITHOUT modifying or removing `submit_plan_node` or its test (it is the Phase 1 contract — see 01-02-SUMMARY). proto.rs defines the real enums `BrokerRequest { CreateSession { intent_id: uuid::Uuid }, RequestFd { path: String }, ReportRead { bytes_read: u64 } }` and `BrokerResponse { SessionCreated { session_id: uuid::Uuid }, FdGranted, Ack, Error { message: String } }` with the standard derive stack. server.rs/session.rs/audit.rs are compiling stubs: `pub async fn run_broker_server(session_id: &str) -> anyhow::Result<()> { Ok(()) }`, `pub fn create_session(intent_id: uuid::Uuid) -> runtime_core::Session` (real, reuses runtime_core::Session per 03-PATTERNS.md §session.rs), and `pub fn open_audit_db(path: &str) -> anyhow::Result<rusqlite::Connection>` stub. Do NOT introduce the forbidden raw-effect token anywhere (scripts/check-invariants.sh Gate 1).

    cli/caprun/Cargo.toml: add `[dependencies]` (sandbox, brokerd, adapter-fs, runtime-core path deps; tokio, anyhow, uuid workspace deps) and register a second binary `[[bin]] name = "caprun-worker" path = "src/worker.rs"` plus `[[test]] name = "e2e" path = "tests/e2e.rs"`. src/main.rs and src/worker.rs become compiling stub `fn main() {}` (worker.rs must compile as its own binary).

    Create these test scaffold files as `#[ignore]`-annotated placeholders (one ignored test each, body `unimplemented!()` is NOT allowed — use an empty assert-true with a `// TODO Wave 2` comment, so `cargo test --lib` stays green and the file exists for Wave 2 to fill): crates/sandbox/tests/confinement_integration.rs, crates/brokerd/tests/uds_ipc.rs, crates/brokerd/tests/audit_dag.rs, crates/adapter-fs/tests/fd_pass.rs, cli/caprun/tests/e2e.rs. Gate the confinement_integration.rs and e2e.rs tests with `#[cfg(target_os = "linux")]`.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test --workspace --lib</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0 on macOS (no landlock/seccompiler/nix-confinement code compiled on macOS).
    - `cargo test --workspace --lib` exits 0 on macOS; the sandbox `noop_on_macos_does_not_panic` test passes.
    - `grep -rn "submit_plan_node" crates/brokerd/src/lib.rs` still finds the Phase 1 fn (untouched).
    - `bash scripts/check-invariants.sh` exits 0 (no forbidden raw-effect token introduced).
    - All five scaffold test files exist; `cargo test --workspace -- --list` shows them (ignored tests included).
  </acceptance_criteria>
  <done>Workspace compiles and unit tests pass on macOS; all new crates and test files exist; brokerd Phase 1 stub preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify seccompiler 0.5.0 deny-rule API + nix 0.31 prctl signature (Linux spike)</name>
  <files>crates/sandbox/tests/api_spike.rs, crates/sandbox/src/seccomp.rs</files>
  <read_first>
    - 03-RESEARCH.md §Pattern 3 (seccomp-bpf worker filter, marked [ASSUMED]) + §Assumptions Log A1, A4 + §Common Pitfalls Pitfall 2 (no_new_privs before seccomp)
    - docs.rs/seccompiler/0.5.0 (resolve via Context7 `seccompiler` or WebFetch) — the exact `SeccompFilter::new` / `SeccompRule` / `SeccompAction` / `apply_filter` API and how to express a per-syscall deny rule
    - docs.rs/nix/0.31.x — `nix::sys::prctl` PR_SET_NO_NEW_PRIVS signature
    - crates/sandbox/src/seccomp.rs (the stub from Task 1)
  </read_first>
  <action>
    Read docs.rs/seccompiler/0.5.0 and confirm the exact constructor and rule-builder API (03-RESEARCH.md A1 flags this as the highest-risk LOW-confidence item). Write crates/sandbox/tests/api_spike.rs gated `#[cfg(target_os = "linux")]` with a test that: (1) calls `nix::sys::prctl` to set PR_SET_NO_NEW_PRIVS — confirming the nix 0.31 call signature (A4); (2) builds a minimal seccompiler filter that DENIES `execve`/`execveat` and `socket` for AF_INET/AF_INET6 (default action allow, match action `SeccompAction::Errno(EPERM)`) for the current arch via `std::env::consts::ARCH.try_into()`; (3) converts to `BpfProgram` and calls `seccompiler::apply_filter`; (4) after applying, attempts `execve` of `/bin/true` and asserts it fails with EPERM/EACCES, and attempts `socket(AF_INET, SOCK_STREAM)` and asserts EPERM. Record the EXACT verified call pattern (constructor signature, rule shape, syscall-number resolution) as a doc-comment block at the top of crates/sandbox/src/seccomp.rs so Wave 2 Plan 02 implements against verified facts, not [ASSUMED]. Do NOT put the forbidden raw-effect literal anywhere. Keep the spike self-contained (it forks via the seccomp self-apply in-process — the test process itself is the probe; if libtest threading interferes, spawn the existing `confine-probe` bin instead and document that path).
  </action>
  <verify>
    <automated>cargo test -p sandbox --test api_spike</automated>
  </verify>
  <acceptance_criteria>
    - On Linux: `cargo test -p sandbox --test api_spike` passes — proving the filter compiles, loads after NO_NEW_PRIVS, and actually blocks execve + AF_INET socket.
    - On macOS: the spike is cfg-gated out and `cargo test -p sandbox` still exits 0.
    - crates/sandbox/src/seccomp.rs top-of-file doc block records the verified seccompiler 0.5.0 call pattern (constructor args, deny-rule construction, arch resolution) and the verified nix 0.31 prctl signature.
  </acceptance_criteria>
  <done>seccompiler 0.5.0 deny-rule API and nix prctl signature are proven on Linux and documented for Wave 2; macOS build unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Verify abstract-namespace UDS bind -> from_std -> accept round-trip (Linux spike)</name>
  <files>crates/brokerd/tests/uds_abstract_spike.rs, crates/brokerd/src/server.rs</files>
  <read_first>
    - 03-RESEARCH.md §Pattern 4 (broker UDS IPC server) + §Common Pitfalls Pitfall 7 (abstract UDS in tokio) + §Assumptions Log A2 + §Open Questions Q1 (fallback to path-based + Landlock exception)
    - docs.rs/tokio/1.52.x tokio::net::UnixListener (from_std) + std::os::unix::net::UnixListener::bind abstract-namespace support
    - crates/brokerd/src/server.rs (the stub from Task 1)
  </read_first>
  <action>
    Write crates/brokerd/tests/uds_abstract_spike.rs gated `#[cfg(target_os = "linux")]`, using a `#[tokio::test]`. The test: (1) binds `std::os::unix::net::UnixListener::bind` to an abstract path with a leading NUL byte (build the address as bytes `\0/agentos/spike-<pid>` — note the abstract path must be constructed so the NUL prefix is preserved; if `bind(&str)` rejects the NUL, use `std::os::linux::net::SocketAddrExt`/`SocketAddr::from_abstract_name` per the verified API); (2) `set_nonblocking(true)` then `tokio::net::UnixListener::from_std`; (3) spawns a client task that connects to the same abstract address and writes a 4-byte LE length prefix + a small JSON body; (4) accepts on the listener, reads the framed message back, asserts round-trip equality. If the abstract bind path returns EINVAL, implement the documented fallback (temp-dir path-based socket) and record that the broker must add a Landlock read/connect exception for that path — but prefer abstract. Record the verified working bind+from_std pattern (including the exact abstract-address construction call) as a doc-comment block at the top of crates/brokerd/src/server.rs for Wave 2 Plan 03.
  </action>
  <verify>
    <automated>cargo test -p brokerd --test uds_abstract_spike</automated>
  </verify>
  <acceptance_criteria>
    - On Linux: `cargo test -p brokerd --test uds_abstract_spike` passes — a full bind(abstract) -> from_std -> accept -> framed-message round-trip succeeds.
    - On macOS: cfg-gated out; `cargo test -p brokerd` exits 0.
    - crates/brokerd/src/server.rs top-of-file doc block records the verified abstract-UDS bind + from_std pattern (or the documented path-based fallback + required Landlock exception) for Wave 2.
  </acceptance_criteria>
  <done>The abstract-UDS-in-tokio path (03-RESEARCH.md A2) is proven on Linux and documented for Wave 2; macOS build unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host (macOS) ↔ CI (Linux) | Confinement only enforces on Linux; macOS stubs must never give a false sense of enforcement |
| crates.io ↔ workspace | New third-party crates enter the TCB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing/False-assurance | macOS no-op stubs | high | mitigate | All confinement code + all negative-assertion tests are `#[cfg(target_os="linux")]`; macOS stub eprintln-warns. CI runs on Linux (03-RESEARCH.md A3 — pin ubuntu ≥ 22.04). |
| T-03-02 | Tampering | seccompiler/landlock API misuse | medium | mitigate | Wave 0 spikes prove the exact APIs on Linux before Wave 2 implements; verified patterns recorded in source doc-blocks. |
| T-03-SC | Tampering (supply chain) | cargo deps (nix, tokio, rusqlite, landlock, seccompiler, sha2, hex, tokio-util) | high | mitigate | 03-RESEARCH.md §Package Legitimacy Audit: all 8 verdicts OK/Approved; birdcage (GPL-3.0) and extrasafe (SUS) REMOVED. No [ASSUMED]/[SUS] packages remain → no human-verify checkpoint required. Pins are exact. |
</threat_model>

<verification>
- `cargo build --workspace` green on macOS.
- `cargo test --workspace --lib` green on macOS.
- On Linux CI: `cargo test -p sandbox --test api_spike` and `cargo test -p brokerd --test uds_abstract_spike` pass.
- `bash scripts/check-invariants.sh` exits 0.
</verification>

<success_criteria>
Workspace skeleton compiles; all Phase 3 crates and test files exist; seccompiler 0.5.0 deny-rule API and abstract-UDS-in-tokio path are proven on Linux and documented in source for Wave 2.
</success_criteria>

<output>
Create `.planning/phases/03-confinement-mediation-substrate/03-01-SUMMARY.md` when done.
</output>
