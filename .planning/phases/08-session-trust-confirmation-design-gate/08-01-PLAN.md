---
phase: 08-session-trust-confirmation-design-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planning-docs/DESIGN-session-trust-state.md
autonomous: true
requirements: [PROC-01]

must_haves:
  truths:
    - "A DESIGN doc specifies SessionStatus::Draft, its one-way monotonic Active→Draft transition, and how the executor learns trust state (a broker-resolved parameter on submit_plan_node, never from IPC)."
    - "The doc states the I1 dynamic-demotion trigger (mint_from_read is the sole trust-flip site, trusted-path-only, never worker-declared) and the I0 creation rule (seed-provenance field, decided by the caprun CLI)."
    - "The doc specifies the new hardcoded sink_effect_class(sink) -> EffectClass table in crates/executor and a new DenyReason variant, with draft-only deny decided in ONE executor TCB function at Step 0.5 (never a broker pre-check)."
    - "Session demotion is specified as an atomic pair: an UPDATE on the sessions read-model row plus an append-only session_demoted audit Event whose parent_id is the triggering file_read Event id (TAINT-04 causal edge)."
  artifacts:
    - planning-docs/DESIGN-session-trust-state.md
  key_links:
    - "Trust state reaches the executor ONLY via a broker-resolved parameter (session_status), never trusted from the PlanNode or any worker IPC message (closes the self-declaration hole, RESEARCH A2)."
    - "session_demoted Event.parent_id == the triggering file_read Event id — the unbroken causal edge Phase 11's ACC-03 relies on."
    - "The new EffectClass and DenyReason enums MUST use exhaustive match with no wildcard arm, so an unhandled future variant is a compile error, not a silent fail-open."
---

<objective>
Author `planning-docs/DESIGN-session-trust-state.md` — the v1.2 session-trust-state invariant spec that extends the APPROVED `DESIGN-taint-model.md` / `DESIGN-plan-executor.md` pair. It specifies the mechanism for I1 dynamic session demotion and the I0 creation-time draft rule, precise enough that Phase 9 can implement TAINT-01..04 and ORIGIN-01..02 without making a single new design decision.

Purpose: This is one half of the PROC-01 design gate. Without it, Phase 9 would re-litigate mid-implementation the three architectural gaps RESEARCH.md surfaced (no SessionStatus::Draft variant; no session-trust parameter on submit_plan_node; no effect-class-per-sink mapping).
Output: One rigorous Markdown DESIGN doc under `planning-docs/`, using MUST/MUST NOT normative verbs throughout, mirroring the structure of `DESIGN-taint-model.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-session-trust-confirmation-design-gate/08-RESEARCH.md
@.planning/phases/08-session-trust-confirmation-design-gate/08-PATTERNS.md
@planning-docs/DESIGN-taint-model.md
@planning-docs/DESIGN-plan-executor.md
</context>

<artifacts_this_phase_produces>
This plan (08-01) creates exactly one file:
- `planning-docs/DESIGN-session-trust-state.md` — DESIGN doc (invariant spec) covering I1 dynamic demotion + I0 creation rule + SessionStatus::Draft + trust-state threading into the executor + the new sink_effect_class table + the new DenyReason variant.

(Sibling plan 08-02 creates `planning-docs/DESIGN-confirmation-release.md`; plan 08-03 creates the gate-record `planning-docs/DESIGN-GATE-RECORD-v1.2.md`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the session-trust-state invariant + threading sections (I1/I0, SessionStatus::Draft, trust-state reaches the executor)</name>
  <files>planning-docs/DESIGN-session-trust-state.md</files>
  <read_first>
    - planning-docs/DESIGN-taint-model.md (closest analog — copy its header/front-matter shape, its bolded-label MUST invariant-statement style, its "Acceptance Predicate (Done When)" numbered-condition format, and its trusted-path-only / anti-self-declaration phrasing verbatim in style)
    - .planning/phases/08-session-trust-confirmation-design-gate/08-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 1, Pitfall 5, Assumptions A1/A2/A5 — these fix the exact names and rules to state)
    - .planning/phases/08-session-trust-confirmation-design-gate/08-PATTERNS.md (the DESIGN-session-trust-state.md subsection — header pattern, invariant-statement pattern, acceptance-predicate pattern)
    - crates/runtime-core/src/session.rs (current SessionStatus enum: Active | WaitingApproval | Done | Failed | RolledBack — the doc adds Draft to this exact enum)
    - crates/brokerd/src/quarantine.rs (mint_from_read — the sole broker taint-mint site the demotion co-locates with)
    - crates/brokerd/src/session.rs (persist_session INSERT — write-once today; the doc must specify the new monotonic UPDATE contract)
    - cli/caprun/src/main.rs (positional-arg-only intent seeding — confirms ORIGIN-01 needs a new CLI on-ramp, Pitfall 5)
  </read_first>
  <action>
    Create `planning-docs/DESIGN-session-trust-state.md`. Open with the same header block as DESIGN-taint-model.md: a `**Requirement:**` line citing PROC-01 (and forward-referencing TAINT-01..04, ORIGIN-01..02), a `**Status:** Draft — pending DESIGN-GATE-RECORD-v1.2.md approval` line, a `**Canonical source:** planning-docs/PLAN.md (wins on any conflict)` line, and a `**Gate:**` line stating that `crates/executor` and `crates/brokerd` MUST NOT gain session-trust or confirmation-release code until this doc AND DESIGN-confirmation-release.md are recorded APPROVED in DESIGN-GATE-RECORD-v1.2.md.
    Then write these sections, every rule as a MUST / MUST NOT predicate (never "should"/"needs to"):
    (1) SessionStatus::Draft variant — add `Draft` to the existing runtime_core SessionStatus enum (Active | WaitingApproval | Done | Failed | RolledBack). State the transition rule as one-way / monotonic: Active → Draft only, mirroring taint monotonicity; a Draft session MUST NOT ever transition back to Active. Name it Draft per RESEARCH A1.
    (2) I1 dynamic-demotion invariant — a bolded label ("I1 dynamic demotion") plus the MUST rule: a session is demoted to Draft when `mint_from_read` taints a value inside it (TAINT-01). State the trusted-path-only rule near-verbatim from RESEARCH Pattern 2: the session's Draft transition is set by the same trusted broker function that mints the tainted ValueRecord — never by the worker, never by a flag any worker IPC message could carry. `mint_from_read` (in brokerd::quarantine) is the sole trust-flip site.
    (3) I0 creation rule — restate for session creation: a Session whose seed derives from external/file-derived content MUST start in Draft. Specify a seed-provenance field recorded at `create_session` time, with the trusted-arg-vs-file-derived determination made by the caprun CLI (ORIGIN-01) and set by the broker's trusted create_session path from that provenance — never self-declared by the (possibly injected) caller. Name the expected new CLI on-ramp shape (e.g. a `--seed-from-file <path>` flag or a third intent-parsing branch) per Pitfall 5, noting the concrete flag name is Phase 9's to finalize but the input path is required, not just a struct field.
    (4) How the executor learns trust state — this is the load-bearing gap (Pitfall 1 / A2). Specify that `submit_plan_node` gains an explicit trust-state parameter (name it `session_status: &SessionStatus`), resolved by the BROKER from ITS OWN session store, never carried on the PlanNode and never trusted from IPC — the same discipline HARD-03 uses for session_id. State the current signature (session_id, effect_id, plan_node, value_store) and the new signature explicitly so Phase 9 has no ambiguity.
    (5) Two-table audit contract (TAINT-04) — specify that demotion performs an atomic pair inside the same lock/transaction as the mint: an `UPDATE sessions SET status = 'Draft'` on the mutable read-model row (the monotonic UPDATE path that does not exist today) PLUS an append-only `session_demoted` audit Event whose `parent_id` equals the triggering `file_read` Event id. State the MUST that the read-model and the ledger can never disagree because both writes are atomic.
    Do NOT specify the executor deny mechanism or the acceptance predicate here — those are Task 2. End this task's content with a clear section boundary so Task 2 appends cleanly.
    Keep this doc additive: reference the APPROVED DESIGN-taint-model.md's I0/I1 text rather than rewriting it; extend, do not restate wholesale.
  </action>
  <acceptance_criteria>
    - `test -f planning-docs/DESIGN-session-trust-state.md` succeeds.
    - `grep -c 'SessionStatus::Draft' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'mint_from_read' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -ci 'seed-provenance\|seed provenance' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'session_demoted' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'parent_id' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'session_status' planning-docs/DESIGN-session-trust-state.md` is >= 1 (the new submit_plan_node parameter is named).
    - `grep -ci 'never self-declared\|trusted.*path\|trusted broker' planning-docs/DESIGN-session-trust-state.md` is >= 1 (anti-self-declaration rule present).
    - The doc contains a `**Gate:**` header line naming crates/executor and crates/brokerd as blocked-until-approved.
  </acceptance_criteria>
  <verify>
    <automated>test -f planning-docs/DESIGN-session-trust-state.md && grep -q 'SessionStatus::Draft' planning-docs/DESIGN-session-trust-state.md && grep -q 'session_demoted' planning-docs/DESIGN-session-trust-state.md && grep -q 'session_status' planning-docs/DESIGN-session-trust-state.md && grep -q 'parent_id' planning-docs/DESIGN-session-trust-state.md</automated>
  </verify>
  <done>The doc's first half specifies SessionStatus::Draft (monotonic Active→Draft), the I1 mint_from_read demotion trigger with trusted-path-only framing, the I0 seed-provenance creation rule with a named CLI on-ramp shape, the new broker-resolved session_status parameter on submit_plan_node, and the atomic UPDATE + session_demoted Event pair with parent_id = the file_read Event id.</done>
</task>

<task type="auto">
  <name>Task 2: Author the executor deny mechanism + acceptance predicate (sink_effect_class table, new DenyReason variant, Step 0.5 placement, exhaustive-match discipline, Done-When)</name>
  <files>planning-docs/DESIGN-session-trust-state.md</files>
  <read_first>
    - planning-docs/DESIGN-session-trust-state.md (the file Task 1 created — append to it)
    - planning-docs/DESIGN-plan-executor.md (analog for the "Sink Sensitivity Map (v0: hardcoded)" section and "Executor Decision Logic" ordered-steps section — mirror both)
    - crates/executor/src/sink_sensitivity.rs (existing is_routing_sensitive / is_content_sensitive hardcoded-table pattern the new sink_effect_class table mirrors)
    - crates/runtime-core/src/executor_decision.rs (existing DenyReason taxonomy — the new variant is appended here, never a second enum)
    - crates/runtime-core/src/plan_node.rs (TaintLabel::is_untrusted exhaustive-match example — the discipline the new enums MUST follow)
    - crates/executor/src/lib.rs (existing fail-closed decision ordering: Step 0 schema validate → resolve → taint/provenance checks)
    - .planning/phases/08-session-trust-confirmation-design-gate/08-RESEARCH.md (Pattern 1, Pitfall 4, Open Questions 1 and 3, Assumption A3, Code Examples)
  </read_first>
  <action>
    Append to `planning-docs/DESIGN-session-trust-state.md`. All rules as MUST / MUST NOT:
    (1) Effect-class-per-sink section (mirror "Sink Sensitivity Map (v0: hardcoded)") — specify a NEW hardcoded function `sink_effect_class(sink: &SinkId) -> EffectClass` living in `crates/executor/src/sink_sensitivity.rs` (or a sibling module in the same crate), mirroring is_routing_sensitive/is_content_sensitive. State that the PlanNode shape stays locked — this is a hardcoded table, NOT a new field on PlanNode (RESEARCH A3, CON-i2-non-bypassable). Map the two current sinks explicitly: `email.send` -> CommitIrreversible, `file.create` -> CommitIrreversible. Specify unknown-sink handling explicitly and fail-closed (an unknown sink MUST NOT default to a permissive class — either treat as CommitIrreversible or reject; state one and justify). Reference the 3-class EffectClass ontology (Observe / MutateReversible / CommitIrreversible) from CON-effect-classes.
    (2) New DenyReason variant — specify appending exactly one variant to the existing single DenyReason enum in runtime_core: `DraftOnlySessionDeniesCommitIrreversible { sink: SinkId }` (carry the SinkId for audit legibility per Open Question 3, matching the existing UnknownSink(String) convention). State the MUST: this is appended to the ONE taxonomy — a second DenyReason-like enum MUST NOT be introduced (Pattern 1).
    (3) Executor decision-logic placement (mirror "Executor Decision Logic") — specify the draft-only check as a new "Step 0.5" that runs immediately after schema validation (Step 0) and before the per-arg resolve/taint loop, because it depends only on the session's status and the sink's effect class, not on any individual arg (Open Question 1). State the predicate: if `sink_effect_class(sink) == EffectClass::CommitIrreversible && session_status == SessionStatus::Draft` then return `ExecutorDecision::Denied { reason: DraftOnlySessionDeniesCommitIrreversible { sink } }`. State the locked MUST: this decision is made in ONE executor TCB function (submit_plan_node), never as a broker pre-check.
    (4) Non-regression MUSTs: a Draft session MUST still allow MutateReversible and Observe class plan nodes (TAINT-03); the new check MUST NOT alter or weaken the existing I2 routing-sensitivity block on genuine taint (protect the v1.1 §9 acceptance).
    (5) Exhaustive-match discipline (Pitfall 4) — state as an explicit MUST that any new enum (EffectClass, and the extended DenyReason) MUST be matched exhaustively with no wildcard `_` arm at every call site, so an unhandled future variant is a compile error, never a silent fail-open. Explicitly forbid `matches!`-style or `_ => permissive` shorthands.
    (6) Acceptance Predicate ("I1/I0 Acceptance Predicate (Done When)") — mirror DESIGN-taint-model.md's numbered-condition format starting at condition 0 (the "who sets this, never self-declared" trust condition), covering: (0) trust state is broker-resolved from the broker's own session store, never self-declared; (1) a mint_from_read demotes the session to Draft with a session_demoted Event whose parent_id is the read Event; (2) an externally-seeded session starts Draft at creation; (3) a CommitIrreversible plan node on a Draft session is Denied with the new variant, decided in the executor; (4) MutateReversible/Observe still succeed on a Draft session. Close with "All N conditions MUST hold simultaneously."
    (7) Accepted Residual Risks — mirror the analog's format for any risk surfaced (e.g. a demotion race between mint_from_read and a concurrent submit_plan_node), each with an `*Accepted for v1.2:*` note naming the mitigation.
    Ensure the whole doc's MUST/MUST NOT density is comparable to DESIGN-plan-executor.md (the gate checklist greps for it).
  </action>
  <acceptance_criteria>
    - `grep -c 'sink_effect_class' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'DraftOnlySessionDeniesCommitIrreversible' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'EffectClass' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -ci 'CommitIrreversible' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -ci 'Step 0.5\|Step 0\.5' planning-docs/DESIGN-session-trust-state.md` is >= 1 (deny-check placement stated).
    - `grep -ci 'exhaustive' planning-docs/DESIGN-session-trust-state.md` is >= 1 (match discipline stated).
    - `grep -ci 'Done When\|Acceptance Predicate' planning-docs/DESIGN-session-trust-state.md` is >= 1.
    - `grep -c 'MUST' planning-docs/DESIGN-session-trust-state.md` is >= 20 (normative-verb density comparable to the analog docs).
    - `grep -ci 'never a broker pre-check\|not a broker pre-check\|one.*TCB.*function\|one TCB' planning-docs/DESIGN-session-trust-state.md` is >= 1 (executor-decides rule stated).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'sink_effect_class' planning-docs/DESIGN-session-trust-state.md && grep -q 'DraftOnlySessionDeniesCommitIrreversible' planning-docs/DESIGN-session-trust-state.md && grep -qi 'exhaustive' planning-docs/DESIGN-session-trust-state.md && [ "$(grep -c 'MUST' planning-docs/DESIGN-session-trust-state.md)" -ge 20 ]</automated>
  </verify>
  <done>The doc specifies the hardcoded sink_effect_class table (email.send/file.create → CommitIrreversible, fail-closed unknown), the appended DraftOnlySessionDeniesCommitIrreversible DenyReason variant, the Step 0.5 executor-only deny placement, the non-regression MUSTs, the exhaustive-match discipline, and a numbered "Done When" acceptance predicate closing with "All N conditions MUST hold simultaneously" — at MUST/MUST NOT density comparable to the analog docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (confined) → broker (IPC over UDS) | untrusted worker messages cross here; the worker MUST NOT be able to declare its own session's trust state |
| broker session store → executor | the executor's draft-only decision depends on trust state that MUST be broker-resolved, never IPC-carried |

## STRIDE Threat Register (design-review level — this phase writes the spec that must foreclose these)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Spoofing / Elevation | session trust-state source | high | mitigate | Doc MUST require the Draft transition (I1) and the initial Draft (I0) be set only by the trusted broker path from provenance/mint — never self-declared by the worker or any IPC flag (RESEARCH Pattern 2, A2). |
| T-08-02 | Elevation | executor deny placement | high | mitigate | Doc MUST assign the draft-only CommitIrreversible deny to one executor TCB function (Step 0.5), never a broker pre-check; a single DenyReason taxonomy (no second enum). |
| T-08-03 | Tampering / Repudiation | demotion audit edge | medium | mitigate | Doc MUST require the session_demoted Event carry parent_id = the triggering file_read Event id, written atomically with the sessions-row UPDATE (TAINT-04). |
| T-08-04 | Elevation (silent fail-open) | new EffectClass / DenyReason enums | medium | mitigate | Doc MUST mandate exhaustive match, no wildcard arm, for both new enums so an unhandled variant is a compile error (Pitfall 4). |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-session-trust-state.md`
- All Task 1 and Task 2 acceptance-criteria greps return non-zero / satisfy their thresholds.
- The doc uses MUST/MUST NOT as its sole normative verbs (spot-check: no load-bearing "should" in rule statements).
- Every one of the three RESEARCH gaps is concretely resolved: SessionStatus::Draft + broker-resolved session_status parameter (gap 1/2), sink_effect_class hardcoded table (gap 3).
</verification>

<success_criteria>
`planning-docs/DESIGN-session-trust-state.md` exists and is precise enough that Phase 9 can implement TAINT-01..04 and ORIGIN-01..02 with no further design decisions: it fixes the variant name (Draft), the transition rule (monotonic Active→Draft), the trust-state parameter (broker-resolved session_status), the effect-class table (sink_effect_class), the deny variant (DraftOnlySessionDeniesCommitIrreversible), the deny placement (Step 0.5, executor-only), the causal audit edge (session_demoted.parent_id = file_read id), and the exhaustive-match discipline.
</success_criteria>

<output>
Create `.planning/phases/08-session-trust-confirmation-design-gate/08-01-SUMMARY.md` when done.
</output>
