---
phase: 35-design-gate-fresh-adversarial-code-trace
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - planning-docs/DESIGN-git-github-http-sinks.md
  - planning-docs/DESIGN-GATE-RECORD-v1.8.md
autonomous: true
requirements: [DESIGN-16]

must_haves:
  truths:
    - "A FRESH, NON-SELF adversarial reviewer (a distinct Fable-5 agent, a different model family from the doc's authoring context) code-traced planning-docs/DESIGN-git-github-http-sinks.md against the actual crates/{sandbox,brokerd,executor,runtime-core} + cli code — not a prose skim."
    - "Every finding was independently re-verified against live code (false-positive discipline) before folding; every accepted finding is resolved as an amendment in the DESIGN doc by TIGHTENING the design (no invariant weakened)."
    - "planning-docs/DESIGN-GATE-RECORD-v1.8.md exists and records gate status CLEARED, reviewer identity & independence, the exact files the reviewer opened, a revision-history table, per-finding claim/code-evidence/resolution, and a no-TCB-code reconfirmation."
    - "No TCB code was written this phase — git status --porcelain crates/ cli/ is empty; check-invariants.sh exits 0; the new mechanism symbols (mint_from_http, TaintLabel::HttpRaw, prepare_git_push, prepare_github_pr, the duplicate-PR CAS, the caprun grant auth-grant) appear ONLY in DESIGN-doc prose, not under crates/."
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD-v1.8.md
  key_links:
    - "The gate record names a reviewer agent DISTINCT from the doc's author, lists every code file the reviewer traced, and ties each finding to the DESIGN-doc section that absorbed its resolution."
    - "Gate CLEARED is the hard authorization for Phases 36-40; the record's closing line reconfirms zero TCB code (git diff touches only planning-docs/ + .planning/)."
    - "The git.push net-allowed-confined-child relaxation (FORK 1) is the flagged top pressure-test item; the record must show the reviewer independently traced and either confirmed or challenged it."
---

<objective>
Clear the v1.8 git/GitHub/HTTP design gate (DESIGN-16): run a FRESH, NON-SELF adversarial code-trace review of `planning-docs/DESIGN-git-github-http-sinks.md` (authored in Plan 35-01), fold every accepted finding back into the doc by tightening the design, and record clearance in `planning-docs/DESIGN-GATE-RECORD-v1.8.md`. This satisfies DESIGN-16's "clears a fresh non-self adversarial code-trace before any TCB code" clause.

Purpose: This is THE hard gate. Per CLAUDE.md and the unbroken precedent (v1.0 P2, v1.2 P8, v1.3 P12, v1.4 P18, v1.5 P23, v1.6 P26, v1.7 P31), NO `crates/executor` / `crates/brokerd` / `crates/sandbox` / `crates/runtime-core` code may be written for Phases 36-40 until this record reads CLEARED. The project's `fresh-context-adversarial-review` discipline is load-bearing: a self-read is NOT sufficient — it has caught 8 real blockers to date (incl. a live exfiltration hole and the recurring P33/P34 confirm-release audit-gap that a passing verifier + green gates both missed). `git.push`'s network-from-a-confined-child credential-injection path is the riskiest new surface in the milestone and demands v1.0-Phase-2-level scrutiny.

Output: `planning-docs/DESIGN-GATE-RECORD-v1.8.md` (gate CLEARED), plus any Round-1 amendments folded into `planning-docs/DESIGN-git-github-http-sinks.md`.

**DOC-ONLY phase. Write NO code under `crates/` or `cli/`. The git diff for this plan must touch only `planning-docs/`.**
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md
@CLAUDE.md
@planning-docs/DESIGN-git-github-http-sinks.md
@planning-docs/DESIGN-GATE-RECORD-v1.7.md
@planning-docs/DESIGN-GATE-RECORD-v1.6.md
</context>

<critical_reviewer_independence_note>
The review spawn is ORCHESTRATOR-OWNED per the standing precedent (`.planning/REQUIREMENTS.md` "Standing precedent"; every prior gate record; the phase intent for Phase 35). The executor of THIS plan MUST spawn a SEPARATE agent as the reviewer via the Agent/Task tool — a distinct **Claude Fable-5** model (`fable`), a different model family/context from the doc's authoring context. The executor MUST NOT perform the review itself by reading the doc (a self-read fails the `fresh-context-adversarial-review` discipline). If `advisor` is used and is unavailable, fall back to `Agent(model:"fable")` per the `advisor-tool-unavailable-fable-fallback` precedent. The gate record MUST name the reviewer agent and prove independence (author≠reviewer, code-traced not prose-read, files-opened listed).
</critical_reviewer_independence_note>

<artifacts_this_phase_produces>
This plan (35-02) produces:

**`planning-docs/DESIGN-GATE-RECORD-v1.8.md`** — modeled on `DESIGN-GATE-RECORD-v1.7.md` / `-v1.6.md`, with: Header (milestone v1.8, document under review `DESIGN-git-github-http-sinks.md`, gate purpose = authorize/block Phases 36-40 TCB code, requirements gated DESIGN-15 + DESIGN-16); Gate status; Reviewer identity & independence (with the exact files-opened list); Revision History table; Findings & resolutions (per-finding claim / re-verified code evidence / resolution → DESIGN §); a "Verified as sound" subsection; No-TCB-code reconfirmation; Verdict.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Spawn a fresh non-self Fable-5 adversarial reviewer to code-trace the DESIGN doc</name>
  <read_first>
    - planning-docs/DESIGN-git-github-http-sinks.md — the doc under review (read to assemble the reviewer's brief, NOT to perform the review yourself).
    - planning-docs/DESIGN-GATE-RECORD-v1.7.md — §"Reviewer identity & independence" and §"What the reviewer must specifically check": MODEL the reviewer brief and independence bar on this.
    - .planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md — the FORK 1 flag ("the net-relaxation is the riskiest surface → explicitly the top item for the adversarial review to pressure-test") and the recurring P33/P34 confirm-release discipline the reviewer must specifically check.
  </read_first>
  <files>(no file writes this task — spawns a reviewer subagent and captures its findings)</files>
  <action>
    Spawn a SEPARATE Fable-5 agent (via the Agent/Task tool, model "fable") as the independent adversarial reviewer. Do NOT review the doc yourself. Give the reviewer a self-contained brief that provides ONLY: (1) the full text of planning-docs/DESIGN-git-github-http-sinks.md, and (2) the instruction to CODE-TRACE it against the actual repo — opening and reading crates/brokerd/src/sinks/{email_smtp.rs,process_exec.rs}, cli/caprun-exec-launcher/src/, crates/brokerd/src/{quarantine.rs,confirmation.rs,server.rs}, crates/executor/src/{sink_sensitivity.rs,sink_schema.rs,lib.rs}, crates/runtime-core/src/{plan_node.rs,effect.rs}, cli/caprun/src/main.rs, and scripts/check-invariants.sh — not a prose skim.

    Direct the reviewer to specifically attack the highest-consequence points: (a) FORK 1 — the git.push net-allowed-confined-child relaxation (the FLAGGED top pressure-test item): does the single-pinned-host:port seccomp relaxation genuinely NOT open arbitrary egress, is the push credential genuinely confined to that child and env_clear()'d otherwise, and is the in-broker-git alternative correctly rejected; (b) mint_from_http — is the inbound response body genuinely minted untrusted-on-arrival, rooted on a REAL http_response_received Event (non-stapled, provenance_chain[0]==that event), and does it genuinely demote the session (I1) — trace it against the mint_from_read/mint_from_exec precedents in quarantine.rs; (c) the marquee tainted-PR-body/commit-message exfil — are title/body/message genuinely I2-sensitive sink args (CONTENT-01) with taint propagated not stapled; (d) SSRF resolve-and-pin — is the denylist complete (loopback/RFC1918/link-local/CGNAT/metadata/ULA/IPv6-mapped), redirects off, userinfo@/non-https/IP-encoding rejected; (e) the P33/P34 confirm-release discipline — does the doc MANDATE terminal-event-before-terminal-state with prepare_git_push/prepare_github_pr (a missing mandate is exactly the recurring MAJOR the discipline exists to catch); (f) credential hygiene — is every broker-held secret genuinely kept out of the ValueNode/plan-arg/audit-literal/worker/planner. Also require it to check I0/I1/I2 non-bypass, the no-new-raw-effect-request-to-sink invariant, executor-hardcoded sink sensitivity, and the Gate 3 mint_from_http extension mandate. Instruct it to return findings by severity (BLOCKER / MAJOR / MINOR / NIT) with file:line evidence for each claim.

    Capture the reviewer's raw findings verbatim for Task 2. Record the reviewer's model/identity, token/tool-use effort, and the exact list of files it reported opening (needed for the gate record's independence section).
  </action>
  <verify>
    <automated>echo "manual: reviewer findings captured; reviewer identity + files-opened recorded for the gate record"</automated>
  </verify>
  <acceptance_criteria>
    - A distinct Fable-5 reviewer agent was spawned (NOT a self-read); its identity/model is recorded.
    - The reviewer's brief provided the DESIGN doc and directed a code-trace against the named crates/ + cli files, with the FORK 1 net-relaxation, mint_from_http, tainted-body exfil, SSRF, the P33/P34 confirm-release discipline, and credential hygiene called out as the highest-consequence attack points.
    - The reviewer returned findings by severity with file:line evidence; the raw findings and the list of files the reviewer opened are captured for Task 2/Task 3.
    - No file under crates/ or cli/ is modified.
  </acceptance_criteria>
  <done>A fresh non-self Fable-5 reviewer has code-traced the doc and returned severity-tagged findings with code evidence; findings + reviewer independence metadata are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify each finding against live code and fold accepted findings into the DESIGN doc</name>
  <read_first>
    - The captured reviewer findings from Task 1.
    - planning-docs/DESIGN-git-github-http-sinks.md — the doc to amend.
    - planning-docs/DESIGN-GATE-RECORD-v1.7.md — §"Findings & resolutions" (the claim / code-evidence / resolution structure) and the "findings independently re-verified against live code before folding" discipline: MODEL on this.
    - The specific code files each finding cites (open and re-verify each cited file:line against live code — AI reviewers generate false positives).
  </read_first>
  <files>planning-docs/DESIGN-git-github-http-sinks.md</files>
  <action>
    For each finding the reviewer returned, INDEPENDENTLY re-verify its claim against the live code it cites (open the exact file:line) before acting — this project's standing discipline is that AI reviewers generate false positives, so every finding is re-checked and either CONFIRMED REAL or dismissed as a false positive with a one-line reason. For each CONFIRMED finding, fold the resolution into the DESIGN doc as a Round-1 amendment: amend the relevant § so the pinned mechanism/default absorbs the fix, and add an "Amendments (post-review)" entry (mirror the prior DESIGN docs' convention) recording what changed and why. Do NOT weaken any invariant to make a finding go away — resolve by TIGHTENING the design (the v1.6 precedent: F1 was resolved by pinning a broker-enforced guarantee, never by deferring to implementer discretion; the v1.7 precedent: the confirm-release MAJOR was resolved by pinning a shared prepare_* precheck + folded terminal event). If a finding is a genuine BLOCKER on a fork decision (e.g. the FORK 1 net-relaxation is ruled unacceptable as specified), tighten the pinned mechanism rather than leaving it unresolved.

    Keep a per-finding resolution ledger (Claim / re-verify outcome / DESIGN-doc section amended) for Task 3's gate record. After folding, re-run scripts/check-invariants.sh and confirm it still exits 0.
  </action>
  <verify>
    <automated>bash scripts/check-invariants.sh && [ -z "$(git status --porcelain crates/ cli/)" ] && echo "invariants green, no TCB code touched"</automated>
  </verify>
  <acceptance_criteria>
    - Every reviewer finding was independently re-verified against the live code it cites and marked CONFIRMED or false-positive with a reason.
    - Every CONFIRMED finding is resolved as a Round-1 amendment in planning-docs/DESIGN-git-github-http-sinks.md by TIGHTENING the design (no invariant weakened); an "Amendments (post-review)" section records each change.
    - A per-finding resolution ledger (claim / re-verify outcome / section amended) is captured for the gate record.
    - scripts/check-invariants.sh exits 0; git status --porcelain crates/ cli/ is empty.
  </acceptance_criteria>
  <done>All findings re-verified and every accepted one folded into the doc as a design-tightening amendment; no invariant weakened; invariant gate green; no TCB code touched.</done>
</task>

<task type="auto">
  <name>Task 3: Write the gate record and reconfirm no TCB code was written</name>
  <read_first>
    - planning-docs/DESIGN-GATE-RECORD-v1.7.md and planning-docs/DESIGN-GATE-RECORD-v1.6.md — MODEL the gate-record structure, the revision-history table, the per-finding claim/evidence/resolution format, the "Verified as sound" subsection, and the no-TCB-code reconfirmation closing on these.
    - .planning/phases/35-design-gate-fresh-adversarial-code-trace/35-CONTEXT.md — the required doc contents the record must confirm the reviewer actually checked (11 pitfalls, three forks, mint_from_http/HttpRaw/CAS/auth-grant, invariant preservation, P33/P34 discipline).
    - The per-finding resolution ledger and reviewer independence metadata from Tasks 1-2.
  </read_first>
  <files>planning-docs/DESIGN-GATE-RECORD-v1.8.md</files>
  <action>
    Create planning-docs/DESIGN-GATE-RECORD-v1.8.md with the full structure: Header (milestone v1.8, document under review DESIGN-git-github-http-sinks.md, gate purpose = authorize/block Phases 36-40 TCB code, requirements gated DESIGN-15 + DESIGN-16). Gate status (CLEARED or BLOCKED, round number, date). Reviewer identity & independence (the fresh distinct Fable-5 agent mechanism, explicitly NOT the authoring context, code-traced not prose-read, the EXACT list of files the reviewer opened, effort in tokens/tool-uses). Revision History table (round / date / reviewer / findings-by-severity / result). Findings & resolutions (per finding: Claim, Code evidence with file:line re-verified against live code, Resolution naming the DESIGN-doc § that absorbed it; false positives recorded as dismissed with the reason). A "Verified as sound" subsection listing the load-bearing claims the reviewer confirmed by tracing real code — at minimum the FORK 1 net-relaxation, mint_from_http fresh-rooting + session demotion, the tainted-body I2 sensitivity, the SSRF denylist, and the P33/P34 confirm-release mandate (evidence the review was a genuine code-trace, not a skim — mirror v1.7's structure). No-TCB-code reconfirmation (git status --porcelain crates/ cli/ empty; the new mechanism symbols — mint_from_http, TaintLabel::HttpRaw, prepare_git_push, prepare_github_pr, the duplicate-PR CAS table, the caprun grant auth-grant event — appear ONLY in DESIGN-doc prose, not under crates/). Verdict.

    Only mark the gate CLEARED if every finding is resolved (folded or dismissed-as-false-positive) and no BLOCKER remains open. If a BLOCKER cannot be resolved by a design amendment, mark BLOCKED and stop — do NOT clear a gate with an open blocker.

    Finally, confirm the whole-phase diff: run git status --porcelain and confirm only planning-docs/ and .planning/ paths appear (no crates/, no cli/). Run scripts/check-invariants.sh once more and confirm exit 0.
  </action>
  <verify>
    <automated>test -f planning-docs/DESIGN-GATE-RECORD-v1.8.md && grep -qiE 'cleared|blocked' planning-docs/DESIGN-GATE-RECORD-v1.8.md && grep -qi 'fable' planning-docs/DESIGN-GATE-RECORD-v1.8.md && [ -z "$(git status --porcelain crates/ cli/)" ] && bash scripts/check-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - planning-docs/DESIGN-GATE-RECORD-v1.8.md exists with all required sections (header, gate status, reviewer identity & independence with the files-opened list, revision history, findings & resolutions, "Verified as sound", no-TCB-code reconfirmation, verdict).
    - The gate status is CLEARED only if every finding is resolved and no BLOCKER is open; a distinct Fable-5 reviewer (not the author) is named and its code-trace independence is documented.
    - Each finding ties to the DESIGN-doc section that absorbed its resolution (or is recorded as a dismissed false positive with a reason); the FORK 1 net-relaxation is explicitly among the "Verified as sound" traced claims.
    - git status --porcelain shows only planning-docs/ and .planning/ paths (no crates/, no cli/); scripts/check-invariants.sh exits 0.
  </acceptance_criteria>
  <done>The gate record exists, reads CLEARED (findings resolved, no open blocker) with a documented non-self Fable-5 code-trace, and reconfirms zero TCB code; the phase's authorization for Phases 36-40 is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DESIGN doc → adversarial reviewer | The doc's claims cross to an independent agent that must verify them against real code, not accept prose at face value. |
| reviewer findings → orchestrator fold | Findings (possibly false positives) cross into design amendments; each must be re-verified against live code before folding. |
| gate record → Phases 36-40 | A CLEARED record is the sole authorization for TCB code; a wrongly-cleared gate would let an unsound git.push net-relaxation or a stapled-taint mint_from_http proceed. |

## STRIDE Threat Register

Design-gate-review scope: the threats to the GATE itself (a bad clearance), not to runtime.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35R-01 | Spoofing | reviewer independence | high | mitigate | The reviewer MUST be a distinct Fable-5 agent (not a self-read); the gate record proves independence (author≠reviewer, files-opened listed, code-traced). |
| T-35R-02 | Repudiation | finding resolution | high | mitigate | Per-finding ledger ties each claim to re-verified code evidence and the amended DESIGN §; no finding silently dropped. |
| T-35R-03 | Tampering | gate clearance | critical | mitigate | CLEARED only if no BLOCKER open and every finding resolved by tightening the design (never weakening an invariant); else BLOCKED. |
| T-35R-04 | Elevation of Privilege | TCB-code leak this phase | critical | mitigate | No-TCB-code reconfirmation: git status crates/ cli/ empty; mechanism symbols (mint_from_http, HttpRaw, prepare_git_push/github_pr, CAS, caprun grant) exist only in DESIGN prose. |
| T-35R-05 | Information Disclosure | unreviewed FORK 1 net-relaxation | critical | mitigate | The record's "Verified as sound" subsection must show the reviewer independently traced the git.push net-allowed-confined-child relaxation (the flagged top pressure-test item). |
| T-35R-SC | Tampering | cargo installs | low | accept | No package installs this phase (doc + review only). |
</threat_model>

<verification>
- `test -f planning-docs/DESIGN-GATE-RECORD-v1.8.md` — the gate record exists.
- The record reads CLEARED (or BLOCKED if a blocker could not be resolved), names a distinct Fable-5 reviewer, lists the files the reviewer opened, ties each finding to a DESIGN-doc resolution, and shows the FORK 1 net-relaxation among the traced-sound claims.
- `[ -z "$(git status --porcelain crates/ cli/)" ]` — no TCB code written this phase.
- `bash scripts/check-invariants.sh` exits 0.
</verification>

<success_criteria>
- A fresh, non-self adversarial code-trace review of `planning-docs/DESIGN-git-github-http-sinks.md` cleared the doc with every finding resolved (DESIGN-16).
- `planning-docs/DESIGN-GATE-RECORD-v1.8.md` records the clearance with reviewer independence, files traced, findings & resolutions, and a no-TCB-code reconfirmation.
- No `crates/executor` / `crates/brokerd` / `crates/sandbox` / `crates/runtime-core` code was written; the phase's git diff touches only `planning-docs/` and `.planning/`.
- Phases 36-40 are authorized to begin.
</success_criteria>

<output>
Create `.planning/phases/35-design-gate-fresh-adversarial-code-trace/35-02-SUMMARY.md` when done.
</output>
