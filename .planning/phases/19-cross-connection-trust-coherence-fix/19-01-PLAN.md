---
phase: 19-cross-connection-trust-coherence-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/brokerd/src/server.rs
  - crates/brokerd/tests/two_connection_intent_bypass.rs
autonomous: true
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "A 2nd connection to a session whose slot is already occupied receives a single framed BrokerResponse::Error and is dropped BEFORE any handle_connection task, ValueStore, or session_status/intent_provided/fd_requested state is constructed for it."
    - "The occupancy latch is set the first time a connection is accepted and is NEVER cleared for the life of the run_broker_server invocation — no release-on-disconnect, no Drop guard, no reconnect path."
    - "The overlapping-connection repro (conn#1 stays open, conn#2 ProvideIntent) resolves SAFE: conn#2 rejected, or if accepted the routed decision is not Allowed."
    - "A NEW sequential-reconnect repro (conn#1 RequestFd then cleanly disconnects, THEN conn#2 connects and attempts ProvideIntent) resolves SAFE by the identical predicate."
    - "The Part A guard(a) intra-connection control still passes against its OWN fresh broker instance."
    - "The regression test's Part C safe-outcome assertions are byte-identical to the pre-fix HEAD version — never weakened."
  artifacts:
    - crates/brokerd/src/server.rs
    - crates/brokerd/tests/two_connection_intent_bypass.rs
  key_links:
    - "The latch check MUST sit in run_broker_server's accept loop BEFORE tokio::spawn (server.rs lines 98-121) — never inside handle_connection where per-connection locals (lines 144-154) are already seeded."
    - "Each test variant (guard-a control, overlapping, sequential-reconnect) MUST spawn its OWN fresh run_broker_server instance on its OWN socket name so its latch starts unset."
---

<objective>
Close the cross-connection `ProvideIntent` bypass by adding a one-way, session-lifetime occupancy latch to the broker's accept loop, and restructure the existing regression test so both the overlapping AND the new sequential-reconnect variants go green by the fix — never by weakening assertions.

Purpose: This is the v1.4 Phase-0 fix. Guard(a)'s "ProvideIntent once, before any RequestFd" guarantee is per-connection; a 2nd `AF_UNIX` connection to the same session socket gets pristine guard(a) state and a stale `session_status`, letting a confined worker mint an attacker-controlled `UserTrusted` literal and route it to `email.send` as `Allowed`. Rejecting the 2nd connection at accept time — before any per-connection trust state is seeded — closes it with the smallest TCB change. Binding spec: `planning-docs/DESIGN-session-trust-coherence.md` §2 (fix shape) + §1 (test requirements), gate CLEARED per `DESIGN-GATE-RECORD-v1.4.md`.

Output: `crates/brokerd/src/server.rs` (accept-loop latch) and `crates/brokerd/tests/two_connection_intent_bypass.rs` (restructured into 3 per-variant fresh-broker instances, `#[ignore]` removed). Final GREEN is observed on real Linux in Plan 02 (these tests show "0 passed" on the macOS dev box by design — `#[cfg(target_os = "linux")]`).
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-session-trust-coherence.md
@planning-docs/DESIGN-GATE-RECORD-v1.4.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a one-way occupancy latch to run_broker_server's accept loop (TRUST-01)</name>
  <files>crates/brokerd/src/server.rs</files>
  <read_first>
    - crates/brokerd/src/server.rs lines 62-122 (the `run_broker_server` doc + accept loop; the latch is added here — current accept `let (stream, _addr) = listener.accept().await?;` at line 99, `tokio::spawn(...)` at line 106).
    - crates/brokerd/src/server.rs lines 124-154 (`handle_connection` — the per-connection locals the latch must protect: `value_store` line 144, `session_status` line 148, `intent_provided`/`fd_requested` lines 153-154. The gate must fire BEFORE any of these exist for a 2nd connection.)
    - crates/brokerd/src/server.rs lines 1030-1039 (`send_response` helper signature — reuse it to send the framed rejection; it takes `&mut UnixStream` and `&BrokerResponse`).
    - planning-docs/DESIGN-session-trust-coherence.md §2 in full (lines 40-64) — the ONE-WAY latch ruling, the "never released" requirement, and the explicit rejection of release-on-disconnect (lines 46-52).
  </read_first>
  <action>
    In `run_broker_server`, declare a single accept-loop-local mutable boolean occupancy latch (a plain `bool`, initialized to indicate the session slot is NOT yet occupied) in the function's own frame, immediately before the `loop {` at line 98. Per DESIGN §2 line 52, a loop-local `bool` is sufficient — the accept loop is single-threaded per session, so NO `Arc`, `Mutex`, or `AtomicBool` cross-task sharing is needed. Do NOT introduce shared coherent multi-connection state (the REJECTED alternative named in DESIGN §2 lines 56-62).

    Inside the loop, after `listener.accept().await?` yields the stream, and BEFORE the `tokio::spawn` that hands the stream to `handle_connection`: if the latch is already set (slot occupied), reject this just-accepted connection — send a SINGLE framed `BrokerResponse::Error` via `send_response` naming the reason (that a connection is already established for this session and only one connection per session is permitted), then let the stream drop and `continue` the loop. The rejection path MUST NOT construct a `handle_connection` task, a `ValueStore`, or seed `session_status`/`intent_provided`/`fd_requested` (DESIGN §2 line 54). The just-accepted `stream` binding will need to be `mut` for `send_response`.

    If the latch is NOT yet set, set it (mark the slot occupied) and then proceed into the existing `tokio::spawn` → `handle_connection` path unchanged. The latch is written exactly ONCE and is NEVER reset to the unoccupied value anywhere — no release-on-disconnect, no `Drop` guard, no reset after `handle_connection` exits (DESIGN §2 lines 46-52: release-on-disconnect is a BLOCKER that leaves the sequential variant exploitable, not a convenience). Do NOT add any release path.

    Do not touch `handle_connection`'s per-connection locals or their semantics. Do not widen guard-(c)/`CAPRUN_ENABLE_IPC_CREATE_SESSION` (DESIGN §4 — out of scope). Do not introduce the token `EffectRequest` anywhere (check-invariants.sh Gate 1).
  </action>
  <acceptance_criteria>
    - `cargo build -p brokerd` compiles clean (no warnings introduced by the change).
    - The latch variable is declared once (initialized unoccupied) in `run_broker_server`'s frame before the `loop`, is set to occupied exactly once (on the first accepted connection), and appears in NO assignment back to the unoccupied value anywhere in the file. Evidence: `grep -n '<latch_name>' crates/brokerd/src/server.rs` shows only the single init, the single set-to-occupied, and the guard check — no reset, no Drop.
    - There is NO `Drop` impl, no `.await`-on-disconnect handler, and no code path inside or after the `tokio::spawn` that clears the latch. Evidence: `grep -niE 'release|reset|Drop for|= false' crates/brokerd/src/server.rs` shows no latch-clearing occurrence (the accept-loop latch is never cleared).
    - The rejection branch calls `send_response` with a `BrokerResponse::Error` and `continue`s the loop WITHOUT calling `handle_connection` — verifiable by reading: the `handle_connection(` call is reachable only on the not-yet-occupied branch.
    - `./scripts/check-invariants.sh` passes (no `EffectRequest` token introduced; TCB invariants intact).
    - `cargo test -p brokerd --lib` passes on the Mac (the in-crate unit tests for guard(a) ordering and dispatch arms are unaffected).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p brokerd 2>&1 | tail -5 && ./scripts/check-invariants.sh && cargo test -p brokerd --lib 2>&1 | tail -15</automated>
  </verify>
  <done>The accept loop refuses every connection after the first with a framed `BrokerResponse::Error`, the latch is one-way (set once, never cleared), no per-connection trust state is seeded for a rejected connection, and the crate builds + passes invariants + lib tests on the Mac. Final Linux green is proven in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure two_connection_intent_bypass.rs into 3 fresh-broker variants and un-ignore (TRUST-02)</name>
  <files>crates/brokerd/tests/two_connection_intent_bypass.rs</files>
  <read_first>
    - crates/brokerd/tests/two_connection_intent_bypass.rs IN FULL (all 325 lines — this is a restructure, not a one-line un-ignore). Note: the module doc (lines 1-30), the `send_req`/`read_resp`/`request_fd` helpers (lines 45-87), the single `two_connection_intent_bypass_repro` fn (lines 100-324) with Part A (lines 166-203), Part B (lines 205-291), and Part C safe-outcome assertions (lines 294-323).
    - planning-docs/DESIGN-session-trust-coherence.md §1 lines 32-36 — the structural note: Part A trips the one-way latch for the WHOLE broker instance, so under the fix, running Part A's control connection against the same broker as Part B would corrupt Part B's `FdGranted` setup expectation (a SETUP-assertion issue, not a safety regression). Each variant needs its OWN fresh `run_broker_server` instance/socket. Also lines 34: the sequential-reconnect variant requirement.
    - crates/brokerd/src/server.rs lines 85-93 (the exact `run_broker_server` parameter list the test must call per variant).
  </read_first>
  <action>
    Restructure the test file into THREE independent `#[tokio::test]` functions, each spawning its OWN fresh `run_broker_server` instance on its OWN unique socket name (derive distinct names, e.g. by suffixing the pid with the variant), each seeding its own in-memory audit DB, `sessions` row, and workspace. Factor the shared setup (open audit DB, seed session row, create workspace + `attacker_doc.txt`, spawn `run_broker_server`, yield) into a helper that returns the socket path (and anything the variants need), so each test gets a correctly-scoped broker whose latch starts unset. Remove the top-level `#[ignore]` attribute (line 101) — none of the three tests may carry `#[ignore]`.

    Variant 1 — guard(a) intra-connection control (from current Part A, lines 166-203): one connection does `RequestFd` then `ProvideIntent` on the SAME connection; assert the `ProvideIntent` is rejected with `BrokerResponse::Error` (guard(a) works normally within one connection). This variant's single connection legitimately trips the latch — that is fine, it only ever opens one connection.

    Variant 2 — overlapping-connection repro (from current Part B, lines 205-291, plus Part C assertions lines 294-323): conn#1 does `RequestFd` and STAYS OPEN; a fresh conn#2 connects to the same socket and attempts `ProvideIntent` with the attacker literal, then (if accepted) routes to `email.send` and evaluates. Apply the SAME safe-outcome predicate as today's Part C: SAFE iff conn#2's `ProvideIntent` was rejected (Error) OR the routed decision was NOT `Allowed`; LIVE EXFIL (panic) iff `IntentAccepted` AND decision `Allowed`.

    Variant 3 — NEW sequential-reconnect repro (DESIGN §1 line 34): conn#1 does `RequestFd` and then CLEANLY DISCONNECTS (drop the stream) BEFORE conn#2 connects; then conn#2 connects to the same socket and attempts `ProvideIntent` with the attacker literal, routing to `email.send` if accepted. Assert the IDENTICAL safe-outcome predicate as Variant 2. This variant is what a release-on-disconnect implementation would fail — it must go green under the one-way latch.

    CRITICAL — do NOT weaken the safe-outcome assertions. The Part C predicate text (the `if intent_accepted && routed_allowed { panic!(...) }` block and the final `assert!(!intent_accepted || !routed_allowed, ...)`) must be preserved VERBATIM in BOTH Variant 2 and Variant 3 — reproduce the exact same condition, message, and semantics from the pre-fix Part C (lines 300-323). You are reorganizing connection topology only. If the fix is correct, the 2nd/reconnecting connection is rejected outright at accept time (`BrokerResponse::Error`), so `intent_accepted` is false and both variants pass on the SAFE branch.
  </action>
  <acceptance_criteria>
    - The file contains exactly three `#[tokio::test]` functions (guard-a control, overlapping, sequential-reconnect); `grep -c '#\[tokio::test\]' crates/brokerd/tests/two_connection_intent_bypass.rs` returns 3.
    - No `#[ignore]` remains: `grep -c 'ignore' crates/brokerd/tests/two_connection_intent_bypass.rs` (excluding comment prose) returns 0 attribute occurrences.
    - Each of the three tests calls `run_broker_server` against its own distinct socket name: `grep -c 'run_broker_server' crates/brokerd/tests/two_connection_intent_bypass.rs` returns 3 (one per variant), and the three socket-name strings are distinct.
    - The safe-outcome predicate is unchanged: the `panic!` message beginning `LIVE EXFIL` and the final `assert!` with the `SAFE expectation` message from pre-fix HEAD (`git show HEAD:crates/brokerd/tests/two_connection_intent_bypass.rs` lines 310-323) appear VERBATIM in both the overlapping and sequential variants. Confirm by diffing the predicate region against HEAD — no character of the assertion condition or message changed.
    - `cargo build --tests -p brokerd` compiles the test file clean on the Mac (the `#[cfg(target_os = "linux")]` module compiles; 0 tests run on macOS — expected, not a gap).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p brokerd --test two_connection_intent_bypass 2>&1 | tail -10 && echo "--- verbatim assertion check ---" && git show HEAD:crates/brokerd/tests/two_connection_intent_bypass.rs | grep -c 'SAFE expectation'</automated>
  </verify>
  <done>The test file is three per-variant fresh-broker tests (guard-a control, overlapping, sequential-reconnect), `#[ignore]` is gone, the Part C safe-outcome assertions are byte-identical to HEAD in both repro variants, and the file compiles under `cargo build --tests` on the Mac. Green is observed on Linux in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined worker → broker (abstract UDS) | A kernel-confined worker retains `AF_UNIX` reachability to the broker's abstract socket (seccomp permits `AF_UNIX`; Landlock does not cover abstract sockets). It can open N connections to one session socket — the untrusted-input crossing this fix closes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | `run_broker_server` accept loop (server.rs 98-121) | critical | mitigate | One-way occupancy latch set before `tokio::spawn` — a 2nd connection is rejected before per-connection guard(a) state is ever seeded, so it cannot mint an attacker literal `UserTrusted`. |
| T-19-02 | Elevation of Privilege | latch lifetime (release-on-disconnect variant) | critical | mitigate | Latch is one-way, never cleared — the sequential-reconnect variant (conn#1 disconnect → conn#2) is rejected identically to the overlapping case. Variant 3 test proves this; a release path would leave it live (DESIGN §2 lines 46-52). |
| T-19-03 | Tampering | regression test safe-outcome assertions | high | mitigate | Part C predicate preserved verbatim in both repro variants; a diff-against-HEAD acceptance gate blocks any weakening that would hide the vuln. |
| T-19-SC | Tampering | cargo dependency surface | low | accept | No new crate dependency is added (the latch is a loop-local `bool`; the test reuses existing imports). No Package Legitimacy Gate needed for this phase. |
</threat_model>

<verification>
- `cargo build -p brokerd` and `cargo build --tests -p brokerd` compile clean on the Mac.
- `./scripts/check-invariants.sh` passes (no `EffectRequest`, TCB invariants intact).
- `cargo test -p brokerd --lib` passes on the Mac; the `two_connection_intent_bypass` module compiles and reports 0 tests on macOS (expected — Linux-only).
- Grep gates: latch never cleared; 3 `#[tokio::test]` fns; 0 `#[ignore]` attributes; 3 distinct `run_broker_server` sockets; Part C assertions verbatim vs HEAD.
- NOTE: the actual RED→GREEN observation of the three tests happens on real Linux in Plan 02 via `scripts/mailpit-verify.sh`. This plan is "done" when the code compiles and passes all Mac-side gates; if Plan 02's Linux run comes back red, gap-closure returns to these two files.
</verification>

<success_criteria>
- The broker accept loop rejects every connection after the first with a framed `BrokerResponse::Error`, before any per-connection trust state is seeded (TRUST-01).
- The latch is one-way — set once on first accept, never cleared (no release-on-disconnect / Drop / reconnect).
- `two_connection_intent_bypass.rs` is restructured into three per-variant fresh-broker tests (guard-a control, overlapping, sequential-reconnect), `#[ignore]` removed, Part C safe-outcome assertions byte-identical to HEAD (TRUST-02).
- All Mac-side gates pass; Linux green deferred to Plan 02.
</success_criteria>

<artifacts_produced>
This phase (Plan 01) creates/modifies:
- `crates/brokerd/src/server.rs` — one-way occupancy latch in the accept loop (TRUST-01)
- `crates/brokerd/tests/two_connection_intent_bypass.rs` — restructured into 3 per-variant fresh-broker tests incl. the new sequential-reconnect variant, `#[ignore]` removed (TRUST-02)
</artifacts_produced>

<output>
Create `.planning/phases/19-cross-connection-trust-coherence-fix/19-01-SUMMARY.md` when done. Record: the exact latch variable name + line, confirmation it is never cleared, the three test fn names + their socket names, and confirmation the Part C assertions are verbatim vs HEAD. Do NOT touch ROADMAP.md or STATE.md (the orchestrator owns phase-completion state).
</output>
