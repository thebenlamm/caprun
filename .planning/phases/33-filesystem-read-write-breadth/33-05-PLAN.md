---
phase: 33-filesystem-read-write-breadth
plan: 05
type: execute
wave: 3
depends_on: ["33-01", "33-02", "33-03", "33-04"]
files_modified:
  - cli/caprun/tests/s9_file_write_block.rs
autonomous: true
requirements: [FS-01, FS-02, FS-03]
must_haves:
  truths:
    - "a tainted value routed into file.write's path OR contents slot is deterministically BlockedPendingConfirmation by the UNMODIFIED submit_plan_node collect-then-Block loop"
    - "the Block is proven genuine (non-stapled): the blocked value's provenance_chain[0] anchors to its originating Event as an unbroken edge"
    - "the entire FS-01/02/03 change set compiles under real Linux — every #[cfg(target_os=\"linux\")] caller is enumerated, closing the cfg-linux test-blindness gap"
  artifacts:
    - "cli/caprun/tests/s9_file_write_block.rs — FS-03 genuine-taint-Block acceptance test"
  key_links:
    - "s9_file_write_block mirrors s9_process_exec_block's non-live half (mint tainted value → submit_plan_node into file.write slot → assert BlockedPendingConfirmation + unbroken provenance_chain[0]), WITHOUT any spawn/confinement machinery (fs write has no exec-child)"
  prohibitions:
    - "NO live spawn/confinement/SMTP machinery in this test — file.write is a single in-broker openat2, not a child process; the full composed live proof is Phase 34 (LIVE-01/02), NOT this phase"
    - "the mandatory Linux verification MUST capture the true exit code BEFORE any pipe and assert on named test counts — never `script | tail` exit-code laundering ([[verification-exit-code-through-pipe]])"
---

<objective>
Prove FS-03's genuine (non-stapled) taint-Block on `file.write` with an in-process acceptance test, and run the MANDATORY Linux compile-check that enumerates every `#[cfg(target_os="linux")]` caller across the whole FS change set.

Purpose: FS-03 requires a tainted path/contents in a sensitive `file.write` slot to Block via the unmodified executor. This plan adds the dedicated negative acceptance test (LIVE-02 requires a dedicated negative test per new sink) and closes the project's standing cfg-linux test-blindness gap — a Mac-green build compiles ZERO Linux test targets, so a signature/flag change can hide broken Linux sites (279/279 macOS-green once hid 8). The full composed live proof is Phase 34, not here.
Output: `cli/caprun/tests/s9_file_write_block.rs` + a green Linux compile-check + scoped Linux test run.

## Artifacts this plan produces
- `cli/caprun/tests/s9_file_write_block.rs` — mints a tainted value into `file.write`'s `path` and `contents` slots and asserts `BlockedPendingConfirmation` with an unbroken `provenance_chain[0]` anchor
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/33-filesystem-read-write-breadth/33-RESEARCH.md
@.planning/phases/33-filesystem-read-write-breadth/33-PATTERNS.md
@.planning/phases/33-filesystem-read-write-breadth/33-VALIDATION.md
@.planning/phases/33-filesystem-read-write-breadth/33-01-SUMMARY.md
@.planning/phases/33-filesystem-read-write-breadth/33-02-SUMMARY.md
@.planning/phases/33-filesystem-read-write-breadth/33-03-SUMMARY.md
@.planning/phases/33-filesystem-read-write-breadth/33-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FS-03 genuine-taint-Block acceptance test (s9_file_write_block.rs)</name>
  <files>cli/caprun/tests/s9_file_write_block.rs</files>
  <read_first>
    - cli/caprun/tests/s9_process_exec_block.rs (the structural template — mint a tainted value, submit_plan_node, assert BlockedPendingConfirmation + provenance_chain[0]; DROP its launcher/pre_exec/spawn machinery)
    - crates/executor/src/lib.rs submit_plan_node (the UNMODIFIED collect-then-Block loop this test exercises)
    - crates/executor/src/sink_sensitivity.rs (file.write path routing-sensitive, contents content-sensitive — from Plan 02)
    - planning-docs/DESIGN-effect-breadth-exec.md §4 (I2 for the new sinks)
  </read_first>
  <behavior>
    - Tainted path → Block: mint an untrusted-tainted ValueNode, route it into file.write's `path` slot via a PlanNode, call submit_plan_node → assert `BlockedPendingConfirmation` (routing-sensitive path with untrusted taint).
    - Tainted contents → Block: same with the taint in the `contents` slot (content-sensitive) → assert `BlockedPendingConfirmation`.
    - Genuine non-stapled chain: the blocked value's `provenance_chain[0]` equals its originating Event's id (unbroken anchor), mirroring s9_process_exec_block's chain assertion.
    - (optional positive contrast, if the harness makes it cheap) a clean (UserTrusted) path+contents pair is NOT blocked at the sensitivity+taint step.
  </behavior>
  <action>
    Create `cli/caprun/tests/s9_file_write_block.rs` mirroring `s9_process_exec_block.rs`'s NON-live half. Reuse whatever ValueStore/mint fixture s9_process_exec_block uses to produce an untrusted-tainted value with a real provenance anchor, then build a `file.write` PlanNode with that value in the `path` (case 1) and `contents` (case 2) slot and call `executor::submit_plan_node`. Assert `BlockedPendingConfirmation` and that `provenance_chain[0]` anchors to the originating Event (unbroken edge — the genuine-taint guarantee, NOT stapled at the sink). Do NOT import or set up any launcher/pre_exec/spawn/SMTP machinery — file.write is a single in-broker openat2 with no child process. This test compiles and runs on macOS (in-process, no cfg-linux gate on the executor path).
  </action>
  <verify>
    <automated>cargo test -p caprun --test s9_file_write_block</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cli/caprun/tests/s9_file_write_block.rs` exists, mirrors s9_process_exec_block's non-live structure, with no spawn/confinement setup.
    - Behavior: tainted path and tainted contents each yield `BlockedPendingConfirmation`; the blocked value's `provenance_chain[0]` is an unbroken anchor to its originating Event.
    - Test command: `cargo test -p caprun --test s9_file_write_block` passes on macOS.
  </acceptance_criteria>
  <done>FS-03's genuine-taint Block on file.write is proven in-process; the test passes on macOS.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory Linux compile-check + scoped Linux test run for the whole FS change set</name>
  <files>cli/caprun/tests/s9_file_write_block.rs</files>
  <read_first>
    - CLAUDE.md ("Linux-only security tests", the mailpit-verify recipe, and the MAILPIT_VERIFY_CMD override contract)
    - .planning/phases/33-filesystem-read-write-breadth/33-VALIDATION.md (Sampling rate: per-wave-merge Linux compile enumeration is MANDATORY for waves touching adapter-fs/brokerd/executor)
    - .planning/phases/33-filesystem-read-write-breadth/33-RESEARCH.md Pitfall 5 (cfg-linux test-blindness)
  </read_first>
  <action>
    Run the MANDATORY Linux verification in the rust:1 Colima container — this enumerates every `#[cfg(target_os="linux")]` caller that a macOS build never compiles (the standing [[cfg-linux-test-blindness]] lesson: write_within's Linux impl + its negative tests are all Linux-gated).

    Step 1 — Linux compile enumeration (catches broken Linux-gated sites from Plans 01/03/04):
    `MAILPIT_VERIFY_CMD='cargo build --tests --workspace --keep-going' bash scripts/mailpit-verify.sh`

    Step 2 — scoped Linux test run of every new target:
    `MAILPIT_VERIFY_CMD='cargo test -p adapter-fs write_within && cargo test -p brokerd file_write && cargo test -p brokerd request_fd && cargo test -p executor file_write && cargo test -p caprun --test s9_file_write_block' bash scripts/mailpit-verify.sh`

    Capture the true exit code BEFORE any pipe and assert on named test counts (the new `write_within_*`, `file_write`, `request_fd`, and `s9_file_write_block` targets all passing). NEVER assert on `script | tail` exit 0 (the standing [[verification-exit-code-through-pipe]] incident — a pipe returns tail's status, always 0). Do NOT fold Phase 34's composed live proof (mailpit-verify full-suite exec+fs acceptance) into this step — Phase 33's gate is the per-requirement tests only.
  </action>
  <verify>
    <automated>MAILPIT_VERIFY_CMD='cargo build --tests --workspace --keep-going' bash scripts/mailpit-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the whole workspace (including all `#[cfg(target_os="linux")]` test targets) compiles under rust:1; every new FS test target passes on real Linux.
    - Test command: both MAILPIT_VERIFY_CMD runs above complete with true exit 0 captured before any pipe, and named FS test counts are non-zero and green.
    - No Phase 34 composed live-proof is run here.
  </acceptance_criteria>
  <done>The full FS-01/02/03 change set compiles and its tests pass on real Linux; the cfg-linux test-blindness gap is closed for this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → executor | a tainted value routed into a sensitive file.write slot must Block, not reach the sink |
| dev machine (macOS) → real Linux | a Mac-green build must not be mistaken for Linux-verified enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-11 | Information Disclosure / Tampering | taint chain not genuinely enforced (stapled-at-sink) | critical | mitigate | s9_file_write_block asserts BlockedPendingConfirmation via the UNMODIFIED submit_plan_node AND an unbroken provenance_chain[0] anchor |
| T-33-12 | Tampering (false assurance) | cfg-linux test-blindness hiding broken Linux-gated sites | high | mitigate | mandatory `cargo build --tests --workspace --keep-going` + scoped test run in the rust:1 container, true-exit-before-pipe |
| T-33-SC | Tampering (supply chain) | dependencies | low | accept | no new package installs this phase — no new deps in any FS plan |
</threat_model>

<verification>
- `cargo test -p caprun --test s9_file_write_block` green on macOS.
- `MAILPIT_VERIFY_CMD='cargo build --tests --workspace --keep-going' bash scripts/mailpit-verify.sh` green on real Linux (all cfg-linux callers compile), true-exit-before-pipe.
- Scoped Linux test run green across adapter-fs/brokerd/executor/caprun new targets, asserted on named counts.
</verification>

<success_criteria>
FS-03's genuine non-stapled taint-Block on file.write is proven in-process, and the entire FS-01/02/03 change set compiles + passes its tests on real Linux with no cfg-linux blind spots — leaving only Phase 34's composed live proof outstanding.
</success_criteria>

<output>
Create `.planning/phases/33-filesystem-read-write-breadth/33-05-SUMMARY.md` when done.
</output>
