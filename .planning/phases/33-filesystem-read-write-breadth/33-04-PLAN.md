---
phase: 33-filesystem-read-write-breadth
plan: 04
type: execute
wave: 2
depends_on: ["33-01", "33-02", "33-03"]
files_modified:
  - crates/brokerd/src/sinks/file_write.rs
  - crates/brokerd/src/sinks.rs
  - crates/brokerd/src/server.rs
autonomous: true
requirements: [FS-02]
must_haves:
  truths:
    - "on an Allowed file.write plan node, the broker resolves path+contents from the ValueStore and calls WorkspaceRoot::write_within, modifying the existing file"
    - "success appends a sink_executed audit Event; failure appends a sink_execution_failed Event FIRST then propagates (no retry) — both chained onto parent_id/parent_hash so verify_chain stays intact"
    - "the file.write dispatch is a fourth sibling Allowed-arm in evaluate_plan_node_and_record, mirroring file.create exactly, with output_value_id untouched (stays None) — file.write is terminal, mints no new value"
  artifacts:
    - "crates/brokerd/src/sinks/file_write.rs::invoke_file_write + its private resolve_arg helper"
    - "crates/brokerd/src/sinks.rs: pub mod file_write;"
    - "crates/brokerd/src/server.rs: the file.write Allowed-dispatch arm"
    - "inline test for invoke_file_write two-phase audit (in-memory rusqlite)"
  key_links:
    - "invoke_file_write's actor field = format!(\"sink:file.write:{effect_id}\"); it calls workspace_root.write_within(&path, contents.as_bytes())"
  prohibitions:
    - "file.write MUST NOT mint (no mint_from_read/mint_from_derivation/mint_from_exec/.mint) — it only resolves existing handles via ValueStore::resolve; therefore NO check-invariants.sh Gate 3 change (RESEARCH drift-note #3)"
    - "NO output_value_id threading in the file.write arm (stays None, unlike process.exec)"
    - "NO new raw EffectRequest token — file.write is a PlanNode{sink,args} from spawn; check-invariants.sh Gate 1 stays green"
    - "resolve_arg MUST be a private copy in file_write.rs — do NOT import/extract a shared cross-module helper"
---

<objective>
Land the `file.write` broker sink: a new `invoke_file_write` module mirroring `invoke_file_create` verbatim (two-phase durable audit), its `sinks.rs` registration, and a fourth Allowed-dispatch arm in `evaluate_plan_node_and_record`.

Purpose: FS-02 requires the write/edit sink to actually modify an existing file, fail-closed on path schema, and be durably audited. This plan wires `write_within` (Plan 01) behind the `file.write` sink id (Plan 02's table rows) using the exact `file.create` dispatch + audit shape. `file.write` is terminal and mints nothing — the materially simpler half of the milestone's sink work.
Output: `invoke_file_write`, its module registration, and the dispatch arm in `crates/brokerd/src/server.rs`.

## Artifacts this plan produces
- `crates/brokerd/src/sinks/file_write.rs`: `invoke_file_write(conn, key, value_store, session_id, effect_id, plan_node, workspace_root, parent_id, parent_hash) -> Result<(Uuid, String)>` + private `resolve_arg`
- `crates/brokerd/src/sinks.rs`: `pub mod file_write;`
- `crates/brokerd/src/server.rs`: `plan_node.sink.0 == "file.write"` Allowed-dispatch arm
- inline test: `invoke_file_write` two-phase audit (success → sink_executed; failure → sink_execution_failed)
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@planning-docs/DESIGN-effect-breadth-exec.md
@.planning/phases/33-filesystem-read-write-breadth/33-RESEARCH.md
@.planning/phases/33-filesystem-read-write-breadth/33-PATTERNS.md
@.planning/phases/33-filesystem-read-write-breadth/33-01-SUMMARY.md
@.planning/phases/33-filesystem-read-write-breadth/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the file_write.rs sink module (invoke_file_write + private resolve_arg) and register it</name>
  <files>crates/brokerd/src/sinks/file_write.rs, crates/brokerd/src/sinks.rs</files>
  <read_first>
    - crates/brokerd/src/sinks/file_create.rs (invoke_file_create at 65-116 + its imports at 29-38 + its private resolve_arg — the verbatim template)
    - crates/brokerd/src/sinks/process_exec.rs (confirms the current per-module-private-helper convention)
    - crates/brokerd/src/sinks.rs (module declarations — add pub mod file_write beside pub mod process_exec)
    - crates/adapter-fs/src/workspace.rs::write_within (the primitive this sink calls — from Plan 01)
    - planning-docs/DESIGN-effect-breadth-exec.md §3.3 (two-phase durable audit + actor convention)
  </read_first>
  <action>
    Create `crates/brokerd/src/sinks/file_write.rs` as a verbatim copy of `invoke_file_create` (file_create.rs:65-116), with these renames only: function `invoke_file_create` → `invoke_file_write`; the sink call `workspace_root.create_exclusive_within(&path, contents.as_bytes())` → `workspace_root.write_within(&path, contents.as_bytes())`; the actor literal `sink:file.create:{effect_id}` → `sink:file.write:{effect_id}` (both the success and failure Event); the `.context(...)` strings referencing file.create → file.write. Keep the two-phase pattern EXACTLY: `Ok(())` branch appends a `"sink_executed"` Event (empty taint vec — the write carries no new taint); `Err(e)` branch appends a `"sink_execution_failed"` Event FIRST, then returns `Err(anyhow::Error::new(e).context("file.write write_within failed"))` — no retry. Chain each Event onto `parent_id`/`parent_hash` via `append_event(conn, key, &event, Some(parent_hash))`. Reuse the literal event-type strings `"sink_executed"`/`"sink_execution_failed"` (the actor field disambiguates which sink — per RESEARCH Open Question recommendation; do NOT invent new event-type strings).

    Copy file_create.rs's own private `resolve_arg(store, plan_node, name)` helper into file_write.rs (do NOT import a shared helper — each sink module keeps its own copy, matching the codebase convention). Copy the imports block from file_create.rs (anyhow Context/Result, chrono Utc, executor ValueStore, runtime_core Event/PlanNode, uuid Uuid, adapter_fs WorkspaceRoot, crate::audit::append_event); drop any ResolvedArg/confirmation import that file_create.rs only needs for its separate from-resolved variant.

    In `crates/brokerd/src/sinks.rs`, add `pub mod file_write;` beside `pub mod process_exec;`.

    Note: ENOENT on a missing target surfaces through `write_within`'s Err branch → `sink_execution_failed` → propagated Err. This is an OS-level sink failure, NOT an ExecutorDecision::Denied/Block — do not introduce any new DenyReason variant (mirror file.create's EEXIST handling shape, RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>cargo build -p brokerd</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/brokerd/src/sinks/file_write.rs` exists with `invoke_file_write` + a private `resolve_arg`; `sinks.rs` declares `pub mod file_write;`.
    - Behavior: the function's signature matches `invoke_file_create` exactly (same 9 params, same `Result<(Uuid, String)>` return); it calls `workspace_root.write_within`; actor is `sink:file.write:{effect_id}`; failure appends before propagating.
    - Test command: `cargo build -p brokerd` compiles clean.
    - Prohibition check: `./scripts/check-invariants.sh` exits 0 (Gate 1 no new EffectRequest; Gate 3 unchanged — file_write.rs contains no mint call).
  </acceptance_criteria>
  <done>invoke_file_write compiles and mirrors invoke_file_create's two-phase audit; the module is registered; check-invariants.sh green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the file.write Allowed-dispatch arm in evaluate_plan_node_and_record + two-phase audit test</name>
  <files>crates/brokerd/src/server.rs</files>
  <read_first>
    - crates/brokerd/src/server.rs (the file.create Allowed-dispatch arm at 877-897 — the exact template; the email.send arm at 908 and process.exec arm at 1049 for placement context; note the process.exec arm sets output_value_id but file.create does NOT)
    - crates/brokerd/src/sinks/file_create.rs test module (its in-memory rusqlite two-phase-audit test, to mirror for file_write)
    - .planning/phases/33-filesystem-read-write-breadth/33-PATTERNS.md ("new file.write Allowed-dispatch arm" — the exact excerpt)
  </read_first>
  <behavior>
    - invoke_file_write success path: given a resolvable path+contents pointing at an existing file in a temp WorkspaceRoot, the file is overwritten and a `sink_executed` Event is appended, chained onto the supplied parent_hash (verify_chain intact).
    - invoke_file_write failure path: given a missing target (ENOENT), a `sink_execution_failed` Event is appended FIRST and the function returns Err (no retry, no sink_executed).
  </behavior>
  <action>
    Add a fourth Allowed-dispatch arm in `evaluate_plan_node_and_record`, gated on `matches!(decision, ExecutorDecision::Allowed) && plan_node.sink.0 == "file.write"`, placed adjacent to the file.create arm (877-897). Mirror the file.create arm exactly: lock the conn mutex, call `crate::sinks::file_write::invoke_file_write(&locked, key, value_store, session_id, effect_id, plan_node, workspace_root, *last_event_id, last_event_hash)?`, then set `*last_event_id = sink_event_id; *last_event_hash = sink_hash;`. Do NOT touch `output_value_id` (it stays `None`, exactly as file.create's arm leaves it — file.write is terminal and mints no value). Arms are gated on disjoint `plan_node.sink.0` strings, so ordering does not affect behavior.

    Add an inline `#[cfg(test)]` two-phase-audit test for invoke_file_write mirroring file_create.rs's own test: build an in-memory rusqlite audit db, a temp WorkspaceRoot with a pre-created target file, mint path+contents into a ValueStore, call invoke_file_write, assert (success) a `sink_executed` Event exists and the file content changed; then a second case with a missing target asserts a `sink_execution_failed` Event and an Err return. These are macOS-compiling (in-process rusqlite `:memory:`), except the actual write behavior on the file which uses write_within's non-Linux stub — assert the audit-Event outcome on both platforms and the file-content change where the stub supports it.
  </action>
  <verify>
    <automated>cargo test -p brokerd file_write</automated>
  </verify>
  <acceptance_criteria>
    - Source: server.rs has a `plan_node.sink.0 == "file.write"` Allowed-arm identical in shape to the file.create arm, with no output_value_id assignment.
    - Behavior: the invoke_file_write test proves success→sink_executed (chained) and failure→sink_execution_failed-then-Err.
    - Test command: `cargo test -p brokerd file_write` passes on macOS.
  </acceptance_criteria>
  <done>The file.write dispatch arm is wired and the two-phase audit test passes on macOS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → broker | an Allowed file.write plan node drives a real broker-side write |
| broker → kernel | write_within's openat2 resolves the target beneath WorkspaceRoot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering | file.write writing outside WorkspaceRoot | high | mitigate | write_within (Plan 01) resolves beneath the dirfd anchor; args resolved from ValueStore only |
| T-33-09 | Repudiation | no durable trace of a write | medium | mitigate | two-phase sink_executed / sink_execution_failed audit chained onto parent_id/parent_hash, verify_chain intact |
| T-33-10 | Elevation of Privilege | a raw request-args-to-sink bypass of I2 | critical | mitigate | file.write is PlanNode{sink,args} from spawn; no new EffectRequest token; check-invariants.sh Gate 1 green |
| T-33-SC | Tampering (supply chain) | dependencies | low | accept | no new package installs this phase — brokerd deps unchanged |
</threat_model>

<verification>
- `cargo build -p brokerd` clean.
- `cargo test -p brokerd file_write` green on macOS.
- `./scripts/check-invariants.sh` exits 0 (Gate 1 no new EffectRequest; Gate 3 unchanged — file.write never mints).
- `git diff` confirms the file.write arm does not assign output_value_id.
</verification>

<success_criteria>
An Allowed file.write plan node modifies an existing WorkspaceRoot file via write_within and records a durable two-phase audit trail; the sink introduces no mint, no EffectRequest, no new decision variant, and no output_value_id threading.
</success_criteria>

<output>
Create `.planning/phases/33-filesystem-read-write-breadth/33-04-SUMMARY.md` when done.
</output>
