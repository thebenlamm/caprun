---
phase: 02-security-design-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - planning-docs/DESIGN-GATE-RECORD.md
autonomous: false
requirements:
  - REQ-design-taint-model
  - REQ-design-plan-executor
must_haves:
  truths:
    - "DESIGN-GATE-RECORD.md records explicit review/approval against a line-by-line checklist covering every required item in both DESIGN docs"
    - "DESIGN-GATE-RECORD.md records the sha256 of each reviewed DESIGN doc so the approval is pinned to exact content"
    - "DESIGN-GATE-RECORD.md is the documented unblock signal for crates/executor in Phase 4 (Decision: APPROVED; Gate status: UNBLOCKED set only after human review)"
    - "The checklist explicitly includes the genuine-taint requirement (taint originates from a read Event, never hand-set)"
  artifacts:
    - planning-docs/DESIGN-GATE-RECORD.md
  key_links:
    - "Each checklist line maps one-to-one to a REQ-design-* done-when criterion (the Phase Requirements -> Review Map in 02-RESEARCH.md)"
    - "Gate status UNBLOCKED is the signal Phase 4 checks before authoring any crates/executor file"
  prohibitions:
    - "No executor code is authored in this phase. No file under crates/executor/** is created or modified. The gate record's UNBLOCKED status authorizes Phase 4 to begin that work — it does not begin it here."
---

<objective>
Author `planning-docs/DESIGN-GATE-RECORD.md` — the concrete recorded gate artifact that satisfies ROADMAP Phase 2 Success Criterion 3 ("both docs are reviewed and approved"). It is a line-by-line checklist against both DESIGN docs, pinned to their sha256 hashes, with a human approval decision that unblocks `crates/executor` in Phase 4.

Purpose: Without a concrete record, "reviewed and approved" lives in a chat message and Phase 4 has nothing to check (research Pitfall 4). This file is the unambiguous unblock signal.
Output: `planning-docs/DESIGN-GATE-RECORD.md` with the checklist auto-pre-filled by grep, the doc hashes, and a human-set APPROVED / UNBLOCKED decision.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-design-gate/02-RESEARCH.md
@planning-docs/DESIGN-taint-model.md
@planning-docs/DESIGN-plan-executor.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author DESIGN-GATE-RECORD.md with the checklist and pinned doc hashes</name>
  <files>planning-docs/DESIGN-GATE-RECORD.md</files>
  <read_first>
    - .planning/phases/02-security-design-gate/02-RESEARCH.md — "What 'Reviewed and Approved' Means (The Gate Artifact)" (the exact checklist), "Phase Requirements -> Review Map", "Gate Condition", "Open Questions" 3 (separate file — RESOLVED: yes)
    - planning-docs/DESIGN-taint-model.md — the document under review (produced by Plan 02-01)
    - planning-docs/DESIGN-plan-executor.md — the document under review (produced by Plan 02-02)
  </read_first>
  <action>
    Write `planning-docs/DESIGN-GATE-RECORD.md` (RESOLVES open question 3: a separate file, not a section inside a DESIGN doc). Include:

    - A header: title "# DESIGN Gate Record", a Date field, a Reviewer field (Ben Lamm), and a Documents section listing planning-docs/DESIGN-taint-model.md and planning-docs/DESIGN-plan-executor.md each with its sha256. Compute the hashes with `shasum -a 256 planning-docs/DESIGN-taint-model.md planning-docs/DESIGN-plan-executor.md` and paste the values so the approval is pinned to exact content.
    - A "## Checklist" section with these nine items as markdown task-list checkboxes, each mapping to a REQ-design-* done-when criterion:
      1. DESIGN-taint-model.md explicitly states the dynamic-taint default
      2. DESIGN-taint-model.md explicitly states the hard planner/worker split for Tier 3+
      3. DESIGN-taint-model.md explicitly states the I0 draft-only rule for tainted-seed Sessions
      4. DESIGN-plan-executor.md specifies ValueNode (literal + provenance + taint)
      5. DESIGN-plan-executor.md specifies PlanNode (sink + args)
      6. DESIGN-plan-executor.md specifies the v0 hardcoded sink sensitivity map
      7. DESIGN-plan-executor.md specifies monotonic taint propagation through the plan DAG
      8. DESIGN-plan-executor.md specifies the literal-value confirmation UX
      9. Both docs acknowledge the genuine-taint requirement (taint originates from a read Event, never hand-set)
    - Pre-fill each checkbox by actually running the corresponding grep against the target doc (reuse the acceptance-criteria greps from Plan 02-01 and 02-02). Mark a box checked ONLY if its grep matches; if any grep fails, leave it unchecked and add a note naming the missing content — do NOT check a box whose evidence is absent.
    - A "## Decision" section containing the literal tokens "APPROVED / NEEDS REVISION" as the available choices, left undecided pending human review.
    - A "## Gate status" section stating crates/executor is "[ UNBLOCKED / BLOCKED ]", left as BLOCKED pending human approval, and a one-line statement that Phase 4 MUST NOT author any crates/executor file until this record shows Decision: APPROVED and Gate status: UNBLOCKED.

    Do NOT set the Decision to APPROVED or the Gate status to UNBLOCKED in this task — that is the human reviewer's action in Task 2. Do NOT create any code under crates/executor.
  </action>
  <acceptance_criteria>
    All grep/shell checks below MUST pass against planning-docs/DESIGN-GATE-RECORD.md:
    - File exists: `test -f planning-docs/DESIGN-GATE-RECORD.md`
    - Both docs referenced: `grep -q "DESIGN-taint-model.md" planning-docs/DESIGN-GATE-RECORD.md && grep -q "DESIGN-plan-executor.md" planning-docs/DESIGN-GATE-RECORD.md`
    - sha256 hashes pinned (two 64-hex digests present): `grep -cE "[0-9a-f]{64}" planning-docs/DESIGN-GATE-RECORD.md | grep -qE "^[2-9]|^[1-9][0-9]"`
    - Checklist present with all nine items (>=9 task-list checkboxes): `grep -cE "^- \[[ xX]\]" planning-docs/DESIGN-GATE-RECORD.md | grep -qE "^(9|[1-9][0-9])$"`
    - Genuine-taint requirement on the checklist: `grep -qi "read Event" planning-docs/DESIGN-GATE-RECORD.md && grep -qiE "hand-set|never hand-set" planning-docs/DESIGN-GATE-RECORD.md`
    - Decision tokens present: `grep -q "APPROVED" planning-docs/DESIGN-GATE-RECORD.md && grep -qi "NEEDS REVISION" planning-docs/DESIGN-GATE-RECORD.md`
    - Gate-status tokens present: `grep -q "UNBLOCKED" planning-docs/DESIGN-GATE-RECORD.md && grep -q "BLOCKED" planning-docs/DESIGN-GATE-RECORD.md`
    - crates/executor named as the gated target: `grep -q "crates/executor" planning-docs/DESIGN-GATE-RECORD.md`
    - No executor code authored: `! find crates/executor -type f 2>/dev/null | grep -q .`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review and approval of both DESIGN docs against the gate checklist</name>
  <action>Halt for human review. Present both DESIGN docs and the pre-filled gate record to the reviewer (Ben Lamm), walk the checklist and adversarial review per how-to-verify below, then have the reviewer set Decision and Gate status in planning-docs/DESIGN-GATE-RECORD.md. Do not proceed until a resume signal is given.</action>
  <what-built>
    Three documents now exist: planning-docs/DESIGN-taint-model.md, planning-docs/DESIGN-plan-executor.md, and planning-docs/DESIGN-GATE-RECORD.md (with the checklist auto-pre-filled by grep and the doc sha256 hashes pinned).
  </what-built>
  <how-to-verify>
    1. Open planning-docs/DESIGN-GATE-RECORD.md and confirm all nine checklist boxes are checked (every required item was found in the docs by grep). If any box is unchecked, the corresponding doc is incomplete — send it back to Plan 02-01 or 02-02 for revision; do NOT approve.
    2. Read planning-docs/DESIGN-taint-model.md end-to-end as an attacker (adversarial review, per 02-RESEARCH.md "Review Framework"): can you find a loophole in the stated invariants? Confirm the dynamic-taint default, the hard Tier 3+ split, the I0 draft-only rule, and the genuine-taint requirement are stated as hard MUST/MUST NOT predicates, not "should".
    3. Read planning-docs/DESIGN-plan-executor.md: confirm ValueNode (literal+provenance+taint), PlanNode (sink+args), the hardcoded sink sensitivity map, monotonic propagation, the literal-value confirmation prompt, and the taint-provenance requirement are all unambiguous enough to implement crates/executor without interpretation.
    4. Confirm the two sha256 hashes in the record match the current files: run `shasum -a 256 planning-docs/DESIGN-taint-model.md planning-docs/DESIGN-plan-executor.md` and compare.
    5. If satisfied: in planning-docs/DESIGN-GATE-RECORD.md set Decision to APPROVED and Gate status to UNBLOCKED, dated. This is the recorded signal that unblocks crates/executor in Phase 4. If not satisfied: set Decision to NEEDS REVISION, list the gaps, and the phase loops back.
  </how-to-verify>
  <resume-signal>Type "approved" once Decision: APPROVED and Gate status: UNBLOCKED are recorded, or describe the gaps for revision.</resume-signal>
</task>

</tasks>

<threat_model>
This plan produces the gate artifact itself; the central threat is the gate approving an incomplete or unsound spec.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | Gate approves an incomplete DESIGN spec | critical | mitigate | Checklist boxes are pre-filled by the same greps that gate Plans 02-01/02-02; a box may be checked only if its grep matches, and a blocking human review re-reads both docs adversarially before APPROVED is set. |
| T-02-07 | Repudiation | Approval not pinned to exact doc content | high | mitigate | The record stores the sha256 of each reviewed doc; the human verifies the hashes match the current files before approving, so a later silent doc edit invalidates the recorded approval. |
| T-02-08 | Elevation of Privilege | Phase 4 writes crates/executor before approval | critical | mitigate | Decision and Gate status default to NEEDS-REVISION/BLOCKED; only the blocking human checkpoint flips them to APPROVED/UNBLOCKED, which is the signal Phase 4 checks. |
</threat_model>

<verification>
- `planning-docs/DESIGN-GATE-RECORD.md` exists with nine checklist items, two pinned sha256 hashes, and Decision/Gate-status sections.
- All Task 1 acceptance-criteria checks pass.
- Human checkpoint (Task 2) completed: Decision: APPROVED and Gate status: UNBLOCKED recorded (or NEEDS REVISION with gaps listed and the phase loops).
- No file exists under `crates/executor/`.
</verification>

<success_criteria>
DESIGN-GATE-RECORD.md records explicit human review/approval against a nine-item checklist (covering both docs, including the genuine-taint requirement), pinned to the docs' sha256 hashes, and is the documented UNBLOCKED signal for crates/executor in Phase 4 — satisfying ROADMAP Phase 2 Success Criterion 3.
</success_criteria>

<output>
Create `.planning/phases/02-security-design-gate/02-03-SUMMARY.md` when done.
</output>
