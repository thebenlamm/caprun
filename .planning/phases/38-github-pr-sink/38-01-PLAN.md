---
phase: 38-github-pr-sink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor/src/sink_schema.rs
  - crates/executor/src/sink_sensitivity.rs
autonomous: true
requirements: [GITHUB-01, GITHUB-03]
must_haves:
  truths:
    - "github.pr is a registered sink whose exact arg set is {owner,repo,base,head,title,body}"
    - "github.pr is classified CommitIrreversible (never the fail-closed default only incidentally)"
    - "title/body are content-sensitive; owner/repo/base/head are routing-sensitive — a tainted value in any of the six Blocks under the UNMODIFIED collect-then-Block loop"
  artifacts:
    - "crates/executor/src/sink_schema.rs — github.pr KNOWN_SINKS row"
    - "crates/executor/src/sink_sensitivity.rs — github.pr sink_effect_class / routing / content / expected_role arms"
  key_links:
    - "KNOWN_SINKS github.pr row -> submit_plan_node Step 0 schema gate (unregistered/unknown-arg -> Deny)"
    - "is_content_sensitive(title|body)=true -> submit_plan_node collect-then-Block (tainted -> Block)"
---

<objective>
Add the executor-crate table rows that make `github.pr` a first-class sink: its
hardcoded arg schema, its `CommitIrreversible` effect-class, and its
routing/content sensitivity. These are TABLE ROWS ONLY — no new
`ExecutorDecision` variant, no new enforcement step, no policy file — exactly the
v1.5/v1.7 discipline (DESIGN §7). This is the foundation both dispatch paths
(Allowed in server.rs, confirm-release in confirmation.rs) build on.

Purpose: A tainted `title`/`body` (assembled from `http_response`/`ExecRaw`/
`doc_fragment` content) MUST Block under the unmodified collect-then-Block loop —
the marquee secret-exfil-via-PR-text defense (GITHUB-03) — and `github.pr` MUST
carry the correct `CommitIrreversible` class (GITHUB-01), never reaching a sink
via the fail-closed `_` default only.
Output: github.pr rows in `sink_schema.rs` + `sink_sensitivity.rs`, with unit
tests mirroring the existing `http.request`/`git.commit` rows.
</objective>

<execution_context>
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/AgentOS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-github-pr-sink/38-CONTEXT.md
@planning-docs/DESIGN-git-github-http-sinks.md
@crates/executor/src/sink_schema.rs
@crates/executor/src/sink_sensitivity.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the github.pr arg schema</name>
  <files>crates/executor/src/sink_schema.rs</files>
  <read_first>
    - crates/executor/src/sink_schema.rs — the `http.request` and `git.commit`
      KNOWN_SINKS entries (the exact-match, single-/multi-arg row shape) and their
      test blocks (registered / exact-args-ok / unknown-arg / duplicate-arg /
      missing-required).
  </read_first>
  <behavior>
    - schema_for("github.pr") is Some (never UnknownSink at Step 0).
    - A node with exactly {owner,repo,base,head,title,body} validates Ok.
    - An extra arg (e.g. "draft") -> UnknownArg; a repeated arg -> DuplicateArg;
      any of the six absent -> MissingArg.
  </behavior>
  <action>
    Add a `SinkSchema` entry to `KNOWN_SINKS` for `github.pr` per DESIGN §4.1/§4.4
    and CONTEXT decisions 1/4: `allowed` and `required` are BOTH exactly
    `["owner","repo","base","head","title","body"]` (exact-match, all six required
    — mirror file.write's exact-match shape, NOT process.exec's optional-arg
    asymmetry). No `draft`/`maintainer_can_modify`/`headers`/`method` args are
    modeled this milestone (GET/PR-create scope) — a plan node carrying any is
    `Denied(UnknownArg)` at Step 0. Add a doc comment (as the git.commit/http.request
    rows have) stating: title/body are content-sensitive and owner/repo/base/head
    routing-sensitive per sink_sensitivity.rs — a tainted value Blocks downstream,
    it does NOT Deny here; this schema gate enforces only the arg NAME set, not taint.
    Add tests mirroring the `http_request_*` schema tests: `github_pr_is_registered_sink`,
    `github_pr_exact_args_ok`, `github_pr_unknown_arg_denied`,
    `github_pr_duplicate_arg_denied`, `github_pr_missing_required_arg_denied`.
  </action>
  <verify>
    <automated>cargo test -p executor --lib sink_schema::tests::github_pr</automated>
  </verify>
  <done>github.pr registers with the exact six-arg schema; all five github_pr schema tests pass.</done>
  <acceptance_criteria>
    - `github.pr` is in KNOWN_SINKS with allowed==required=={owner,repo,base,head,title,body}.
    - No new arg names beyond the six; extras fail closed (UnknownArg).
    - `cargo test -p executor --lib` is green with the new github_pr schema tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: github.pr sensitivity + effect-class rows</name>
  <files>crates/executor/src/sink_sensitivity.rs</files>
  <read_first>
    - crates/executor/src/sink_sensitivity.rs — the `http.request` and `git.commit`
      arms of `sink_effect_class`, `is_routing_sensitive`, `is_content_sensitive`,
      `expected_role`, plus their per-sink `*_ROUTING_SENSITIVE`/`*_CONTENT_SENSITIVE`
      consts and test blocks. Note the `expected_role` None-rationale used by
      process.exec/git.commit/http.request.
  </read_first>
  <behavior>
    - sink_effect_class(github.pr) == CommitIrreversible (an explicit arm, not the
      `_` fail-closed default).
    - is_content_sensitive(github.pr, "title") and (…, "body") == true.
    - is_routing_sensitive(github.pr, "owner"/"repo"/"base"/"head") == true.
    - is_content_sensitive(github.pr, "owner"/"repo"/"base"/"head") == false
      (routing-only — no over-widening).
    - expected_role(github.pr, any of the six) == None (unconstrained at the
      structural Step-1c role gate — the Block comes from routing/content
      sensitivity + the untrusted-taint check, per the process.exec/git.commit rationale).
  </behavior>
  <action>
    Per DESIGN §4.1/§4.4/§8 and CONTEXT decisions 1/4: add
    `GITHUB_PR_ROUTING_SENSITIVE = &["owner","repo","base","head"]` and
    `GITHUB_PR_CONTENT_SENSITIVE = &["title","body"]` consts with doc comments
    (title/body are the payload that leaves the boundary — the marquee exfil arg;
    owner/repo/base/head determine WHERE the PR lands). Add a `"github.pr" =>
    EffectClass::CommitIrreversible` arm to `sink_effect_class` with a doc comment
    citing DESIGN §4.1 (a PR is an external, irreversible effect — a real explicit
    arm so a future refactor cannot silently drop it to the `_` default). Add
    `"github.pr"` arms to `is_routing_sensitive` and `is_content_sensitive`
    delegating to the two consts. Add a `"github.pr"` arm to `expected_role`
    returning `None` for all six args (reuse the process.exec/git.commit
    unconstrained-role rationale verbatim in the doc comment: no origin_role-
    producing mint site exists for a legitimately-authored PR field, so pinning
    Some(...) would fail-closed-Deny the legit flow; `None` disables ONLY the
    structural role gate and is NOT an I2 bypass). Add tests mirroring the
    http.request/git.commit sensitivity tests: `github_pr_is_commit_irreversible`,
    `github_pr_title_body_content_sensitive`, `github_pr_owner_repo_base_head_routing_sensitive`,
    `github_pr_routing_args_not_content_sensitive`, `github_pr_expected_role_is_none`.
  </action>
  <verify>
    <automated>cargo test -p executor --lib sink_sensitivity::tests::github_pr</automated>
  </verify>
  <done>github.pr is CommitIrreversible; title/body content-sensitive; owner/repo/base/head routing-sensitive; all six args role-unconstrained; tests pass.</done>
  <acceptance_criteria>
    - A tainted `title` or `body` will Block (is_content_sensitive true) under the
      unmodified collect-then-Block loop — verified indirectly here by the classifier
      tests, end-to-end in Plan 38-05.
    - No routing arg becomes content-sensitive (no over-widening).
    - `cargo test -p executor` fully green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker/planner -> broker (PlanNode) | untrusted `github.pr` args cross here; sensitivity classification decides Block vs Deny vs Allowed |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Information Disclosure | sink_sensitivity title/body | critical | mitigate | title/body content-sensitive -> tainted Block (marquee PR-text exfil, GITHUB-03/§4.4) |
| T-38-02 | Tampering | sink_sensitivity owner/repo/base/head | high | mitigate | routing-sensitive -> tainted Block (PR mis-routing) |
| T-38-03 | Elevation of Privilege | sink_effect_class default | medium | mitigate | explicit CommitIrreversible arm, not the `_` default — a reorder cannot silently relax it |
</threat_model>

<verification>
- `cargo test -p executor` green (all new github_pr schema + sensitivity tests).
- `./scripts/check-invariants.sh` still exits 0 (table-rows-only change; no mint token, no EffectRequest, no new enforcement step).
</verification>

<success_criteria>
github.pr is a registered CommitIrreversible sink with title/body content-sensitive
and owner/repo/base/head routing-sensitive, delivered as executor table rows only.
</success_criteria>

<output>
Create `.planning/phases/38-github-pr-sink/38-01-SUMMARY.md` when done.
</output>
