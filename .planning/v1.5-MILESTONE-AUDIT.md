---
milestone: v1.5
audited: 2026-07-12
status: passed
scores:
  requirements: 11/11
  phases: 3/3
  integration: 5/5
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 24-slot-type-binding-enforcement
    items:
      - "file.create `contents` slot is unconstrained AND not I2-sensitive (pre-existing Assumption A2 scope-out). NOT a T2 exfil vector — file.create has no egress. Revisit only if file.create ever gains egress."
      - "DESIGN §3 body expected-role amended from [\"body\"] to [\"body\",\"doc_fragment\"] during Phase 24 (no \"body\" claim_type exists; body arrives as doc_fragment). Sound & documented (DESIGN amended in-place, commit 92b9d6f) — recorded as a deviation, not a defect. body stays content-sensitive so I2 backstops it."
nyquist:
  compliant_phases: [23, 24, 25]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v1.5 — Slot-Type Binding Enforcement (T2) — Audit

**Status: PASSED.** All 11 requirements satisfied across all three sources
(phase VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS traceability), cross-phase
integration independently traced WIRED with no orphans or broken seams, and the end-to-end
enforcement flow proven live on real Linux.

## Definition of done (from ROADMAP)
The T2 gap — the executor not checking that a handle's semantic origin-role matches the semantic
role of the slot it is routed into — is closed and enforced in the Rust TCB, proven by a held-out
swapped-handle acceptance test with a genuine audit chain, a regression audit that no fixture
silently bypasses the check, and an independent green full-workspace regression on real Linux.

## Phase verifications (3/3 passed)

| Phase | Name | VERIFICATION status | Notes |
|-------|------|---------------------|-------|
| 23 | Slot-Type Binding Design Gate | passed | DESIGN doc cleared a fresh (non-self, Fable-5) adversarial review before any TCB code (design-gate discipline). |
| 24 | Slot-Type Binding Enforcement | passed | origin_role threaded through all mint sites; exhaustive SlotTypeMismatch; Step 1c fail-closed enforcement. gsd-verifier 4/4. |
| 25 | Regression & Live Proof | passed (re-verified) | Held-out test + 0-NEEDS-FIX audit + live-Linux green (309/0). Initial gaps_found was a sign-off recording lag (now closed); re-verify passed 4/4. |

## Requirements coverage (11/11 satisfied — 3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS `[x]` | Final |
|--------|-------|--------------|---------------------|--------------------|-------|
| DESIGN-07 | 23 | passed | listed (23-01/02) | ✓ | satisfied |
| DESIGN-08 | 23 | passed | listed | ✓ | satisfied |
| DESIGN-09 | 23 | passed | listed | ✓ | satisfied |
| DESIGN-10 | 23 | passed | listed | ✓ | satisfied |
| T2-02 | 24 | passed | 24-01 | ✓ | satisfied |
| T2-03 | 24 | passed | 24-03 | ✓ | satisfied |
| T2-04 | 24 | passed | 24-02 | ✓ | satisfied |
| T2-05 | 24 | passed | 24-03 | ✓ | satisfied |
| T2-06 | 25 | passed | 25-01 | ✓ | satisfied |
| T2-07 | 25 | passed | 25-02 | ✓ | satisfied |
| T2-08 | 25 | passed | 25-03 | ✓ | satisfied |

No orphaned requirements (every REQ-ID in the traceability table appears in a phase VERIFICATION).
No unsatisfied requirements → no FAIL-gate trip.

## Cross-phase integration (5/5 hops WIRED — traced in code, not from SUMMARYs)

Independent integration check traced every seam to file:line (no mocks, no stapling):

1. **DESIGN table (23) ↔ `expected_role()` impl (24)** — match. `to`/`cc`/`bcc` → `["recipient","email_address"]`, `subject` → `["subject"]`, `file.create path` → `["path","relative_path"]`. `body` amended to `["body","doc_fragment"]` (documented, sound — see tech debt).
2. **`origin_role` threading** — genuine end-to-end: set at 3 mint sites (`mint_from_read`/`mint_from_intent`/`mint_from_derivation`, `quarantine.rs`), carried on `ValueRecord.origin_role` (`value_record.rs:37`), read by Step 1c via `record.origin_role.as_deref()` on the SAME resolved record (`executor/src/lib.rs:131-146`). No re-derivation, no second field.
3. **`DenyReason::SlotTypeMismatch` exhaustive** — both `code()` and `Display::fmt` in `executor_decision.rs` match every variant explicitly; no `_ =>` catch-all swallows the new variant.
4. **Held-out test (25) exercises real enforcement** — `s9_acceptance.rs::slot_type_binding_swapped_subject_recipient_denies` drives the real `mint_from_intent` → `submit_plan_node` → Step 1c → `expected_role` path, asserts `Denied{SlotTypeMismatch}` + `verify_chain` true. Isolation control proves only the role-check can fire.
5. **E2E chain** — swapped handles → mint_from_intent tags → Step 1c → `Denied{SlotTypeMismatch}` → `plan_node_evaluated` audit event → `verify_chain` true. All 6 hops real code, no broken seam. Proven live on Linux (T2-08: 309 passed/0 failed).

Files traced: `DESIGN-slot-type-binding.md`, `sink_sensitivity.rs`, `executor/src/lib.rs`, `value_store.rs`, `value_record.rs`, `executor_decision.rs`, `quarantine.rs`, `server.rs`, `s9_acceptance.rs`.

## Nyquist coverage

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 23 | exists | true | none |
| 24 | exists | true | none |
| 25 | exists | true | none |

(The verify:post validate-phase hook is inactive this session; VALIDATION.md files nonetheless present and compliant.)

## Tech debt (non-blocking, accepted)

1. **file.create `contents` unconstrained** (pre-existing, Assumption A2 scope-out) — the file.create `contents` arg carries no expected-role and is not I2-sensitive. NOT a T2 exfil vector because file.create has no egress. Revisit only if file.create ever gains an egress path.
2. **body expected-role amendment** — Phase 24 corrected the DESIGN's `["body"]` to `["body","doc_fragment"]` because no `"body"` claim_type exists (body content arrives as `doc_fragment`). Documented deviation, DESIGN amended in-place; recorded for traceability, not a defect. Recipient exfil slots (to/cc/bcc) were untouched and still reject doc_fragment.

## Verdict

v1.5 achieved its definition of done. The T2 slot-type-binding gap — v1.4's accepted residual — is
now enforced in the TCB and proven closed on real Linux. Ready to complete/archive.
